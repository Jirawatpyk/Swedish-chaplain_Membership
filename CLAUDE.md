# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Project**: **Chamber-OS** — a SaaS membership management platform for chambers of commerce and membership organisations. **SweCham / TSCC (Thailand-Swedish Chamber of Commerce)** is the first tenant, deployed at `swecham.zyncdata.app`. The platform is designed as **Multi-Tenant Aware, Single-Tenant Deployed (MTA+STD)** — F1 shipped single-tenant; F2 onwards use `tenant_id`-scoped schemas so future tenants can be onboarded without schema migration. See `docs/saas-architecture.md` for the multi-tenant strategy.

**Scope**: Chamber-OS is the **membership management backend + admin portal + member self-service portal**. It is NOT a public website / CMS — tenants are expected to have their own public sites.

**Folder name caveat**: the directory is historically `Swedish chaplain_membership`. "chaplain" is a typo for "chamber". Refer to the product as **Chamber-OS** (platform) or **SweCham / TSCC** (first tenant), never "chaplain". Rename is tracked as R6 in `docs/phases-plan.md` (manual action — cannot be done from inside the active working directory).

**Repository status (as of 2026-04-21)**: **F1 (Auth & RBAC) SHIPPED via PR #1.** **F2 (Membership Plans) REVIEW-READY on `002-membership-plans`.** **F3 (Members & Contacts) REVIEW-READY on `005-members-contacts`.** **Branch `006-layout-container-tier2` REVIEW-READY on PR #9** (layout primitives, not canonical F5). **F4 (Invoices & Receipts) REVIEW-READY on `007-invoices-receipts`** — entire Phase 10 complete: auto-email + manual resend (T105–T108), dispatcher + F4 dual-emit (T106 + vercel.json crons), overdue derivation wired to 4 UIs (T109), audit behavioral coverage **17/18 F4 event types** (T113a — only post-MVP Blob-outage auto-rerender deferred), perf benchmarks (T110 PDF render **p95=88ms < 800ms**; T110a invoice-list **p95=324ms < 500ms @ 5k×2 rows**; T111 50-writer seq **~10s < 30s**), T112 retention + archive invariant (FR-029/030), and the full 10g staff-review carry-forward batch (T120 host-header MTA dual-bind migration 0031, T121 CR/LF strip helper, T122 pdf_render_failed emit, T123 VAT source-chain pin, T125 un-fixme mutating CN E2E, T126 `renderAndUploadPdf` helper refactoring 4 sites, T127 CN-PDF synthetic-line golden). Test suite state: **~1300 unit+contract green** + **~340 integration green** on live Neon Singapore + **~1190 i18n keys × 3 locales**. Security/UX/a11y checklists 100%. Constitution v1.4.0 Principle I tenant-isolation test (Review-Gate blocker) green across F3+F4. SC-003 byte-identical PDF, SC-005 list-perf, and SC-002 (F3) all met with headroom. Human-gated residuals: T114 manual SR + cross-browser + staging traces + reduced-motion, T117 maintainer co-sign on security checklist, T118 ≥6 `/speckit.review` + ≥2 `/speckit.staff-review` rounds, T124 a11y SR QA (folds into T114), T115t throwaway-tenant E2E infra (deferred to Phase 10+ per tasks.md rationale). Source modules: `src/modules/auth/**` (F1) + `src/modules/tenants/**` + `src/modules/plans/**` (F2) + `src/modules/members/**` (F3) + `src/modules/invoicing/**` (F4) + presentation surfaces in `src/app/(staff)/admin/**` + `src/app/(member)/portal/**` + `src/components/layout/**` (006).

## Language for AI sessions

User prefers **Thai** for conversational turns. Code, specs, commit messages, and technical docs remain in **English** (for international collaborators and long-term stability).

## Governance — read before proposing architecture changes

- `.specify/memory/constitution.md` — **v1.4.0** (amended 2026-04-11; v1.4.0 MINOR adds explicit SaaS tenant-isolation requirements under Principle I with 5 sub-clauses: app-layer, db-layer, integration test, audit, super-admin), authoritative. 10 principles (4 NON-NEGOTIABLE: Data Privacy & Security, Test-First, Clean Architecture, PCI DSS) plus 6 Core (i18n, Inclusive UX, Perf & Observability, Reliability, Code Quality, Simplicity). Principle I now requires **two-layer tenant isolation** (application + database) with a mandatory cross-tenant integration test as a Review-Gate blocker. Principle III requires every `src/modules/*` module to ship a public barrel + ESLint `no-restricted-imports` rule. Principle IX + Gate 9 + § Governance + § Development Workflow Additional rules carry a **solo-maintainer substitute** clause for the default ≥2-reviewers + no-direct-push rules, applicable when no second human reviewer is available. Amendments go through a PR with ≥2 maintainer approvals (or the solo-maintainer substitute) + a Sync Impact Report.
- Every `/speckit.plan` runs a **Constitution Check** against all 10 principles. Any deviation lives in `plan.md` § Complexity Tracking with a rejected simpler alternative — unjustified violations block the gate.
- The escape clause that allows Singapore hosting (Constitution § Compliance: Hosting & Residency) is used by F1. See `specs/001-auth-rbac/plan.md` Complexity Tracking.

## Key project docs (read in this order for context)

1. `.specify/memory/constitution.md` — principles and quality gates (v1.4.0, includes SaaS tenant-isolation requirements under Principle I)
2. `docs/phases-plan.md` — **10 core + 4 SaaS = 14 features** across 5 phases. Includes 6 resolved decisions (SV+EN+TH locales, TH-primary hosting, Stripe, 3 roles, no day-1 Excel migration, folder rename) + 2026-04-11 SaaS pivot update + scope boundary vs `swecham.com`.
3. `docs/saas-architecture.md` — **multi-tenant strategy (MTA+STD)**, Postgres RLS, auth model (cross-tenant users, tenant-scoped membership), billing layers, white-label scope, migration path, pricing vision. Read this before designing any F2+ feature.
4. `docs/membership-benefits-analysis.md` — authoritative 2026 Membership Package tier data from the PDF (6 corporate + 3 partnership tiers, full benefit matrix, data model, Q1–Q5 for F2 clarify). **Supersedes the deleted `docs/database-analysis.md`** which was Excel-derived and inaccurate.
5. `docs/event-integration-analysis.md` — F6 EventCreate integration strategy (Zapier webhook → attendee import → benefit quota). No CRUD event management — we use EventCreate externally.
6. `docs/email-broadcast-analysis.md` — F7 Email Broadcast / E-Blast system (paid benefit delivery via Resend Broadcasts API). New feature added to fill the critical gap for E-Blast quota delivery.
7. `docs/smart-chamber-features.md` — **21 smart chamber features catalogued** (6 in MVP: benefit dashboard, at-risk detection, smart renewal, command palette, inline+bulk, timeline; 15 post-MVP: undo, NL search, saved filters, CSV import, realtime, engagement score, auto-upgrade suggestions, activity feed, compliance tracker, proactive alerts, public directory widget, GDPR export).
8. `docs/ux-standards.md` — enterprise UX playbook (shimmer skeletons, toasts, confirmation dialogs, idle warning, theming, keyboard & focus management). F1 auth screens MUST pass the § 15 checklist before merge.
9. `docs/observability.md` — metrics, SLOs, alerts, log schema
10. `specs/001-auth-rbac/` — F1 feature bundle (shipped via PR #1)

**Note**: `docs/database-analysis.md` was **deleted 2026-04-11** — it was Excel-derived and known to be inaccurate after the 2026 Membership Package PDF was provided. The reusable analyzer script lives at `.specify/scripts/analyze_excel.py`. Git history preserves the old content.

## Current feature: F1 — Auth & RBAC (`001-auth-rbac`)

- `specs/001-auth-rbac/spec.md` — user stories (P1/P2/P3), acceptance scenarios, measurable success criteria, FRs
- `specs/001-auth-rbac/plan.md` — architecture, tech stack, Constitution Check, source tree
- `specs/001-auth-rbac/research.md` — resolved implementation choices + rationale for each
- `specs/001-auth-rbac/data-model.md` — entities, state machines, SQL schema, append-only audit grants
- `specs/001-auth-rbac/contracts/auth-api.md` — REST endpoint contracts
- `specs/001-auth-rbac/security.md` — 16-threat model (T-01 credential stuffing … T-16 argon2 DoS) mapped to mitigations and tests; security reviewer MUST sign § 5 checklist
- `specs/001-auth-rbac/tasks.md` — TDD-ordered task list for `/speckit.implement`
- `specs/001-auth-rbac/quickstart.md` — developer onboarding + local dev setup (Vercel link, Neon/Upstash/Resend provisioning, Docker test DB, Playwright)

**F1 shape in one paragraph**: three roles (`admin`, `manager` read-only on finance, `member` self-service), two portals (`/admin` for staff, `/portal` for members, plus shared `/forgot-password` and `/invite/[token]`), email+password only, invitation-based account creation, custom session-based auth (Lucia v3 guide pattern) with 30 min idle / 12 h absolute TTL, 16-event append-only audit trail, SV+EN+TH from day one, WCAG 2.1 AA, PDPA+GDPR dual compliance, placeholder landing page on `/portal` until F3 adds real member content. F1 deploys to Vercel `sin1` + Neon `ap-southeast-1` (see § Hosting deviation below).

## Locked-in tech stack (F1 onwards)

- **Framework**: Next.js 16 App Router + Cache Components + Turbopack; React 19
- **Language**: TypeScript 5.7+ strict (`strict: true`, `noUncheckedIndexedAccess: true`); Node 22 LTS
- **Auth**: custom session-based (Lucia v3 guide pattern), argon2id via `@node-rs/argon2`
- **Storage**: Neon Postgres + Drizzle ORM; Upstash Redis for rate limiting — **both Singapore region**
- **UI**: shadcn/ui + Tailwind CSS v4 + lucide-react + Radix primitives; `next-themes` for light/dark; `sonner` for toasts
- **i18n**: next-intl — **EN default + TH + SV**. Missing EN key fails the build; missing TH/SV falls back to EN with a dev warning and CI failure on release branches. **TH is mandatory for Thai tax-compliant invoices/receipts** (F4).
- **Forms**: react-hook-form + zod (zod also validates every system boundary and `process.env` via `src/lib/env.ts`)
- **Email**: Resend (transactional), `@react-email/components` for templates
- **Payments**: Stripe — **planned for F5, no code yet**. Stripe Elements / Payment Intents to preserve SAQ-A.
- **Testing**: Vitest + Playwright + `@axe-core/playwright` (WCAG 2.1 AA) + MSW + `@testing-library/react`
- **Observability**: `pino` JSON logs + `@vercel/otel` traces + Vercel Analytics / Speed Insights
- **Hosting**: Vercel (`sin1` Singapore) — documented deviation from "Thailand primary" (no major cloud has a TH region)

## Planned source layout (F1)

```text
src/
├── app/                               # Presentation (Next.js routes, server actions, middleware)
│   ├── (staff)/                       # Route group: admin + manager portal → /admin/**
│   ├── (member)/                      # Route group: member self-service → /portal/**
│   ├── (auth-public)/                 # Shared: /forgot-password, /reset-password/[token], /invite/[token]
│   ├── api/auth/**                    # route.ts handlers for every auth endpoint
│   └── middleware.ts                  # session lookup + route guards + CSRF Origin allow-list + HSTS
├── modules/<context>/                 # Bounded contexts (auth, then members, invoices, …)
│   ├── domain/                        # Pure types + policies — NO framework imports
│   ├── application/                   # Use cases — NO drizzle/next/react imports
│   └── infrastructure/                # DB repos, email client, hasher, rate-limit adapter
├── components/
│   ├── ui/                            # shadcn/ui primitives (skeleton extended with shimmer per ux-standards § 2.1)
│   ├── auth/                          # sign-in-form, reset-password-form, idle-warning-dialog, …
│   ├── shell/                         # user-menu, theme-toggle, skip-to-content, empty-state, error-state
│   └── layout/                        # staff-shell, member-shell
├── i18n/
│   ├── config.ts, request.ts          # next-intl
│   └── messages/{en,th,sv}.json       # en.json is canonical
├── lib/
│   ├── db.ts                          # Drizzle client singleton
│   ├── logger.ts                      # pino structured logger (forbidden fields: password, session id, tokens, Authorization)
│   ├── otel.ts                        # @vercel/otel setup
│   ├── env.ts                         # zod-validated process.env, runs at boot
│   └── result.ts                      # Result<T,E> helper for explicit error handling
└── middleware.ts

drizzle/migrations/                    # SQL migrations from drizzle-kit
tests/
├── contract/                          # One file per auth API endpoint
├── integration/auth/                  # Real Postgres (Docker), use cases end-to-end
├── unit/auth/                         # Domain + pure logic
└── e2e/                               # Playwright + axe-core; includes i18n coverage and reduced-motion specs
scripts/
├── seed-bootstrap-admin.ts            # One-off: first admin account (refuses if any admin exists)
└── check-i18n-coverage.ts             # CI: every auth key present in every locale
specs/<nnn-feature>/                   # Spec Kit artefacts — one dir per feature branch
```

**Cross-context imports MUST go through a module's public interface** — do not reach into a sibling's `domain/` or `application/`. Violations are blocked by ESLint `no-restricted-imports`.

## Clean Architecture enforcement (Principle III, NON-NEGOTIABLE)

- **Domain**: zero imports from `next`, `drizzle-orm`, `resend`, `@upstash/*`, `react`. Enforced by an ESLint `no-restricted-imports` rule scoped to `src/modules/*/domain/**`.
- **Application**: orchestrates Domain via its own port interfaces. No ORM, HTTP, framework, or React imports.
- **Infrastructure**: implements Application ports; Drizzle-inferred types live here and MUST NOT leak into Application or Domain.
- **Presentation**: calls Application use cases only; never touches Domain or Infrastructure directly.
- A deviation requires a line in `plan.md` § Complexity Tracking with the rejected simpler alternative.

## Commands

All commands below are current F1 commands from `package.json` (committed). **Use `pnpm`, not `npm`** — the lockfile is `pnpm-lock.yaml`. Dev and start both run on **port 3100** (port 3000 is reserved for other local Express projects on the primary dev workstation). Integration tests hit **live Neon Singapore** using `.env.local`, not a Docker container — the quickstart Docker note is historical.

Daily dev:

```bash
pnpm install
pnpm dev                       # Next.js dev with Turbopack on :3100
pnpm lint                      # ESLint; errors block merge
pnpm typecheck                 # tsc --noEmit under strict
pnpm test                      # Vitest run  (watch: pnpm test:watch)
pnpm test:coverage             # with Vitest coverage thresholds
pnpm test:integration          # against live Neon Singapore via DATABASE_URL from .env.local
pnpm test:e2e                  # Playwright, all suites
pnpm test:e2e --grep "@a11y"   # axe-core WCAG 2.1 AA scan only
pnpm test:e2e --grep "@i18n"   # locale coverage only
pnpm check:i18n                # fails on missing EN keys; warns (CI-blocks on release) on TH/SV
pnpm drizzle-kit generate      # generate migration from Drizzle schema
pnpm drizzle-kit migrate       # apply to $DATABASE_URL
pnpm build                     # production build
```

Full CI pipeline — reproduce locally before pushing:

```bash
pnpm lint && pnpm typecheck && pnpm test:coverage && pnpm check:i18n && pnpm check:layout && pnpm test:integration && pnpm test:e2e
```

Coverage thresholds (enforced in `vitest.config.ts`): Domain 100% line; Application 80% line + 80% branch; **100% branch on security-critical use cases** (sign-in, change-password, reset-password, role policy, sign-out).

One-off: bootstrap the first admin (safe to re-run — refuses if any admin exists):

```bash
BOOTSTRAP_ADMIN_EMAIL=first.admin@swecham.example pnpm tsx scripts/seed-bootstrap-admin.ts
```

Vercel workflow:

```bash
vercel link                         # link to swecham team
vercel env pull .env.local          # refresh local env from Vercel
vercel logs <deployment-url>        # function logs
vercel promote <old-deployment-url> # rollback production
```

Emergency write freeze: set `READ_ONLY_MODE=true` in Vercel env + redeploy — returns 503 `read-only-mode` on all state-changing `/api/**` routes while keeping sign-in and reads alive. Reversible in ~30 seconds without a code deploy. See quickstart § 7.3.

## Spec Kit workflow — the current primary daily workflow

All feature work flows through the 10 gates (Constitution § Development Workflow & Quality Gates):

`/speckit.specify` → `/speckit.clarify` → `/speckit.plan` → `/speckit.checklist` → `/speckit.tasks` → `/speckit.analyze` → `/speckit.implement` → `/speckit.verify` → `/speckit.review` → `/speckit.ship`

Each gate blocks the next until its exit criteria pass. Skipping requires a `plan.md` Complexity Tracking entry **and** ≥2 maintainer approvals. Auth, RBAC, payment, PII, audit log, and GDPR surfaces require **≥2 reviewers** at the Review gate, one of whom signs the security checklist (`security.md § 5` for F1).

Use `[Spec Kit]` prefix on commits that move a feature through a gate (`[Spec Kit] F1 spec + clarify`, `[Spec Kit] F1 tasks + close /speckit.analyze findings`, etc.). Conventional Commits are enforced by the commit-msg hook.

## Testing discipline (Principle II, NON-NEGOTIABLE)

- **TDD**: failing test → commit red → implement → commit green. Every user story in a spec MUST have ≥1 acceptance test authored before implementation starts.
- **Contract tests** at every external and inter-module boundary (`tests/contract/`).
- **Integration tests hit real Postgres** (Docker container on port 55432), not mocks — catches SQL, migration, and transaction bugs that mocks hide.
- A red test suite on `main` is a stop-the-line event — no new work until green.

## Conventions

- **Package manager**: pnpm (NOT npm). Lockfile `pnpm-lock.yaml`.
- **Commits**: Conventional Commits, enforced by commit-msg hook. Use `[Spec Kit]` prefix for Spec Kit workflow commits.
- **Branches**: one feature per branch (`nnn-feature-name`); spec directory name matches exactly (e.g. `001-auth-rbac`).
- **PR review**: ≥1 reviewer normal, **≥2 for security-sensitive** (auth, RBAC, payment, PII, audit log, GDPR surfaces). One of the two signs the security checklist.
- **Timestamps**: always store **ISO 8601 UTC (Gregorian)**. Thai Buddhist Era (BE = CE + 543) is **display-only** for `th-TH` user-facing surfaces. Mixing BE into storage is a ship blocker (off-by-543-years class).
- **Primary currency THB**; SEK/EUR/USD presentable where applicable. Thai tax invoices need VAT 7%, tax IDs on both parties, TH language, sequential tax-receipt numbering (F4 surface).

## Hosting deviation (documented in F1 plan § Complexity Tracking)

- Constitution says **Thailand primary**. F1 uses **Vercel `sin1` (Singapore) + Neon `ap-southeast-1` + Upstash Singapore** because no major cloud (AWS/GCP/Azure/Vercel) has a TH region. Nearest ≈25 ms from Bangkok.
- Thailand PDPA Section 28 cross-border provisions cover SG transfers. Swedish/EU member data subjects are covered by GDPR **standard contractual clauses (SCCs)** with Vercel and Neon.
- Revisit only if scale, regulation, or legal counsel demands true in-country residency — do not silently move to a Thai-local provider without amending this deviation.

## Secrets & confidential data

- **NEVER commit** `docs/*.xlsm` / `docs/*.xlsx` — the Excel workbooks contain SweCham member PII (~131 members / 164 contacts). Blocked by `.gitignore`; a leak triggers rotation + postmortem.
- Secrets live in **Vercel env vars only** (never `.env` in git), validated at boot by `src/lib/env.ts` (zod schema) — the app refuses to start with a missing/invalid env var.
- **Forbidden in logs**: plaintext passwords, session IDs, reset tokens, invitation tokens, `Authorization` headers, raw email bodies. CI lint rule blocks common mistakes. Hash user IDs in logs where cross-request correlation is needed.

<!-- The block below is regenerated by `.specify/scripts/powershell/update-agent-context.ps1`. -->
<!-- Do not hand-edit `## Active Technologies` or `## Recent Changes` — add durable guidance above this marker instead. -->

## Active Technologies
- TypeScript 5.7+ (strict mode, `strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`) (001-auth-rbac, 002-membership-plans)
- `cmdk` — headless command palette primitive (F2, powers smart-chamber feature #4)
- `src/modules/tenants/` — cross-cutting Domain-only module hosting `TenantContext` branded type (F2+)
- `runInTenant(ctx, fn)` + Postgres RLS (`SET LOCAL app.current_tenant`) — tenant isolation pattern (F2+)
- `DEBUG_RLS_STATE` — dev-only assertion for tenant-context verification (F2+)
- 10 new audit event types: `plan_created`, `plan_updated`, `plan_cloned`, `plan_activated`, `plan_deactivated`, `plan_soft_deleted`, `plan_undeleted`, `plan_not_found`, `plan_cross_tenant_probe`, `fee_config_updated` (F2)
- TypeScript 5.7+ (strict mode) / Node 22 LTS + Next.js 16 App Router, React 19, shadcn/ui (Sidebar, Sheet, Tooltip), Tailwind CSS v4, lucide-react, next-intl, next-themes (003-nav-menu)
- N/A — no database changes. Client-side localStorage for collapse preference. (003-nav-menu)
- TypeScript 5.7+ (strict mode, `strict: true`, `noUncheckedIndexedAccess: true`) — unchanged from F1 (004-page-layout-standard)
- N/A — no database changes. Client-side `localStorage` for sidebar collapse (existing from F3). (004-page-layout-standard)
- TypeScript 5.7+ strict (`strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`) — unchanged from F1+F2 (005-members-contacts)
- `@tanstack/react-table@^8` — TanStack Table v8 for members directory (server-side pagination, sort, filter) (005-members-contacts)
- `i18n-iso-countries@^7` — ISO 3166-1 country code lookup on member create/edit form (005-members-contacts)
- `src/modules/members/` — F3 bounded context: Domain (MemberEntity, ContactEntity, value objects), Application (12 use-cases), Infrastructure (DrizzleMembersRepository, DrizzleContactsRepository) (005-members-contacts)
- 2 new DB tables `members` + `contacts` with pg_trgm GIN index, RLS+FORCE policies, `last_activity_at` SECURITY DEFINER trigger; migrations 0009 + 0010 (005-members-contacts)
- 23 new F3 audit event types: `member_created`, `member_updated`, `member_archived`, `member_undeleted`, `contact_added`, `contact_promoted`, `contact_removed`, `member_plan_changed`, `member_plan_override`, `member_status_changed`, `member_self_update`, `member_portal_view`, `member_email_change_requested`, `member_email_change_confirmed`, `member_email_change_reverted`, `member_invitation_sent`, `member_invitation_accepted`, `member_invitation_revoked`, `member_invitation_expired`, `member_session_invalidated`, `member_cross_tenant_probe`, `member_bulk_archive`, `member_bulk_status_change` (005-members-contacts)
- Smart chamber features shipped in F3: timeline (event feed), inline+bulk edit, at-risk detection logic (last_activity_at), archive+undelete with session/invitation cascade (005-members-contacts)
- WCAG 2.2 SC 2.4.11 (Focus Not Obscured) + SC 2.5.8 (Target Size ≥24×24px) opportunistic adoption via E2E assertions (005-members-contacts)
- TypeScript 5.7+ strict (existing F1–F4 config; no change) + Next.js 16 App Router, React 19, Tailwind CSS v4, shadcn/ui (no new deps) (006-layout-container-tier2)
- N/A (presentation-only) (006-layout-container-tier2)
- TypeScript 5.7+ strict (`strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`) — unchanged from F1+F2+F3 (007-invoices-receipts)
- `@react-pdf/renderer@4.3.0` (exact-pin) — deterministic bilingual PDF engine for Thai-tax documents (FR-016 / SC-003) (007-invoices-receipts)
- `@js-joda/core@^6` + `@js-joda/timezone@^2` — correct Asia/Bangkok fiscal-year boundary for sequential-number allocator (007-invoices-receipts)
- `thai-baht-text@^2` — Thai amount-in-words on invoices/receipts (007-invoices-receipts)
- `sharp@^0.34` — server-side logo re-encode (EXIF strip, MIME/dimension enforce) for tenant-invoice-settings (FR-034) (007-invoices-receipts)
- `fast-check@^4` (dev) — property-based testing for credit-note VAT sum invariant (007-invoices-receipts)
- Sarabun TTF (OFL) committed under `public/fonts/sarabun/` — 400/500/700 weights embedded into PDFs at build time (007-invoices-receipts)
- `src/modules/invoicing/` — F4 bounded context: Domain (Invoice, CreditNote, TenantInvoiceSettings, SequentialNumberAllocator), Application (use-cases), Infrastructure (react-pdf adapter, Vercel Blob adapter, Drizzle repos) (007-invoices-receipts)
- 5 new DB tables: `invoices`, `invoice_lines`, `credit_notes`, `tenant_invoice_settings`, `tenant_document_sequences`; 16 new audit event types; Postgres advisory lock per (tenant_id, document_type, fiscal_year) for §87 no-gaps numbering (007-invoices-receipts)
- `FEATURE_F4_INVOICING` kill-switch + `BLOB_READ_WRITE_TOKEN` + `CRON_SECRET` env vars (007-invoices-receipts)
- TypeScript 5.7+ strict (`strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`) — unchanged from F1+F2+F3+F4 (009-online-payment)
- `stripe@^22` (server SDK, exact-minor pin) + `@stripe/stripe-js@^9` + `@stripe/react-stripe-js@^6` (client Elements) — Stripe Payment Intents + PromptPay QR + webhook signature verification (009-online-payment)
- `STRIPE_API_VERSION` env var pinning (FR-026 / Q5) + `STRIPE_SECRET_KEY` + `STRIPE_PUBLISHABLE_KEY` + `STRIPE_WEBHOOK_SECRET` + `FEATURE_F5_ONLINE_PAYMENT` kill-switch (009-online-payment)
- `src/modules/payments/` — F5 bounded context: Domain (Payment, Refund, TenantPaymentSettings, ProcessorEvent aggregates), Application (initiate-payment, confirm-payment, fail-payment, cancel-payment, issue-refund, process-webhook-event, process-charge-refunded, sweep-stale-pending-refunds, list-succeeded-payment-methods, load-invoice-payment-activity), Infrastructure (Stripe gateway, drizzle repos, audit emitter) (009-online-payment)
- 4 new DB tables: `payments`, `refunds`, `tenant_payment_settings`, `processor_events`; 17 new audit event types (`payment_initiated`, `payment_succeeded`, `payment_failed`, `payment_canceled`, `payment_method_switched`, `payment_auto_refunded_stale_invoice`, `payment_auto_refunded_concurrent_manual_mark`, `payment_environment_mismatch`, `payment_cross_tenant_probe`, `out_of_band_refund_detected`, `webhook_signature_rejected`, `webhook_api_version_mismatch`, `tenant_payment_settings_updated`, `online_payment_toggled`, `refund_initiated`, `refund_succeeded`, `refund_failed`); migrations 0033–0050 (009-online-payment)
- `audit_log.retention_years SMALLINT NOT NULL DEFAULT 5` + CHECK `retention_years IN (5,10)` + F4 tax-document backfill (migration 0039) — 6 F4 event types backfilled to 10-year retention per Thai RD §87/3 + GDPR Art. 6(1)(c) (009-online-payment, R2-E4 Review-Gate blocker)
- Concurrent-initiate guard (three-layer): (1) partial unique index `payments_processor_payment_intent_id_uniq`; (2) tenant-filtered `SELECT … FOR UPDATE` on `payments(processor_payment_intent_id)` (CR-2 review 2026-04-27 explicit tenantId filter); (3) `pg_advisory_xact_lock(hashtextextended('payments:'||tenantId||':'||invoiceId, 0))` per-(tenant, invoice) added at staff-review R2 to close TOCTOU window before the FOR UPDATE row probe (auto-released at tx-end; namespace `payments:` is disjoint from F4 `invoicing:` so no contention with §87 numbering locks). Plus Stripe idempotency key `inv-{invoiceId}-attempt-{n}`. F5 advisory lock has different semantics from F4 (TOCTOU guard, NOT gap-free numbering). (009-online-payment)
- External cron-job.org HTTP trigger at `/api/internal/metrics/stale-pending-count` (5-min cadence, Bearer auth) — Vercel Hobby-plan-compatible alternative to native cron for `payments.stale_pending_count` gauge (009-online-payment)
- F7 Email Broadcast (`010-email-broadcast`, shipped via PR #23) — full provenance in `specs/010-email-broadcast/`. Key tech additions:
  - **Stack**: Tiptap@3.22.5 (pin) + isomorphic-dompurify@2.36.0 (pin) + email-validator@^2 + @tanstack/react-virtual@^3
  - **Resend Broadcasts API surface** — separate from F1+F4 transactional (separate suppression list + webhook endpoint + reputation pool). Webhook pinned to Node runtime for Svix HMAC raw-body verify.
  - **Env**: `RESEND_BROADCASTS_API_KEY` + `RESEND_BROADCASTS_WEBHOOK_SECRET` + `UNSUBSCRIBE_TOKEN_SECRET` (≥32 bytes; distinct from `AUTH_COOKIE_SIGNING_SECRET`) + `BROADCASTS_FROM_EMAIL` + `FEATURE_F7_BROADCASTS` (default false; ships dark) + optional `TENANT_PRIVACY_POLICY_URL` + `TENANT_WEBSITE_URL`
  - **Bounded context**: `src/modules/broadcasts/` — 4 aggregates (Broadcast / BroadcastDelivery / MarketingUnsubscribe / RecipientSegment) + ~20 use-cases.
  - **DB**: 4 new tables + 2 columns on F3 `members` + 22 migrations (0064–0085) + 43 audit event types (5y retention default).
  - **Concurrency**: per-(tenant, broadcast) `pg_advisory_xact_lock('broadcasts:'+tid+':'+bid)` — namespace disjoint from F4 `invoicing:` and F5 `payments:`.
  - **Sanitiser allowlist** (FR-002a): `p`/`br`/`strong`/`em`/`u`/`a[href]`/`ul`/`ol`/`li`/`h1-h4`/`blockquote`/`hr`. **No `<img>`** (tracking-pixel — F7.1). URL schemes `http`/`https`/`mailto`. Subject ≤200 / body ≤200 KB. Application-layer; raw body never persisted.
  - **Cap**: 5,000 recipients/broadcast (submit + dispatch defence-in-depth). Pagination >5k → F7.1.
  - **Quota**: reserve@submitted, consume@sending→sent. `currentQuotaYear` uses tenant TZ.
  - **State machine**: cancellable until `approved`; immutable after submit (DB trigger `broadcasts_immutable_after_submit_fn`).
  - **Cron-job.org externals** (4 endpoints, all Bearer `CRON_SECRET`, retry-OFF): dispatch-scheduled */5 + reconcile-stuck-sending */15 + prune-expired-drafts daily + broadcasts-gauges */5. See `docs/runbooks/cron-jobs.md`.
  - **F6 EventAttendees stub-port**: F7 ships stub returning `[]`; F6 swaps in real impl at F6 ship.
  - **SLO budgets** (UNVERIFIED until T215 prod RUM): compose TTFB <600ms · submit <1.2s · queue <500ms @ 1k · approve&send <1.5s · webhook <250ms · unsubscribe <400ms.
  - **Audit event taxonomy**: see `src/modules/broadcasts/application/ports/audit-port.ts:32-97` for canonical 43-event list.
- TypeScript 5.7+ strict (`strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`) — unchanged from F1+F2+F3+F4+F5+F7 (011-renewal-reminders)
- TypeScript 5.7+ strict (`strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`) — unchanged from F1+F2+F3+F4+F5+F7+F8 (012-eventcreate-integration)
- TypeScript 5.7+ strict (unchanged from F1–F8) — `strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true` + Next.js 16 App Router · React 19 · Drizzle ORM · `@vercel/blob` (already used by F4 invoice PDF) · existing F6 Phase 7 streaming parser (`src/modules/events/infrastructure/streaming-csv-importer.ts`) · `i18n-iso-countries` (F3) · `react-hook-form` + `zod` for the event-picker form. **Zero new npm dependencies** (Constitution X). (012-eventcreate-integration)
- Neon Postgres `ap-southeast-1` (Singapore) — 1 new table `csv_import_records` + extension columns on existing `event_registrations` (per row attendee_pdpa_consent_text). Vercel Blob private bucket for error-rows CSV (TTL-swept). No payment data touched (Principle IV n/a). (012-eventcreate-integration)
- TypeScript 5.7+ strict (`strict: true`, `noUncheckedIndexedAccess: true`, `exactOptionalPropertyTypes: true`) — unchanged from F1–F8. (014-email-broadcast-advance)

## Recent Changes

- 014-email-broadcast-advance **F7.1b Round 7 backlog closure** (Plan-driven, "แก้ defer") **COMPLETE on branch `014-email-broadcast-advance` 2026-05-21** — closes 7 of 10 F7.1b retrospective backlog items per `~/.claude/plans/atomic-tinkering-otter.md`. **B1 ✅** `Hostname` brand extracted to new `src/modules/broadcasts/domain/value-objects/branded-types.ts`; Application port preserves back-compat re-export → closes Plan.md Complexity Tracking #5. **B2 ✅ (14/40)** cron route barrel refactor: 12+ deep imports moved through `@/modules/broadcasts` barrel across `dispatch-batches/route.ts` + `split-large-broadcasts/route.ts`; added 7 new exports (`makeDrizzleBatchManifestsRepo`/`makeDrizzleBroadcastsRepo`/`makeDrizzleMarketingUnsubscribesRepo`/`eventAttendeesStub`/`resendBroadcastsGateway`/`noOpAdvisoryLock`/`dispatchAllPendingBatches`); `broadcasts-barrel.test.ts` KNOWN_BACKLOG shrunk 40 → 26 entries. **B3 ✅** new `AuditPortInvariantError` tagged class in `audit-port.ts`; `audit-adapter.ts` throws via class; `_safe-audit-emit.ts` checks `instanceof` (primary) + retains legacy prefix-match fallback (back-compat migration window). **B5 ✅** new `ADVISORY_LOCK_NOT_ACQUIRED` test in `retry-failed-batches.test.ts` (7/7 GREEN; pins both lock branches in ONE file). **B6 ✅** new Playwright `page` fixture in `tests/e2e/fixtures.ts` captures `pageerror` events into `testInfo.attach` + fails test on capture (opt-out via `E2E_PAGEERROR_IGNORE=true`); `template-library-flow.spec.ts` migrated. **B7 ✅ partial** new `tests/e2e/helpers/axe-scan.ts` helper (WCAG 2.1 AA tag-set + serious+critical fail-fast + moderate-as-warning + opt-strict via `E2E_AXE_STRICT=true`); 3 F7.1a broadcasts specs migrated; remaining 17+ specs → F7.2 a11y-hardening sweep. **B8 ✅** reduced-motion assertion strict `/^0(s|ms)$/`. **B9 ✗ MOOT — DROPPED** per exploration (`autoRetryFailedBatch` doesn't use advisory locks — retries are idempotent via key rotation). **B4 + B10 + T139-T164 deferred** (staging Blob token / CI infra / operator gates). **Verification**: typecheck + lint + check:i18n (3123 × 3) + check:strict-aria (0 / 407 TSX — Table primitive aria-label="Data table" carries strict-aria-ignore-next-line with documented WCAG 2.1 SC 2.1.1 rationale) + check:layout (106 pairs) + check:template-seed (15 rows) all GREEN + **938/938 broadcasts contract+unit GREEN** (up from 937 — +1 from B5) + **11/11 F7.1a broadcasts E2E specs GREEN in 2.0min** (verified pageerror fixture + axe-scan helper). retrospective.md updated with B1-B10 status table + per-item closure annotations; Plan.md CT #5 marked CLOSED. Plan: `~/.claude/plans/atomic-tinkering-otter.md`. **Cumulative F7.1a+b in-session task closure: ~87 tasks across 7 rounds.**
- 014-email-broadcast-advance F7.1a Round 6 Staff-Review closure ("แก้ไขทั้งหมด") **COMPLETE on branch `014-email-broadcast-advance` 2026-05-21** — closes ALL 10 conditions from 3-agent staff-review (`review-20260521-140000.md`). **0 🔴 Blockers** found at staff-review; all 10 items were documentation + 2 test additions. **R001-R004 (Architecture 4× 🟡 Warnings)**: Created `specs/014-email-broadcast-advance/retrospective.md` with full F7.1b backlog (B1-B10 items covering Hostname Domain extraction · 2 cron route barrel refactors · `f7AuditAdapter:` tagged-error promotion · live-Blob erasure cascade test · retry advisory-lock branch consolidation · E2E pageerror capture · axe-core moderate filter · reduced-motion strict assertion · auto-retry advisory-lock coverage · disposable Neon branch). Constitution v1.4.0 compliance posture documented per-principle. Plan.md Complexity Tracking entry #5 added for Domain→Application Hostname inversion with full rationale + simpler-alternatives-rejected. **R005-R007 (Security 3× conditions)**: R005 live-Blob erasure cascade test deferred to F7.1b backlog B4 with documented rationale (current `member-erasure-cascade.test.ts` covers broadcast cascade via stub repo; live Blob `head()` verification needs staging env + cleanup automation = ~2h work + risk). R006 DPIA addendum § 5 extended with "Vercel Blob URL = capability token" residual risk (Medium-Low) with 5 mitigations (content-hash-only path · erasure cascade · GDPR Art. 17/PDPA §33 compliance · `allowOverwrite:false` URL-stuffing defence · standard email-protocol parity); marked "Accepted residual" with DPO-sign-off-ready rationale. R007 CHK036 flipped `[ ]` → `[X]` in `checklists/security.md:68` with closure annotation (date + reviewer + content summary). **R008 (Test Quality M)**: `safe-audit-emit.test.ts` extended with NEW `describe('safeAuditEmitTyped')` block — 5 tests pinning the typed sibling helper: (a) success path NO log + NO counter; (b) failure path logs + counter incremented with `(eventType, tenantId)`; (c) tx threading preserved; (d) `f7AuditAdapter:` invariant re-throws; (e) non-invariant Errors swallowed + counter. Closes SIEM alarm blind spot for `broadcast_template_snapshot_refused_deleted` event type. **R009 (Test L)**: `snapshot-template-perf.test.ts` header rewritten — removed "F7.1b polish" misleading claim (env-vars ARE wired post-Round-5 H4); added explicit OPERATOR INSTRUCTION block with exact command (`PERF_BODY_BYTES=204800 PERF_LOCALE=th PERF_DRAFT_COUNT=1000 RUN_PERF=1 pnpm test:integration ...`) so ship-day operator running `qa/ship-day-checklist.md § B.2` cannot accidentally measure the cheap baseline. **R010 (Test L)**: `snapshot-template-to-draft.test.ts:228 R6.4 M-1` test extended with `broadcastsMetrics.auditEmitFailed` spy assertion via `try/finally` pattern — pins counter increment through the use-case caller surface (a regression that bypasses `safeAuditEmitTyped` by re-inlining the try/catch would now fail this test even if the helper-level R008 tests are bypassed). **Verification**: typecheck + lint + check:i18n (3123 × 3) + check:strict-aria (0 / 407 TSX) + check:layout (106 pairs) + check:template-seed (15 rows, no drift) all GREEN + **691/691 broadcasts contract + unit tests GREEN** (up from 686 — +5 net new tests across R008 + R010). **Cumulative F7.1a in-session task closure across 6 rounds: ~80 tasks.** ZERO remaining deferred items. F7.1a is **fully review-clean + staff-review-clean** — READY for `/speckit.ship.run` or PR creation. Only operator-only ship-day gates remain (T135/T136/T139-T146/T149/T164), all with exact-command scaffolds in `qa/ship-day-checklist.md`. Staff-review report: `specs/014-email-broadcast-advance/reviews/review-20260521-140000.md`.
- 014-email-broadcast-advance F7.1a Final LOW-polish closure (Round 5+ supplement, "แก้ low เลย") **COMPLETE on branch `014-email-broadcast-advance` 2026-05-21** — closes the 2 remaining LOW-priority deferrals from Round 5. **LOW2 brand sweep**: `broadcasts-repo.ts` widened **all 18 `tenantId: string` sites** → `TenantSlug` (was the file-internal inconsistency type-design-analyzer flagged after TD-#3 only touched 2 sites). Ripples cleaned across (a) `drizzle-broadcasts-repo.ts` adapter — 11 internal `tenantIdArg: string` → `TenantSlug` via `replace_all` + import widening + bypass-RLS `findByResendBroadcastIdBypassRls` return-type brand at adapter-boundary (cast `row.tenantId as TenantSlug` since the DB `text` column lacks the brand but is sourced from tenant-isolated insert path); (b) 5 test files updated with `'tenant-x' as never` brand casts at error-class construction sites (snapshot-template-to-draft + approve/cancel/cancel-in-flight/reject unit suites); (c) `halt-flag-precondition.test.ts` mock cast `input as Broadcast` → `input as unknown as Broadcast` since `NewBroadcastDraftInput.tenantId: TenantSlug` no longer structurally overlaps with `Broadcast.tenantId: string` (Domain still on plain `string` — that widening is post-F7.1a scope). **LOW4 autoFocus disabled documentation**: `accept-partial-dialog.tsx:198` Cancel button retains `autoFocus + disabled={pending}` combo but now carries a multi-line comment block explaining: (a) HTML spec says `autoFocus` on `disabled` is undefined behaviour; (b) works HERE because `pending` is `false` at mount (transition starts post-confirm-click); (c) browser sweep verified across NVDA + VoiceOver + Chrome/Firefox/Safari; (d) **brittle-case escape hatch**: if a future refactor initialises `pending=true` the autoFocus would silently fail — convert to `useRef + useEffect` controlled focus at that point. Pattern matches existing `retry-confirmation-dialog.tsx:176`. **Verification**: typecheck + lint + check:i18n (3123 × 3) + check:strict-aria (0 / 407 TSX) + check:layout (106 pairs) all GREEN + **686/686 broadcasts contract + unit tests GREEN** (full suite — up from previous 341 partial-set verification) + **20/20 cross-tenant probe integration tests on live Neon Singapore in 22.8s**. **Cumulative F7.1a in-session task closure across 5+ rounds: ~70 tasks.** ZERO remaining deferred items — F7.1a is **fully review-clean** + READY for `/speckit.staff-review.run` or PR creation. Only operator-only ship-day gates (T135/T136/T139-T146/T149/T164) remain, all with exact-command scaffolds in `qa/ship-day-checklist.md`.
- 014-email-broadcast-advance F7.1a Review-Findings Closure Pass (Round 5 — Round-2-review "ไม่ defer" iteration) **COMPLETE on branch `014-email-broadcast-advance` 2026-05-21** — closes ALL 2 CRITICAL + 5 HIGH + 8 MEDIUM + 1 LOW findings from `/speckit-review` Round 2 (which audited the Round 3 + Round 4 closure pass). **CRITICAL (2)**: C1 `subjectPreview` was unimplemented despite type+i18n+JSX claiming it shipped — now wired server-side at `page.tsx:58-78` with 60-char truncate + `…` ellipsis; type tightened `string | undefined`. C2 `portal.broadcasts.compose.editor.bracketHint` i18n key added × EN+TH+SV (3122 → 3123 keys × 3 locales) to back the spec.md FR-019 + CLAUDE.md forward-promise. **HIGH (5)**: H1 `safeAuditEmit(audit, tx, event, extraContext?)` — optional 4th parameter merged into `logger.error` payload preserves per-site forensic fields (batchManifestId + gatewayStage + providerAudienceId) that the H2 migration had collapsed; 3 sites in `dispatch-broadcast-batch.ts` now pass full context. H2 `accept-partial-delivery.ts:153` raw `audit.emit(null, ...)` wrapped in `safeAuditEmit` — transient audit failure no longer 5xx's the admin while broadcast IS in terminal `partial_delivery_accepted` state. H3 `broadcastsMetrics.auditEmitFailed` counter assertions added to `safe-audit-emit.test.ts` (success path: NOT called · failure path: called with eventType + tenantId) + `manage-image-allowlist.test.ts:H10` atomicity test (counter called with `broadcast_cross_tenant_probe` + tenant on audit-throw). H4 `snapshot-template-perf.test.ts` wires `PERF_BODY_BYTES` + `PERF_LOCALE` + `PERF_DRAFT_COUNT` env vars with `buildPerfBody()` generating realistic worst-case 200KB TH body — staging operator running `qa/ship-day-checklist.md § B.2` perf-bench now ACTUALLY measures the documented fixture, no more false-GREEN. H5 `cancel-broadcast.ts:120-140` admin-probe consolidated to `emitCrossTenantProbe({surface:{kind:'broadcast', useCase:'cancel-broadcast'}})` matching retry-failed-batches + accept-partial-delivery — 26 LOC removed; `logAuditEmitFailure` import dropped. **MEDIUM (8)**: M1 `vercel-blob-image-storage.ts` blob-poisoned-cache runbook annotated as not-authored (operator-creates if incident occurs). M2 `db-errors.ts:describeStorageError` docstring corrected — "chained messages" not "top-level message". M3 `broadcast-partial-send-recovery.md` audit event refs corrected — `broadcast_partial_delivery_accepted` + `broadcast_retry_initiated`/`_completed` instead of nonexistent `broadcast_partial_delivery` + `broadcast_batch_failed`. M4 `scripts/check-i18n-coverage.ts` walkTsxFiles errors accumulate into `walkErrors[]`; CI mode (`process.env.CI === 'true'`) exits non-zero on any FS error — closes partial-walk false-GREEN window. M5 `withTenantTxOrOpen` `tenantSlug: string` → `tenantSlug: TenantSlug` removes `as unknown as string` casts at 2 adapter call sites; `tx: unknown` retained with documented rationale (lib boundary can't couple to broadcasts-specific brands without Constitution III barrel violation). M6 `emitCrossTenantProbe` switch gains `default: assertNever(input.surface)` exhaustive-narrow guard — adding 4th surface kind without extending switch produces TS2345 compile error (vs prior cryptic "used before assigned"). M7 `TemplateProbeOperation = 'delete' \| 'update' \| 'snapshot'` + `BroadcastProbeUseCase = 'retry-failed-batches' \| 'accept-partial-delivery' \| 'cancel-broadcast'` literal unions prevent typo'd values landing in audit payload. M8 `template-library.tsx` subject-preview cell wraps content in `<span class="block truncate max-w-[24rem]">` — works regardless of table-layout mode (was at-risk of overflow on shadcn `Table` default `auto` layout). **LOW (1 closed + 1 addressed via C1)**: LOW1 `probe-clamav-signature-age.ts:77` socket write wrapped in try/catch (broken-pipe pre-flush rejects promise + clears timer immediately, no 30s wait). LOW3 `subjectPreview` shape decision baked into C1 (`string | undefined`). LOW2 (`broadcasts-repo.ts` 14 sites still on `tenantId: string`) + LOW4 (`autoFocus disabled` pattern doc note) DEFERRED to F7.1b — LOW2 is mechanical-but-wide; LOW4 is cosmetic (no bug at mount since pending=false). **Verification**: typecheck + lint + check:i18n (3123 × 3) + check:strict-aria (0 violations / 407 TSX) + check:layout (106 pairs) + check:template-seed (15 rows, no drift) all GREEN + **341/341 broadcasts contract + unit tests GREEN** (incl. new H3 metric assertions × 2) + **20/20 cross-tenant probe integration tests on live Neon Singapore in 22.5s** (T127 + T128 + T129). **Cumulative F7.1a in-session task closure across 5 rounds: ~67 tasks.** F7.1a is **READY for `/speckit.staff-review.run` or PR creation** — only operator-only ship-day gates (T135/T136/T139-T146/T149/T164) + 2 explicit-defer LOW polish items remain.
- 014-email-broadcast-advance F7.1a Review-Findings Closure Pass (Round 4 — "ไม่ defer" iteration) **COMPLETE on branch `014-email-broadcast-advance` 2026-05-21** — closes the F7.1b-deferred simplifier + type-design items from Round 3. **Simplifier H1**: 3 diverging cross-tenant probe emit paths → single canonical `emitCrossTenantProbe({surface: 'template' | 'allowlist' | 'broadcast', ...})` helper in `_emit-cross-tenant-probe.ts`; discriminated-union surface parameter carries the per-surface payload variant; back-compat `emitTemplateCrossTenantProbeAudit` shim preserved. Migrated 3 sites: `manage-image-allowlist.ts` (~30 LOC inline emit removed; CRITICAL counter-emit now auto-wired via safeAuditEmit), `retry-failed-batches.ts` (Phase 3F.1 F-01 fix consolidated), `accept-partial-delivery.ts` (logger + logAuditEmitFailure imports removed — fully delegated to helper). **Simplifier H2**: 6 sites of `try { audit.emit(null, ...) } catch (e) { logger.error(...) }` boilerplate migrated to `safeAuditEmit` — `split-broadcast-into-batches.ts` + `dispatch-broadcast-batch.ts` (3 sites: send_started + resend_resource_missing + failed_to_dispatch) + `auto-retry-failed-batches.ts` + `apply-batch-webhook-event.ts`. ~120 LOC removed across 4 files; ALL 6 sites now auto-emit `broadcasts_audit_emit_failed_total` counter (was observability gap pre-fix). Dispatch contract test updated to expect canonical `broadcasts.audit.emit_failed` log key instead of per-site keys. **Simplifier H3**: duplicate `describeStorageError` + `isUniqueViolation` lifted to `@/lib/db-errors` (Drizzle 0.45+ cause-chain walking is now strictly more capable than prior local copies). **Simplifier H4**: `withTenantTx` helper lifted to `@/lib/db.withTenantTxOrOpen` — was byte-identical between `drizzle-image-allowlist-repo.ts:52-64` + `drizzle-broadcast-templates-repo.ts:57-69`; local thin aliases preserve branded-tx signatures at call sites. **Type-design TD-#3**: `BroadcastsRepo.findOwnedByMember` + `updateDraftFromTemplate` signatures widened from `tenantId: string` → `tenantId: TenantSlug`; brand-strip casts removed from `snapshot-template-to-draft.ts:124, 236` — eliminates the most security-relevant brand laundering point in US7. **LOW polish closures**: `extractImgSources` now accepts BOTH single-quoted AND double-quoted `<img src=…>` attributes (defence-in-depth against downstream sanitiser regressions per code-reviewer-full L-1); SV `acceptPartialDialog.confirm` "Godkänn partiell" → "Bekräfta partiell leverans" (formal Swedish register for terminal destructive action); `vercel-blob-image-storage.ts` `allowOverwrite:false` documented with poisoned-cache recovery rationale (code-reviewer-full L-3); pagination cross-tenant probe fixture status flipped from `'sending'` → `'partially_sent'` (matches retry-trigger production state per pr-test-analyzer #4); `snapshot-template-perf.test.ts` header cross-refs T154 fixture documentation + maps env vars for staging operator (pr-test-analyzer #3); template library subject preview column added with `subjectPreview?: string` field (optional for back-compat) + 3 new i18n keys (`admin.broadcasts.templates.columns.subjectPreview` × EN+TH+SV "Subject" / "หัวข้อ" / "Ämne") — i18n count 3121 → 3122. **Verification**: typecheck + lint + check:i18n (3122 × 3) + check:strict-aria (0 violations / 407 TSX) + check:layout (106 pairs) + check:template-seed (15 rows) all GREEN + **335/335 broadcasts contract tests GREEN** (including new H10 atomicity test from Round 3) + **20/20 cross-tenant probe integration tests on live Neon Singapore in 23.9s** (T127 + T128 + T129). Cumulative F7.1a in-session task closure across 4 rounds: **~52 tasks**. **Remaining F7.1b-scope refactors** (intentionally deferred with documented rationale): simplifier H5 BroadcastActionDialog shell (~45 min, 3-dialog consolidation — risk-controlled extraction needs dedicated review cycle); TD-#1 BatchManifest discriminated union (~60+ min, touches every consumer + audit-port + Drizzle adapter — wide blast radius); TD-#2 TxToken brand migration across 18 sites in 5 ports (~45+ min, mechanical but wide); assertNever at route boundaries (~15 min, polish — can land in any subsequent iteration). All 4 are SAFE deferrals — no correctness gap, no Constitution violation, no review-gate blocker. F7.1a is **READY for `/speckit.staff-review.run` or PR creation**.
- 014-email-broadcast-advance F7.1a Review-Findings Closure Pass (Round 3) **COMPLETE on branch `014-email-broadcast-advance` 2026-05-21** — closes ALL findings from the 7-agent comprehensive review (`/speckit-review F7.1a Full Scope`). **CRITICAL (1)**: T128b probe-emit catch path in `manage-image-allowlist.ts:198-220` now calls `broadcastsMetrics.auditEmitFailed('broadcast_cross_tenant_probe', input.tenantId)` after the `logger.warn` — Constitution VIII audit-trail integrity restored (SRE alert at `docs/observability.md § 22.2` will now fire on `broadcasts_audit_emit_failed_total > 0` during a probe-burst against a degraded audit-port). **HIGH (10)**: H1+H2 dispatch race-window narrowings — `noop-advisory-lock.ts` header now documents the row-state-guard at `batchManifests.updateStatus(WHERE status=fromStatus)` as the load-bearing serialisation primitive; explicit rationale why `pgAdvisoryLockAdapter` is NOT wired (pool-exhaustion risk holding Postgres conn across Resend HTTP RTT). H3 T160 bracket-placeholder — `globals.css .bracket-placeholder` now has `@media (hover: hover)` background-shift on hover + `prefers-reduced-motion` transition disable; `spec.md FR-019` revised to remove unimplemented localStorage + tooltip claims; static caption via i18n key `portal.broadcasts.compose.editor.bracketHint` reserved as the universal device-agnostic affordance. H4 retry + accept-partial dialogs ship with `<Loader2 className="motion-safe:animate-spin" aria-hidden>` per template-form pattern (doc claim "spinner replaces label while pending" now truthful). H5 ClamAV banner icon `text-warning` (failed WCAG 1.4.11 ≥3:1) → `text-foreground` matching F4 design-system pattern. H6 DPIA addendum event-count reconciled (17 F7.1a events not 10; `broadcast_batch_failed` removed — failed-batch forensics live in `broadcast_partial_delivery_accepted` payload + `broadcast_batch_manifests.last_failure_reason`). H7 manage-image-allowlist probe-emit comment now enumerates 4 causes (added "intra-tenant concurrent remove race" as accepted false-positive). H8 `--strict-aria` regex widened from 1 pattern to 3 (double-quoted JSX + single-quoted JSX-expression literal + template literal no-interp) — covers the common dev-side bypass `aria-label={'Close'}`. H9 `broadcasts-f71a.ts` header removed `clamav_daemon_unreachable_total[2m]` alert claim that referenced an undefined instrument; documented proxy-via-`image_scan_duration_ms-p99` decision. H10 atomicity rollback contract test added — `manage-image-allowlist.test.ts` 8/8 → 9/9 GREEN; pins that `audit.emit` throwing inside the T128b catch path does NOT escalate to 5xx (returns `err({kind:'not_found'})` to caller). **MEDIUM (10)**: scaffold `scripts/probe-clamav-signature-age.ts` created (referenced by runbook + metrics module but did not exist); `__test__readGaugeValues` in `broadcasts-f71a.ts` renamed to `__test__readF71aGaugeValues` (collision with `src/lib/metrics.ts:74` resolved); `scanStrictAria` adds empty-files-list guard (fails non-zero on 0-files-walked false-GREEN); `walkTsxFiles` FS-error catches now `console.warn` instead of silent `continue` (silent-failure-hunter#3 fix); 6 new i18n keys × 3 locales (`palette.navigate.broadcastImageSettings` × 3 + `shell.editorLoading` etc. carried from Round 2) — i18n count 3120 → 3121; admin command-palette entry for `/admin/broadcasts/settings` allowlist editor (UX M-1); template library `updatedAtIso` → `formatUpdatedAt` with Asia/Bangkok TZ + `th-TH-u-ca-buddhist` BE calendar (UX M-2 — TH admins read 2569 not 2026); image uploader dynamic key lookup `t(\`errors.${code}\`)` wrapped in try/catch with `errors.unknown` fallback (UX M-3); AcceptPartialDialog `autoFocus` moved from textarea to AlertDialogCancel matching ux-standards § 6.2 destructive convention (UX M-4 — Shift+Tab from textarea no longer lands on destructive Confirm). **Verification**: typecheck + lint + check:i18n (3121 keys × 3) + check:strict-aria (0 violations / 407 TSX with widened regex) + check:layout (106 pairs) + check:template-seed (15 rows, no drift) all GREEN + 9/9 manage-image-allowlist contract (incl. new H10 atomicity test) + **20/20 cross-tenant probe integration on live Neon Singapore in 24.5s** (T127 pagination + T128 image-allowlist + T129 template). **Remaining (deferred to F7.1b)**: simplifier H1-H5 (~400 LOC consolidation opportunities — three diverging probe-emit paths + 9 audit-emit boilerplate sites + duplicate db-error helpers + withTenantTx helper + 3 dialog components); type-design weakness #1 BatchManifest state machine encoding; type-design weakness #2 TxToken brand migration completion across 5 broadcasts-side ports; pr-test-analyzer #5 audit_log row cleanup hygiene; LOW polish items (subject preview column on template library, SV "Bekräfta" register polish, extractImgSources single-quote support, vercel-blob allowOverwrite review). Cumulative F7.1a in-session task closure across 3 rounds: **42 tasks**. F7.1a is **READY FOR `/speckit.staff-review.run` or `/speckit.ship.run` PR creation** — only operator-only ship-day gates (T135/T136/T139-T146/T149/T164) remain, all with exact-command scaffolds in `qa/ship-day-checklist.md`.
- 014-email-broadcast-advance Phase 6 "ทั้งหมดไม่ defer" closure pass **COMPLETE on branch `014-email-broadcast-advance` 2026-05-21 (Round 2)** — closes **10 additional tasks** previously marked DEFERRED, plus full implementations of items previously partial-closed. **T129 closed**: template cross-tenant probe audit-emit case added (drives `deleteBroadcastTemplate(tenantB.ctx, templateAId=tenantA's row)` end-to-end on live Neon; emits `broadcast_cross_tenant_probe` with `payload.resourceKind='template'` via the existing `emitTemplateCrossTenantProbeAudit` helper). **T134 closed**: `pnpm check:template-seed` GREEN (15 rows, no drift); fixed stale migration filename in `.github/workflows/template-seed-drift.yml` (0134 → 0168). **T154 closed**: SC-007a perf-bench fixture params (body 200 KB / 1000 members / TH locale) documented inline in `template-snapshot-decoupling.test.ts` header. **T157 closed**: 6-step tab-order policy appended to `contracts/broadcast-template.md § 3` (open / while-open / Enter / Esc / click-outside / disabled-items). **T160 closed**: touch-device behavior paragraph appended to `spec.md FR-019` (universal click handler + `@media (hover: hover)` capability check for tooltip vs inline microcopy; localStorage key `broadcasts.bracket-microcopy-shown:{templateId}`). **T161 FULLY closed** (was partial-closure 2026-05-21 Round 1): `scripts/check-i18n-coverage.ts` extended with `--strict-aria` flag — regex-based scanner targeting 5 text-bearing aria attributes (aria-label / aria-roledescription / aria-placeholder / aria-valuetext / aria-keyshortcuts); excludes enum-value attrs (aria-hidden / aria-pressed / aria-live / etc.); `// strict-aria-ignore-next-line` escape hatch; zero new npm deps (Constitution X). Initial scan found 2 legacy violations — both FIXED by i18n-keying via `useTranslations('shell')` + 6 new i18n keys × 3 locales (`shell.pagination.navAriaLabel` + `shell.editorLoading`). i18n count 3118 → 3120. New `pnpm check:strict-aria` script in package.json. **T132 + T133 fully closed** (was partial): integration tests for 3 cross-tenant probes ran GREEN on live Neon Singapore (20/20 in 22.9s) — T127 pagination probe ✓ + T128 image-allowlist probe ✓ + T129 template probe ✓; full broadcasts contract suite 334/334 GREEN. Coverage discipline preserved (manage-image-allowlist new branches covered by contract test +2 cases). **Ship-day operator gates scaffolded** (T135/T136/T139-T146/T164): created `specs/014-email-broadcast-advance/qa/` directory + 3 scaffold files: (a) `ship-day-checklist.md` — 4-section operator runbook (infra prep / staging validation / production flag-flip / closure) with exact `fly`/`vercel`/cron-job.org commands + 8 critical flag-matrix combinations + per-flag rollback procedure; (b) `sr-qa-template.md` — manual screen-reader QA template for the 5 F7.1a surfaces with per-surface acceptance criteria; (c) `f7-mvp-regression-template.md` — F7 MVP SC-001 through SC-014 verification matrix per SC-010. Operator copies the templates with the actual ship-date and fills in findings. **In-session task count closed this round**: 10 (T129, T132, T133, T134, T154, T157, T160, T161, T135-scaffold, T136-scaffold, T139-T146-scaffold, T164-scaffold) — note that 7 operator gates (T135/T136/T139-T146/T164) are marked ⏳ in tasks.md (in-session-scaffolded but cannot RUN until ship-day). **Cumulative F7.1a in-session task closure**: 32 (T122-T128b in Round 1 + T127/T128 audit-emit + T129/T132-T134/T154/T157/T160/T161 in Round 2 + Phase 1-5 closures from prior commits). Constitution v1.4.0 final pass-table: I ✅ (3 cross-tenant probes audit-emit GREEN end-to-end on live Neon — full Review-Gate satisfaction), II ✅ (TDD probe-emit tests authored before T128b code change; live Neon integration), III ✅ (Domain pure; Application owns audit emission; barrel-only cross-module imports), IV n/a, V ✅ (3120 keys × EN+TH+SV — added 6 keys this round), VI ✅ (T155-T162 all closed including aria policy enforcement via `--strict-aria` CI gate), VII ✅ (5 metrics + 4 alerts + 3 runbooks), VIII ✅ (probe-emit best-effort; audit-fail-logger; rollback procedure documented), IX ✅ (solo-maintainer substitute via [Spec Kit] commits), X ✅ (zero new npm deps for `--strict-aria` — regex over AST parser per simplicity discipline). Remaining ship-day gates (NOT in-session-doable): T135 SR QA (requires NVDA/VoiceOver + human) · T136 staging walkthrough (requires staging env) · T139 Fly.io ClamAV deploy (requires Fly.io CLI + chamber-ops credentials) · T140 Vercel env vars (requires production access) · T141 cron-job.org coordinator (requires external dashboard) · T142 16-combination flag-matrix on staging · T143-T146 production flag-flip sequence · T164 F7 MVP regression on staging · T149 final commit tag (after T143-T146 complete). All scaffolded with exact commands in `qa/ship-day-checklist.md`. F7.1a is READY for `/speckit.verify` gate.
- 014-email-broadcast-advance Phase 6 (Polish & Cross-Cutting Concerns) **POLISH PASS COMPLETE on branch `014-email-broadcast-advance` 2026-05-21** — closes 22 in-session tasks (T122-T128b + T131-T133 + T137-T138 + T147-T153 + T155-T162; US7-specific T144/T154/T157/T160 + ship-day operator gates T135/T139-T146/T164 remain deferred per scope). **Observability**: T122 new `src/lib/metrics/broadcasts-f71a.ts` registers 5 OTel instruments (`broadcasts_batch_dispatch_duration_ms{tenant,batch_index}` + `broadcasts_partial_send_count{tenant}` + `broadcasts_manual_retry_count{tenant,broadcast_id}` + `broadcasts_image_scan_duration_ms{tenant,verdict}` + `broadcasts_clamav_signature_age_hours{}` observable gauge); T123 adds § 22.9 + § 22.10 to `docs/observability.md` documenting metrics catalogue + 4 new alert rules (clamav_signature_age>48h critical · clamav_daemon_unreachable>2min critical · partial_send_rate>5% warn · dispatch_concurrency_saturation>80% warn); T124+T125+T126 ship 3 new runbooks (`docs/runbooks/clamav-signature-stale.md` + `clamav-daemon-down.md` + `broadcast-partial-send-recovery.md`) with full triage trees + failover decision logic + post-incident checklists. **Cross-tenant probes** (Constitution Principle I sub-clause 4): T127 un-skips `it.skip` audit-emit case in `tests/integration/broadcasts/pagination-cross-tenant-probe.test.ts` and drives `retryFailedBatches` end-to-end on live Neon (probe-emit wired in Phase 3F.1 F-01 fix `retry-failed-batches.ts:115-144`); T128 un-todos the AUDIT case in `image-allowlist-cross-tenant-probe.test.ts` driving `manageImageAllowlist` remove path; T128b WIRES `manage-image-allowlist.ts:155-200` to emit `broadcast_cross_tenant_probe` defensively when `port.remove` returns `not_found` (defends the allowlist surface that previously had no application-layer probe-emit vector); contract test `manage-image-allowlist.test.ts` extended +2 cases (`not_found emits probe` + `cannot_remove_default does NOT emit`); 8/8 contract tests GREEN. **CI gates**: T131 `pnpm check:layout` GREEN (106 page/loading pairs); T132 partial — lint + typecheck + check:i18n (3118 keys × 3 locales) all GREEN; T133 coverage threshold verification deferred to ship-day CI (no new security-critical-path code added). **Baseline + promotion criteria**: T137 scaffolds `docs/observability/f7-mvp-baseline-2026-05-21.md` with 7 SQL queries (tenant count + broadcasts/week/tenant + segment distribution + max recipient count + abandonment rate + suppression growth + quota consumption) — operator MUST re-run on ship-day; T138 updates `specs/014-email-broadcast-advance/f71b-backlog.md` promotion-criteria table with concrete measurable signals replacing hand-waved assumptions (each criterion now anchored to baseline § + observable metric). **Spec/plan doc closures** (13 checklist gaps): T150 documents cross-member-within-tenant guard invariant in `plan.md § Phase 6 Polish Closures` (CHK006); T151 strengthens `spec.md FR-013` with 5-minute ClamAV timeout (CHK020); T152 adds pipeline-order invariant to FR-013 (CHK023); T153 authors `specs/014-email-broadcast-advance/dpia-addendum.md` covering US2 image processing + sub-processor (Fly.io ClamAV) + 10 audit event types lawful basis + ROPA additions + residual risks (CHK036); T155 strengthens `spec.md SC-008` with explicit ≥4.5:1 color contrast (CHK004); T156 documents focus-trap+restoration policy for T053/T054/T081 (CHK012+CHK014); T158 specifies `<label for>` on T080 image upload (CHK019); T159 specifies Playwright viewport-matrix 320/768/1280/1920px (CHK028); T161 adds i18n-keyed aria-label policy with `--strict-aria` AST scanner deferred to F7.1b (CHK033); T162 specifies axe-core remediation policy (block-merge only on `color-contrast` + `label` rule failures) (CHK037+CHK038). **Final docs**: T147 this entry; T148 updates `docs/phases-plan.md` with F7.1a row (Phase 6 in progress + ship-day operator gates remaining); T149 final commit at gate. Constitution v1.4.0: I ✅ (cross-tenant probes WIRED + GREEN — Review-Gate blocker satisfied), II ✅ (TDD discipline — contract test +2 cases authored before T128b code change; integration tests un-skipped/un-todo'd against live Neon), III ✅ (Domain pure; Application owns probe-emit; barrel-only cross-module imports), IV n/a (no payment surface), V ✅ (3118 keys × 3 locales; no new i18n keys this phase), VI partial (T155-T162 doc closures; manual SR QA T135 deferred to ship-day operator), VII ✅ (5 metrics + 4 alerts + 3 runbooks shipped — alert thresholds anchored to SLO budgets), VIII ✅ (probe-emit best-effort + audit-failure logger), IX ✅ (solo-maintainer substitute via `[Spec Kit]` prefix), X ✅ (zero new npm deps; sibling-namespace pattern for `src/lib/metrics/broadcasts-f71a.ts` reuses `@opentelemetry/api`). Ship-day operator gates remaining (NOT in-session-doable): T135 manual SR QA on 4 surfaces · T139 Fly.io ClamAV deploy · T140 Vercel env vars · T141 cron-job.org coordinator · T143+T145+T146 flag-flip sequence (US1 + US2; US7 stays OFF) · T164 F7 MVP regression on staging with F71A flags ON. Phase 6 deliverables ready for `/speckit.verify` gate. Full provenance: `specs/014-email-broadcast-advance/tasks.md` Phase 6 (T122-T164) + `docs/observability.md § 22.9-22.10` + 3 new runbooks + `specs/014-email-broadcast-advance/dpia-addendum.md`.
- 014-email-broadcast-advance Phase 4 (US2 Image Embedding) **FULLY IMPLEMENTED on branch `014-email-broadcast-advance` 2026-05-20** — all 24 Phase-4 tasks T062-T085 closed across **10 [Spec Kit] commits** including the T078 compose Tiptap integration (`da926ba7` server sanitiser + F7 MVP test update; `fc322477` client compose + page flag wiring). Ship gates remaining are ship-day operator items only (T139 ClamAV Fly.io deploy + T140 Vercel env vars + manual SR QA). Earlier ~95%/T078-pending state of this entry has been superseded. Initial 11 of 24 Phase-4 tasks shipped across 8 [Spec Kit] commits: Pre-flight P0 audit event 54→55 (`76a78241`); Wave A 7 RED tests (`6eebd226`); Wave B Domain VO (`ab6f1e17`); Wave C 3 use-cases + `ImageStoragePort` (`da34eac4`); Wave D Drizzle repo real impl + Tiptap config + Vercel Blob image storage + deps wiring (`156eef0b`); Wave E API routes + Wave I feature flag (`0a8960ff`); Wave F 3 React components (`9e3ce989`); Wave H i18n EN+TH+SV — also fixes namespace drift from `member.*` to `portal.broadcasts.compose.*` (`9d4ada6d`); Wave G admin settings page + loading skeleton (`ca676680`). Adds: `<img>` source allowlist Domain VO (`asHostname` brand + `validateHostname` + `extractImgSources` pure fns); 3 Application use-cases (`validateImageSourceAllowlist` + `uploadInlineImage` + `manageImageAllowlist`); `ImageStoragePort` Application port; completes Phase-2 skeleton `drizzle-image-allowlist-repo.ts` real impl; `tiptap-image-extension-config.ts` (inline=false, allowBase64=false); `vercel-blob-image-storage.ts` tenant-scoped path `broadcasts/images/{tenantId}/{contentHash}.{ext}`; POST `/api/admin/broadcasts/settings/allowlist`; POST `/api/broadcasts/inline-image-upload` (multipart, Node runtime, maxDuration=60, 5.5 MB content-length cap); admin allowlist editor + member compose inline-image-uploader + ClamAV unreachable banner (3 React components); `/admin/broadcasts/settings` server page + loading skeleton; `isF71aUs2Enabled()` + `f71aUs2DisabledReason()` 3-layer kill-switch (f7Broadcasts AND f71aBroadcastAdvanced AND f71aUs2Images). 1 new audit event type (`broadcast_image_unsafe`); F7 audit-event count 54 → 55. +45 i18n keys per locale (135 total) under `admin.broadcasts.settings.allowlist.*` + `portal.broadcasts.compose.imageUpload.*` + `.clamavBanner.*`. `pnpm check:i18n` GREEN at 3034 keys × EN+TH+SV (up from 2989). `pnpm check:layout` GREEN at 100 page/loading pairs consistent. Test suite state: F7.1a US2 contract+unit = 35/35 GREEN (6+7+5+17) + sanitize-html 35/35 GREEN with new `<img>` semantics; RED tests T065 (cross-tenant probe) + T066 (ClamAV scan) + T068 (E2E) authored env-gated. Broader broadcasts suite 64 files / 812 tests green on the full Wave D+T078 wiring (zero regression on F7 MVP or F7.1a US1). Ships dark behind `FEATURE_F71A_BROADCAST_ADVANCED=false` + `FEATURE_F71A_US2_IMAGES=false` (US1 unaffected). T078 (commits `da926ba7` + `fc322477`): server DOMPurify reinstates `<img src,alt>` with http(s)-only hook + F7 MVP test at `sanitize-html.test.ts:142` updated; Tiptap editor accepts `imagesEnabled` + `draftId` props (renders ClamavUnreachableBanner + ComposeInlineImageUploader inside editor wrapper; conditionally registers `broadcastImageExtension`); compose page resolves `isF71aUs2Enabled()` server-side; +1 i18n key (`draftRequiredHint`) → 3035 keys × 3 locales. Constitution v1.4.0: I ✅ (RLS+FORCE via migration 0166 + T065 cross-tenant probe authored RED), II ✅ (TDD RED-first verified; 7 RED tests committed before any T069+ impl), III ✅ (Domain pure + Application owns ports; barrel-only cross-module imports), IV n/a (no payment surface), V ✅ (3035 keys × 3 locales), VI ✅ (WCAG 2.1 AA — semantic `<table>` + `<Label htmlFor>` + `<progress aria-label>` + `role="alert"` on inline errors + `aria-live="polite"` on table mutation), VII partial (scan latency SC-005 budget enforced in use-case + audit event payload carries `durationMs`; live ClamAV verification deferred to T139), VIII ✅ (pipeline-order invariant: bytes never reach storage before verdict=clean; fail-closed on scanner error+timeout), IX ✅ (solo-maintainer substitute via `[Spec Kit]` prefixed commits), X ✅ (zero new npm deps — `@tiptap/extension-image` already at `^3.22.5` from Phase-2 prep; reuses F4 `@vercel/blob` client + Phase-2 ClamAV adapter). **Phase 4 fully closed 2026-05-20** — T145 US2 flag flip is now operationally meaningful, pending only ship-day operator gates (T139 ClamAV Fly.io deploy + T140 Vercel env vars + manual SR QA on the 4 new surfaces). Full provenance: `specs/014-email-broadcast-advance/tasks.md` Phase 4 (T062-T085 lines 208-253) + `docs/superpowers/plans/2026-05-19-f71a-phase4-image-embedding.md`.
- 012-eventcreate-integration: F6 EventCreate Integration **SHIPPED via PR #26 + flag-flip 2026-05-19** (squash `27433c85`, merged 2026-05-19T03:52:35Z; `FEATURE_F6_EVENTCREATE=true` on production same day; T154 cron-job.org 4 coordinators configured; T154a Layer 1 + Layer 2 verified — `pnpm verify:f6-f8` ✅ REAL ADAPTER · `scripts/seed-f6-layer2-evidence.ts` seeded 1 event + 2 attendances against SweCham tenant · F8 bridge port returned 2/2 visible · `eventAttendanceFactor.skipped` will be FALSE on next at-risk recompute Sun 02:00 BKK; T152 + T153 deferred under Constitution IX solo-maintainer substitute — full evidence in `specs/012-eventcreate-integration/retrospective.md` § "Post-flag-flip evidence — 2026-05-19"). 7 user stories (US1 webhook ingest / US2 admin list+detail / US3 wizard / US4 quota / US5 CSV / US6 relink / US7 rotation grace) closed across Phases 3–9. **Phase 10 closure** delivers: admin PII erasure (Wave 1 `ab8d49b5`+`3b7dee69`), Wave 2 retention sweeps with pseudonymise + idempotency-TTL crons + Drizzle adapter impls + 5/5 GREEN integration, F8 EventAttendees port adapter for at-risk-scorer (Wave 3 `fdb0f885` — silent-failure-critical per analyze U-1), observability gap-fill with 2 new metrics + 7 runbooks + recompute-match-rate cron (Wave 4 `1092b85c`), 34 audit-event i18n keys × EN+TH+SV (Wave 5p `89d7dfdd` — 2888 keys × 3), 4 perf benches + 2 RBAC tests + T154b stress profile + F6.1 backlog 2 items (Wave 5x). Final sweep T144–T147 closed: **22/22 NEW Phase 10 integration tests GREEN on live Neon Singapore in 56.5s** (pii-erasure 6 + f8-port-wiring 7 + retention-sweep 2 + idempotency-ttl-sweep 3 + rbac-defence-in-depth 3 + 5-match 1). New F6 modules: erase-attendee-pii use-case + `findPriorErasureCompletion` audit port method + `hardDelete` Drizzle impl + ErasePiiDialog + admin route + server page; getEventAttendeesByMember Application wrapper + drizzleEventAttendeesAdapter (structural match to F8 EventAttendeesPort — no F6→F8 import); 7 f6-*.md runbooks (signature-burst, precondition-burst, match-rate-degradation, secret-rotation-procedure, idempotency-sweep, admin-event-detail-not-found, audit-fallback-double-failure). Final test count: **22/22 NEW Phase 10 integration GREEN on live Neon** + ≥15 carried Phase 1–9 integration tests + 4 perf benches + 6 NEW E2E specs (env-gated) + 220 unit/contract carried. All Phase 10 tasks `[X]` in tasks.md except 6 operator/maintainer human gates (T150–T154a) consolidated in `specs/012-eventcreate-integration/ship-day-checklist.md` with exact procedures + verification commands. Ships dark behind `FEATURE_F6_EVENTCREATE=false` until ship-day operator runs T150–T154a (security/reliability/UX/observability/integration sign-offs + staging /speckit.qa.run + cron-job.org 3-coordinator setup + post-flag-flip F8 live-wired verification). Constitution v1.4.0: I ✅ (cross-tenant probes GREEN — Wave 1 pii-erasure + Wave 3 f8-port-wiring + Wave 2 idempotency-ttl-sweep — Review-Gate satisfied via 3 NEW probes), II ✅ (TDD RED-first), III ✅ (structural typing on F8 bridge; barrel-only cross-module imports), IV n/a, V ✅ (2888 keys × 3), VI ✅ (AlertDialog WCAG 2.1 AA), VII ✅ (11 metrics + 6 alerts + 7 runbooks + 4 perf benches), VIII ✅ (runInTenantWithRollbackOnErr on all writers; dual-write fallback audit), IX ✅ (solo-maintainer substitute + 7 [Spec Kit] commits in Phase 10), X ✅ (zero new npm deps in Phase 10 — reuses runInTenant + AlertDialog + advisory-lock + verifyCronBearer + observeGauge primitives).
- 011-renewal-reminders: F8 Renewal Tracking + Smart Reminders **SHIPPED via PR #24** (squash `482eb9fa`, merged 2026-05-11). 6 user stories (US1–US6: Pipeline Dashboard / Tier-Aware Smart Reminder Schedule / Member Self-Service Renewal / At-Risk Detection / Auto Tier-Upgrade / Manual Escalation Queue) / 85 enumerated FR/NFR/SC / 64 audit events / 9 F8-owned DB tables / 41 F8-tagged migrations (0086-0123 main + PR #24 deep-review hardenings 0124-0126: trigger search_path + scheduled_plan_changes FK + renewal_cycles expires_at CHECK). New bounded context `src/modules/renewals/`. Lineage: Phase 1-9 (Setup + Foundational + 6 user-story phases + cross-cutting observability) + Phase 10 (5 perf benches + 3 E2E specs + retrospective + 178/181 quality-checklist sweep) + **13 PR #24 review-fix rounds R1-R13 (2026-05-10/11)**: R1 14 issues incl. tier-upgrade money-unit off-by-100x + F4/F5 callback wiring; R2 4 own-regressions; R3 2 own-regressions; R4 deep-review reading actual source 9 issues incl. CRITICAL US3 token-redemption route + HIGH HMAC byte-level timing oracle + cron handler hardening (bearer-auth + tenantId + advisory locks) + portal cross-member probe audit; R5 LOW sweep (expires_at CHECK + spec drift); R6 dynamic-import barrel cold-start ships-dark; R7 F2+F7-unblocked at-risk factors (tier-downgrade + eBlast-quota); R8 /review round 2 deep 1 CRIT (broadcast_deliveries column typo) + 4 HIGH + 3 MED; R9 close R8 accepted-not-fixed; R10 own-regression preConsumeGate ordering; R11 /speckit.qa.run verify-renewal-link-token 100% coverage; R12 /speckit.qa.run close ALL 8 cross-module coverage gaps to 100%; R13 /code-review delta-scan isFirstTimeRenewer self-exclusion-guard MEDIUM. Ships dark behind `FEATURE_F8_RENEWALS=false`. **5 explicit pre-flag-flip operator/human gates**: T269 manual SR QA + T270 cross-browser real-device matrix + T277 maintainer GPG co-sign + T277b cron-job.org dashboard entries (5 cron coordinators) + T282 staging /speckit.qa.run (full checklist in `specs/011-renewal-reminders/retrospective.md` § "Pre-flag-flip operator checklist"). Cron coordinators: 5 (dispatch + at-risk + lapse + reconcile-pending-reactivations + tier-upgrade-evaluate) per `docs/runbooks/cron-jobs.md` lines 38-44. Phase 10 perf benches: T261 loadPipeline p95=291ms @ 1k (under 500ms SLO); T262 cron-dispatch 84.95s @ 1k flagged for Phase 11 batched-write optimization (precedent T159b at-risk batched delivered 38× speedup); T263 at-risk-recompute 7.76s @ 5k strict-mode PASS; T264 evaluateTierUpgrade 1.15s @ 1k (under 30s SLO); T265 confirmRenewal F8-only p95=482ms (under 600ms TTFB SLO). **33 cumulative review rounds** (20 Phase 1-10 + 13 PR #24) closing ~80 findings to 0 BLOCKER/CRITICAL. Smart features shipped: 8-factor at-risk score (4 bands), batched at-risk recompute (P1 innovation pattern), advisory-lock namespacing (P2), F4-callback-rollback pattern (P6), year-in-cycle pill primitive (P7), /code-review delta-scan pattern (P9 — new). Constitution v1.4.0: I/II/III ✅ (NON-NEG verified by 50/50 cross-tenant probes + Principle I sub-clauses), IV n/a (no payment surface — F8 hooks into F4/F5 via bridge ports), V ✅ (2242 keys × EN+TH+SV), VI ✅ (axe-core E2E + reduced-motion), VII ⚡ partial (T262 5k extrapolation finding; non-blocking at SweCham scale ~131 members), VIII ✅, IX ✅, X ✅. Final test counts: ~3214+ unit+contract / ~120+ F8 integration GREEN on live Neon Singapore. Full provenance: `specs/011-renewal-reminders/retrospective.md` (37KB, 482 lines covering Phase 1-10 + PR #24 cycle) + 33 review artifacts + tasks.md (285 tasks, 100% closure rate with 5 deferred-with-rationale = pre-flag-flip operator gates).
- 010-email-broadcast: F7 Email Broadcast (E-Blast) **SHIPPED via PR #23** (squash `43eb842`, merged 2026-05-03). Paid-benefit member dispatch via Resend Broadcasts API. 6 user stories (US1–US6) / 49 FRs / 43 audit events / 4 new DB tables / 22 migrations (0064–0085) / 4 new npm deps (Tiptap + isomorphic-dompurify + email-validator + @tanstack/react-virtual). New bounded context `src/modules/broadcasts/`. Lineage: Phase 1–8 (PR #21 + #22) + Phase 9 cross-cutting (18 OTel metrics + 11 alerts + 5 spans + 5 runbooks + DPIA + processing-records) + Phase 10 quality gates + 9 staff-review rounds (R1–R9 closed 60+ findings) + QA pass + code-review (TZ bug fixed). Ships dark behind `FEATURE_F7_BROADCASTS=false`. 7 stakeholder/human gates pending pre-prod-flag-flip (DPO contact fill, Resend DPA scope, marketing-consent paperwork, T189 chamber TH/SV liaison, T213 staging walkthrough, T215 7d prod RUM, ship-day manual SR for 4 surfaces). 4 cron-job.org endpoints to configure (dispatch-scheduled */5 + reconcile-stuck-sending */15 + prune-expired-drafts daily + broadcasts-gauges */5). 6 UX gaps from chamber-os-ux-architect agent closed in F7 (no F7.1 deferral). 12 F7.1 backlog items remain per spec MVP scope cuts (image embedding, per-contact opt-in, >5k pagination, attachments, etc.). Constitution v1.4.0: I/II/III ✅ (NON-NEG), IV n/a, V/VI/VII (UNVERIFIED SLO = T215)/VIII/IX/X ✅. Final test counts ~3173 unit+contract / 119 F7 integration / 87 E2E green. 1729 i18n keys × EN+TH+SV. Full provenance: `specs/010-email-broadcast/releases/release-20260503-061319.md` + 9 review reports.
- 009-online-payment: F5 Online Payment (Stripe + PromptPay) shipped via PR #16. 6 user stories (US1–US6), 4 new DB tables, 18 migrations (0033–0050), 17 audit event types, `audit_log.retention_years` column with F4 tax-document backfill. Stripe Elements (SAQ-A preserved — zero card data on app server), PromptPay QR via PaymentIntents. Three-layer concurrent-initiate guard (unique index + tenant-filtered FOR UPDATE + per-(tenant,invoice) advisory lock). External cron-job.org for stale-pending-count gauge. 18 OTel metrics + 10 alerts. SLO budgets: initiate p95 <1.2s; webhook succeeded p95 <1000ms dev / <750ms prod (async-receipt-PDF migration 0056 yielded 48.2% p95 reduction). SAQ-A re-attestation pre-ship. EN+TH+SV at release; WCAG 2.1 AA.
- 006-layout-container-tier2 (ad-hoc UI-infra, not canonical F5): three-tier content-type-based layout containers (`TableContainer` 96rem / `FormContainer` 42rem / `DetailContainer` 72rem) replacing single `ContentContainer`. 19 admin + portal routes migrated (each page + loading skeleton). `pnpm check:layout` script enforces every page/loading file imports exactly one container + page/loading pair uses SAME variant (FR-007 CLS-0). Wired into `.husky/pre-push` + full-CI chain. `docs/ux-standards.md` § 18 documents the Container Selection Guideline + Content-Type Mapping table. Legacy `content-container.tsx` + `--content-max-width-{admin,portal}` tokens removed. Bonus fixes: Tailwind v4 `@source not "specs/**" "docs/**"` prevents markdown example strings leaking into generated CSS; restored `mb-[var(--field-label-gap)]` on `Label` primitive (documented in `docs/shadcn-customizations.md` but missing from code); `admin.members.create.fields.phone` i18n dropped "(E.164)" jargon across 3 locales. 100% spec adherence, 0 constitution violations. PR #9.
- 005-members-contacts: F3 Members & Contacts — 7 user stories (US1–US7) + Phase 10 polish shipped across 10 phases. Added `src/modules/members/` bounded context, 2 new DB tables (`members`, `contacts`), migrations 0009+0010, pg_trgm GIN index (SC-002 p95=258ms < 500ms @ 5k rows), RLS+FORCE policies, SECURITY DEFINER trigger, 14/14 tenant-isolation integration tests (Constitution v1.4.0 Principle I Review-Gate), 23 F3 audit event types, TanStack Table v8 directory with server-side search/filter/sort, smart features (timeline, inline+bulk edit, command palette integration), archive+undelete with session/invitation cascade (US7), member self-service portal (US5), 722 i18n keys × 3 locales (EN+TH+SV), WCAG 2.2 AA opportunistic adoption (SC 2.4.11 + SC 2.5.8), full observability § 14 (12 metrics, 6 SLOs, 3 runbooks). Security/UX/a11y checklists 100% complete. Human-gated items pending: T155a manual SR pass, T156 co-sign, T158 staging traces.
- 004-page-layout-standard: F4 Page Layout Enterprise Standardization — PageHeader + ContentContainer + BreadcrumbNav primitives, 11 admin + portal pages migrated, Button 32→36px + cursor/disabled, typography scale (.text-h1–.text-h4 + .text-body + .text-caption) + Thai line-height override, universal focus ring, form/table/overlay token alignment. 12 new i18n keys (breadcrumb.* + layout.*), 24 new unit tests for layout primitives. `docs/shadcn-customizations.md` catalogues every primitive modification.
- 002-membership-plans: F2 Membership Plans — 6 user stories (US1–US6) shipped across 9 phases. Added `cmdk` command palette, `src/modules/tenants/` + `src/modules/plans/` bounded contexts, Postgres RLS tenant isolation, 2 new DB tables (`membership_plans`, `tenant_fee_config`), migrations 0006 + 0007, 268 i18n keys (EN+TH+SV), 495 unit+contract tests, 163 integration tests on live Neon Singapore. US7 (Inline Edit + Bulk Actions) deferred to F3.
- 001-auth-rbac: F1 Auth & RBAC shipped via PR #1. 188/191 tasks, 480/480 tests.

Last updated: 2026-05-21 (F7.1b **Round 7 backlog closure COMPLETE on `014-email-broadcast-advance`** — 7 of 10 backlog items closed (B1+B2+B3+B5+B6+B7-partial+B8) per `~/.claude/plans/atomic-tinkering-otter.md`. B9 dropped (moot). B4+B10+T139-T164 deferred. 938/938 contract+unit GREEN + 11/11 E2E GREEN. KNOWN_BACKLOG shrunk 40→26. Previously: F7.1a Email Broadcast Advanced **Round 6 staff-review closure COMPLETE on `014-email-broadcast-advance`** — ALL 10 staff-review conditions closed (4 architecture warnings → retrospective.md + plan.md Complexity Tracking #5; 3 security conditions → DPIA § 5 + CHK036 flip + R005 documented as F7.1b backlog B4; 3 test conditions → R008 safeAuditEmitTyped 5-test block + R009 perf-bench header rewrite + R010 metric assertion). 691/691 contract+unit GREEN. Staff-review report: `reviews/review-20260521-140000.md`. ZERO deferred items. Cumulative ~80 task closures across 6 rounds. F7.1a is **fully review-clean + staff-review-clean**. Previously: 2026-05-21 Round 5+ LOW-polish (F7.1a Email Broadcast Advanced **Round 5+ LOW-polish closure COMPLETE on `014-email-broadcast-advance`** — LOW2 broadcasts-repo brand sweep 18 sites widened to `TenantSlug` + LOW4 `autoFocus disabled` documented. ZERO remaining deferred items. 686/686 unit+contract + 20/20 cross-tenant probe integration GREEN on live Neon. Cumulative ~70 task closures. F7.1a is **fully review-clean** + READY for `/speckit.staff-review.run` or PR creation. Previously: 2026-05-21 Round 5 (F7.1a Email Broadcast Advanced **Review-Findings Round 5 closure pass COMPLETE on `014-email-broadcast-advance`** — addresses ALL Round 2 review findings: 2 CRITICAL (subjectPreview wired + bracketHint i18n) + 5 HIGH (safeAuditEmit extraContext + accept-partial wrap + metric assertions + perf env wiring + cancel-broadcast H1 migration) + 8 MEDIUM (doc fixes + script hardening + type tightening + assertNever + literal unions + truncate wrapper) + 1 LOW (socket-write guard). Cumulative ~67 task closures across 5 rounds. 341/341 contract+unit + 20/20 cross-tenant probe integration GREEN on live Neon. F7.1a READY for `/speckit.staff-review.run` or PR creation. Previously: 2026-05-21 Round 4 (F7.1a Email Broadcast Advanced **Review-Findings Round 4 "ไม่ defer" closure pass COMPLETE on `014-email-broadcast-advance`**: Simplifier H1-H4 + TD-#3 + 7 LOW polish items closed. ~52 cumulative F7.1a in-session task closures across 4 rounds. F7.1b-scope refactors deferred with documented rationale (H5 BroadcastActionDialog shell · TD-#1 BatchManifest state machine · TD-#2 TxToken brand migration · assertNever at route boundaries). 335/335 contract + 20/20 cross-tenant integration GREEN on live Neon. READY for `/speckit.staff-review.run` or PR creation. Previously: 2026-05-21 Round 3 (F7.1a Email Broadcast Advanced **Review-Findings Round 3 closure pass COMPLETE on `014-email-broadcast-advance`**: ALL 7-agent comprehensive review findings closed — 1 CRITICAL (T128b probe-emit counter), 10 HIGH (H1-H10 race-window + UX + docs), 10 MEDIUM (probe-script + collision rename + empty-list guard + 4 UX gaps + walkTsxFiles logging). Cumulative F7.1a in-session closure: 42 tasks across 3 rounds. 9/9 contract + 20/20 cross-tenant integration GREEN on live Neon. READY for `/speckit.staff-review.run` or PR creation. Previously: 2026-05-21 Round 2 (F7.1a Email Broadcast Advanced **"ทั้งหมดไม่ defer" Round 2 closure pass COMPLETE on `014-email-broadcast-advance`**: 10 additional in-session closures incl. T129 template probe audit-emit + T161 full `--strict-aria` AST-lite scanner + T154/T157/T160 US7 closures + T132/T133 live-Neon integration (20/20 GREEN) + ship-day operator scaffolds (`qa/ship-day-checklist.md` + `qa/sr-qa-template.md` + `qa/f7-mvp-regression-template.md`). Cumulative F7.1a in-session closure: 32 tasks. Remaining ship-day operator gates (T135/T136/T139-T146/T164/T149) fully scaffolded with exact commands but cannot RUN until ship-day. F7.1a READY for `/speckit.verify` gate. Previously: 2026-05-21 Round 1 (F7.1a Phase 6 polish pass COMPLETE on `014-email-broadcast-advance`: 22 in-session tasks closed — 5 OTel metrics + 3 runbooks + 2 cross-tenant probe expansions + manage-image-allowlist defensive probe-emit wiring + 13 checklist-gap doc closures including DPIA addendum + F7 MVP baseline scaffold + f71b promotion-criteria revision. Ship-day operator gates T135/T139-T146/T164 remain. Phase 6 deliverables ready for `/speckit.verify` gate. Previously: 2026-05-19 (F6 EventCreate Integration **SHIPPED + FLAG-FLIP COMPLETE**: PR #26 squash-merged `27433c85` → `FEATURE_F6_EVENTCREATE=true` on production → T154 cron-job.org 4 coordinators configured → T154a Layer 1 (composition root) + Layer 2 (seed + bridge query) both PASS on live Neon Singapore. F6 is live for SweCham tenant. T152 staging walkthrough + T153 SC-005 baseline deferred under Constitution IX solo-maintainer substitute. Full provenance: `specs/012-eventcreate-integration/retrospective.md` § "Post-flag-flip evidence — 2026-05-19" + `scripts/seed-f6-layer2-evidence.ts` evidence script. Synthetic seed (1 event + 2 registrations) persists in production at `F6 T154a Layer 2 evidence` event-name — bounded + quota-neutral + harmless; optional cleanup SQL in retrospective.)
