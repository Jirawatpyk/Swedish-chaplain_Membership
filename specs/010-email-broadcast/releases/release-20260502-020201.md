# F7 US4 Release Archive — 2026-05-02

## PR
- **URL**: https://github.com/Jirawatpyk/Swedish-chaplain_Membership/pull/21
- **Title**: `feat: F7 US4 — public unsubscribe + tenant-scoped suppression`
- **Base**: `main`
- **Head**: `010-email-broadcast`

## Readiness Check (at ship time)

| Check | Result |
|---|---|
| Branch sync | 3 commits ahead of `origin/main`; 0 behind |
| Working tree | Clean |
| US4 tasks (T136–T148) | 13/13 complete |
| Reviews | 3 rounds `/speckit.review` passed; 38 findings closed; 0 deferrals |
| Static checks | typecheck + lint + i18n parity all green |
| Unit + contract tests | 571/571 green |
| Integration on live Neon | 5/5 green |

## Commit range

```
1cd75fb feat: F7 US4 — public unsubscribe + tenant-scoped suppression
0c9162a refactor: F7 US4 — review remediation (code + tests + UX)
3e65f52 refactor: F7 US4 — final review remediation (13 findings)
```

## Changelog entry

### Added
- Public `/unsubscribe/[token]` server-rendered route fulfilling PDPA §24 + GDPR Art. 21 + ePrivacy obligations
- HMAC-SHA256 unsubscribe-token signer + verifier + pre-tenant peek resolver (`UNSUBSCRIBE_TOKEN_SECRET`)
- Tenant-scoped suppression repo (`marketing_unsubscribes` table) with `(tenant_id, email_lower)` PK + idempotent upsert
- Application use-case `unsubscribeRecipient` with dual audit emit (`broadcast_unsubscribed` + `broadcast_suppression_applied`) on first-time only
- Email-template helpers `signUnsubscribeUrl` + `buildListUnsubscribeHeaders` for RFC 8058 one-click + admin direct links
- Anti-enumeration rate limit (20/5min per IP, fail-open per GDPR Art. 21)
- 4 page states (`success` / `already` / `invalid` / `error`) with locale-aware copy + lucide state icons
- Segment-level `error.tsx` last-line-of-defence error boundary
- `TenantSlug` lightweight branded type cross-cutting Domain module (single source of truth across F2–F7)

### Changed
- `TenantContext.slug` is now `TenantSlug` (was plain `string`) — comparison vs `TenantSlug`-typed inputs is apples-to-apples without coercion
- Deleted duplicate `TenantSlug` brand from `src/modules/plans/domain/plan.ts` — F2 + F7 share single nominal type
- `broadcastsMetrics` extended with `auditEmitFailed` counter + `repo_error` + `unhandled_error` outcome labels
- All `broadcastsMetrics` calls wrapped in `safeMetric(...)` — OTel exporter throws cannot bypass page "never throws" contract
- 1630 i18n keys × 3 locales (was 1611 → +19 for US4)

### Fixed
- N/A (greenfield US4)

### Technical Notes
- **Convergent two-surface unsubscribe** (FR-029): Resend body merge tag (`{{{RESEND_UNSUBSCRIBE_URL}}}`) + signed `/unsubscribe/[token]` route both land in `marketing_unsubscribes`
- **Tokens valid forever** (FR-030 + GDPR Art. 21): no expiry; idempotent upsert handles replay
- **Pre-tenant peek + RLS bind + HMAC verify-under-bound-tenant**: narrowest possible RLS-bypass window
- **Static `metadata`** over `generateMetadata`: locale-aware tab title would cost SLO-F7-006 < 400ms perf budget

## Linked artefacts

- Spec: `specs/010-email-broadcast/spec.md` (FR-029 → FR-032 + AS1–AS6)
- Plan: `specs/010-email-broadcast/plan.md` (Complexity Tracking entries for FR-029 body-link + rate-limit fail-open)
- Tasks: `specs/010-email-broadcast/tasks.md` T136–T148 (all flipped `[X]`)
- PR description: `specs/010-email-broadcast/releases/pr-description-20260502-020201.md`
- Reviews: `specs/010-email-broadcast/reviews/review-20260430-225120.md` + 3 in-session multi-agent rounds (38 findings closed)

## Out of scope

- US6 — scheduled future-dated send via cron (T164–T171a)
- Phase 9 — cross-cutting observability T172+ (full FR-035 metrics catalogue)
- F7.1 — `<img>` allowlist + per-recipient send loop

## Generated by
- `/speckit.ship.run` — 2026-05-02 02:02 +07:00
- Operator: Jirawatpyk
