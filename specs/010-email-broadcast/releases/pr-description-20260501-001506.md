## Summary

F7 — **Email Broadcast (E-Blast)** MVP slice (US1 + US2) — Chamber-OS member compose+submit + admin review queue powered by **Resend Broadcasts API**. Members compose rich-text broadcasts to segment-based recipient lists (`all_members`, `tier:<code>`, `event_attendees_last_90d`, `custom`) under a per-plan annual quota; admins review the queue, approve (send-now or schedule), reject (with required reason), or cancel. Includes Q12 admin-on-behalf-of-member submission, Q14 halt-state banner, Q15 GDPR Art. 7 acknowledgement banner, 48-hour SLA tracking, strict-allowlist HTML sanitisation (FR-002a), 5,000 recipient cap (FR-016a), tenant-graph custom-list validator (FR-015d), and full RLS+FORCE multi-tenant isolation per Constitution v1.4.0 Principle I.

**Scope**: MVP slice only — US3 (member quota dashboard), US4 (delivery webhook + auto-halt), US5 (public unsubscribe), US6 (scheduled-send polish) explicitly deferred to Phase 5–8 per Ultraplan; documented in `releases/release-20260430-mvp-slice.md` § "Known limitations".

## Specification

- [`spec.md`](specs/010-email-broadcast/spec.md) — 6 user stories · 49 FRs (30 in MVP scope) · 11 success criteria · 12 clarifications across 4 sessions
- [`plan.md`](specs/010-email-broadcast/plan.md) — Constitution Check GREEN · 13 Complexity Tracking entries (Webhook Node runtime, p95 budget exceptions, RLS pre-tenant bypass, new deps, F6 stub-port pattern, Tiptap, dedicated unsubscribe secret, cron-job.org trigger, solo-maintainer substitute)
- [`tasks.md`](specs/010-email-broadcast/tasks.md) — 129/129 MVP tasks complete · 95 explicit US3–US6 deferrals
- [`research.md`](specs/010-email-broadcast/research.md) · [`data-model.md`](specs/010-email-broadcast/data-model.md) · [`contracts/`](specs/010-email-broadcast/contracts/) · [`security.md`](specs/010-email-broadcast/security.md)

## Implementation

### New bounded context: `src/modules/broadcasts/`
- **Domain**: `Broadcast`, `BroadcastDelivery`, `MarketingUnsubscribe`, `RecipientSegment` aggregates + `QuotaCounter` + `EmailLower` value objects
- **Application** (15 use-cases): `save-draft`, `submit-broadcast`, `sanitize-html`, `validate-custom-recipients`, `resolve-segment-recipients`, `approve-broadcast`, `reject-broadcast`, `cancel-broadcast`, `proxy-submit-broadcast`, `dispatch-scheduled-broadcast`, `compute-quota-counter`, `enforce-tenant-context`, `clear-halt`, plus 2 internal helpers
- **Infrastructure**: Resend Broadcasts gateway, DOMPurify sanitiser adapter, RFC 5321 email validator, F3+F2 bridge adapters, EventAttendees stub-port (F6 swaps in real impl), HMAC unsubscribe-token signer, audit emitter

### Database
- 4 new tables: `broadcasts`, `broadcast_deliveries`, `marketing_unsubscribes`, `broadcast_segment_definitions` — all RLS+FORCE
- 2 new columns on F3 `members`: `broadcasts_halted_until_admin_review` (Q14) + `broadcasts_acknowledged_at` (Q15)
- 37 new audit event types (5-year retention; no F4 tax-document overlap)
- Migrations 0064–0076 (incl. ship-blocker fix `0076_audit_log_f7_audience_drift.sql` + `_journal.json` backfill)
- Cron dispatch concurrency: `SELECT FOR UPDATE SKIP LOCKED` + `pg_advisory_xact_lock(hashtextextended('broadcasts:'||tenantId||':'||broadcastId, 0))` — disjoint lock namespace from F4/F5

### Routes
- Member: `/portal/broadcasts/new` (compose) + `/api/broadcasts/{draft,submit,quota}` + `/api/broadcasts/[id]/cancel`
- Admin: `/admin/broadcasts` (queue) + `/admin/broadcasts/[id]` (detail) + `/api/admin/broadcasts/*` (9 routes)
- Webhook: `/api/webhooks/resend-broadcasts` (Svix HMAC, Node runtime — same constraint as F5)
- Cron: `/api/cron/broadcasts/dispatch-scheduled` (5-min cadence via cron-job.org Bearer auth — Hobby plan compatible)

### Dependencies
- `@tiptap/react@3.22.5` + `@tiptap/starter-kit@3.22.5` (exact-pinned per OWASP A06)
- `isomorphic-dompurify@2.36.0` (exact-pinned)
- `email-validator@^2`
- `@tanstack/react-virtual@^3` (CHK039 admin queue virtualization >100 rows)

### Infra uplift bundled in this PR
- **Node 22 LTS adoption** (engines bumped, pnpm overrides cleared) — removed all 4 layers of the original isomorphic-dompurify ESM workaround
- **Cross-browser E2E coverage** — chromium + mobile-chrome + mobile-safari (87/87 pass)

## Testing

```
Unit              17 files    234 tests   pass
Contract           7 files    102 tests   pass
Integration        7 files     65 tests   pass  (live Neon Singapore)
E2E                6 files     87 tests   pass  (3 projects × 29, --workers=1)
─────────────────────────────────────────────
Total F7          37 files    488 tests   pass / 0 todo / 0 fixme / 0 skip
```

- 100% branch coverage on security-critical surfaces (sha256 reason hashing, halt-flag precondition, dual-actor proxy submission)
- 14 cross-tenant isolation probes (Constitution v1.4.0 Principle I Review-Gate blocker)
- Static gates green: `typecheck` ✅ · `lint` ✅ (0 warnings) · `check:i18n` ✅ (1,554 keys × 3) · `check:layout` ✅ (66 pairs)
- Full project unit suite green: 207 files / 2,375 tests / 0 fail (F1+F2+F3+F4+F5+F7)

## Review Notes

**6 prior remediation passes** closed 111 cumulative findings:
- 4× `/speckit.review` rounds (`critiques/critique-20260429-{074517,081224-round2,084621-round3,091030-round4-final}.md`)
- 1× `/speckit.verify` LOW remediation
- 1× ship-blocker fix (DB↔TS audit-event enum drift, migration 0076 + journal backfill)
- 1× staff-review (`reviews/review-20260430-225120.md`) → S002–S006 RESOLVED

**Outstanding conditional gate**:
- 🟡 **S001 — Chamber TH/SV liaison sign-off on SV strings** (256 keys auto-translated EN→SV during round-4; 0 `[F7-SV-REVIEW]` placeholders remain). This is a **content-quality gate**, not a code defect. Ship procedure: hold merge until liaison confirms SV copy quality.

**Architectural posture (verified vs Constitution v1.4.0)**:
- I (tenant isolation, NON-NEGOTIABLE): RLS+FORCE on all 4 tables + 14 cross-tenant probes ✅
- II (TDD, NON-NEGOTIABLE): 100% branch on security-critical paths ✅
- III (Clean Architecture, NON-NEGOTIABLE): module barrel + ESLint guard + F1 audit-log stays internal ✅
- IV (PCI DSS): N/A — no payment surface ✅
- V–X (i18n, UX, perf, reliability, code quality, simplicity): all green per release record

## Checklist

- [x] All MVP-scope tasks completed (129/129)
- [x] Code review passed — 5 `/speckit.review` rounds + 1 `/speckit.staff-review` (verdict: APPROVED WITH CONDITIONS)
- [x] QA testing — `tests/e2e/` 87/87 pass on 3 Playwright projects
- [x] Local CI gates green (no GitHub Actions configured for this repo — Vercel deploys on merge)
- [x] Changelog/release record updated (`releases/release-20260430-mvp-slice.md`)
- [ ] **Chamber TH/SV liaison sign-off on SV strings** — gating final merge
- [x] Documentation updated (CLAUDE.md "Active Technologies" + "Recent Changes" entries)

## Migration / Operational Notes

1. **Apply DB migrations first**: `pnpm db:migrate` (0064–0076 — verified clean against journal-synced state)
2. **Set new env vars on Vercel** before flipping kill-switch:
   - `RESEND_BROADCASTS_API_KEY` (re_-prefixed)
   - `RESEND_BROADCASTS_WEBHOOK_SECRET` (≥32 bytes)
   - `UNSUBSCRIBE_TOKEN_SECRET` (≥32 bytes, distinct from `AUTH_COOKIE_SIGNING_SECRET`)
   - `BROADCASTS_FROM_EMAIL` (e.g. `"SweCham <noreply@zyncdata.app>"`)
3. **Configure cron-job.org** for `/api/cron/broadcasts/dispatch-scheduled` per `docs/runbooks/cron-jobs.md` (5-min cadence, Bearer auth via shared `CRON_SECRET`)
4. **Wire pre-prod alerts** before first prod dispatch — see `docs/runbooks/broadcasts-dispatch-failure.md` § "Pre-prod alert wiring checklist"
5. **Flip kill-switch**: `FEATURE_F7_BROADCASTS=true` (currently `false` shipping dark on main)
6. **Resend Broadcasts webhook endpoint**: register at Resend console pointing to `https://swecham.zyncdata.app/api/webhooks/resend-broadcasts` (Svix signing scheme)

## Commit Range

`origin/main..HEAD` — **26 commits** across spec → plan → tasks → 4-phase implementation → 6 remediation passes → Node 22 + cross-browser uplift.

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
