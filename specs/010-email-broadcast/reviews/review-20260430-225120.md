# Code Review Report: F7 Email Broadcast — MVP slice (US1+US2)

**Reviewer**: AI Agent (Staff Engineer Perspective, `/speckit.staff-review`)
**Date**: 2026-04-30
**Feature**: [`specs/010-email-broadcast/spec.md`](../spec.md)
**Branch**: `010-email-broadcast`
**Commits ahead of `main`**: 22
**Last commit**: `9ef6689 [Spec Kit] F7 — backfill _journal.json with migrations 0074/0075/0076`
**Verdict**: ⚠️ **APPROVED WITH CONDITIONS**

---

## Executive Summary

F7 ships a complete MVP slice (US1 member compose+submit + US2 admin review queue) with mature engineering posture: **6 prior remediation passes** (5× `/speckit-review` rounds + 1× `/speckit.verify` LOW remediation) closed **111 cumulative findings**, including 1 ship-blocker (DB↔TS audit-event enum drift, fixed via migration 0076 + journal backfill in commits `12baa31` + `9ef6689`). All static gates are green at submission: `typecheck` ✅, `lint` ✅ (0 warnings), `check:i18n` ✅ (**1,554** keys × EN/TH/SV), `check:layout` ✅ (66 page/loading pairs). Test posture is exceptional — **37 files / 430 tests / 0 todo / 0 fixme / 0 skip** (234 unit + 102 contract + 65 integration on live Neon Singapore + 29 E2E on chromium). Constitution v1.4.0 alignment is clean: Principle I (tenant isolation) ships RLS+FORCE on all 4 F7 tables with 14 cross-tenant probe tests; Principle II (TDD) achieves 100% branch coverage on security-critical surfaces (sha256 reason hashing, halt-flag precondition, dual-actor proxy submission); Principle III (Clean Architecture) is enforced by ESLint barrel guard; Principle X (i18n) ships all 3 locales at release. The **single conditional gate** is the explicit `[F7-SV-REVIEW]` Swedish placeholder review by the chamber liaison — flagged in the release record as the documented `/speckit.ship`-gate dependency. No new blockers found in this pass.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| S001 | 🟡 Warning | `src/i18n/messages/sv.json` | (256 keys) | Spec Compliance | F7 Swedish strings ship as `[F7-SV-REVIEW]`-tagged auto-translations from EN→SV map; chamber liaison review is the documented `/speckit.ship`-gate dependency, not a code defect. Per Constitution X (i18n) the keys exist and `check:i18n` is green, so this is a **content-quality** condition, not a structural blocker. | Hold `/speckit.ship` until chamber TH/SV liaison signs off on SV strings. EN+TH are production-ready. |
| S002 | 🟡 Warning | `docs/runbooks/f7-dompurify-esm-workaround.md` | — | Architecture | 4-layer DOMPurify ESM workaround (dynamic import + lazy require + pnpm overrides + `serverExternalPackages`) carries upstream-tracking debt. Documented but no GitHub issue link or expected-removal milestone. | Add an upstream-tracking issue link + revisit checkpoint (e.g. `f7.next` review) to prevent the workaround from becoming permanent technical debt. Non-blocking for ship. |
| S003 | 🟡 Warning | `specs/010-email-broadcast/tasks.md` | — | Spec Compliance | 95 of 224 tasks remain `[ ]` — these are explicitly US3–US6 deferrals per Ultraplan. Release record (`releases/release-20260430-mvp-slice.md`) documents the deferral, but `tasks.md` itself doesn't visually mark the post-MVP cutoff. | Optionally add a `## ── MVP slice ends here ──` separator in `tasks.md` so future readers don't mistake the deferred tasks for incomplete in-scope work. Cosmetic; non-blocking. |
| S004 | 🟢 Suggestion | `drizzle/migrations/meta/_journal.json` | idx 74–76 | Correctness | Journal entries 74/75/76 were backfilled with synthetic `when` timestamps (`1779235200000`, `1779235260000`, `1779321600000`) to match a manual `sql.unsafe()` apply. Future reviewers may not realise the journal was hand-edited. | Add a one-line comment in the F7 release record under "Fixed" describing the journal-backfill rationale (already partly documented in commit `9ef6689` body — fine as-is, but cross-referencing helps audit). |
| S005 | 🟢 Suggestion | `src/modules/broadcasts/application/use-cases/dispatch-scheduled-broadcast.ts` | replay path | Observability | The `broadcast_resend_drift_check_unverifiable` audit + `severity:critical` logger.error is correctly emitted on un-verifiable replay (R5-S1 fix). Worth confirming there is a corresponding alert rule firing on `broadcasts.drift_check_unverifiable.count > 0` in production Grafana. | Verify the alert listed in `docs/observability.md` (added during round-5) is actually wired to the prod alerting backend before first prod dispatch. |
| S006 | 🟢 Suggestion | `tests/e2e/global-setup.ts` | — | Test Quality | E2E suite runs on chromium only at `--workers=1` (per `MEMORY.md` workstation constraint). Cross-browser is intentionally deferred per F7 plan, but no docs cite the per-feature decision. | Cross-link the chromium-only decision in F7 plan § Complexity Tracking → defers to project-wide policy. Non-blocking; prevents future "why is this chromium-only?" question. |

**Note on absence of 🔴 Blockers**: Five remediation rounds + ship-blocker fix already closed all blocker-class findings. The latest verify-run result (`releases/release-20260430-mvp-slice.md` row 18) is **0 CRITICAL / 0 HIGH / 0 MEDIUM / 3 LOW (G1+G2+H1 — all addressed)**. This staff-review pass confirms no new blockers were introduced by the round-5 remediation commits (`12baa31`, `9ef6689`).

---

## Spec Coverage Matrix (49 FRs in `spec.md`)

| Slice | FRs | Status |
|---|---|---|
| **MVP slice (US1+US2)** — submit/draft/cancel + admin review queue + halt + dual-actor + sanitisation + quota + recipient-cap + custom-list validator + 5,000 cap + state-machine + 48h SLA + cron-dispatch foundation | FR-001…FR-008, FR-013–FR-017, FR-026–FR-030, FR-038–FR-042 + 6 amendments (FR-002a/004a/015a/015c/015d/016a) | ✅ **Implemented** — 100% per release record |
| **Deferred to Phase 5+ (US3–US6)** — quota dashboard + history + delivery webhook tracking + per-broadcast complaint auto-halt + public unsubscribe page + scheduled-send polish | FR-009–FR-012, FR-018–FR-025, FR-031–FR-037 | 🚧 **Deferred** — explicitly out of MVP scope per Ultraplan; documented in `tasks.md` and release record |

**MVP slice coverage**: **30/30 in-scope FRs implemented (100%)**. **Whole-feature coverage**: 30/49 (61%) — remainder is documented post-MVP scope.

---

## Test Coverage Assessment

| Area | Tests Exist? | Coverage | Gaps |
|---|---|---|---|
| Domain (state machine, branding, quota, invariants, email-lower) | ✅ | 100% line per Constitution II | None |
| Application use-cases (15 use-cases) | ✅ | 234 unit tests; 100% branch on security-critical surfaces (sha256 reason, halt precondition, dual-actor) | None — Wave-6 filled all 92 prior `it.todo()` placeholders with real assertions |
| Infrastructure (Resend gateway, sanitiser, custom-validator, members-bridge, route-helpers) | ✅ | Full happy + error paths | None |
| API contracts (8 routes) | ✅ | 102 contract tests | None |
| Integration (live Neon Singapore) | ✅ | 65 tests across audience-cap, custom-recipient, event-attendees stub, halt-flag precondition, html-sanitiser, kill-switch, **tenant-isolation 14×** | None |
| E2E (chromium only) | ✅ | 29 tests across 6 suites — admin review queue, member compose/submit, draft restore, empty segment, quota block, rate limit | Cross-browser firefox/webkit deferred (project-wide policy) |
| Cross-tenant isolation (Constitution I Review-Gate blocker) | ✅ | 14 cross-tenant probes across all 4 F7 tables (4×4 surfaces) | None |
| **Runtime skips/todos** | n/a | **0** | None — `tests/e2e/global-setup.ts` ensures clean run |

---

## Metrics Summary

| Metric | Value |
|---|---|
| Files reviewed | 250 changed (vs `main`) |
| Lines added / removed | 41,577 / 27 |
| 🔴 Blockers | **0** |
| 🟡 Warnings | 3 (S001 SV liaison, S002 dompurify debt, S003 tasks.md cosmetic) |
| 🟢 Suggestions | 3 (S004 journal note, S005 alert verify, S006 chromium docs) |
| Spec coverage (MVP slice) | **100%** (30/30 FRs) |
| Spec coverage (whole feature) | 61% (US3–US6 explicitly deferred) |
| Test count | **430** (0 todo / 0 fixme / 0 skip) |
| Static gates | typecheck ✅ · lint ✅ (0 warnings) · check:i18n ✅ (1,554 × 3) · check:layout ✅ (66 pairs) |
| Constitution v1.4.0 alignment | I ✅ · II ✅ · III ✅ · IV n/a · V ✅ · VI ✅ · VII ✅ · VIII ✅ · IX ✅ (solo-maintainer substitute) · X ✅ |
| Prior review rounds | 5 (`critique-20260429-074517` … `critique-20260429-091030-round4-final`) + 1 verify-run + 1 ship-blocker = 6 remediation passes |
| Cumulative findings closed | 111 |

---

## Recommended Actions

### Must Fix (Blockers)
None. ✅

### Should Fix (Warnings) — gating `/speckit.ship`
1. **S001 — SV liaison sign-off** — schedule chamber TH/SV liaison review of the 256 `[F7-SV-REVIEW]` strings in `src/i18n/messages/sv.json`. EN+TH are production-ready; SV is the only locale gate.
2. **S002 — DOMPurify upstream tracking** (≤30 min) — open a GitHub issue link in `docs/runbooks/f7-dompurify-esm-workaround.md` so the 4-layer workaround has a removal path.
3. **S003 — `tasks.md` cosmetic separator** (≤5 min) — add `## ── MVP slice ends here ──` between completed and deferred tasks.

### Nice to Fix (Suggestions)
1. **S004** — note journal backfill in release record's "Fixed" section.
2. **S005** — verify the new `broadcasts.drift_check_unverifiable.count` alert is wired to prod alerting backend.
3. **S006** — cross-link the chromium-only E2E decision in plan § Complexity Tracking.

---

## Verdict Rationale

**⚠️ APPROVED WITH CONDITIONS** — F7 MVP slice is technically ready to ship with the following gating conditions:

1. **Hard gate (SV liaison)**: Do not merge to `main` without chamber TH/SV liaison sign-off on the 256 SV placeholder strings. This is the *only* condition that blocks production deployment.
2. **Soft gates (tracking)**: S002 + S004 + S005 are non-blocking but should be cleared in the first follow-up PR after F7 ships, ideally before US3 starts.

The implementation has been through more remediation rigor (6 passes, 111 findings closed) than the median feature in this codebase, the test suite is comprehensive (430 tests, 0 skips), tenant isolation is verified end-to-end (14 cross-tenant probes), and all static gates are green. The remaining warnings are content-quality and tracking debt, not structural defects. **This is a ship-ready feature pending the chamber liaison review.**

---

## Post-Review Next Steps

```bash
# 1. Ensure chamber liaison sign-off on SV strings
#    (manual gate — not scriptable)

# 2. Once SV approved, optionally apply S002+S003 polish in a single follow-up PR

# 3. Then proceed:
/speckit.ship
```

---

*Generated by `/speckit.staff-review` — Staff-level code review for spec-driven development.*
*Prior reviews: `specs/010-email-broadcast/critiques/` (rounds 1–4) + `specs/010-email-broadcast/audit-report-2026-04-29.md` + `specs/010-email-broadcast/releases/release-20260430-mvp-slice.md`.*
