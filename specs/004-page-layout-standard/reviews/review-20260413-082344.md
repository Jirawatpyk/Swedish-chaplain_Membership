# Code Review Report: F4 — Page Layout Enterprise Standardization

**Reviewer**: AI Agent (Staff Engineer Perspective)
**Date**: 2026-04-13
**Feature**: [spec.md](../spec.md)
**Branch**: `004-page-layout-standard`
**Base**: `main`
**Commits under review**: `b8ef7ec`, `483d56d`, `1d2be16` (3 commits, +4038 / −204, 69 files)
**Verdict**: ⚠️ **APPROVED WITH CONDITIONS**

---

## Executive Summary

F4 delivers a complete presentation-layer foundation — three layout primitives (`PageHeader`, `ContentContainer`, `BreadcrumbNav`), ~30 CSS design tokens, 7 shadcn primitive updates, and migration of all 11 admin + portal pages — at a quality level well above what a presentation-only feature usually earns. The code is clean, boundaries are respected (RSC by default, client islands only where Context demands), tenant/auth surfaces are untouched, and there is zero new attack surface. 25 layout unit tests and two rounds of automated review have already shaken out the usual defects; the residual issues are narrow. The only item that prevents a clean ✅ is one concrete SC-010 gap on the plan-detail page (three section `<h2>`s still use `text-sm`, which the typography E2E test silently skips via a `text-h{N}` class filter). That is a minor technical-debt ticket, not a ship blocker, and the remaining warnings + suggestions are polish-level.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| R001 | 🟡 Warning | `src/app/(staff)/admin/plans/[year]/[planId]/page.tsx` | 188, 209, 226 | Spec Compliance (SC-010 / FR-017) | Three section `<h2>` elements use `className="text-sm font-semibold uppercase …"` instead of a F4 typography utility (`.text-h{N}` / `.text-caption`). SC-010 requires every `<h2>`/`<h3>`/`<h4>` on migrated pages to match FR-017 tokens. | Pick one: (a) relabel as `<h3 className="text-caption uppercase tracking-wide font-semibold">` to preserve the small-uppercase label look while keeping a token-driven size; (b) promote to `.text-h4` (18px) if a true section heading is intended; or (c) keep as-is but log an explicit SC-010 deviation in `plan.md` Complexity Tracking so future readers know the test filter is intentional, not an oversight. |
| R002 | 🟡 Warning | `tests/e2e/typography-scale.spec.ts` | 49–52 | Test Quality | The test silently skips `<h{N}>` elements that don't already carry a `text-h{N}` class. That makes the test cosmetic: any heading still using ad-hoc utilities passes by invisibility rather than by meeting the spec. | Either (a) flip the logic — fail if an `<h2>`/`<h3>`/`<h4>` element is missing a `text-h{N}` class on a migrated page (enforces SC-010 literally); or (b) keep the filter but add a second assertion that fails if any page under `PAGES` contains `<h{2..4}>` without a `text-h*` class. Pair with R001. |
| R003 | 🟡 Warning | `src/app/globals.css` | 291–304 | Dead Code / Clarity | `.focus-ring` utility is defined with a focus-visible variant but is never used anywhere under `src/`. Meanwhile the real universal focus ring is implemented twice: once as the global `*:focus-visible { outline: 2px solid currentColor }` rule (line 361–365) and once via `focus-visible:ring-*` inside shadcn components. The three implementations drift. | Either (a) adopt `.focus-ring` on the primitives that currently roll their own `focus-visible:` Tailwind classes and delete the fallback global rule; or (b) delete `.focus-ring` outright — the `*:focus-visible` + per-component ring classes already satisfy SC-011. Either simplifies the review surface; keeping all three invites divergence. |
| R004 | 🟡 Warning | `src/app/globals.css` vs `src/components/ui/button.tsx` | global `*:focus-visible` uses `currentColor`; Button uses `focus-visible:ring-ring/50` | Architectural Consistency (SC-011) | SC-011 says "focus ring of **identical** computed outline / box-shadow" across 5 pages. In practice Button renders a `box-shadow: 0 0 0 3px var(--ring)/50` while Link or native elements render `outline: 2px solid currentColor`. The E2E test was relaxed to accept "either a box-shadow or an opaque outline" (see R005), so the assertion holds, but the visual result is not identical. | Converge on one pattern. Easiest: drop the global `*:focus-visible` rule and move all primitives to `focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50` (Button pattern). Record the decision in `docs/shadcn-customizations.md`. |
| R005 | 🟢 Suggestion | `tests/e2e/focus-ring.spec.ts` | 47–65 | Test Quality | The assertion correctly rejects "transparent outline + no box-shadow" (good round-2 fix). But it still passes on a single box-shadow OR a single opaque outline, not on the *combined* WHCM-compatible pattern that `.focus-ring` was designed to produce. This is consistent with the current implementation (see R004) — it becomes a problem only once that inconsistency is resolved. | Defer until R004 is resolved. At that point the assertion can tighten to `hasBoxShadow && hasTransparentOutlineOrOpaqueOutline` to verify both the visual ring and the WHCM fallback are present. |
| R006 | 🟢 Suggestion | `src/components/layout/breadcrumb-provider.tsx` | 40–47 | Correctness (edge case) | `setLabel` short-circuits on duplicate values via `prev.get(segment) === label` — correct for strings. If a caller ever passes a non-primitive label (e.g. a `ReactNode`), the equality check will compare identities and force a state update on every render. Today the only caller (`PlanBreadcrumbLabel`) passes a `string`, so the risk is latent. | Either (a) restrict the API type to `(segment: string, label: string) => void` (it already is — leave a code comment noting the short-circuit relies on primitive equality); or (b) if you widen to `ReactNode` later, swap the short-circuit to a deep-key check or drop it. |
| R007 | 🟢 Suggestion | `src/components/layout/breadcrumb-path.ts` | 27–40 | Correctness | `parseBreadcrumbPath` builds `href` via `parts.slice(0, index + 1).join('/')` AFTER `safeDecode` ran on each part. If a segment contains a literal `/` after decoding (e.g. a pathological URL-encoded value), the reconstructed `href` is wrong. | Two options: (a) keep the original raw URL segment for `href` construction and use the decoded value only for the `label` lookup; or (b) `encodeURIComponent` each decoded part when rebuilding `href`. Option (a) preserves the user's original URL exactly. |
| R008 | 🟢 Suggestion | `src/app/__test__/button-matrix/page.tsx` | 7–8, 38 | Security / Safety | Solid defense-in-depth (`ALLOW_TEST_ROUTES` env + `dynamic = 'force-dynamic'` + `notFound()`). Minor gap: the route still renders on dev without auth — so anyone with local dev access can browse the matrix. Intentional for authoring tests, but worth calling out explicitly in the comment. | Tighten the top comment to "Dev/CI-only, **unauthenticated** by design — never enable `ALLOW_TEST_ROUTES=1` in an environment with real session cookies." No code change required. |
| R009 | 🟢 Suggestion | `src/components/ui/table.tsx` | 22–30 | Performance / UX | `TableHeader` is `sticky top-0` with `z-10`. When a Table sits inside a scrollable container that is itself inside a shorter viewport, sticky behavior can surprise (header appears to "jump" if the parent container already scrolls). | Add a short note in `docs/shadcn-customizations.md` that sticky header assumes the Table lives in a full-page scroll context (the default). If anyone puts a Table inside a fixed-height card, they can opt out with `className="[&_thead]:static"`. |
| R010 | 🟢 Suggestion | `eslint.config.mjs` | 182, 190, 198 | Rule Robustness | The `no-restricted-syntax` selector is triplicated (FunctionDeclaration, ArrowFunction block body, ArrowFunction expression body) with identical regexes. That's fine today but drifts easily when the forbidden-class list changes. | Hoist the regex to a named constant and reuse across the three selectors: `const AD_HOC_LAYOUT_CLASS = /(^|\\s)(max-w-|mx-auto|container(\\s|$)|p-\\d|px-\\d|py-\\d|space-y-|text-(xl\|2xl\|3xl\|4xl))/` then reference it in each selector's value-regex. Zero-risk refactor. |
| R011 | 🟢 Suggestion | `src/app/(staff)/admin/layout.tsx` vs `src/app/(member)/portal/layout.tsx` | admin 66–71, portal 50–52 | Architectural Consistency | The two shells place `ContentContainer` differently: portal layout wraps `{children}` in `<ContentContainer variant="portal">`, so portal pages themselves do not use ContentContainer; admin layout does not wrap children, so every admin page must import and render ContentContainer itself. The result works but is a shape mismatch future contributors will trip over. | Document the rationale in `docs/shadcn-customizations.md` (admin needs BreadcrumbNav between the top bar and the container, portal doesn't — so admin pages own the container for breadcrumb placement). Alternatively, move BreadcrumbNav **inside** a shared `<AdminPageShell>` component that wraps ContentContainer and have admin pages drop straight into it — mirrors portal and removes the boilerplate from every page. |
| R012 | 🟢 Suggestion | Build manifest | — | Observability | 32 of the 92 tasks in `tasks.md` are E2E/QA/visual-regression items that require `pnpm test:e2e` against seeded data + manual cross-viewport verification. The PR will ship without those results in the merge log. | Call this out explicitly in the Ship gate PR description (which of the 32 deferred items are verified manually before merge vs. post-ship). The prior F3 retrospective flagged a similar pattern as P-1; worth pre-empting for F4. |

**Categories**: Correctness, Security, Performance, Spec Compliance, Error Handling, Test Quality, Architecture, Dead Code

---

## Spec Coverage Matrix

| Requirement | Status | Implementation Notes |
|-------------|--------|---------------------|
| FR-001 PageHeader component | ✅ Implemented | `src/components/layout/page-header.tsx` RSC, title/subtitle as ReactNode, badge + actions slots, CSS logical properties. |
| FR-002 ContentContainer (72rem admin) | ✅ Implemented | `content-container.tsx` with `MAX_WIDTH_BY_VARIANT` lookup; E2E asserts computed `max-width: 1152px`. |
| FR-003 No ad-hoc utility classes on page roots | ✅ Implemented | ESLint `no-restricted-syntax` with 3 selectors covering FunctionDeclaration + ArrowFunctionExpression block + expression body. |
| FR-004 Usable at 320px | ✅ Implemented | Design tokens + `layout-responsive.spec.ts`. |
| FR-005 Breakpoints 640/768/1024 | ✅ Implemented | Tailwind defaults + `page-header-wrap.spec.ts`. |
| FR-006 Breadcrumb nav | ✅ Implemented | `breadcrumb-nav.tsx` + `breadcrumb-path.ts` with MIN_DEPTH=3 rule. |
| FR-007 Mobile breadcrumb truncation | ✅ Implemented | `truncateForMobile` keeps last 2 segments + ellipsis; `sm:hidden` / `hidden sm:flex` gate. |
| FR-008 Portal 64rem variant | ✅ Implemented | `variant: 'portal'` wires to `--content-max-width-portal: 64rem`. |
| FR-009 Compose shimmer/empty-state as children | ✅ Implemented | No built-in slot API (correct per spec clarification). |
| FR-010 Design tokens, no magic numbers | ✅ Implemented | All 30 tokens in `globals.css`; `src/components/layout` has zero pixel literals. |
| FR-011 i18n keys for breadcrumb/layout | ✅ Implemented | 12 keys × 3 locales (EN/TH/SV) = 36 translations. |
| FR-012 No a11y regressions | ✅ Implemented (scaffolded) | `layout-a11y.spec.ts` present; pending E2E execution. |
| FR-013 CSS logical properties | ✅ Implemented | `padding-inline`, `margin-block-end` used in PageHeader + admin shell header. |
| FR-014 Button cursor + disabled + 36px | ✅ Implemented | `button.tsx` size=default → `h-9`, `cursor-pointer`, `disabled:cursor-not-allowed`. |
| FR-015 Same for other triggers | ✅ Implemented (by inheritance) | Global `button, [role="button"], a[href] { cursor: pointer }` in globals.css. |
| FR-016 Top bar consistency (56px) | ✅ Implemented | `--top-bar-height: 3.5rem` applied on both admin + portal layout headers. |
| FR-017 Typography scale | ⚠️ Partial | Classes defined + Thai override in place. 3 residual `<h2 className="text-sm">` on plan detail page bypass the scale (R001). |
| FR-018 Universal focus ring | ⚠️ Partial | Works but three co-existing implementations (`*:focus-visible`, `.focus-ring` utility, `focus-visible:ring-*` on primitives) diverge (R003, R004). |
| FR-019 Form-field 36px + gap | ✅ Implemented | `input`, `textarea`, `select`, `label` all aligned to `--input-height`. |
| FR-020 Table tokens + sticky header | ✅ Implemented | `h-[var(--table-row-height)]`, `px-[var(--table-cell-padding-x)]`, sticky thead, `focus-within` mirrors hover. |
| FR-021 Card padding/radius/shadow | ✅ Implemented | Card uses `--card-padding/radius/shadow`; dark mode has its own `--card-shadow` override. |
| FR-022 Modal/Sheet/AlertDialog tokens | ✅ Implemented | `--modal-max-width-{sm,md,lg}`, `--modal-duration`, `--modal-easing` consumed by dialog/alert-dialog/sheet. |
| FR-023 DropdownMenu triggers use `variant="ghost"` | ✅ Implemented (audit only) | Existing call sites verified by grep; no code change needed. |

**Coverage**: 21/23 fully implemented, 2/23 partial (FR-017 via R001, FR-018 via R003+R004). **91.3% fully implemented, 100% present**.

---

## Test Coverage Assessment

| Area | Tests Exist? | Coverage | Gaps |
|------|-------------|----------|------|
| `breadcrumb-path.ts` (parse + truncate) | ✅ | 9 unit tests | — |
| `breadcrumb-provider.tsx` (Context + dev-throw) | ✅ | 5 unit tests | — |
| `content-container.tsx` | ✅ | 5 unit tests | — |
| `page-header.tsx` | ✅ | 6 unit tests | — |
| FR-003 ad-hoc utility class blocker | ✅ (ESLint) | Static check at lint time | — |
| SC-001 page-shell composition on 11 pages | ✅ scaffolded | `layout-consistency.spec.ts` | Requires seeded DB + `E2E_ADMIN_*` env to execute. |
| SC-002 responsive 320/375/768/1024/1440 | ✅ scaffolded | `layout-responsive.spec.ts` | Execution pending. |
| SC-004 breadcrumb depth ≥ 3 | ✅ scaffolded | `breadcrumb-navigation.spec.ts` | Execution pending. |
| SC-006 CLS=0 on sidebar toggle | ✅ scaffolded | `layout-cls.spec.ts` | Execution pending. |
| SC-008 Button 6×8×2 = 96 matrix | ✅ scaffolded | `button-cursor-states.spec.ts` + `/app/__test__/button-matrix` | Execution pending (env-gated). |
| SC-010 typography scale | ⚠️ partial | `typography-scale.spec.ts` | Test filter skips non-migrated headings (R002). |
| SC-011 universal focus ring | ⚠️ partial | `focus-ring.spec.ts` | Accepts heterogeneous impls (R005). |
| SC-012 form-field consistency | ✅ scaffolded | `form-field-consistency.spec.ts` | Execution pending. |
| SC-013 table consistency | ✅ scaffolded | `table-consistency.spec.ts` | Execution pending. |
| SC-014 overlay consistency | ✅ scaffolded | `overlay-consistency.spec.ts` | Execution pending. |
| a11y / axe-core on migrated pages | ✅ scaffolded | `layout-a11y.spec.ts` | Execution pending. |

**Unit test run**: `pnpm test tests/unit/layout` — 25/25 passing in 8.7s.
**E2E**: 15 spec files authored, all gated on `E2E_ADMIN_EMAIL`/`E2E_ADMIN_PASSWORD` — execution deferred to pre-ship step.

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Files reviewed | 69 (src: 43, tests: 18, specs: 8) |
| Diff size | +4038 / −204 lines |
| 🔴 Blockers | 0 |
| 🟡 Warnings | 5 (R001–R005) |
| 🟢 Suggestions | 7 (R006–R012) |
| Spec coverage | 91.3% fully + 8.7% partial = 100% present |
| Unit-test result | 25/25 pass (layout suite) |
| Constitution violations | 0 |
| Security findings | 0 |
| Performance findings | 0 |

---

## Recommended Actions

### Must Fix (Blockers)
_None._

### Should Fix (Warnings) — Before Ship

1. **R001 + R002 together**: Pick a resolution for the three `<h2 className="text-sm">` on plan-detail page **and** tighten `typography-scale.spec.ts` to actually enforce SC-010. Doing both keeps the spec + test + code in agreement; doing only one leaves future drift.
2. **R003 + R004 together**: Collapse the three focus-ring implementations into one. Simplest path: delete `.focus-ring` utility, keep `*:focus-visible` for unclassed elements, document that primitives override via Tailwind `focus-visible:ring-*`. Update `docs/shadcn-customizations.md` with the decision.
3. **R005**: Once R004 lands, tighten the focus-ring E2E to assert both patterns.

### Nice to Fix (Suggestions) — Post-Ship OK

4. **R006**: Add a comment on `setLabel` noting the primitive-equality short-circuit is intentional.
5. **R007**: Preserve raw URL segments for `href` construction; use decoded values only for label lookup.
6. **R008**: Clarify the button-matrix route's auth posture in its top comment.
7. **R009**: Document sticky-header assumption in `docs/shadcn-customizations.md`.
8. **R010**: Hoist the `no-restricted-syntax` regex to a named constant.
9. **R011**: Consider an `<AdminPageShell>` wrapper to mirror the portal's layout shape.
10. **R012**: Call out the 32 deferred E2E/QA items explicitly in the Ship PR description.

---

## Verdict

⚠️ **APPROVED WITH CONDITIONS** — the feature is structurally sound and safe to ship after addressing R001–R005. R006–R012 are polish items suitable for a follow-up cleanup commit or the F4 retrospective backlog.

**Next step**: either (a) invoke `/speckit.ship` now and open the two typography items + focus-ring convergence as follow-up tickets in the retrospective, or (b) land R001+R002+R003+R004 in a single `refactor(layout): tighten typography + focus-ring convergence` commit first, then ship with a clean ✅.

---

*Generated by `/speckit.staff-review.run` — Staff-level code review for spec-driven development.*
