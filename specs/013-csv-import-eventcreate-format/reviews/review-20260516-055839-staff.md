# F6.1 Staff-Engineer Review — Full Scope (T061 operator gate)

**Date**: 2026-05-16
**Branch**: `012-eventcreate-integration`
**HEAD**: `a7f160f0` · 131 commits ahead of `main`
**Scope**: Feature 013 — CSV Import Primary Path + EventCreate Format Adapter (F6.1)
**Spec**: `specs/013-csv-import-eventcreate-format/`
**Reviewer**: Claude (Opus 4.7) via `/speckit-staff-review-run` — 7-agent parallel orchestration
**Prior rounds**: R1 / R2 / R3 (closed 27 findings; latest at `review-20260516-121132-round3.md`)

---

## Executive Summary

**Verdict: ⚠️ APPROVED WITH CONDITIONS — Changes Required before flag-flip**

F6.1 is structurally sound. The 10 Constitution v1.4.0 principles all hold; tenant isolation (Principle I, NON-NEGOTIABLE) passes all 5 sub-clauses; the 3 new audit event types fire on every required path; the strict-audit invariant chain introduced in R2 is intact through R3; the cross-tenant probe integration tests (T017 + T037) cover both `runInTenant` app-layer + RLS+FORCE db-layer. Zero new npm dependencies (Principle X). All 3 R3 IMPORTANT residuals (R3-I-1/I-2/I-3) are closed in HEAD.

However, the staff-review surfaces **2 SHIP-BLOCKER findings** that would break production silently, plus **3 i18n bugs that cause runtime render failures**, plus **multiple operator/documentation gates that are required for PDPA compliance and observability completeness**.

**Counts**: 0 CRITICAL · **5 BLOCKER** · **7 HIGH** · **6 MEDIUM** · ~10 LOW · ~30 POSITIVE

---

## 1. Findings Table

### 🔴 BLOCKERS — must fix before flag-flip

| ID | Severity | Source agent | File:line | Description | Recommendation |
|----|----------|--------------|-----------|-------------|----------------|
| **B-1** | 🔴 BLOCKER | chamber-os-architect | `drizzle/migrations/meta/_journal.json:1021,1035` | **Migration journal index collision**: `idx:145` appears twice — `0145_audit_receipt_prefix_changed` (F4) AND `0145_events_source_allow_admin_manual` (F6.1). When `drizzle-kit migrate` runs in production, the first idx:145 will be recorded as applied and the second migration will be **silently skipped**. Result: `events.source` CHECK constraint stays at `CHECK source IN ('eventcreate')`, so every `createEvent` call from the inline-create modal (T026) fails with Postgres `23514 check_violation`. F6.1 feature breaks silently on every fresh deployment. | Renumber `0145_events_source_allow_admin_manual.sql` → `0152_events_source_allow_admin_manual.sql` + update `_journal.json` entry from `idx:145` to `idx:152` with correct `when` timestamp. After renumber: `pnpm drizzle-kit migrate` on a throwaway DB + `pnpm test:integration` (memory: `feedback_migration_apply_before_commit`). |
| **B-2** | 🔴 BLOCKER | i18n-translation-reviewer | `src/i18n/messages/th.json` — `admin.events.import.preview.previewRowsTitle` | TH uses bare `{count}` instead of ICU `{count, plural, ...}`. next-intl will render `ตัวอย่าง (# แถว)` literally — number substitution fails. | Replace with `"ตัวอย่าง ({count, plural, other {# แถว}})"`. TH has no `one` plural per CLDR. |
| **B-3** | 🔴 BLOCKER | i18n-translation-reviewer | `src/i18n/messages/th.json` — `admin.events.import.eventPicker.inlineCreateModal.errors.rateLimitDetailWithSeconds` | TH uses bare `{seconds}` — same runtime substitution failure. | Replace with `"...ลองใหม่หลังจาก {seconds, plural, other {# วินาที}}"`. |
| **B-4** | 🔴 BLOCKER | i18n-translation-reviewer | `src/i18n/messages/sv.json` — `admin.events.import.eventPicker.inlineCreateModal.errors.rateLimitDetailWithSeconds` | SV uses bare `{seconds}` — same runtime substitution failure. | Replace with `"...om {seconds, plural, one {# sekund} other {# sekunder}}."`. |
| **B-5** | 🔴 BLOCKER | pdpa-gdpr-compliance-officer | `docs/compliance/dpia-template.md` + `docs/compliance/processing-records.md` + `docs/compliance/eventcreate-privacy-notice-template.md` + `docs/runbooks/f6-manual-erasure.md` | **GDPR Art. 35 DPIA gap** + **PDPA Art. 30 processing records gap**: F6.1 introduces a new PII storage tier (Vercel Blob with attendee CSV rows VERBATIM), a new fingerprint-based safety net, and the `attendee_pdpa_consent_acknowledged` column — none of which appear in the DPIA template, processing records, EventCreate privacy notice (still says "Zapier only"), or manual-erasure runbook. Code-layer PDPA implementation is correct; compliance documentation lags. | (1) Add F6.1 DPIA section to `dpia-template.md` covering Blob storage + 30-day TTL + fingerprint + consent column with DPO sign-off; (2) update `processing-records.md` §F6 with new processing activities; (3) update `eventcreate-privacy-notice-template.md` in all 3 locales to mention CSV-direct path + 30-day Blob retention; (4) extend `f6-manual-erasure.md` to cascade erasure to error-CSV Blobs. |

### 🟠 HIGH — should fix before flag-flip (or document accepted)

| ID | Severity | Source agent | File:line | Description | Recommendation |
|----|----------|--------------|-----------|-------------|----------------|
| **H-1** | 🟠 HIGH | reliability-guardian | `src/modules/events/application/use-cases/import-csv.ts:93,1644,1769–1781` + `audit-port.ts:454–480` + `drizzle-csv-import-records-repo.ts:85–123` + `0139_csv_import_records.sql` | `rowsStateChanged` is computed in the use-case but **never persisted to `csv_import_records.rows_state_changed`** AND **absent from the `csv_import_record_completed` audit event payload**. On retry the column stays `0`, making operator decisions and the audit trail incomplete. | Wire `rowsStateChanged` into `updateRecord` + audit payload before commit. Add `rows_state_changed INTEGER NOT NULL DEFAULT 0` column if not already in migration 0139. |
| **H-2** | 🟠 HIGH | reliability-guardian | `src/app/api/admin/events/import/route.ts:539–582` | Cross-tenant probe audit (`csv_import_cross_tenant_probe`) is emitted via `safeEmitStandalone` (fire-and-forget) outside the transaction. If the standalone emit fails silently (Neon hiccup, pool timeout), the probe attempt is never recorded — violates Constitution Principle I clause 4 security-monitoring requirement. | Add `probe_audit_emit_failed_total` metric counter on `safeEmitStandalone` failure so the gap is at least observable. (Note: this contradicts security-threat-modeler T-09 which marked the asymmetric fail-open as acceptable; the contradiction is reconciled by adding the counter — keep fail-open semantics, gain observability.) |
| **H-3** | 🟠 HIGH | performance-slo-guardian | `docs/observability.md` §24.1 + §24.3 | The 2 new F6.1 OTel counters (`eventcreate_csv_adapter_mode_detected_total{tenant, format}` + `eventcreate_csv_error_csv_downloaded_total{tenant}`) AND the adapter-drift alert (declared in `plan.md` §VII) are **absent from the metrics catalog and alerts table**. SRE configuring dashboards post-flag-flip has no authoritative reference. | Add both counters to §24.1 F6 metrics table with label definitions + SLO ref. Add adapter-drift alert ("adapter detection rate < 50%") to §24.3 with severity + runbook ref + PromQL sketch. |
| **H-4** | 🟠 HIGH | senior-tester | `vitest.config.ts` | No per-file 100%-branch coverage threshold for F6.1 security-critical paths (`import-csv.ts`, `generate-error-csv-signed-url.ts`, `events/domain/**`). Global `branches:80` does NOT enforce Constitution Principle II's 100% branch requirement on security-critical paths. A regression that drops a `csv_import_cross_tenant_probe` emit branch could pass CI. | Add per-file entries: `'src/modules/events/application/use-cases/import-csv.ts': { branches:100, lines:100, functions:100 }`; same for `generate-error-csv-signed-url.ts`; `'src/modules/events/domain/**/*.ts': { lines:100, ... }`. If 100% branch unreachable today, document deviation in `plan.md` § Complexity Tracking. |
| **H-5** | 🟠 HIGH | pdpa-gdpr-compliance-officer | `docs/runbooks/f6-manual-erasure.md` | Manual-erasure runbook does NOT cover cascading erasure to error-CSV Blobs. If an attendee invokes GDPR Art. 17 / PDPA §30 right-to-erasure AND their registration row is deleted, their PII may still survive in the error-CSV Blob for up to 30 days. | Add §F6.1 to erasure runbook: query `csv_import_records.error_csv_blob_url WHERE error_csv_expires_at > NOW()` for the affected event_id + actor + run-time-range; `del()` matching Blobs; emit `csv_import_error_csv_manually_erased` audit. |
| **H-6** | 🟠 HIGH | pdpa-gdpr-compliance-officer | `docs/compliance/eventcreate-privacy-notice-template.md` (EN/TH/SV) | Privacy notice template still says EventCreate→Zapier auto-ingest only. F6.1 makes CSV-direct manual upload the primary path. Attendees registering at the EventCreate platform need to know their data flows via CSV upload to Chamber-OS, with a 30-day Blob retention for error rows. Article 13 GDPR / §23 PDPA collection-notice requirement. | Add CSV-direct flow to template in all 3 locales; mention 30-day error-CSV TTL + 15-min signed-URL access pattern + 5-year csv_import_records retention. |
| **H-7** | 🟠 HIGH | pdpa-gdpr-compliance-officer | `docs/compliance/processing-records.md` §F6 | §F6 processing record does not list (a) `csv_import_records` table; (b) Vercel Blob error-CSV storage tier; (c) `attendee_fingerprint` SHA-256 first-16-hex; (d) `attendee_pdpa_consent_acknowledged` column. PDPA §30 records-of-processing inventory incomplete. | Add the 4 new data items under §F6 with purpose, lawful basis, retention, recipients, transfers. |

### 🟡 MEDIUM — should fix or document

| ID | Severity | Source agent | File:line | Description | Recommendation |
|----|----------|--------------|-----------|-------------|----------------|
| **M-1** | 🟡 MEDIUM | security-threat-modeler | `src/modules/events/infrastructure/vercel-blob-error-csv-store.ts:50` | **Vercel Blob `access:'public'`** — non-Enterprise tier has no true private bucket. URL has random suffix (`addRandomSuffix:true`) acting as capability token, but is publicly-accessible for the 30-day TTL window. Anyone with the URL bypasses audit trail. Documented design trade-off per research.md R6. | Add `plan.md` § Complexity Tracking entry documenting accepted PDPA risk + rejected alternative (Enterprise Blob, +$XX/month). Add runbook note (`docs/runbooks/eventcreate-csv-import.md` §3) that error-CSV blobs are publicly-URL-accessible for up to 30 days; admins should not upload CSVs with strictly-confidential attendee lists. |
| **M-2** | 🟡 MEDIUM | security-threat-modeler | `src/app/api/admin/events/import/route.ts:306–313` | **Timing asymmetry in `lookupEventByIdTimingSafe`**: `wrong_tenant` branch awaits the audit emit tx (~30–50ms) while `not_found` returns immediately. Attacker could enumerate existing-but-wrong-tenant event UUIDs by timing measurement. Audit tx latency is below BKK→sin1 RTT floor (~25ms) so risk is low at SweCham scale. | Verify `TESTS-I3` (p95-symmetry integration test) is present in `tests/integration/events/` and included in `pnpm test:integration`. If missing, add it with explicit comment that the timing delta is accepted. |
| **M-3** | 🟡 MEDIUM | security-threat-modeler | Error-CSV download path (admin tool) | **CSV formula injection**: error CSV is served VERBATIM (per FR-021 spec). Cells starting with `=`, `@`, `+`, `-` will execute as formulas if opened in Excel/Sheets without "disable formula execution" enabled. Admin-only tool but still a XSS-equivalent risk. | Add note to `docs/runbooks/eventcreate-csv-import.md` (or spec FR-021) instructing admins to open error CSV in text mode or disable automatic calculations. No code change required (VERBATIM is per-spec). |
| **M-4** | 🟡 MEDIUM | reliability-guardian | `src/modules/events/application/use-cases/generate-error-csv-signed-url.ts:231` | Use-case forwards raw infrastructure error message directly to caller — leaks Vercel Blob SDK error string to the Presentation layer. Clean Architecture leak. | Wrap `blobRepo.generateSignedUrl(...)` in `try/catch` and return typed `BlobSignedUrlError` result variant; route layer maps to i18n key. |
| **M-5** | 🟡 MEDIUM | senior-tester | US5 AS3 — history shows "Running…" | No test in T034 contract or T036 integration covers `outcome:'running'` record rendering in the history list. Spec AS3 explicitly requires this UX. | Add a contract test in `csv-import-history-api.test.ts` that returns a record with `outcome:'running'` and verifies the response shape; add a UI assertion in `csv-import-history-table.tsx` test (or extend T048 a11y E2E). |
| **M-6** | 🟡 MEDIUM | performance-slo-guardian | `src/modules/events/infrastructure/vercel-blob-error-csv-store.ts:114` | Pino error log includes `blobUrl: input.blobUrl` — the URL IS the capability token for the next 30 days. Logging at error level leaks the token to log exporters. | Replace with `blobUrlLength: input.blobUrl.length` or truncated prefix. |

### 🟢 LOW — polish (defer to pre-ship checklist)

| ID | Source | Finding |
|----|--------|---------|
| L-1 | i18n | TH `ผู้ดูแล` → `ผู้ดูแลระบบ` in `alreadyArchivedDescription` + `rateLimitDetailWithSeconds` (terminology consistency) |
| L-2 | i18n | SV `admin-verifiering` → `administratörsverifiering` in `tier_upgrade_pending_admin_verification_due` (Anglicism in F8 SV — not F6.1 but flagged) |
| L-3 | i18n | TH `รูปแบบที่มา` → `แหล่งนำเข้า` in `history.columns.sourceFormat` (shorter, more idiomatic) |
| L-4 | architecture | `src/lib/events-csv-import-deps.ts:51` deep-imports `pino-audit-port` — not a violation (lib exempted) but barrel rule does not cover 100%. Consider re-exporting via `src/modules/events/index.ts`. |
| L-5 | architecture | `FEATURE_F6_EVENTCREATE_ADAPTER` default `true` — verify intent (rollback safety net flips OFF when needed; default-on means F6.1 active at first deploy). |
| L-6 | senior-tester | `classify-pdpa-consent.test.ts` claims 20 sub-tests; `grep -c 'it('` shows 5 top-level — verify nested `it()` count. |
| L-7 | perf | Per-batch advisory-lock contention within a single import (3 workers serialise) — documented trade-off in spec, accepted. |
| L-8 | reliability | T057 76s @ 200 rows cross-region NOT a real regression — perf agent reconstructed the arithmetic from inherited Phase 7 per-row RTT cost. T059 prod-region bench will likely pass with ~33s headroom. |

---

## 2. Constitution v1.4.0 Compliance Matrix

| # | Principle | Status | Evidence / Notes |
|---|-----------|--------|------------------|
| I | Data Privacy & Security (NON-NEG) | ✅ **PASS** | All 5 sub-clauses verified. App-layer `runInTenant` on every dispatch; DB-layer RLS+FORCE on `csv_import_records` (migration 0139:180-187); cross-tenant integration tests T017+T037 GREEN; `csv_import_cross_tenant_probe` HIGH-severity audit on both probe paths; admin-bypass paths (`findByIdAcrossTenants` + `listExpiredErrorCsvBlobsAllTenants`) return only `{tenantId}` — no bulk data exposure |
| II | Test-First (NON-NEG) | ⚠️ **PASS WITH CONDITION** | 266 unit + 13 integration GREEN; R3-I-2 fixed in `e48cefc1`. **Gap H-4**: per-file branch thresholds for security-critical F6.1 paths NOT enforced in `vitest.config.ts` |
| III | Clean Architecture (NON-NEG) | ✅ **PASS** | Domain has zero framework imports; Application has zero ORM/HTTP/React imports; Infrastructure does not leak Drizzle types; module barrel re-exports new F6.1 use-cases; M-4 is a minor leak through SDK error message |
| IV | PCI DSS (NON-NEG) | ⚪ **N/A** | No payment surface touched. F4 cross-cutting dropped per Q2 |
| V | i18n (EN+TH+SV) | ❌ **FAIL** | 2808 keys × 3 locales parity OK, but **3 ICU bugs** (B-2/B-3/B-4) break runtime rendering. Must fix before flag-flip |
| VI | Inclusive UX (WCAG 2.1 AA) | ✅ **PASS** | 7 visual states covered in `eventcreate-a11y.spec.ts` (Phase 7 4 + F6.1 3); reduced-motion inherited; min-h-11 WCAG 2.5.8 |
| VII | Perf & Observability | ⚠️ **PASS WITH CONDITIONS** | SLOs unverified pending T059 prod-region bench. **H-3 catalog gap**. Adapter-drift alert declared but not in observability.md. 2 OTel counters wired correctly in code |
| VIII | Reliability | ⚠️ **PASS WITH CONDITIONS** | Strict-audit invariant intact through R3. **H-1 rowsStateChanged not persisted + missing from audit**. **H-2 cross-tenant probe fire-and-forget without metric counter**. Idempotency + atomic tx + advisory lock all correct |
| IX | Code Quality | ⚠️ **PASS WITH RESIDUALS** | TypeScript strict, ESLint clean, Conventional Commits, R3-I-1/I-2/I-3 closed. ~10 LOW polish items remain |
| X | Simplicity (YAGNI) | ✅ **PASS** | Zero new npm dependencies confirmed (`git diff main:package.json` empty); US3 match-preview + US4 CSV template dropped cleanly (zero orphan code in `src/`) |

**Net**: I/III/IV/V/VI/X PASS or N/A; II/VII/VIII/IX PASS WITH CONDITIONS. **V FAIL** until B-2/B-3/B-4 closed.

---

## 3. MTA+STD Tenant-Isolation Verdict (Principle I, Review-Gate Blocker)

| Clause | Requirement | Verdict | Evidence |
|--------|-------------|---------|----------|
| **1. App-layer** | `runInTenant` + branded `TenantId` on every use-case | ✅ PASS | `runListCsvImportRecords` `runInTenant(asTenantContext(...))` (`events-csv-import-deps.ts:284`); `runGenerateErrorCsvSignedUrl` (line 323); `importCsv` `runInTenantTx`; sweep per-row `withTenantScope` |
| **2. DB-layer** | `ENABLE` + `FORCE ROW LEVEL SECURITY` + tenant-isolation policy | ✅ PASS | Migration `0139_csv_import_records.sql:180-187` — `ENABLE + FORCE + policy USING (tenant_id = current_setting('app.current_tenant', TRUE))` (secure-by-default zero rows visible when GUC unset) |
| **3. Integration test** | Cross-tenant probe asserts denial | ✅ PASS | T017 (`csv-import-cross-tenant-eventcreate.test.ts`) + T037 (`error-csv-cross-tenant-isolation.test.ts`) — both exercise app-layer `runInTenant` AND db-layer RLS+FORCE; GREEN on live Neon SG |
| **4. Audit** | `*_cross_tenant_probe` on every deny path | ⚠️ PASS WITH H-2 | `csv_import_cross_tenant_probe` emitted: import route wrong_tenant branch (blocking await) + signed-URL findByIdAcrossTenants (fail-open + pino). **H-2** — fail-open path lacks metric counter for observability of emit failure |
| **5. Super-admin** | n/a (F13 future) | ⚪ N/A | — |

**Advisory lock namespace** `csv-import:{tenantId}:{eventId}` confirmed disjoint from F4 `invoicing:`, F5 `payments:`, F7 `broadcasts:`, F8 `renewals:` namespaces.

---

## 4. Audit-Trail Integrity Verdict

| Event Type | Migration | Declared in `audit-port.ts` | Emitted From | Strictness | Verdict |
|------------|-----------|------------------------------|--------------|------------|---------|
| `csv_import_error_csv_downloaded` | 0141 | line 138 | `generate-error-csv-signed-url.ts:141-174` | STRICT (URL only returned after audit ok) | ✅ PASS |
| `csv_import_cross_tenant_probe` (import path) | 0141 | line 139 | `import/route.ts:318-326` | BLOCKING await | ✅ PASS |
| `csv_import_cross_tenant_probe` (signed-URL path) | 0141 | line 139 | `generate-error-csv-signed-url.ts:215-241` | FAIL-OPEN + pino | ⚠️ PASS WITH H-2 (no metric counter) |
| `csv_import_event_mismatch_overridden` | 0141 | line 140 | `import-csv.ts:1884` (force_proceed branch) | `emitStandalone` before commit | ✅ PASS |
| `event_created` (T026 admin-manual surface) | 0144 | line 160 | `create-event.ts:187` | inside tx | ✅ PASS |
| `csv_import_row_cancelled_no_prior` | 0150 | line 145 | `import-csv.ts:1135` | strict-audit chain | ✅ PASS |
| `csv_import_row_state_changed` | 0150 | line 150 | `import-csv.ts:560` | strict-audit chain (R2-CR-2) | ⚠️ PASS WITH H-1 (`rowsStateChanged` not in payload) |

---

## 5. Spec Coverage Matrix (US1 / US2 / US5)

| User Story | Acceptance Scenarios | Test Coverage | Verdict |
|------------|---------------------|---------------|---------|
| **US1 P1** Upload raw EventCreate export | AS1 header detect + 10-row preview; AS2 multi-line; AS3 Status filter; AS4 mailto strip; AS5 payment infer | T012 contract (12 sub) + T013 unit (45) + T014 PDPA (20) + T015 fingerprint (9 + fast-check) + T016 real-fixtures IT + T017 cross-tenant IT + T018 safety-net IT + T019 a11y | ✅ FULL |
| **US2 P1** Re-upload idempotency + state changes + cancellation | AS1 dup detection; AS2 Notes payment change; AS3 cancellation cascade | T031 IT (2 scenarios) + T032 IT cancellation + state-change strict-audit unit + cancellation-skip-marker unit | ✅ FULL (H-1 affects audit payload) |
| **US5 P2** Import history + error CSV | AS1 list reverse-chrono; AS2 download error CSV verbatim + `_error_reason`; AS3 in-progress "Running…" | T034 contract (12) + T035 contract (9) + T036 IT (4) + T037 IT cross-tenant (3) + T038 IT blob roundtrip (3) + T039 IT blob fail (1) + T048 a11y | ⚠️ M-5 GAP: AS3 `outcome:'running'` not tested |

---

## 6. Pre-Flag-Flip Operator Gates (status verified)

| Task | Status | Owner | Notes |
|------|--------|-------|-------|
| **T058** cron-job.org dashboard entry for `/api/internal/retention/sweep-error-csv-blobs` (daily 05:00 Asia/Bangkok, Bearer auth, 2-day failure alert) | ❌ OPEN | Operator | **PDPA Art. 5(1)(e) blocker** — without this, error-CSV blobs accumulate past 30-day TTL |
| **T059** SC-006 prod-region perf bench (`RUN_PERF_PROD_REGION=1 pnpm test:perf` on Vercel sin1 + intra-region Neon `ap-southeast-1`, 1k rows < 60s) | ❌ OPEN | Operator | SC-006 UNVERIFIED until run; cross-region 76s @ 200 rows is reconstructed arithmetic, prod-region predicted ~27s |
| **T060** Manual staging E2E (`pnpm test:e2e --grep "F6.1 EventCreate CSV import" --workers=1` against staging with seeded TSCC tenant + committed fixtures) | ❌ OPEN | Operator | 3 scenarios expected GREEN |
| **T061** Maintainer security checklist co-sign | ⚠️ THIS REPORT | Maintainer | This staff-review IS T061 in progress — outputs conditions B-1..B-5 + H-1..H-7 |

---

## 7. R3 Residuals Verification

| Finding | Status in HEAD `a7f160f0` | Evidence |
|---------|--------------------------|----------|
| **R3-I-1** stale `?.` on `onSweepClearFailed` | ✅ CLOSED | `sweep-expired-error-csv-blobs.ts:209,222` — both call sites use `deps.onSweepClearFailed(...)` without `?.` |
| **R3-I-2** test asserts wrong log event (`f6_csv_batch_tx_aborted` vs `f6_csv_batch_fan_out_rejected`) | ✅ CLOSED | `csv-import-batch-tx-abort.test.ts:391,403` — test 3 now asserts BOTH events; source emits both at correct sites |
| **R3-I-3** orphan fragment "csv-import-api contracts R3" | ✅ CLOSED | `csv-import-result.tsx:15` — fragment removed; file-top header rewritten to "F6 Phase 7 + F6.1 augmentations" |
| All 15+ SUGGESTIONs from R3 (S-1..S-15) | ✅ CLOSED in `e48cefc1` per R3-fix commit message | Verified |

---

## 8. Strengths (cross-agent consensus)

1. **`EmailHashPrefix` unique-symbol brand** — gold-standard PII safety. Zero `as EmailHashPrefix` casts outside producer.
2. **Strict-audit invariant chain** on state-change + mismatch-override + brand-collision tests — 3 unit-test trio forms a regression-resistance harness.
3. **`_internals` test-seam export** — clean test-only namespace pattern; precedent from `streaming-csv-importer.ts`. Enables 9 brand-collision tests without weakening production API.
4. **R4 discriminated `SweepExpiredErrorCsvBlobsOutput`** — textbook "illegal state unrepresentable". Pattern recommended for F4/F5 sweep-style outcomes.
5. **`unwrapNeverErr<T>`** — exploits `Result<T, never>` channel as compile-time canary.
6. **`AllotmentSnapshot` port lift** — eliminates opaque `Extract<…, {ok:true}>['value']`.
7. **`auditCompletionEmitted` UI chip** — WCAG-styled, dark-mode, `role="status"`, test-id; forensic gap surfacing.
8. **`csvErrorCsvUploadFailed(reason)` discriminator** — dashboards can `group by reason`.
9. **Discriminated-union narrowing pattern uniformly applied** in sweep tests (5 sites).
10. **Cross-tenant integration tests** (T017 + T037) cover BOTH app-layer + RLS+FORCE — exemplary Constitution I clause 3 compliance.

---

## 9. Recommended Action Plan

### Phase A — Close BLOCKERS (est. ~3-4 hours)

1. **B-1**: Renumber migration `0145_events_source_allow_admin_manual.sql` → `0152_events_source_allow_admin_manual.sql`; update `_journal.json` from `idx:145` to `idx:152` with correct `when` timestamp; apply on throwaway DB; run `pnpm test:integration`. ⚠️ Without this fix, F6.1 breaks silently in production.
2. **B-2 / B-3 / B-4**: 3 ICU plural fixes in `th.json` + `sv.json`. Run `pnpm check:i18n` + `pnpm test:e2e --grep "@i18n" --workers=1` to verify runtime rendering.
3. **B-5**: 4 compliance documents updated by DPO (DPIA + processing records + privacy notice EN/TH/SV + erasure runbook). Sign-off required.

### Phase B — Close HIGH (est. ~4-6 hours)

4. **H-1**: Wire `rowsStateChanged` into `updateRecord` + audit payload; add migration if column missing from 0139.
5. **H-2**: Add `probe_audit_emit_failed_total` counter on `safeEmitStandalone` failure in route handler.
6. **H-3**: Update `docs/observability.md` §24.1 + §24.3 with 2 F6.1 counters + adapter-drift alert.
7. **H-4**: Add per-file coverage thresholds in `vitest.config.ts` for `import-csv.ts`, `generate-error-csv-signed-url.ts`, `events/domain/**`.
8. **H-5 / H-6 / H-7**: Compliance doc updates (DPO sign-off scope).

### Phase C — Operator gates (T058–T061)

9. **T058** Register cron-job.org entry → commit ID to `docs/runbooks/cron-jobs.md`.
10. **T059** Run prod-region perf bench → capture to `perf-bench-T059.log`.
11. **T060** Run staging E2E with seeded admin.
12. **T061** Maintainer co-signs this report after Phases A+B+C close.

### Phase D — Polish (defer post-ship if timeline pressure)

13. **M-1..M-6** + **L-1..L-8** — see findings table.

---

## 10. Verdict

⚠️ **APPROVED WITH CONDITIONS — Changes Required**

Feature F6.1 is structurally complete, security-correct, and well-tested. The 3 R3 IMPORTANT residuals are closed. Cross-tenant isolation passes all 5 Constitution Principle I sub-clauses. Zero new npm deps. Strict-audit invariant intact. Code-layer PDPA implementation is correct.

**5 BLOCKERS must close before flag-flip:**
- **B-1** migration journal collision (breaks production silently)
- **B-2 / B-3 / B-4** 3 ICU plural bugs (TH/SV runtime render failures)
- **B-5** GDPR Art. 35 DPIA + PDPA Art. 30 processing records + privacy notice + erasure runbook gaps (4 documents)

After Phase A + B + C close, F6.1 is ship-ready and `/speckit-ship` can run.

---

## Agent transcripts (SendMessage continuation IDs)

| Agent | ID |
|-------|----|
| security-threat-modeler | `a43507bc3fddf60b2` |
| pdpa-gdpr-compliance-officer | `aa645bfcb8c59a9fb` |
| chamber-os-architect | `aa1b35a7e798f63e6` |
| senior-tester | `a09be34c193c07747` |
| performance-slo-guardian | `aad78c6dca929223c` |
| i18n-translation-reviewer | `a0cba0a3dccc73c0e` |
| reliability-guardian | `a284a96f4b42436a8` (re-emit) / `aa9931b5c2e080d8e` (original) |

---

**Reviewer**: Claude (Opus 4.7) via `/speckit-staff-review-run` 7-agent parallel orchestration
**Total findings**: 0 CRITICAL · 5 BLOCKER · 7 HIGH · 6 MEDIUM · ~10 LOW · ~30 POSITIVE
**Constitution v1.4.0**: 6 PASS · 4 PASS-WITH-CONDITIONS · 0 FAIL (V will pass after B-2/B-3/B-4 fix)
