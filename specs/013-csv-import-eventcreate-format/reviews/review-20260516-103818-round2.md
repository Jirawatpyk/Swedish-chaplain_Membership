# F6.1 /speckit-review — Round 2 (post-R1 audit)

**Date**: 2026-05-16
**Scope**: 3 R1 commits — `f46a3cc0` + `9d679196` + `11863786` (13 CRITICAL + ~35 IMPORTANT + ~12 SUGGESTION fixes)
**Branch**: `012-eventcreate-integration`
**Agents launched (parallel)**: 6
- `pr-review-toolkit:code-reviewer`
- `pr-review-toolkit:comment-analyzer`
- `pr-review-toolkit:pr-test-analyzer`
- `pr-review-toolkit:silent-failure-hunter`
- `pr-review-toolkit:type-design-analyzer`
- `pr-review-toolkit:code-simplifier`

**Aggregate**: **~35 R2 findings** — **4 CRITICAL · ~16 IMPORTANT · ~15 SUGGESTION · ~25 POSITIVE**

---

## R2 CRITICAL (4) — must-fix BEFORE shipping R2

| ID | Source | Severity rationale | File:Line | Summary |
|---|---|---|---|---|
| **R2-CR-1** | silent-failure + types | PDPA/GDPR data-minimisation breach + comment-vs-code lie | `import-csv.ts:744 / :766 / :1090` + `audit-port.ts:692` | **PII leak**: `CancellationSkipMarker(rowNumber, parsed.row.attendee_email)` stores RAW email → `safeEmitCancellationNoPrior(..., e.email)` passes raw → audit payload `attendeeEmailHash` stores plaintext under a misleadingly-named "hash" field. Docblock claims SHA-256 hash; code never hashes. |
| **R2-CR-2** | silent-failure | Strict-audit invariant violation | `import-csv.ts:548-581` | `maybeApplyStateChange` outer catch (CR-8) swallows the `TxStageError('audit_emit')` (CR-5) thrown when state-change audit emits fails. Result: DB UPDATE commits, audit row missing, admin sees `rowsAlreadyImported`, SRE alerts on wrong metric. PDPA Art. 30 + GDPR Art. 30 broken. |
| **R2-CR-3** | silent-failure | PII leak in stderr log | `import-csv.ts:573-574` | CR-8 catch block logs raw `attendeeEmail: parsed.row.attendee_email` to stderr — directly violates CLAUDE.md "raw email bodies" forbidden-fields list. Every state-change failure exposes attendee email to log aggregator. |
| **R2-CR-4** | comments | Comment-vs-code lie | `list-csv-import-records.ts:134-141` | I-9 doc comment claims "Log fatal so SREs see the regression" but the function body has NO logger call (no logger import even). Reader trusts comment; SRE alerting silently broken. R1 commit message admitted "deferred to R2"; needs to be either implemented or clearly marked as TODO. |

---

## R2 IMPORTANT (~16) — should-fix in R3 polish round

### Wiring incomplete
- **R2-I-1**: `auditCompletionEmitted` boolean threaded through `ImportCsvOutcome` but route handler `import/route.ts:407-457` drops it → UI cannot surface degraded-trail chip (R1 I-10 closure incomplete).
- **R2-I-2**: `csv-import-eventcreate-format.test.ts` contract mocks construct outcomes WITHOUT `auditCompletionEmitted` + `rowsStateChanged` — typecheck passes via untyped `vi.fn()`; partial-shape mocks no longer match real outcome.

### Observability metric gaps
- **R2-I-3**: `csv_import_row_state_changed` audit-emit failures don't increment `csvImportAuditEmitFailed` (folded into catch-all `csvImportStateChangeFallback{reason="threw"}` bucket).
- **R2-I-4**: `csvErrorCsvUploadFailed` metric lacks `result_err | threw` discriminator — inconsistent with `csvImportSafetyNetFallback` + `csvImportStateChangeFallback` which DO discriminate.

### Test gaps for R1's NEW code paths
- **R2-I-5**: CR-9 `tryEmitMismatchOverride` Err / throw branches → **0 tests** (strict-audit invariant for FR-019c).
- **R2-I-6**: `auditCompletionEmitted` field value never asserted in any test (true on happy path AND false on audit-emit failure).
- **R2-I-7**: I-8 batch fan-out `f6_csv_batch_fan_out_rejected` log emit → **0 tests** (defensive-depth contract claim).
- **R2-I-8**: CR-10 `CancellationSkipMarker` Symbol-brand collision-resistance → **0 tests** (`isCancellationSkip(imposter)` returns false?).
- **R2-I-9**: CR-1 strict-audit `f6_error_csv_audit_emit_blocking_failure` log emit not asserted in unit test.
- **R2-I-10**: CR-7 db_error probe-audit severity not asserted (only assertion on not_found branch).
- **R2-I-11**: Strict-audit integration test for `csv_import_row_state_changed` emit failure missing (would catch R2-CR-2 directly).

### Comment drift
- **R2-I-12**: `audit-port.ts:4` docblock says "37 F6 audit event types" but const has 43 entries (migrations 0141 + 0144 + 0150 omitted).
- **R2-I-13**: `audit-port.ts:71-73` inline anchor + `:97` ("Admin actions (10)") + `:583-587` ("CSV-import audit events (3)") all drift from current 43-entry reality.
- **R2-I-14**: `events-csv-import-deps.ts:295-298` hybrid F6AuditPort comment claims "uses `emit` for success path (in-tx commit)" but actual code routes ALL emit through standalone-tx. Comment from earlier shape.
- **R2-I-15**: `import-csv.ts:548-557` state-change throw-comment cites "FR-018 contract" but FR-018 is the refund-cascade FR; this code path is the FR-018-orthogonal Notes-inferred state-change. Should cite "PDPA Art. 30 / GDPR Art. 30 processing-records".

### Type-design
- **R2-I-16**: `csv_import_row_state_changed.payload.{previousPaymentStatus,newPaymentStatus}` typed as `string` instead of `PaymentStatus` branded union (typed values flowing in are widened on the audit boundary).

---

## R2 SUGGESTION (~15) — quick wins for R3 polish PR

### Comments
- `import-csv.ts:991-996`: `recordAuditEmitFailure` parameter comment is self-contradictory ("TypeScript-enforced" then "string indexer permits but the audit caller does the discipline").
- `import-csv.ts:1129-1130`: "the new `parseStreamWithFormat`" — "new" qualifier dated (legacy method removed).
- `eventcreate-csv-adapter.ts:291-293`: anchor with function name for searchability.
- `sweep-expired-error-csv-blobs.test.ts:9`: file docblock says "Blob delete storage_error → skipped + ERROR log" but impl + test use `warn`.

### Simplifier
- `unwrapNeverErr<T>` helper extraction for the 2 wrapper unreachable-err branches in `events-csv-import-deps.ts:331-342 + 375-384` (-10 LOC).
- Move `import { db }` + `import { sql }` to top-of-file in `events-csv-import-deps.ts:203-204`.
- `toErrMessage(e: unknown): string` helper for the 8 `e instanceof Error ? e.message : String(e)` sites in `import-csv.ts` (defer R3 mass-cleanup).

### Type-design
- `SweepExpiredErrorCsvBlobsOutput.scanFailed: boolean` → discriminated union `{ok:true, ...} | {ok:false, scanFailed:true, reason}` makes "scan-failed implies counters zero" unrepresentable.
- `AllotmentSnapshot` duplicated 3× (`process-attendee-in-tx.ts` + `archive-event.ts` + `toggle-event-category.ts`) — lift to named export on `quota-accounting-port`.
- `isCancellationSkip` redundant cast `(e as CancellationSkipMarker)._csvSkipBrand`.
- `CancellationSkipMarker.brand` static field is unused.
- Brand `Sha256Prefix` for `safeEmitCancellationNoPrior`'s `emailHash` parameter (compile-time guarantee, closes R2-CR-1 at the contract level).
- Sweep `onScanFailed?` + `onSweepClearFailed?` → make required with no-op defaults (current optional permits silent test/composition omission).

### Test
- I-6 cancel+state interleave: pin r1's intermediate paymentStatus to 'paid' before second upload.
- Sweep `blob_not_found` test: spy on `withTenantScope` + assert DB clear actually ran.
- T053 adapterEnabled: add 4th scenario — generic-CSV under `adapterEnabled=false` still parses successfully.
- CR-3 integration test: inject failing audit port to exercise audit-emit-failure → signing_failure path at integration level.

### Code-review
- `recordAuditEmitFailure` "TypeScript-enforced" claim contradicts reality — replace `Readonly<Record<string, unknown>>` with discriminated union for compile-time actorUserId enforcement.

---

## R2 POSITIVE (~25 cross-agent consensus) — R1 fixes done well

### Architecture wins
1. **Barrel architecture guard** correctly enforced — `makePinoAuditPort` removed; 4 deps files deep-import; `events-barrel.test.ts` GREEN.
2. **`maybeApplyStateChange` defensive-guard purge** — no `typeof X === 'function'` checks remain; Constitution Principle III "ports guaranteed by composition" honoured.
3. **Migration 0150 idempotency pattern** mirrors 0132/0137/0141/0144 — `DO $$` blocks with `EXCEPTION WHEN duplicate_object`.

### Logic wins
4. **CR-7 probe-on-db_error**: `generate-error-csv-signed-url.ts:195-241` falls through correctly; no premature short-circuit.
5. **CR-9 strict-audit on override**: `tryEmitMismatchOverride` boolean → caller `unexpected_error` is textbook strict-audit pattern (contrast vs R2-CR-2 broken pattern).
6. **CR-10 Symbol brand**: `Symbol()` (non-shared) is correct for single-realm Node; `instanceof` + brand-equality dual-check defends cross-realm + class-name spoofing.
7. **S-11 deterministic ORDER BY** on `findByEventAndEmail` closes non-deterministic duplicate-attendee race.
8. **I-6 sweep `scanFailed` → route 500** correctly wired end-to-end; cron-job.org "2 consecutive failures" alert can fire.
9. **I-8 Promise.allSettled fan-out** result iteration with explicit defensive-depth comment.
10. **Relaxed `isRefundTransition`** correctly drops matched-member gate from flip (broader applicability) while keeping advisory-lock + credit-back audit guarded.

### Type-design wins
11. **`auditCompletionEmitted` REQUIRED** (not optional) is right call for forensic surface.
12. **`AllotmentSnapshot`** replaces opaque `Extract<..., {readonly ok:true}>['value']` with explicit named fields — decouples from Result envelope.
13. **`RegistrationsRepository.findByEventAndEmail` + `updatePaymentStatus`** are exemplary port additions (typed PaymentStatus throughout; rejects pseudonymised rows; surfaces invariant_violation).
14. **T053 adapterEnabled three-way coverage** (undefined→default, true→detect, false→reject) — minimum coverage with high catch rate.

### Test wins
15. **CR-6 dual presence/absence assertion** (`csv_import_row_cancelled_no_prior` present + `csv_import_row_failed` absent) — strictly stronger than either alone.
16. **I-2 count-side-channel assertion** asserts BOTH `rows.length === 0` AND `totalRecords === 0` — Constitution Principle I clause 3.
17. **Sweep `onSweepClearFailed`** test correctly separates DB-clear-err vs withTenantScope-throw branches.
18. **Generate-error-csv-signed-url same-tenant race test** catches the class of regression where probe condition is "simplified".

### Comment cleanup wins
19. **I-7 docblock shrink** 69→20 lines, kept genuine WHY (time-budget semantics, spec authority, Clean Arch).
20. **I-1 relative anchor** ("from preceding reportStage call above") survives code reordering.
21. **I-9 self-referential preamble strip** in streaming-csv-importer.ts.
22. **I-10 archaeology purge** from `recordAuditEmitFailure` JSDoc.
23. **S-6 T052-deferred fix** uses concrete metric name + helper-emits-list / use-case-emits-log split.

### Simplifier wins
24. **Net LOC**: `import-csv.ts` −16 LOC (101 removed, 85 added) — actual simplification.
25. **M6 parseSourceIp inline** correctly handles null + empty XFF edge cases.

---

## Constitution v1.4.0 — Round 2 verdict

| Principle | R1 status | R2 audit | Net |
|---|---|---|---|
| **I** Tenant isolation | ✅ | 🟡 PII drift in audit payload (R2-CR-1) | ❌ regression introduced by R1 CR-10 |
| **II** Test-first | ✅ | 🟡 R1 NEW code paths have 5 untested invariants | 🟡 |
| **III** Clean Architecture | ✅ | ✅ Barrel guard enforced; deps deep-import | ✅ |
| **IV** PCI DSS | n/a | n/a | n/a |
| **V** i18n | ✅ | ✅ | ✅ |
| **VI** Inclusive UX | ✅ | 🟡 auditCompletionEmitted wiring stops at use-case | 🟡 |
| **VII** Perf & Observability | ✅ | 🟡 metric discriminator missing (R2-I-3/I-4) | 🟡 |
| **VIII** Reliability | ✅ | ❌ strict-audit invariant broken (R2-CR-2) | ❌ |
| **IX** Code Quality | ✅ | 🟡 comment drift (R2-I-12 through I-15) | 🟡 |
| **X** Simplicity | ✅ | ✅ | ✅ |

**Net**: R1 closed 13 CRITICAL but introduced **2 new CRITICAL regressions** (R2-CR-1 PII drift + R2-CR-2 strict-audit broken) plus 2 comment-vs-code drift CRITICALs. Not shippable until R2 CRITICALs closed.

---

## Recommended Action Plan

### Phase 1 — Close 4 R2 CRITICAL (must-fix before push · est. 2-3 hrs)

1. **R2-CR-1 + R2-CR-3** (45 min): SHA-256 hash attendee_email at the `CancellationSkipMarker` throw site + strip raw email from CR-8 catch logger payload. Add test asserting payload matches `/^[0-9a-f]{16}$/`.
2. **R2-CR-2** (30 min): `maybeApplyStateChange` outer catch re-throws `TxStageError('audit_emit', ...)` to escape savepoint. Add `csvImportAuditEmitFailed(tenantId, 'csv_import_row_state_changed')` bump.
3. **R2-CR-4** (15 min): Rephrase `mapRepoError` comment as `// TODO(R3): elevate to logger.fatal once logger port threaded into list-csv-import-records deps` OR thread logger via deps (medium-cost — defer the latter to R3).

### Phase 2 — R2 IMPORTANT (~16 items · est. 4-6 hrs)

Highest priority:
- R2-I-1 + R2-I-2: thread `auditCompletionEmitted` through route + UI + contract test mocks
- R2-I-11: integration test for state-change audit-emit failure (would have caught R2-CR-2)
- R2-I-3: bump `csvImportAuditEmitFailed` from state-change emit failure path
- R2-I-12 through I-15: audit-port section-count + hybrid-audit-port comment drift fixes

### Phase 3 — R2 SUGGESTION (~15 items · est. 3-4 hrs)

Bundle with R3 polish PR. None are blockers.

---

## Recommended next steps

1. **Land Phase 1 (4 R2 CRITICAL) as a "[Spec Kit] fix(F6.1): R2 ship-blockers" commit** — small focused diff, easy to review.
2. **Phase 2 + Phase 3 in a follow-up "R3 polish" commit** — separate from ship-blockers.
3. Re-run `/speckit-review` after Phase 1 with **focused agents** (silent-failure-hunter + type-design-analyzer + comment-analyzer) to verify R2-CR-1/2/3/4 closures — no need for full 6-agent sweep.

---

## Agent transcripts location

All 6 agent outputs captured in conversation history. Agent IDs for follow-up SendMessage:

- code-reviewer: `a9414bace20cd54f6`
- comment-analyzer: `a7e25293d4b78cd85`
- pr-test-analyzer: `aef1d40b163ed4422`
- silent-failure-hunter: `a2a469be004f68866`
- type-design-analyzer: `a599ec23defbd5eaa`
- code-simplifier: `a32cdd2fc68fcb790`

---

**Reviewer**: Claude (Opus 4.7) via `/speckit-review` Round 2 multi-agent orchestration
**Next gate**: `/speckit-review F6.1 Round 3` after Phase 1 closeout (only 3 agents needed)
