# F6.1 /speckit-review — Round 3 (post-R2+R3+R4 audit)

**Date**: 2026-05-16
**Scope**: 7 F6.1 commits — `28fd32f0` + `5593703f` + `32adba39` + `18e64970` + `230be2cd` + `75d5a5f2` (R2 Phase 1+2+3 + R3 Batch A+B+C + R4 final)
**Branch**: `012-eventcreate-integration`
**Agents launched (parallel)**: 6 standard speckit-review agents

**Aggregate**: **~28 R3 findings** — **0 CRITICAL** · **3 IMPORTANT** · **~15 SUGGESTION** · **~10 POSITIVE**

---

## R3 CRITICAL — none ✅

All R2-CR-1/2/3/4 fixes are correctly applied. No new ship-blockers detected.

---

## R3 IMPORTANT (3)

### R3-I-1 — Stale `?.` optional-chain on sweep callback (4 agents flagged the SAME issue)

**File**: `src/modules/events/application/use-cases/sweep-expired-error-csv-blobs.ts:223`
**Commit**: `75d5a5f2` (R4)
**Confidence**: 95%

R4 promoted `onSweepClearFailed` to REQUIRED (line 93) and updated the primary call site at line 210 to `deps.onSweepClearFailed(...)`. BUT the catch-block call site at line 223 still uses `deps.onSweepClearFailed?.(...)` — the optional-chain is now dead code that contradicts the type signature.

**Fix**: `deps.onSweepClearFailed(candidate.tenantId);` (drop `?.`)

### R3-I-2 — R2-I-7 fan-out test asserts wrong log event (test-vs-source mismatch)

**File**: `tests/unit/events/csv-import-batch-tx-abort.test.ts:272-374`
**Commit**: `18e64970` (R3 Batch A)
**Confidence**: 90%

The `it(...)` name claims to verify `f6_csv_batch_fan_out_rejected` (source `import-csv.ts:990`) but the actual assertion at line 369-372 finds `f6_csv_batch_tx_aborted` (outer-catch entry log, line 956). The inner `Promise.allSettled`-rejection branch has **zero direct test coverage**. A regression that drops the `logger.error({event:'f6_csv_batch_fan_out_rejected',...})` block would NOT be caught.

**Fix**: Either rename the test to match what it actually verifies, OR stub `safeEmitRowFailed` via `vi.spyOn` to force a rejected promise + assert the specific log event fires.

### R3-I-3 — Orphan fragment `csv-import-api contracts R3` in csv-import-result.tsx

**File**: `src/components/events/csv-import-result.tsx:15`
**Commit**: pre-R3 (`ba7a7d06` F6 Phase 7 review-3) — missed by Batch C mass strip
**Confidence**: 90%

Line reads `"contracts/csv-import-api.md csv-import-api contracts R3 — admins must clearly see"` — the `csv-import-api contracts R3` is a leftover review-round tag that escaped Batch C. Reader is misled into thinking `R3` is a section anchor inside the contracts doc.

**Fix**: `"per contracts/csv-import-api.md — admins must clearly see…"`

---

## R3 SUGGESTION (~15 selected highlights — full lists in agent transcripts)

### Simplifier wins remaining
- **S-1: Nested ternary on Counter `tone`** (`csv-import-result.tsx:309-315`) — replace 3-tier nested ternary with object lookup. Violates CLAUDE.md "avoid nested ternary" rule.
- **S-2: Nested ternary on markRefunded error** (`process-attendee-in-tx.ts:621-629`) — 4-tier nested ternary; replace with switch.
- **S-3: Orphan JSDoc on `recordAuditEmitFailure`** (`import-csv.ts:1021-1034`) — when `toErrMessage` was inserted, the `recordAuditEmitFailure` JSDoc got stranded as un-attached. Currently `recordAuditEmitFailure` has NO docstring.
- **S-4: `toErrMessage` duplicated in sweep use-case** (`sweep-expired-error-csv-blobs.ts:219`) — 2 inline sites missed by R3 Batch B "12 sites → helper" claim. R3 actually collapsed 13 sites; 2 more in sweep + import-csv recovery path missed.
- **S-5: Test boilerplate `if (result.value.kind !== 'ok') throw` × 5 sites** in sweep tests — extract `expectOkSweep(result)` helper that returns narrowed type.
- **S-6: Surviving review-round tags in audit-port.ts** (lines 141, 147, 476, 687, 702, 717) — ~6 tags escaped Batch C mass strip.

### Silent-failure / observability suggestions
- **S-7: `safeEmitCancellationNoPrior` omits `actorUserId` in `recordAuditEmitFailure` context** (`import-csv.ts:1158, 1167`) — the helper comment says "Each call-site MUST include `actorUserId`" but these 2 sites only pass `{ rowNumber, ... }`. PDPA Art. 5(1)(b) forensic attribution gap.

### Type-design suggestions
- **S-8: Promote `ListCsvImportRecordsDeps.logger` to required + no-op default** — same pattern R4 used for sweep callbacks. Currently optional permits silent regression-detection loss.
- **S-9: Hoist shared trio `{historyPersisted, auditCompletionEmitted, summary, errorCsvAvailable}` from completed+timeout outcome variants** — DRY for future 5th shared field.
- **S-10: Freeze `_internals` at runtime** with `Object.freeze` — `as const` is compile-time only.

### Comment hygiene
- **S-11: csv-import-result.tsx file-top header still says "F6 Phase 7"** — file now owns substantial F6.1 surface (recordId, errorCsvAvailable, historyPersisted, auditCompletionEmitted, rowsStateChanged). Update to "F6 Phase 7 + F6.1 augmentations".
- **S-12: Duplicate "scan_failed → 500" claim repeated 3× in sweep file** (docstring + step-1 inline + discriminated-union JSDoc). Collapse to single canonical location with cross-references.

### Test improvements
- **S-13: Brand-collision test uses `Symbol.for(...)` (global registry)** instead of `Symbol(...)` (per-module). The `instanceof` gate masks the brand-equality check — the test passes for the wrong reason. Strengthen by `Object.create(CancellationSkipMarker.prototype)` + wrong brand value.
- **S-14: State-change strict-audit test doesn't pin payload structure** — a regression dropping `previousPaymentStatus`/`newPaymentStatus`/`rowHash` from the audit emit would slip through. Add snapshot.
- **S-15: Override strict-audit test doesn't assert `csv_import_records.insert NOT called`** directly — fixture's throw-on-insert mock provides implicit coverage but indirect.

---

## R3 POSITIVE (~10 cross-agent consensus on R2-R4 wins)

1. **`EmailHashPrefix` unique-symbol brand** — gold-standard PII safety pattern. Verified zero `as EmailHashPrefix` casts outside the producer (`hashAttendeeEmail`).
2. **Strict-audit re-throw chain on state-change** — R2-CR-2 correctly preserved through R3 metadata strip + R4 hardening. Pinned by 3 unit tests.
3. **`_internals` test-seam export** — clean test-only namespace with precedent from `streaming-csv-importer.ts`. Enables 9 brand-collision tests without weakening production API.
4. **R4 discriminated `SweepExpiredErrorCsvBlobsOutput`** — textbook "illegal state unrepresentable" win. Pattern recommended for F4/F5 sweep-style outcomes.
5. **`unwrapNeverErr<T>` helper** — exploits `Result<T, never>` channel as compile-time canary. `logger.fatal` before throw is correct ordering.
6. **`AllotmentSnapshot` port lift** — eliminates opaque `Extract<…, {ok:true}>['value']` pattern.
7. **`auditCompletionEmitted` UI chip** — same WCAG/styling as `historyDegraded`, dark-mode variants, `role="status"`, test-id hook. Forensic gap surfacing.
8. **`csvErrorCsvUploadFailed(reason)` discriminator** — both call sites pass correct literals. Dashboards can `group by reason`.
9. **Discriminated-union narrowing pattern uniformly applied** in sweep tests (5 sites).
10. **Strict-audit invariant tests trio** — `tryEmitMismatchOverride` (3 tests) + `maybeApplyStateChange` (3 tests) + brand collision (9 tests) form a regression-resistance harness for the strict-audit invariants R2 introduced.

---

## Round 3 quantitative agent summary

| Agent | CRITICAL | IMPORTANT | SUGGESTION | POSITIVE |
|---|---|---|---|---|
| code-reviewer | 0 | 1 | 3 | 7 |
| comment-analyzer | 0 | 1 | 3 | 6 |
| pr-test-analyzer | 0 | 1 | 3 | 8 |
| silent-failure-hunter | 0 | 1 | 2 | 5 |
| type-design-analyzer | 0 | 1 | 3 | 6 |
| code-simplifier | 0 | 0 | 5 | 5 (non-findings = wins) |
| **Aggregate** | **0** | **3 unique** | **~15** | **~10** |

(IMPORTANT items count as 3 unique because 4 agents flagged the same R3-I-1 stale `?.` issue.)

---

## Constitution v1.4.0 — Round 3 verdict

| Principle | Status |
|---|---|
| I Tenant isolation | ✅ R1 CR-7 probe-on-db_error preserved; PII brand enforced |
| II Test-first | ✅ 266 unit + 13 integration GREEN |
| III Clean Architecture | ✅ `_internals` test-seam does NOT violate boundary |
| IV PCI DSS | n/a |
| V i18n | ✅ 2808 keys × 3 locales |
| VI Inclusive UX | ✅ Degraded chips + WCAG 2.5.8 |
| VII Perf & Observability | ✅ Metric discriminators wired correctly |
| VIII Reliability | ✅ Strict-audit re-throw preserved |
| IX Code Quality | 🟡 3 IMPORTANT residuals + ~15 polish items |
| X Simplicity | 🟡 Helpers add LOC but win clarity (net trade-off positive) |

**Net**: F6.1 is **ship-ready** with 3 low-priority IMPORTANT residuals + ~15 polish items. None are ship-blockers.

---

## Recommended Action Plan

### Phase 1 — Close 3 R3 IMPORTANT (est. 30 min)

1. **R3-I-1**: `sweep-expired-error-csv-blobs.ts:223` — drop `?.` (1-line)
2. **R3-I-2**: `csv-import-batch-tx-abort.test.ts` R2-I-7 test — rename or fix assertion (5-10 lines)
3. **R3-I-3**: `csv-import-result.tsx:15` — remove orphan `csv-import-api contracts R3` text fragment (1-line)

### Phase 2 — High-value SUGGESTIONs (est. 1-2 hrs)

4. S-1 + S-2: 2 nested ternaries (CLAUDE.md anti-pattern)
5. S-3: orphan JSDoc fix for recordAuditEmitFailure  
6. S-7: actorUserId in safeEmitCancellationNoPrior failure context
7. S-4 + S-6: complete remaining `toErrMessage` collapse + remaining metadata strip

### Phase 3 — Lower-priority polish (defer to pre-ship checklist)

- S-5 + S-9 + S-10 + S-11 + S-12 + S-13 + S-14 + S-15

---

## Recommendation: CLOSE (after Phase 1)

R3 review confirms F6.1 is in excellent shape. The 3 IMPORTANT residuals are all <10 LOC fixes. After Phase 1 closeout, no further review rounds needed — slice is `/speckit-ship` ready.

---

## Agent transcripts location

All 6 agent IDs for SendMessage follow-up:
- code-reviewer: `a8c1c09ab7207fc12`
- comment-analyzer: `a2b9b87e77fa63a77`
- pr-test-analyzer: `a9400ef94f902829c`
- silent-failure-hunter: `aed54335085712e75`
- type-design-analyzer: `a7707318e8ec1fc11`
- code-simplifier: `a14a7098fb97b9fce`

---

**Reviewer**: Claude (Opus 4.7) via `/speckit-review` Round 3 multi-agent orchestration
