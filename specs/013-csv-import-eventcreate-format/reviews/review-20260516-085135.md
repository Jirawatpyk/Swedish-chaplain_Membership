# F6.1 /speckit-review — Round 1 (post US2+US5+Polish)

**Date**: 2026-05-16
**Commit reviewed**: `3aa4b5bd` — `[Spec Kit] feat(F6.1): US2 cancellation + state-change + US5 history/signed-URL/sweep + Phase 6 polish`
**Branch**: `012-eventcreate-integration`
**Scope**: Phase 4 US2 (cancellation cascade + state-change) + Phase 5 US5 (history + signed-URL + TTL sweep) + Phase 6 polish · 51 files · +5186 / −146 LOC
**Agents launched (parallel)**: 7
- `pr-review-toolkit:code-reviewer` (general code quality)
- `pr-review-toolkit:comment-analyzer`
- `pr-review-toolkit:pr-test-analyzer`
- `pr-review-toolkit:silent-failure-hunter`
- `pr-review-toolkit:type-design-analyzer`
- `pr-review-toolkit:code-simplifier`
- `enterprise-ux-designer` (added per user request)

**Aggregate finding count: 105+ findings** — 13 CRITICAL · 49 IMPORTANT · 43+ SUGGESTION

---

## CRITICAL Issues (13) — must-fix before merge

| ID | Source | Severity rationale | File:Line | Summary |
|---|---|---|---|---|
| **CR-1** | tests | Constitution Principle II 100% branch threshold violation | MISSING `tests/unit/events/generate-error-csv-signed-url.test.ts` | `generateErrorCsvSignedUrl` use-case (244 LOC, 7 branches incl. strict-audit gate) has ZERO unit tests. Audit-emit-blocks-signed-URL invariant asserted nowhere. |
| **CR-2** | tests | Constitution Principle II + spec AS-US5-11 | MISSING `tests/unit/events/sweep-expired-error-csv-blobs.test.ts` + cron route contract | Sweep cron + use-case have ZERO tests. Bearer-auth gate untested. Orphan-blob accounting untested. |
| **CR-3** | tests | spec FR-021 / AS-US5-07 | `tests/integration/events/error-csv-cross-tenant-isolation.test.ts:160-185` | Strict-audit invariant unverified — own-tenant success path doesn't assert `csv_import_error_csv_downloaded` audit row appears, nor that it's absent on `signing_failure`. |
| **CR-4** | tests | spec FR-018 / AS-US2-03 | `tests/integration/events/cancellation-cascade-eventcreate.test.ts:110-212` | T032 asserts payment_status flip but NOT quota credit-back + `quota_credit_back_refund` audit emit on matched-member path. |
| **CR-5** | tests | spec FR-018 / AS-US2-02 | `tests/integration/events/re-upload-idempotency-eventcreate.test.ts:172-309` | State-change audit emit unverified — T031 confirms DB column flip + counter, but no audit row assertion. |
| **CR-6** | tests | spec AS-US2-04 | MISSING | `CancellationSkipMarker` first-time-cancellation skip path covered nowhere. Regression that demotes marker to `kind:'failed'` would pass. |
| **CR-7** | errors | Constitution Principle I clause 4 audit-chain | `src/modules/events/application/use-cases/generate-error-csv-signed-url.ts:187-198` | Cross-tenant probe NOT emitted when own-tenant lookup hits `db_error` — short-circuits to 404 before reaching the admin-probe step. Forensic trail lost. |
| **CR-8** | errors | Silent failure / data corruption | `src/modules/events/application/use-cases/import-csv.ts:484-523` | `maybeApplyStateChange` outer `catch {}` swallows ALL errors with no log, no audit, no metric. RLS violations + serialisation failures hidden; admin's Notes fix silently dropped + reported as `rowsAlreadyImported`. |
| **CR-9** | errors | Strict-audit invariant violation | `src/modules/events/application/use-cases/import-csv.ts:1166-1168 + 1610-1664` | `safeEmitMismatchOverride` is fail-open — if `csv_import_event_mismatch_overridden` audit emit fails, import STILL commits with no forensic record that the safety net was bypassed. |
| **CR-10** | errors | Audit chain | `src/modules/events/application/use-cases/import-csv.ts:697-708` | `CancellationSkipMarker` class-collision risk (no Symbol brand) + no audit emit at all on the skip path (state mutation rolled back, but no forensic trace). |
| **CR-11** | comments | Inconsistent severity label | `src/modules/events/application/use-cases/generate-error-csv-signed-url.ts:205` + audit-port.ts:129 | Comment says "HIGH-severity" but actual emit is `severity: 'critical'` — three-way mismatch (JSDoc / inline comment / emitted value). Misleads SRE responders. |
| **CR-12** | ux | WCAG SC 4.1.3 / a11y | `src/app/(staff)/admin/events/import/history/page.tsx:77` | Error div uses `role="status"` (polite); must be `role="alert"` (assertive) so SR interrupts current reading on error. |
| **CR-13** | ux | Display correctness / TZ | `src/app/(staff)/admin/events/import/history/page.tsx:86-92` | `Intl.DateTimeFormat` missing `timeZone: 'Asia/Bangkok'` — renders 7h behind on Vercel UTC runtime. F4/F5 already caught this same trap. Also blocks correct Thai BE display. |

---

## IMPORTANT Issues (49 selected highlights — full list in agent transcripts)

### Code quality (code-reviewer — 5 IMPORTANT)
- **I-1** `csv-import-result.tsx`: `rowsStateChanged` counter incremented but NEVER rendered in result card → SC-004 spec drift ("re-uploaded payment-status changes... reports them in rowsProcessed").
- **I-2** `api/internal/retention/sweep-error-csv-blobs/route.ts:51-60`: bearer mismatch → 401 with `logger.warn` but NO `cron_bearer_auth_rejected` audit row. F8 retrospective explicitly flagged this pattern. Use `gateCronBearerOrRespond` helper.
- **I-3** `csv-import-history-table.tsx:113-114`: "Event" column header but cell shows `originalFilename`. Rename i18n key or display event identifier.
- **I-4** `import-csv.ts:683-688`: 2nd duplicate cancellation re-upload counted as `rowsProcessed` not `rowsAlreadyImported` — idempotency contract broken for Cancellation rows.
- **I-5** `import-csv.ts:463-524`: State-change path UPDATEs `payment_status` silently — no per-row audit. PDPA Art. 30 + GDPR Art. 30 require traceable processing records.

### Test gaps (test-analyzer — 7 IMPORTANT)
- I-1 `FEATURE_F6_EVENTCREATE_ADAPTER` sub-flag (T053) has ZERO tests. Safety net for emergency rollback unprovable.
- I-2 Cross-tenant `eventIdFilter` leak path on history endpoint uncovered.
- I-3 Probe audit-emit failure (cross-tenant probe path) untested.
- I-4 T039 Blob upload failure mock only exercises `storage_error` kind; other error kinds + pino log assertion missing.
- I-5 `as never` casts bypass enum guards in T037 — type-system escape hatch.
- I-6 Cancellation + state-change interleave (same row Cancelled + Notes changed) not tested.
- I-7 `csv-import-audit-emit-failure.test.ts` not updated for `rowsStateChanged` counter path.

### Comments rot (comment-analyzer — 11 IMPORTANT)
- I1 `process-attendee-in-tx.ts:386`: stale line-number `// from line 323` (actually 337).
- I2 `import-csv.ts:863`: stale `line ~420` reference (now ~815).
- I4 `sweep-error-csv-blobs/route.ts:10-11`: JSDoc promises helper enforces strictness but route layer adds its own ≥16 chars pre-check.
- I5 `drizzle-registrations-repository.ts:57-69`: comment cites "Phase 10 T110" caller that's still a `not_implemented` stub.
- I6 `process-attendee-in-tx.ts:6-7`: "Phase 7 T094" refers to F6 spec 012, not F6.1 spec 013 — disambiguate.
- I7 `import-csv.ts:1-69`: 69-line file-top docblock includes review-round metadata ("H-7 docblock fix", "H-6 clarification 2026-05-15") that will rot.
- I8 25+ review-round tags (`Round 1`, `R2-I4`, `H-2`, `H-6`, `H-14`, `H-15`, `M-2`, `CR-4`, `CR-5`, `TYPE-D2/D3/D4`) embedded in body comments. Mass cleanup needed.
- I9 `streaming-csv-importer.ts:297-303`: self-referential "C-1 obsolete claim removed" comment.
- I10 `import-csv.ts:904-911`: review-process archaeology baked into permanent JSDoc.
- I11 `import-csv.ts:306-314`: cites "Drizzle docs § Transactions/nested" without link — unverifiable claim.
- C1 `generate-error-csv-signed-url.ts:205` + audit-port.ts:129: severity-label mismatch (HIGH vs critical) — see CR-11.

### Silent failures (silent-failure-hunter — 10 IMPORTANT)
- I-1 `sweep-expired-error-csv-blobs.ts:148-192`: `clearErrorCsvBlob` failure is `logger.warn`, not `logger.error`; orphan blob has no retry tracker.
- I-2 `import-csv.ts:1022-1027`: unexpected_error returns raw `(e as Error).message` — potential internal leak (e.g. Drizzle `relation does not exist`).
- I-3 `import-csv.ts:1316-1354`: error-CSV blob `put` failure → `logger.warn` only; no metric, no audit, no UI affordance for "blob lost".
- I-4 `drizzle-csv-import-records-repo.ts`: `wrapDbError` discards original error message + stack across 10 repo methods. Caller logs `err.kind` only.
- I-5 `events-csv-import-deps.ts:328-332 + 360-362`: "unreachable err branch" throws raw `Error` with no log, no event tag, no metric.
- I-6 `sweep-expired-error-csv-blobs.ts:86-101`: scan failure returns 200 OK + `sweptCount:0` → cron-job.org "2 consecutive failures" alert cannot detect.
- I-7 `import-csv.ts:1069-1081`: `f6_eventcreate_adapter_unknown_columns` pino emit has no fault-tolerance wrapper.
- I-8 `import-csv.ts:868-880`: `Promise.allSettled` swallows rejections silently from `safeEmitRowFailed`.
- I-9 `list-csv-import-records.ts:132-135`: `mapRepoError` flattens non-`db_error` kinds to generic `db_error` channel — `not_implemented` becomes 500 without `logger.fatal`.
- I-10 `import-csv.ts:1719-1736`: `csv_import_completed` audit emit failure not propagated to `auditCompletionEmitted` field on outcome.

### UX/A11y (enterprise-ux-designer — 8 IMPORTANT)
- I1 `loading.tsx`: skeleton renders 6 rows vs perPage default 30 → layout shift (CLS).
- I2 `csv-import-history-table.tsx:202`: `aria-label="Pagination"` hardcoded English.
- I3 Empty state violates `ux-standards.md § 3.1` anatomy (icon + title + body + CTA).
- I4 "Event" column header / `originalFilename` data mismatch (mirror of code-review I-3).
- I5 Outcome column text-only with no visual differentiation — `sourceFormat` uses Badge, `outcome` doesn't.
- I6 Missing `error.tsx` route-level error boundary for `/admin/events/import/history/`.
- I7 `page.tsx:80`: `loadError` microcopy says "Reload" but renders no reload affordance.
- I8 No `nav.csvImportHistory` command palette entry — smart-features.md #3.

### Type design (type-design-analyzer — major refactor candidates)
- `ImportSummary.rowsStateChanged` is dead state (written but never read by any consumer). JSDoc claims audit-payload surface but audit-port schema omits it.
- `Extract<QueryAllotmentsResult, {ok:true}>['value']` narrow is leaky-abstraction; `PlanAllotments` + `ConsumedQuota` are already exported.
- `adapterEnabled?: boolean` with sentinel-defaulting violates `pdpaConsentAcknowledged` REQUIRED-tri-state precedent in same module.
- `EventCreateAttendeeRow.isAttending + isCancellation` twin booleans permit illegal state (both true).

---

## SUGGESTION (43+ items — see agent transcripts for full list)

Quick-win batch (each ≤15 min):
- comment cleanup (S1-S15): strip review-round metadata across ~25 sites; fix "US5 deferred" → "US5"
- code-reviewer S-1: sanitize CRLF in EventCreate `unknownColumns` pino log (log-injection)
- code-reviewer S-7: add `requestId` to sweep cron 500 body
- code-reviewer S-8: tighten `previousPaymentStatus: string` → `PaymentStatus`
- code-reviewer S-11: add deterministic `.orderBy()` to `findByEventAndEmail` `.limit(1)`
- silent-failure S-3: upgrade safety-net fail-open from `logger.warn` to `logger.error`
- simplifier H1: drop dead `typeof === 'function'` guards (port methods are required)
- simplifier H2: drop `_UnusedTypes` suppression artefact
- simplifier H4: reuse `formatLocalisedDate` helper in history page → also fixes Thai BE display
- ux S2: remove orphaned `columns.actor` i18n keys + row.actor.userId from interface

---

## Positive Observations — cross-agent consensus on what was done well

1. **Admin-bypass repo split** (`CsvImportRecordsAdminRepository`) — exemplary Constitution Principle I clause 4 enforcement; disjoint method names prevent accidents. Type-design + code-review + simplifier all flag as a model pattern.
2. **`GenerateErrorCsvSignedUrlOutcome` discriminated union** — 4 well-formed variants, perfect "make illegal states unrepresentable". Type-design rated 5/5/5/4.
3. **Strict-audit invariant placement** in signed-URL use-case (`emit BEFORE return URL`) — correctly enforces PDPA/GDPR PII-access trail.
4. **`CancellationSkipMarker` rationale comment** — captures the non-obvious SAVEPOINT-rollback semantic. (Pattern itself flagged for post-MVP refactor per type-design.)
5. **Cross-tenant probe pattern** — `findByIdAcrossTenants` + critical-severity audit + structured `logger.error` on emit failure (modulo CR-7 db_error short-circuit).
6. **`min-h-11` WCAG 2.5.8 compliance** on every interactive in the new UI surfaces.
7. **`TableContainer 96rem`** correct container choice for 8-column history table per § 18.1.
8. **EN/TH/SV i18n key parity 100%** at 2799 keys × 3 locales; TH register chamber-formal; SV compound-noun convention correct.
9. **`aria-busy` + `aria-hidden` Skeleton + `data-testid` hooks** — strong a11y + E2E selector hygiene.
10. **F6.1 `csv-import:` advisory-lock namespace** correctly disjoint from F4 `invoicing:`, F5 `payments:`, F7 `broadcasts:`, F8 `renewals:`.
11. **Real-CSV fixture as canonical baseline** + synthetic narrow-branch CSVs separation pattern in T031.
12. **Live Vercel Blob roundtrip with eventual-consistency tolerance** (`[404, 410, 200]`) in T038.
13. **Q2 cross-cutting drop verification** in T032 — assert NO F4 invoice + NO F5 processor event side effects.
14. **`updatePaymentStatus` returns `previousPaymentStatus`** — captures audit-payload-ready "before" without a 2nd query.

---

## Constitution v1.4.0 — gate-impact mapping

| Principle | Status | Blocker findings |
|---|---|---|
| **I — Tenant isolation (NON-NEG)** | 🟡 PARTIAL | CR-7 (probe-on-db_error gap), CR-12 a11y not tenant-isolation but Principle I sub-clause 4 partial |
| **II — Test-first (NON-NEG)** | ❌ FAIL | CR-1, CR-2, CR-3, CR-4, CR-5, CR-6 — 100% branch claim retrospective overstated |
| **III — Clean Architecture (NON-NEG)** | ✅ PASS | admin-bypass split exemplary; no boundary violations |
| **IV — PCI DSS (NON-NEG)** | ✅ n/a | no payment surface in this slice |
| **V — i18n** | 🟡 PARTIAL | ux I2 hardcoded "Pagination" string |
| **VI — Inclusive UX** | 🟡 PARTIAL | CR-12 (role=alert), I3 (empty state anatomy), I6 (no error.tsx) |
| **VII — Perf & Observability** | 🟡 PARTIAL | silent-failure I-1, I-3, I-6 (logger.warn → error upgrades); missing OTel counters on probe-audit-fail + sweep-clear-fail + blob-upload-fail |
| **VIII — Reliability** | 🟡 PARTIAL | CR-8 silent catch; CR-9 fail-open mismatch override; CR-10 marker |
| **IX — Code Quality** | 🟡 PARTIAL | 25+ review-round tags, comment rot, dead `_UnusedTypes` |
| **X — Simplicity** | 🟡 PARTIAL | 6 HIGH-VALUE + 8 MEDIUM simplifications proposed |

**Net**: The retrospective claim of "10/10 PASS" is overstated. The three new Application use-cases ship with **integration coverage only** — no unit-level branch coverage. Per Principle II's literal requirement, this slice should not merge without CR-1, CR-2, and CR-3 closed.

---

## Recommended Action Plan (priority order)

### Phase 1 — Blockers (must close before R1 sign-off · est. 8-12 hrs)

1. **CR-7** (30 min) — `generate-error-csv-signed-url.ts`: run admin-probe in BOTH `not_found` AND `db_error` branches.
2. **CR-8** (45 min) — `import-csv.ts:484-523`: replace bare `catch {}` with structured `logger.warn` + metric + audit. Also fix 3 silent `return null` paths.
3. **CR-9** (1 hr) — strict-audit on `safeEmitMismatchOverride`: on emit failure return `unexpected_error` so import does NOT commit.
4. **CR-12** (5 min) — `role="status"` → `role="alert"` on error div.
5. **CR-13** (5 min) — add `timeZone: 'Asia/Bangkok'` to `Intl.DateTimeFormat` (or reuse `formatLocalisedDate` per simplifier H4 — kills two birds).
6. **CR-1** (3 hrs) — unit test file `generate-error-csv-signed-url.test.ts` covering 7+ branches incl. strict-audit invariant.
7. **CR-2** (2 hrs) — unit test file `sweep-expired-error-csv-blobs.test.ts` + cron contract test.
8. **CR-3** (30 min) — extend `error-csv-cross-tenant-isolation.test.ts` to assert `csv_import_error_csv_downloaded` audit row on success + absence on signing_failure.
9. **CR-4** (45 min) — extend `cancellation-cascade-eventcreate.test.ts` to assert `quota_credit_back_refund` audit row + quota allotments decrement.
10. **CR-5** (20 min) — extend `re-upload-idempotency-eventcreate.test.ts` to assert state-change audit row.
11. **CR-6** (30 min) — add first-time-Cancellation skip test scenario to T032.
12. **CR-10** (45 min) — add Symbol-brand to `CancellationSkipMarker` + emit low-severity `csv_import_row_cancelled_no_prior` audit on skip.
13. **CR-11** (5 min) — align severity label across audit-port comment + JSDoc + inline comment.

### Phase 2 — Important fixes (target R1 closeout · est. 6-10 hrs)

14. **code-review I-1 + ux I3 + ux I4 + ux I5** — `rowsStateChanged` UX + Empty state anatomy + Event/file column + Outcome badge — 90 min.
15. **code-review I-2** — adopt `gateCronBearerOrRespond` in sweep route — 20 min.
16. **code-review I-5** — add `csv_import_row_state_changed` audit event type — 1 hr.
17. **test-analyzer I-1** — adapterEnabled sub-flag unit test — 30 min.
18. **silent-failure I-1, I-3** — upgrade log levels + add metric counters — 30 min.
19. **silent-failure I-6** — sweep cron scan-failure to 500 — 15 min.
20. **ux I1 (CLS), I2 (hardcoded aria-label), I6 (error.tsx), I7 (reload affordance), I8 (command palette)** — 2 hrs.
21. **type-design refactors** — `rowsStateChanged` surface OR remove; `Extract<...>['value']` → named tuple; `adapterEnabled` REQUIRED — 90 min.

### Phase 3 — Suggestions batch (target R2 cleanup · est. 4-6 hrs)

22. Strip 25+ review-round metadata tags from comments (mass sed/regex pass).
23. Reuse `formatLocalisedDate` helper (simplifier H4) — also fixes BE display.
24. Drop dead `typeof === 'function'` guards (simplifier H1).
25. Drop `_UnusedTypes` suppression (simplifier H2).
26. Suggestion batch: requestId in sweep 500, PaymentStatus tightening, .orderBy() on findByEventAndEmail, log-injection sanitization, CR-5 missing nav entry, F6.2 saved-filters flag.

---

## Re-run guidance

After Phase 1 closeout, re-run **`/speckit-review F6.1 Round 2`** focusing on:
- Verify CR-1/CR-2 tests actually achieve 100% branch on the targeted invariants (vitest coverage report)
- Confirm CR-7/CR-8/CR-9/CR-10 audit chain integrity via fresh integration tests
- Confirm CR-12/CR-13 a11y + tz fixes via axe-core E2E
- Spot-check that comment cleanup didn't introduce drift in WHY-context (positive observation #4)

After Phase 2, re-run with the additional aspect **simplify** as a final polishing pass.

---

## Agent transcripts location

All 7 agent outputs were captured in the conversation history. The most actionable agent IDs (for follow-up SendMessage iterations if needed):

- code-reviewer: agentId `a8b59b2093d1732e8`
- comment-analyzer: agentId `a02f1d3cb8355e252`
- pr-test-analyzer: agentId `afbc66bd78d3b94fc`
- silent-failure-hunter: agentId `a03fc5f3af7f2493f`
- type-design-analyzer: agentId `adeb6031aa94a77e4`
- code-simplifier: agentId `a890e4ca1d4c86d4d`
- enterprise-ux-designer: agentId `acafdf71620137a5a`

---

**Reviewer**: Claude (Opus 4.7) via `/speckit-review` multi-agent orchestration
**Next gate**: `/speckit-review F6.1 Round 2` after Phase 1 closeout
