# Critique Report: CSV Import Primary Path + EventCreate Format Adapter (F6.1 / 013) — Pass 2

**Date**: 2026-05-15 13:37 (post-cuts, post-cascade)
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Previous critique**: [critique-20260515-122138.md](critique-20260515-122138.md)
**Verdict**: ✅ **PROCEED** (with 5 polish-level recommendations)

---

## Executive Summary

Pass-1 critique surfaced 24 findings; clarify session 2 + 7-item polish pass closed 23 of them (US3+US4 cut, F4 cross-cutting dropped, PDPA boolean classification, rollback sub-flag, inline event-create modal, timing-safe 404, zero-downtime migration notes, mid-flip behaviour, TTL sweep alerting, Blob upload failure handling). The remaining outstanding item from Pass-1 — P6 Eventbrite parallel evaluation — was acknowledged as strategic/non-blocking and explicitly deferred. The spec is now structurally leaner: **3 user stories instead of 5, ~21 FRs, 6 SCs, 2 migrations, 2 new audit events, no cross-module touch, ~5-6 implementation days**.

Pass-2 finds **0 🎯 Must-Address blockers** and **5 💡 Recommendations** — all polish-level. The most consequential is **X-R2-1: event-mismatch on re-upload safety net** (admin could select wrong event in the dropdown, causing attendees to be double-counted across two events because the idempotency key includes the selected event's `event_external_id`). This is admin error, not a system bug, but the user-facing protection is currently zero — worth a "you previously uploaded this file to a different event" warning.

The spec is **ready for `/speckit-tasks`**. The 5 recommendations can be addressed inline with task generation or deferred to implementation phase without blocking gate progression.

---

## Product Lens Findings 🎯

### Problem Validation

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P-R2-1 | ✅ — no finding | Pass-1's P1/P2 closed via user stakeholder answers (EventCreate sales confirmed Enterprise-only; 1 tenant uses EventCreate). Problem statement is strong. | No action |

### User Value Assessment

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P-R2-2 | 💡 | **US5 (history page) at 1-tenant + 1-admin scale**: the persistent listing page (FR-022, paginated table with TanStack) adds operational comfort but at SweCham scale (1 admin, ~50 imports/year) admin can remember last few imports without a listing UI. The HIGH-VALUE sub-feature is "Download error CSV from past import" (FR-021) which could be served from the result card's persistent link after each commit, OR from the admin's email/notification, without building a full history page. Splitting US5 into "error-CSV downloads (essential)" + "history listing UI (defer-able)" might shave ~1 day. NOT recommending the cut — just flagging it as observable scope. | Consider splitting US5 into US5a (error-CSV download per import — keep) + US5b (history listing UI — could defer to v1.x). If kept, leave the listing UI as low-priority polish during implementation — focus on error-CSV download value first. |

### Alternative Approaches

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P-R2-3 | ✅ — no finding | P6 Eventbrite-native parallel evaluation explicitly deferred at user request (strategic decision, separate roadmap). No new alternatives surfaced. | No action |

### Edge Cases & UX

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P-R2-4 | 💡 | **Event-mismatch on re-upload safety net missing** — see X-R2-1 cross-lens entry below. Admin who accidentally selects the wrong event in the dropdown can double-count attendees across two events because the idempotency hash includes `event_external_id`. The inline "Create event" modal (FR-003) makes this risk slightly higher because admin might create a near-duplicate event while in the import flow. | See X-R2-1 below. |
| P-R2-5 | 💡 | **`FR-003` inline event-create modal**: spec says "saves via the same use-case as `/admin/events/new`" but doesn't name the specific F6 use-case. Implementation could create event-creation surface drift if the import-page modal diverges from the canonical F6 event-create form. | Specify in plan.md or research.md: "modal reuses `src/modules/events/application/use-cases/create-event.ts` (or equivalent canonical name from F6 Phase 4/6) — admin-only RBAC, same Zod validation, same audit emit. The modal is presentation-only — no new use-case." |

### Success Measurement

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P-R2-6 | 💡 | **SC-008 measurability**: "90% of admins running their first EventCreate import succeed without contacting support, measured by support-ticket volume in the first 60 days post-launch." At 1-tenant scale this is unmeasurable (sample size = 1 admin × 1 first import = N/A statistical claim). The metric was originally written assuming multi-tenant onboarding. | Recast SC-008 for 1-tenant reality: "TSCC admin successfully completes the first EventCreate CSV import within 5 minutes of attempting it, with no support contact required" (binary, measurable from `csv_import_records` + admin self-report). OR drop SC-008 and rely on operational signals (OTel adapter-detection metric + post-launch retrospective). |

---

## Engineering Lens Findings 🔬

### Architecture Soundness

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E-R2-1 | ✅ — no finding | Reduced scope (no F4 cross-cutting, no dry-run tx, no match-preview component) results in cleanest architecture yet. Reuses 100% Phase 7 scaffolding. Single new Application port (`ErrorCsvStore`) + single new Infrastructure adapter. | No action |

### Failure Mode Analysis

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E-R2-2 | 💡 | **Concurrent import safety at multi-admin scale**: per-(tenant, event) advisory lock prevents two admins importing same event simultaneously. But the per-(tenant, event) lock does NOT prevent: (a) admin A importing event 1 while admin B imports event 2 of the same tenant — pool consumption = 2 × batchConcurrency = 6 connections (acceptable on Neon Singapore default 10); (b) admin importing while another admin manually edits a registration in F6 admin UI — race possible if edits and import-overwrites overlap. Today this is theoretical at 1-tenant + 1-admin, but documenting the invariant prevents surprise when scale grows. | Add to research.md or plan.md operational notes: "Concurrent operation invariants — (1) two imports of different events same tenant: OK (separate advisory locks); (2) import + manual UI edit on same registration: race possible — FR-019 (re-upload always wins) covers the conflict resolution but documents the race window. Multi-admin coordination is a v1.x consideration." |

### Security & Privacy Review

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E-R2-3 | ✅ — no finding | Pass-1's E6 PDPA disclosure surface tightened by Q1 (boolean only — no raw text), E7 audit-coverage tradeoff documented, E8 timing-safe 404 specified. PCI N/A confirmed. Cross-tenant tests planned. | No action |

### Performance & Scalability

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E-R2-4 | ✅ — no finding | Inherits Phase 7 SC-006 1k/60s; no dry-run tx; reduced UI surface. Performance budgets still adequate. | No action |

### Testing Strategy

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E-R2-5 | 💡 | **Inline event-create modal test coverage** (FR-003 P8 fix): the new modal adds a new UI surface that needs (a) unit test for the modal component, (b) integration test that "create event from modal → event auto-selected in dropdown → import proceeds", (c) a11y scan in the new visual state. Plan.md mentions "1 new visual state (event-picker)" for axe scan but doesn't differentiate the modal's open state from the dropdown-closed state. | Update plan.md § Testing inventory to add: "Inline event-create modal — unit test for modal component (open/close + form validation) + integration test for end-to-end flow (modal create → dropdown reflects → import succeeds) + a11y scan in modal-open state (extends to 2 visual states for event-picker surface)". Marginal test count increase ~3-4 tests. |

### Operational Readiness

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E-R2-6 | ✅ — no finding | TTL sweep monitoring documented (E5), mid-import flag-flip behavior documented (E14), zero-downtime migration notes (E13), rollback sub-flag + threshold (Q3). Operational story is complete. | No action |

### Dependencies & Integration Risks

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E-R2-7 | ✅ — no finding | Zero new npm dependencies (Constitution X). Vercel Blob reused from F4. F4 module untouched per Q2 cut. No cross-module dependency risks remain. | No action |

---

## Cross-Lens Insights 🔗

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|---------------|-------------------|------------|
| **X-R2-1** | **Event-mismatch on re-upload safety net missing** — Admin uploads CSV to Event A (first run, idempotency key includes `event_external_id_A`). On second run, admin accidentally selects Event B in the dropdown → idempotency hash differs (key now `event_external_id_B`) → all 84 rows process as new on Event B. Attendees now exist in BOTH events; partnership/cultural quotas DOUBLE-COUNTED for matched members; admin needs F6 admin UI to manually delete + credit-back. | Real admin-error risk at chamber scale. The inline event-create modal (P8 fix) slightly raises the risk because admin might create a near-duplicate event mid-flow. No UI safeguard currently — admin sees no warning before commit. Recovery is manual (delete wrong-event registrations + manual quota credit-back). | Idempotency design (FR-017) is intentionally event-scoped — a CSV uploaded to event A and the same CSV uploaded to event B are SEMANTICALLY different operations (e.g., same attendees attending two different chamber events). The hash captures this correctly. The vulnerability is in the human selection step, not the data model. | **Add a content-fingerprint pre-commit check** (~half day): when CSV is uploaded, compute a fingerprint over (attendee_email_set + Status=Attending count). Before commit, query `csv_import_records` WHERE `tenant_id = ? AND fingerprint = ? AND uploaded_at > NOW() - INTERVAL '30 days'` — if a match exists with a DIFFERENT `event_id` than the currently selected event, show admin a warning dialog: "These attendees were imported to event 'Event X' on date Y. You are importing to event 'Event Z'. Continue?" with a default-to-cancel. Admin can override but is informed. Captures the 80%-case admin-error scenario without locking the workflow. |

---

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | **0** |
| 💡 Recommendations | **5** |
| 🤔 Questions | **0** |
| Product findings | 6 entries (3 no-finding + 3 recommendation) |
| Engineering findings | 7 entries (4 no-finding + 3 recommendation) |
| Cross-lens findings | 1 (X-R2-1 event-mismatch) |

---

## Consolidated Findings Table

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| P-R2-2 | Product | 💡 | User Value | US5 history listing UI low-priority at 1-tenant scale | Optional: split into US5a (error CSV — keep) + US5b (history UI — defer). NOT recommending cut, just flagging |
| P-R2-4 | Product | 💡 | Edge Cases | Event-mismatch on re-upload safety net missing | See X-R2-1 |
| P-R2-5 | Product | 💡 | UX clarity | Inline event-create modal doesn't name F6 use-case | Specify reused use-case path in plan.md / research.md |
| P-R2-6 | Product | 💡 | Success Measurement | SC-008 unmeasurable at 1-tenant scale | Recast as binary outcome OR drop; rely on OTel signals |
| E-R2-2 | Engineering | 💡 | Failure Mode | Concurrent operation invariants not documented | Add note: 2 imports different events OK; import + manual-edit race covered by FR-019 |
| E-R2-5 | Engineering | 💡 | Testing | Inline modal test coverage not enumerated | Add modal unit + integration + 2nd a11y scan to plan.md test inventory |
| **X-R2-1** | **Both** | **💡** | **Scope × UX × Reliability** | **Event-mismatch re-upload → attendee double-count across events** | **Add content-fingerprint pre-commit check + warning dialog when fingerprint matches a recent (≤30 day) import to a DIFFERENT event. ~half day effort.** |

---

## Recommended Actions

### 🎯 Must-Address (Before Proceeding)

**None.** 0 🎯 blockers.

### 💡 Recommendations (Strongly Suggested — addressable during `/speckit-tasks` or implementation)

**Top 3 ranked by impact/effort**:

1. **X-R2-1 — Event-mismatch safety net** (~half day): add content-fingerprint check + warning dialog. Concrete spec edit: extend `csv_import_records` data model with `attendee_fingerprint TEXT` column; extend `importCsv` use-case to compute + lookup; add new outcome variant `event_mismatch_warning` with admin-confirm flow. This is the only recommendation with admin-visible UX impact.

2. **P-R2-5 — Name the F6 event-create use-case in plan** (~5 min): trivial documentation fix. Closes the inline-modal ambiguity.

3. **P-R2-6 — Recast SC-008** (~5 min): drop or rewrite as binary outcome for 1-tenant scale.

### Polish recommendations (defer to implementation phase if desired)

4. **E-R2-2 — Concurrent operation invariants doc**: add ~3 lines to research.md or plan.md notes.

5. **E-R2-5 — Inline modal test coverage enumeration**: extend plan.md test inventory by ~3-4 tests.

### Observations (not recommendations)

- **P-R2-2 US5 split**: presented for awareness; current scope is acceptable.

### 🤔 Questions (Need Stakeholder Input)

**None.** All previous questions answered by user in Pass-1 follow-up.

---

## Comparison vs. Pass-1 Critique

| Metric | Pass-1 (2026-05-15 12:21) | Pass-2 (2026-05-15 13:37) |
|---|---|---|
| 🎯 Must-Address | 0 | **0** |
| 💡 Recommendations | 15 | **5** |
| 🤔 Questions | 3 | **0** |
| Cross-lens findings | 4 | **1** |
| Verdict | ⚠️ PROCEED WITH UPDATES | ✅ **PROCEED** |

**Net effect of clarify session 2 + 7-item polish pass**: closed 19 of Pass-1 findings, deferred 1 (P6 Eventbrite — strategic), partially-addressed 2 (P10 onboarding, SC-008 measurability). Pass-2 surfaces 5 new minor items that emerged after the scope reduction crystallised — none blocking.

---

## Verdict

✅ **PROCEED to `/speckit-tasks`**

**Rationale**:
- 0 🎯 Must-Address blockers
- 5 💡 Recommendations are polish-level + can be addressed inline during task generation (X-R2-1 is the only one with non-trivial impl effort but is also marked as defer-able to v1.x if user prefers shipping the leaner MVP first)
- Spec/plan/research/data-model/contracts/quickstart artefacts are internally consistent after cascade
- Constitution Check 10/10 PASS
- Clarifications session 2 closed all 5 prior questions
- Critique pass-1 findings 19/24 closed via clarify + polish, 1 strategic deferred, 4 partial-or-now-moot

**Recommended path**:
1. Optionally apply X-R2-1 (event-mismatch safeguard) if you value pre-commit warning over scope minimality — ~half day spec + impl effort
2. Apply trivial fixes P-R2-5 + P-R2-6 + E-R2-2 + E-R2-5 (~15 min spec edits total)
3. `/speckit-tasks` to break feature into ~30-40 tasks

---

**Severity Legend**:
- 🎯 **Must-Address**: Blocks proceeding to implementation
- 💡 **Recommendation**: Strongly suggested improvement
- 🤔 **Question**: Needs stakeholder input to resolve

---

*Generated by `/speckit-critique-run` Pass 2 — Dual-lens strategic and technical review after clarify session 2 cuts. AI assistant executes the workflow myself per `feedback-speckit-skill-execution` memory.*
