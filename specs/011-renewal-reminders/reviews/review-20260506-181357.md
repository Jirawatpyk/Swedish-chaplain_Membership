# Staff Review Report — F8 Renewal Tracking Round 11 (K-Wave Closure)

**Date**: 2026-05-06 18:13:57 (Asia/Bangkok)
**Branch**: `011-renewal-reminders`
**Scope**: K1–K11 commits (`8e09ddf4..0e352330`, 11 commits) — remediation pass that closed 48+ findings from the 7-agent /speckit.review (Round 10).
**Reviewers**: 5 specialist agents in parallel (chamber-os-architect · reliability-guardian · senior-tester · security-threat-modeler · enterprise-ux-designer) + staff-engineer synthesis
**Verdict**: ⚠️ **APPROVED WITH CONDITIONS** — **0 Blockers** + **9 Warnings** + **11 Suggestions**

---

## Executive Summary

**Trajectory**: Round 1 (50+ findings) → R2 (24) → R3 (14) → R4 (9, 1 Blocker) → R5 (10, 0 Blockers) → R6–R10 cumulative remediation → **R11 (20 total, 0 Blockers, 9 Warnings)**.

K-wave (commits K1–K11) closed all 12 critical + 16+ important + 20+ suggestion findings from the Round 10 7-agent /speckit.review. The closures are correct: K1 fixed correlationId forgery on 2 cron routes + threw→err in 4 use-cases + outer-catch audit emit (Principle VIII); K2 closed FR-049 (7 forbidden-log paths) + FR-052b (404+audit on kill-switch); K3 added BLK-1 + BLK-2 retry CTA + cache-bust; K5 added Resend permanent-error allowlist + kill-switch counter; K6 added Gate 1+2 tests + isPermanentGatewayError pin + `cron_bearer_auth_rejected` audit (54→55 events); K7 added 3 compile-time count assertions + emitSkipAudit exhaustiveness; K9 closed UX empty-state secondary link + 2 redundant-aria sites; K10 fixed test for K9; K11 closed 16 pre-existing parallel-test flakes via server-only stub + testTimeout 30s.

R11 Warnings cluster around 4 areas: **(a)** test pins missing for newly-added audit emissions (TST-K-2, TST-K-4); **(b)** observability gaps around novel Resend error names + audit DoS amplification on cron 401 (REL-K-2, SEC-K-5); **(c)** UX K3-BLK-2 Retry CTA semantics (UX-K-3); **(d)** K9 a11y closure missed UrgencyPill (UX-K-6). All 9 Warnings are well-bounded and resolvable in a single K12 wave (~150 LOC + ~6 new tests). **No Constitution NON-NEG violation**; no new attack surface; no regression.

---

## Findings Table

### 🔴 Blockers (0)

None. K-wave closes all R10 critical findings cleanly. No new ship-blocking surface introduced.

### 🟡 Warnings (9)

| ID | Pass | File:Line | Description | Recommendation |
|----|------|-----------|-------------|----------------|
| **UX-K-3** | UX | `src/app/(staff)/admin/renewals/settings/schedules/page.tsx:121-133` | K3-BLK-2 Retry + Go-back use `<Link>` (navigation) for what is an **action** (cache-bust + RSC re-fetch). Violates ux-standards.md § 4.3 + WCAG SC 4.1.2 (Name/Role/Value). Also `?_retry=${correlationId}` pollutes URL bar + browser history; no busy state during in-flight retry. | Convert Retry to `<form action>` or `<Button onClick={() => router.refresh()}>` wrapped in `useTransition` for pending state. Display correlationId as `<code className="text-xs font-mono">` text below card, NOT as URL query param. |
| **UX-K-6** | UX | `src/components/renewals/urgency-pill.tsx:54` | K9 closed redundant `aria-label` on TierBadge + LapsedTab reason badge but **missed UrgencyPill** — same non-interactive `<span>` + visible text pattern. Older VoiceOver double-announces "T-30 days, T-30 days". Same WCAG SC 4.1.2 issue K9 already fixed elsewhere. | Remove `aria-label={label}` at urgency-pill.tsx:54 + add unit test analogous to `tier-badge.test.tsx:48-60` (assert `getAttribute('aria-label') === null`). |
| **REL-K-1** | Reliability | 7 admin route handlers (`route.ts`/`[cycleId]/*.ts`/`schedules/[tierBucket]/route.ts`) | Catch-block log uses `err: e instanceof Error ? e.message : String(e)` — passes a STRING to pino's `err` serializer field. Pino's `err` serializer expects an `Error` instance to render `stack`, `type`, `code`. String form drops stack trace + breaks Sentry/Grafana `err.type` filters. Diverges from `dispatch-renewal-cycle.ts:263-266` + `dispatch-coordinator/route.ts:162-164` which DO pass Error objects. | Change to `err: e instanceof Error ? e : new Error(String(e))` in all 7 sites for stack-trace + type capture. |
| **REL-K-2** | Reliability | `src/modules/renewals/infrastructure/resend-transactional-renewal-gateway.tsx:194-199` | K5-H1 added `logger.warn` for novel Resend error names but does NOT emit a metrics counter. If Resend rolls a new permanent error code, it silently burns 24h transient-retry budget × N members until someone greps logs. No alert path. | Add `renewalsMetrics.unknownResendErrorName()` counter alongside warn-log; create alert rule "any non-zero rate over 5 min". One-line code change + Grafana rule. |
| **SEC-K-2** | Security (STRIDE-I) | `src/modules/renewals/infrastructure/resend-transactional-renewal-gateway.tsx:195` | `logger.warn` logs `resendErrorMessage: message` — free-text from Resend SDK. Novel error shapes could embed account-scoped identifiers (sending domain, API-key prefix, account ID). Field name `resendErrorMessage` is NOT covered by current REDACT_PATHS — no `*.message` wildcard for this path. | Either (a) log only `resendErrorName` (drop `resendErrorMessage` entirely), OR (b) add `resendErrorMessage` to REDACT_PATHS in `src/lib/logger.ts`. Option (a) preferred (less data + less to maintain). |
| **SEC-K-5** | Security (STRIDE-D) | `src/app/api/cron/renewals/dispatch-coordinator/route.ts:145-168` (401 audit-emit path) | K6 added `cron_bearer_auth_rejected` audit emit on 401 path, but no rate-limit guard sits in front. Sustained bad-Bearer probing at 1k req/s → 1k INSERT/s into `audit_log`. Vercel concurrency limits help but do not eliminate the DB-write-amplification vector. | Add Upstash rate-limit check (reuse F1 sign-in pattern: `f8:cron:bearer-auth:${ip}` 60/60sec) BEFORE the audit emit on the 401 path. Audit only emits when rate-limit allows; subsequent 401s short-circuit without DB write. |
| **TST-K-2** | Tests | `tests/unit/renewals/application/use-cases/dispatch-renewal-cycle.test.ts` (gap) | K1-C8 outer-catch audit emit (`renewal_reminder_send_failed` before synthetic `failed_transient`) has **no direct test pin**. Existing test "per-cycle exception is isolated" (line 150-168) pins the failedTransient counter but not the audit type. `fakeDeps` does not wire an audit emitter. | Add audit emitter mock to `fakeDeps`. Add test "outer-catch per-cycle exception emits renewal_reminder_send_failed audit with failure_kind=dispatcher_crash". |
| **TST-K-4** | Tests | `tests/unit/api/cron/renewals/dispatch-coordinator.test.ts:59-63` + `per-tenant.test.ts:96-99` | K6 added `cron_bearer_auth_rejected` audit emission on the 401 Bearer-rejected path but **no test asserts the audit type emitted**. Existing tests pin only HTTP status + error code. Security-critical audit path; Constitution Principle II expects 100% branch coverage on auth-related code. | In coordinator test: add `expect(auditEmitMock).toHaveBeenCalledWith(expect.objectContaining({ type: 'cron_bearer_auth_rejected' }), ...)` to both 401 cases. In per-tenant test: wire `auditEmitMock` first, then add same assertion. |
| **CON-K-3** | Architecture | `src/modules/renewals/application/ports/renewal-audit-emitter.ts` (F8AuditPayloadShapes) | New `cron_bearer_auth_rejected` (K6) lacks a typed entry in `F8AuditPayloadShapes` — falls back to `Record<string, unknown>`. Coordinator emits `{ route: '/api/cron/renewals/dispatch-coordinator' }` but a future caller typo `path` instead of `route` would not be caught at compile time. Inconsistent with `renewal_kill_switch_blocked` which IS typed. | Add `cron_bearer_auth_rejected: { readonly route: string }` (or richer shape with `source_ip_hash` per SEC-K-4) to `F8AuditPayloadShapes`. |

### 🟢 Suggestions (11)

| ID | File:Line | Description |
|----|-----------|-------------|
| UX-K-2 | `schedule-editor.tsx:494-503` | Catch block does not differentiate offline (`TypeError: Failed to fetch`) from server 500 / validation error. ux-standards § 4.4 prefers context-specific copy ("Could not reach server — check your connection"). Add `e instanceof TypeError && /fetch/.test(e.message)` branch + new i18n key `error.offline`. |
| UX-K-5 | `empty-state.tsx` (primary CTA variant) | "View all members" uses `buttonVariants({ variant: 'outline', size: 'sm' })`. ux-standards § 3.1 specifies primary CTA = solid (`variant: 'default'`). Cosmetic but explicit standards violation. |
| UX-K-7 | F8 a11y E2E gap | K10 unit test pins `aria-label === null` at attribute level. No screen-reader-level E2E spec exists for tier-badge / urgency-pill double-announcement. axe-core does NOT flag redundant aria-label that matches text content (not a violation per axe rules). Add manual SR sanity-test annotation per ux-standards § 15. |
| REL-K-3 | `src/app/api/admin/renewals/route.ts:48-94` | Kill-switch check runs BEFORE `requireRenewalAdminContext` (which embeds rate-limit). Authed admin probes do not consume rate-limit budget when feature flag is off. Low practical risk (route is session-gated) but document the intent: "kill-switch responds 404 BEFORE auth-context to fully disguise feature existence". |
| SEC-K-3 | `src/app/api/admin/renewals/route.ts:48` | 404 body `{"error":{"code":"feature_disabled"}}` partially defeats the 404 disguise — admin-authed probes can fingerprint via the `feature_disabled` code. Replace with generic `not_found` body for full disguise (low priority — authed scope only). |
| SEC-K-10 | Legacy cron routes (`lockout-cleanup`, `outbox-dispatch`, `sweep-stale`, `outbox-purge`) | Still use `requestIdFromHeaders()` which reads `x-request-id` from inbound headers. Not introduced by K-wave — pre-existing. Migrate to `uuidv7()` per K1 pattern in F9 hardening pass for full correlationId-forgery closure across the platform. |
| TST-K-1 | `tests/unit/renewals/application/ports/is-permanent-gateway-error.test.ts` | K6 covers 4 permanent kinds + 1 transient (`gateway_5xx`). Other transient kinds (e.g. `recipient_temporary`) not tested. Classifier uses if-chain → no compile-time exhaustiveness. Add `it.each` for all transient kinds + one `it('returns false for unrecognized kind', () => f({kind: 'unknown_future' as never}))`. |
| TST-K-3 | `tests/contract/renewals/admin-pipeline-route.test.ts:105-113` | Contract pins 404 + `error.code === 'feature_disabled'` but does NOT assert `renewal_kill_switch_blocked` audit was emitted. One-liner: `expect(auditEmitMock).toHaveBeenCalledWith(expect.objectContaining({ type: 'renewal_kill_switch_blocked' }), ...)`. |
| TST-K-5 | `src/modules/renewals/application/use-cases/_lib/dispatch-one-cycle.ts:92-95` | `_AssertSkipReasonCount extends 13` is the SOLE pin for the 13-member tuple length. Audit-event tuple has dual coverage (type-assert + runtime `.length === 55`). Add one-liner `expect(SKIP_REASONS.length).toBe(13)` to a unit test for parity. |
| CON-K-1 | `tests/unit/api/cron/renewals/dispatch-coordinator.test.ts` (gap) | K5 `tenants_skipped_kill_switch` counter has no end-to-end test (mock per-tenant fetch returning `{skipped:true, reason:'feature_flag_disabled'}` then assert counter == 1, succeeded == 0). Code is correct but unprotected from regression. |
| CON-K-2 | `src/modules/renewals/application/ports/is-permanent-gateway-error.ts` | Classifier uses if-chain; no exhaustiveness. If `DispatchFailureKind` adds a variant, classifier silently misses. Convert to `switch` with `default: const _exhaustive: never = kind` pin or use `satisfies` pattern. Aligns with K1-E1 exhaustiveness pattern in route handlers. |

---

## Spec Coverage Matrix (R10 → R11)

| R10 Finding Theme | Closure Wave | R11 Status |
|----|--------------|-----------|
| Critical security/correctness (5 items) | K1 | ✅ Closed; ⚠️ TST-K-2 audit-pin gap |
| FR-049 forbidden log paths | K2 | ✅ Closed cleanly (logger-redaction.test.ts is exemplary) |
| FR-052b kill-switch 404+audit | K2 | ✅ Closed; ⚠️ TST-K-3 audit-pin missing; 🟢 SEC-K-3 body fingerprint |
| UX BLK-1 unsaved-changes guard | K3 (deferred to K9b) | ✅ Correctly deferred (verified no silent ship) |
| UX BLK-2 retry CTA + cache-bust | K3 | ⚠️ UX-K-3 (Link vs button + URL pollution) |
| Comment rot (8 sites) | K4 | ✅ Closed |
| 3 reliability HIGH (Resend allowlist, kill-switch counter, StepRow key) | K5 | ✅ Closed; ⚠️ REL-K-2 metrics gap; ⚠️ SEC-K-2 message leak; ⚠️ CON-K-1 counter test gap |
| Coverage gaps (4 of 8) | K6 | ✅ Closed; ⚠️ TST-K-4 cron audit-pin missing; ⚠️ CON-K-3 untyped payload |
| Type design hardening (count + exhaustiveness) | K7 | ✅ Closed; 🟢 TST-K-5 SKIP_REASONS runtime pin; 🟢 CON-K-2 classifier exhaustiveness |
| L6 + M5 simplifications | K8 | ✅ Closed (no findings) |
| UX polish (empty-state + redundant aria) | K9 | ⚠️ UX-K-6 UrgencyPill missed; 🟢 UX-K-5 outline variant; 🟢 UX-K-7 SR-level E2E |
| Test fix for K9 | K10 | ✅ Closed cleanly |
| 16 pre-existing parallel-test flakes | K11 | ✅ Closed (server-only stub + testTimeout 30s); honest commit comment |

**12/13 R10 themes fully closed; 4 themes have residual R11 Warnings (test pins + UX polish).**

---

## Test Coverage Assessment

- **3787 / 3787 unit + contract tests GREEN** ✅ (K11 cleared the 16 pre-existing flakes)
- **Quality gates**: typecheck ✓ · lint ✓ (0/0) · check:i18n ✓ (1859 × 3 locales) · check:audit-counts ✓ (F2 14↔14, F8 55↔55) · check:multi-tenant ✓
- **K6 Gate 1+2 + isPermanentGatewayError**: assertions pin exact audit types + `vi.doMock` env override pattern correct. Strong.
- **K2 logger-redaction.test.ts**: imports REDACT_PATHS from production source → drift-proof. All 6 FR-049 paths covered at depth ≥2 + camelCase + snake_case. Exemplary.
- **K1 throw→err migration in 4 use-case tests**: triple-assertion pattern (`.ok=false` → `.error.kind='server_error'` → `.error.message` regex). No weak migrations.
- **K10 tier-badge.test.tsx**: `getAttribute('aria-label') === null` explicit assertion. Strong attribute-level pin.
- **Residual gaps**: 5 audit-emission test pins missing (TST-K-2, TST-K-3, TST-K-4 + CON-K-1, CON-K-2 — closed via 6 new test cases ≈ 80 LOC).

---

## Constitution v1.4.0 Alignment

| Principle | Status | Notes |
|-----------|--------|-------|
| **I — Tenant Isolation (NON-NEG)** | ✅ Pass | K1 uuidv7 did not replace tenant context resolution; runInTenant + RLS unchanged; check:multi-tenant ✓ |
| **II — Test-First (NON-NEG)** | ⚠️ At-risk | TST-K-2, TST-K-3, TST-K-4 leave 3 audit-emission paths unpinned; security-critical paths ought to be 100% branch-covered |
| **III — Clean Architecture (NON-NEG)** | ✅ Pass | K1 server_error variant in Application Result union; K7 count assertions are TYPE-only; K6 cron_bearer_auth_rejected in Application port |
| **IV — PCI DSS (NON-NEG)** | ✅ N/A for K-wave | F8 has no payment surface; SEC-K-2 mitigation hardens FR-049 logging discipline (Principle I) |
| V — i18n | ✅ Pass | K9 +1 key (settingsLink) × 3 locales; TH/SV translations natural; 1857 → 1859 keys |
| VI — Inclusive UX | ⚠️ At-risk | UX-K-3 (Retry CTA WCAG 4.1.2); UX-K-6 (UrgencyPill missed by K9 redundant-aria sweep); UX-K-5 (primary CTA variant) |
| VII — Perf & Observability | ⚠️ At-risk | REL-K-2 (no metrics on novel Resend error); REL-K-1 (string-not-Error in 7 routes degrades structured-log fidelity) |
| VIII — Reliability | ✅ Pass | K1-C8 outer-catch audit emit + best-effort log fallback; K5 kill-switch counter correctly disambiguates 404 vs 500 vs network reject |
| IX — Code Quality | ✅ Pass | K11 commit message trajectory honest; Conventional Commits; solo-maintainer substitute; 0 lint warnings |
| X — Simplicity | ✅ Pass | K8 simplifications net-reduce; K7 count-assert pattern is dead-simple compile-time guard |

**4 NON-NEG Principles**: 3 ✅ Pass · 1 ⚠️ At-risk (II — closeable in K12 with 6 test additions). **No Constitution Blocker.**

---

## Metrics

- **Findings**: **0 Blockers** + **9 Warnings** + **11 Suggestions** = 20 total
- **Files reviewed**: ~30 K-wave-touched files (out of 276 in full F8 diff)
- **K-wave commits**: 11 (K1–K11)
- **Tests**: 3787 / 3787 GREEN (was 3771 + 16 fail before K11)
- **Quality gates**: 5/5 GREEN (typecheck · lint · check:i18n · check:audit-counts · check:multi-tenant)
- **Audit events**: F8 55 ↔ 55 ✓; F2 14 ↔ 14 ✓
- **i18n**: 1859 keys × 3 locales (EN canonical + TH + SV)
- **Cumulative trajectory**: 50+ → 24 → 14 → 9 → 10 → … → **20 R11**, 0 Blockers since R5

---

## Recommended Actions

### 🟡 Wave K12 — Conditions for ✅ APPROVED (close before flag-flip)

1. **UX-K-3** — Convert Retry CTA from `<Link>` to `<Button onClick={() => router.refresh()}>` wrapped in `useTransition`. Move correlationId out of URL into `<code>` text under card. Add busy state indicator. (~30 LOC + 1 unit test)
2. **UX-K-6** — Remove `aria-label={label}` from `urgency-pill.tsx:54` + add unit test `expect(getAttribute('aria-label')).toBeNull()` parallel to TierBadge K10 test. (~10 LOC)
3. **REL-K-1** — Change `err: e.message` → `err: e instanceof Error ? e : new Error(String(e))` in 7 admin route handlers. (~7 line edits)
4. **REL-K-2** — Add `renewalsMetrics.unknownResendErrorName()` counter alongside K5 `logger.warn`; create alert rule. (~5 LOC + Grafana rule)
5. **SEC-K-2** — Drop `resendErrorMessage` from K5 warn-log (log only `resendErrorName`); OR add `resendErrorMessage` to REDACT_PATHS. Option A preferred. (~2 line edits)
6. **SEC-K-5** — Add Upstash rate-limit on cron-coordinator 401 path BEFORE audit emit. Reuse F1 sign-in limiter pattern. (~15 LOC + 1 unit test)
7. **TST-K-2** — Wire audit emitter into `fakeDeps` of `dispatch-renewal-cycle.test.ts` + add test pinning K1-C8 outer-catch `renewal_reminder_send_failed` emission. (~25 LOC)
8. **TST-K-4** — Add audit-type assertion to both 401 tests in coordinator + per-tenant test files (wire `auditEmitMock` in per-tenant first). (~20 LOC)
9. **CON-K-3** — Add typed `cron_bearer_auth_rejected` payload shape to `F8AuditPayloadShapes`. (~5 LOC)

**Estimated K12 effort**: ~120 LOC source + ~80 LOC tests + ~3 i18n keys (none expected). Single session. No DB migration. Audit-event count stays at 55.

### 🟢 Track as Phase 4+ Backlog (post-K12 polish)

10. **UX-K-2** — Offline vs server-error differentiation in schedule-editor catch (~10 LOC + 1 i18n key).
11. **UX-K-5** — Empty-state primary CTA `outline` → `default` variant (1 line).
12. **UX-K-7** — Manual SR-level E2E annotation per ux-standards § 15.
13. **REL-K-3** — Document kill-switch + rate-limit ordering rationale in `route.ts` comment.
14. **SEC-K-3** — Replace `feature_disabled` with `not_found` body for full 404 disguise (1 line).
15. **SEC-K-10** — Migrate legacy crons (`lockout-cleanup`, `outbox-dispatch`, `sweep-stale`, `outbox-purge`) from `requestIdFromHeaders()` → `uuidv7()` in F9 hardening pass.
16. **TST-K-1** — `it.each` for all transient kinds + exhaustiveness defence in `is-permanent-gateway-error.test.ts`.
17. **TST-K-3** — `renewal_kill_switch_blocked` audit assertion in admin-pipeline-route contract test.
18. **TST-K-5** — Runtime `.length` complement to `_AssertSkipReasonCount` type assertion.
19. **CON-K-1** — End-to-end test of `tenants_skipped_kill_switch` counter path through coordinator.
20. **CON-K-2** — Convert `isPermanentGatewayError` if-chain to `switch` with `never` exhaustiveness pin.

---

## Verdict

⚠️ **APPROVED WITH CONDITIONS** — second consecutive round at **0 Blockers**.

K-wave (K1–K11) is the cleanest remediation pass to date. All R10 Critical findings closed correctly; no Constitution NON-NEG violation; no new attack surface; +16 net tests (3771 → 3787); +1 i18n key × 3 locales; +1 audit event with proper assertions. The 9 Warnings are well-bounded test-pin gaps (4) + observability/UX polish (5) — no source bug, no architectural deviation.

**Conditions for full ✅ APPROVED at Round 12** (all in a single K12 wave):

1. UX-K-3 + UX-K-6 (UX/a11y closure parity with K9)
2. REL-K-1 (Error-object logging across 7 routes)
3. REL-K-2 + SEC-K-2 + SEC-K-5 (3 observability/security hardening items in resend-gateway + cron 401)
4. TST-K-2 + TST-K-4 (2 missing audit-emission test pins for security-critical paths)
5. CON-K-3 (typed payload shape for `cron_bearer_auth_rejected`)

**Trajectory verdict**: F8 Phase 4 has converged through 11 staff-review rounds. K12 closes the final block of conditions; expect ✅ **APPROVED** at Round 12 with ~200 LOC effort. F8 is positioned for ship-dark merge to main with `FEATURE_F8_RENEWALS=false` after K12; full flag-flip after the 11 deferred-suggestion items in 21+.

---

## Next Steps

1. Apply Wave K12 (conditions 1-9) — single session, no DB migration.
2. Re-run `/speckit.staff-review round 12` for closure confirmation; expect ✅ APPROVED verdict.
3. After R12 ✅: merge to main + ship dark with `FEATURE_F8_RENEWALS=false`.
4. Track Suggestions 10-20 in Phase 4 backlog before flag-flip at MVP-wide chamber go-live (Option C per /speckit.clarify round-3).
5. Pre-flag-flip: complete deferred K-wave residuals (K5b orphan-pending sweep, K6b a11y E2E + zero-tenant + parallel-coordinator tests, K7b TenantTx brand cleanup, K8b larger refactors, K9b List-Unsubscribe header).
