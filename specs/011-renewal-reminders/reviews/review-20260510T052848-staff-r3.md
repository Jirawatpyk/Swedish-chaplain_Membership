# F8 Phase 10 — Staff Review Round 3 (T275)

**Date**: 2026-05-10
**Reviewer**: senior-tester agent (independent triangulation against r4-code-reviewer)
**Scope**: Phase 10 polish (T261-T268, T271 new files + perf-benchmarks.md)
**Lens**: Test correctness + invariant soundness from a senior tester's perspective

## Summary

- **0 BLOCKER**
- **1 CRIT** (test passes silently when seed is broken)
- **4 IMP**
- **3 SUG**

Most findings overlap with R4 code-reviewer (H1 ↔ F5; H2/M1/M2 not covered by senior-tester; F3 net-new from senior-tester lens).

## CRIT

### F3 — T264 missing `expect(out.suggestionsCreated).toBeGreaterThan(0)` assertion

**File**: `tests/integration/renewals/tier-upgrade-evaluate-perf.test.ts`

Bench captured `suggestionsCreated=0 / alreadyAtTarget=999` despite seeding 33% of members above-threshold. With no positive-path assertion, a broken seed (e.g. members lack `joined_at` or `status='active'` so candidate-list query filters everyone) lets the test pass green while measuring only the early-exit branch.

**Recommended fix**: investigate why no suggestions create + add `expect(out.suggestionsCreated).toBeGreaterThan(0)` to pin the suggestion-create branch is exercised. **Note**: the perf NUMBER (1.15ms/member) is still valid since the loop did iterate; only the branch coverage claim is undertested.

## IMP

### F1 — Percentile formula duplicated across T261 + T265 with no unit test

Extract to `tests/integration/helpers/percentile.ts` + add 3-case unit test (empty array, single sample, sorted-50-samples).

### F2 — T261 warmup uses only `t-90`; measured loop cycles 4 urgency tabs

Cold Postgres planner cache for `t-30`, `t-7`, `lapsed` first hit during measurement → tail-latency skew.

**Fix**: warmup loop should cycle the same 4 urgency tabs as the measured loop.

### F4 — T265 comment-code mismatch

File comment claims `Status='issued'` but `invoiceRows.push({status: 'draft'})`. Mirrors self-service-renewal-tx test pattern (draft is FK-resolvable). Update comment to match.

### F5 — T271 RBAC `[403, 404]` accept-array

Same finding as R4 H1. Drop 404; route exists, expect strict 403.

## SUG

### F6 — T262 cron-dispatch finding (84.95s @ 1k) deserves a formal Phase 11 ticket

Already noted in `perf-benchmarks.md` § T262 finding + tasks.md T262 closure. Recommend opening `phase-10-backlog.md` entry with batched-write pattern reference (T159b precedent).

### F7 — T268 BE-year regex `/256[5-9]/` only covers 2022-2026 CE → 2565-2569 BE

Will silently pass-through 2027+ CE (= 2570+ BE) in 12 months. Extend to `/25[6-9]\d/`.

### F8 — perf-bench doc could include CV (coefficient of variation) per measurement

p50/p95/p99 are summary stats; CV = stddev/mean shows variance. Useful to detect noise-vs-signal regressions in trend tracking.

## Conclusion

R4 code-reviewer's H1+H2+H3 + M1/M2/M3 + this report's F3 cover the meaningful gaps. F1/F2/F6/F7/F8 are improvement-class follow-ups suitable for a future polish wave. The 4 perf benches and SC-004 baseline doc (T266) are sound.
