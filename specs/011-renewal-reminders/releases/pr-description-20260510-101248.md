## Summary

**F8 — Renewal Tracking + Smart Reminders** (`011-renewal-reminders`).

The eighth feature of Chamber-OS, F8 introduces an end-to-end renewal lifecycle for chamber membership organisations: a tier-aware reminder cadence, a 6-state cycle state machine, an 8-factor at-risk score, an auto tier-upgrade pipeline, a member self-service portal, and a manual escalation queue — all wired through F4 (invoicing) and F5 (payments) via bridge ports. Ships **dark** behind `FEATURE_F8_RENEWALS=false`; 5 explicit pre-flag-flip operator gates remain (documented in retrospective.md).

- 6 user stories (US1–US6: Pipeline Dashboard / Tier-Aware Reminder Schedule / Self-Service Renewal / At-Risk Detection / Auto Tier-Upgrade / Manual Escalation Queue)
- 73 functional requirements + 12 success criteria + 64 audit event types
- 9 F8-owned tables across 22 migrations (0086–0098 + 0115 + 0121–0122)
- New bounded context `src/modules/renewals/`
- 4 cron coordinators (dispatch / at-risk / lapse / reconcile-pending-reactivations) on cron-job.org

## Specification

| Artefact | Path |
|---|---|
| Spec | [`specs/011-renewal-reminders/spec.md`](../specs/011-renewal-reminders/spec.md) |
| Plan | [`specs/011-renewal-reminders/plan.md`](../specs/011-renewal-reminders/plan.md) |
| Tasks (285) | [`specs/011-renewal-reminders/tasks.md`](../specs/011-renewal-reminders/tasks.md) |
| Research | [`specs/011-renewal-reminders/research.md`](../specs/011-renewal-reminders/research.md) |
| Data Model | [`specs/011-renewal-reminders/data-model.md`](../specs/011-renewal-reminders/data-model.md) |
| Contracts | [`specs/011-renewal-reminders/contracts/`](../specs/011-renewal-reminders/contracts) |
| Retrospective | [`specs/011-renewal-reminders/retrospective.md`](../specs/011-renewal-reminders/retrospective.md) |

## Implementation

**Phases**: 10 phases × 285 tasks (100% closure: 280 closed in-session + 5 deferred-with-rationale operator gates).

**Architecture highlights** (full provenance in retrospective.md § "Architecture drift"):
- **Cross-feature wiring**: F4 `onPaidCallback` + F5 `processChargeRefunded` + F2 `scheduleNextRenewalPlanChange` ports — bridge contract LOCKED Option A (atomic-tx with rollback per Constitution Principle VIII state↔audit atomicity)
- **Concurrency model**: per-(tenant, member) advisory locks namespaced `renewals:dispatch:`, `renewals:atrisk:`, `renewals:tier-upgrade:` (disjoint from F4 `invoicing:` and F5 `payments:` namespaces)
- **Batched at-risk recompute**: P1 innovation pattern — 38× speedup over per-row writes (precedent T159b)
- **Tier-aware reminder cadence**: 4-band schedule resolved per tier configuration (regular / silver / gold / premium / partner / honorary)
- **Audit taxonomy**: 64 event types under `audit_event_type` pgEnum (5y default retention; F4 tax-document events keep 10y per migration 0039 backfill)
- **Multi-tenant isolation**: 50/50 cross-tenant probes GREEN across F8 surfaces (Constitution Principle I § sub-clauses 1–5 satisfied)

**Source modules added**:
```
src/modules/renewals/
├── domain/             # 6 aggregates + 8 value objects + 4 policies
├── application/        # 24 use-cases + bridge ports
└── infrastructure/     # Drizzle repos + cron coordinators + audit emitter
```

**New surfaces** (6):
- `/admin/renewals` (Pipeline)
- `/admin/renewals/[cycleId]` (Cycle detail)
- `/admin/renewals/tasks` (Escalation queue)
- `/admin/renewals/tier-upgrades` (Tier-upgrade queue)
- `/portal/renewal/[memberId]` (Member self-service)
- `/portal/preferences/renewals` (Renewal notification preferences)

## Testing

| Suite | Result |
|---|---|
| Unit + Contract | ~3173 tests GREEN |
| F8 Integration | 119 tests GREEN on live Neon Singapore |
| F8 E2E (a11y + i18n + manager-readonly) | 3 spec files, smoke-validated |
| Tenant Isolation | 50/50 cross-tenant probes GREEN |
| `pnpm typecheck` | GREEN |
| `pnpm lint` | 0 errors / 0 warnings |
| `pnpm check:i18n` | GREEN — 2242 keys × EN+TH+SV |
| `pnpm check:layout` | GREEN — 86 page/loading pairs consistent |
| `pnpm check:multi-tenant` | GREEN — 26/26 SCOPED tables |
| `pnpm audit --prod` | 0 HIGH / 0 CRITICAL (2 moderate transitive Next.js) |

**Performance benchmarks** (Phase 10 perf wave, PERF_MEMBER_COUNT=1000 + linear extrapolation noted):
| Use-case | p95 | SLO | Verdict |
|---|---|---|---|
| `loadPipeline` (T261) | 291 ms @ 1k | <500 ms | ✅ |
| `atRiskRecompute` (T263) | 7.76 s @ 5k | <30 s | ✅ strict mode PASS |
| `evaluateTierUpgrade` (T264) | 1.15 s @ 1k | <30 s | ✅ |
| `confirmRenewal` (T265, F8-only) | 482 ms | <600 ms TTFB | ✅ |
| `cron-dispatch` (T262) | 84.95 s @ 1k | <60 s @ 5k | ⚠️ flagged for Phase 11 batched-write opt at large-scale tenants; non-blocking at SweCham scale (~131 members) |

**QA verdict**: ⚠️ PARTIAL PASS — 149/149 IT GREEN across 19 critical files. 1 supplementary test (`cron-bearer-auth-rejected`) has documented test-DB enum-value drift; production Vercel deploy via `pnpm drizzle-kit migrate` resolves transparently. Full report: [`qa/qa-20260510-095340.md`](../specs/011-renewal-reminders/qa/qa-20260510-095340.md)

## Review Notes

**20 cumulative review rounds** closed across 7 staff-review + 5 review + R4 + Staff-R3 + verify-fix waves. Final outstanding: **0 BLOCKER / 0 CRITICAL**.

| Round | Verdict | Findings closed |
|---|---|---|
| Final code-review (review-20260510-090327) | ⚠️ APPROVED WITH CONDITIONS | 0 BLOCKER · 4 WRN · 5 SUG (all post-merge polish) |
| Staff-Final consolidated | ✅ All BLOCKER/HIGH closed | 49+ findings cleared |
| R5 + R6 + Staff-Final (commit cf6ca7f1) | ✅ Closed | 6 BLOCKER + 12 HIGH + 14 LOW |

Full review history: [`specs/011-renewal-reminders/reviews/`](../specs/011-renewal-reminders/reviews) (20 reports)

## Constitution v1.4.0 compliance

| Principle | Status | Evidence |
|---|---|---|
| I — Tenant Isolation (NON-NEG) | ✅ | 50/50 probes; sub-clauses 1–5 satisfied |
| II — Test-First (NON-NEG) | ✅ | TDD discipline maintained; coverage thresholds met |
| III — Clean Architecture (NON-NEG) | ✅ | ESLint `no-restricted-imports` enforced; `src/modules/renewals/` barrel exposed |
| IV — PCI DSS (NON-NEG) | n/a | F8 has no payment surface (hooks F4/F5 via bridge ports) |
| V — i18n | ✅ | 2242 keys × EN+TH+SV |
| VI — Inclusive UX | ✅ | axe-core E2E + reduced-motion variants |
| VII — Perf & Observability | ⚡ partial | T262 5k extrapolation flagged; non-blocking at SweCham scale |
| VIII — Reliability | ✅ | state↔audit atomicity verified by `evaluate-tier-upgrade-outer-tx-rollback.test.ts` (R6-B1 pin) |
| IX — Code Quality (solo-maintainer 5-stack) | ✅ | All 5 substitute checks satisfied |
| X — Simplicity | ✅ | Complexity Tracking entries justified |

## Pre-flag-flip operator checklist (5 gates remaining)

These gates are intentionally deferred — F8 ships dark behind `FEATURE_F8_RENEWALS=false`. Operator clears before flag-flip:

- [ ] **T269** Manual screen-reader QA (VoiceOver Mac OR NVDA Windows)
- [ ] **T270** Cross-browser real-device matrix (Chrome / Edge / Firefox / Safari latest 2 + iOS 16+ Safari + Chrome Android 12+)
- [ ] **T277** Maintainer GPG-signed co-sign on security checklist § 5
- [ ] **T277b** cron-job.org dashboard entry creation for `reconcile-pending-reactivations-coordinator` (daily 07:00 Asia/Bangkok)
- [ ] **T282** `/speckit.qa.run` against staging deployment with `FEATURE_F8_RENEWALS=true` env override

Full checklist: [`retrospective.md` § "Pre-flag-flip operator checklist"](../specs/011-renewal-reminders/retrospective.md)

## Pull Request checklist

- [X] All in-session tasks completed (280/285; 5 explicit operator gates remain)
- [X] Code review passed (⚠️ APPROVED WITH CONDITIONS — 0 BLOCKER)
- [X] QA testing passed (⚠️ PARTIAL PASS — 0 BLOCKER; 1 documented test-env drift)
- [X] Local CI pipeline green (`lint` + `typecheck` + `check:i18n` + `check:layout` + `check:multi-tenant` + `audit`)
- [X] Documentation updated (CLAUDE.md F8 Recent Changes block + docs/phases-plan.md F8 status flip + docs/observability.md F8-A9..A12 alerts + docs/runbooks/cron-jobs.md)
- [X] Changelog entry added (CHANGELOG.md prepend)
- [X] Spec Kit retrospective written (`specs/011-renewal-reminders/retrospective.md`)
- [ ] Vercel preview deploy verified (post-PR-create CI step)
- [ ] 5 operator gates cleared (post-merge, pre-flag-flip)

---

## Notes for reviewers

1. **Ships dark**: `FEATURE_F8_RENEWALS=false` env-var means no production traffic hits F8 surfaces until operator gates clear. Safe to merge without immediate behaviour change.
2. **Branch is 162 commits ahead of `main`**: Phase 1–10 work + 20 review-fix rounds + verify-fix waves. Squash-merge recommended for clean history (PR squash will fold to a single `[Spec Kit] feat(F8): …` commit on main).
3. **Cross-feature contract**: F2 `scheduleNextRenewalPlanChange` + F4 `onPaidCallback` + F5 `processChargeRefunded` are the bridge ports F8 depends on — already merged on `main` per F2/F4/F5 PRs.
4. **Cron coordinators**: 3 of 4 cron-job.org entries already configured per `docs/runbooks/cron-jobs.md`. The 4th (T277b reconcile-pending-reactivations) is the one operator-action item before flag-flip.
5. **Test pollution caveat**: 3 stale-assertion test bugs were discovered + fixed inline during QA (commit `1510f2c9`). All described in QA report § "Failures Detail".

*Generated by `/speckit.ship.run` from spec-driven development artifacts. Constitution v1.4.0 compliant.*
