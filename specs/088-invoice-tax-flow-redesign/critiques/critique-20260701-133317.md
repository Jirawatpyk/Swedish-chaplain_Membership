# Critique Report (Round 2 — Verification): 088 Invoice / Receipt Tax-Flow Redesign

**Date**: 2026-07-01 (round 2, post round-1 remediation)
**Feature**: [spec.md](../spec.md) · **Plan**: [plan.md](../plan.md)
**Prior**: [critique-20260701-124839.md](./critique-20260701-124839.md)
**Verdict**: ⚠️ **PROCEED WITH UPDATES** (coherence fixes only — no fundamental issue)

---

## Executive Summary

Round-1 closure is **complete: 12 / 12 🎯 Must-Address items VERIFIED**, all concrete enough for `/speckit.tasks`. However, the round-1 remediation touched spec/plan/data-model but **did not propagate** to `research.md §3/§10`, the `contracts/`, `SC-005`, and the quickstart orientation — producing **coherence breaks**. Two are material: (F1) the seeded WHT wording (แบบ A) is the exact "entity income-tax-exempt" framing the bundle's own research + tax-auditor **reject** in favour of ม.65 ทวิ (13); (F3/F4) the pay contract — where FR-017/019/021 actually live — was never refreshed, leaving the `tax_receipt_issued` SC-001 signal dangling. (F8) the fail-closed branch gate lacks a paired `legal_entity_type` cutover audit, so genuine registrants would silently lose the §86/4 branch line. None are architecture flaws; all are documentation-coherence fixes to fold in before Tasks.

---

## Closure of Round-1 🎯 (12/12 VERIFIED)

| ID | status | note |
|----|--------|------|
| P1, P3, P5, P8, P9 | ✅ | build-gate, two-doc FR-016, renewal FR-018+AS, in-flight FR-017, rollback runbook — all present |
| E1, E2, E4, E5, E6, E7, E13 | ✅ | juristic snapshot field (F.1), fail-closed NULL, async null-safety+date/FY (F.2), void bill-fallback (F.3), bill-stream assertion + flag + verify script |

Minor residuals (folded into round-2 findings): §C.1 not reconciled with §F.1; P8 count-query + E7 `check:*` not written; `tax_receipt_issued` not in the pay contract.

---

## Round-2 New-Issue Findings

| ID | Sev | Category | Finding | Suggestion |
|----|-----|----------|---------|------------|
| **F1** | 🎯 | WHT wording | Seeded default **แบบ A** (`quickstart.md:71-72,95-96` "…ได้รับการยกเว้นภาษีเงินได้…") is the exact "entity income-tax-exempt" framing that `research.md:182/253/274` **reject** and spec US5 (`spec.md:89`) + `tenant-invoice-settings.md:20` say should be ม.65 ทวิ (13)/แบบ B. Bundle contradicts itself; rendered on BOTH docs. | Keep แบบ A as the seed IF that's the commercial call, but (a) relabel it a **WHT-note fact-confirm** (accountant signs off แบบ A vs แบบ B at Review, before first issuance); (b) reconcile spec/contract to say "seed = แบบ A (customer), legal basis ม.65 ทวิ (13), accountant may align" so the bundle names ONE seed consistently. |
| **F2** | 💡 | Combined-numbering | `research.md:69 §3` still says the `!combinedMode` branch STAYS (settings routes around it), contradicting F.5/plan "**delete** the branch". Contract `tenant-invoice-settings.md:33` still offers selectable `'combined'`. | Pick one: delete the branch; update research §3; drop `'combined'` from the settings bodySchema/form NOW (fail-closed) not "tighten CHECK later"; note the flip's only remaining job is the `RC` prefix. |
| **F3** | 🎯 | Contracts | `pay-and-record-payment.md:15-16` Covers still lists only FR-002/004/005/013 — **FR-017/019/021 absent**: no error code for the legacy-bill reject, `tax_receipt_issued` not in the Audit list (only `receipt_rendered`), no FR-019 re-render surface. | Add FR-017/019/021 to Covers; define the FR-017 reject code (e.g. `legacy_invoice_needs_reissue` → 409); add `tax_receipt_issued` (10y) to Audit events; reference FR-019's re-render path. |
| **F4** | 🎯 | Audit chain | `tax_receipt_issued` named only in `spec.md:19`/FR-021; `research.md:236 §10` still generic ("receipt-issued", "names fixed in data-model/contracts") but data-model never names it and the contract omits it → SC-001 evidence chain broken at every hop. | Name `tax_receipt_issued` in research §10 + a data-model audit-taxonomy table with the 4-place note; list it in the pay contract; confirm it's a new enum value (schedule the 4-place add). |
| **F5** | 💡 | FR-016 | "superseded once paid" (`spec.md:165`) contradicts the Clarification "two distinct legal documents" (`spec.md:17`) + FR-015 "MUST NOT be hidden". "visually primary" not testable. | Replace "superseded" with "payable record — tax receipt issued, see RC-…"; pin a concrete assertion (RC carries a "Tax receipt" badge / listed first). |
| **F6** | 💡 | SC-005 | FR-020 adds email as an SC-005 surface but SC-005 (`spec.md:189`) lists only PDF/admin/portal/i18n. | Add "transactional email (subjects+bodies)" to SC-005; name the owning surface/contract. |
| **F7** | 🤔 | FR-019 | Admin alert channel + re-render endpoint undefined (only `pdf_render_permanently_failed` + reconcile-cron exist). | Specify the re-render endpoint (or reuse the F4 resend surface) + alert channel; assert re-render reuses the SAME allocated RC (SC-002 gap-freeness). |
| **F8** | 🎯 | Cutover / §86/4 | Fail-closed gate (§F.1) reads `legal_entity_type`, but cutover only populates branch columns (`data-model.md:364`, `quickstart §2`), never `legal_entity_type` (nullable, likely empty) → every genuine corporate registrant renders a §86/4 receipt with **no สำนักงานใหญ่/Head Office line** (inverts US3 AS1). Round-1 E2 asked for this data-audit; only the fail-closed half was adopted. | Add a cutover step (data-model §E + quickstart §2) to audit/populate `members.legal_entity_type` for the 131 members before first issuance; e2e assert populated-type → สำนักงานใหญ่ renders, NULL-type → no line. |
| **F9** | 💡 | Async allocation | `research.md:65 §3` says the async worker **allocates** RC; `data-model.md:374 §F.2` says it **reads** `receipt_document_number_raw`. Disagreement affects where FR-021 fires + whether E5's FY threading is needed. | State once: `record-payment` allocates RC in-tx + enqueues; worker READS the number + `paymentDate` for dating only; pin `tax_receipt_issued` to the record-payment allocation moment (sync + async). Fix research §3. |
| **F10** | 💡 | Stale doc | `quickstart.md:13` still says "FR-001…FR-015 … 2 clarifications"; spec now has FR-001…FR-021 + 4 clarifications. | Update to "FR-001…FR-021 … 4 clarifications". |

---

## Findings Summary

| Metric | Count |
|--------|-------|
| Round-1 🎯 closed | 12 / 12 ✅ |
| Round-2 🎯 Must-Address | 4 (F1, F3, F4, F8) |
| Round-2 💡 Recommendations | 5 (F2, F5, F6, F9, F10) |
| Round-2 🤔 Questions | 1 (F7) |

---

## Recommended Actions (before `/speckit.tasks`)

### 🎯 Must-Address
1. **F1** — reconcile the WHT seed: keep แบบ A (customer) as the seed but label it an accountant-sign-off item; make spec/contract name แบบ A consistently as the seed with ม.65 ทวิ (13) as the legal basis. *(WHT-note-membership-only fact-confirm #3 = resolved by best practice: render ONLY on `invoice_subject='membership'`.)*
2. **F3 + F4** — refresh `pay-and-record-payment.md`: add FR-017/019/021 to Covers, define the FR-017 reject code, add the `tax_receipt_issued` (10y, new enum, 4-place) audit event; name it in research §10 + a data-model audit table.
3. **F8** — add a `legal_entity_type` cutover data-audit/populate step (data-model §E + quickstart §2) + the branch-render e2e assertions.

### 💡 Recommendations
F2 (delete combined branch + drop the selectable `'combined'` + fix research §3), F5 (FR-016 wording + testable assertion), F6 (email in SC-005), F9 (async allocates-vs-reads authoritative statement), F10 (quickstart orientation).

### 🤔 Question
F7 (FR-019 alert channel + re-render surface — confirm reuse of the F4 resend path).

---

*Round-2 verification — read-only for spec/plan; remediation applied only on approval.*
