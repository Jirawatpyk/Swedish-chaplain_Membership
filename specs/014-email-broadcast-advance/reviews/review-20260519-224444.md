# F7.1a Staff-Engineer Review — 2026-05-19 22:44

**Branch**: `014-email-broadcast-advance` at `ebbca627`
**Base**: `main`
**Reviewer**: Staff-engineer-level (Claude Opus 4.7 — solo-maintainer substitute per Constitution IX)
**Spec**: `specs/014-email-broadcast-advance/spec.md`
**Constitution**: v1.4.2

---

## Executive Summary

### Verdict: ⚠️ **APPROVED WITH CONDITIONS — for US1-ONLY ship**

The branch is **US1-complete + foundational + hardened** but does **NOT implement the full F7.1a spec**. US2 (image embedding + ClamAV) and US7 (multi-template library) are spec'd, planned, tasked, and infra'd but **0 % implemented** at code level.

The branch is **safe to merge** as `F7.1a Phase 1 = US1-only release` provided:
1. The release artefact is renamed F7.1a-US1 (not F7.1a) to honour the spec contract
2. Phase 4-6 tasks (US2 + US7 + polish) move to F7.1a-Phase-2 follow-up branch
3. F7.1a-US1 ships behind the existing `FEATURE_F71A_US1` kill-switch (ships dark)
4. Pre-flag-flip operator gates (manual SR QA, cross-browser, staging walkthrough) execute

### Findings tally (against the F71A diff)

| Severity | Count | Status |
|---|---|---|
| 🔴 Blocker | 0 | — |
| 🟡 Warning | 4 | listed below |
| 🟢 Suggestion | 6 | listed below |

### Phase-by-phase task closure

| Phase | Scope | Done | Total | % |
|---|---|---|---|---|
| Phase 1 | Shared infrastructure setup | 10 | 10 | **100 %** |
| Phase 2 | Foundational prerequisites | 21 | 21 | **100 %** |
| Phase 3 | **US1 Pagination** (P1 — MVP-blocker) | 30 | 30 | **100 %** |
| Phase 4 | **US2 Image embedding** (P1) | 0 | 24 | **0 %** |
| Phase 5 | **US7 Multi-template** (P2) | 0 | 36 | **0 %** |
| Phase 6 | Polish + Cross-Cutting | 1 | 43 | 2 % |
| **Total** | — | **62** | **164** | 38 % |

---

## Scope Reality Check (CRITICAL CONTEXT)

This branch implements **US1 only**. The spec calls for US1 + US2 + US7. Three options for shipping:

**Option A — Recommended**: Rename the branch ship as `F7.1a-Phase-1 (US1-only)` and split US2 + US7 into a follow-up branch `014a-email-broadcast-us2-us7` (or `015-email-broadcast-us2`). This matches the actual delivered scope + preserves the Phase 3 spec contract.

**Option B**: Hold the branch unmerged until Phase 4 + 5 implement, then ship the full F7.1a in one go. Estimated +4-6 weeks; conflicts with `FEATURE_F71A_US1` kill-switch's intended dark-launch model.

**Option C — NOT recommended**: Merge as-is + mark Phase 4-5 "deferred to F7.1b". Risks downstream stakeholder confusion since the spec explicitly says US7 was *promoted from F7.1b back into F7.1a* (line 17 of spec.md).

---

## Findings

### 🟡 Warning

#### W-1 — Scope contract divergence (PROCESS, not code)

**Spec.md § Context lines 14-19** declares F7.1a scope = US1 + US2 + US7. The branch delivers only US1 (Phase 3) with Phase 4-5 at 0/60 tasks. No documented scope-cut decision in the branch's plan.md Complexity Tracking, retrospective, or release artefact.

**Recommendation**: Pick Option A or B above. If Option A, amend spec.md § Context with a "Phase 1 US1-only release" note + create the follow-up branch tracking ticket. Update `findings-index.md` with the deferral attribution.

**Files**: `specs/014-email-broadcast-advance/spec.md`, `specs/014-email-broadcast-advance/tasks.md`

#### W-2 — Polish phase 1 / 43 tasks closed (Phase 6 = 2 %)

Phase 6 (Polish + Cross-Cutting) includes critical pre-ship items not yet done: manual SR QA (NVDA / VoiceOver), cross-browser real-device matrix, staging walkthrough, retrospective, performance bench. Only 1 task closed in 43.

Some of these are properly *operator gates* (cannot be code-completed) but several are missing test/doc surface: 50k end-to-end test exists (`pagination-50k-end-to-end.test.ts` confirmed) but no Phase 6 task lists it as closed; retrospective.md exists but no Phase 6 task ticks it; etc.

**Recommendation**: Sweep `specs/014-email-broadcast-advance/tasks.md` Phase 6 — mark the tasks that have actual artefact in the codebase as `[X]` so the project status reflects reality. Pre-flag-flip operator gates remain `[ ]` until ship day.

**Files**: `specs/014-email-broadcast-advance/tasks.md`

#### W-3 — F2/F8 concurrent agent commits interleaved with F7.1a commits (POSSIBLE merge friction)

82 commits on branch vs `main`, of which 63 are F71A-tagged. 19 commits are F2 (plans) or F8 (renewals) work from a concurrent agent worktree. These commits modify shared files (e.g. `src/lib/metrics.ts`, `src/modules/plans/*`, `tests/unit/plans/*`).

The F71A scope is genuinely uncontaminated (per multi-round grep verification across rounds) but the merge artefact will be a **mixed PR** that's harder to review at ship time. PR-bot post-merge release notes will need careful curation.

**Recommendation**: At merge time, write a release-note delta tied to the F71A commits only (`git log main..HEAD --grep="F7.1a\|F71A"`). The non-F71A commits are F2/F8 work that should be release-noted against their own features.

**Files**: 19 non-F71A commits in `main..HEAD`

#### W-4 — Notation drift cleanup is documentation-debt-positive but inline-comment-debt-negative

Phase 3F.11.3 introduced `specs/014-email-broadcast-advance/findings-index.md` to consolidate finding-ID notation across 6 review-agent taxonomies (F-NN, F-N, Finding N, Type Bottom #N, Comment #N, UX F-N). Phase 3F.11.8 had to fix 2 sites where inline comments used a *hybrid* notation (`F-Finding-4`) that the index didn't recognise.

Inline `Phase 3F.X (Y — fix)` markers across the codebase now number ~80 tags. Long-term maintenance: these tags rot when Phase 3F is no longer top-of-mind. Round 3 review found 1 HIGH (stale line-number ref) + 3 LOW comment issues from this category.

**Recommendation**: Post-merge, sweep inline `Phase 3F.x` tags after F7.1a-Phase-2 (US2+US7) ships. The tags are valuable during active remediation rounds; less valuable 6 months later. Consider a `git grep "Phase 3F"` housekeeping pass at F7.1a final close.

**Files**: ~30 source files across `src/modules/broadcasts/`

### 🟢 Suggestion

#### S-1 — TxToken Step 5 (BroadcastsRepo.withTx F7 MVP widen)

Phase 3F.11.13-14 completed TxToken Steps 1-4 for F71A. The sole remaining `asTxToken(tx)` laundering line in F71A is `cancel-broadcast.ts:256` where F7 MVP `BroadcastsRepo.withTx` callback still receives `unknown`. Widening F7 MVP would cascade to F4/F5/F7 MVP consumers — out of F7.1a scope but a clean follow-up.

**Recommendation**: Open F7.1a.1 backlog ticket `TX-TOKEN-STEP-5` to widen `BroadcastsRepo.withTx` after F7.1a-Phase-2 ships. Defensible deferral — F7 MVP is shipped and stable; the brand laundering line is documented.

#### S-2 — IdempotencyKey brand validation at constructor (vs runtime)

`asIdempotencyKey(raw: string): IdempotencyKey` is the boundary cast for Drizzle adapter rehydration. No runtime validation. A future adapter regression that returns a malformed key would silently propagate to the Resend gateway.

**Recommendation**: Consider adding an optional runtime guard (e.g., `/^broadcast-[0-9a-f-]+-batch-\d+-attempt-\d+(-(autoretry|manualretry)-\d+)?$/.test(raw)`) in `asIdempotencyKey` behind a `DEBUG_IDEMPOTENCY_KEY_FORMAT` env flag. Production-ready when implemented; defensible to skip for SweCham scale.

**Files**: `src/modules/broadcasts/domain/value-objects/idempotency-key.ts`

#### S-3 — Reduce `Phase 3F.X` inline-tag density

`grep -rn "Phase 3F.11" src/ | wc -l` shows ~50+ inline tags across the broadcasts module. Several files have 5-10 such tags. While each provides traceability, the tag-to-code ratio at some sites is > 1:5.

**Recommendation**: Optional post-ship sweep — collapse adjacent tags (e.g., "Phase 3F.11.1 (C2) + Phase 3F.11.6 + Phase 3F.11.11 + Phase 3F.11.15 IdempotencyKey brand" into one block). Defer to F7.1a-final close.

#### S-4 — `audit-emit-failure-logger.ts` helper coverage

Phase 3F.11.9 extracted `logAuditEmitFailure` helper applied at 4 sites. No dedicated test exists for the helper itself (it's covered transitively via the 4 use-case contract tests).

**Recommendation**: Optional — add a 5-line unit test in `tests/unit/broadcasts/audit-emit-failure-logger.test.ts` verifying the helper forwards args to pino correctly. Marginal value (helper is 3 lines) but locks the contract.

**Files**: `src/modules/broadcasts/application/audit-emit-failure-logger.ts`

#### S-5 — Migration 0173 lacks rollback procedure documentation

`drizzle/migrations/0173_f71a_webhook_batch_missing_event.sql` adds a single enum value. Postgres `ALTER TYPE ... DROP VALUE` is not supported — this migration is **forward-only**. No rollback documentation in the migration comment or in `docs/runbooks/`.

**Recommendation**: Add a comment to 0173 stating rollback path is "delete-and-recreate the audit_event_type enum" (heavy, requires data backfill) or "accept the dead enum value if F71A is reverted". Same applies to migrations 0167, 0169, 0170 (all add audit enum values). Optional doc-only fix.

**Files**: `drizzle/migrations/0173_f71a_webhook_batch_missing_event.sql`

#### S-6 — F-15 sweep has no integration test

Phase 3F.11.16 added the `cron.broadcasts.reconcile.batch_manifest_provider_id_orphan` observability sweep. Contract test for the reconcile cron is GREEN (8/8) but doesn't exercise the new sweep query. The sweep is observability-only (no state mutation) so the test gap is low-risk but worth noting.

**Recommendation**: Optional — add a live-Neon integration test seeding 1 batch manifest in (`status='sending' AND provider_broadcast_id IS NULL AND updated_at < now() - interval '15 minutes'`) state, run the cron, assert the warn log fires. Defer to F7.1a-final if SweCham scale doesn't surface the gap in prod.

**Files**: `src/app/api/cron/broadcasts/reconcile-stuck-sending/route.ts:248-303`

---

## Spec Coverage Matrix

### US1 — Recipient-list pagination (FR-001 to FR-008d)

| FR | Description | Status | Evidence |
|---|---|---|---|
| FR-001 | 50k recipient ceiling at submit | ✅ Implemented | `check_estimated_recipient_cap` migration 0171; submit-broadcast validates |
| FR-002 | Concurrency cap 1-8 default 4 | ✅ Implemented | `tenant_broadcast_settings.dispatch_concurrency_cap` + `batch-dispatcher.ts` |
| FR-003 | Single broadcast row from member view | ✅ Implemented | Per-batch tables disjoint; broadcast row unchanged |
| FR-004 | Cancel halts within ≤60s | ✅ Implemented | `cancel-broadcast.ts` + `markCancelled` atomic tx |
| FR-005 | Per-batch failure retry | ✅ Implemented | `auto-retry-failed-batches.ts` + sweep cron |
| FR-006 | Per-batch breakdown UI | ✅ Implemented | `batch-breakdown.tsx` component |
| FR-007 | 50k ceiling at BOTH submit + dispatch | ✅ Implemented | Validation at both boundaries |
| FR-008 | `broadcast_dispatched_in_batches` audit | ✅ Implemented | Audit event emitted; payload type defined |
| FR-008a | partially_sent state + 3-retry budget | ✅ Implemented | `retry-failed-batches.ts` + manual_retry_count column |
| FR-008b | Transient `sending` retry state | ✅ Implemented (spec-amendment-aware — reuses `sending` not separate `retrying`) |
| FR-008c | Accept-partial action | ✅ Implemented | `accept-partial-delivery.ts` + admin route |
| FR-008d | Concurrent retry advisory lock | ✅ Implemented + integration tested | SC-007 concurrent test (Phase 3F.11.11) |

**US1 verdict**: ✅ **12 / 12 FRs covered with code + tests + integration verification**

### US2 — Image embedding (FR-009 to FR-015)

| FR | Description | Status |
|---|---|---|
| FR-009 | `<img>` reopened under allowlist | ❌ NOT IMPLEMENTED |
| FR-010 | Tenant-configurable allowlist | ❌ NOT IMPLEMENTED (migration 0164 exists but no use case) |
| FR-011 | Submit-boundary reject | ❌ NOT IMPLEMENTED |
| FR-012 | Compose UI image upload | ❌ NOT IMPLEMENTED |
| FR-013 | ClamAV virus scan | ❌ NOT IMPLEMENTED |
| FR-014 | URI scheme + inline-handler reject | ❌ NOT IMPLEMENTED |
| FR-015 | Allowlist mutation audit | ❌ NOT IMPLEMENTED |

**US2 verdict**: ⏸️ **0 / 7 FRs implemented — Phase 4 not started**

### US7 — Multi-template library (FR-016 to FR-023)

| FR | Description | Status |
|---|---|---|
| FR-016 | Admin CRUD templates | ❌ NOT IMPLEMENTED |
| FR-017 | Template constraints (name/subject/body) | ❌ NOT IMPLEMENTED |
| FR-018 | Compose template picker | ❌ NOT IMPLEMENTED |
| FR-019 | Snapshot semantics + stale banner | ❌ NOT IMPLEMENTED |
| FR-020 | 5 starter templates × 3 locales seeded | ❌ NOT IMPLEMENTED |
| FR-021 | Template audit events + starter badge | ❌ NOT IMPLEMENTED |
| FR-022 | started_from_template_id audit field | ❌ NOT IMPLEMENTED |
| FR-023 | Template delete unblocks drafts | ❌ NOT IMPLEMENTED |

**US7 verdict**: ⏸️ **0 / 8 FRs implemented — Phase 5 not started**

### Success Criteria

| SC | Description | Status |
|---|---|---|
| SC-001 | 10k recipients dispatched in ≤10 min | ✅ Integration tested (`pagination-7500-end-to-end.test.ts`) |
| SC-002 | 50k recipients dispatched in ≤45 min | ✅ Integration tested (`pagination-50k-end-to-end.test.ts`) |
| SC-003 | 100% img src allowlist match | ⏸️ N/A (US2 not built) |
| SC-004 | Zero `<img src>` allowlist leaks | ⏸️ N/A (US2 not built) |
| SC-005 | ClamAV scan ≤500ms p95 | ⏸️ N/A (US2 not built) |
| SC-006 | 95% retry success in 3-budget | ⏸️ Cannot verify pre-prod |
| SC-007 | Concurrent retry race → exactly one win | ✅ Live-Neon integration test (Phase 3F.11.11) |
| SC-007a | Template populate ≤500ms | ⏸️ N/A (US7 not built) |
| SC-007b | 5 starter × 3 locale seed | ⏸️ N/A (US7 not built) |
| SC-007c | ≥30% adoption metric | ⏸️ Post-ship measurement |
| SC-008 | WCAG 2.1 AA on new surfaces | ⚠️ PARTIAL — US1 surfaces fixed in Phase 3F.11.2/3F.11.8/3F.11.14; manual SR QA pending |
| SC-009 | Full EN+TH+SV i18n parity | ✅ 2989 keys × 3 locales GREEN |
| SC-010 | Zero F7 MVP regression | ✅ 232/232 contract tests GREEN |

**Success-criteria verdict**: 5 verifiable, 1 partial, 7 pending (US2/US7 not built or post-ship measurement).

---

## Constitution v1.4.2 Compliance

### Principle I — Data Privacy & Security (NON-NEGOTIABLE)

✅ **PASS**

- Two-layer tenant isolation (app + DB) verified
- `runInTenant` + RLS + FORCE policies on `broadcast_batch_manifests` (migration 0166 + 0172)
- Cross-tenant probe audit events (`broadcast_cross_tenant_probe` + `broadcast_webhook_batch_missing`) with forensic-trail logger.error fallback (Phase 3F.11.9)
- `audit-emit-failure-logger.ts` ensures sub-clause 4 forensic record on audit-port failure
- TxToken brand chain enforces compile-time tenant-tx hygiene (no `as never` regressions)

### Principle II — Test-First Development (NON-NEGOTIABLE)

⚠️ **PASS via Complexity Tracking #4 (documented deviation)**

- Phase 3 Cluster B/C implementations (T045, T046, T056, T057) shipped before contract tests
- Contract tests retrofitted in Phase 3F.5 + 3F.10 + 3F.11.5 (~50 new test cases)
- CT #4 entry in `plan.md` explicitly states "NOT a new project norm — F7.1a-specific deviation"
- Mitigating evidence: T037 lean-end-to-end on live Neon was present from Phase 3 ship
- Contract tests caught real fix-pass regressions (F-04 + C4) during Phase 3F.11.x

### Principle III — Clean Architecture (NON-NEGOTIABLE)

✅ **PASS**

- Domain layer has zero `next` / `drizzle` / framework imports
- Application ports define interfaces; Drizzle adapters implement
- New `audit-emit-failure-logger.ts` correctly placed in Application layer (composes pino but doesn't compose ports)
- `BroadcastConcurrentMutationError` exposed via barrel for typed `instanceof` narrowing in route handler (Phase 3F.11.9)
- TxToken brand prevents Drizzle types from leaking into Application (single `unbrandTx` boundary in `@/lib/db`)
- IdempotencyKey brand keeps key construction in Domain

### Principle IV — Payment Security PCI DSS (NON-NEGOTIABLE)

N/A — F7.1a has no payment surface.

### Principle V — Internationalization

✅ **PASS**

- `pnpm check:i18n` GREEN at 2989 keys × 3 locales
- F-14 hardcoded EN strings closed in Phase 3F.11.1 C3
- TH + SV translations natural per Round 3 enterprise-ux-designer review
- Audit event types localised via `audit.event.*` keys

### Principle VI — Inclusive UX (Mobile + WCAG 2.1 AA)

⚠️ **PASS partial** (US1 surfaces only; US2 + US7 not built)

- `finalFocus` on `<AlertDialogContent>` Popup (Phase 3F.11.1 C1) — WCAG SC 2.4.3 / 2.4.11
- aria-live moved out of `<summary>` (Phase 3F.11.2 H2) — ARIA 1.2 compliance
- Ring contrast `ring-white` on destructive bg (Phase 3F.11.8 MEDIUM-2) — WCAG SC 1.4.11
- Fallback heading ref for focus return when trigger unmounted (Phase 3F.11.14 UX M1)
- aria-hidden on duplicate visible summary text (Phase 3F.11.8 LOW)
- Manual SR QA + cross-browser real-device matrix: **PENDING** (Phase 6 operator gate)

### Principle VII — Performance & Observability

✅ **PASS**

- `summary.split`, `summary.orphanRecovered`, `summary.raceLost`, `orphaned_provider_id_count` counters on cron (Phase 3F.11.9 + 3F.11.16)
- 4 cron coordinators wired (per `docs/runbooks/cron-jobs.md`)
- pino structured logs at all observability-critical paths
- Metric SLO budgets defined in observability docs

### Principle VIII — Reliability (Error + Data Integrity + Audit)

✅ **PASS**

- F-15 observability sweep fills the missing-providerBroadcastId gap (Phase 3F.11.16)
- BroadcastConcurrentMutationError narrowing distinguishes benign race from real failure
- 17 audit event types added; retention 5y default; all wrapped in try/catch + logger.error fallback
- 232/232 contract tests GREEN; 6/6 advisory-lock integration tests GREEN including SC-007 concurrent
- TxToken + IdempotencyKey brands prevent type-laundering classes of bug

### Principle IX — Code Quality Standards

✅ **PASS via solo-maintainer substitute**

- 4 `/speckit-review` rounds executed (Round 1 → 3F closure → Round 2 → 3F.11 closure → Round 3 → 3F.11.x closure → this staff review)
- `pnpm typecheck` PASS F71A scope
- `pnpm check:i18n` GREEN
- ESLint clean for F71A files
- TDD CT #4 documents deviation per § Governance

### Principle X — Simplicity (YAGNI)

✅ **PASS**

- Phase 3F.11.15 IdempotencyKey brand is the only TS-type-only addition with zero runtime cost
- F-15 sweep is observability-only (no auto-backfill — that's correctly deferred to F7.1a.1 backlog requiring a Resend dashboard feature)
- No speculative engineering; every refactor traces to a specific review finding
- 4 CT entries documented (CT #1-3 from plan; CT #4 added Phase 3F.11.7)

---

## Test Coverage Assessment

### Unit + Contract Tests

`pnpm vitest run tests/contract/broadcasts/` → **232 passed (22 files)** as of Phase 3F.11.13
- Phase 3F.11.5 added 4 NEW test files + 2 extended files (~30 new cases)
- Phase 3F.11.10-15 added ~10 more cases (audit-throw logger.error assertions, event-type cross-product, idempotency-key rotation, dispatch error branches)
- All Round 1 + Round 2 + Round 3 test gap findings closed
- 1 remaining test backfill (S-6 above): F-15 sweep integration test (LOW)

### Integration Tests (live Neon Singapore)

- `pagination-7500-end-to-end.test.ts` ✅ — SC-001 verification
- `pagination-50k-end-to-end.test.ts` ✅ — SC-002 verification
- `pg-advisory-lock-adapter.test.ts` ✅ — 6 cases including SC-007 concurrent
- `batch-manifests-fk-cascade.test.ts` ✅ — FK CASCADE verification
- `concurrent-retry-race.test.ts` ✅ — SC-007 advisory-lock at application layer
- `webhook-idempotency.test.ts` ✅ — Resend webhook idempotency

### E2E Tests

- `pagination-batch-breakdown.spec.ts` ✅ — admin UI batch breakdown surface

### Coverage Gaps (per Round 3 + this review)

- F-15 sweep integration test (S-6 — LOW)
- Manual SR QA (Phase 6 operator gate)
- Cross-browser real-device matrix (Phase 6 operator gate)
- 60-day post-ship SC-007c adoption metric (impossible pre-ship)

---

## Metrics

- **Files reviewed**: 93 (F71A-scoped)
- **Lines changed**: +12,285 / -51
- **Commits reviewed**: 63 (F71A-tagged) + 19 (F2/F8 concurrent agent, NOT in F71A scope)
- **Spec FRs total**: 27 (12 US1 + 7 US2 + 8 US7)
- **Spec FRs implemented**: 12 (US1: 100%; US2: 0%; US7: 0%)
- **Success criteria verifiable now**: 5 / 13
- **Constitution principles passing**: 10 / 10 (Principle II + VI partial via documented Complexity Tracking)
- **Migrations**: 13 new (0161-0173)
- **Audit event types added**: 17 (F7AuditEventType 53 → 54 in 3F.11.3 M3 + 16 in Phase 2 baseline)
- **i18n keys**: +130 ish (2989 total)

---

## Recommended Actions

### Before merging (immediate)

1. **DECISION REQUIRED** (W-1): Pick Option A (split US2+US7 to follow-up branch) or Option B (hold for full F7.1a). Update spec.md § Context + create release artefact named accordingly.
2. **W-2 sweep**: Mark already-shipped Phase 6 tasks as `[X]` in tasks.md so status reflects reality.
3. **W-3 release notes**: Curate F71A-only changelog at merge; don't conflate with F2/F8 concurrent agent commits.

### Before flag-flip (operator gates per Phase 6)

4. Manual SR QA on NVDA + Chrome (or VoiceOver + Safari) — 4 admin surfaces (retry dialog, accept-partial dialog, batch breakdown, fail-open inline error panel)
5. Cross-browser real-device matrix: iOS Safari + Android Chrome + Desktop Firefox + Desktop Safari (minimum)
6. Staging walkthrough on `swecham-staging.zyncdata.app` with a real 7,500-recipient broadcast (use synthetic recipient seed)
7. Production cron-job.org coordinator setup (4 cron endpoints: dispatch-batches, split-large-broadcasts, reconcile-stuck-sending, broadcasts-gauges)
8. Pre-flag-flip co-sign on security checklist (Constitution IX solo-maintainer substitute)

### Post-ship (F7.1a.1 backlog tickets to open)

9. S-1: TxToken Step 5 (F7 MVP `BroadcastsRepo.withTx` widen)
10. S-2: IdempotencyKey runtime validation behind debug env flag
11. S-3: Phase 3F.x inline-tag density sweep
12. S-4: `audit-emit-failure-logger.ts` dedicated unit test
13. S-5: Migration rollback doc for audit-event-type ADD VALUE migrations
14. S-6: F-15 sweep integration test (live-Neon)
15. **F7.1a Phase 2 spec amendment** if Option A above: US2 + US7 in separate feature branch with their own /speckit cycle

---

## Verdict & Next Step

⚠️ **APPROVED WITH CONDITIONS — for F7.1a-US1-only release**

The implemented scope (US1 + foundation + remediation through Round 3) is **technically excellent and ship-ready**:
- Zero blockers
- 232/232 contract tests GREEN
- 6/6 advisory-lock integration tests GREEN including SC-007
- 50k end-to-end integration test GREEN on live Neon
- Constitution v1.4.2 compliance with documented exception for TDD-retrofit (CT #4)
- TxToken brand chain Steps 1-4 complete
- IdempotencyKey brand + rotation factories
- F-15 observability sweep
- 4 review rounds + 17 Phase 3F.11.x remediation commits

**Conditions to merge**:
1. Reframe ship artefact as "F7.1a Phase 1 — US1 (Pagination) only" — NOT "F7.1a complete"
2. Open follow-up branch / ticket for Phase 4 (US2) + Phase 5 (US7)
3. Sweep Phase 6 tasks.md to reflect actual closure state
4. Execute pre-flag-flip operator gates (manual SR QA, cross-browser, staging walkthrough)

**Recommended next command**: `/speckit.ship` with the US1-only release scope, OR continue Phase 4 (US2) implementation in a follow-up worktree before shipping.

If pursuing US1-only ship: branch is **safe to merge** behind `FEATURE_F71A_US1=false` (ships dark). Flag-flip happens post-operator-gates.

---

*Reviewed by Claude Opus 4.7 (1M context) per Constitution IX solo-maintainer substitute. Review file: `specs/014-email-broadcast-advance/reviews/review-20260519-224444.md`.*
