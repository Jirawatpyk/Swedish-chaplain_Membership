---

description: "Task list for 119 — E-Blast Two-Sided Approval Workflow, Writing Tool Upgrade & Marketing Dashboard"
---

# Tasks: E-Blast Two-Sided Approval Workflow, Writing Tool Upgrade & Marketing Dashboard

**Input**: Design documents from `/specs/119-eblast-approval-workflow/`
**Prerequisites**: plan.md (required), spec.md (required for user stories), research.md, data-model.md, contracts/, quickstart.md, exploration-2026-09-18.md

**Tests**: MANDATORY (Constitution Principle II, NON-NEGOTIABLE). Every user story carries at least one acceptance test that is written first and observed RED before implementation; every story touching a `tenant_id`-scoped table also carries a cross-tenant probe integration test (Review-Gate blocker). One task per behaviour the spec states, each naming the test that must go RED first — never one task per test file. Generated from `.specify/templates/tasks-template.md` and this feature's spec/plan, never by copying an earlier feature's tasks.md.

**Organization**: grouped by user story in the spec's priority order (US1, US2, US3 = P1 → US4, US5, US6 = P2 → US7 = P3). Each phase heading names the **PR** it ships in, per `plan.md` § Delivery slicing.

**Ship order is NOT file order.** Task IDs run sequentially down this file so they can be cited unambiguously — **and are never renumbered once written**: a task added after the fact takes a lettered suffix in place (`T141a`, `T086a`, `T129a`), so a citation in a commit, a review or a runbook never silently changes meaning. The delivery order is:

1. **PR-1** (unflagged, live on merge) = Phase 2 → Phase 5 (US3) → Phase 8 (US6)
2. **PR-2** (ships dark behind `FEATURE_EBLAST_MEMBER_APPROVAL`) = **T120 + T121 + T122 sequenced first** (see § Phase dependencies) → Phase 3 (US1, with T006a beside T063, T129a before T065's arms, and T149 + T150 now in its Tests-first block) → Phase 4 (US2) → Phase 7 (US5) → Phase 6 (US4) → Phase 9 (US7), including T149, T150, T152 and T152a, which MUST land in the same PR as the entry route they gate (`/speckit.analyze` C1: the flag gate is the whole of the dark ship — T062 creating `POST …/[id]/version` without T152 gating it means the approval round is live in prod for every `broadcasts.write` holder the moment PR-2 merges, and T063 hiding the button is not a gate)
2a. **Four PR-2 tasks are sequenced out of file order, deliberately** (round 4 C1, H1, H4, M1): **T121 + T122** register the gauges, the five counters and `broadcasts_member_decide_ms` that T058/T059/T066 (Ph3), T078 (Ph4) and T130 (Ph7) emit — and an emit against an unregistered field does not typecheck (contract § 4.2), so they run at the **head** of PR-2, not at the end of Phase 6; **T120** writes the five stage labels the same early screens render; **T129a** builds the five email templates T065's and T131's dispatcher arms call; and **T149 + T150** are T152's RED, so they moved into Phase 3's Tests-first block beside T062's other contract tests rather than sitting in Phase 9 behind the gate they prove (Constitution II).
3. **There is no PR-3.** The maintainer merged the former PR-3 (dashboard + trial) into PR-2 on 2026-09-18, so the approval round, its notifications, the dashboard that reports on its stages, the flag matrix and the UAT walkthrough all ship together behind one flag. That closes five round-3 findings by construction: the metric registrations, the five new stage labels, the FR-051 pass on the queue, the flag-flip sequencing and the RED-before-implementation ordering of T151/T116 are now all inside one PR (round 3 C1, C2, H4, M2, M7, M9).
4. **Phase 10 (Polish)** items are tagged with the PR they must land in; nothing in it is deferred past its PR — including the coverage pins and the port fakes, which are split per PR (T157/T157a, T158/T158a, T159/T159a) because a pin on a file the PR does not create fails the required coverage check.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: can run in parallel — different files, and nothing incomplete is depended on
- **[Story]**: `US1`–`US7`. Phase 1 (Setup) and Phase 2 (Foundational — shared groundwork more than one story depends on) carry no story label; Phase 10 tasks carry their PR instead
- Every task names exact file paths and, where it changes behaviour, the **RED test** that must fail first

## Path Conventions

- Bounded context: `src/modules/broadcasts/{domain,application,infrastructure}/` (public barrel `src/modules/broadcasts/index.ts`; cross-context imports go through it). Cross-module seams compose in `src/lib/*-deps.ts`.
- Presentation: `src/app/(staff)/admin/broadcasts/**`, `src/app/(staff)/admin/settings/broadcasts/**`, `src/app/(member)/portal/broadcasts/**`, `src/app/api/**/route.ts`, `src/components/broadcast/**`
- Tests: `tests/unit/broadcasts/**` (module), `tests/unit/broadcast/**` (components — **two folders, one letter apart**; check which one you are in), `tests/contract/broadcasts/`, `tests/integration/broadcasts/` (live Neon `dev` branch), `tests/e2e/broadcasts/` (Playwright + axe, **always `--workers=1`**)
- Migrations: hand-written SQL in `drizzle/migrations/` + a `meta/_journal.json` entry (CLAUDE.md § Gotchas — `db:generate` is abandoned at 0018)

---

## Phase 1: Setup

**Purpose**: the three things that must be true before any code is written.

- [x] T001 Confirm the working branch is `119-eblast-approval-workflow` and re-read `drizzle/migrations/meta/_journal.json` — the tail today is `idx: 304` / `when: 1798543600000` / `0303_member_change_requests_reason_partial_ck`, so `0304` is `idx: 305` / `when: 1798543700000` and `0305` is `idx: 306` / `when: 1798543800000`. If another branch has landed a migration since, renumber **both** before writing either file (research V5; a duplicate `when` makes `db:migrate` a silent no-op that still prints "✓ applied").
- [x] T002 [P] Re-pin `@tiptap/extension-image` from `^3.22.5` to exact `3.22.5` in `package.json:85` and refresh `pnpm-lock.yaml` — a caret on an editor extension lets a patch release change the serialised HTML that both the shared sanitiser policy and the design-block parser key on (quickstart § 3.1). This is a **pin, not a new dependency** (Constitution X, zero new npm deps).
- [x] T003 Add `FEATURE_EBLAST_MEMBER_APPROVAL` as a zod boolean defaulting to `false` in `src/lib/env.ts`, list it in `.env.example` (so `pnpm check:env-example` passes), and add `isEblastMemberApprovalEnabled() = env.features.f7Broadcasts && env.features.eblastMemberApproval` to `src/modules/broadcasts/infrastructure/feature-flags.ts` (research R18 — the 2-layer form of the existing `isF71aUs*Enabled` pattern). **Do not set the var in Vercel**: setting an env var IS a production deploy on this repo.
- [x] T004 [P] Close **V1** before any brand task: confirm `BlobStoragePort` can expose `resolvePublicUrl(key)` over `@vercel/blob`'s `head()` and be exported from `src/modules/invoicing/index.ts` **without** dragging `sharp`/`server-only` into a client bundle (the barrel-cycle lesson at `src/modules/invoicing/index.ts:531-538`). If it cannot, fall back to research R12 alternative (3) and record the operator re-upload step in `quickstart.md` § 3.3.
- [x] T005 [P] Close **V3** only — **V4 is already decided**: (V3) confirm the outbox dispatcher can open a tenant tx for the read-at-send of `broadcast_versions` / `broadcast_member_decisions` the way the two F114 arms do (`src/app/api/cron/outbox-dispatch/route.ts:415-542`). **V4 RESOLVED (maintainer, 2026-09-18)**: the synchronous test copy (T105) goes through the shared transactional sender `emailSender` exported from `src/modules/auth/infrastructure/email/resend-client.ts:148` — a module-level singleton the dispatcher already imports at `src/app/api/cron/outbox-dispatch/route.ts:54`, so it is reusable outside it — wrapped by a broadcasts Infrastructure adapter behind `TestCopyMailerPort`, rate-limited 10/h. **The sixth-`notification_type` fallback is withdrawn and MUST NOT be built**: `notification_type` stays at +5 in `0305`, and the test copy adds no enum value to `0304` — which matters, because `0304` (PR-1, where T105 ships) carries no `notification_type` change at all and the fallback would have had no migration to live in (research V4/R23, data-model § 7.3).
- [ ] T006 **[PR-1]** Create the **PR-1** i18n namespace roots in **all three** locales at once so `pnpm check:i18n` never sees a one-locale window: `admin.settings.broadcasts.brand.*`, `admin.settings.index.categories.eblastBrand.*`, `broadcast.editor.*` (toolbar, alt-text dialog, link dialog, paste notice, preview dialog) in `src/i18n/messages/{en,th,sv}.json`. EN is canonical — **no italic on Thai copy** and no Latin-only placeholder left in `th.json`. **The two approval namespaces are deliberately NOT created here** (`/speckit.analyze` round 3 H2): they are used by no PR-1 screen, and T147 — in this same PR — deletes unreferenced E-Blast keys and adds a test forbidding them, so creating them early would make PR-1 unmergeable against its own gate. They land in T006a.
- [ ] T006a **[PR-2]** Create the **PR-2** i18n namespace roots in all three locales at once, **sequenced with T063** — the first PR-2 surface that reads them, and the only sequencing that keeps T147's gate satisfiable (round 4 M2: "alongside the first surface" was ambiguous while § Implementation Strategy sequenced this task first, and keys landing before any reader makes `dead-i18n-keys.test.ts` red for the gap): `portal.broadcasts.approval.*` and `admin.broadcasts.approval.*` in `src/i18n/messages/{en,th,sv}.json` — every key referenced by a PR-2 screen (T063, T084, T085, T086) and none that is not, so T147's `dead-i18n-keys.test.ts` stays green in both PRs (round 3 H2). **T120's five stage labels are a different matter and DO run first**: they live in the two pre-existing `*.status` namespaces, which already have readers — RED: `pnpm check:i18n` plus `tests/unit/broadcast/dead-i18n-keys.test.ts` re-run over the widened namespace set.

---

## Phase 2: Foundational — PR-1 shared groundwork

**Purpose**: the pieces more than one story depends on: the ONE sanitiser policy (editor, server, preview, send, test copy), migration `0304`, the design-block renderer and the wrapper change with its byte-identical guard, brand settings, and the image lifecycle record. Work that only one story needs lives inside that story.

**⚠️ CRITICAL**: PR-1 ships **unflagged into live outgoing email**. T007 is a merge blocker and must be captured from **today's** output before any wrapper edit.

### Tests first (write, observe RED)

- [x] T007 Capture the byte-identical wrapper baseline **before touching `renderBroadcastHtml`** — with no brand colour, no postal address, no logo on file and no design block in the body, the rendered HTML must equal today's byte for byte (spec § Feature flag: "that snapshot test is a merge blocker for the unflagged tool upgrade"; plan Amendment 2) — RED: `tests/unit/broadcasts/infrastructure/render-broadcast-html-baseline.test.ts` "no brand, no blocks → byte-identical to the 2026-09-18 baseline"; snapshot committed from `src/modules/broadcasts/infrastructure/resend/email-template.ts` as it stands at `main`.
- [x] T008 [P] Element-and-attribute parity across editor → server sanitiser → preview render → send-time render for one document containing **every** offered construct, with a **positive control** that fails when a tag is removed from one config (SC-011, research R9) — RED: `tests/integration/broadcasts/eblast-content-parity.test.ts` "element+attribute multiset is identical at every stage" + "positive control: dropping `blockquote` from one config fails the comparison".
- [x] T009 [P] Design-block round-trip survives arbitrary attribute order and hostile text: serialise → sanitise → `applyDesignBlocks` yields the same block set, and an unknown `data-eb` value is left as a plain element (research R10 — unknown ⇒ no block ⇒ fail-safe; **no `default: return _exhaustive` anywhere near it**) — RED: `tests/unit/broadcasts/domain/design-block-markers.property.test.ts` "property: any attribute order parses to the same DesignBlock" and "`data-eb=\"anything\"` renders as a plain element" (uses `fast-check@^4`, already a dev dep from F4).
- [x] T010 [P] No route reachable with `settings.broadcasts` alone can write `tenant_invoice_settings.logo_blob_key`, with a positive control that fails when a write path is added (FR-041b, the panel's confirmed #7 blocker) — RED: `tests/contract/broadcasts/brand-cannot-write-invoice-logo.test.ts` "every settings.broadcasts-reachable route leaves logo_blob_key unchanged" + "positive control".
- [x] T011 [P] Two-tenant probe on `broadcast_images` **and on `tenant_broadcast_settings`** — reads **and** writes refused in **both** directions under RLS + FORCE (Constitution I.3, Review-Gate blocker) — RED: `tests/integration/broadcasts/eblast-approval-tenant-isolation.test.ts` "tenant B cannot read or write tenant A's broadcast_images" **and** "tenant B cannot read or write tenant A's `tenant_broadcast_settings` brand columns" — the settings table is not new, but 0304 turns it into a tenant-scoped **write** surface and T021 newly registers it in `SCOPED_TABLES`, so it gets the same probe as a new table. **T011 owns this file**; T047 extends it with the `broadcast_versions` / `broadcast_member_decisions` arms in PR-2 rather than creating it — one author per file, no parallel writes across the PR boundary.
- [x] T012 [P] A brand colour whose contrast with white text is below 4.5:1 is refused with the computed ratio and the **previous colour stays in force** (FR-041b, spec § Edge Cases "Brand colour too light") — RED: `tests/unit/broadcasts/domain/brand-contrast.test.ts` "`#f5f5f5` → ratio < 4.5, refused" and `tests/contract/broadcasts/admin-eblast-brand.test.ts` "422 `colour_contrast` carries `{ ratio, required }` and the stored colour is unchanged".

### Shared policy and Domain

- [x] T013 Create the single sanitiser policy `src/lib/broadcast-content-policy.ts` exporting `makeBroadcastSanitizerConfig({ images: boolean })` — pure and import-free so a client component may read it without a Presentation → Domain import (Principle III, research R9; `src/lib/email-brand.ts` is the precedent). It adds `data-eb` to `ALLOWED_ATTR`, keeps `ALLOWED_ATTR: ['href','target','rel','src','alt','data-eb']`, keeps `FORBID_ATTR: ['style']` and keeps `class` absent — which is why text alignment stays impossible and out of scope — RED: covered by T008.
- [ ] T014 Delete the three divergent configs and point every consumer at the shared policy: the editor paste config (`src/components/broadcast/tiptap-editor.tsx:74-90`, today missing `target`/`rel`), the server sanitiser (`src/modules/broadcasts/infrastructure/sanitizer/dompurify-sanitizer.ts:46-100`) and the preview, which **stops sanitising at all** (`src/components/broadcast/preview-pane.tsx:24-60`, today forbids `img` at `:55` so uploaded images are invisible — audit finding #2) — RED: T008 goes green only when all three agree.
- [x] T015 [P] `parseBlockMarkers` / `validateBlocks` in `src/modules/broadcasts/domain/design-blocks/block-markers.ts`: a bounded, attribute-order-independent matcher for `<a …data-eb="cta"…>` and `<img …data-eb="banner"…>` — no HTML parser, no jsdom, no new dependency. Violations are `cta_text_length` (text outside 1–60), `too_many_cta` (> 3 per message), `cta_link_scheme` (scheme outside `http`/`https`/`mailto`), `banner_alt_required` (description outside 1–125) — RED: T009 plus `tests/unit/broadcasts/domain/design-block-validate.test.ts` "4th CTA → too_many_cta".
- [x] T016 `applyDesignBlocks(sanitisedHtml, brand)` in `src/modules/broadcasts/domain/design-blocks/render-blocks.ts` — platform-owned markup (bgcolor table cell for the button, 600 px image cell for the banner) generated **after** sanitisation and **never** fed back through DOMPurify (the panel carry-forward). The pre-transform markup IS the degradation path: a client that cannot render the table still shows the plain link and the plain image (FR-042) — RED: `tests/unit/broadcasts/domain/render-design-blocks.test.ts` "CTA renders with the brand colour and degrades to the same `<a>`".
- [x] T017 [P] WCAG 2.1 relative luminance + contrast ratio + `meetsAaOnWhiteText(hex)` in `src/modules/broadcasts/domain/brand/contrast.ts` — ~30 lines of pure arithmetic, no colour library (Constitution X; there is no contrast helper anywhere in `src/` today) — RED: T012.
- [x] T018 [P] `BrandSettings` value objects in `src/modules/broadcasts/domain/brand/brand-settings.ts`: `primaryColor: string | null` (`#RRGGBB`), `postalAddress: string | null` (≤ 300 chars, **line breaks allowed**), `logoUrl: string | null` (read-only, never written here) — RED: `tests/unit/broadcasts/domain/brand-settings.test.ts` "a 301-character address is refused; a 300-character one with line breaks is accepted".

### Migration 0304 and the schema

- [x] T019 Write `drizzle/migrations/0304_eblast_images_and_brand.sql` by hand and register it in `meta/_journal.json` as `idx: 305`, `when: 1798543700000`, `tag: "0304_eblast_images_and_brand"`. It carries, verbatim from `data-model.md` §§ 4, 5, 6, 7.2:
      **`broadcast_images`** — `tenant_id text NOT NULL`; `id uuid NOT NULL DEFAULT gen_random_uuid()` with PK `(tenant_id, id)`; `owner_kind text NOT NULL CHECK (owner_kind IN ('broadcast','template'))`; `owner_id uuid NOT NULL` (**no FK** — two possible parents); `content_hash text NOT NULL`; `blob_url text NOT NULL`; `blob_key text NOT NULL`; `mime_type text NOT NULL CHECK (mime_type IN ('image/png','image/jpeg','image/webp','image/gif'))`; `byte_size integer NOT NULL CHECK (byte_size BETWEEN 1 AND 5*1024*1024)`; `uploaded_by_user_id uuid NOT NULL`; `created_at timestamptz NOT NULL DEFAULT now()`; `deleted_at timestamptz NULL`. Indexes `(tenant_id, owner_kind, owner_id)`, `(tenant_id, content_hash)` (the last-reference rule) and `(tenant_id, deleted_at) WHERE deleted_at IS NOT NULL` (the sweep). **Not backfilled** — images uploaded before 0304 have no row and are never swept.
      **RLS**, byte-identical to `drizzle/migrations/0064_create_broadcasts.sql:160-169` including the two spaces in `FORCE  ROW LEVEL SECURITY`: `ENABLE ROW LEVEL SECURITY`, `FORCE  ROW LEVEL SECURITY`, and `CREATE POLICY "tenant_isolation_on_broadcast_images" … TO chamber_app USING ("tenant_id" = current_setting('app.current_tenant', TRUE)) WITH CHECK (same)`.
      **`tenant_broadcast_settings` +4**: `brand_primary_color text NULL CHECK (brand_primary_color ~ '^#[0-9a-fA-F]{6}$')`, `brand_postal_address text NULL CHECK (char_length(brand_postal_address) BETWEEN 1 AND 300)`, `brand_updated_at timestamptz NULL`, `brand_updated_by_user_id uuid NULL`.
      **`audit_event_type` += 4**, one `ALTER TYPE "audit_event_type" ADD VALUE IF NOT EXISTS '…';` **per line** (the extractor in `scripts/run-migrations.ts` hoists them into the AUTOCOMMIT pass): `broadcast_test_copy_sent`, `broadcast_brand_settings_changed`, `broadcast_image_uploaded`, `broadcast_image_removed`.
      **Re-read `meta/_journal.json` immediately before writing; after `pnpm db:migrate`, verify via `information_schema` / `pg_class` / `pg_enum` (quickstart § 0) that the DDL actually landed — never trust the migrator's "✓ applied"; and run the touched integration suites BEFORE committing the migration** (CLAUDE.md § Gotchas, F4 R8 incident) — RED: T011.
- [x] T020 Mirror 0304 in Drizzle: add the `broadcastImages` table and the four `brand_*` columns on `tenantBroadcastSettings` in `src/modules/broadcasts/infrastructure/schema.ts` (Drizzle-inferred types stay in Infrastructure and never leave) — RED: T011.
- [x] T021 [P] Register `broadcast_images` **and** the pre-existing-but-unlisted `tenant_broadcast_settings` in `scripts/check-multi-tenant-ready.ts` `SCOPED_TABLES` (the F7 block at `:77-80`); `broadcast_versions` and `broadcast_member_decisions` join in T049 — RED: `pnpm check:multi-tenant` fails before the registration and passes after (the gate is the test).
- [x] T022 Add the **four** 0304 audit event types in all **five** places as one unit (CLAUDE.md § Gotchas, F7 flavour — the ten 0305 values are T050, and the split is exactly the migration boundary, nowhere else): (1) `F7_AUDIT_EVENT_TYPES` in `src/modules/broadcasts/application/ports/audit-port.ts:50-178` with the static assert at `:234` moved **55 → 59** — **and, in the same edit, the assert's docblock at `:180-192` (which today still claims "the tuple length is 60" and itself instructs updating "the tuple, this literal, and the header taxonomy") plus the file-header taxonomy comment**; a stale count comment beside a self-checking assert is how the next author gets the number wrong; (2) `DB_ONLY_AUDIT_EVENT_TYPES` in `src/modules/auth/infrastructure/db/schema.ts:522-678` (every `broadcast_*` value lives there, not in the pgEnum tuple); (3) the `ADD VALUE` lines already in T019; (4) `audit.eventType.<name>` labels in `src/i18n/messages/{en,th,sv}.json` with real **Thai script** (asserted by `tests/unit/.../audit-event-label-coverage.test.ts:107`); (5) `REQUIRED_ENUM_VALUES.audit_event_type` in `scripts/lib/enum-migration-guard.ts` — RED: `tests/integration/broadcasts/audit-event-type-parity.test.ts` (tuple ↔ `pg_enum` on the `broadcast_` prefix) and `tests/unit/broadcasts/application/audit-event-type-emission.test.ts:102` (every tuple value must have a real emit site — so none may be added ahead of its emitter).

### Brand settings surface

- [x] T023 `BrandSettingsRepo` port in `src/modules/broadcasts/application/ports/brand-settings-repo.ts` + `src/modules/broadcasts/infrastructure/db/drizzle-brand-settings-repo.ts` reading and writing the four `brand_*` columns **through the `tx` from `runInTenant`**, never the pool-global `db` (the F7.1a US2 incident rule) — RED: T012's contract arm.
- [x] T024 `setBrandSettings` use case (`src/modules/broadcasts/application/use-cases/set-brand-settings.ts`) — refuses a colour below 4.5:1 with the computed ratio, audits `broadcast_brand_settings_changed { previous: { primaryColor, postalAddress }, next: { … }, actor_role }` (values, not member-written text, so this payload legitimately carries them), and **voids nothing**: no version, no `approved_version_id`, no stage is touched (FR-012, FR-041c). **100 % branch pinned** — RED: T012 + `tests/contract/broadcasts/admin-eblast-brand.test.ts` "a brand PATCH leaves approved_version_id unchanged".
- [x] T025 `getBrandSettings` use case returning colour, address, the **read-only** logo URL, `addressMissing: boolean`, `defaults.primaryColor = '#10487a'` and a `manageHref` that is non-null **only** for a `settings.invoicing` holder (contracts § `GET /api/admin/broadcasts/brand`) — RED: `tests/contract/broadcasts/admin-eblast-brand.test.ts` "an admin without settings.invoicing gets `manageHref: null`".
- [x] T026 `GET | PATCH /api/admin/broadcasts/brand` in `src/app/api/admin/broadcasts/brand/route.ts` with `requireApiPermission(request, 'settings.broadcasts')` on **both** verbs (so `check:api-route-guard` sees it) — `marketing` gets 403; `PATCH` accepts **no** logo key at all — RED: T010 + `tests/contract/broadcasts/admin-eblast-brand.test.ts` "marketing → 403 permission_denied (audited)".
- [ ] T026a **[PR-1]** The **new write buckets on the four PR-1 routes**. Staff, **30 requests / 60 seconds per (tenant, actor)**: `PATCH /api/admin/broadcasts/brand` (T026), `POST /api/admin/broadcasts/[id]/images` (T106) and `POST /api/admin/broadcasts/templates/[id]/images` (T107). **Member, 60 / minute per (tenant, user)**: `POST /api/broadcasts/inline-image-upload` — the same member-write bucket as the decision route, added here because that route is PR-1's (T033 records its row, T146 adds its ownership check) and because a 5 MB cap and a ClamAV scan bound **one** request, never the rate of them; it carries no bucket at all today (maintainer decision, round 4 H5; `portal-eblast-approval-api.md` § Rate limits no longer says "this feature does not add one"). All four over the existing `broadcastsRateLimiter` (`src/modules/broadcasts/infrastructure/rate-limiter.ts:14`), in the `RECIPIENT_COUNT_RATE_MAX` / `_WINDOW_SECONDS` shape (`src/lib/broadcasts-recipient-count.ts:30-31`), refused **429 `broadcast_rate_limit_exceeded`** with `retryAfterSeconds` — an **atomic** check, never a peek-then-act. `approve`/`reject`/`cancel` carry no bucket today, so every number here is an addition, not a reuse (contracts `admin-eblast-formatting-api.md` § Rate limits; spec § Roles) — RED: `tests/contract/broadcasts/admin-eblast-rate-limits.test.ts` "the 31st brand PATCH in a minute → 429 with `Retry-After`", "the 31st image upload in a minute → 429, and nothing is stored on the refused call", **and "the 61st `POST /api/broadcasts/inline-image-upload` in a minute → 429 `broadcast_rate_limit_exceeded` with `Retry-After`, with no blob stored, no `broadcast_images` row and no scan run"**. **T026a owns this file**; T062a extends it in PR-2.
- [x] T027 `TenantLogoUrlPort` + the READ-only barrel export `getTenantLogoPublicUrl(tenantCtx)` on `src/modules/invoicing/index.ts`, wired through `src/lib/broadcast-brand-deps.ts`, reusing `loadTenantLogo`'s 50-entry FIFO positive cache and 60 s negative cache (research R12). Fail-soft: `null` ⇒ the header renders the chamber name exactly as today (FR-041a) — RED: `tests/contract/broadcasts/tenant-logo-url-port.test.ts` "no logo on file → null, header falls back to the chamber name" (and T010 proves there is no write path).
- [ ] T028 Brand settings page `src/app/(staff)/admin/settings/broadcasts/brand/page.tsx` + `loading.tsx` + `error.tsx` with `requirePagePermission('settings.broadcasts')` (so `check:staff-page-guard` sees it) and an in-page gate on `env.features.f7Broadcasts` — the proxy kill-switch predicate covers `/admin/broadcasts`, **not** `/admin/settings/**` (research R18). A user without the permission, **`marketing` included**, sees no nav entry, no Settings-index card and no page — invisible, not disabled — RED: `tests/contract/broadcasts/brand-page-guard.test.ts` "marketing → redirect/403, no page".
- [ ] T029 Register the Brand page in **three** places, because the nav guard key must equal the page's `requirePagePermission` key and both files' docblocks record past incidents of one being forgotten: the Settings section of `src/config/nav.ts`, the `CATEGORIES` array in `src/app/(staff)/admin/settings/page.tsx`, and `admin.settings.index.categories.eblastBrand.*` in all three locales — RED: `tests/unit/broadcast/brand-settings-nav-registration.test.tsx` "the nav entry, the settings card and the page name the same permission key".
- [ ] T030 `src/components/broadcast/brand/brand-settings-form.tsx` — colour field with a **live contrast readout** and Save disabled while the ratio is below 4.5:1 (so the refusal is never a surprise), a multi-line address field flagged when empty, and the logo shown as a read-only preview with its source; the colour is used in **email only**, never in portal or admin chrome (FR-041c) — RED: `tests/unit/broadcast/brand-settings-form.test.tsx` "typing `#f5f5f5` shows the ratio and disables Save".

### The wrapper, the preview route and the image record

- [x] T031 Change `renderBroadcastHtml` (`src/modules/broadcasts/infrastructure/resend/email-template.ts:150-173`) to render the **logo header** when a logo is on file and the chamber name otherwise (FR-041a), the **footer postal address** from brand settings (chamber name only while unset), and to call `applyDesignBlocks` **after** sanitisation. Brand chrome is read **live** at render time and never frozen into a version (FR-041c) — RED: T007 must stay green (no brand, no blocks → byte-identical) and `tests/unit/broadcasts/infrastructure/render-broadcast-html-brand.test.ts` "logo present → header carries the logo; absent → the chamber name". **T031 owns that test file**; T090 and T091 extend it rather than creating it — one author, one file, no parallel writes.
- [x] T032 `renderBroadcastPreview` use case + `POST /api/broadcasts/preview` (`requireMemberContext`) and `POST /api/admin/broadcasts/preview` (`requireApiPermission('broadcasts.read')`) — both thin wrappers over the **same** `renderBroadcastHtml` the sender calls, returning a full HTML document for an `<iframe srcdoc>`; 400 `invalid_body` over the limits, **429 at 30 renders/minute per actor**, no audit event, counter `broadcasts_preview_rendered_total` **registered by T122a in this PR** (research R11; the member/staff pair mirrors the existing `recipient-count` precedent) — RED: `tests/contract/broadcasts/eblast-preview-route.test.ts` "31st render in a minute → 429 with Retry-After" and "a 201 KB body → 400".
- [x] T122a **[PR-1] Sequenced BEFORE T032** (round 5 A3 — registration precedes the emitter, the same rule as T121/T122 at the head of PR-2; it keeps its ID). Register the **two preview metrics T032 emits** in `src/lib/metrics.ts` — counter `broadcasts_preview_rendered_total{tenant,surface}` and histogram `broadcasts_preview_render_ms{tenant}` — plus the OTel span `broadcasts.preview.render` with attributes limited to ids and stage. **This half cannot wait for T122**: T032 ships in PR-1 and `contracts/dashboard-and-notifications.md` § 4.2 is the registration list — "a metric not on it does not exist", and an emit against an unregistered field does not typecheck (`/speckit.analyze` round 3 C1). The other four counters, the decide histogram and the remaining three spans are T122 — RED: `tests/unit/broadcasts/application/eblast-metrics.test.ts` "a preview render increments the counter exactly once and records the histogram".
- [x] T033 `BroadcastImagesRepo` + `drizzle-broadcast-images-repo.ts` (threading `tx`) and a `broadcast_images` row written by `uploadInlineImage` on every successful upload — **the member entry point to this use case, `POST /api/broadcasts/inline-image-upload`, takes the 60 / minute per-(tenant, user) bucket T026a builds** (round 4 H5), so a refused call writes no row at all — audited `broadcast_image_uploaded { member_id | related_member_id, owner_kind, owner_id, image_id, byte_size, mime_type, content_hash }` — **never** the blob URL. **A member upload MUST carry snake_case `member_id`** (a staff upload carries `related_member_id` instead): migration 0009's `last_activity_at` SECURITY DEFINER trigger fires on that one key and on no other, and two features have already shipped `memberId` and had the trigger silently never fire (#336/#337) — RED: `tests/contract/broadcasts/upload-inline-image-records-row.test.ts` "a success writes one broadcast_images row and one broadcast_image_uploaded audit row" and "a **member** upload's payload carries snake_case `member_id` and the member's `last_activity_at` moves; a **staff** upload carries `related_member_id` and does not move it".
- [x] T034 Widen `ImageStoragePort` with `delete(key)` and implement it over `@vercel/blob`'s `del` (four sibling adapters already do this) — today the port has **no** delete method at all, so inline-image blobs are never reclaimed — RED: `tests/unit/broadcasts/application/image-storage-port-delete.test.ts` "delete is called for a swept image".
- [x] T035 `reclaimOrphanedImages` use case + the **image-sweep block** in `src/app/api/cron/broadcasts/prune-expired-drafts/route.ts`: a second, independently transacted block with its own `SET LOCAL statement_timeout`, its own try/catch and its own OK flag in the response body, deleting a blob **iff no live `broadcast_images` row of either `owner_kind` — no E-Blast and no template — shares its `content_hash`** (the last-reference rule), then removing the row and auditing `broadcast_image_removed { …, reason: 'sweep', actor_role: 'system' }`. Keep `export const GET = POST` (native Vercel Cron invokes GET). The reminder/expiry steps join the same block in **T130** — RED: `tests/contract/broadcasts/eblast-image-sweep-cron.test.ts` "a blob whose hash is still referenced by a template is kept; an unreferenced one is deleted; a second run the same day is a no-op".

**Checkpoint**: the shared policy, the brand surface, the real wrapper and the image record exist. US3 and US6 can now proceed; PR-1 is not mergeable until T007, T008, T010 and the Phase-10 PR-1 docs tasks are green.

---

## Phase 3: User Story 1 — Marketing formats and the member signs off (Priority: P1) — **PR-2** 🎯

**Goal**: marketing creates a formatted version without touching the member's original, sends it to the member, the member approves, marketing confirms the send time, and the delivered email is exactly the approved version.

**Independent Test**: with one test member and one marketing user, submit an E-Blast, format it, send it to the member, approve it as the member, confirm the schedule as marketing, and verify the E-Blast that is delivered is identical to the version the member approved.

### Tests first (write, observe RED)

- [ ] T036 [US1] **FR-012a, before the migration is written**: a direct DB `UPDATE broadcasts SET subject = …`, `SET body_html = …` or `SET scheduled_for = …` outside the application is still refused with `broadcast_immutable_after_submit` on **every non-exempt transition** and on a **no-status-change** update in each new stage, while the two exempt edges succeed — RED: `tests/integration/broadcasts/eblast-immutability-trigger.test.ts` "non-exempt transition → broadcast_immutable_after_submit", "no-status-change update in `in_design` → refused", "E1 `member_approved → approved` releases subject/body_html", "E2 `member_approved|approved → approved|changes_requested|in_design` releases scheduled_for", "`proposed_send_at` is refused on every post-draft write", **and — closing FR-005 — "`segment_type`, `segment_params` and `custom_recipient_emails` are refused on every post-draft write, including the two exempt edges"** (the audience is the member's choice and is not marketing's to change; the API arm of the same requirement is on T046).
- [ ] T037 [P] [US1] The full new edge matrix is refused or permitted identically by the DB trigger and the Domain map — for the **new edges only** (R-7: three pre-existing divergences are recorded, not fixed here) — RED: `tests/integration/broadcasts/eblast-state-machine-edges.test.ts` "every (from,to) in data-model § 8.2 behaves identically at the DB and in `canTransition`" and "`expired_no_member_response` has no outgoing edge".
- [ ] T038 [US1] The end-to-end acceptance path: submit → start version → save → send to member → member approves → confirm schedule → the row is dispatchable and `broadcasts.subject`/`body_html` **equal the approved version byte-for-byte** (US1-AS1..AS6, SC-002) — RED: `tests/integration/broadcasts/eblast-approval-happy-path.test.ts` "the delivered content equals the version the member approved".
- [ ] T039 [P] [US1] Starting a formatted version leaves the member's original untouched and both versions readable by marketing (US1-AS1, FR-001) — RED: `tests/contract/broadcasts/admin-eblast-start-version.test.ts` "after start, `broadcasts.subject`/`body_html` are unchanged and `version_no = 0` holds the original".
- [ ] T040 [P] [US1] Sending a version to the member makes it read-only and moves the stage (US1-AS2, FR-003) — RED: `tests/contract/broadcasts/admin-eblast-send-version.test.ts` "PATCH after send → 409 `stage_changed`" and "stage = awaiting_member_approval, round incremented, one member outbox row in the member's language".
- [ ] T041 [P] [US1] Approve-as-submitted still behaves exactly as today — no version rows, no sign-off round, and the history carries `approvedAsSubmitted { at, by }` instead of an empty thread (US1-AS7, FR-007, SC-006) — RED: `tests/contract/broadcasts/admin-eblast-approve-as-submitted.test.ts` "no broadcast_versions row is written and the thread returns approvedAsSubmitted".
- [ ] T042 [P] [US1] Confirming the schedule pre-selects the member's proposal, refuses a past proposal with the existing floor, refuses `keep_proposal` when there is none, and records the difference — the proposal itself is the immutable `proposed_send_at`, shown to both sides throughout and never overwritten by the confirmed time (US1-AS5, **FR-016**, FR-017, FR-018) — RED: `tests/contract/broadcasts/admin-eblast-schedule.test.ts` "`keep_proposal` with a past proposal → 422 `broadcast_schedule_too_soon`", "no proposal → 409 `no_proposal`", "the audit row carries `differs: true` when the confirmed time is not the proposal".
- [ ] T043 [P] [US1] A member company with **no active portal user** cannot have a version sent for approval (spec § Edge Cases, proxy-submitted E-Blast) — RED: `tests/contract/broadcasts/admin-eblast-send-version.test.ts` "no portal user → 409 `no_portal_user` and no stage change".
- [ ] T044 [P] [US1] An image whose host left the tenant allow-list blocks **both** the send-to-member and the **promotion**, naming which image and why, leaving the version editable (spec § Edge Cases) — RED: `tests/contract/broadcasts/admin-eblast-image-allowlist-recheck.test.ts` "send → 422 `image_source_not_allowlisted` with `{ images: [{ src, host, reason }] }`" and "promotion on `POST …/schedule` is refused with the same code".
- [ ] T045 [P] [US1] Two marketing users editing the same working copy: the second save loses, clearly (FR-033, spec § Edge Cases) — RED: `tests/contract/broadcasts/admin-eblast-version-concurrency.test.ts` "two PATCHes with the same `expectedUpdatedAt` → the second is 409 `version_changed` with `currentUpdatedAt` and the current content".
- [ ] T046 [P] [US1] RBAC and session pins per route (spec § Roles, FR-013) **plus the FR-005 audience pin** — RED: `tests/contract/broadcasts/admin-eblast-rbac.test.ts` "`manager` → 403 on every write route including test copy, 200 on `GET …/version`", "`marketing` → 200 on format/send/schedule/images, **403 on brand**", "a member session → 403 on every staff route, including for a person who also holds a portal account of the owning member", and **"a `PATCH …/version` or `POST …/version/send` carrying `segmentType`/`segmentParams`/`customRecipientEmails` leaves `broadcasts.segment_type`, `segment_params` and `custom_recipient_emails` unchanged" (FR-005)** — asserted directly, because "the zod schema has no such field" is today's shape, not a guarantee.
- [ ] T047 [US1] Two-tenant probe on `broadcast_versions` and `broadcast_member_decisions` — reads and writes refused in both directions (Constitution I.3, Review-Gate blocker) — RED: `tests/integration/broadcasts/eblast-approval-tenant-isolation.test.ts` "tenant B cannot read or write tenant A's versions or decisions".
- [ ] T149 **[PR-2]** [US7] With the flag off, today's approve/reject flow behaves exactly as today and the tool/screen improvements remain (US7-AS1, FR-034, SC-006) — it is the RED for T152, and T152 is what makes PR-2 a dark ship (`/speckit.analyze` C1). **It lives in this Phase-3 Tests-first block, not in Phase 9** (round 4 H1): T152 must land with T062, which is Phase 3, so leaving its RED in Phase 9 would have implemented the merge gate before the test that proves it — Constitution II, and a guard whose test never went red has never been exercised — RED: `tests/contract/broadcasts/eblast-flag-matrix.test.ts` "every existing E-Blast acceptance test passes unchanged with the flag off" and "`POST …/[id]/version` on a **`submitted`** broadcast → 404 with the flag off, 201 with it on". **T149 owns this file**; T150 and T152a extend it.
- [ ] T150 [P] **[PR-2]** [US7] With the flag off, a row **already** in a new stage stays completable and cancellable — the flag gates **the `submitted → in_design` edge, not the route and not the exits** (US7-AS3, FR-034, research R18). **Also hoisted into Phase 3 with T149** (round 4 H1) — RED: `tests/contract/broadcasts/eblast-flag-matrix.test.ts` "with the flag off, a broadcast in `in_design` can still be saved, sent, decided and scheduled", **"with the flag off, `POST …/[id]/version` on a broadcast in `changes_requested` → 201 (re-entry after the member asked for changes is an EXIT-side write: gating it would leave the row only cancellable, never completable, which FR-034 forbids — round 3 H1)"**, "with the flag off, the same POST on `member_approved`/`approved` with `current_round >= 1` → 201, and with `current_round = 0` → 409 `round_zero` in both flag states", and `tests/contract/broadcasts/portal-eblast-decision.test.ts` "with the flag off, a broadcast already in `awaiting_member_approval` can still be approved".

### Schema, Domain and the shadow sweep

- [ ] T048 [US1] Write `drizzle/migrations/0305_eblast_member_approval.sql` by hand — **the FR-012a bundle, one migration** — and register it as `idx: 306`, `when: 1798543800000`, `tag: "0305_eblast_member_approval"`. It carries, verbatim from `data-model.md`:
      **`broadcast_status` += 5**, one `ALTER TYPE "broadcast_status" ADD VALUE IF NOT EXISTS '…';` **per line**: `in_design`, `awaiting_member_approval`, `changes_requested`, `member_approved`, `expired_no_member_response`.
      **`broadcast_versions`** — `tenant_id text NOT NULL`; `id uuid NOT NULL DEFAULT gen_random_uuid()` PK `(tenant_id, id)`; `broadcast_id uuid NOT NULL` FK `(tenant_id, broadcast_id) → broadcasts(tenant_id, broadcast_id) ON DELETE CASCADE`; `version_no smallint NOT NULL CHECK (version_no >= 0)`; `subject text NOT NULL CHECK (char_length BETWEEN 1 AND 200)`; `body_html text NOT NULL CHECK (octet_length BETWEEN 1 AND 200*1024)`; `body_source text NOT NULL`; `note_to_member text NULL CHECK (char_length <= 1000)`; `authored_by_user_id uuid NOT NULL`; `authored_by_role broadcast_actor_role NOT NULL`; `sent_to_member_at timestamptz NULL`; `created_at`/`updated_at timestamptz NOT NULL DEFAULT now()`. Indexes: `UNIQUE (tenant_id, broadcast_id, version_no)`, `broadcast_versions_one_unsent_idx UNIQUE (tenant_id, broadcast_id) WHERE sent_to_member_at IS NULL`, `(tenant_id, broadcast_id, version_no DESC)`, `(tenant_id, broadcast_id)`. Trigger `broadcast_versions_immutable_after_send_fn` BEFORE UPDATE raising `broadcast_version_immutable_after_send` (`ERRCODE = check_violation`) on any change to `subject`, `body_html`, `body_source`, `note_to_member`, `version_no`, `authored_by_user_id` or `sent_to_member_at` once `OLD.sent_to_member_at IS NOT NULL`, exempted only under `app.allow_broadcast_redaction` and then **only** for the four content columns (whitelist by omission, the 0299 shape).
      **`broadcast_member_decisions`** — PK `(tenant_id, id)`; `broadcast_id` and `version_id` FKs `ON DELETE CASCADE`; `round smallint NOT NULL CHECK (round >= 1)`; `decision text NOT NULL CHECK (decision IN ('approved','changes_requested','approval_withdrawn'))`; the one reason CHECK carrying both bounds: `(decision = 'approved' AND (reason IS NULL OR char_length(reason) BETWEEN 1 AND 500)) OR (decision <> 'approved' AND reason IS NOT NULL AND char_length(reason) BETWEEN 1 AND 2000)`; `decided_by_user_id`, `decided_by_contact_id uuid NOT NULL`; `decided_at timestamptz NOT NULL DEFAULT now()`. **Neither actor column carries an FK, by decision**: `users` is cross-tenant (a composite-key FK from a `tenant_id`-scoped table cannot reach it) and `decided_by_contact_id` is left unconstrained for the same reason the erasure scrub keeps decision rows — SC-002's proof must survive a contact's removal. Indexes `(tenant_id, broadcast_id, decided_at DESC)`, `(tenant_id, version_id)` **and `(tenant_id, decided_by_contact_id)`** (the 0302 FK-column-index lesson applies to the lookup even without the constraint — "which contact approved this" is a DSAR and audit query). Triggers: `broadcast_member_decisions_append_only_fn` raising `broadcast_decision_append_only` BEFORE UPDATE except under the redaction GUC when **only** `reason` changes, and BEFORE DELETE unconditionally.
      **RLS + FORCE + the 0064 policy** on both tables, byte-identical to T019's template (two spaces in `FORCE  ROW LEVEL SECURITY`).
      **`broadcasts` +6 columns**: `proposed_send_at timestamptz NULL`, `stage_entered_at timestamptz NOT NULL DEFAULT now()`, `current_round smallint NOT NULL DEFAULT 0`, `approved_version_id uuid NULL` FK `(tenant_id, approved_version_id) → broadcast_versions(tenant_id, id)`, `member_reminder_stage smallint NOT NULL DEFAULT 0 CHECK (BETWEEN 0 AND 3)`, `member_expiry_notified_at timestamptz NULL`. New indexes `broadcasts_stage_queue_idx (tenant_id, status, stage_entered_at DESC)`, `broadcasts_awaiting_member_idx (tenant_id, stage_entered_at) WHERE status = 'awaiting_member_approval'`, `broadcasts_approved_version_idx (tenant_id, approved_version_id)`. **Backfill — and its ORDER IS NORMATIVE, in three steps: the columns (`ALTER TABLE broadcasts ADD COLUMN`, all six), then the backfills, then the `CREATE OR REPLACE` of `broadcasts_immutable_after_submit_fn`** (`/speckit.analyze` round 3 H5 + round 4 L3 — backfill 1 writes `proposed_send_at`, which does not exist until the `ADD COLUMN`, so "backfills before the function" is only half the rule; mirrored in `data-model.md` § 3): `UPDATE broadcasts SET proposed_send_at = scheduled_for WHERE status = 'submitted' AND scheduled_for IS NOT NULL;` and `UPDATE broadcasts SET stage_entered_at = COALESCE(submitted_at, updated_at) WHERE stage_entered_at IS DISTINCT FROM COALESCE(submitted_at, updated_at);` MUST both appear in the file **BEFORE** the `CREATE OR REPLACE FUNCTION broadcasts_immutable_after_submit_fn` statement below. F1 adds `proposed_send_at` to that function's frozen blocklist, and the function is a BEFORE UPDATE trigger: replace it first and the backfill's own `UPDATE` on `submitted` rows raises `broadcast_immutable_after_submit` and the whole migration aborts. Without the `stage_entered_at` backfill every pre-existing waiting row would read `now()` and T117's re-based `ageBadge` would reset every 24 h/48 h SLA badge to zero on deploy (round 3 M3).
      **`broadcasts_immutable_after_submit_fn` amended** starting from the live body at `drizzle/migrations/0299_broadcasts_audience_import_coherence.sql:90-174` with exactly three changes: **E1** `IF OLD.status = 'member_approved' AND NEW.status = 'approved' THEN content_changed := FALSE;`; **E2** `IF OLD.status IN ('member_approved','approved') AND NEW.status IN ('approved','changes_requested','in_design') THEN scheduled_for_changed := FALSE;` (the existing `submitted → approved` exemption kept **verbatim**); **F1** `proposed_send_at` joins the frozen blocklist. `segment_type`, `segment_params`, `custom_recipient_emails` stay frozen on every transition. The six new columns are **added to the GUC redaction arm's forbidden list** so the erasure scrub cannot move a row through the workflow.
      **`broadcasts_state_machine_fn` amended** with the arms in data-model § 8.2, keeping the `ELSE → empty targets` fail-closed default of `0217:65-70`.
      **`notification_type` += 5**, one `ADD VALUE` per line: `eblast_submitted_marketing`, `eblast_member_decided_marketing`, `eblast_version_sent_member`, `eblast_schedule_confirmed_member`, `eblast_approval_lifecycle`.
      **`audit_event_type` += 10** (T050's list), one `ADD VALUE` per line.
      **Re-read `meta/_journal.json` immediately before writing; after `pnpm db:migrate`, verify via `information_schema` and `SELECT prosrc FROM pg_proc` that both function bodies were actually replaced (quickstart § 0 greps for `member_approved` ≥ 2 and `awaiting_member_approval`) — a fixture proves nothing, read the SOURCE; and run the touched integration suites BEFORE committing the migration** — RED: T036, T037, T047.
- [ ] T049 [US1] Mirror 0305 in Drizzle and register the gates: `broadcastStatusEnum` 10 → 15 in the **same order** and the two new tables + six columns in `src/modules/broadcasts/infrastructure/schema.ts`; `SCOPED_TABLES += broadcast_versions, broadcast_member_decisions` in `scripts/check-multi-tenant-ready.ts`; in `scripts/lib/enum-migration-guard.ts` **add a new `broadcast_status` key to `REQUIRED_ENUM_VALUES` carrying the five values** — there is no such key today, so this is a key addition, not an append to an existing list (round 4 L4) — and append the five `notification_type ×5` values to the key that does exist — because a silently-no-op `ADD VALUE` would 500 **every** hand-off in prod instead of failing the deploy (R-9) — RED: `pnpm check:multi-tenant` + `tests/integration/broadcasts/audit-event-type-parity.test.ts`.
- [ ] T050 [US1] Add the **ten** 0305 audit event types in all **five** places as one unit (the same five as T022, with the static assert at `audit-port.ts:234` moved **59 → 69** and its docblock at `:180-192` plus the file-header taxonomy corrected to 69 in the same edit — T022 already fixed the stale "60"; do not let it drift again): `broadcast_version_started`, `broadcast_version_sent_to_member`, `broadcast_member_approved`, `broadcast_member_changes_requested`, `broadcast_member_approval_withdrawn`, `broadcast_member_approval_voided`, `broadcast_schedule_confirmed`, `broadcast_approval_reminder_sent`, `broadcast_approval_expiry_warned`, `broadcast_approval_expired`. Keep the `broadcast_` prefix (the parity test scopes on it). Retention is 5 years for all ten (`f7RetentionFor`) — no tax document is produced — RED: `tests/integration/broadcasts/audit-event-type-parity.test.ts` and `tests/unit/broadcasts/application/audit-event-type-emission.test.ts:102` ("every tuple value has a real emit site" — none may be added ahead of its emitter).
- [ ] T051 [US1] Widen the Domain status union and run the **shadow sweep** — every compile-time exhaustive map and every hand-listed literal set, from research R6, verified file by file: *compile-time (the build breaks — good)* `domain/value-objects/broadcast-status.ts:15-27` (tuple 10 → 15) and `:93-99` (`TERMINAL_*` += `expired_no_member_response`), `domain/policies/broadcast-status-transitions.ts:35-56`, `domain/invariants/one-active-broadcast-state.ts:48-174` (keys at 49, 60, 71, 83, 94, 105, 117, 127, 144, 161), `domain/broadcast.ts:219-273` (`BroadcastPhase`) and `:283-403` (`phaseOf` switch — **no `default` arm; never add `return _exhaustive`, which is fail-open at runtime**), `src/components/broadcast/status-badge-mapping.ts:35-67`; *runtime-derived (the build stays green — these are the dangerous ones, so each names the task that actually WIDENS it, never the task that merely tests it — all four citations were wrong, `/speckit.analyze` round 3 H6)* `src/components/broadcast/admin/queue-filters.tsx:64-69` (`IN_REVIEW_STATUSES`, hand-listed — widened by **T116**, the one site in this list owned by a different user story (US4); T096/T151 only test around it), `drizzle-broadcasts-repo.ts:384` and `:1322` (**T080**), `domain/policies/cancel-cutoff-policy.ts:47,49` (**T081**), `drizzle-broadcast-approval-counter.ts:24` (**T132**), `src/app/(staff)/admin/broadcasts/[id]/page.tsx:93,200` and `src/app/(member)/portal/broadcasts/[id]/page.tsx:254` (CTA gating), `scripts/reset-broadcast-quota.ts:68-73` + `scripts/inventory-broadcast-outbox.ts:115,126` (operator in-flight sets — widen; a report that hides five stages is worse than no report). **Leave alone, recorded so nobody "fixes" them**: `drizzle-broadcasts-repo.ts:394` (consumed set), the SLA window at `admin/broadcasts/page.tsx:78` + `sla-stats/route.ts:64`, `broadcasts-gauges/route.ts` `queuePending`, and the three cross-module `['sent','partial_delivery_accepted']` readers in insights/renewals — RED: the enumeration tests that pin the old shape must go red first: `tests/unit/broadcasts/domain/broadcast-state-machine.test.ts:34-44,:49 (toHaveLength(10)),:68-88`, `tests/unit/broadcasts/domain/retired-broadcast-status.test.ts:43-131`, `tests/unit/broadcast/queue-filters-grouping.test.tsx:101-131`, `tests/unit/broadcasts/components/status-badge-mapping.test.ts:27`, `tests/integration/broadcasts/state-machine-f71a-transitions.test.ts`.
- [ ] T052 [P] [US1] `stageOf(status)` and `turnOf(status)` in `src/modules/broadcasts/domain/stage/{broadcast-stage.ts,whose-turn.ts}` — total functions, no default arm; `turnOf` returns `'marketing' | 'member' | null` with **no `'system'` value** (FR-026), and the retired `partially_sent` / `partial_delivery_accepted` map to a `historical` stage no filter offers — RED: `tests/unit/broadcasts/domain/broadcast-stage.test.ts` "every status maps to exactly one stage" and "turnOf is null for draft, approved, sending and all five closed statuses".
- [ ] T053 [P] [US1] `IN_PROGRESS_BROADCAST_STATUSES` as the **one** Domain constant in `src/modules/broadcasts/domain/stage/in-progress-statuses.ts` = `['submitted','approved','in_design','awaiting_member_approval','changes_requested','member_approved'] as const satisfies readonly BroadcastStatus[]` — the allowance bucket **and** the cancel cascade derive from it (research R7, the Finding-G pattern) — RED: `tests/unit/broadcasts/domain/in-progress-statuses.test.ts` "the constant and `TERMINAL_BROADCAST_STATUSES` are disjoint and together cover every non-historical status".
- [ ] T054 [P] [US1] Version and decision aggregates + invariants in `src/modules/broadcasts/domain/approval/{broadcast-version.ts,member-decision.ts}`: `isVersionEditable(v) === (v.sentToMemberAt === null)`, `requiresReason(kind)` true unless `'approved'`, `reasonBounds(kind)` = **null or 1–500** for approved (never "0–500": a 0-length note would pass Domain and be refused by the DB CHECK, which is `reason IS NULL OR char_length BETWEEN 1 AND 500`) and 1–2,000 otherwise, `scheduleDiffers(proposed, confirmed)` — RED: `tests/unit/broadcasts/domain/member-decision-invariants.test.ts` "a 501-character approval note is refused while a 500-character one is accepted" and "an empty-string approval note is refused, a null one is accepted".

### Application, Infrastructure and routes

- [ ] T055 [US1] `BroadcastVersionsRepo` (`src/modules/broadcasts/application/ports/broadcast-versions-repo.ts` + `src/modules/broadcasts/infrastructure/db/drizzle-broadcast-versions-repo.ts`) and `BroadcastDecisionsRepo` (`src/modules/broadcasts/application/ports/broadcast-decisions-repo.ts` + `src/modules/broadcasts/infrastructure/db/drizzle-broadcast-decisions-repo.ts`) — every method takes the `tx` from `runInTenant` and **never** the pool-global `db` (RLS bypass); the decisions repo has **no update path** at all (append-only) — RED: T047.
- [ ] T056 [US1] `startFormattedVersion` use case in `src/modules/broadcasts/application/use-cases/approval/start-formatted-version.ts`: `FOR UPDATE` re-read, stage re-check, **lazy materialisation of `version_no = 0`** from `broadcasts.subject`/`body_html`/`body_source` (the last moment the member's original is intact), seed the working copy from the latest sent version or from v0 on round 1, transition to `in_design`, stamp `stage_entered_at`, audit `broadcast_version_started { related_member_id, broadcast_id, version_id, round, from_stage }`. Accepted from `submitted`, `changes_requested`, and from `member_approved`/`approved` **only when `current_round >= 1`** (else 409 `round_zero`). **The T152 flag gate applies to the `submitted` arm and no other** — the other three arms are exit-side writes on rows already in the round and stay available with the flag off (FR-034, round 3 H1). **100 % branch pinned** — RED: T039.
- [ ] T057 [US1] The **voiding arm inside `src/modules/broadcasts/application/use-cases/approval/start-formatted-version.ts`** — step 5 of the contract, **not** its own module: there is no `void-member-approval.ts`, and its 100 % branch pin therefore rides on `start-formatted-version.ts` under T158 (`/speckit.analyze` round 3 M8) — on the `member_approved|approved → in_design` transition: clear `approved_version_id`, clear `scheduled_for` (trigger exemption E2) and emit `broadcast_member_approval_voided { …, cancelled_schedule_at }` — the **only** thing that voids an approval is a change to `subject`/`body_html`/`body_source`; a brand PATCH, a schedule PATCH and a `note_to_member`-only change void nothing (FR-012, spec § Edge Cases "Marketing edits after the member approved") — RED: `tests/contract/broadcasts/admin-eblast-approval-not-voided.test.ts` "a brand PATCH, a non-cancel schedule PATCH and a note-only change each leave `approved_version_id` unchanged" + "opening a new working copy clears it".
- [ ] T058 [US1] `saveFormattedVersion` use case with **optimistic concurrency** on `expectedUpdatedAt`, re-running the shared sanitiser policy and every size and block rule on **every save** (FR-004): subject ≤ 200, `bodyHtml` ≤ 200 KB **including design-block markup**, image ≤ 5 MB, the per-tenant image-source allow-list; a violation is 422 and the version is **not saved**. `noteToMember` is bounded **≤ 1,000 characters in the route's zod schema** so an over-long note is a 422, never the DB CHECK's 500 (FR-006). No audit event (a save is not a hand-off); counter `broadcasts_version_saved_total`, **registered in `src/lib/metrics.ts` by T122 in this same PR** (the former PR-3 is folded into PR-2, so no emitter ships a PR ahead of its registration — round 3 C1) — RED: T045 + `tests/contract/broadcasts/admin-eblast-version-content-rules.test.ts` "61-character CTA text → 422 `cta_text_length`", "a 4th CTA → 422 `too_many_cta`", "`javascript:` in a CTA link → 422 `cta_link_scheme`", "a banner without a description → 422 `banner_alt_required`", **"a 1,001-character `noteToMember` → 422 `validation_error` while a 1,000-character one is accepted"**.
- [ ] T059 [US1] `sendVersionToMember` use case: re-check the content rules **again at this moment** (FR-004), require at least one active portal user of the owning member (409 `no_portal_user`), re-evaluate **every image's host against the allow-list** (422 naming the image), stamp `sent_to_member_at`, set `current_round = version_no` (**incremented here and only here**), transition to `awaiting_member_approval`, stamp `stage_entered_at`, reset `member_reminder_stage = 0` and `member_expiry_notified_at = NULL`, audit `broadcast_version_sent_to_member { …, note_length, notified }`, enqueue one `eblast_version_sent_member` row. **Re-sending byte-identical content is allowed** and becomes the next round (FR-011). **100 % branch pinned** — RED: T040, T043, T044 + `tests/contract/broadcasts/admin-eblast-send-version.test.ts` "a version byte-identical to the previous round is accepted as round N+1".
- [ ] T060 [US1] `confirmSchedule` use case in `src/modules/broadcasts/application/use-cases/approval/confirm-schedule.ts` (FR-017) with the four modes `keep_proposal | schedule | send_now | cancel`: from `member_approved` it **promotes the approved version** into `broadcasts.subject`/`body_html`/`body_source` (trigger exemption E1 — the only post-submit content write, on the only exempt edge, copying from `approved_version_id`), sets `scheduled_for` (E2) and transitions to `approved`; from `approved` it changes the time, or `cancel` clears `scheduled_for` and moves the row to `changes_requested` so it leaves the **only dispatchable status**. The existing `now + 5 min` floor applies (`broadcast_schedule_too_soon`); promotion re-checks the images. Audit `broadcast_schedule_confirmed { …, proposed_send_at, confirmed_send_at, differs, mode }`; enqueue `eblast_schedule_confirmed_member`. **100 % branch pinned** — RED: T038, T042, T044.
- [ ] T061 [P] [US1] `listBroadcastVersions` use case + `GET /api/admin/broadcasts/[id]/version` (`broadcasts.read`, so a `manager` can read it): the member's original, every sent version with author and time, every decision with its reason, the working copy, `updatedAt`, and `approvedAsSubmitted` on the no-formatting path — the member's original and every version sent to the member, each with who sent it and when, kept for the life of the E-Blast record (**FR-002**). This is the **history record** (FR-032) — the audit trail is never read to build it — RED: T041 + `tests/contract/broadcasts/admin-eblast-version-thread.test.ts` "a manager gets the full thread".
- [ ] T062 [US1] Route handlers `POST | PATCH | GET` in `src/app/api/admin/broadcasts/[id]/version/route.ts`, `POST` in `src/app/api/admin/broadcasts/[id]/version/send/route.ts` and `POST` in `src/app/api/admin/broadcasts/[id]/schedule/route.ts` — Node runtime, `requireApiPermission` naming `broadcasts.write` / `broadcasts.write` / `broadcasts.send` (so `check:api-route-guard` sees them), each state change ONE `runInTenant` with **throw-to-rollback** (`return err()` inside the callback would COMMIT), **no `Idempotency-Key`** (the stage + version id is the key; CI smoke has no Redis), every fault arm named `M119.admin.<route>.<arm>` — RED: T045, T046.
- [ ] T062a [US1] The **staff write buckets on the three PR-2 formatting routes**, extending `tests/contract/broadcasts/admin-eblast-rate-limits.test.ts` (T026a owns that file): **30 / 60 s per (tenant, actor)** on `POST` and `PATCH /api/admin/broadcasts/[id]/version`, `POST …/[id]/version/send` and `POST …/[id]/schedule`. Over `broadcastsRateLimiter`, **atomic check, never peek-then-act**, refused 429 `broadcast_rate_limit_exceeded` with `retryAfterSeconds` (contracts `admin-eblast-formatting-api.md` § Rate limits; spec § Roles). **The member `POST /api/broadcasts/[id]/cancel` bucket is NOT here — it is T081a** (round 4 M8): its RED extends `tests/contract/broadcasts/portal-eblast-decision.test.ts`, a file T067 creates in Phase 4, and it limits the route T081 widens in Phase 4, so pinning it from a Phase 3 task would put a RED test in a file that does not exist yet — RED: `tests/contract/broadcasts/admin-eblast-rate-limits.test.ts` "the 31st `PATCH …/version` in a minute → 429 with `Retry-After`" and "the 31st `…/version/send` in a minute → 429, with no version sent and no outbox row on the refused call".
- [ ] T063 [US1] The staff detail page `src/app/(staff)/admin/broadcasts/[id]/page.tsx`: stage header (stage, whose turn, time in stage, round, proposed vs confirmed time), the writing tool with the member's original beside it read-only, "Start formatted version" (flag-gated), "Send to member", "Confirm schedule", the standing **no-portal-user** and **de-allow-listed image** warnings, shadcn `Card` sections replacing the bare `rounded-md border` blocks at `:114,158`, and `manager` read-only with every action control **absent, not disabled** — RED: `tests/unit/broadcast/admin-eblast-detail-actions.test.tsx` "a manager sees no action control" + `tests/e2e/broadcasts/eblast-approval.spec.ts` "@eblast marketing formats and sends".
- [ ] T106a [US1] **The PR-2 half of the staff image route** (satisfies **US3-AS3** — tagged by phase, traced by requirement; `/speckit.analyze` H1; T106 shipped the PR-1 half): widen the accepted stage set of `POST /api/admin/broadcasts/[id]/images` in `src/app/api/admin/broadcasts/[id]/images/route.ts` from the PR-1 set `('draft','submitted')` to **`('draft','submitted','in_design')`**, so marketing can add an image or a banner to the formatted version they are working on — which is what US3-AS3 and FR-040 actually describe. `in_design` is a `0305` value and does not exist in PR-1, which is why this is not part of T106; the rules (5 MB, png/jpeg/webp/gif, SHA-256 dedup, fail-closed ClamAV, per-tenant source allowlist, `owner_kind='broadcast'`, the audit row with `related_member_id`) are unchanged from T106 — RED: `tests/contract/broadcasts/admin-eblast-images.test.ts` extended (T096 owns the file): "upload on an `in_design` broadcast → 201 and the image appears in the working copy" and "upload on `awaiting_member_approval` → 409 `stage_changed` (a sent version is read-only)".
- [ ] T064 [P] [US1] `src/components/broadcast/approval/schedule-confirm-dialog.tsx` — the member's proposal shown and **pre-selected** when still far enough in the future, the difference called out when marketing changes it, `finalFocus` back to its trigger — RED: `tests/unit/broadcast/schedule-confirm-dialog.test.tsx` "a past proposal is not pre-selected and the picker is required" + "focus returns to the trigger on close".
- [ ] T065 [US1] The three US1 dispatcher arms in `src/app/api/cron/outbox-dispatch/route.ts` `buildPayload` for `eblast_version_sent_member`, `eblast_member_decided_marketing` and `eblast_schedule_confirmed_member`. **Depends on T129a** — the five `@react-email` builders in `src/modules/broadcasts/infrastructure/email/broadcast-approval-emails.ts` that these three arms call: T129a carries its ID in Phase 7 but is **sequenced inside the Phase 3 block, before this task** (round 4 H4), because an arm cannot render a template that does not exist. `context_data` carries **ids and discriminators only**, the arm reads the rows at send time inside `runInTenant(asTenantContext(row.tenantId), …)` and renders there (the F114 precedent at `:415-542`). **`versionId` and `round` are nullable on `eblast_member_decided_marketing`**, because the same type carries a whole-E-Blast withdrawal raised from `submitted` or `draft`, where no version and no round exist; the arm must render from the broadcast alone in that case and MUST NOT throw — an arm that throws is indistinguishable from the missing-arm `default: null` and retries silently for ~16 h. Member rows carry the contact's `preferred_language`; staff rows carry the platform default. **FR-021b containment**: a staff rendering carries the subject, the member company, the stage and a link — and nothing else — RED: `tests/contract/broadcasts/eblast-notification-arms.test.ts` "every `notification_type` value has an arm (enumerated from the enum, with a positive control)", "a staff rendering contains none of the body, the member's reason, marketing's note or the send times — positive control fails when the reason is spliced back in", **"an `eblast_member_decided_marketing` row with `versionId: null` and `round: null` (a withdrawal from `submitted`) renders a complete email and does not throw"**, and — **closing SC-004** — "every hand-off enqueues its outbox row **inside the state-changing transaction**: rolling the tx back leaves zero outbox rows, and committing leaves exactly one per recipient with `created_at` inside the tx window" (the 5-minute budget is then the 1-minute dispatcher tick, recorded in `docs/observability.md` § 28 by T160 — enqueue-outside-the-tx is the only way the budget can be missed).
- [ ] T066 [P] [US1] `MarketingDirectoryPort` + `src/lib/broadcast-marketing-deps.ts`: `marketingRoles() = ROLES.filter(r => hasPermission(r, 'broadcasts.write'))` **minus** `['admin','super_admin']`, falling back to the admin tiers when that set has no active user (FR-021a). Derived from the **evaluator**, never from a literal `'marketing'` string and never from `ROLE_BUNDLES` (super-admin keys come from the evaluator's early return — "`ROLE_BUNDLES` is not the whole model") — RED: `tests/contract/broadcasts/marketing-roster.test.ts` "removing `broadcasts.write` from the marketing bundle changes the roster without touching this code" and "an empty roster increments `broadcasts_no_marketing_recipient_total`".

**Checkpoint**: US1 is exercisable end to end with the flag on; the delivered content provably equals the approved version.

---

## Phase 4: User Story 2 — The member asks for changes and marketing fixes them (Priority: P1) — **PR-2**

**Goal**: the way back. A reason is mandatory, the E-Blast returns to marketing with the feedback attached to the version it concerns, the cycle repeats, and an approval can be withdrawn before sending begins.

**Independent Test**: as the member, request changes with a comment on a formatted version; verify it returns to marketing with the comment; send a second version; approve it; verify the second version — not the first — is what is sent, and that the history shows both rounds.

### Tests first (write, observe RED)

- [ ] T067 [US2] A member requesting changes **without a reason** is refused with an announced field error (US2-AS1, FR-010) — RED: `tests/contract/broadcasts/portal-eblast-decision.test.ts` "blank reason on `changes_requested` → 422 `reason_required`" and `tests/unit/broadcast/member-sign-off-actions.test.tsx` "the reason field is `aria-invalid` and its error is announced".
- [ ] T068 [P] [US2] A reason outside its bounds is refused, on both sides of the one CHECK (FR-009, FR-010) — RED: `tests/contract/broadcasts/portal-eblast-decision.test.ts` "a 2,001-character reason → 422 `validation_error`" and "a 501-character approval note → 422 while 500 is accepted".
- [ ] T069 [P] [US2] Round 2 works and both rounds stay visible to both sides in order, and the **hand-off notification fires on each round** (US2-**AS2**, AS3, AS4, FR-011, FR-032, **FR-021** — "member requested changes → marketing" and "version sent → member" are two of the six hand-offs, and AS2 requires marketing to be notified with the feedback attached to the version it concerns; round 4 L6) — RED: `tests/integration/broadcasts/eblast-approval-rounds.test.ts` "after a change request and a second version, the thread shows both versions, both notes and the feedback attached to round 1, oldest first".
- [ ] T070 [P] [US2] Withdrawing an approval before sending returns the E-Blast to Changes requested, **cancels the confirmed schedule** and clears `approved_version_id` (US2-AS6, FR-015a) — RED: `tests/contract/broadcasts/portal-eblast-decision.test.ts` "`approval_withdrawn` from `approved` → stage `changes_requested`, `scheduled_for` NULL, `approved_version_id` NULL, `cancelled_schedule_at` in the audit payload".
- [ ] T071 [P] [US2] The cut-off is entry into `sending`, and not one stage earlier (FR-015, spec § Edge Cases) — RED: `tests/contract/broadcasts/portal-eblast-decision.test.ts` "withdraw while `sending` → 409 `sending_started`" and "withdraw while `approved`/Scheduled → 200".
- [ ] T072 [P] [US2] A stale version id and a raced decision are refused clearly (FR-033, spec § Edge Cases "act at the same moment") — RED: `tests/contract/broadcasts/portal-eblast-decision.test.ts` "an older `versionId` → 409 `stale_version` with the current version" and "approve twice → 409 `stage_changed` carrying the recorded decision".
- [ ] T073 [P] [US2] Owning-member and session rules (FR-013, spec § Tenant scope) — RED: `tests/integration/broadcasts/eblast-approval-cross-member-probe.test.ts` "member B cannot read or decide member A's E-Blast inside one tenant — 404 + `broadcast_cross_member_probe`" and `tests/contract/broadcasts/portal-eblast-decision.test.ts` "a staff session → 403 on `POST …/decision`, asserted directly".
- [ ] T074 [P] [US2] A **lapsed** member may still decide, and the expiry clock keeps running (spec § Edge Cases) — RED: `tests/contract/broadcasts/portal-eblast-decision.test.ts` "a lapsed member's approve → 200 (deciding is not a benefit action)".
- [ ] T075 [P] [US2] The E-Blast holds **one** allowance place through **every** in-progress stage however many rounds happened, and rejection, withdrawal and expiry free it (US2-AS5, FR-020, SC-007) — RED: `tests/integration/broadcasts/eblast-allowance-bucket.test.ts` "one member with a broadcast in each in-progress stage: the quota counter refuses the next submit at the plan limit, and `rejected`/`cancelled`/`expired_no_member_response` release it with `quota_year_consumed` still NULL".
- [ ] T076 [US2] Erasure reaches every version, every reason, every note, every image **and every notification about the E-Blast** (spec § Personal data, research R17) — RED: `tests/integration/broadcasts/eblast-erasure-reach.test.ts` "a member erased at Awaiting member approval: no non-sentinel value in either child table, the broadcast cancelled, every image row stamped, pending outbox rows cancelled, and a second run changes nothing".
- [ ] T077 [P] [US2] The member decision route is rate-limited per user with an atomic check, never a peek-then-act — RED: `tests/contract/broadcasts/portal-eblast-decision.test.ts` "the 61st decision in a minute → 429 with `Retry-After`".

### Implementation

- [ ] T078 [US2] `recordMemberDecision` use case in `src/modules/broadcasts/application/use-cases/approval/record-member-decision.ts` with its three arms — `approved` (set `approved_version_id`, transition to `member_approved`), `changes_requested` (transition to `changes_requested`, reset `member_reminder_stage = 0`) and `approval_withdrawn` (clear `approved_version_id` **and** `scheduled_for` via exemption E2, transition to `changes_requested`) — each in ONE `runInTenant` with a `FOR UPDATE` re-read, a stage re-check and throw-to-rollback, writing the decision row, stamping `stage_entered_at`, auditing with snake_case **`member_id`** (so the 0009 `last_activity_at` trigger fires) and `reason_length` but never the reason text, and enqueuing one `eblast_member_decided_marketing` row **per marketing recipient** — **this is FR-021's "member approved → marketing" and "member requested changes → marketing" hand-off, and US2-AS2's "marketing staff are notified"** (round 4 L6); the enqueue is unconditional and the flag lives at the drainer (T152a). **100 % branch pinned** — RED: T067, T068, T070, T071, T072, T074.
- [ ] T079 [US2] The `POST` handler in `src/app/api/broadcasts/[id]/decision/route.ts` — `requireMemberContext` **plus** the owning-member check; a miss on another member of the same tenant is **404 + `broadcast_cross_member_probe`**, an unknown or other-tenant id is 404 + `broadcast_cross_tenant_probe`, never 403 (no existence leak). Response carries the new `stage` and `whoseTurn` so the page needs no second fetch — RED: T073, T077.
- [ ] T080 [US2] Replace the literal `('submitted','approved')` at **both** `src/modules/broadcasts/infrastructure/db/drizzle-broadcasts-repo.ts:384` (`countMemberQuotaBucketsOnTx`, the allowance reservation, under the existing `pg_advisory_xact_lock('broadcasts-quota:…')` recheck) and `:1322` (`listInFlightOwnedByMember`, the erasure/cancel cascade) with `IN_PROGRESS_BROADCAST_STATUSES` — one constant, two sites, because FR-020 makes them definitionally the same set — RED: T075, T076.
- [ ] T081 [US2] Widen `domain/policies/cancel-cutoff-policy.ts:47,49` to `IN_PROGRESS_BROADCAST_STATUSES` and widen the existing `POST …/cancel` (member withdrawal) and `POST …/reject` (marketing, with a reason) staff routes to the same set, answering **409 `sending_started`** from `sending` onward. **Name `broadcasts.write` on `POST /api/admin/broadcasts/[id]/cancel`** in the same edit: it is a state-changing staff route that names no permission key in the contract's permission map today, which is exactly what `check:api-route-guard` exists to catch (`src/app/api/admin/broadcasts/[id]/cancel/route.ts`); the existing `sending`-with-batches operator arm is **not** extended to the member. **In the same edit, give both routes the staff write bucket — 30 / 60 s per (tenant, actor)** over `broadcastsRateLimiter`, atomic check, refused 429 `broadcast_rate_limit_exceeded` with `retryAfterSeconds` (round 4 M6): neither carries one today, and widening the accepted stage set of the only two unbucketed state-changing staff routes without adding it is exactly the gap `admin-eblast-formatting-api.md` § Rate limits exists to close. Reuses the existing `broadcast_rejected` / `broadcast_cancelled` audit events and notification types, with the stage it was rejected from added. **In the same transaction, stamp `deleted_at` on every `broadcast_images` row of that E-Blast and audit `broadcast_image_removed { related_member_id, owner_kind: 'broadcast', owner_id, image_id, blob_deleted: false, reason: 'withdrawn' | 'rejected' }`** — the two reasons `contracts/dashboard-and-notifications.md` § 2 and `data-model.md` § 4 declare and that nothing emitted (round 3 M4); the bytes go on T035's next sweep under the last-reference rule, which is what makes spec § Personal data's "not reachable" true for a withdrawn or rejected E-Blast (US2-AS5, FR-015) — RED: T071 + `tests/contract/broadcasts/admin-eblast-reject-widened.test.ts` "reject from `awaiting_member_approval` → 200; from `sending` → 409 `sending_started`" and "a reject stamps every image row and emits `broadcast_image_removed` with `reason: 'rejected'`; a member withdrawal emits `'withdrawn'`; an image whose hash is still referenced by a template is stamped but not deleted by the sweep" **and "the 31st staff reject in a minute → 429 `broadcast_rate_limit_exceeded` with `Retry-After`, with no stage change and no image row stamped; the 31st staff `…/cancel` likewise"**.
- [ ] T081a [US2] The **member write bucket on the widened portal withdrawal** — **60 / minute per (tenant, user)** on `POST /api/broadcasts/[id]/cancel`, the same new bucket as the member decision route (T077), since a withdrawal is the same class of member write. Over `broadcastsRateLimiter`, **atomic check, never peek-then-act**, refused 429 with `retryAfterSeconds` (`portal-eblast-approval-api.md` § Rate limits; spec § Roles). **Split out of T062a** (round 4 M8) because it pins a route T081 widens in this phase and its RED extends a file T067 creates in this phase, neither of which exists while Phase 3 runs — RED: `tests/contract/broadcasts/portal-eblast-decision.test.ts` (T067 owns the file) "the 61st `…/cancel` in a minute → 429 with `Retry-After`" and "the refused call leaves the stage unchanged and writes no decision row".
- [ ] T082 [US2] `BroadcastApprovalScrubPort` (`src/modules/broadcasts/application/ports/broadcast-approval-scrub-port.ts`, called from `src/modules/broadcasts/application/use-cases/scrub-broadcast-content-for-member.ts`) joining the **same atomic erasure transaction** as `scrubContentForMemberInTx` (`src/modules/broadcasts/infrastructure/db/drizzle-broadcasts-repo.ts:1362-1412`, which has no status predicate and therefore already covers the new stages): `subject`, `body_html`, `body_source`, `note_to_member` on every version and `reason` on every decision → `'[redacted]'` (rows kept — SC-002's proof must survive), every `broadcast_images` row stamped `deleted_at` and audited `broadcast_image_removed { …, reason: 'erasure' }`, and the member's pending `eblast_*` outbox rows cancelled in the same transaction. Blob bytes are deleted best-effort after commit and durably by T035's sweep — RED: T076.
- [ ] T083 [P] [US2] Add a `broadcast-versions.json` section, scoped to the requesting member's own E-Blasts and carrying the same `[redacted]` sentinels, to the member DSAR export (research R17) — RED: `tests/integration/broadcasts/eblast-erasure-reach.test.ts` "the DSAR archive of an erased member contains only sentinels in `broadcast-versions.json`".
- [ ] T084 [US2] `src/components/broadcast/approval/member-sign-off-actions.tsx` — Approve (optional note ≤ 500, confirmed in a dialog that states marketing will now confirm the send time and that **the content cannot change without a new approval**), Request changes (reason 1–2,000 required, `ReasonConfirmationDialog`, destructive tier), Withdraw approval and Withdraw E-Blast (both reason-confirmed), each with `finalFocus` back to its trigger; after a decision the page shows the new stage and **a way back to the E-Blast list** (FR-009, FR-010) — RED: T067 + `tests/unit/broadcast/member-sign-off-actions.test.tsx` "each dialog returns focus to its trigger".
- [ ] T085 [US2] `src/components/broadcast/approval/version-thread.tsx`, used by **both** sides — an ordered list with **a heading per round**, version first and its decision second, oldest → newest, navigable by keyboard and screen reader; the portal names authors only as **"you" / "the chamber"**, never a staff user (the F114 `organisation` precedent); an approve-as-submitted E-Blast shows the single "approved as submitted" entry instead (FR-032, FR-007) — RED: T069 + `tests/unit/broadcast/version-thread.test.tsx` "no staff name appears in the portal rendering".
- [ ] T086 [US2] The member sign-off page `src/app/(member)/portal/broadcasts/[id]/page.tsx` compare view: the latest **sent** version rendered through the preview route in an iframe **beside** the member's original on wide screens, marketing's note above it, the proposed send time; at phone width the **formatted version comes first** and the original is reachable **below it on the same page** — never a link away, never a tab that unmounts the decision controls (FR-008). An **unsent** version is never in the payload — RED: `tests/e2e/broadcasts/eblast-approval.spec.ts` "@eblast member approves on a 375 px viewport" and `tests/contract/broadcasts/portal-eblast-versions.test.ts` "an unsent version is absent from `GET …/versions`".
- [ ] T087 [P] [US2] The `GET` handler in `src/app/api/broadcasts/[id]/versions/route.ts` returning the broadcast summary (stage, whoseTurn, round, proposed/confirmed times, `approvedVersionId`, `stageEnteredAt`, `expiresAt`), the versions, the decisions and `approvedAsSubmitted` — `authoredBy` is `"member" | "organisation"` only — RED: T086's contract arm.
- [ ] T141a [US2] **The PR-2 half of the FR-049 widening** (satisfies **FR-049 / US6** — tagged by phase, traced by requirement; plan Amendment 5; T141 shipped the subject+body half in PR-1): add `stage`, `whoseTurn`, `round`, `proposedSendAt`, `confirmedSendAt` and `expiresAt` to `GET /api/broadcasts/[id]` in `src/app/api/broadcasts/[id]/route.ts`, and make the body shown on `src/app/(member)/portal/broadcasts/[id]/page.tsx` **the latest sent version while the E-Blast is awaiting the member**, falling back to the record's own content otherwise. Every field here reads a `0305` column (`proposed_send_at`, `current_round`, `stage_entered_at`), the `broadcast_versions` table, or T052's `stageOf`/`turnOf` — none of which exists in PR-1, which is why this is not part of T141 — RED: `tests/contract/broadcasts/portal-eblast-detail-workflow-fields.test.ts` "awaiting the member → the body is the latest **sent** version and `whoseTurn` is `member`" and "in `draft`/`sent` → the body is the record's own content and `whoseTurn` is null".
- [ ] T086a [US2] **The PR-2 half of the FR-051 nine-screen pass** (satisfies **FR-051 / US6** — tagged by phase, traced by requirement; plan Amendment 4): run `docs/ux-standards.md` § 15 by hand **and** the axe scan at 320 px on the **three screens PR-2 builds or rebuilds** — the staff detail/format surface (`src/app/(staff)/admin/broadcasts/[id]/page.tsx`, T063), which PR-1 never scanned; the **sign-off compare view** of `src/app/(member)/portal/broadcasts/[id]/page.tsx` (T086), which PR-1 scanned in its body-only shape and PR-2 changes; and — **since the former PR-3 folded into PR-2** — the **staff queue** `src/app/(staff)/admin/broadcasts/page.tsx` with `src/components/broadcast/admin/{queue-filters,queue-table,queue-table-client,queue-card-list}.tsx`, which T116–T120 rebuild with stage chips, counts, six new columns, the stalled badge and the relabelled vocabulary (round 3 H4: FR-051 takes the pass in **every** delivery that changes a screen, and PR-1's pass was taken on the pre-rebuild queue). Identical bar: zero serious or critical, shimmer skeleton matching the real page, `finalFocus` on every dialog, keyboard order, theming, and the SV/TH chip strip not reflowing at 320 px. **It is the LAST UI task of PR-2** — it runs after Phase 6, not only after Phase 4 — RED: `tests/e2e/broadcasts/eblast-a11y.spec.ts` "@a11y axe at 320 px on the portal sign-off, the staff format screen and the rebuilt staff queue", **always `--workers=1`**, in ≤ 10-minute foreground chunks.

**Checkpoint**: the round trip closes. US1 + US2 together are the shippable approval round, and both approval screens have taken the FR-051 pass.

---

## Phase 5: User Story 3 — Marketing has a real writing tool (Priority: P1) — **PR-1**

**Goal**: a toolbar that offers everything the platform keeps, required alt text, design blocks, a real preview at two widths, and a test copy — with nothing silently stripped between the editor and the delivered email.

**Independent Test**: as marketing, format a submitted E-Blast using a heading, a banner image, a paragraph, a call-to-action button and the chamber logo; preview it at desktop and phone width; send a test copy; verify the test copy, the preview and the delivered email are identical and that no element was stripped.

### Tests first (write, observe RED)

- [ ] T088 [US3] Every formatting the platform keeps has a **visible control**, and nothing typeable by shortcut or paste produces content the platform later strips (US3-AS1, FR-038) — RED: `tests/unit/broadcast/tiptap-toolbar-coverage.test.tsx` "the toolbar offers exactly the constructs the shared policy keeps (H2, H3, quote, divider, bullet, ordered, bold, underline, link, image, CTA, banner) and no more" and "`# `, `> `, `---`, ` ``` `, `~~strike~~` cannot produce a node the server sanitiser removes".
- [ ] T089 [P] [US3] An image cannot be inserted without a 1–125-character description, and an empty one is an **announced** field error (US3-AS2, FR-040) — RED: `tests/unit/broadcast/image-alt-dialog.test.tsx` "insert is refused with an announced error until a description of 1–125 characters is given".
- [ ] T090 [P] [US3] A CTA button renders in the chamber's brand colour in the preview **and** in the delivered email, and no colour or font control exists anywhere (US3-AS4, FR-041, FR-041c) — RED: `tests/unit/broadcasts/infrastructure/render-broadcast-html-brand.test.ts` "the CTA cell carries the tenant's `brand_primary_color`, and the platform default `#10487a` when none is set".
- [ ] T091 [P] [US3] The logo appears in the header when one is on file and the chamber name otherwise (US3-AS5, FR-041a), rendered by `src/modules/broadcasts/infrastructure/resend/email-template.ts` — RED: `tests/unit/broadcasts/infrastructure/render-broadcast-html-brand.test.ts` (T031's file), extended: "no logo on file → the chamber name, and the hint tells a user who cannot fix it to **ask an administrator**".
- [ ] T092 [P] [US3] The inline preview shows a translated empty state rather than a blank box (US3-AS6, FR-043) — RED: `tests/unit/broadcast/preview-pane-empty-state.test.tsx` "an empty message renders the translated empty-state line, not an empty prose div".
- [ ] T093 [P] [US3] The Preview dialog shows the complete email at both widths and returns focus to its trigger (US3-AS7, FR-043) — RED: `tests/unit/broadcast/preview-dialog.test.tsx` "desktop 600 px ⇄ phone 375 px, header + body + footer with unsubscribe, `finalFocus` back to the Preview button, reduced motion respected".
- [ ] T094 [P] [US3] Italic is not offered when the interface language is Thai, while italic **content** from a paste or a template is kept as-is (US3-AS8, FR-044) — RED: `tests/unit/broadcast/tiptap-toolbar-locale.test.tsx` "the italic control is absent under `th`" and "`<em>` in the body survives the Thai render unchanged".
- [ ] T095 [P] [US3] A user who saved and changed nothing is not warned on leaving (US3-AS9, FR-045) — RED: `tests/unit/broadcast/compose-dirty-state.test.tsx` "after a successful draft save with no further edit, no beforeunload warning fires" (today the guard compares against immutable initial props, `compose-form.tsx:203-217`).
- [ ] T096 [P] [US3] A staff user can add an image to a **staff-owned E-Blast draft** under the identical rules, and nowhere else (US3-AS3 **for the compose-on-behalf half**, FR-040). **The PR-1 acceptance stage set is `('draft','submitted')`** — `in_design` is a `0305` value that does not exist in PR-1, so the formatting half of US3-AS3 is satisfied in PR-2 by **T106a** (`/speckit.analyze` H1; a PR-1 route gated on `in_design` could only ever 409 and its success path could never go green) — RED: `tests/contract/broadcasts/admin-eblast-images.test.ts` "upload on a staff-owned `draft` → 201, the row and the audit are written; on a `submitted` broadcast → 201 (the proxy author may still illustrate it); on a **closed** one → 409; on another tenant's → 404 + probe audit; an infected file → 422 with nothing stored". **T096 owns this file**; T106a extends it rather than creating it.
- [x] T097 [P] [US3] A test copy goes only to the requester's own address, marked as a test, through the identical pipeline, changing nothing (FR-037) — RED: `tests/contract/broadcasts/eblast-test-copy.test.ts` "a body-supplied `to` is ignored and the session address is used", "the subject carries the localised `[Test]` prefix", "no stage, version or allowance changes", "the 11th call in an hour → 429 with `Retry-After`", "four CTA blocks → 422 `too_many_cta`".
- [ ] T046a **[PR-1]** [US3] The **PR-1 staff role matrix** on the three staff routes PR-1 ships, which T046 would otherwise not pin until PR-2 (`/speckit.analyze` round 3 M6): `POST /api/admin/broadcasts/[id]/images` (T106), `POST /api/admin/broadcasts/templates/[id]/images` (T107) and `POST /api/admin/broadcasts/test-copy` (T105) — RED: `tests/contract/broadcasts/admin-eblast-pr1-rbac.test.ts` "`manager` → 403 on all three (write routes, read-only role)", "`marketing` → 200 on all three", "a member session → 403 on all three", and "each route names its permission key so `pnpm check:api-route-guard` passes" (`broadcasts.write` on every one, including the admin test copy).

### Implementation

- [ ] T098 [US3] Rebuild `src/components/broadcast/tiptap-toolbar.tsx`: H2 and H3 only (the subject is the email's title, so H1 is never offered), quote, divider, bulleted and numbered lists, bold, underline, link, image, CTA, banner — with the APG **roving-tabindex** pattern, arrow keys plus **Home/End**, a visible focus state, and at 320 px it **wraps onto further rows with no overflow menu** (FR-038, FR-048; today it is `role="toolbar"` with 7+ tab stops at `:147-151`) — RED: T088 + `tests/unit/broadcast/tiptap-toolbar-a11y.test.tsx` "one tab stop; arrows move; Home/End jump to the ends".
- [ ] T099 [P] [US3] The alt-text insert dialog in `src/components/broadcast/tiptap-editor.tsx` — a **labelled** description field, 1–125 characters, collected **before** the image node can exist (so it is never an upload field) and carried into the sent email as `alt` (FR-040) — RED: T089.
- [ ] T100 [P] [US3] The link dialog refuses a scheme outside `http` / `https` / `mailto` **in the dialog**, with an inline message — never accepted and stripped later; `zod`'s `.url()` accepts `javascript:`, so the allow-list is asserted here and again at the sanitiser (FR-038) — RED: `tests/unit/broadcast/link-dialog-scheme.test.tsx` "`javascript:alert(1)` and `ftp://…` are refused in the dialog".
- [ ] T101 [P] [US3] Paste handling: content outside the shared policy is dropped **at paste time** and the user is told **once per editing session** in a non-blocking toast — not a dialog, not repeated per paste (FR-038) — RED: `tests/unit/broadcast/paste-notice.test.tsx` "a second paste in the same session shows no second notice".
- [ ] T102 [US3] The two Tiptap custom nodes on `@tiptap/core` (no new package): `ctaButton` serialising to `<a data-eb="cta" href="…">text</a>` in `src/modules/broadcasts/infrastructure/tiptap-cta-button-config.ts`, and `bannerImage` serialising to `<img data-eb="banner" src="…" alt="…">` in `…/tiptap-banner-image-config.ts`. The user supplies `href`, the text, `src` and `alt` and nothing else, because those are the only attributes the shared config allows — RED: T009, T090 + `tests/unit/broadcasts/infrastructure/tiptap-design-block-nodes.test.ts`.
- [ ] T103 [US3] Rewrite `src/components/broadcast/preview-pane.tsx` to render the **real email** returned by the preview route into an `<iframe srcdoc>` (no `src`, so no network; sandboxed) — replacing today's `dangerouslySetInnerHTML` of the bare body at `:115` — debounced ~400 ms on the deferred body, with the translated empty state (FR-043, research R11) — RED: T092.
- [ ] T104 [P] [US3] New `src/components/broadcast/preview-dialog.tsx` — desktop **600 px** and phone **375 px**, `finalFocus` back to the trigger, `prefers-reduced-motion` respected on open/close; reused on the member compare screen (FR-043) — RED: T093.
- [x] T105 [US3] `sendTestCopy` use case in `src/modules/broadcasts/application/use-cases/send-test-copy.ts` + `TestCopyMailerPort` in `src/modules/broadcasts/application/ports/test-copy-mailer-port.ts`, implemented by a broadcasts Infrastructure adapter over the **shared transactional sender** `emailSender` (`src/modules/auth/infrastructure/email/resend-client.ts:148`, already imported by `src/app/api/cron/outbox-dispatch/route.ts:54` — **V4 resolved, see T005**): synchronous and non-durable so the result is reported in-band, through the **transactional** Resend surface and never the Broadcasts surface (a test must not enter the marketing suppression list or reputation pool), with **no outbox row and no sixth `notification_type`** (the withdrawn fallback would have needed an enum value `0304` does not carry) + the `POST` handlers in `src/app/api/broadcasts/test-copy/route.ts` (`requireMemberContext`) and `src/app/api/admin/broadcasts/test-copy/route.ts` (**`requireApiPermission(request, 'broadcasts.write')`** — it named no key, which is exactly what `check:api-route-guard` exists to catch; round 3 M6). Recipient resolved **server-side** from the session; localised `[Test]` subject prefix; the identical sanitiser, `applyDesignBlocks`, brand header and footer; **10 per user per hour**; audit `broadcast_test_copy_sent { related_member_id, broadcast_id | null, version_id | null, recipient_hash }` with `actor_role` = the session role (`member` for a portal user). **The member key is `related_member_id` even for a portal user** — `contracts/dashboard-and-notifications.md` § 2 is the single source of truth and lists it there deliberately: a test copy to one's own inbox is not member activity on the E-Blast, so it must NOT move `last_activity_at` via the 0009 trigger (round 3 M5) — RED: T097.
- [ ] T106 [P] [US3] The `POST` handler in `src/app/api/admin/broadcasts/[id]/images/route.ts` (`broadcasts.write`, **stage in `('draft','submitted')` — the PR-1 set, because `in_design` arrives with `0305`; T106a widens it in PR-2**, same tenant) sharing `src/modules/broadcasts/application/use-cases/upload-inline-image.ts` so the 5 MB cap, the png/jpeg/webp/gif MIME list, the SHA-256 dedup, the **fail-closed** ClamAV scan and the per-tenant source-allowlist auto-seed apply identically to a staff image, and recording `owner_kind='broadcast'` (FR-040) — RED: T096.
- [ ] T107 [P] [US3] `POST /api/admin/broadcasts/templates/[id]/images/route.ts` (`broadcasts.write`) recording `owner_kind='template'`; starting an E-Blast from a template carries its images **by reference** (today's snapshot semantics, unchanged) and a later template edit or delete does not change E-Blasts already started from it — the last-reference rule is what keeps the member's draft working. Blocks and links from a template are the member's own content from that moment: editable, deletable, **no per-block authorship** and no "from template" marking anywhere (FR-046a) — RED: `tests/contract/broadcasts/admin-eblast-template-images.test.ts` "a template image is recorded under `owner_kind='template'`; deleting the template leaves a draft started from it working".
- [ ] T108 [US3] Let the **template editor** carry every block the tool offers (`src/app/(staff)/admin/broadcasts/templates/**`, `template-form.tsx` today `imagesEnabled={false}`) and count template use so adoption is measurable — today the shipped `?template=` path does not increment `startedFromCount` and the counting route has no UI caller (FR-046, FR-046a, exploration § B) — RED: `tests/contract/broadcasts/template-start-count.test.ts` "starting from a template through the shipped path increments `startedFromCount` exactly once".

**Checkpoint**: US3 complete. With T008 green, nothing a user can add is silently removed between editor, preview, test copy and delivered email.

---

## Phase 6: User Story 4 — Marketing tracks every E-Blast on one dashboard (Priority: P2) — **PR-2**

**Goal**: the existing queue — not a second list — gains per-stage counts, whose turn, time in stage, round, both send times, a stalled flag, an upcoming-sends preset and delivery results.

**Independent Test**: seed E-Blasts across all stages; open the dashboard as a marketing user and verify counts per stage match, "whose turn" is correct for each row, stalled items are flagged, upcoming sends are listed in time order, and sent items show delivery results.

### Tests first (write, observe RED)

- [ ] T109 [US4] Per-stage counts match the seeded rows and selecting a stage filters the list, announced through the list's **one existing** `role="status"` region and never a second one (US4-AS1, FR-025) — RED: `tests/contract/broadcasts/eblast-dashboard-counts.test.ts` "counts match seeded rows per stage" + `tests/unit/broadcast/queue-live-region-single.test.tsx` "exactly one live region is mounted after the change".
- [ ] T110 [P] [US4] Every row carries member, subject, stage, whose turn, time in stage, round, proposed and confirmed send times and last activity — with `whoseTurn` **null** for `draft`, `approved`, `sending` and all five closed statuses, and no `"system"` value ever produced (US4-AS2, FR-026) — RED: `tests/contract/broadcasts/eblast-dashboard-rows.test.ts` "whoseTurn is correct for every status", "the round does not move on a withdrawn approval and does move on the next send", **"`expired_no_member_response` groups under Closed, never under In review — it is in `TERMINAL_BROADCAST_STATUSES` and its `whoseTurn` is null"** (`/speckit.analyze` H4: only **four** of the five new statuses are in-review), and — **closing FR-031** — "a **proxy-submitted** E-Blast (staff submitted on the member's behalf) appears in the list with the same columns as a member-submitted one", so the dashboard provably covers every E-Blast that goes through the platform.
- [ ] T111 [P] [US4] A marketing-held row older than **48 h** (the review target) and a member-held row older than **3 days** (the first-reminder threshold) are both flagged stalled, by an **icon and a text label**, never colour alone, with the label in the accessible name — while a marketing-held row at **30 h** (past the 24 h amber level, short of 48 h) is **not** flagged, **not** counted and **not** announced as stalled: amber stays the pre-warning it already is (US4-AS3, FR-027) — RED: `tests/unit/broadcast/stalled-badge.test.tsx` "the accessible name reads 'Stalled — 3 days'; the icon is `aria-hidden`; no `aria-label` on a role-less span" and "a 30 h marketing-held row renders amber but carries no stalled label and is absent from the stalled count".
- [ ] T112 [P] [US4] Upcoming sends list in send-time order (US4-AS4, FR-028) and sent rows show recipients / delivered / bounced / complained (US4-AS5, FR-029), with **no contact-level data** anywhere in the query (FR-036) — RED: `tests/contract/broadcasts/eblast-dashboard-presets.test.ts` "the upcoming preset orders by `scheduled_for`" and "a sent row carries the four counts and no recipient address".
- [ ] T113 [P] [US4] A `manager` sees everything and can act on nothing (US4-AS6) — RED: `tests/contract/broadcasts/eblast-dashboard-manager.test.ts` "the full list is returned and every action control is absent, not disabled".
- [ ] T114 [P] [US4] Counts and the first page render within 2 s at 1,000 E-Blasts, served by the new index (SC-008) — RED: `tests/integration/broadcasts/eblast-dashboard-pagination.test.ts` "1,000 seeded rows across all stages: the budget holds and `broadcasts_stage_queue_idx` appears in `EXPLAIN`" — **assert it in a single-file run**; the pre-push hook exports `INTEGRATION_FOLDER_RUN=1` and the test must **report rather than assert** when it sees that, because 100+ files on one Neon compute contend.
- [ ] T115 [P] [US4] At phone width the card shows member, subject, stage, whose turn and time in stage only, with no horizontal scroll and no hidden-column menu (FR-026) — RED: `tests/unit/broadcast/queue-card-list-columns.test.tsx` "round and both send times are absent below md".

### Implementation

- [ ] T116 [US4] Add the **four in-progress** new statuses — `in_design`, `awaiting_member_approval`, `changes_requested`, `member_approved` — to the hand-listed `IN_REVIEW_STATUSES` in `src/components/broadcast/admin/queue-filters.tsx:64-69` (otherwise they land under "Closed"), and add the **fifth**, `expired_no_member_response`, to `TERMINAL_STATUSES` at `:74-75` instead — it is a closed outcome in `TERMINAL_BROADCAST_STATUSES` with `turnOf` null (data-model § 8.1b), so filing it under "In review" would offer marketing a row nobody can act on (`/speckit.analyze` H4). The chip strip and the loading skeleton's chip count (`loading.tsx:65`, from `OFFERED_BROADCAST_STATUSES.length`) then follow automatically. Add the per-chip **count**, and offer a new-stage chip **only when the flag is on or the tenant has ≥ 1 row in it** — never offer a filter that can only return zero rows (the module's own retired-status rule) — RED: T109 **and T151, which must be written and observed RED before this task is implemented** (T151 is the flag-visibility test for exactly this rule; it sits in Phase 9 by story, but it is this task's RED — Constitution II, see § Phase dependencies).
- [ ] T117 [US4] Add the **Stage**, **Whose turn**, **Time in stage**, **Round**, **Proposed**, **Confirmed** and **Last activity** columns to `src/components/broadcast/admin/queue-table.tsx` / `src/components/broadcast/admin/queue-table-client.tsx` / `src/components/broadcast/admin/queue-card-list.tsx`, re-basing the existing `ageBadge` struct (`src/components/broadcast/admin/queue-table.tsx:110-121`) on `stage_entered_at` and applying it to **every** waiting stage rather than only `submitted`. Preserve the shared TanStack instance, the fixed-bottom bulk bar, the single announcer at `src/components/broadcast/admin/queue-table-client.tsx:439-447`, `finalFocus` on every dialog and the manager read-only mode — RED: T110, T115.
- [ ] T118 [P] [US4] The stalled comparison — one comparison against `stage_entered_at`: marketing-held stages at **`SLA_RED_HOURS` (48 h) only**, member-held at the **3-day** reminder threshold — using the same badge struct in `src/components/broadcast/admin/queue-table.tsx` and `src/components/broadcast/admin/queue-card-list.tsx` so label and variant cannot drift apart. **`SLA_AMBER_HOURS` (24 h, `src/components/broadcast/admin/queue-table.tsx:97-98`) keeps rendering as today's pre-warning and is NOT stalled**: it never enters the stalled count, never carries the "Stalled" label and is never announced as one — FR-027 names 48 h and 3 days as the whole of "stalled", and quietly promoting amber would flag roughly every row that arrived yesterday (FR-027) — RED: T111.
- [ ] T119 [P] [US4] The **Upcoming sends** filter preset (`?status=approved&sort=scheduled_for&from=now`) in `src/components/broadcast/admin/queue-filters.tsx` (parsed in `src/app/(staff)/admin/broadcasts/page.tsx` and `src/app/api/admin/broadcasts/route.ts`) and delivery counts on sent rows from the existing `broadcast_deliveries` aggregate, rendered in `src/components/broadcast/admin/queue-table.tsx`; the URL stays the source of truth and `status_all=1` sentinel semantics are unchanged (FR-028, FR-029, FR-030) — RED: T112.
- [ ] T120 [US4] **Sequenced FIRST in PR-2, at its head, beside T121 and T122** (round 4 M1 — it keeps its ID in Phase 6 by story, but T063, T084, T085 and T141a all render `stageOf(status)` and would read raw key paths without it; ship order is not file order). The **stage-vocabulary relabel** (plan Amendment 1): `approved` reads **"Scheduled"** in **both** namespaces — `admin.broadcasts.queue.status` (en.json:3214-3225) and `portal.broadcasts.list.status` (6014-6025) — plus the **five new stage labels**, in EN, TH and SV. It ships in PR-2, not PR-1, so live copy does not change before the stages it belongs to exist. **Write the five new labels before T063/T084/T085/T141a are demoed or scanned**: those PR-2 screens render `stageOf(status)` and next-intl does not throw on a missing key, so without them the staff header, the thread and the portal read raw key paths and T086a would scan untranslated chrome (round 3 C2 — closed by the PR-3→PR-2 merge, but the ordering inside the PR still matters). **SV runs up to +28 %**: the label must fit its chip, truncating with a `title` rather than reflowing the strip (FR-019, FR-025, **US2-AS2** — "the E-Blast shows as 'Changes requested by member'" is a label, so AS2 is unsatisfiable until these exist — and **US7-AS2**, which requires every stage to be exercisable in the UAT walkthrough; round 4 L6) — RED: `pnpm check:i18n` plus `tests/unit/broadcast/dead-i18n-keys.test.ts` over the two widened `status` namespaces (T006a's shape — a key present in EN and missing in TH or SV fails; round 5 A2: the chip-strip e2e cannot go red at the head of PR-2 because `eblast-approval.spec.ts` is T063's file and the five chips are T116's). The 320 px "@i18n the SV and TH chip strip does not reflow" case is T156's re-run after T116 lands.
- [ ] T121 [P] [US4] **Sequenced FIRST in PR-2, with T120 and T122** (round 4 C1 — a registration list is only useful ahead of its emitters, and `contracts/dashboard-and-notifications.md` § 4.1 is that list). Four gauges on the **existing** broadcasts half of `/api/internal/metrics/broadcasts-gauges` — no new cron, no new transaction, same `observed` tenant set, same zero-fill convention (a count reports 0, never its last value): `broadcasts_awaiting_member_approval_count`, `broadcasts_awaiting_member_oldest_age_seconds`, `broadcasts_changes_requested_count`, `broadcasts_marketing_turn_count`, each labelled `tenant`. **Leave `broadcasts_queue_pending` on `('submitted','approved')`** — its alert threshold (`docs/observability.md` § 22.3, line 1382) is calibrated to it and widening it silently would move an alert nobody re-tuned — RED: `tests/contract/broadcasts/eblast-gauges.test.ts` "the four gauges zero-fill over the observed tenant set and `queue_pending` is unchanged".
- [ ] T122 [P] [US4] **Sequenced FIRST in PR-2, with T120 and T121 — it may NOT wait for Phase 6** (round 4 C1): its emitters are T058 and T059 (Ph3), T066 (Ph3), T078 (Ph4) and T130 (Ph7), all of which run earlier in the PR-2 order, and § 4.2 plus T122a both state that an emit against an unregistered field does not typecheck. Keeping the ID in Phase 6 by story is bookkeeping; building it there would be a broken build for three phases. The **five remaining counters and the one remaining histogram** of the six-counter / two-histogram inventory (`broadcasts_preview_rendered_total` and `broadcasts_preview_render_ms` were registered in PR-1 by **T122a**, because T032 emits them there — round 3 C1): `broadcasts_version_sent_total{tenant,round}` (T059), `broadcasts_member_decision_total{tenant,decision}` (T078), `broadcasts_approval_expired_total{tenant}` (T130), `broadcasts_no_marketing_recipient_total{tenant}` (T066), **`broadcasts_version_saved_total{tenant}`** (T058's save path — named in the admin contract but missing from the metric inventory, so it would have shipped unregistered; `/speckit.analyze` M2) and `broadcasts_member_decide_ms` in `src/lib/metrics.ts` — six registrations here (five counters + one histogram); six counters and two histograms in all, T122a having registered the preview pair in PR-1 — plus the OTel spans `broadcasts.version.send`, `broadcasts.member.decide`, `broadcasts.schedule.confirm` with attributes limited to ids, stage and round (`broadcasts.preview.render` came with T122a) — **never** values — RED: `tests/unit/broadcasts/application/eblast-metrics.test.ts` "each use case emits its counter exactly once per action".

**Checkpoint**: one list answers "what is going out, whose turn is it, and what is stuck".

---

## Phase 7: User Story 5 — Nobody has to poll: hand-off notifications and reminders (Priority: P2) — **PR-2**

**Goal**: marketing is told when a member submits (today they are not), the member is reminded on day 3 and day 7, both sides are warned on day 23, the E-Blast closes on day 30, and nothing is ever auto-approved.

**Independent Test**: submit an E-Blast and verify marketing staff are notified; leave an E-Blast awaiting member approval past the reminder threshold and verify exactly one reminder per threshold is sent and nothing is auto-approved.

### Tests first (write, observe RED)

- [ ] T123 [US5] Every marketing-role user is notified on submit, or the admins when the tenant has none, and an empty roster pages (US5-AS1, FR-021a) — RED: `tests/integration/broadcasts/eblast-submit-notifies-marketing.test.ts` "with a marketing user: one row each; with none: the admins; with neither: nothing sent, `broadcasts_no_marketing_recipient_total` incremented".
- [ ] T124 [P] [US5] Exactly one reminder per threshold across a 40-day simulated clock, and a new version **resets** the clock (US5-AS2, FR-022, FR-022a) — RED: `tests/contract/broadcasts/eblast-approval-lifecycle-cron.test.ts` "day 3 → one `reminder_day3`; day 7 → one `reminder_day7`; day 23 → one warning **to both sides**; running twice the same day changes nothing; sending a new version resets `member_reminder_stage` to 0 and restarts the clock" (clock injected through `ClockPort`).
- [ ] T125 [P] [US5] Nothing is **ever** auto-approved, and expiry exists on exactly one `from` state (US5-AS3, FR-014, FR-022a) — RED: `tests/contract/broadcasts/eblast-approval-lifecycle-cron.test.ts` "a row waiting 400 days produces exactly one expiry and zero approvals" and "a row parked at `member_approved` or `approved` for 400 days is untouched".
- [ ] T126 [P] [US5] Expiry frees the allowance place and cannot be reopened (FR-022a, SC-007) — RED: `tests/integration/broadcasts/eblast-allowance-bucket.test.ts` "after `expired_no_member_response`, `quota_year_consumed` is still NULL and the member may submit again" + `tests/integration/broadcasts/eblast-state-machine-edges.test.ts` "`expired_no_member_response` has no outgoing edge".
- [ ] T127 [P] [US5] Marketing sees the waiting count from anywhere in the staff portal (US5-AS4, FR-023) — RED: `tests/unit/broadcast/nav-marketing-count.test.tsx` "the badge counts the marketing-turn set and is hidden while the flag is off **and** no row is in a new stage".

### Implementation

- [ ] T128 [P] [US5] The reminder/expiry schedule policy in `src/modules/broadcasts/domain/approval/approval-schedule-policy.ts`: `nextReminder(stageEnteredAt, now, reminderStage) → 'day3'|'day7'|'day23'|'expire'|null`, driven by the **counter** rather than date arithmetic so "exactly one per threshold" holds across rounds — RED: `tests/unit/broadcasts/domain/approval-schedule-policy.test.ts` "each stage advances at most once per threshold".
- [ ] T129 [US5] Enqueue `eblast_submitted_marketing` **one row per marketing recipient** on the existing submit transaction in `src/modules/broadcasts/application/use-cases/submit-broadcast.ts` (and the proxy path `src/modules/broadcasts/application/use-cases/proxy-submit-broadcast.ts`), and add its `buildPayload` arm in `src/app/api/cron/outbox-dispatch/route.ts` — the staff rendering carries the subject, the member company, the stage and a link, and **nothing else** (FR-021b, and today nobody is emailed on submit at all). The enqueue happens **inside** the submit transaction, never after it — that is what makes SC-004's 5-minute budget reduce to the 1-minute dispatcher tick — and it is **unconditional**: no env-var branch inside a transaction that must roll back atomically. **The flag lives at the drainer instead (T152a, round 4 H2)**: submit is an existing unflagged action, so an unconditional *drain* would start emailing marketing on every member submission the moment PR-2 deployed; with `FEATURE_EBLAST_MEMBER_APPROVAL` off the row is written, skipped by the dispatcher and delivered on the first tick after the flip, which is how FR-034's "behave as today" stays true here — RED: T123 + T065's containment **and** enqueue-in-tx assertions. T149a's "enqueued, not delivered" arm is T152a's RED, written in Phase 9 — it *verifies* this task's enqueue stays unconditional but is not its RED (round 5 A5).
- [ ] T129a [US5] **Sequenced inside the Phase 3 block, before T065's dispatcher arms** (round 4 H4 — it keeps its ID here by story, but Phase 3's arms call it, so it cannot wait for Phase 7; ship order is not file order). The **five email templates × three locales** in `src/modules/broadcasts/infrastructure/email/broadcast-approval-emails.ts` (plan § Project Structure; one builder per `notification_type`: `eblast_submitted_marketing`, `eblast_member_decided_marketing`, `eblast_version_sent_member`, `eblast_schedule_confirmed_member`, `eblast_approval_lifecycle` with its four `kind` bodies), built with `@react-email/components` like the existing F7/F114 builders and called from T065's and T131's dispatcher arms — **staff** bodies carry only subject + member company + stage + link (FR-021b), **member** bodies carry what changed, who acted, both send times when they differ, the day 3 / 7 / 23 / 30 timeline on the "version ready" body, and a link back. Locale is the contact's `preferred_language` for member rows and the platform default for staff rows (FR-024); keys live under the namespaces T006 created — **no italic in the Thai copy** — RED: `tests/unit/broadcasts/infrastructure/broadcast-approval-emails.test.ts` "every builder renders in en, th and sv with no missing key and no Latin-only fallback in `th`" and "a staff body contains none of the body, the member's reason, marketing's note or the send times — positive control fails when the reason is spliced in".
- [ ] T130 [US5] `expireStaleMemberApprovals` use case + steps 2 and 3 of the second block in `src/app/api/cron/broadcasts/prune-expired-drafts/route.ts` (the block T035 created): reminders and the day-23 warning, then the day-30 transition `awaiting_member_approval → expired_no_member_response` in one `runInTenant` per row, stamping `member_expiry_notified_at`. **All three audit emit sites live here**, each with `actor_role: 'system'`: `broadcast_approval_reminder_sent { …, reminder: 'day3' | 'day7' }` (the key is `reminder`, as § 2 spells it — round 5 A4) on each reminder, `broadcast_approval_expiry_warned { …, days_waiting }` on the day-23 warning, and `broadcast_approval_expired { …, days_waiting, allowance_released: true }` on the close — T050's RED requires a real emit site for every tuple value, and naming only the third would leave two values added ahead of their emitters. Enqueue `expired_day30` to both sides. **The route keeps the existing `CRON_SECRET` bearer check** that guards every job in `src/app/api/cron/**` — the new block joins an already-guarded handler and must not introduce an unauthenticated path to it. Scans select on `status = 'awaiting_member_approval'` **and nothing else**; ≤ 200 rows per kind per tick, oldest first; its own transaction, its own `SET LOCAL statement_timeout`, its own try/catch and its own `approvalLifecycleOk` flag, with a 500 only at the end so a fault in one half never drops the other — RED: T124, T125, T126.
- [ ] T131 [P] [US5] The `eblast_approval_lifecycle` dispatcher arm rendering `kind ∈ reminder_day3 | reminder_day7 | expiry_warning_day23 | expired_day30` for **both** audiences — the staff rendering obeys the four-field rule, the member rendering restates the remaining timeline — and the `eblast_version_sent_member` body stating the **full day 3 / 7 / 23 / 30 timeline** at the moment the clock starts (FR-021b, FR-022a) — RED: `tests/contract/broadcasts/eblast-notification-arms.test.ts` "the member 'version ready' rendering states day 3, day 7, day 23 and day 30" and "`eblast_schedule_confirmed_member` carries both times and the 'not the time you proposed' line only when they differ".
- [ ] T132 [US5] The nav waiting badge in `src/config/nav.ts` + the staff shell — the marketing-turn set counted by the **same Domain predicate** as the gauge, as a live indexed `count(*)` at render (not the cron snapshot, so it is correct immediately — the F114 `NeedsAttentionList` precedent), visible to every role holding `broadcasts.read` including `manager`. The existing `drizzle-broadcast-approval-counter.ts:24` (`status = 'submitted'`) becomes that set — RED: T127.

**Checkpoint**: a two-sided flow that cannot stall silently.

---

## Phase 8: User Story 6 — Every E-Blast screen meets the platform's standard (Priority: P2) — **PR-1**

**Goal**: the defects the audit found are gone before SweCham tests, so the trial tests the feature and not the defects.

**Independent Test**: run the platform's UX checklist and an automated accessibility scan on every E-Blast screen; type into the compose form, pick a template and confirm nothing is lost.

### Tests first (write, observe RED)

- [ ] T133 [US6] Picking a template while the subject or message is non-empty asks for confirmation before replacing anything (US6-AS1, FR-046, **SC-012** — "zero cases of typed content lost without a confirmation", the template-selection arm; round 4 M7, the ninth buildable success criterion had no task citing it, audit BLOCKER #1) — RED: `tests/unit/broadcast/template-picker-confirm.test.tsx` "with typed content, selecting a template opens a confirmation and cancelling leaves the text intact" (today `compose/template-picker.tsx:71-78` pushes a URL and `new/page.tsx:322` remounts the form via `key=`, destroying the text with no confirm and no undo).
- [ ] T134 [P] [US6] A member can read the subject **and content** of an E-Blast they submitted (US6-AS2, FR-049) — RED: `tests/unit/broadcast/portal-eblast-detail-body.test.tsx` "the detail renders the subject and the body from the broadcast record's own content" (today the page renders no body at all). **Scoped to PR-1 deliberately**: the "latest **sent** version while awaiting the member" rule needs `broadcast_versions`, which migration `0305` creates in PR-2, so it is asserted by T141a instead (plan Amendment 5).
- [ ] T135 [P] [US6] Every E-Blast route shows the platform's error state with a retry (US6-AS3, FR-047) — RED: `tests/unit/broadcast/eblast-error-boundaries.test.tsx` "each of the five routes missing `error.tsx` today renders the shared error state" (only `/portal/broadcasts/new/error.tsx` exists).
- [ ] T136 [P] [US6] A validation error on the message is announced on the editor itself (US6-AS4, FR-048) — RED: `tests/unit/broadcast/compose-body-error-announced.test.tsx` "the error is associated through `TiptapEditor`'s `invalid`/`describedById` props (`tiptap-editor.tsx:106,113`), not the wrapper `<div tabIndex={-1}>` at `compose-form.tsx:536-550`".
- [ ] T137 [P] [US6] The staff compose-on-behalf form offers what the member form offers (US6-AS5, FR-039) — RED: `tests/unit/broadcast/proxy-compose-parity.test.tsx` "drafts, images, the template picker, the allowance display, the subject counter, the preview and the unsaved-changes guard are all present" (today `proxy-compose-form.tsx` has none of them).
- [ ] T138 [P] [US6] A member uploading an image against a draft that is not theirs is refused and audited as a cross-member probe (US6-AS7, FR-040) — RED: `tests/contract/broadcasts/upload-inline-image-ownership.test.ts` "another member's draft → 404 + `broadcast_cross_member_probe`", "an unknown or other-tenant id → 404 + `broadcast_cross_tenant_probe`", "a closed broadcast → 409" (today `draftId` is an **unvalidated form string** at `route.ts:76` although `route.ts:5` claims a check).
- [ ] T139 [US6] Every E-Blast screen **PR-1 builds** passes the automated scan with zero serious or critical findings and the toolbar is operable with arrow keys (US6-AS6, SC-013, FR-051) — RED: `tests/e2e/broadcasts/eblast-a11y.spec.ts` "@a11y axe at 320 px on the screens PR-1 builds, named by identity rather than by count (`/speckit.analyze` M1): the **seven whole screens** — portal compose, portal benefits E-Blast tab, staff queue, staff compose-on-behalf, template list/new/edit (one surface), E-Blast settings, Brand settings — **plus the body view of `/portal/broadcasts/[id]`**, which PR-1 builds (T141) and PR-2 rebuilds as the sign-off compare view" — **always `--workers=1`**, run in ≤ 10-minute foreground chunks. PR-2 takes the identical pass under **T086a** on `/admin/broadcasts/[id]` (T063) **and on the sign-off view of `/portal/broadcasts/[id]`** (T086). FR-051's nine screens are therefore covered as: seven here whole, `/admin/broadcasts/[id]` in PR-2 whole, and `/portal/broadcasts/[id]` **scanned once in each PR because each PR changes it** — the earlier "seven / two" phrasing enumerated eight and summed to ten, which is why the split is now stated by screen (plan Amendment 4, spec FR-051).

### Implementation

- [ ] T140 [US6] Make template selection non-destructive: confirm when dirty, then **re-seed in place** rather than remounting the form (`src/components/broadcast/compose/template-picker.tsx`, `src/app/(member)/portal/broadcasts/new/page.tsx:322`) — this is the implementation half of **SC-012**'s template-selection arm (round 4 M7) — RED: T133.
- [ ] T141 [P] [US6] Widen `GET /api/broadcasts/[id]` in `src/app/api/broadcasts/[id]/route.ts` with the **subject and body** of the broadcast record, and render them on `src/app/(member)/portal/broadcasts/[id]/page.tsx` (replacing the raw `<h2 className="text-h4">` inside `CardContent` at `:147` with the shared heading component) — **this is the PR-1 half of the FR-049 widening and nothing more**. `stage`, `whoseTurn`, `round`, `proposedSendAt`, `confirmedSendAt`, `expiresAt` and the latest-sent-version body rule are **T141a in PR-2**, because every one of them reads a `0305` column, the `broadcast_versions` table or T052's Domain maps — none of which exists here (plan Amendment 5, `contracts/portal-eblast-approval-api.md` § `GET /api/broadcasts/[id]`) — RED: T134.
- [ ] T142 [P] [US6] Add the five missing error boundaries — `src/app/(member)/portal/broadcasts/[id]/error.tsx`, `src/app/(staff)/admin/broadcasts/error.tsx`, `src/app/(staff)/admin/broadcasts/[id]/error.tsx`, `src/app/(staff)/admin/broadcasts/templates/error.tsx`, `src/app/(staff)/admin/settings/broadcasts/error.tsx` — and reshape `src/app/(member)/portal/broadcasts/new/loading.tsx` and `src/app/(staff)/admin/broadcasts/[id]/loading.tsx` so they match the real page (the portal compose skeleton omits the QuotaDisplay card, the template combobox, the Card wrapper and the count lines today) — RED: T135.
- [ ] T143 [P] [US6] Track a **saved snapshot** for the dirty guard in `src/components/broadcast/compose-form.tsx:203-217` (today it compares against immutable initial props, so it still warns after a successful save), show a busy state on the save control and a "Saved at HH:MM" indicator afterwards (FR-045, **SC-012** — the navigation-and-draft-saving arm of "zero typed content lost without a confirmation"; round 4 M7) — RED: T095.
- [ ] T144 [P] [US6] Pass `invalid` and `describedById` through to `src/components/broadcast/tiptap-editor.tsx` (props at `:106,113`) from both `src/components/broadcast/compose-form.tsx:536-550` and `src/components/broadcast/proxy-compose-form.tsx:452-466` (the pattern `src/components/broadcast/admin/template-form.tsx:283-286` already uses) — RED: T136.
- [ ] T145 [US6] Bring `src/components/broadcast/proxy-compose-form.tsx` (page `src/app/(staff)/admin/broadcasts/new/page.tsx`) to parity: draft save/resume, `src/components/broadcast/compose-inline-image-uploader.tsx` wired with `imagesEnabled` and a real `draftId`, `src/components/broadcast/compose/template-picker.tsx`, the member's allowance display, the subject counter, the preview and the unsaved-changes guard (FR-039) — RED: T137.
- [ ] T146 [US6] Add the real draft-ownership check to `src/app/api/broadcasts/inline-image-upload/route.ts` (`:76`, where `draftId` is an unvalidated form string) and to `src/modules/broadcasts/application/use-cases/upload-inline-image.ts` — resolve `draftId` inside `runInTenant`, require the caller's own member **and** a `draft` they own, refuse a **closed** broadcast with 409, and answer 404 (never 403 — no existence leak) with the matching probe audit event. **Wire T026a's 60 / minute per-(tenant, user) bucket on this route in the same edit** (round 4 H5) — atomic check, **above** the blob write and the ClamAV call, refused 429 `broadcast_rate_limit_exceeded` with `retryAfterSeconds`; the route carries no bucket today and the 5 MB cap bounds one request, not the rate. The success path's `broadcast_image_uploaded` payload carries snake_case **`member_id`** (T033) so the 0009 `last_activity_at` trigger fires — a member uploading an image to their own draft **is** member activity, and `memberId` in camelCase would be a silent no-op (#336/#337) (FR-040, US6-AS7) — RED: T138 + T033's member-key arm.
- [ ] T147 [P] [US6] Replace the hand-rolled empty state at `admin/broadcasts/templates/page.tsx:94-114` with the shared empty-state component, and **delete the dead i18n keys** listed in exploration § B — `compose.delete.*`, `button.deleteDraft`, `fields.bodyPlaceholder`, `fields.previewLoading`, `imageUpload.progressAria`, `editor.bracketHint`, the whole `staleDraftBanner.*` — wiring or deleting `compose/stale-draft-banner.tsx`, which is never mounted (FR-051) — RED: `pnpm check:i18n` after the deletions, plus `tests/unit/broadcast/dead-i18n-keys.test.ts` "no key in the E-Blast namespaces is unreferenced".
- [ ] T148 [US6] Widen the compose pages to a two-column 72 rem layout ≥ lg (editor beside the 600 px preview), stacked below, on `portal/broadcasts/new/page.tsx:303` and `admin/broadcasts/new/page.tsx:30` — replacing the 42 rem `FormContainer` — and record the departure from the form container tier as an exception in **`docs/ux-standards.md` § 18.2 in the same change** (FR-050; the docs edit is a PR-1 merge gate, not a follow-up) — RED: `pnpm check:layout` plus `tests/e2e/broadcasts/eblast-a11y.spec.ts` "@a11y the compose page has no horizontal scroll at 320 px and stacks below lg".

**Checkpoint**: PR-1 is complete and mergeable once T007, T008, T010, T139, T148 and the Phase-10 PR-1 docs tasks are all green.

---

## Phase 9: User Story 7 — SweCham can trial the whole flow safely (Priority: P3) — **PR-2**

**Goal**: the flow can be exercised end to end with no email reaching a real member, and switched off again without losing in-flight work.

**Independent Test**: with the new flow switched on, run the UAT walkthrough using a designated test member and a recipient list made only of SweCham staff addresses; verify zero emails reach anyone else; switch the flow off and verify today's flow behaves exactly as before.

### Tests first (write, observe RED)

- [ ] T149a [P] **[PR-2]** [US7] With the flag off, **no F119 hand-off email is delivered** although the rows are enqueued — the drainer arm of the flag (maintainer decision, round 4 H2; `contracts/dashboard-and-notifications.md` § 3). This is the RED for **T152a** and it is what makes FR-034's "behave as today" literally true for `eblast_submitted_marketing`, which rides an existing unflagged action — RED: `tests/contract/broadcasts/eblast-flag-matrix.test.ts` (T149 owns the file) "with the flag off, a submit writes its `eblast_submitted_marketing` outbox row and a dispatcher tick sends **nothing**, leaves the row pending, sets no `lastError` and counts no attempt", "the same tick with the flag on sends it", and "all five new `notification_type` values are skipped with the flag off, enumerated from the enum with a positive control that fails when one is dropped from the skip set".
- [ ] T151 [P] **[PR-2]** [US7] A new-stage chip and the nav badge are hidden while the flag is off **unless** rows exist in those stages — never offer a filter that can only return zero rows (research R18) — RED: `tests/unit/broadcast/queue-filters-flag-visibility.test.tsx` "flag off + zero rows → chip absent; flag off + one row → chip present". **Write and observe this RED before T116**, which implements the rule: it lives in this phase by story while T116 lives in Phase 6, both now in PR-2, and a guard whose test never went red has never been exercised (Constitution II).

### Implementation

- [ ] T152 **[PR-2 — merge gate]** [US7] Gate exactly **one transition** on `isEblastMemberApprovalEnabled()`, inside `start-formatted-version.ts` and mirrored by the route: `POST /api/admin/broadcasts/[id]/version` answers **404** while the flag is off **only when the re-read row's status is `submitted`** — the `submitted → in_design` edge, the single entry into the approval round (data-model § 8.2, research R18). **It is an edge gate, not a route gate** (round 3 H1): with the flag off the same POST from `changes_requested`, `member_approved` or `approved` still answers 201, because re-opening a working copy after the member asked for changes is the only way an in-flight row can be **completed**, and FR-034 requires in-flight rows to stay completable, not merely cancellable. The gate therefore reads the stage **after** the `FOR UPDATE` re-read, never from the request. Also hide the "Start formatted version" control on the staff detail page (a UI affordance, never the gate). Every other route stays available (FR-034). **This task MUST land in PR-2, in the same PR as T062 which creates that route** (`/speckit.analyze` C1): prod is live with real members, and a PR-2 that merges without this gate puts the approval round in the hands of every `broadcasts.write` holder the moment it deploys — T063's hidden button is not a gate, and "merge without setting the env var" only ships dark if the code reads the flag. PR-2 may not merge until `tests/contract/broadcasts/eblast-flag-matrix.test.ts` is green in **both** flag states. Confirm the new `/api/broadcasts/**` and `/api/admin/broadcasts/**` paths are covered by the existing `matchesF7KillSwitchPath` prefixes (`src/proxy.ts:43-68`) — the Brand page under `/admin/settings/**` is **not**, and gates in-page instead (T028) — RED: T149, T150 (now in Phase 3's Tests-first block, written and observed failing before this gate is implemented beside T062 — round 4 H1). T151 is **T116's** RED (the chip visibility rule), not this gate's — it lands later, before T116 (round 5 A5).
- [ ] T152a **[PR-2]** [US7] Gate the **five new `notification_type` values at the outbox drainer** on `isEblastMemberApprovalEnabled()` in `src/app/api/cron/outbox-dispatch/route.ts` — the F114 precedent, where the drainer skips that feature's two types while its flag is off (maintainer decision, round 4 H2). The **enqueue stays unconditional**: `submitBroadcast`, `proxySubmitBroadcast`, `recordMemberDecision`, `sendVersionToMember`, `confirmSchedule` and the lifecycle cron all write their rows inside the state-changing transaction with no env-var branch, because a rollback must stay atomic and a flag read inside a tx is a hidden coupling. The **drainer** excludes the five values from its selection while the flag is off, so with the variable absent from the environment **nothing is emailed**, rows sit pending, and they drain on the first tick after the flip. A skipped row is **not an error**: no `lastError`, no attempt counted, no `no_template_handler` (`:543`) — that arm is the ~16 h silent-outage ladder and a deliberate skip must never enter it. This is why FR-034 holds for `eblast_submitted_marketing`, which rides `submit`, an existing **unflagged** action: without the skip, PR-2 would start emailing SweCham marketing on every member submission the moment it deployed, from a PR that is supposed to be dark — RED: T149a.
- [ ] T153 [US7] Write the **UAT walkthrough in EN + TH** — `specs/119-eblast-approval-workflow/uat-walkthrough-en.md` and `specs/119-eblast-approval-workflow/uat-walkthrough-th.md`, with the precondition list kept in step with `specs/119-eblast-approval-workflow/quickstart.md` § 4 (FR-035, SC-005, **US7-AS2** — "every stage can be exercised and no email is delivered outside that list" is the scenario this script is the instrument for; round 4 L6) — covering submit → format → request changes → re-format → approve → confirm schedule → sent, with its preconditions stated up front: a designated test member company, **each participating staff address added as a contact of that member** (without this `validateCustomRecipients` refuses the list and the trial stalls at submit — the single most likely day-one failure), a plan with ≥ 3 E-Blasts of allowance, the custom-recipient audience on every trial E-Blast, brand settings set, the RoPA updated, and the Resend Free-plan ceiling of **two broadcasts in flight**.
- [ ] T154 [P] [US7] Walk the **rollback matrix** in `specs/119-eblast-approval-workflow/quickstart.md` § 3.5 on the dev branch, correcting that file and `docs/runbooks/eblast-approval.md` where reality differs, and confirm each layer behaves as documented: flag off keeps in-flight rows completable with the clock still running **and stops the five hand-off emails at the drainer without losing them — rows queue and a re-flip resumes them** (T152a, round 4 H2); a PR-2 revert makes new-stage rows unreachable so they must be **cancelled first**; migrations `0304`/`0305` are undone by no layer and `ALTER TYPE … ADD VALUE` cannot be reversed at all.

**Checkpoint**: the trial can start and can be stopped without losing work.

---

## Phase 10: Polish & Cross-Cutting Concerns

Each item names the PR it must land in. Nothing here is deferred past its PR.

- [ ] T155 **[PR-1]** Walk the **`docs/ux-standards.md` § 15 checklist** by hand on the FR-051 surfaces PR-1 builds, **named by screen identity, not by a count** (round 4 M4 — "the seven … (the same list as T139)" was wrong: T139's list is the seven whole screens **plus** the body view of `/portal/broadcasts/[id]`): portal compose · portal benefits E-Blast tab · staff queue · staff compose-on-behalf · template list/new/edit (one surface) · E-Blast settings · Brand settings — **and the body view of `/portal/broadcasts/[id]`**, which T141 builds in PR-1 and which the § 15 walk must cover for the same reason the axe scan does. Record the result — this is the half of FR-051 the axe suite (T139) cannot cover: shimmer skeletons that match the real page, toast and confirmation tiers, focus management on every dialog, theming, keyboard order. A screen missed here is a screen SweCham tests. The two approval screens take the same walk in PR-2 under **T086a** (FR-051, plan Amendment 4, quickstart § 3.1).
- [ ] T156 **[PR-1]** The five **live-look** items on the running dev server (research V2, exploration § C) — they cannot be settled by reading code: the 320 px **row count** of `src/components/broadcast/tiptap-toolbar.tsx` and whether anything clips (the shape is decided: wrap, no overflow menu — the look measures the cost), layout shift as the deferred `src/components/broadcast/preview-pane.tsx` settles, its empty-state first paint, NVDA on the changed toolbar, and the SV lengths of the five new stage chips in `src/components/broadcast/admin/queue-filters.tsx`. Record the findings in `specs/119-eblast-approval-workflow/quickstart.md` § 3.1. **Ask the maintainer to start `pnpm dev` on :3100 — never start or kill it yourself.** (The chip item re-runs in **PR-2** once T120's labels exist, under T086a's SV/TH check.)
- [ ] T157 **[PR-1]** Pin coverage in `vitest.config.ts` (repo root) on the files **PR-1 creates only** (`/speckit.analyze` H2 — a pin on a file the PR does not create fails the required `Unit + contract coverage vs pinned thresholds` check, which is a merge blocker on `main`): Domain **100 % line** on `src/modules/broadcasts/domain/design-blocks/block-markers.ts`, `…/design-blocks/render-blocks.ts`, `src/modules/broadcasts/domain/brand/contrast.ts` and `…/brand/brand-settings.ts`; **plus 100 % branch** on `src/modules/broadcasts/application/use-cases/set-brand-settings.ts` — the one security-critical use case PR-1 ships (T024 already claims the pin, and leaving it to a PR-2 task would ship the code a PR ahead of its gate; H6). Measure each pinned file with `pnpm vitest run <suites> --coverage --coverage.include=<file>` before pushing — a green `pnpm test` is **not** the CI coverage job.
- [ ] T157a **[PR-2]** Pin Domain **100 % line** in `vitest.config.ts` (repo root) on the seven Domain files **PR-2 creates** (the other half of T157, split per `/speckit.analyze` H2): `src/modules/broadcasts/domain/stage/broadcast-stage.ts` (T052), `…/stage/whose-turn.ts` (T052), `…/stage/in-progress-statuses.ts` (T053), `src/modules/broadcasts/domain/policies/broadcast-status-transitions.ts` (T051), `src/modules/broadcasts/domain/approval/approval-schedule-policy.ts` (T128), `…/approval/broadcast-version.ts` and `…/approval/member-decision.ts` (T054). Measure each the same way.
- [ ] T158 **[PR-2]** Pin **100 % branch** in `vitest.config.ts` (repo root) on **four files here — the fifth of the five is T157's, in PR-1** (round 4 L1: the line read "five files" and then listed four, because `set-brand-settings.ts` is pinned by T157 and must not be double-owned). The five files carry the six security-critical use cases (research R20 as corrected — promotion is not its own file, `promoteApprovedVersion` lives inside `confirm-schedule.ts`): `src/modules/broadcasts/application/use-cases/approval/start-formatted-version.ts`, `…/approval/send-version-to-member.ts`, `…/approval/record-member-decision.ts` and `…/approval/confirm-schedule.ts`. The sixth use case, `set-brand-settings`, is PR-1's and is pinned by **T157**, not here (H6 — the "if it lands first" hedge left a security-critical pin without an owner). Measure each the same way.
- [ ] T158a **[PR-2]** After T157a and T158 land, assert the pins actually bite: run `pnpm vitest run tests/unit/broadcasts --coverage` and confirm `vitest.config.ts` lists **all sixteen** F119 pinned files — **eleven Domain 100 %-line pins** (four from T157 in PR-1: `design-blocks/block-markers.ts`, `design-blocks/render-blocks.ts`, `brand/contrast.ts`, `brand/brand-settings.ts`; seven from T157a) **and five Application 100 %-branch pins** (`set-brand-settings.ts` from T157 in PR-1, plus T158's four approval use cases) — with no pin naming a path that does not exist. The count was previously written as "twelve" here and in `plan.md` § II while the enumeration summed to sixteen (`/speckit.analyze` round 3 H7); a verification task whose own total is wrong verifies nothing. A pin on a missing path is silently satisfied, which is how a pinned file stops being measured.
- [ ] T159 **[PR-1]** Create `tests/helpers/eblast-approval-fakes.ts` — **named for T159a, which extends it in PR-2 with the approval ports** (round 4 L8: PR-1 introduces no approval port at all, so the file's name describes its PR-2 shape, not its PR-1 contents; do not rename it in PR-2) — stubbing **every** method of **every** new port **PR-1 introduces** (`/speckit.analyze` H3 — the PR-2 ports do not exist yet and a fake for a missing port does not compile): `BroadcastImagesRepo` (T033), `BrandSettingsRepo` (T023), `TenantLogoUrlPort` (T027), `TestCopyMailerPort` (T105) and the widened `ImageStoragePort` (T034) — an unstubbed port method is an unexercised branch and a stale stub after an arity change fails silently.
- [ ] T159a **[PR-2]** Extend `tests/helpers/eblast-approval-fakes.ts` with **every** method of the four ports PR-2 introduces: `BroadcastVersionsRepo` and `BroadcastDecisionsRepo` (T055), `MarketingDirectoryPort` (T066) and **`BroadcastApprovalScrubPort`** (T082) — the last was missing from the original list entirely, so the erasure port would have gone unstubbed and its branches unexercised on the one path that must reach every version, reason, note and image (H3). **Sequencing** (round 5 A6): the helper is extended **per port as that port lands** — the T055 fakes ship with T055's RED, T066's with T066, T082's with T082 — so no unit test in Phase 3/4 waits on Phase 10; this Phase-10 ID is the **completeness audit** that every method of the four ports has a fake and no consumer stubs one inline.
- [ ] T160 **[PR-2]** Write `docs/observability.md` **§ 28** (the file ends at § 27, line 2205): the four gauges, **six** counters (the count read "five" here while `contracts/dashboard-and-notifications.md` § 4.2 and `plan.md` § VII said six — round 3 M10), two histograms, the OTel spans, and the three alerts — `broadcasts_awaiting_member_oldest_age_seconds > 7 d` (warning), `> 14 d` (page, both inside the 30-day expiry clock) and `broadcasts_no_marketing_recipient_total > 0` (page).
- [ ] T160a **[PR-2]** Measure the plan's performance budgets **once** on the running dev server and record every number in `docs/observability.md` § 28 beside T160's metrics (`/speckit.analyze` M12 — today no task measures them, which is how F7's SLO budgets became permanently "UNVERIFIED"): server p95 for `POST /api/admin/broadcasts/preview`, `PATCH …/[id]/version`, `POST /api/broadcasts/[id]/decision` and `POST …/[id]/schedule` (budget **< 400 ms** each, read from the `broadcasts_preview_render_ms` / `broadcasts_member_decide_ms` histograms and the route timings), the test copy (**< 3 s**), and compose **LCP < 2.5 s · INP < 200 ms · CLS < 0.1** from the browser on `/portal/broadcasts/new`. Measure **alone**, never inside a folder run — the pre-push hook exports `INTEGRATION_FOLDER_RUN=1` and 100+ files on one Neon compute quadruple a query's p95. A budget that is not met is recorded as **UNVERIFIED with the measured number and the date**, never silently dropped and never re-stated as if it passed. **Ask the maintainer to start `pnpm dev` on :3100 — never start or kill it yourself.**
- [ ] T161 **[PR-2]** Write `docs/runbooks/eblast-approval.md`: a stuck stage, the expiry clock, the flag rollback, the image sweep and its ≤ 24 h backstop window, and the `no_template_handler` symptom (a notification type shipped without its dispatcher arm retries silently for ~16 h).
- [ ] T162 **[PR-2, precondition of the flag flip]** Update `docs/compliance/processing-records.md` (RoPA) with exactly what spec § Personal data names: the new purpose; the new fields **by name** (versions, notes, reasons, decisions, uploaded images); the **staff recipients** of hand-off emails (`marketing`-role users, admins as fallback); the **chamber postal address** now printed in every footer; the new export category `broadcast-versions.json`; the erasure reach including the notifications; and the note that a **sent** outbox row keeps the recipient address frozen at enqueue under the existing outbox retention. **The flag is not set until this lands** (quickstart § 3.2 step 4).
- [ ] T163 **[each PR]** Add the feature's entry to `docs/changelog.md` with the PR number and the review-round provenance.
- [ ] T164 **[each PR]** Run `pnpm typecheck` **and** full `pnpm lint` (the `typecheck` and `lint` scripts in `package.json`) as the last step after the final edit of every commit — neither is in any gate (not pre-push, not CI), and lint catches errors typecheck and vitest both miss. Never run `pnpm format` / `prettier --write`.
- [ ] T165 **[each PR]** Run the gate list (every script named below is defined in `package.json`) before opening the PR: `pnpm lint && pnpm typecheck && pnpm test:coverage && pnpm check:i18n && pnpm check:layout && pnpm check:fixme && pnpm check:template-seed && pnpm check:money-recipient && pnpm check:audit-events && pnpm check:audit-counts && pnpm check:multi-tenant && pnpm check:staff-page-guard && pnpm check:api-route-guard && pnpm check:actor-role-truth && pnpm check:env-example && pnpm check:portal-guard`, then the integration suites **that PR has created**, **by file path** and never `-- <pattern>` (which runs the whole ~40-minute suite). **The list is per PR** — naming a path the PR does not create makes the gate unsatisfiable, the same defect class as a coverage pin on a missing file (`/speckit.analyze` round 3 M1). **PR-1 (three)**: `tests/integration/broadcasts/{eblast-approval-tenant-isolation,eblast-content-parity,audit-event-type-parity}.test.ts`. **PR-2 (all twelve)**: those three plus `tests/integration/broadcasts/{eblast-approval-cross-member-probe,eblast-immutability-trigger,eblast-state-machine-edges,eblast-allowance-bucket,eblast-erasure-reach,eblast-dashboard-pagination,eblast-approval-happy-path,eblast-approval-rounds,eblast-submit-notifies-marketing}.test.ts`. The last four of the twelve were created by T038, T069, T123 and T022/T049/T050 but were absent from this gate, so the FR-012a/SC-002 happy path itself was never run by it (`/speckit.analyze` M4) — then `tests/e2e/broadcasts/eblast-approval.spec.ts` and `tests/e2e/broadcasts/eblast-a11y.spec.ts` in ≤ 10-minute foreground chunks with `--workers=1`.
- [ ] T166 **[each PR]** Dispatch the named project-agent passes that stand in for the second human reviewer (`specs/119-eblast-approval-workflow/plan.md` § Complexity Tracking #1; agent definitions in `.claude/agents/`): `security-engineer`, `pdpa-gdpr-compliance-officer`, `reliability-guardian`, `enterprise-ux-designer`, `senior-tester`, then a `whole-branch-reviewer` seam pass. **`drizzle-migration-reviewer` is dispatched only in a PR that carries a migration** — PR-1 (`0304`) and PR-2 (`0305`); dispatching it on a migration-less PR produces a review of nothing and dilutes the substitute (round 3 L5). File-mutating agents run sequentially; read-only reviewers may run concurrently.

---

## Dependencies & Execution Order

### PR order (the real ship order)

**Two PRs, not three.** **PR-1** (Phase 1 → Phase 2 → Phase 5 → Phase 8, plus T026a, T046a, T122a, T155, T156, T157, T159, T163–T166) → **PR-2** (**T120 + T121 + T122 first** → Phase 3 **incl. T149, T150, T006a-with-T063, T129a-before-T065, T062a, T106a and T152 with T062** → Phase 4 **incl. T081a, T141a** → Phase 7 → Phase 6 → Phase 9 **incl. T149a, T151, T152a**, then **T086a** last of the UI work, plus T157a, T158, T158a, T159a, T160, T160a, T161, T162, T163–T166). The former PR-3 (Phase 6 + the rest of Phase 9 + T160/T160a/T161) is folded into PR-2 by the maintainer's 2026-09-18 decision. **Four PR-2 tasks run out of file order and the file says so on each of them** (round 4 C1, H1, H4, M1): T120/T121/T122 at the head, T129a before T065, T149/T150 in Phase 3's Tests-first block.

PR-1 is unflagged and changes live outgoing email, so it carries three hard merge gates: **T007** (byte-identical no-brand/no-block wrapper snapshot), **T008** (SC-011 element parity with its positive control) and **T139 + T155 + T148** (the FR-051 pass on the seven whole screens PR-1 builds plus the body view of `/portal/broadcasts/[id]`, plus the § 18.2 exception). PR-2 carries two of its own: **T152** — the flag gate on the `submitted → in_design` **edge** (not the route — round 3 H1), green in **both** flag states via `eblast-flag-matrix.test.ts` (T149/T150), without which PR-2 is not a dark ship at all (`/speckit.analyze` C1) — and **T086a**, the identical FR-051 pass on `/admin/broadcasts/[id]`, on the sign-off view of `/portal/broadcasts/[id]` **and on the staff queue T116–T120 rebuild**. The pass follows the screen, so neither PR ships a screen it changed unscanned (plan Amendment 4). PR-2 may not merge before PR-1 because it saves versions through the shared sanitiser policy and renders previews through the shared wrapper. Because the dashboard now ships with the stages it reports on, no emitter ships ahead of its metric registration, no screen renders a stage label it does not have, and the flag is flipped only after the dashboard, the runbook and the UAT walkthrough exist.

### Phase dependencies

- **Phase 1 (Setup)**: no dependencies. T004 (V1) blocks T027; T005 (V4) blocks T105.
- **Phase 2 (Foundational)**: depends on Phase 1. Blocks Phase 5 and Phase 8 entirely, and blocks Phase 3's content-rule tasks (T058, T059) and preview reuse (T086).
- **Head of PR-2, before Phase 3 (round 4 C1, M1)**: **T121** (four gauges) and **T122** (five counters + `broadcasts_member_decide_ms` + three spans) are **registrations**, and `contracts/dashboard-and-notifications.md` § 4.2 makes them a precondition of every emit — an emit against an unregistered field does not typecheck, and their emitters T058/T059/T066 (Ph3), T078 (Ph4) and T130 (Ph7) all run before Phase 6. **T120** (the relabel + the five stage labels) runs here too, because T063/T084/T085/T141a render `stageOf(status)` and next-intl does not throw on a missing key. All three keep their Phase-6 IDs; only the build order moves.
- **Phase 3 (US1)**: depends on Phase 2. T048 (migration 0305) blocks everything else in Phases 3, 4 and 7. T051 (shadow sweep) blocks T116 and T132. **T106a** needs T048's `in_design` value and T106's route; **T152 must land with T062** — the route and its flag gate ship together or PR-2 is not dark — and **T149 + T150 are therefore written and observed RED in this phase's Tests-first block, before T152 is implemented** (round 4 H1, Constitution II). **T006a lands with T063** and not earlier, or T147's `dead-i18n-keys.test.ts` goes red on the unreferenced approval namespaces (round 4 M2). **T129a** (the five email builders) is built here, before T065's three dispatcher arms call it (round 4 H4).
- **Phase 4 (US2)**: depends on Phase 3 — T078 needs T055's repos and T048's tables. **T081a** (the member `…/cancel` bucket) runs after T081 and needs T067's `portal-eblast-decision.test.ts` to exist — which is why it is not part of T062a (round 4 M8). **T141a** needs T048's columns, T051/T052's Domain maps and T055's versions repo; **T086a** is the last task of PR-2's UI work — it needs T063 (staff format surface, Phase 3) and T086 (portal sign-off) to exist, so it runs after both.
- **Phase 5 (US3)**: depends on Phase 2 only. Independent of Phases 3, 4, 6, 7.
- **Phase 6 (US4)**: **now PR-2**, running after Phase 7 — **except T120, T121 and T122, which are hoisted to the head of PR-2 above** (round 4 C1, M1; round 3 C2 asked for "as soon as Phase 6 opens", which is still three phases too late for a registration list and a label set that Phase 3 reads). What remains here depends on Phase 3 (T051, T052) and Phase 4 (round semantics). **T151 (Phase 9) is T116's RED test and MUST be written and observed failing before T116 is implemented** — both now in PR-2, so this is an ordering rule inside the PR; T116 is the only implementation of the rule T151 pins (Constitution II).
- **Phase 7 (US5)**: depends on Phase 3 (T048, T066) and on T035 (the cron block it extends). **T129a keeps its ID here but is built in the Phase 3 block** (round 4 H4): the five email builders are called by T065's three arms (Phase 3) as well as T131's, and an arm cannot render a template that does not exist. T131 then reuses them.
- **Phase 8 (US6)**: depends on Phase 2; T137/T145 also want T099's alt-text dialog from Phase 5.
- **Phase 9 (US7)**: **all of it is [PR-2]**. **T149 and T150 have moved into Phase 3's Tests-first block** (round 4 H1) — they are T152's RED and T152 lands with T062, so leaving them here implemented the merge gate before its evidence. T152 depends only on Phase 3 (T062's route and T003's flag helper) and must land with T062. **T149a** (the drainer arm of the flag matrix) is the RED for **T152a** and both stay here: the skip must exist before the flag flip, not before any particular route, and T149a extends the file T149 created. T151 depends on Phase 6's T116 being its implementation (write T151 first). T153 (the EN + TH UAT walkthrough) and T154 (the rollback-matrix walk) depend on Phases 3, 4, 6 and 7 existing, and **T153 is a precondition of the flag flip**, not a follow-up.
- **Phase 10**: each item lands inside its named PR, not after it — which is why T157/T158/T159 are split into PR-1 and PR-2 halves (T157a, T158a, T159a): a coverage pin or a port fake belongs in the PR that creates the file it names, and no earlier.

### User-story dependencies

- **US3 and US6 ship together as PR-1** — except the three halves that need `0305` and ride in PR-2: T106a (US3-AS3, the `in_design` stage on the staff image route), T141a (FR-049, the workflow fields on the portal detail) and T086a (FR-051, the three PR-2 screens). A per-story trace reports US3/US6 complete only after PR-2 (round 5 A7).
- **US1 is not shippable without US2** (the spec's own reasoning: the first disagreement would force a cancel-and-start-over), and **US5**'s hand-off notifications are what stop a two-sided flow stalling — the three ship together as PR-2.
- **US4** reports on the stages **US1/US2** create and **US7** exercises all of them — both now ship inside PR-2, because a dark approval round whose dashboard, stage labels, metrics and UAT script arrive in a later PR cannot be trialled and cannot be observed.

### Within each story

- Tests are written and observed **FAILING** before implementation (commit red, then green). RED is the evidence the task measures behaviour rather than the mock — a guard whose test never went red has never been exercised.
- Domain before Application; Application before Infrastructure; Infrastructure before routes; routes before pages.
- Apply the migration **and run the touched integration suites** before committing a schema change — unit-test mocks hide the schema gap; the failure only surfaces against live Neon.

### Parallel opportunities

- Phase 1: T002, T004, T005 in parallel.
- Phase 2: T008–T012 in parallel (different test files); T015, T017, T018 in parallel; T021 in parallel with T022.
- Head of PR-2: **T121 and T122 in parallel** (different halves of `src/lib/metrics.ts` and the gauges route — coordinate the one file); **T120 is never parallel with T006a or T147**, all three write `src/i18n/messages/*.json`.
- Phase 3: T037, T039–T047, T149, T150 in parallel (different test files); T052, T053, T054 in parallel after T051. **T106a after T062**; **T152 and T062a with T062** (same route file — not parallel with it); **T006a with T063**; **T129a before T065**.
- Phase 4: T068–T075, T077 in parallel; T083, T087, T141a in parallel; **T081a after T081** (it limits the route T081 widens). **T086a is never parallel and no longer ends Phase 4** — it is the last UI task of the whole of PR-2 and needs every PR-2 screen finished, including Phase 6's rebuilt queue.
- Phase 5: T089–T097 and T046a in parallel; T099, T100, T101, T104, T106, T107 in parallel after T098.
- Phase 6: T110–T115 in parallel; T118 and T119 in parallel after T117. **T120, T121 and T122 are not here** — they were built at the head of PR-2 (round 4 C1, M1).
- Phase 8: T134–T138 in parallel; T141–T144, T147 in parallel.
- **Never** in parallel: anything touching `src/i18n/messages/*.json`, `src/modules/broadcasts/infrastructure/schema.ts`, `drizzle/migrations/meta/_journal.json`, `vitest.config.ts` or `src/config/nav.ts` — one committer at a time on each.

---

## Parallel Example: User Story 1

```bash
# Launch the RED acceptance tests for US1 together (different files, no shared state):
Task: "FR-012a trigger test in tests/integration/broadcasts/eblast-immutability-trigger.test.ts"      # T036
Task: "state-machine edge matrix in tests/integration/broadcasts/eblast-state-machine-edges.test.ts"  # T037
Task: "send-version preconditions in tests/contract/broadcasts/admin-eblast-send-version.test.ts"     # T040, T043
Task: "schedule contract in tests/contract/broadcasts/admin-eblast-schedule.test.ts"                  # T042
Task: "RBAC pins in tests/contract/broadcasts/admin-eblast-rbac.test.ts"                              # T046

# Then, after T051 lands, the three pure Domain modules together:
Task: "stageOf/turnOf in src/modules/broadcasts/domain/stage/"                                        # T052
Task: "IN_PROGRESS_BROADCAST_STATUSES in src/modules/broadcasts/domain/stage/in-progress-statuses.ts" # T053
Task: "version + decision invariants in src/modules/broadcasts/domain/approval/"                      # T054
```

---

## Implementation Strategy

### PR-1 first (the tool and the screens)

1. Phase 1 (Setup) → Phase 2 (Foundational, incl. **T026a**, and **T122a sequenced before T032** — the preview metrics are registered before T032 emits them, round 5 A3) → Phase 5 (US3, incl. **T046a**) → Phase 8 (US6) → the PR-1 items of Phase 10 (T155, T156, T157, T159, T163–T166).
2. **STOP and VALIDATE**: T007 byte-identical, T008 parity with its positive control, T139 + T155 on the seven whole screens plus the body view of `/portal/broadcasts/[id]`, T147's dead-key test green (which is why T006 no longer creates the approval namespaces), the three PR-1 integration suites by file path, the five live-look items on the maintainer's dev server.
3. Merge. Every E-Blast improves immediately; the approval round is pointless without this.

### PR-2 (the approval round + the dashboard + the trial, dark)

1. **T120 + T121 + T122 first** (the stage labels and the whole metric registration list, out of file order and hoisted out of Phase 6 — round 4 C1, M1: every counter's emitter and every stage label's reader runs below this line, and an emit against an unregistered field does not typecheck) → Phase 3 (US1 — **T149 + T150 RED in its Tests-first block before T152**, round 4 H1; **T129a before T065's arms**, H4; **T006a with T063**, M2; incl. **T106a**, and **T152 + T062a alongside T062**) → Phase 4 (US2, incl. **T081a after T081** and **T141a**) → Phase 7 (US5) → Phase 6 (US4 — what is left of it, and **T151 before T116**) → Phase 9 (US7 — T149a, T151, T152a, T153, T154) → **T086a last of the UI work** → T157a, T158, T158a, T159a, T160, T160a, T161, T162.
2. **STOP and VALIDATE**: **the flag matrix in both states (T149/T150/T149a green — the `submitted` edge 404 with the flag absent, `changes_requested` re-entry still 201, and not one of the five hand-off emails delivered while the variable is absent; PR-2 is not mergeable without it)**, all twelve live-Neon suites by file path, the two-tenant probes in both directions on all three new tables, the FR-012a trigger test, the quickstart § 0 `pg_proc` checks against the dev branch, SC-008 at 1,000 rows in a **single-file** run, the chip-visibility rule in both flag states (T151), T160a's measured budgets recorded in `docs/observability.md` § 28 (any budget not met written down as UNVERIFIED with its number), and **T086a** — the FR-051 § 15 + axe pass on `/admin/broadcasts/[id]`, the sign-off view of `/portal/broadcasts/[id]` and the rebuilt staff queue, the same bar PR-1 applied to its screens.
3. Merge **without** setting `FEATURE_EBLAST_MEMBER_APPROVAL`. Update the RoPA (T162). Only then set the flag — setting an env var IS a production deploy — and only with the dashboard, the runbook (T161) and the EN + TH UAT walkthrough (T153) already in this same PR, which is the whole reason the former PR-3 was folded in.
4. Run the UAT walkthrough with SweCham.

---

## Notes

- `[P]` = different files, nothing incomplete depended on. `[Story]` maps a task to its user story for traceability.
- Verify each test **fails** before implementing. Commit only the tests the task names — roughly one focused test per stated behaviour, sized like the neighbouring files; scratch checks stay out of the repo.
- Every tenant-scoped query threads the `tx` from `runInTenant`; a repo method that reaches for the pool-global `db` silently bypasses RLS.
- `return err()` inside a `runInTenant` callback **COMMITS** — throw to roll back, and put refusal guards above the first write.
- `default: { return _exhaustive }` is **fail-open** — it returns the value at runtime. Write `void _exhaustive; return <safe>`.
- Audit payloads carry ids, keys, counts and lengths — never subject, body, note or reason text. `actorRole` is the session role, `?? null`, never a literal (`check:actor-role-truth`). Member-activity events carry snake_case **`member_id`**; staff and system events carry **`related_member_id`**.
- Timestamps are ISO 8601 UTC; Buddhist Era is display-only for `th-TH`.
- e2e: **always `--workers=1`**, in ≤ 10-minute foreground chunks, using `e2e-member-empty` (the `e2e-member` persona is LAPSED by the F8 fixture and will 403).
- Commit messages use Conventional Commits with the `[Spec Kit]` prefix on gate commits; the commitlint header limit is **100 characters** — count it with `wc -m` before committing.
