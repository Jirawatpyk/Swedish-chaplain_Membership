# Operations, Performance & Observability Requirements Checklist: Member Portal — Approval Workflow for Member Changes

**Purpose**: Validate that performance budgets, observability (logs / metrics / traces / alerts), cutover, runbook and cost-budget requirements are complete, clear, consistent and measurable before `/speckit.tasks`. Formal gate; `performance-slo-guardian` + `observability-instrumentor` co-sign at `/speckit.review`.
**Created**: 2026-09-11
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [contracts/notifications-and-audit.md](../contracts/notifications-and-audit.md) · [quickstart.md](../quickstart.md) · `docs/observability.md`

**Note**: Generated by `/speckit.checklist`. `[x]` = reviewer judged the requirement text satisfactory; not implementation status.

## Requirement Clarity — performance budgets

- [ ] CHK001 Are API p95 budgets stated per route (submit, decide, queue, review, gate) with the load assumption (≤ 5 reviewers, ≤ 5,000 rows) rather than one blanket "< 400 ms"? [Clarity, Plan § Technical Context]
- [ ] CHK002 Are the user-facing timing criteria (staff notified ≤ 5 min in 99 %; member notified ≤ 5 min; reviewer decision ≤ 60 s / ≤ 3 interactions) mapped to the mechanism that bounds them (outbox cadence, one-click default), so each is verifiable? [Measurability, Spec §SC-002, §SC-003, §SC-005]
- [ ] CHK003 Are Core Web Vitals budgets (LCP < 2.5 s, INP < 200 ms, CLS < 0.1) stated for the queue and review pages with skeleton/loading requirements? [Completeness, Plan Constitution VII]
- [ ] CHK004 Is the dashboard count's freshness requirement stated ("live at render", not the cron snapshot) and its cost bounded (one indexed count/min)? [Clarity, Spec §US6 AS3, Research R12]
- [ ] CHK005 Is the reviewer fan-out of outbox rows bounded (one row per reviewer; what happens at 20+ reviewers on a future tenant) or explicitly scoped to the current staff size? [Gap, Contract notifications §1] → closed: § Assumptions (≤ 20 reviewers) + § Out of Scope

## Requirement Completeness — observability

- [ ] CHK006 Are the metrics enumerated with name, type, labels and emitting site (2 gauges, 3 counters, 1 histogram) and registered in `docs/observability.md` § 14? [Completeness, Contract notifications §4]
- [ ] CHK007 Are the alert thresholds stated (7 d warn / 14 d page on `oldest_age_seconds`) and tied to the FR-037 statutory rationale, with an owner and a runbook link? [Clarity, Spec §FR-037, Research R12]
- [ ] CHK008 Is the "no new cron" constraint stated with the reason (37 of 40 Pro jobs) and the host tick named (per-tenant gauges route), plus the verify-before-task item that it can carry a second module's gauges without renaming existing metrics? [Consistency, Research R12, V2]
- [ ] CHK009 Is the `errorId` taxonomy requirement stated for every new failing route/arm (`M114.<route>.<arm>`), following the F8 gate pattern, so no silent 500 can ship? [Completeness, Contract notifications §4]
- [ ] CHK010 Are the log field rules stated (allowed: requestId, tenantId, hashed user id, request id; forbidden: values, reasons, emails) for both use cases and the dispatcher arms? [Completeness, Contract notifications §4, `docs/observability.md` § 3]
- [ ] CHK011 Are OTel span requirements named for submit and decide (span names, attributes limited to ids)? [Gap, Plan Constitution VII mentions spans without naming them] → closed: contracts/notifications-and-audit § 4 (span names + attributes)
- [ ] CHK012 Are the counters' `reason` label values closed (`rate_limited`, `forbidden`, `archived`, `already_decided`, `validation`) so dashboards do not fan out on free text? [Clarity, Contract notifications §4]

## Requirement Completeness — cutover & rollback

- [ ] CHK013 Is the cutover sequence stated in order with the "setting the env var IS the flip" caution (no `ignoreCommand` → next merge deploys) and the tenant-switch step as the last, audited action? [Completeness, Quickstart § 3, Spec §FR-031]
- [ ] CHK014 Is a first-submission observation defined as the completion criterion of the cutover (staff email arrived, queue shows it, dashboard count 1, gauge 1) and where it is recorded (`reviews/cutover.md`)? [Measurability, Quickstart § 3 step 4]
- [ ] CHK015 Is the rollback matrix stated per layer — tenant setting OFF vs flag OFF vs code revert — with the effect on pending rows, banners, counts and the narrowed `PATCH /profile` (which must widen back when the gate is off)? [Completeness, Quickstart § 3 step 5, Research R6, R11]
- [ ] CHK016 Is it stated which changes are **unflagged and live on merge** (migration DDL, enum values, `tenant_member_settings` column) so an operator knows what a flag flip does *not* undo? [Gap, 108 precedent § Rollback matrix; this spec/plan do not list the unflagged surface] → closed: FR-039 + quickstart § 3 rollback matrix
- [ ] CHK017 Is `.env.example` + `check:env-example` + the zod boot schema named for the new flag, with its default (OFF) and prod/preview values? [Completeness, Research R11]

## Requirement Completeness — runbook & operations

- [ ] CHK018 Is a runbook required (`docs/runbooks/member-change-requests.md`) covering: stuck pending queue, coalescing questions ("why no email?"), rate-cap questions, dispatcher failures for the two new types, and the rollback matrix? [Completeness, Plan § Source Code]
- [ ] CHK019 Is the operator action for "no reviewer exists" defined (what the warning says, who fixes it) so the silent-request case cannot persist? [Clarity, Spec § Edge Cases "No staff user is entitled to review"]
- [ ] CHK020 Are the post-cutover watch signals listed (oldest-age gauge, `email_dispatch_failed` for the new types, `member_self_update_forbidden` spikes) with a watch period? [Completeness, Quickstart § 4]
- [ ] CHK021 Are prod read-only measurements named as verify-before-task items (V4: contacts with a login that are not primary = 0 on 2026-09-10) so tests seed what prod cannot show? [Assumption, Research V4]

## Requirement Consistency — CI & gates

- [ ] CHK022 Is the CI cost of the live-Neon suites acknowledged (~7× local; integration-smoke headroom) and the tenant-isolation suite placed where the required check runs it, per the panel's budget note? [Consistency, Research R16, Panel § Unverified 4 "CI budget"]
- [ ] CHK023 Are the static gates the feature must extend listed (`check:multi-tenant` tables, `check:audit-events`, `check:staff-page-guard`, `check:api-route-guard`, `check:actor-role-truth`, `check:i18n`, `check:env-example`) and is each named in the quickstart's gate line? [Completeness, Quickstart § 2]
- [ ] CHK024 Is `pnpm typecheck` + full `pnpm lint` named as the last pre-commit step (neither is in a gate)? [Consistency, Plan Constitution IX, CLAUDE.md § Gotchas]

## Notes

- CHK005, CHK011, CHK016 are gaps to close in `tasks.md` (plan-level); no spec-text change required unless the operator wants the unflagged surface listed in the spec as 108 did.
- 2026-09-11: every item annotated "→ closed:" was resolved the same day in the spec (Clarifications session "gap closure — AMENDMENT", FR-038–FR-040) or in plan.md / contracts / quickstart; checkboxes stay reviewer-owned and are ticked at `/speckit.review`.
