# Staff-Engineer Review — 108 PR-D (contact marketing state + audience page)

**Gate**: `/speckit-staff-review-run` · **Date**: 2026-09-06 15:22 SEAST
**Branch**: `108-pr-d-contact-marketing` · **HEAD**: `c2c8cfb35` · **PR**: [#344](https://github.com/Jirawatpyk/Swedish-chaplain_Membership/pull/344) (draft)
**Scope**: `git diff origin/main...HEAD` — 155 files, +11,233 / −346
**Method**: five project reviewers on Opus, read-only, one per review pass. Each was told the review ledger (`reviews/pr-d.md`) contains **allegations to verify against the code, not facts**.

| Pass | Reviewer | Files read | Verdict |
|---|---|---|---|
| 1 Correctness & logic | `reliability-guardian` | 35 | CONDITIONS |
| 2 Security | `security-engineer` | 48 + 7 gates + 225 tests run live | APPROVE WITH FIXES |
| 3 Performance & scalability | `performance-slo-guardian` | 31 | PASS WITH CONDITIONS |
| 4 Spec compliance & architecture | `spec-compliance-auditor` | 56 (+~40 via sub-verifiers) | PARTIALLY COMPLIANT, 0 blockers |
| 5 Test quality | `senior-tester` | 45 | **NOT MERGEABLE** |

---

## Executive summary

### ❌ CHANGES REQUIRED

**One blocker, independently confirmed twice.** `T1` — the coverage pin this branch added for `set-contact-marketing-opt-out.ts` (100% branch) is not met. Pass 5 measured 97.72% locally; the required CI check agrees:

```
ERROR: Coverage for branches (97.72%) does not meet
"src/modules/members/application/use-cases/set-contact-marketing-opt-out.ts" threshold (100%)
```

`Unit + contract coverage vs pinned thresholds` — **fail**, 19m56s. It is one of the six required checks on `main`, so this PR cannot merge as it stands. The fix is a single test case.

Everything else in this PR is sound. Across five passes there is **no security blocker, no tenant-isolation gap, no `err()`-commits-the-transaction defect, no N+1, no unbounded loop, no high-cardinality metric label, no PII in any audit payload, log line, toast or metric label, and no architecture-boundary violation.** Pass 2 did not take the co-signed checklists on trust — it re-ran seven static gates and a 225-test permission suite at this HEAD.

### The pattern worth naming

Of the 20 warnings, **the large majority are claims stated more strongly than the code supports** — not logic defects. And most were introduced by cycles 15 and 16, the cycles whose whole purpose was closing "recorded but never read":

| Where the claim lives | What the code does | Finding |
|---|---|---|
| Ledger: carry-forward is "the shape SweCham's pending import has" | `scripts/import-members.ts` inserts contacts raw, bypassing `addInTx` entirely | C1 |
| Migration `0294` header + `schema-contacts.ts`: "PR-D's own queries do not use it … drop it if unused" | `state=on` builds `removed_at IS NULL AND marketing_opt_out_at IS NULL AND tenant_id = $1` — the partial-index predicate verbatim, with the index's leading column | P1 |
| `docs/observability.md` (added this morning): "a drop to 0 … means the opt-out filter stopped running" | the emit is behind `if (droppedByPreference > 0)`; at SweCham's 0 opt-outs the series never appears, so healthy and broken are the same signal | P2 |
| `checklists/security.md` CHK019: "timing-safe 404" | not timing-safe (the probe path writes an audit row first), and `grep "timing"` in spec + contract returns zero — the requirement exists only in the checklist's own wording | S3 |
| `domain/contact.ts` + `contact-repo.ts`: "a new contact always starts in `RECEIVES_MARKETING`" | false since cycle 15 added the carry-forward | C6 |
| Cycle 15 ledger: "every claim now matches the code beside it" | the same cycle's changes invalidated four test docstrings (T3) and two domain comments | T3, C6 |
| Three co-signs stamped at `5b818ee8c` | HEAD is `c2c8cfb35`, five commits later | S1 |
| Ledger T060: "measured 100/100/100/100" | stale — predates cycle 15 adding the branch that now fails | T1 |

This is the same class the feature spent sixteen cycles closing, reappearing one layer up: **the fix was real, the sentence describing it outran it.** Two of these (P1, P2) were written by this session within the last three hours.

---

## Findings

### 🔴 Blocker (1)

| ID | File:line | Defect | Recommendation |
|---|---|---|---|
| **T1** | `vitest.config.ts:602-608` · `set-contact-marketing-opt-out.ts:197` | Pin is 100% branch; actual 97.72%. Uncovered: the `: undefined` arm of `cause: errKind('cause' in re ? re.cause : undefined)`. Every test feeds a `RepoError` that HAS a `cause`; the one shape without it (`repo.not_found`) early-returns at `:195` before the ternary. `repo.conflict` has no `cause` field at all and `repo.unexpected.cause` is optional, so the arm is reachable in production. Introduced by cycle 15's errors HIGH-2 fix, into a file already pinned at 100%. | Add one `err({ code: 'repo.unexpected' })` (no `cause`) arm to the existing switch in `set-contact-marketing-opt-out.test.ts:93-106` and one `it()` asserting `server_error`. Do the same for `list-marketing-audience.ts:178` — same bug class, currently unpinned and therefore silently passing the 80% floor. |

### 🟡 Warnings (20)

**Correctness**

| ID | File:line | Defect | Recommendation |
|---|---|---|---|
| C1 | `drizzle-member-repo.ts:393-418` (callers `:868`, `:884`) · `scripts/import-members.ts:211` | The cycle-15 self-opt-out carry-forward covers **one of three** insert paths. `insertContactRow` (used by `createWithPrimaryContactInTx`, i.e. `POST /api/members`) and the import script both insert without looking for a prior objection. **Fail OPEN**: a person who opted out, was removed, and is re-added under a new member receives E-Blasts again. The import script is the exact scenario the ledger cites as the fix's motivation. | Extract `findCarriedSelfOptOut(tx, tenantId, email)` and call it from `addInTx`, `insertContactRow`, and the import script. |
| C2 | `drizzle-contact-repo.ts:127-143` | The carry-over query selects the latest row **that has** a self opt-out (`WHERE source='self' ORDER BY marketing_opt_out_at DESC`), not the latest row **for that address**. An opt-IN clears only the current row, so a removed older row resurrects the objection on the next re-add. Fails closed (privacy-safe) but silently discards the person's own opt-in and contradicts FR-032. | `ORDER BY created_at DESC LIMIT 1` and carry only if that row is still `self` — safe because `contacts_tenant_email_uniq` guarantees the old row was removed first. |
| C3 | `drizzle-contact-repo.ts:143-152` · `contact-crud.ts:293-307` | A contact born opted-out emits no `contact_marketing_opted_out` for its own id, and `contact_created`'s payload omits the marketing state. Staff see "off (by contact)" with a timestamp predating the contact, and the audit viewer filtered by that `contact_id` explains nothing. | Emit the event in the same tx when `added.value.marketing.source === 'self'`, carrying `carried_from_contact_id`; or at minimum add `marketing_opt_out_source` to `contact_created`. |

**Security**

| ID | File:line | Defect | Recommendation |
|---|---|---|---|
| S1 | `checklists/{security,privacy,ux}.md` | Co-signs stamped at `5b818ee8c`; HEAD is `c2c8cfb35`. | Re-stamp at merge HEAD. Evidence that the drift is benign is already established: `git diff 5b818ee8c..HEAD -- src/ scripts/ drizzle/` is a JSDoc reorder in `src/lib/metrics.ts` and nothing else; the five commits otherwise **add** a live-Neon route suite to a required job. |

**Performance / observability**

| ID | File:line | Defect | Recommendation |
|---|---|---|---|
| P1 | `0294_contacts_marketing_opt_out.sql:28-35` · `schema-contacts.ts` | Header says PR-D does not use the partial index and instructs PR-C to "drop it if unused". True only for the default view — `state=on` implies the predicate exactly, and `state=on` is half of the FR-027a pre-flight preset. Acting on the instruction could drop the only index that preset can plan against. | Correct both comments; require PR-C to `EXPLAIN` **PR-D's** `state=on` query before considering a drop. |
| P2 | `resolve-segment-recipients.ts:151,157` · `docs/observability.md:1307` | `marketing_opt_out_filter_count` is emitted only when the drop is non-zero. At SweCham (0 unsubscribes, 0 opt-outs at cutover) the series never appears — identical to the signal produced if step 4b were deleted. The catalogue row's stated alarm cannot fire. **This is the one finding that is live at today's scale.** | Emit unconditionally when the filter runs (`.add(0)` still registers the series), or add `..._filter_evaluated_total`; then correct the catalogue row. |
| P3 | `docs/observability.md` § 22.2 | New admin surface with a per-request whole-list fetch, offset pagination and a `router.refresh()` per toggle, and no latency SLO row and no latency instrument. A 10× regression would move neither new counter. | Add `SLO-F7-011 /admin/marketing/audience TTFB p95 < 500 ms`, Speed Insights, pre-flight preset as the reference query. |
| P4 | `list-marketing-audience.ts:124-130,163` · `drizzle-member-repo.ts:1330-1339` | Any `state` filter loads the whole `marketing_unsubscribes` list and binds it into two queries per render. The set is bounded by **time**, not tenant size (it also accrues bounces, complaints, `admin_added`, and addresses that were never contacts), and `<> ALL($1::text[])` cannot use an index. Empty at SweCham today (the bind is omitted entirely when the set is empty). | Add `broadcasts.suppression_list_size{tenant}` to the existing gauges cron — the leading indicator. Escalate to a SQL-side anti-join only if it crosses; that is a cross-module change needing a plan entry. |

**Spec compliance**

| ID | Where | Defect | Recommendation |
|---|---|---|---|
| A1 | `submit-broadcast.ts:580-606` | `droppedByPreference` has zero consumers, so FR-022a's "tell the sender how many were dropped" never happens. | Record in spec/tasks that PR-D ships DROP-without-TELL as an interim owned by T089 (PR-C). |
| A2 | contract + integration tests | FR-030b replay and 429 proven only against mocked `idempotency` and `rateLimiter`; the live file mints a fresh key every call. | One live case: POST twice with the same key, assert stored body + exactly one audit row. |
| A3 | `marketing-state-badge.tsx:58` | FR-031b's 10-code vocabulary reaches only the audience table; the member page explains "why" through a second, overlapping `state.*Aria` vocabulary. | Point the member page at `shared.marketing.reason.*`, or amend FR-031b. |
| A4 | `set-contact-marketing-opt-out.ts:175-177` | Staff audits use `related_member_id`; FR-053 still says "member id" and its rationale is written only for a different event. | AMENDMENT block at FR-053 (PR #335 precedent). |
| A5 | `contracts/contact-marketing-api.md` §2 | Contract lists only 409 `suppressed`; the route returns 400×2 / 404 / 409×2 / 429 / 503×2 / 500. Its "per contact" wording for the portal GET would violate FR-032 — the code is right, the doc is wrong. | Rewrite §2. |
| A6 | `drizzle-contact-repo.ts:117-152` | The carry-forward is in literal tension with FR-027 ("no contact starts switched off") and is authorised by no FR or AMENDMENT — only by the review ledger. | Add an AMENDMENT at FR-027. Independently raised as C7. |
| A7 | `admin/marketing/audience/page.tsx:74` | `notFound()` behind `FEATURE_F7_BROADCASTS` gates a page FR-035 calls "permanent … MUST be reachable"; authorised only by the ledger. | Amend FR-035 or drop the gate. Low live impact — F7 is ON in prod. |
| A8 | `resolve-segment-recipients.ts:141` | The live dispatch audience changes with no feature flag; FR-045's rollback story covers only the audience widening. | Do **not** add a flag — flag-off would re-send to people who objected. State in FR-045 that this ships unflagged by design; rollback is a code revert. |

**Test quality**

| ID | File:line | Defect | Recommendation |
|---|---|---|---|
| T2 | `contact-marketing-routes.test.ts:268-270` | `body.contacts.filter(…).every(…)` runs on an empty array — the seed creates one contact, so `.every()` is vacuously true and a serialiser leaking `marketing` on every contact would pass. The page-level gate and its `catch { suppressed = 'unknown' }` are stubbed and unasserted. The real protection exists at `profile-marketing.test.ts:326-346`; these two just *read* like proof. | Seed a second contact via `addSecondaryContact`; assert exactly one toggle node, on the own row; add the `isSuppressed` throw case. |
| T3 | four test/domain files | Four docstrings contradicted by the code beside them, all surviving cycle 15's "every claim now matches" sweep: `contact-marketing-deps.test.ts:1-10` (states a reason its own body comment at `:74-79` calls false), `set-contact-marketing-opt-out.test.ts:13` (`not_found` vs the real `removed`), `contact-block-marketing.test.tsx:46` ("pre-108 two-state badge" over a mock of the new 5-state one), `marketing-audience-query.test.ts:157` ("four contacts", asserts 5). Ten other docstrings were checked and hold. | Correct the four. |
| T4 | `.github/workflows/integration-smoke.yml` | `marketing-audience-query.test.ts` — which seeds 20,000 rows over 200 sequential transactions and asserts wall-clock `< 3000 ms` twice — is now in a required 20-minute job on a cold Neon branch with no perf-exemption pattern. | Keep the FR-052 cross-tenant and filter cases in smoke; move the `aud20k` describe to the nightly sweep. |
| T5 | `contact-marketing-routes.test.ts:99` | Same gap as A2, from the test side: the live file mints a fresh Idempotency-Key per call, so the claim protecting the audit trail has no unmocked evidence anywhere. | As A2. |

### 🟢 Suggestions (26) + INFO/NIT (3)

Recorded in full in the five pass reports. Highest-value: **C4** (`setMarketingOptOutInTx` is the only new tenant-scoped WRITE without the explicit `tenant_id` predicate its own sibling READs gained in this PR — RLS FORCE covers it, and `TenantTx` carries no slug, so this is a signature change and a follow-up ticket) · **C11** (`MarketingSwitch` on the member page is not gated by `f7Broadcasts` while the audience page is — staff can toggle a preference no send path reads) · **S3** (CHK019's "timing-safe" wording) · **A14** (FR-033's F4 leg rests on structural proof only — one seeded opt-out in `primary-contact-read-agreement.test.ts` converts it to behavioural) · **A15** (no CI gate keeps the F3 audit-event count honest; `completeness.test.ts:82` also asserts 37 but is the unrelated F1 tuple — a trap) · **P8** (metric names in the catalogue use dots, the instruments emit underscores; a dashboard query copied from the doc returns nothing) · **T6** (integration cases deliberately pass state between them; safe only because Vitest runs a file serially).

---

## Spec coverage matrix

**39 items counted** = 26 FRs in PR-D scope + 13 acceptance scenarios (US4 AS1–9, US6 AS1–4). Out of scope and excluded: FR-001…021, FR-023/024/026/028/029, FR-040…045, FR-054 (PR-A/B/C).

- **FR: 23 IMPLEMENTED · 3 PARTIAL · 0 MISSING**
- **AS: 12 PASS · 0 FAIL · 1 UNVERIFIABLE**
- **94.9%** (PARTIAL and UNVERIFIABLE at ½). Strict floor, counting them 0: **89.7%**.

| ID | Status | What is missing |
|---|---|---|
| FR-022a | PARTIAL | Drop-at-dispatch works on every segment kind; the "tell the sender" leg is unimplemented (T089 / PR-C). |
| FR-030b | PARTIAL | Rate limit + Idempotency-Key wired correctly; replay and 429 proven only against mocks. |
| FR-031b | PARTIAL | The 10-code vocabulary is exact but consumed on 1 of the 3 surfaces the FR names. |
| US4-AS9 | UNVERIFIABLE | LCP < 2.5 s at 20k contacts never measured (e2e has no CI job). Query side proven: ~0.5 s against a 3 s budget. |

**No in-scope requirement is unimplemented.** Scope creep, both benign: the carry-forward behaviour (A6) and 103 lines of collateral component refactor including a fix to a **pre-existing** axe `dlitem` violation on `/portal/profile` (A13).

### Architecture conformance — Constitution Principle III

Domain purity **PASS** · Application free of ORM/framework/React **PASS** · Infrastructure types do not leak upward **PASS** · cross-context imports via public barrel only **PASS** · **no barrel cycle** (members gained zero broadcasts imports; the adapter sits in the composition root `src/lib/contact-marketing-deps.ts`, which Principle III exempts) **PASS** · client-reachable `marketing-audience-filter.ts` is `import type`-only over the members barrel **PASS** · tenant scoping through `runInTenant` `tx` with an explicit `tenant_id` predicate on all three new queries **PASS**.

---

## Test coverage assessment

**Verified strong.** Fail-closed and degraded paths are both tested in both directions: the suppression lookup re-throws with a class-only log and a metric on all three ops, the bridge throw rejects the resolve, and every `state` filter answers empty on a degraded read while the unfiltered view still lists every row as `unavailable`. FR-053a has four independent no-`@` assertions, and `errKind` returns `e.constructor.name` only — never `.message` — so the log-line leg holds structurally as well. The dispatch filter is proven on live Neon through the real bridge on all four segment kinds.

**Gaps, in priority order**: T1 (blocker) · the two mock-only legs of FR-030b (A2/T5) · US6-AS3's vacuous assertion and unasserted page gate (T2) · route-level cross-tenant and `409 idempotency_conflict` never produced route-side (A16) · FR-033's F4 leg structural-only (A14) · the `on → included in the next send` round trip is unit-level only (US4-AS3).

**Coverage floors are thinner than they look**: `src/app/**` and `src/components/**` are outside `coverage.include` entirely, so both route handlers, the audience page, both switches and the badge have **no floor at all**; `list-marketing-audience.ts` — the FR-027a/FR-031a decision surface — sits at 97.61% branch with no per-file pin, one pin away from the same failure as T1.

---

## Metrics

| | |
|---|---|
| Files in scope | 155 (+11,233 / −346) |
| Files read by reviewers | 215 file-reads across five passes (35 · 48 · 31 · 56 · 45), plus 7 static gates and 2 suites re-run live at this HEAD |
| Findings | **1 blocker · 20 warnings · 26 suggestions · 3 info/nit = 50** |
| Spec coverage | 94.9% (strict 89.7%) — 0 requirements unimplemented |
| Architecture violations | 0 |
| Security blockers | 0 |
| Required CI checks | 5 pass · **1 fail** (coverage) · integration smoke pending at time of writing |

---

## Recommended actions

**Before merge — blocking**

1. **T1** — add the missing `RepoError`-without-`cause` case; do `list-marketing-audience.ts:178` at the same time. Re-run `pnpm test:coverage` (the pipeline exit code, not a filtered one) and confirm the required check turns green.

**Before merge — should fix**

2. **P2** — make the opt-out filter counter emit when it runs, and correct the catalogue row. At SweCham's current data this is the difference between a metric that works and one that cannot ever fire.
3. **C1 + C2** — one repo change closing both: a shared `findCarriedSelfOptOut` used by all three insert paths, ordered by the latest row for the address rather than the latest row carrying an objection. Add the two missing directions to `contact-marketing-opt-out.test.ts` (create-member path must carry; opt-in then re-add must not resurrect).
4. **P1** — correct the `0294` header and the `schema-contacts.ts` mirror before PR-C reads either and drops the index.
5. **S1 + S3** — re-stamp the three co-signs at merge HEAD with the src-diff evidence; reword CHK019 to "non-disclosing 404", which is what the code does and what the tests prove.
6. **A4–A8** — one docs commit: AMENDMENT blocks at FR-027, FR-035, FR-045 and FR-053, and contract §2 rewritten to the routes' real responses.
7. **T3** — correct the four rotted docstrings.
8. **T4** — move the 20k-row perf describe out of the required smoke job.

**Follow-ups, not blocking**: C3 (audit row for a contact born opted-out) · C4 (explicit `tenant_id` on the new write — signature change) · C11 (flag-gate the member-page switch) · A1/A2/A3/T2/T5/A14/A16 (test and vocabulary gaps) · P3/P4 (SLO row + suppression-list gauge) · A15 (register F3 in the audit-count gate) · P8 (catalogue naming).

---

## Verdict

### ❌ CHANGES REQUIRED

One blocker (**T1**), confirmed by both a local coverage run and the required CI check, standing between this branch and a merge — and one test case away from resolved. No security, isolation, correctness-of-money, or architecture defect was found by any pass.

The honest summary of this gate: **the feature is right; a number of the sentences written about it are not.** Twenty warnings, most of them documentation and claim drift, several written during the very cycles that were closing that same class — including two written by this session today. That is worth carrying forward as the lesson, not just the fix list.

**Next**: fix T1, then re-run this gate. Once green: `/speckit.ship`.
