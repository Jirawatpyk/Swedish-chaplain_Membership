# 108 Phase 9 cutover — review round 3 (`/code-review`, 10 finder angles + gap sweep)

**Scope** `git diff origin/main...HEAD` on `108-phase9-cutover` — 54 commits, 143 files, +8,563 / −12,696.
**Run** one forked `/code-review` agent, 10 finder angles plus a gap sweep, top five verified by hand by the agent; **findings 1, 2, 4 re-verified by me** before this file was written (see the `V` column).
**Rounds 1 + 2** `review-20260908-223000.md` (S1–S56) · `review-20260909-081200.md` (R2-1–R2-56).

---

## Why this round is not a duplicate of round 2

Round 2 re-reviewed **the round-1 fixes** and found five defects they introduced. This round was
launched independently, saw the branch cold, and its gap sweep grepped both prior files. The
result is a clean partition:

| | Findings | Meaning |
|---|---|---|
| **Already logged, still unfixed** | 3 · 6 · 7 · 9 · 12 · 13 · 10 | Three reviewers now agree. Not new information — **confirmation of severity**. |
| **Absent from both rounds** (grep: 0 hits) | **1 · 2 · 4 · 5 · 8 · 11 · 14 · 15** | Eight defects two review rounds walked past. |

Finding 13 **is** round 2's group A (R2-1 / R2-44) reached from a different direction. Finding 3 is
round 2's R2-8/R2-27 taxonomy strand. That two independent passes converge on the same two
strands is the strongest signal in this file about what to fix first.

---

## The sharpest one, stated once

> **Round 1 closed S5 with an arm that matches a kind the resolver never produces.**

`resolve-segment-recipients.ts:422` returns `err({kind:'broadcast_empty_segment_blocked'})`.
`broadcasts-deps.ts:942` passes it through raw (`return err(resolved.error)`).
`build-audience-tick.ts:762` matches **`broadcast_audience_post_suppression_empty`** — a kind
produced only by the legacy `dispatch-scheduled-broadcast.ts:485`.

So `mapResolveError` falls to `dispatch.server_error`, the cron buckets it `retryable`, and an
approved broadcast whose audience emptied stays `approved` and is re-claimed every five minutes
for ever — with no terminal state, no audit row and no member email. **That is verbatim the
defect `onResolveFailure`'s own docblock says it exists to remove.** It was removed for
`too_large` only.

Two things made it invisible:

1. `ResolveAudienceError` carries a `| { readonly kind: string }` member (`build-audience-tick.ts:166`),
   so comparing against an impossible literal **typechecks**.
2. Every unit test stubs `resolveRecipients` with the phantom kind — so the suite exercises dead
   code and passes green.

The lesson is not "check your string literals". It is that **a union with an open `string` member
cannot enumerate its own arms**, and once that escape hatch exists the compiler stops being the
thing that finds this class. Same shape as the `return _exhaustive` family already in this repo's
memory.

---

## Ledger

`V` = re-verified by me in source this round, not taken from the report.

### Blocks merge

| # | V | Where | Finding |
|---|---|---|---|
| **3-1** | **V** | `resend-broadcasts-gateway.ts:363` | **`sdk.broadcasts.send(id, payload)` DISCARDS the idempotency key.** `node_modules/resend/dist/index.js:252-259` calls `this.resend.post(path, {scheduled_at})` — **two args**; `:599` sets `Idempotency-Key` only from `post()`'s **third** `options` argument (which the sibling `create` does pass). The `as Parameters<…>[1]` cast suppressed the tsc error, and the comment one line above states the opposite: "Resend SDK accepts idempotencyKey as a request option." So the two-tick send path has **no dedupe at all**, and `build-audience-tick.ts:511`'s S8 note asserts a 409 replay guarantee that does not exist. Across ticks `createBroadcast` mints a NEW id first, so the second send is a different resource with no key: an unambiguous duplicate to the whole audience, repeating every 5 min. Body unchanged from `origin/main`, so this is **also live on the legacy leg in prod today** — but this branch builds a re-entrant replay loop on top of it. |
| **3-2** | **V** | `build-audience-tick.ts:762` | **S5 is still open for the empty audience** — the arm names a kind the resolver never returns. See above. |
| **3-13** | | `cron/broadcasts/dispatch-scheduled/route.ts:258` | No arm for `broadcast_not_found` / `broadcast_invalid_state_transition` → `default` → `unknown_error` + `cronUnknownErrorCount`, the counter this file documents as the page-on-call enum-drift signal. A routine admin cancel pages on-call. **= round 2 group A (R2-1 / R2-44), reached independently.** Also: the retryable arm increments `dispatchResolveFailedTotal` (documented meaning: the audience could not be RESOLVED) for every Resend 5xx/429, with no log line — a provider outage points the operator at Neon. |
| **3-4** | **V** | `tests/integration/broadcasts/recipient-count-routes.test.ts:128` | **RED TEST on this branch.** The assertion restates `isF71aUs1Enabled()`; `configuredAudienceCeiling()` (`broadcasts-deps.ts:147`) reads `isF7ImportAudienceEnabled()`. Under `.env.local` it computes 50,000 and the route returns 500. Green in CI only because both flags default false and the two expressions happen to agree at 500 — **so it goes RED on the very flip it was written to protect.** Same staleness in `broadcasts-deps.ts:131`, `src/lib/env.ts:628`, `.env.example`. |
| **3-8** | | `build-audience-tick.ts:552`, `:650` | `safeAuditEmit` called with a **LIVE tx**. It swallows the INSERT failure, so an aborted (25P02) transaction commits as a ROLLBACK tag Drizzle does not inspect: `attachResendIds` + `applyTransition('sending')` are silently discarded while the function returns `ok({kind:'sent'})` **after the mail went out** — and the next tick re-enters `confirmImport` and sends the whole broadcast again. Every other `safeAuditEmit` in the module passes `tx=null`; legacy uses raw `deps.audit.emit(tx, …)` at `:1012` precisely so a failed emit tears the mutation down; the rule is written at `manage-image-allowlist.ts:122-128`. Second half: `broadcastsMetrics.auditEmitCount(…)` fires **unconditionally** on the next line, so the series silently changed meaning from "rows written" to "emits attempted". |
| **3-15** | | `build-audience-tick.ts:734` | `onResolveFailure` **awaits `failTerminally(...)` and discards its Result**, returning `err(mapped)` whether or not the terminal write committed — while the `malformed_segment` arm four lines below does `return failTerminally(...)`. Two arms of one function disagree about whether the terminal write may fail silently. When it fails the caller still reports `broadcast_audience_too_large` → cron counts `permanent_failed` ("finished, nothing to do", so no alert) while the row is still `approved` and re-refused every 5 min. |
| **3-5** | | `build-audience-tick.ts:475` | The completion rule compares the **import job's** row count against the freshly-resolved count, never the audience's actual membership — and imports are `on_conflict=upsert` into a **reused** audience with nothing ever removing a contact (`unsubscribe-recipient.ts` makes no gateway call). Tick 1 imports {A,B,C} and dies before `attachAudienceImport` commits; C unsubscribes; tick 2 imports {A,B} into the same audience: `failed=0`, `parts==total==2`, `total==resolvedCount==2` — every clause passes — and the send reaches **all three**. GDPR Art. 21 / PDPA §32. `getAudienceContactCount` exists on the port and is never called here. |

### Blocks the flag flip

| # | Where | Finding |
|---|---|---|
| **3-11** | `scripts/inventory-broadcast-outbox.ts:126` | **The one pre-cutover operator gate argues against acting.** Header + exit-1 message describe Phase 9b's split/batch model that `ca51f59a1` deleted on this same branch, and name `FEATURE_F71A_US1_PAGINATION` as the ceiling knob after the gate moved to `FEATURE_F7_IMPORT_AUDIENCE`. It prints "no action needed" for exactly the rows this merge refuses terminally (accepted ceiling 5,000 → 500, unflagged). The header is already marked "CORRECTED 2026-09-08" — **second time the restated rule is wrong, now in the opposite direction.** Same stale flag in `src/lib/env.ts:628`, `.env.example:386`, and `docs/runbooks/broadcast-audience-build.md` step 6 — a paragraph this branch EDITED, which contradicts its own § B remedy 2 two screens later. An operator following step 6 sets a Vercel var (= a prod redeploy on this repo) and the ceiling does not move. |
| **3-6** | `build-audience-tick.ts:526` | Nothing holds a lock across the send; the post-send `withTx` has no `BroadcastConcurrentMutationError` guard. `lockForUpdate`'s advisory lock is released at `:225`, before every gateway call — so the cron header's claim that it "survives the entire dispatch tx" is false here. A cancel in the window leaves the mail delivered, the row `cancelled`, `resend_broadcast_id` NULL for ever (every later webhook dropped → bounces never reach suppression), and pages on-call. *Already in round 2.* |
| **3-7** | `build-audience-tick.ts:290` | No FR-021 wall-clock retry budget on the import path. `IMPORT_STUCK_AFTER_MS` is only evaluated inside the `job.status !== 'completed'` branch, which needs a **successful** poll. A tick-1 failure never persists `audience_import_id`, so the T106 gauge and the documented rollback drain query are **structurally blind** to it. `dispatchBudgetExhausted` — the page-on-call metric whose steady state is 0 — can never fire with the flag on. *Already in round 2.* |
| **3-9** | `build-audience-tick.ts:487` | `markAudienceImportCompleted` commits in its own tx **before** the work it certifies; nothing in `src/` READS `audience_import_completed_at`; the write carries no status precondition. A completed-but-unsent row matches **neither** operator predicate — so an operator following the documented drain sees ZERO rows in flight, flips the flag off, and hands the row to the legacy path, which reuses `resend_audience_id`: the "deliver to a half-built audience" outcome the runbook warns about, **reached by following the runbook**. *Already in round 2.* |
| **3-12** | `build-audience-tick.ts:630` | The nine-value failure taxonomy reaches no surface a human watches: `'reason'` is in `logger.ts:332` REDACT_PATHS (observed: `reason:"[REDACTED]"`), the metric dimension is the literal `'app_error'` where legacy calls `phaseToFailureReason(phase)` — even though the closed union already offers `'timeout'` (= `audience_import_stuck`) and `'resend_403'` (= the measured Free-plan cap) — and 7 of 10 reasons render as one generic sentence in the member email. `broadcasts-dispatch-failure.md`'s "group `failure_reason`" step is dead. *Already in round 2.* |
| **3-3** | `_classify-thrown.ts:56` | The shared classifier flattens a 4-way gateway taxonomy to a boolean, so `idempotency_conflict` (409) and `resource_missing` (404) both become permanent → `failTerminally`. Legacy routes the first to a **success-replay** arm advancing to `sending` (`:710`) and the second to `broadcast_resend_resource_missing` which **deliberately does not email the member** (`:915-935`). Import path writes a false `broadcast_failed_to_dispatch` row and emails "your E-Blast did not go out"; `countForMemberQuota` excludes `failed_to_dispatch`, so a delivered broadcast also refunds the quota slot. **Coupled to 3-1:** with no key transmitted the send-side 409 arm is unreachable today; fix 3-1 alone and this path activates, converting a silent duplicate into a false failure email. **The two must be closed together.** *Already in round 2 as R2-8/R2-27.* |
| **3-10** | `drizzle-broadcasts-repo.ts:1548` | The new erasure UNION arm is a **cartesian product**: `CROSS JOIN unnest(emailArray)` pairs every address with EVERY broadcast carrying an import id, not only those that held the member — and each pair becomes one serial Resend DELETE. A 3-address member in a tenant with 200 import-built broadcasts = 600 sequential round-trips (~4 min) inside an Art. 17 erasure, blowing the budget mid-loop. Fix is `AND b.resend_broadcast_id IS NULL`; narrowing on `audience_import_completed_at IS NULL` would be **wrong** (that stamp lands before `createBroadcast`). Also: `broadcasts-gateway-port.ts:159` says "For erasure use `deleteContactGlobally`" and makes it a REQUIRED member 13 doubles implement, while its only erasure caller says at `subprocessor-erasure-adapter.ts:68` that it is "deliberately NOT called here". The adapter's reason is sound; the port misdirects a compliance reader. *Already in round 2.* |
| **3-14** | `scripts/seed-dev-secondary-contacts.ts:69` | The "refusing to seed unroutable addresses" guard is **mis-anchored and lets through what it exists to block**: `example\.(com|org|net)$` is anchored immediately after `@`, so `qa@sub.example.com` slips, and the bare `test`/`localhost`/`invalid` alternatives are unreachable because the format check on `:62` already requires a dot. Verified by running the regex: `qa@mail.invalid`, `qa@foo.test`, `qa@sub.example.com` all UNBLOCKED. **Dev shares production's `RESEND_BROADCASTS_API_KEY` and `BROADCASTS_FROM_EMAIL`** (the script's own header says so), so the next dev dispatch pushes those into the shared audience and the bounces land on the production domain's reputation — the one combination the filter is for. It also reaches into `tests/helpers/db-host-guard.ts` instead of `scripts/lib/seed-target-guard.ts`, and the two prod guards have already diverged. |

### Below the 15-finding cap, all verified by the agent

- `check:f71a-schema` still certifies **deleted** batch DDL.
- `broadcast_batch_manifests` is orphaned in `schema.ts` and still `LEFT JOIN`ed.
- `tests/e2e/broadcasts/pagination-batch-breakdown.spec.ts` + `scripts/seed-partial-broadcast.ts` target deleted components and **skip rather than fail**.
- `updateEstimatedRecipientCount` and `deliverablePerTick` ship as **required** port members with zero production callers; fixtures pin the latter to 5,000 vs the real 500.
- `broadcast_send_started` silently drops `scheduledFor` / `actualSendAt` / `delaySeconds` into an append-only table **under a comment claiming parity**.
- 2 lint warnings · unreachable `variant="halt"` branch with ~33 orphaned i18n strings · duplicated `resolverDeps` literal · a new `.orderBy` whose 26-line justification cites **three files this branch deleted**.

---

## Gates as of this round

Green: `pnpm typecheck` · `lint` (0 errors, 2 warnings) · 1,021 broadcasts unit · 1,937 contract · 18 `check:*` · the new live-Neon import suites.

Two exceptions, both named rather than smoothed over:

- **`tests/integration/broadcasts/recipient-count-routes.test.ts` is reproducibly RED** under the dev/prod flag configuration — finding 3-4.
- `check:plan-divergence` fails on leaked `test-swecham-*` tenants on the shared dev Neon branch — integration-test residue, not a branch defect.

Out of scope and untouched: the four working-tree `.claude/agents/*.md` edits and the reviews files.

---

## Fix order — merged with round 2's groups A–O

The two rounds are not two queues. Where they name the same code the fix is one edit:

| Order | Group | Closes |
|---|---|---|
| **1** | **A** (round 2) + **3-13** | R2-1 · R2-44 · 3-13 · half of R2-10 — the only strand **live at merge** on the legacy leg |
| **2** | **3-1 + 3-3 together** | the idempotency gap and the 409/404 flattening. Neither may land alone: 3-1 alone activates 3-3's false-failure path; 3-3 alone leaves the duplicate send |
| **3** | **3-2** | one arm + delete the `\| {kind: string}` escape hatch so the compiler can enumerate again |
| **4** | **3-15** | `return failTerminally(...)` in both arms |
| **5** | **3-8** | `tx=null`, and move `auditEmitCount` inside the success path |
| **6** | **3-4** | one flag name in four places |
| **7** | **3-5** | `getAudienceContactCount` on the completion rule, or a fresh audience per attempt |
| **8** | **3-11 + 3-14** | the two operator-facing scripts — both actively mislead |
| **9** | round 2 **B–O** + **3-6 · 3-7 · 3-9 · 3-10 · 3-12** | as ordered in `review-20260909-081200.md` |
| **10** | the below-cap sweep | deletion debris + the two lint warnings |

---

## What this round says about the last two

Round 1 found 56, round 2 found that round 1's fixes introduced 5 defects, and round 3 — cold, no
knowledge of either — found **8 more that neither had seen**, including a red test and a
never-transmitted idempotency key.

The pattern across all three is one thing: **every defect that survived was one where a comment,
a docblock, or a test asserted the behaviour instead of the code providing it.** 3-1's comment says
the SDK accepts the key. 3-2's docblock says the refusal is terminal. 3-9's runbook says the drain
is safe. 3-11's header says "CORRECTED". The tests that should have caught 3-2 stubbed the phantom
kind and went green.

**So the rule this round adds to the two already in `README.md`: a claim in prose is not covered by
a green suite. Grep the assertion, not the sentence.**

---

## Fix status — Fix All, 2026-09-09

Thirteen commits, each one RED-test-first or mutation-proved, and each verified
against `tsc --noEmit` + the touched suites before the next began. Mutation proof
was used wherever the fix was written before its test, because a green run on a
test written after the fix proves only that the test agrees with the code.

| # | Commit | Closes | Proof |
|---|---|---|---|
| 1 | `aeb8d42ce` | R2-1 · R2-44 · **3-13** · half R2-10 | 5 contract cases RED→GREEN; 6 unit cases mutation-proved (4 killed, 2 controls survive) |
| 2 | `9650d7577` | **3-1** + **3-3** (= R2-8 · R2-27) | header-on-the-wire test; restoring the original SDK call kills 2 of 4 |
| 3 | `a381065b5` | **3-2** + **3-15** | phantom-kind flip turned 2 cases RED before any source change |
| 4 | `24cfc0509` | **3-4** | the red live-Neon file now passes under the dev/prod flags |
| 5 | `4a18fb2e0` | **3-8** | tx-identity assertions; both directions mutation-proved |
| 6 | `c43717737` | 2 lint warnings | `pnpm lint` 0/0 for the first time on this branch |
| 7 | `cd104d733` | **3-11** + **3-14** | 26 unit cases pinning the three verified addresses by value |
| 8 | `8537506ce` | **3-10** (half) + **3-12** | derived reason list + distinctness guard, mutation-proved |
| 9 | `b8ea5016a` | below-cap sweep | 18-file pure deletion; −514 lines |
| 10 | `391076cc0` | **3-6** + **3-9** | id-write and stamp-ordering both mutation-proved |
| 11 | *this* | **3-5** + **3-7** | drift check and budget both mutation-proved |

### Two findings were rejected on evidence, not deferred

- **3-10's cost model.** It rests on "nothing sets `audience_deleted_at` for a
  successfully SENT broadcast". `TERMINAL_BROADCAST_STATUSES` includes `sent`,
  `cleanup-audiences` runs **every 15 minutes** with a **1-hour** grace, and it
  stamps that column — so the live set is roughly the last hour of terminal
  broadcasts plus in-flight ones, not a tenant's lifetime of sends. The
  suggested narrowing `AND b.resend_broadcast_id IS NULL` would also open a real
  Art. 17 gap: a broadcast already sent whose webhooks have not arrived has that
  column set and zero delivery rows, so BOTH arms would go blind. The port
  docblock contradiction it also named **was** real and is fixed.
- **`check:f71a-schema` certifying deleted DDL.** It certifies DDL that is still
  THERE, and the table still holds rows — dev 2 (both with a
  `provider_audience_id`), prod 0 with 0 broadcasts. Those ids are reachable via
  the erasure/reclaim `LEFT JOIN`, so dropping the join, the declaration or the
  assertion would leak Resend audiences for no gain.

### What the fixes themselves surfaced

Three things that neither the reviews nor I had seen, each found by the act of
writing the test rather than by reading:

1. **My first 3-15 fix was wrong and 4 tests said so.** `failTerminally` returned
   `err(error)` on BOTH paths, so committed and not-committed were
   indistinguishable — there was nothing for the caller to check. The real fix was
   to make the cleanup branch return `dispatch.server_error`.
2. **`getAudienceContactCount` was unstubbed in both harnesses**, so `viaGateway`
   caught a "not a function" TypeError and my new 3-5 check silently took its
   unverifiable branch — 60 tests passed without exercising it once. An unstubbed
   port method is an unexercised branch that looks like a covered one.
3. **`deliverablePerTick: 5_000` appeared 40 times in one test file** against a
   production value of 500. The field was never read, so the number meant
   nothing — but that is what a fixture drifting 10× from production looks like
   before anyone notices it is dead.

### Still open, deliberately

- **S8's across-tick half** — `createBroadcast` carries no idempotency key, so a
  tick that dies between the send and its transaction mints a NEW Resend resource
  and sends to a different path where no key can dedupe. Closing it means
  persisting `resend_broadcast_id` BEFORE the send, a change to the two-tick
  contract. **A flip blocker, not a merge blocker** — the whole use case is dark
  at merge (`FEATURE_F7_IMPORT_AUDIENCE` defaults false).
- **The unreachable `variant="halt"`** on `cancel-broadcast-action.tsx` (zero
  renderers) and its clean 8-key × 3-locale `admin.broadcasts.haltDialog` subtree.
  A UI prop-shape change whose name collides with the LIVE member marketing halt,
  so it gets its own commit rather than riding along with a deletion batch.
- **FR-043's 400 ms @ 5,000 band**, unmeasured, and the 3 s @ 20,000 budget
  asserted only through `ciScaled(3_000)`. Both must be measured from `sin1`.

---

## Addendum — the fix phase produced three more defects, same class

Written after the queue closed, because the base rate held and the record should
say so plainly.

Round 1's 15 fix commits introduced 5 defects (round 2 found them). Rounds 2+3's
21 fix commits introduced **3**, all of the class this scope exists to remove —
**a claim that is not true of the code** — and all of them written by me AFTER I
had fixed the identical class elsewhere:

| # | What I wrote | The claim that was false |
|---|---|---|
| 1 | the FR-021 budget (fix for 3-7) | a Neon/RLS fault produced reason `retry_budget_exhausted`, whose member sentence reads *"our email provider was unreachable for over an hour"*, and fired `dispatch_budget_exhausted{sub_kind="api"}`. Two invented causes — one in a member's inbox, one on an alerting series. **Two commits after I made `body2` conditional for exactly this reason (R2-13).** |
| 2 | the CAS precondition (fix for R2-40) | `BroadcastConcurrentMutationError(..., 'unknown' as never)` — a fabricated status, cast past the compiler, ten lines from the helper that probes the real one. |
| 3 | `budgetEpoch` (fix for 3-7) | `broadcast as unknown as {…}` where `Broadcast` already declares all three fields. The cast was never needed — and a cast is how a wrong assumption about a type survives `tsc`. That is 3-1's mechanism, reproduced. |

**None of them was caught by a gate.** `tsc`, eslint and 13,911 unit+contract
tests were green on all three. Defect 1's own comment argued against defect 1 in
the line above it — the same self-contradicting-prose shape this file named as
the reason eight findings survived two review rounds.

What did catch them: a review pass over the FIXES. Which is rule 1 in
`README.md`, arrived at from the other direction for the third time.

**A fourth was caught by re-running, not reading.** The live-Neon suites touching
`sql\`now()\`` and the `RemoveContactOutcome` doubles had not been run since those
landed; `audience-import-repo.test.ts` failed on a POSITIVE CONTROL asserting the
pre-R2-3 scope. It was replaced by the control for the bound that took its place,
plus a case proving the legacy leg is now covered. `tsc` passing on a
`.set({col: sql\`now()\`})` is not evidence that it writes.
