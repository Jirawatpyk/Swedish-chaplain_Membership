# 108 PR-C — Review round 2 (`/speckit-review` + the T092 review stack), 2026-09-07

**Branch**: `108-pr-c-audience-1n` · **PR**: #346 · **HEAD reviewed**: `deb5a1c09` · **Base**: `a586351cd` (origin/main)
**Reviewers (12, all Opus, all read-only)**: `/speckit-review` ×6 — code · tests · errors · types · comments · simplify — plus the six T092 names — `security-engineer` · `reliability-guardian` · `performance-slo-guardian` · `observability-instrumentor` · `enterprise-ux-designer` · `i18n-translation-reviewer`.
**Why the T092 six ran only now**: T092 was ticked `[x]` on `3a23ec354` with only its gate half done; none of the six named agents had been run and none of the three checklists had been co-signed for PR-C. Row 18 disclosed "e2e NOT run" and said nothing about the review stack. This round is the first time those six have seen the branch.

**CI at review time**: all 6 required checks green (integration smoke 15 m 01 s, including the two PR-C live tests added in `d9e68a33d`).

## Verdict: CHANGES REQUIRED

0 BLOCKER from any single reviewer. What blocks is convergence: six independent reviewers landed on one unfixed leg, four on one missing bound, and the UI layer — which no reviewer had seen before this round — is **live on merge with the flag OFF** and carries six HIGH findings.

The round-1 findings are closed as classes where they were fixed (H-1 bridge throws: every call site walked, no unhandled rejection, memoizer never caches a rejection; H-2 ceiling: composition root pinned against a six-row flag matrix that is not a tautology; the three cron contract tests mirror the routes' imports key-for-key). Three of round 1's classes survive **one hop away** from where they were fixed — which is the pattern this round was asked to hunt.

## Retraction

Earlier in this session I said the query-shape change (`105cce8a4`) made the 20k proof "faster" (3,698 → 3,350 ms). Two runs of **identical** code measured 3,350 and 3,143 ms — a 207 ms spread. A 348 ms delta at n=1 vs n=2 is noise. The correct framing: **3.1–3.4 s on this workstation, within noise of the 3.7 s already disclosed**; neither credit nor blame the correlated EXISTS. The PR body's figure is corrected in the same commit as the fixes below.

## Convergence table (the findings that matter most are the ones several reviewers reached independently)

| # | Finding | Reviewers | Severity (mine) |
|---|---|---|---|
| C1 | **The tier-empty guard landed on the two flag-ON reads only.** `findMembersBySegmentForBroadcast` (the `primary_only` leg = prod today) still drops the tier predicate on `tierCodes: []` and reads every active member. `buildSegmentFromBroadcast` (three copies) manufactures exactly that input with `?? []` on an unvalidated jsonb column. **And the guard has no test** — `grep "refusing to read every member" tests/` = 0; delete the four lines and nothing goes red. My own Iron Law violation in `a087296d8`. | types F-1, errors M, tests M-1, simplify (relay), reliability M-1, code LOW | **HIGH — before merge** |
| C2 | **The tier-empty refusal is laundered into a transient class.** It throws inside the repo `try` → `repo.unexpected` → bridge throw → `resolve.server_error` → the row stays `approved` and is retried every tick forever, now also incrementing `dispatch_resolve_failed_total` forever. The resolver's own HIGH-4c comment describes this failure mode as the reason the `try` was narrowed. | types F-2, errors LOW, simplify (relay), tests M-1, code LOW | **HIGH — before merge** (validate `tierCodes` at the `buildSegmentFromBroadcast` boundary so a malformed row never enters the retry path) |
| C3 | **The audience is materialised in full before the ceiling is consulted.** No page cap, no wall-clock budget; a 300k-contact tenant walks 60 pages into memory before `broadcast_audience_too_large` can fire → opaque OOM/504 with no typed refusal, no counter, no log. Behind an authenticated endpoint any member can call 30/min with no tenant-level limiter and no memoisation outside the crons. | errors M, security M-2, reliability L-1, code LOW, perf (memory OK to 50k) | **pre-flip** — see Contradictions §1: a bound AT the ceiling would reintroduce the bug tests M-2 pins |
| C4 | **`setMemberHalt` in `members-bridge.ts:356-360` still collapses every repo failure into `member_halt.member_not_found`** — 20 lines below the four lookups `0542599b9` fixed, and it is a WRITE path: admin clicks "clear halt" during a Neon blip → 404 "member does not exist", halt still set, nothing logged; the webhook auto-halt logs a false reason. | errors HIGH | **HIGH — before merge** |
| C5 | **The H-2 ceiling fix's rejected formula survives in seven places** — `contracts/broadcast-audience.md:16` (normative), `docs/runbooks/cron-jobs.md:389` (tells on-call prod's ceiling is 50,000 today; it is 5,000), `domain/audience-ceiling.ts:15-18`, `research.md:291`, `tasks.md:268,281`, `resolve-segment-recipients.ts:57`. | comments HIGH-1, code M-3, reliability L-4 | **before merge** |
| C6 | **Proxy (staff) form's `selfExclusionNotice` renders on member selection, not segment kind, ABOVE the picker**, and this PR strengthened its copy ("a member's contacts are excluded") — while custom list / attendee have no self-exclusion. Staff are told the opposite of what happens. The member form got `showsSelfExclusionHint`; the staff form did not. | UX H-1, i18n H3 | **HIGH — before merge (live, unflagged)** |
| C7 | **Attendee segment inherits the custom-list copy** ("each line below is one email… Maximum 100 entries") with no textarea following it, next to a real server count. The new comment in `submit-feedback.ts:83` says a fifth kind must not inherit — while the fourth does. | UX H-2, i18n L1 | **HIGH — before merge** |
| C8 | **`droppedByPreference` is measured at `:346` and discarded on both refusal paths**, justified by a comment at `broadcasts-recipient-count.ts:147` that says the resolver "refused before it measured" — false. A tier whose contacts all opted out shows "0 recipients" and never says N people objected (FR-022a). | types F-5, code M-2 | **before merge** |
| C9 | **`approved_overdue_count` latches** — the `GROUP BY` emits no row for a tenant at zero and `observeGauge` never deletes, so a resolved incident keeps reading 1 until the lambda recycles; the new "≥ 1 sustained 30 min" rule is a latch, not a level. Repo already ships the fix pattern (`forgetAutoInvoiceGauges`, "0 means 0"). | errors LOW, observability HIGH | **before merge** |
| C10 | **COALESCE pair independence leaves a GDPR back-reference erasure cannot reach.** `member_id` and `contact_id` COALESCE separately; the reachable path is a contact lookup that THROWS (Neon blip) → member fallback succeeds → row becomes `(M2, C1)` → erasing M1 (`WHERE member_id = M1`) misses `C1` forever. | security M-1, errors LOW | **before merge** (CASE expression + test) |
| C11 | **Route-helpers still hard-codes 5,000** in `message`/`messageThai` of the 422 envelope — contradicts `details.cap: 50000` in the same body when both flags are ON; the compose form falls back to `message` when i18n misses. A second definition of the ceiling (FR-042). | code M, i18n L7 | **before merge** |
| C12 | **`OrphanReason` consumer at `submit-broadcast.ts:706` is a negative filter** (`!== 'all_opted_out'`) — a fourth reason is admitted by default into the append-only `broadcast_member_missing_primary_contact_email`. The round-1 HIGH relocated to its consumer; `tsc` cannot see it. | types F-3 | **before merge** |
| C13 | **Portal compose page: a failed member lookup and a genuine miss take the same silent redirect** to `/portal/benefits` — which then THROWS on the same failed read, so the user crosses two pages to reach an error card; if the second read succeeds they land on benefits with no explanation. The admin page in the same commit got an explicit banner. | errors M, simplify #7, UX M-3 | **before merge** |
| C14 | **Docs describe the deferred import build as shipped**: `contracts § 4`, `quickstart.md:118` (Cutover 5) and `tasks.md:300` (T094) tell the operator to watch `broadcasts.audience_import_status` — a metric that does not exist. **An operator flipping the flag has no valid observation instruction.** `research.md:437`, `plan.md:124` likewise. | observability BLOCKER (CHK016/CHK013), code M-3 | **before merge** (operations.md cannot be co-signed until fixed) |
| C15 | `docs/observability.md:1317` unescaped `\|` breaks the label-enum row; `:1343` "13 alerts" over 15 rows. | comments, perf, observability | LOW — before merge |
| C16 | Runbook `:42` "only the page read produces `resolve.server_error`" — three reads are inside the `try`; step numbering off by one from `:24`; § C pairs `dispatch_resolve_failed` with `approved_overdue` but a `dispatch-batches` failure leaves the row in `sending` (signal = `stuck_sending_count`). | comments M-1/M-2, observability M | before merge |
| C17 | `droppedByPreference` over-counts the sender's own opted-out contacts (F3's count is not narrowed by `requestingMemberId`; self-exclusion removes them later). | types LOW, errors LOW, tests L-5 | before merge (pass `requestingMemberId` to the count, or subtract) |
| C18 | `countOptedOutContactsBySegment` not memoised per tick while `getContactsBySegment` is → count and audience describe different instants. | types LOW, code LOW, reliability L-3, observability LOW | before merge (third map, same key) |

## Single-reviewer findings accepted

**Perf (EXPLAIN-verified on dev Neon, not inferred)**
- **HIGH-1** `drizzle-member-repo.ts:493-497` — the `after_contact` cursor `(members.member_id, contacts.contact_id) > ($1,$2)` is a ROW comparison spanning **two relations**; `match_rowcompare_to_indexcol` needs every left column in one index, so it is structurally unqualifiable. Forced-index runs: Index Cond = `tenant_id` only. Every page after the first rescans from the tenant's first member — O(pages²·page_size). Fix: `member_id >= $1 AND (member_id > $1 OR contact_id > $2)` (null arm verified: a later member's NULL `contact_id` is admitted by the first disjunct, so orphan resume is preserved).
- **HIGH-2** `split-large-broadcasts/route.ts` exports no `maxDuration`; both siblings export 300. A platform kill runs no `catch` → the counter this PR added never increments.
- **HIGH-3** the 20k test's EXPLAIN assertion cannot detect the intended plan's absence (`/Nested Loop…Seq Scan/` does not match the measured `Hash Right Join → Seq Scan on contacts`; `/Limit/` passes with a full Sort beneath; only page-1 `all_members` is planned). Assert presence: index name, `Index Cond` carrying the cursor bound, no unbounded Sort; add a deep-cursor and a tier case. **This is why HIGH-1 shipped.**
- MEDIUM-4 `dispatch-batches` and `split-large-broadcasts` use the raw bridge, not `makeTickMemoizedMembersBridge`.
- MEDIUM-1 (0294 index unused at 133 contacts — **caveated by the reviewer; do not act on that measurement**), MEDIUM-2 (`band` label; 5,000-band FR-043 test; SLO-F7-013 has no § 22.3 row), MEDIUM-3 (no `maxDuration` on the count routes; pooler drops `statement_timeout`), LOW-1 (partial index for `marketing_opt_out_at IS NOT NULL`), LOW-2 (EXISTS computed on every row, read only on orphans).
- FR-043 from `sin1`: **likely holds (~1.4–1.8 s projected against 3 s) but UNVERIFIED** — the ~1.4 s RTT-independent residual has not been decomposed, and HIGH-1's rescans may be a large share of it.

**UX** (all live on merge; the flag gates only `estimateNote` copy selection)
- H-3 the ABSENCE of a hint on custom/attendee reads as "same rule applies"; attendee is worse because the sender cannot inspect the list. Add `selfExclusionHint.customIncluded` / `.attendeesIncluded` ×3.
- H-4 "the submission would be refused" while Submit stays enabled — `recipientCount` is not in `submitDisabled`. **Decision for the user, see below.**
- H-5 `unavailable` is visually identical to `loading` (`text-muted-foreground` = the empty-state sentinel here) and is a **terminal state**: the effect depends only on `[url]`, so a fixed-URL segment that hits 429 once never shows a number again this session. Fix: `text-warning` + icon + a "Try again" link that bumps a retry nonce (additive to `useRecipientCount`; keep `seqRef` and the settled-answer/url pairing intact).
- H-6 the preference-excluded number lives only in a 4 s toast followed by `router.push()`. **Decision for the user, see below.**
- M-1 the "halt state unavailable" alert does not degrade the queue it does not vouch for — bulk approve sits directly beneath it. M-2 the alert's anatomy differs from `HaltStateBanner` in the same slot; `role="alert"` on a server-rendered element is not announced. M-4 CLS: the count line flips 0/1/2 lines. M-5 three words for one concept (ceiling / limit / cap). M-6 `droppedByPreference`/`orphans` parsed then unused on the client.
- § 15 checklist: PASS 7 · **FAIL 2 (#3 axe — the four T084 cases have no `AxeBuilder`; #7 error states)** · PARTIAL 2 · WAIVED 1 · N/A 6. `ux.md` cannot be co-signed at HEAD.
- **Do not touch** `useRecipientCount`'s core: no stale count across segments, monotonic `seqRef`, no SR spam, contrast passes both themes, FR-053a passes.

**i18n**
- H1 `recipientCount.ready` at count = 0 says "0 recipients will receive this broadcast" in all three locales while submit will be refused → new key `recipientCount.empty` ×3 (destructive tone).
- H2 EN canonical "1 recipient**s**" — plural missing (SV `mottagare` and TH need nothing).
- H4 `errorValues` fallback → next-intl does NOT throw; it renders the raw key path as the toast; the `catch` at `compose-form.tsx:323` is dead; the comment "pre-108 behaviour" is false. Not reachable today (both routes always send `cap`), but not to be left with a wrong comment.
- TH: `{count}` without `number` (→ `50000` ungrouped) ×2; `ที่อยู่` → `อีเมล`; `ปิดรับ` → `ปิดการรับข่าวสาร` (house term); `ready` reads better with E-Blast as subject. SV: `sina` binds to the wrong subject → `medlemmens`; `mottagarens` singular with a plural count → `mottagarnas` + `e-postadress`; `avböjt` → `avregistrerat sig`; `inlämningen` → `skicka in`; `får inte` → `tar inte emot`.
- Verified clean: parity 5,300 ×3; every `t()` in the six consumers resolves (rendered per locale via `intl-messageformat`); no hard-coded 5,000 in any locale; no italics on Thai in the diff.

**Security** — APPROVE WITH FIXES for flag-OFF merge; two conditions before flag-ON (C3, C10). Verified clean: `tx` threaded on both new queries with `c2.tenant_id` explicit under RLS; a real two-tenant proof at the repo layer (Principle I.3); `broadcasts.write` is the NARROWER key (manager holds `broadcasts.read`); F3's audit adapter writes no actor role at all; rate limit is an atomic Lua consume; unknown-id and foreign-id share one query and one 404; the flag-OFF list (a)–(f) has no hidden entry. Test gap: the route-level cross-tenant probe uses a random UUID, not a seeded tenant-B member. LOW: `droppedByPreference` on the member body is the same class of fact as the stripped `orphans` (design tension for `pdpa-gdpr-compliance-officer`, permitted by FR-022a); count routes skip the halt/benefit gates the compose page applies (same shape as `/api/broadcasts/quota`).

**Reliability** — PASS, 0 BLOCKER / 0 HIGH; co-sign footer for `reliability.md` supplied (CHK013–017, 025–028; CHK018–022 N/A-DEFERRED with the import build). M-2 `unsubscribe-recipient.ts` nests a second `runInTenant` under `withTx` and PR-C added a third sequential acquisition — hoist the attribution reads above the tx. M-3 `quickstart.md:48` rollback matrix says "flip the flag OFF" while three changes are UNFLAGGED (active-only narrowing, `.limit(5000)` removal, bridge throws); add an "unflagged on merge" row.

**Observability** — `operations.md` NOT CO-SIGNED at HEAD (CHK016, CHK013 — C14). M: `audienceResolvedTotal` has no `phase` label (a compose poll and a dispatch are one series); `err: probe.error` at `admin/broadcasts/recipient-count/route.ts:93` logs the whole `RepoError` including `DrizzleQueryError.query/params` (line 79 in the same file does it right); `recipient_count_ms` has no `outcome` — a fast-failing count records ~20 ms samples so SLO-F7-013 reads healthy while 100 % of counts 503; `audience_mode` is never written to any audit/log per send. Verified clean: all five metric names resolve; labels bounded; `safeMetric` everywhere; all three crons count a failed build on both arms; the overdue gauge measures the dispatchable set exactly; both alerts can fire and duplicate nothing.

**Comments** — HIGH-2 the resolver's HIGH-4c comment promises a mapping-loop `TypeError` reaches the cron's `uncaught_error` bucket "the one that pages" — every one of the four callers wraps `resolveSegmentRecipients` in its own catch-all, so the observable difference from `resolve.server_error` is a log event name. HIGH-3 the member count route's file docblock still lists `orphans` in the 200 body 60 lines above the code that strips it. M-5 `.env.example`/`env.ts` describe half the flag's blast radius (the ceiling now moves with it too). M-6 ledger row 25: `(this commit)` where a hash belongs, and "every claim re-checked against the code" is overstated — row 17's finding recurring one row later.

**Tests** — M-2 pipeline ORDER pinned for one boundary of three (hoist the ceiling above the filters → nothing fails; 5,010 raw with 20 suppressed would be refused instead of sending 4,990); M-3 the crons' per-row loop is reached on the `!resolved.ok` branch only — hand the dispatcher `[]` and it ships green; L-1 the "read per call, not cached" case cannot fail (`vi.resetModules()` between reads) and the docblock's "takes effect on the next request without a restart" is untrue (`env.ts` memoises); L-3 the count-lib pin has ~8 points of slack.

**Simplify** (advisory) — extract the member-leg sourcing into a helper so the resolver reads top-to-bottom again; the `buildBroadcastRecipientContactsQuery` docblock is attached to the wrong function; stacked/contradicting docblocks in `scrub-…`, `marketing-unsubscribes-repo.ts:145`, `dispatch-scheduled-broadcast.ts:~531`; the "Staff review A11" comments in both crons describe a state this branch fixed; `page.tsx:110 if (memberLookup.ok)` is dead after the redirect at `:106`, and the comment at `:155-158` ("never inside the try/catch") is contradicted by the new redirect inside the catch. KEEP: the four `not_found → null, else throw` blocks (the repetition is the documentation), the two count routes separate (trust boundary), the two compose forms separate.

## Contradictions between reviewers — resolved, not averaged

1. **Walk bound (security/errors "stop at ceiling") vs tests M-2 (filters-before-ceiling must hold).** A raw-row bound AT the ceiling refuses audiences that pass after suppression — the regression tests M-2 asks to pin — and the 20k test pins that a refusal carries the TRUE post-filter count. The bound must be either a sanity cap well above the ceiling or a COUNT pre-check mirroring the filters. **Design work → pre-flip precondition, recorded; not a merge patch.**
2. **Simplify #4 (`makeResolveSegmentDeps` in both batch crons, plain bridge) vs perf MEDIUM-4 (memoised bridge in those crons).** Perf wins: keep the literal, swap the bridge.
3. **Types ("exceeds: unmeasured fields correctly omitted") vs code M-2 (measured at `:346` before both refusals).** Code is right on the facts. Carry `droppedByPreference` on both refusals; delete the false comment.
4. **Comments HIGH-2 (the narrowed try's stated consequence is false) vs tests L-4 (pin the narrowing).** Together: the narrowing has no observable consequence beyond a log event name. Fix the comment to say what is true; do NOT add a test pinning a distinction that does not matter.
5. **Security ("flag-OFF list complete") vs code LOW (three omissions) vs UX + reliability M-3 (UI and three behaviours land unflagged).** All correct about different scopes. The PR body's opening sentence "Flag OFF = today's behaviour" is false for the UI layer and for the three unflagged behaviours — fix that sentence, not only the operator notes.

## Decisions that are the user's

- **UX H-4** — (a) gate `submitDisabled` on `ready && (exceeds || count === 0)` with visible reason text; never gate on `unavailable` (server recomputes) — matches FR-041 intent; or (b) soften the copy and keep submit-to-discover. **Recommend (a).**
- **UX H-6** — minimum now (separate toast, `duration: 8000` when count > 0) vs persisting the number on the broadcast detail page (a new column read). **Recommend minimum now, persistence as a follow-up task.**

## Before-merge set (the fix order)

TDD on every code item; the two with tests already in place go RED first:
1. **Tier guard** — two tests (`{tier, []}` throws on BOTH reads) → RED on the prod leg → route `findMembersBySegmentForBroadcast` through `broadcastSegmentTierFilter` → validate `tierCodes` at `buildSegmentFromBroadcast` (three copies → one shared validator) so a malformed row is a permanent, logged, audited failure, never a retry → fix `contracts:41` claim. (C1, C2)
2. **Perf HIGH-3 → HIGH-1** — strengthen the EXPLAIN assertion (index name, `Index Cond` with the cursor bound, no unbounded `Sort`, page-4 cursor, tier case) → RED against the ROW comparison → rewrite the cursor → GREEN → re-run 20k, record the number. HIGH-2 `maxDuration = 300`.
3. **`setMemberHalt` bridge** — test: `repo.unexpected` rejects, `member_not_found` still typed → fix (`clearHalt` already has `clear_halt.server_error`). (C4)
4. **`OrphanReason` positive filter** at `submit-broadcast:706` (exhaustive switch + `never`). (C12)
5. **`droppedByPreference` on both refusals**; delete the false comment; member/staff count bodies carry it on `empty`. (C8) + subtract/narrow the sender's own opted-out contacts (C17) + memoise the count per tick (C18).
6. **COALESCE CASE** + the test security specified (seed `(M1,C1)`, throw the contact lookup, re-unsubscribe as M2's primary, assert `contact_id ≠ C1`, erase M1, assert no back-reference). (C10)
7. **Hoist attribution reads above `withTx`** in `unsubscribe-recipient.ts`. (reliability M-2)
8. **`approved_overdue_count` zero-fill** (the `forgetAutoInvoiceGauges` pattern) + contract test. (C9) + `phase` label on `audienceResolvedTotal` + `err: probe.error.code` + `outcome` on `recipient_count_ms`.
9. **Compose page**: throw on a failed read (error.tsx), redirect only on `repo.not_found`, `?unavailable=broadcasts` explains on benefits; delete the dead `if`; fix the contradicted comment. (C13)
10. **UI**: proxy notice gated + moved (C6); `estimateNote.attendees` + `selfExclusionHint.{customIncluded,attendeesIncluded}` ×3 (C7, H-3); `recipientCount.empty` ×3 + EN plural (i18n H1/H2); H-4 per decision; `unavailable` tone + icon + retry nonce (H-5); toast split + duration (H-6 per decision); `haltUnknown` prop to `BulkApproveConfirmDialog` + banner anatomy matching `HaltStateBanner` (M-1/M-2); `min-h` wrapper always rendered (M-4/L-1); "limit" everywhere (M-5); TH/SV copy fixes; `errorValues` fallback + dead catch (i18n H4); axe scans on the four T084 cases (§ 15 #3). Then `check:i18n` AND `pnpm test:e2e --grep "@i18n"` AND the compose spec `--workers=1`.
11. **Route-helpers**: drop the number from `message`/`messageThai`. (C11)
12. **Tests**: pin filters-before-ceiling and self-exclusion-before-dedupe (tests M-2); reach the success branch in the two batch-cron contract tests (M-3); replace the unfalsifiable "read per call" case (L-1); route-level cross-tenant probe with a seeded tenant-B member (security gap 1).
13. **Docs**: the seven ceiling sites (C5); resolver HIGH-4c comment (comments HIGH-2); member route docblock (HIGH-3); contract § 4 DEFERRED + § 5 table (C14/M-3); quickstart Cutover 5 + T094 + research R15 + plan:124 rewritten against the five signals PR-C ships (C14); quickstart rollback "unflagged on merge" row (reliability M-3); runbook `:42` `:24` numbering, § C `stuck_sending` (C16); observability `:1317` pipes + `:1343` count + SLO-F7-013 § 22.3 row (C15); `.env.example`/`env.ts` flag blast radius (comments M-5); the simplify docblock items; PR body opening sentence + 20k figure (retraction).
14. **Process**: T092 `[x]` → `[~]` listing the review-stack half, or `[x]` only once every named agent has run AND all three co-signs are written; row 18 disclosed; row 25 hash + retraction; `reliability.md` footer written; `security.md` footer scoped to flag-OFF with the two flag-ON conditions named; `operations.md` re-signed after C14; `ux.md` signed after § 15 #3/#7 close.

## Pre-flip preconditions (record in `pr-c.md` + quickstart; NOT merge blockers)

- C3 walk bound — design (sanity cap ≫ ceiling, or COUNT pre-check mirroring the filters); page-walk-stops test.
- Tenant-level limiter on the two count routes (per-user 30/min is the only bound today).
- The two `*/5` crons' shared `approved` predicate once audiences > 10,000 become acceptable (code M-4) — the predicate needs a flag read, which belongs in the composition root.
- 0294 index decision — measure at 20k (HIGH-3's output), not at 133 contacts.
- `band` label on `recipient_count_ms` + a 5,000-band FR-043 test (the only band SweCham can exercise).
- `audience_mode` in the `broadcast_sent` audit payload (single reviewer; verify `F7AuditPayloadShapes` first).
- Security re-review for the flag-ON signature.

## Co-sign status at HEAD `deb5a1c09`

| Checklist | Status | Blocking |
|---|---|---|
| `reliability.md` | **CO-SIGNED for PR-C** (CHK013–017, 025–028; 018–022 N/A-DEFERRED) — footer supplied, to be written | — |
| `security.md` | **APPROVE WITH FIXES, flag-OFF scope** — footer to be written; flag-ON withheld | C3, C10 before flip |
| `operations.md` | **NOT CO-SIGNED** | CHK016 + CHK013 (C14) |
| `ux.md` | **cannot sign** | § 15 #3, #7 |
