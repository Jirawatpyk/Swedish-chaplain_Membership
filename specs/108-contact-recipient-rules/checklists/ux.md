# UX / Accessibility / i18n Requirements Checklist: Contact Recipient Rules

**Purpose**: Validate that the user-facing requirements — Marketing audience page, member-page badges and switch, portal self-toggle, restore-primary dialog, compose-time count, warnings and copy — are complete, unambiguous, consistent with `docs/ux-standards.md` / `docs/ux-patterns.md`, WCAG 2.1 AA and EN/TH/SV. Formal gate; reviewed by `enterprise-ux-designer`, `mobile-a11y-ux-reviewer`, `i18n-translation-reviewer`.
**Created**: 2026-09-04
**Feature**: [spec.md](../spec.md) · [contracts/contact-marketing-api.md](../contracts/contact-marketing-api.md) · [contracts/broadcast-audience.md](../contracts/broadcast-audience.md)

**Note**: This checklist is generated by the `/speckit.checklist` command based on feature context and requirements.

## Terminology and consistency

- [x] CHK001 Is one canonical label chosen for the primary contact's money role ("Billing contact" vs "Primary") and used consistently across member page, audience page, portal and i18n keys? [Ambiguity, Spec §US4, §FR-031, Contract §3–4] → resolved: FR-031 (Primary badge + descriptor)
- [x] CHK002 Are the four marketing states named identically everywhere (on / off by staff / off by contact / unsubscribed) with one i18n key set, and is the "unknown / status unavailable" fifth state included where degraded lookups can occur? [Consistency, Spec §FR-031, Reliability CHK024] → resolved: FR-031/FR-031a/FR-035
- [x] CHK003 Is the audience-page column set consistent with the member-page badge information (no state shown on one surface that cannot be explained on the other)? [Consistency, Spec §FR-035, §FR-031] → resolved: FR-031b

## Marketing audience page

- [x] CHK004 Are default sort order, default filters and the pre-flight preset specified? [Gap, Spec §FR-035, Contract §3] → resolved: FR-035 defaults + preset
- [x] CHK005 Are empty states specified for: no contacts in tenant, filters match nothing, degraded suppression lookup? [Gap, Spec §FR-035] → resolved: FR-035b
- [x] CHK006 Are loading states (skeleton matching the table's column count, `loading.tsx`) required so layout shift is avoided? [Completeness, Plan Constitution VI] → resolved: FR-035b skeleton
- [x] CHK007 Is the mobile layout (320 px, stacked rows or horizontal scroll inside the container, switch reachable) specified for a seven-column table? [Gap, Spec §FR-051] → resolved: FR-035c
- [x] CHK008 Is the count shown above the table defined (what it counts under the current filters) and is its relationship to the compose-time count stated? [Clarity, Spec §SC-011, Contract §3] → resolved: FR-035 count + SC-011
- [x] CHK009 Are the filter chips' labels, removal behaviour and URL persistence defined consistently with the members directory pattern? [Consistency, Contract §3] → resolved: contract §3 chips
- [x] CHK010 Is pagination behaviour for tens of thousands of contacts specified (page size, "select all matching" explicitly out of scope)? [Clarity, Spec §FR-035] → resolved: FR-035 (50/page, no select-all)

## Inline switch and toggle feedback

- [x] CHK011 Is it specified whether switching a contact off requires a confirmation, and whether an Undo toast is offered (ux-patterns § 2.3)? [Ambiguity, Spec §FR-030, Contract §3] → resolved: FR-030c
- [x] CHK012 Are the toast messages defined for changed / unchanged / suppressed / forbidden / failed outcomes in all three locales? [Completeness, Contract §3, Spec §FR-050] → resolved: contract §3 + FR-050
- [x] CHK013 Is the switch's accessible name required to include the contact's name and the current state, and is the state change announced? [Completeness, Spec §FR-051] → resolved: contract §3 + FR-051
- [x] CHK014 Is the read-only presentation for staff without the right defined (badge instead of a disabled switch, no tooltip implying it can be enabled)? [Clarity, Spec §FR-034] → resolved: FR-035 (read-only badge)
- [x] CHK015 Is the explanation shown when staff try to switch on a suppressed address specified as copy (what it says, where it appears, how it is dismissed)? [Clarity, Spec §US4 scenario 4] → resolved: US4 s4 + contract §3 (copy authored with the i18n task)
- [x] CHK016 Is the minimum target size (≥ 24×24 px) and keyboard operability required for the switch inside a dense table row? [Completeness, Spec §FR-051] → resolved: FR-035c + FR-051

## Member detail page

- [x] CHK017 Is the "no primary contact — payment emails are not being sent" banner specified with placement, severity, copy, and the condition under which it disappears? [Gap, Spec §FR-003, Contract §4] → resolved: FR-003 banner
- [x] CHK018 Are the two badges per contact (billing indicator, marketing state) required to be distinguishable by text/icon, not colour alone? [Completeness, Spec §FR-051] → resolved: FR-051 + contract §4
- [x] CHK019 Does the contact block's action cluster remain consistent (promote/remove hidden for the primary; switch placement relative to invite/verify buttons)? [Consistency, Contract §4] → resolved: contract §4

## Restore-primary dialog (unarchive)

- [x] CHK020 Is the dialog specified with its trigger (409 on unarchive), content (radio list of designatable contacts), primary action label, cancel behaviour and `finalFocus` target? [Completeness, Contract §5] → resolved: contract §5
- [x] CHK021 Is the zero-contacts variant specified (message + "Add contact" link) and is the flow back to the dialog after adding a contact defined? [Coverage, Spec §Edge Cases, Contract §5] → resolved: contract §5 + Edge Cases
- [x] CHK022 Are keyboard and screen-reader requirements for the dialog stated (focus trap, escape, announced title/description)? [Completeness, Spec §FR-051] → resolved: FR-051 + plan Constitution VI

## Portal self-service

- [x] CHK023 Is the location and wording of the marketing control on the portal profile specified (own contact only; label; explanation that money emails are unaffected for the primary)? [Clarity, Spec §FR-032, §FR-033] → resolved: contract §2 + FR-032/FR-033
- [x] CHK024 Is the "unsubscribed" presentation in the portal (no control, explanatory text) specified? [Completeness, Spec §Assumptions "No resubscribe flow"] → resolved: Assumptions + contract §2
- [x] CHK025 Is it specified whether other contacts' marketing states are visible to a portal contact (read-only list vs hidden)? [Ambiguity, Spec §US6 scenario 3, Privacy CHK008] → resolved: FR-032

## Compose-time count and hints

- [x] CHK026 Are the count's format (locale digit grouping), placement (next to the segment picker), live-region announcement and update trigger (segment change, debounce) specified? [Completeness, Contract broadcast-audience §5, Spec §US5] → resolved: FR-040 + contract audience §5
- [x] CHK027 Are the states "count unavailable", "exceeds ceiling" (with the ceiling value) and "dropped by preference: n" defined as copy in three locales? [Completeness, Spec §FR-041, §FR-022a, §FR-050] → resolved: FR-040b/FR-041/FR-022a + FR-050
- [x] CHK028 Is the self-exclusion hint ("you and your colleagues won't receive your own broadcast") required, replacing the current "primary contact email is excluded" wording? [Gap, Spec §FR-022, Research R9] → resolved: FR-022b
- [x] CHK029 Is the rewritten estimate-note copy required to interpolate the ceiling rather than hardcode "5,000"? [Consistency, Research R9] → resolved: R9

## Internationalisation and typography

- [x] CHK030 Are all new keys enumerated by namespace so `check:i18n` and the Thai-script label test can be run as acceptance? [Completeness, Contract §6, Spec §FR-050] → resolved: contract §6
- [x] CHK031 Are TH typographic constraints (no italic on Thai text; muted colour reserved for empty sentinels, not links) restated for the new badges and hints? [Gap, ux-standards / project memory] → resolved: Assumptions (TH typography)
- [x] CHK032 Are timestamps on the audience page ("changed at") required to use the existing locale formatters (BE display-only for TH, UTC stored)? [Consistency, Plan Constitution V] → resolved: plan Constitution V
- [ ] CHK033 Is content-length variance (SV/TH labels longer than EN) accounted for in badge and column width requirements? [Coverage, Constitution V] → OPEN: add SV/TH length-variance note to FR-050 during the enterprise-ux-designer pass in tasks

## Acceptance measurability

- [x] CHK034 Can SC-005 ("read state without a dialog; change in ≤ 2 interactions") and SC-010 (pre-flight on one screen, ≤ 2 interactions each) be verified with a scripted E2E walk, and are the personas (marketing, manager) named? [Measurability, Spec §SC-005, §SC-010] → resolved: SC-005/SC-010 + quickstart manual verification
- [x] CHK035 Are axe `@a11y` and `@i18n` sweeps required for each new or changed surface (audience page, member page, portal profile, compose, restore dialog)? [Completeness, Plan Constitution VI] → resolved: plan Constitution VI

## Notes

- Gaps CHK004–CHK007, CHK011, CHK017, CHK028, CHK031 should be resolved by an `enterprise-ux-designer` pass that writes the missing sentences into spec.md or contracts before `/speckit.tasks`.
