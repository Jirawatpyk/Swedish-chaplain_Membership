# Privacy (PDPA / GDPR) Requirements Checklist: Contact Recipient Rules

**Purpose**: Validate that the personal-data requirements — lawful basis, purpose, minimisation, data-subject rights, retention, erasure, cross-border — are complete, clear and consistent for the new per-contact marketing state, the widened audience, unsubscribe attribution and the audit trail. Formal gate; reviewed by `pdpa-gdpr-compliance-officer` at plan/review.
**Created**: 2026-09-04
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [data-model.md](../data-model.md)

**Note**: This checklist is generated by the `/speckit.checklist` command based on feature context and requirements.

## Lawful basis and purpose

- [x] CHK001 Is the lawful basis for sending marketing to secondary contacts (TH PDPA §24(5) legitimate interest; GDPR Art. 6(1)(f)) written as a requirement with a named record-of-processing update, not only as a clarification note? [Gap, Spec §Clarifications D3/Q2, Plan Constitution I] → resolved: FR-055
- [x] CHK002 Is the purpose of the new per-contact marketing state ("audience management") stated, and is any secondary use (analytics, export) explicitly excluded? [Completeness, Spec §FR-030/§FR-035a] → resolved: FR-030/FR-035a purpose
- [x] CHK003 Is the decision that existing secondary contacts become recipients at cutover **without** an additional notice stated explicitly, with the reasoning (Art. 14 attestation already captured at creation)? [Clarity, Spec §Clarifications Q2, §FR-027] → resolved: Clarifications Q2 + FR-027
- [x] CHK004 Is the balancing test for legitimate interest (B2B professional capacity, existing relationship, easy objection) documented somewhere the compliance reviewer can cite? [Gap, Spec §Clarifications D3] → resolved: FR-055 (LIA note)

## Data-subject rights

- [x] CHK005 Are the objection paths enumerated for every contact type — unsubscribe link (all), portal self-toggle (contacts with login), staff on request (no login) — and is each path's effect on the next send stated? [Completeness, Spec §FR-024, §FR-032, §US4] → resolved: FR-024/FR-030/FR-032
- [x] CHK006 Is the precedence rule (personal unsubscribe > staff/self opt-out > default on) stated once and referenced consistently in FR-022, FR-025, FR-033 and the data model? [Consistency, Spec §FR-025, data-model §1] → resolved: FR-025 + data-model §1
- [x] CHK007 Is the "no resubscribe flow" assumption stated as a requirement with the user-facing consequence (portal shows "unsubscribed" and no control)? [Clarity, Spec §Assumptions] → resolved: Assumptions (no resubscribe)
- [x] CHK008 Does the spec define whether a portal contact may **see** other contacts' marketing states (it defines only that they cannot change them)? [Ambiguity, Spec §FR-032, §US6 scenario 3] → resolved: FR-032 (not shown)
- [x] CHK009 Is the primary contact's right to object to marketing (Q5) reconciled with the requirement that money emails cannot be switched off, in one unambiguous statement? [Consistency, Spec §FR-033, §Clarifications Q5] → resolved: FR-033

## Data minimisation

- [x] CHK010 Are the fields shown on the audience page limited to what the purpose needs, and is the exclusion of `pii_sensitive` fields for the marketing role written as a requirement rather than inherited from 016 tests? [Completeness, Spec §FR-035a] → resolved: FR-035a
- [x] CHK011 Is it specified that recipient-count responses, submit responses and audit payloads never carry email addresses? [Gap, Spec §FR-053, Contract broadcast-audience §5] → resolved: FR-040a + FR-053a
- [x] CHK012 Is logging of the new paths constrained (member/contact id hashes only, never addresses), consistent with the repo's forbidden-fields rule? [Consistency, Plan Constitution VII] → resolved: FR-053a
- [x] CHK013 Are the three new `contacts` columns classified for erasure (KEPT vs SCRUBBED) in a requirement, with the rationale that they are not personal data once the contact is scrubbed? [Gap, data-model §2.1] → resolved: FR-056

## Retention and erasure

- [x] CHK014 Is retention for the new audit events (5 years) stated, and is it consistent with existing F3/F4 retention rules? [Completeness, Research R13, Spec §FR-053] → resolved: FR-053 (5y)
- [x] CHK015 Is the fate of `marketing_unsubscribes.contact_id` after member erasure specified (suppression rows are never deleted; is the contact reference nulled, kept, or hashed)? [Gap, Spec §FR-024, data-model §2.3] → resolved: FR-056
- [x] CHK016 Is the fate of `marketing_opt_out_by_user_id` when the referenced staff user is erased specified (no FK; audit remains authoritative)? [Gap, data-model §2.1] → resolved: FR-056
- [x] CHK017 Are requirements defined for any transient recipient data used to build the provider audience — retention, deletion on completion, erasure cascade? [Coverage, data-model §2.5] → resolved: FR-044; after the R9 correction no recipient list is persisted (CSV generated in memory per tick; only the provider import id + completion stamp live on `broadcasts`)
- [x] CHK018 Does the money-email "no recipient" audit event avoid storing any address and remain useful for the compliance trail (ids, event type)? [Clarity, Research R2] → resolved: FR-003 payload

## Cross-border and processors

- [x] CHK019 Is it stated that no new subprocessor or data transfer is introduced (Resend, Stripe, Neon, Vercel unchanged) so the existing SCC / PDPA §28 documentation still covers the feature? [Gap, Plan Technical Context] → resolved: Assumptions (no new subprocessor)
- [x] CHK020 Is the behaviour of the payment processor's own customer emails (Stripe Dashboard toggle) captured as an operational privacy control at Live switch? [Traceability, Spec §Clarifications, quickstart §Cutover 7] → resolved: Clarifications + quickstart cutover 7

## Transparency and records

- [x] CHK021 Are the audit events sufficient to answer "why did or didn't this person receive this broadcast?" (opt-out with source and actor, unsubscribe with contact, skipped money email with reason)? [Completeness, Spec §FR-031, §FR-053] → resolved: FR-031b + FR-053
- [x] CHK022 Is the pre-flight review (FR-027a) defined as an accountable step — who performs it, and is the outcome recorded? [Ambiguity, Spec §FR-027a] → resolved: FR-027a (owner + record)
- [x] CHK023 Are the spec amendments to 010 (Q8/FR-015c lawful-basis wording) listed so the older "primary only" privacy reasoning is superseded in writing? [Traceability, Spec §Related specifications] → resolved: Related specifications

## Notes

- Items marked [Gap] need a sentence in spec.md (Requirements or Assumptions) or in data-model.md before the compliance reviewer signs.
- Reference the constitution Principle I sub-clauses when resolving CHK010–CHK013.

## Co-Sign Footer — PR-D (T066)

**T066 Review Gate — Privacy Checklist Co-Sign (PR-D)**

- **Co-signer**: Claude Fable 5.1 (AI maintainer), solo-maintainer substitute per Constitution v1.4.2 Principle IX
- **Date**: 2026-09-06
- **Branch**: `108-pr-d-contact-marketing` (PR-D — US4 staff marketing state + Marketing audience page, US6 portal self-toggle)
- **Branch HEAD at co-sign**: `dd3408d79` (fix(108): keep the count line at zero as the focus fallback (not live) โ€” H4 vs M-14)
- **Verification at this HEAD**: lint 0 · typecheck 0 · `check:i18n` 5289 keys × 3 · the eleven static gates OK · unit/RTL suites touched by cycles 9–14 green (members, broadcasts, components, lib, scripts, e2e-helpers, app pages) · full `pnpm test` after cycle 12: 1219 files / 13581 tests (one `.env.example` regression closed in the cycle-11 follow-up) · live Neon: `contact-marketing-opt-out` 22, `contact-marketing-opt-out-guard` 4, `marketing-opt-out-dispatch` 4, `marketing-audience-query`, `self-service-whitelist`, six broadcasts dispatch/audience files · e2e `admin-marketing-audience` 10/10 + `portal-marketing-toggle` 3/3 (re-run after cycle 14).
- **User-invoked gates**: `/speckit-review` ran 2026-09-06 (five agents, 60 findings, all closed) and `/speckit-staff-review-run` ran the same day (five reviewers; report in `reviews/review-20260906-152258.md`). PR #344 remains a DRAFT until the maintainer marks it ready.

- **Reviewer stack**: `pdpa-gdpr-compliance-officer` (round 1: B-1, B-2, H-1, M-1, L-1..L-4) → cycles 9, 10, 12 → two `whole-branch-reviewer` passes → cycles 13–14.
- **Row-level outcome for the rows the reviewer marked**: CHK005 (objection honoured on the next send) — CLOSED: dispatch drops opted-out addresses on every segment kind, proved on live Neon through the real bridge; CHK006 (one precedence, stated once) — CLOSED: FR-025 AMENDMENT + data-model § 3 + the guard under the row lock; CHK021 (audit answers "why did/didn't they receive") — CLOSED: a self "off" over a staff "off" is recorded and audited as the person's objection; CHK001/003/004 (LI basis + LIA in the ROPA) — CLOSED: `docs/compliance/processing-records.md` splits sender-side contract from recipient-side legitimate interest with the D3 LIA, records the per-contact preference as its own activity with its two audit events, and states the true retention rationale (no PII; the audit trail is authoritative; NOT an address-keyed suppression — only `marketing_unsubscribes` survives erasure).
- **Residual recorded, not closed by PR-D**: the Art. 14 / PDPA §23 notice to a secondary contact is attested by staff, never delivered by the system (M-1) — recorded in the ROPA as a PR-C flag-flip gate condition. `marketing_opt_out_by_user_id` for `source='self'` is the data subject's own id, kept at parity with `linked_user_id` and swept with it when F1 user erasure lands (L-3, recorded in the ROPA retention table and the scrub-coverage rationale).

**Co-sign verdict**: privacy.md is **CO-SIGNED for PR-D** at HEAD `dd3408d79`, with the Art. 14 residual carried forward to PR-C's flag flip.

### Re-affirmed after `/speckit.review` (round 3, cycle 15)

The co-sign above was signed at an earlier HEAD. Round 3 (five agents, 60
findings) closed one RED unit test, four HIGH items and the comment rot; the
substantive changes on this checklist's surface are listed in
`reviews/pr-d.md` § Review rounds. **Re-affirmed at HEAD `5b818ee8c`** — lint 0,
typecheck 0, thirteen static gates OK, the live-Neon proofs green (including
the new transaction-rollback and objection-carry-over cases).

### Re-affirmed after `/speckit-staff-review-run` (2026-09-06)

Staff review S1 caught this footer naming a HEAD five commits behind the branch
— the failure this gate exists to catch — so the correction is recorded rather
than quietly overwritten. That particular drift was benign
(`git diff 5b818ee8c..7e06cd7a7 -- src/ scripts/ drizzle/` was a JSDoc reorder
in `src/lib/metrics.ts` and nothing else), but the review that followed was not:
one BLOCKER and twenty warnings, closed across four commits.

**Re-affirmed at HEAD `0b574a30c`** — the last commit carrying source changes;
only this re-stamp follows it. Evidence at that HEAD: `pnpm test:coverage`
**exit 0 with zero threshold errors** (this is the required check that was RED
when the gate opened — 97.72 % branch against a 100 % pin) · full unit +
contract 1224 files / 13642 tests · fifteen static gates OK · lint 0 ·
typecheck 0 · live Neon: `contact-marketing-opt-out` 28 (incl. the two new
carry-forward directions), `contact-marketing-routes` 8 (incl. the live
idempotency replay and conflict), `contact-marketing-opt-out-guard` 4,
`primary-contact-read-agreement` 7 (incl. FR-033 with an opted-out primary).

Changes on THIS checklist's surface since the previous stamp: a person's own
opt-out now follows the address on EVERY insert path, not just `addInTx` —
`createWithPrimaryContactInTx` and the member import script were re-subscribing
people who had objected (C1, fail-open), and a withdrawn objection could be
resurrected from a long-removed row (C2, discarding the person's own opt-in).
FR-027 gained the AMENDMENT that authorises the carry-forward. The Art. 14
residual is unchanged and still carried forward to PR-C's flag flip.
