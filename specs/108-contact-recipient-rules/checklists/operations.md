# Operations & Observability Requirements Checklist: Contact Recipient Rules

**Purpose**: Validate that cutover, flag lifecycle, verification prerequisites, monitoring, alerting, runbooks, documentation and spec amendments are specified well enough for a solo operator to ship and run this feature on a live tenant. Formal gate; reviewed by `observability-instrumentor` and `performance-slo-guardian`.
**Created**: 2026-09-04
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [quickstart.md](../quickstart.md) · [research.md](../research.md) R9, R10, R15

**Note**: This checklist is generated by the `/speckit.checklist` command based on feature context and requirements.

## Cutover and flag lifecycle

- [x] CHK001 Is the delivery order (PR-A → B → D → C → pre-flight → flip → flag deletion) written as a requirement with the reason each step precedes the next? [Completeness, Research R10, Plan §Phase Outputs] → resolved: R10 + plan Phase Outputs
- [x] CHK002 Is the flag's default, its single read site, and its parameter-passing into the resolver specified so tests can run both legs? [Clarity, data-model §5, Plan Constitution X] → resolved: data-model §5
- [x] CHK003 Is the flag-deletion criterion ("one clean week of sends") quantified — what counts as clean (0 bounce-rate alerts? 0 misrouted complaints? N sends)? [Ambiguity, Research R10] → resolved: Assumptions (clean week)
- [x] CHK004 Is it stated which behaviour changes ship **unflagged** (`status = 'active'` narrowing; Tier A; invariant) so the operator knows what changes on each deploy? [Clarity, Research R8/R10, quickstart §Cutover 2] → resolved: R8/R10 + quickstart cutover 2
- [x] CHK005 Are env-var documentation requirements (`.env.example`, env-boot gate, Vercel env) named for the new flag? [Gap, Plan §Project Structure] → resolved: plan env line + R15

## Verify-before-task prerequisites

- [x] CHK006 Is V1 (prod counts: members violating the invariant must be 0; secondaries total; secondaries with login) assigned as a blocking task before PR-B merges, with the script named and its output limited to counts? [Completeness, Research §V1, quickstart] → resolved: FR-010a + quickstart V1
- [x] CHK007 Is V2 (Resend duplicate-contact semantics) assigned before the resumable-push design is finalised, with both outcomes' consequences written? [Completeness, Research §V2/R9] → resolved: R9/V2
- [x] CHK008 Is V3 (enum-guard multi-statement) assigned before migration 0295 is authored? [Completeness, Research §V3/R13] → resolved: R13/V3
- [x] CHK009 Is the primary-contact invariant pre-check's operator remedy documented (how to fix a violating member before redeploying)? [Gap, data-model §2.2] → resolved: quickstart remedy

## Pre-flight review (FR-027a)

- [x] CHK010 Is the pre-flight review defined with an owner role, a preset filter, a completion signal and a place it is recorded (go-live checklist)? [Ambiguity, Spec §FR-027a, Contract contact-marketing-api §3] → resolved: FR-027a
- [x] CHK011 Is it explicit that the review is operational, not technical (first dispatch is not blocked), and is the risk of skipping it stated? [Clarity, Spec §FR-027a] → resolved: FR-027a

## Monitoring, metrics and alerts

- [x] CHK012 Are the new metrics named with labels (audience resolved/pages/push progress/count latency; skipped money email by reason) and added to `docs/observability.md` as a requirement? [Completeness, Research R15, Plan Constitution VII] → resolved: R15
- [x] CHK013 Are alert thresholds defined for: audience push with no progress (stuck), skipped money emails above baseline, recipient-count latency above budget? [Gap, Research R15] → resolved: R15 alert thresholds
- [x] CHK014 Are performance budgets stated with measurement points (count p95 at 5,000 and 20,000; toggle p95; audience page LCP; per-tick push budget)? [Measurability, Plan Technical Context, Spec §FR-043, §SC-004] → resolved: plan Technical Context + SC-004
- [x] CHK015 Is the Resend rate limit captured as an external dependency with an owner action and a fallback? [Dependency, Research R9] → resolved: docs verified 2026-09-04 (default 10 req/s per team, not the code comment's 2); the import-API design (R9) needs 2–3 calls per broadcast so no increase is required; T095 records the team's actual limit + T110 tracks the SDK/segments follow-up (R16)
- [x] CHK016 Is the first-send observation step (progress gauge, outbox, estimated = delivered) defined with pass/fail criteria? [Measurability, quickstart §Cutover 5] → resolved: quickstart cutover 5

## Runbooks and documentation

- [x] CHK017 Is `docs/runbooks/broadcast-audience-build.md` required with its minimum content (stuck detection, resume, manual abort, duplicate-push handling)? [Completeness, Plan §Project Structure, Research R15] → resolved: R15 + FR-045 remedy
- [x] CHK018 Are the existing runbooks that change (`reconcile-stuck-sending`, cron-jobs, void-pdf-reconcile) listed for update? [Gap, Contract broadcast-audience §4] → resolved: R15 runbooks list
- [x] CHK019 Is the go-live readiness document required to gain the Stripe "Successful payments" toggle item for the Live switch? [Traceability, Spec §Clarifications, quickstart §Cutover 7] → resolved: plan go-live-readiness line
- [x] CHK020 Is CLAUDE.md's migration counter (currently 0291) required to advance with each migration PR? [Consistency, CLAUDE.md §Repository status] → resolved: CLAUDE.md convention (task per migration PR)

## Spec amendments and traceability

- [x] CHK021 Are the AMENDMENT blocks for specs 010, 014 backlog, 005, 007 and 016 assigned to a PR (PR-C) with the house format (raise/decide date, operator heads-up, rationale, lockstep test)? [Traceability, Spec §Related specifications, Research §11 precedent] → resolved: plan Phase Outputs (PR-C) + Related specs
- [x] CHK022 Are the frozen artefacts that must change (i18n estimate-note copy in three locales, nav parity lists, e2e MUST_NOT_SEE lists, pinned counts) enumerated as deliverables so gates cannot pass vacuously? [Completeness, Research R6/R9] → resolved: R6/R9
- [x] CHK023 Is the FR ↔ SC ↔ contract traceability complete (every FR has at least one SC or contract test naming it)? [Traceability, Spec §Requirements, §Success Criteria] → resolved: /speckit.analyze 2026-09-04 — 100% coverage (reviews/task-coverage-review.md); 0 CRITICAL; 5 MEDIUM remediated in the same session

## Support and incident readiness

- [x] CHK024 Are the reason codes staff can read for "why didn't X receive the E-Blast" enumerated (inactive member, halted, removed contact, staff off, self off, unsubscribed, sender's own contact, orphan) and surfaced consistently on the member page and audience page? [Gap, Spec §FR-031, §FR-035] → resolved: FR-031b
- [x] CHK025 Is the incident response for a misrouted money email (sent to a former primary before PR-A) or a wrongly widened broadcast (before flag OFF) described — who is notified, what is logged? [Gap, Research R10] → resolved: FR-045 + quickstart incident notes
- [x] CHK026 Is a rollback matrix defined per PR (code revert vs flag OFF vs migration forward-fix) given that migrations are forward-only and prod auto-migrates? [Gap, Plan Constitution VIII] → resolved: quickstart rollback matrix

## Notes

- CHK003, CHK005, CHK009, CHK013, CHK018, CHK024–CHK026 are gaps; the operations reviewer should decide which belong in spec.md vs quickstart.md vs runbooks.
