# Reliability Requirements Checklist: Contact Recipient Rules

**Purpose**: Validate that error paths, transactional boundaries, idempotency, concurrency, DB-level guards and degraded modes are specified completely and consistently across spec.md, data-model.md and the contracts. Formal gate; reviewed by `reliability-guardian` and `drizzle-migration-reviewer`.
**Created**: 2026-09-04
**Feature**: [spec.md](../spec.md) · [data-model.md](../data-model.md) · [contracts/](../contracts/) · [research.md](../research.md) R4, R5, R8, R9

**Note**: This checklist is generated by the `/speckit.checklist` command based on feature context and requirements.

## Invariant: exactly one primary contact

- [x] CHK001 Is the invariant stated with its exact scope (non-archived, non-erased members) and its two sanctioned exceptions (archived, erased) in one sentence that the trigger, the policy and the tests all cite? [Clarity, Spec §FR-010, §FR-013, data-model §2.2] → resolved: FR-010/FR-013 + data-model §2.2
- [x] CHK002 Is the concurrency outcome for promote-vs-remove specified as "one of the two is refused" with the refusal surfaced as a typed conflict, and is SC-002 measurable (100 races, 0 violations)? [Measurability, Spec §US2 scenario 1, §SC-002] → resolved: US2 s1 + SC-002
- [x] CHK003 Does FR-012's mutation list ("add, edit, promote, remove, unarchive") need "edit"? Is it clear which mutations can affect primacy so the check is not applied where it cannot matter? [Ambiguity, Spec §FR-012] → resolved: FR-012 (edit removed)
- [x] CHK004 Is the DB-level guard specified as deferred-to-commit, and are the transactions that must pass (demote-then-promote; scrub + erased_at in one tx) named as test cases? [Completeness, data-model §2.2, Research R4] → resolved: data-model §2.2 + R4
- [x] CHK005 Is the migration pre-check's failure behaviour (raise → deploy fails) stated as a requirement, together with the operator prerequisite (V1 = 0 violations) that makes it safe? [Gap, Plan §Complexity Tracking #4, quickstart] → resolved: FR-010a + plan Complexity #4
- [x] CHK006 Are the DB error → application error mappings defined (trigger raise → `primary_contact_race` / `no_primary_contact`) so callers never see a 500 for an expected refusal? [Clarity, Contract contact-marketing-api §5] → resolved: contract contact-marketing §5

## Unarchive with designation

- [x] CHK007 Is the unarchive + designate operation specified as atomic (both succeed or both fail) with the audit event named? [Completeness, Spec §FR-014] → resolved: FR-014
- [x] CHK008 Is the 409 remedy payload (designatable contacts) defined, including the zero-contacts case? [Completeness, Contract §5, Spec §Edge Cases] → resolved: contract §5 remedy
- [x] CHK009 Is it stated whether designating a **removed** contact is refused, and what happens if the designated contact is removed concurrently? [Gap, Spec §FR-014] → resolved: FR-014 (removed / concurrent)

## Money-email paths

- [x] CHK010 Is the transactional boundary for "resolve recipient + enqueue + audit skip" defined per path (caller's tx vs the port's own tx on resend)? [Clarity, Contract money-email-recipient §1–3] → resolved: contract money §1–3
- [x] CHK011 Is the audit-emit failure policy for the new skipped-send event stated (throw-to-rollback like existing money audits vs fail-open like denials)? [Gap, Research R2] → resolved: FR-003 (same tx, aborts)
- [x] CHK012 Are the `EmailDispatchOutcome` values and their mapping to HTTP/UI outcomes kept consistent across the four paths? [Consistency, Contract §1, §3] → resolved: contract money §1/§3

## Broadcast audience resolution

- [x] CHK013 Is "no silent truncation" turned into a testable property (resolved count equals the eligible set for 100 / 5,000 / 20,000 contacts) and does SC-004 pin the sizes? [Measurability, Spec §FR-041, §SC-004] → resolved: SC-004
- [x] CHK014 Is the failure of a single page during keyset pagination required to abort the resolve with an error (never a partial audience)? [Gap, Spec §FR-041, Contract broadcast-audience §2] → resolved: FR-041 (page failure aborts)
- [x] CHK015 Is the single-ceiling requirement defined with its two values and the condition that selects them, and is the split threshold (10,000) required to stay below the ceiling? [Clarity, Spec §FR-042, Contract §3] → resolved: FR-042 + contract audience §3
- [x] CHK016 Are the two flag legs (`primary_only`, `all_contacts`) required to be behaviour-identical except for the enumerated differences, and is `status = 'active'` required in both? [Consistency, Research R8/R10, Contract §2] → resolved: R8/R10 + contract §2
- [x] CHK017 Is the orphan signal redefined so it fires only for members with zero eligible contacts, and is its cap (50 + truncation row) restated? [Clarity, Spec §FR-029, Contract §6] → resolved: FR-029 + contract §6

## Resumable audience push

- [x] CHK018 Are the requirements for building the Resend audience across cron ticks specified — snapshot of the recipient list, per-tick time budget, progress persistence, resume condition, send-only-when-complete? [Gap in spec / defined in Contract §4, Research R9] → resolved: FR-044
- [x] CHK019 Is idempotency of contact creation per `(audience, email)` stated as a requirement with the verification item (V2) that decides the guard strategy? [Completeness, Research R9/V2] → resolved: FR-044 + V2
- [x] CHK020 Is the stuck-state definition (`audience_building` with no progress for 30 min) and its reconcile action specified, with a runbook named? [Completeness, Contract §4, Plan Constitution VII] → resolved: FR-044 (30 min) + R15 runbook
- [x] CHK021 Are recovery requirements defined for a tick that fails mid-push (partial `pushed_at` stamps, retry semantics, no duplicate sends)? [Coverage, Contract §4] → resolved: FR-044 + contract §4
- [x] CHK022 Is the audience working set's lifecycle (deleted at `sent`/`failed`) specified, including the erasure cascade? [Completeness, data-model §2.5] → resolved: FR-044 (delete + erasure)

## Toggle, count and degraded modes

- [x] CHK023 Is same-state idempotency for the toggle (200 `unchanged`, no audit) specified for both staff and self paths? [Consistency, Contract contact-marketing-api §1–2] → resolved: contract §1–2
- [x] CHK024 Are degraded modes specified for the audience page and member page when the suppression lookup fails (tri-state "status unavailable", never "on")? [Gap, Spec §FR-031/§FR-035, Contract §3] → resolved: FR-031a
- [x] CHK025 Is the recipient-count failure mode defined ("count unavailable", never a stale number) and is the compose submission still allowed or blocked in that state? [Ambiguity, Contract §5, Spec §US5] → resolved: FR-040b
- [x] CHK026 Are rollback requirements stated for the audience change (flag OFF restores `primary_only`; already-sent emails cannot be recalled; what is communicated to whom)? [Gap, Research R10] → resolved: FR-045 + quickstart rollback matrix

## Migrations and schema

- [x] CHK027 Are the six migrations ordered against the PR order with a renumbering rule if PRs land out of order, and is the enum-only-file rule (V3) captured? [Completeness, Research §Pinned facts, data-model §2] → resolved: research pinned facts
- [x] CHK028 Is the "verify DDL landed" step (duplicate `when` = silent no-op) required in the quickstart for each migration? [Consistency, quickstart §Migrations] → resolved: quickstart migrations
- [x] CHK029 Is the correlated-null CHECK for the three opt-out columns specified so partial states are unrepresentable at the DB, mirroring the domain union? [Consistency, data-model §1, §2.1] → resolved: data-model §1/§2.1
- [x] CHK030 Is RLS ENABLE + FORCE required for any new table, and is the trigger function's tenant filtering stated explicitly? [Completeness, data-model §2.2, §2.5] → resolved: data-model §2.2/§2.5

## Notes

- CHK005, CHK009, CHK011, CHK014, CHK018, CHK024, CHK026 identify requirements that live only in plan/contract or nowhere; promote them to spec.md before `/speckit.tasks`.

## Co-Sign Footer

**T041 Operator Gate — Reliability Checklist Co-Sign (PR-B scope: CHK001–CHK009, CHK027–CHK030)**

- **Co-signer**: Claude (AI maintainer, solo-maintainer substitute per Constitution v1.4.2)
- **Date**: 2026-09-05
- **Branch**: `108-pr-b-primary-contact-invariant`
- **Branch HEAD at co-sign**: `bdcb2c49b` (fix(108): T041 review round 2 — 20 findings from the re-review, all fixed RED→GREEN)
- **Re-verified after round 3**: `d62be454f` (fresh-agent whole-branch re-review of `846218b8b`: 1 MEDIUM + 2 LOW, all fixed RED→GREEN — `promotePrimary` now designates when the member has no current primary instead of refusing; `RestorePrimaryDialog` forgets its pick on every close). CHK001–CHK009 and CHK027–CHK030 re-read against the new tree: the invariant surfaces keep their shape (throw-to-rollback, members-first lock order, one predicate), 0293 is v5 by comment only (`when 1798542400000`, sha256 pin green), and the reviewer read all three use cases for an `err()` inside `runInTenant` and found none. Verdict unchanged: **CO-SIGNED**.
- **Verification method**: two full review rounds by `security-engineer`, `reliability-guardian`, `drizzle-migration-reviewer` + `enterprise-ux-designer` (4 reviewers × 2 rounds, 45 findings, 45 fixed, 0 rejected — every fix RED first; ledger `reviews/pr-b.md`); live-Neon integration on the dev branch (T030 5/5 incl. the ×100 race, T031 24/24 incl. a mutation proof of every exemption and a pg_trigger + sha256 pin of the applied DDL, cross-tenant isolation 4/4 incl. the same-uuid-two-tenants probe, the six trigger-sensitive money/import suites 57/57, the members integration folder in batches); unit + contract on the touched surfaces; e2e 8/8 on chromium (4 pre-existing + 4 new); `pnpm lint` 0 · `pnpm typecheck` 0 · `check:i18n` 5188/5188; the reliability reviewer's round-2 verdict was **SIGN** with no BLOCKER/HIGH.
- **Result**: **13/13 PASS** in PR-B scope · 0 DEFERRED · 17 N/A (CHK010–CHK026 belong to PR-A/C/D and are not re-asserted here)
- **Key evidence per category**:
  - *Invariant (CHK001–CHK006)*: one predicate at all three enforcement points — `drizzle/migrations/0293_primary_contact_invariant_triggers.sql` (pre-check DO block, `contacts_check_member_primary`, both triggers) and the inventory script; the AMENDMENT at spec.md FR-010 records the ≥1-contact-row scope and why. SC-002 measured: `tests/integration/members/primary-contact-race.test.ts` ×100 → 0 violations, every loser a typed 409 (was 50/50 at zero on `main`). DB error → app error: `src/lib/db-errors.ts` `primaryContactTriggerViolation` mapped in `contact-crud.ts`, `undelete-member.ts`, `bulk-action.ts`.
  - *Unarchive with designation (CHK007–CHK009)*: `undelete-member.ts` `ensurePrimaryBeforeRestore` — designation + status flip + `member_primary_contact_changed` (old id honestly null) in ONE tx; removed / other-tenant / concurrently-removed contact refused with a re-read list; erased members gated (`undelete_erased`); route 409 payload with `designatable[]` incl. the zero-contacts case (`tests/contract/members/archive-undelete.test.ts`).
  - *Migrations and schema (CHK027–CHK030)*: 0293 journaled at idx 294 / `when 1798542300000` (bumped per revision — T031 pins the applied sha256, so an edit-after-apply cannot pass silently); DDL verified in `pg_trigger` / `pg_proc` / `pg_indexes` on dev; the function is SECURITY DEFINER with `search_path = pg_catalog, public, pg_temp`, filters by the row's `tenant_id`, is callable by nobody but its owner (proved: app role → `permission denied`), and RLS ENABLE + FORCE on `contacts`/`members` is untouched.
- **Constitution v1.4.2**: I Data Privacy & Security **PASS** (two-layer isolation, cross-tenant live test incl. same-uuid probe, no PII in the raise, erased-member gates) · II Test-First **PASS** (every task and every review fix RED → GREEN; two honest exceptions stated in the RED commit and mutation-proved after) · III Clean Architecture **PASS** (policy in Domain, orchestration in Application, DDL in Infrastructure; the route calls the use case only) · IV PCI DSS **N/A** · V i18n **PASS** (11 + 2 keys ×3) · VI Inclusive UX **PASS** (axe clean on both dialog variants; focus never on `<body>`; alert inside the modal; aria-disabled over disabled) · VII Perf & Observability **PASS** (one indexed count per changed row; `lock_timeout` on deploy; warn-logs carry `err` kinds only) · VIII Reliability **PASS** (throw-to-rollback everywhere inside `runInTenant`; audit in the same tx as state; deferred trigger as the backstop) · IX Code Quality **PASS** (lint/typecheck 0; two review rounds) · X Simplicity **PARTIAL** — the triple enforcement (write, tx, commit) is more than one layer on purpose; each layer catches what the one above cannot, and the reasons are written at each site.

**Co-sign verdict**: Reliability checklist (CHK001–CHK009, CHK027–CHK030) is **CO-SIGNED** for PR-B, with 0 deferrals.

— Signed in good faith based on two full reviewer rounds, live-Neon rehearsal with mutation proofs, and a real-browser e2e run. Any future regression in the exactly-one-primary invariant, the unarchive designation, or the 0293 DDL surfaced post-co-sign requires a new round + re-sign.
