# QA Report: F2 Membership Plans — US4 Deactivate / Soft-Delete / Undelete

**QA Mode**: Hybrid (CLI + live Neon + running dev server + Playwright MCP browser walk)
**Date**: 2026-04-11T17:10:43Z
**Feature**: [spec.md](../spec.md) — US4 "Admin deactivates and soft-deletes plans" (P2)
**Environment**: Windows 11 Pro · Node 20.18.1 · pnpm 10.0.0 · Next.js 16.2.3 · Neon Singapore (`ap-southeast-1`) · dev server on `http://localhost:3100`
**Verdict**: ✅ **ALL PASSED**

---

## QA Summary

Systematic QA of F2 US4 against the running dev server + live Neon Singapore + a full Playwright MCP browser walk through the deactivate → delete → show-deleted → undelete → activate cycle on the real SweCham `start-up` plan. Scope: the `activatePlan` / `deactivatePlan` / `softDeletePlan` / `undeletePlan` use cases, the four new API routes (POST /activate, POST /deactivate, DELETE, POST /undelete), the `PlansTable` row-level DropdownMenu + state-aware menu items + ConfirmationDialog wiring, and the audit-log payload shape for all four `plan_*` events.

Five lanes of coverage:

1. **Automated suites (fresh run)**: 484/484 unit+contract + 6 new Phase 6 integration scenarios on live Neon (4 audit-diff state-mutation round-trips + 2 soft-delete-with-members stub-swap scenarios)
2. **API smoke**: auth gate on all 4 endpoints (401 `no-session`), CSRF Origin rejection, Idempotency-Key interaction — auth runs first
3. **Bundle integrity**: 0 argon2 references + 0 Module-not-found errors on `/admin/plans`
4. **Full Playwright MCP browser walk**: sign-in → open /admin/plans → click Start-up row Actions → Deactivate → confirm dialog → badge flips Inactive → Delete → confirm dialog → row hidden → Show deleted toggle → row reappears with "Deleted" badge → Restore menu (only "Restore" visible, state-aware) → confirm dialog → row returns as Inactive (AS4) → Activate (no confirmation per design) → badge flips Active → 9 plans restored
5. **Audit trail verification**: direct SQL query against `audit_log` confirms all 4 state-mutation events written in chronological order with correct `{field: {before, after}}` diff shapes and payloads matching the `auditPayloadSchema` discriminated union

**No critical findings this session.** Unlike Phase 5 QA (which caught a LockWrapper hydration error live), Phase 6's `PlansTable` extensions have no HTML-nesting concerns — the DropdownMenu + ConfirmationDialog wrappers are BaseUI/Radix primitives that already handle portal + focus management correctly. The browser console shows **0 errors, 0 warnings** across the full walk (only Fast Refresh HMR logs from dev).

---

## Test Results

| ID | User Story | Scenario | Mode | Result | Evidence |
|----|-----------|----------|------|--------|----------|
| TC-001 | US4 AS1 | Deactivate Start-up via row dropdown → confirm dialog → toast → badge flips to Inactive | Browser | ✅ PASS | `screenshots/p6-02-dropdown-startup-active.png` → `p6-03-deactivate-confirm-dialog.png` → `p6-04-deactivate-success-toast-badge.png`. Menu shows Edit/Deactivate/Delete (Active plan — no Activate item). Dialog title "Deactivate Start-up?" + AS1 copy. Cancel focused (UX § 6 safest default). Badge flips to Inactive post-confirm. |
| TC-002 | US4 AS2 | Delete inactive Start-up → destructive red confirm dialog → row hidden from default list + counter updates | Browser | ✅ PASS | `p6-05-delete-confirm-dialog.png` → `p6-06-after-delete-row-hidden.png`. Dialog shows red Delete button (destructive styling), description "archived for audit". Counter goes 9 → 8 plans in 2026. |
| TC-003 | US4 AS4 | Show-deleted toggle → URL `?showDeleted=true` → deleted row reappears with outline "Deleted" badge + counter goes back to 9 | Browser | ✅ PASS | `p6-07-show-deleted-reveals-row.png`. Switch toggle flips visual + URL. Row shows outline "Deleted" badge. |
| TC-004 | US4 AS4 | Undelete deleted row → Restore-only menu → confirm dialog → row returns as Inactive (never Active) | Browser | ✅ PASS | `p6-08-dropdown-deleted-row.png` → `p6-09-restore-confirm-dialog.png` → `p6-10-after-undelete-inactive-badge.png`. Deleted-row dropdown contains ONLY "Restore" (no Edit/Activate/Delete). Restored row shows Inactive badge per AS4 (repo forces `is_active=false`). |
| TC-005 | US4 AS1 | Activate inactive Start-up — no confirmation dialog, fires immediately → badge flips to Active | Browser | ✅ PASS | `p6-11-activate-no-dialog-immediate.png`. Dropdown menu labels exposed as `['Edit','Activate','Delete']`. Clicking Activate skips dialog (by design — non-destructive + fast path) and row reaches Active state. |
| TC-006 | SC-007 | Audit trail captures all 4 US4 events with correct diff shapes + chronological order | Integration | ✅ PASS | `responses/tc-audit-trail-start-up.txt` — 4 rows from `audit_log WHERE tenantId='swecham' AND payload->>plan_id='start-up'`: `plan_deactivated (is_active:true→false)` → `plan_soft_deleted (deleted_at:null→ISO)` → `plan_undeleted (deleted_at:ISO→null)` → `plan_activated (is_active:false→true)`. Timestamps monotonic. |
| TC-007 | Contract | POST /activate + POST /deactivate (9 cases) | Contract | ✅ PASS | `responses/tc-010-phase6-contract.txt` — 9/9 in isolation |
| TC-008 | Contract | DELETE /[planId] (5 cases) | Contract | ✅ PASS | `responses/tc-010-phase6-contract.txt` — 5/5: 200 soft-delete, 409 `plan_has_active_members` with `details.affected_member_count`, 404, 401, 400 missing-key |
| TC-009 | Contract | POST /undelete (4 cases) | Contract | ✅ PASS | `responses/tc-010-phase6-contract.txt` — 4/4: 200 with `is_active:false` + `deleted_at:null` (AS4), 404, 401, 400 missing-key |
| TC-010 | Integration | audit-diff state-mutations round-trip through auditPayloadSchema on live Neon (4 events) | Integration | ✅ PASS | `responses/tc-011-phase6-integration.txt` — 4/4: each of `plan_activated` / `plan_deactivated` / `plan_soft_deleted` / `plan_undeleted` round-trips `auditPayloadSchema.safeParse({event_type, payload}).success === true` with correct diff shape |
| TC-011 | Integration | soft-delete with attached members stub swap (count=3 refuse / count=0 ok) on live Neon | Integration | ✅ PASS | `responses/tc-011-phase6-integration.txt` — 2/2: stub returning 3 → `err({type:'has_active_members', count:3})` + plan NOT deleted; stub returning 0 → soft-delete succeeds + `deleted_at` set |
| TC-012 | RBAC | POST /activate + POST /deactivate + DELETE + POST /undelete unauth → 401 `no-session` | API smoke | ✅ PASS | `responses/tc-008a-activate-unauth.json` + `tc-008b-delete-unauth.json` + `tc-008c-undelete-unauth.json` + `tc-008d-deactivate-unauth.json` — all 4 return `HTTP 401` with `{"error":"no-session"}` body when Origin is set |
| TC-013 | CSRF | Missing Origin header → 403 `csrf-rejected` reason `missing-origin` (pre-auth gate) | API smoke | ✅ PASS | Initial attempts without Origin returned 403 `{"error":"csrf-rejected","reason":"missing-origin"}` — confirms CSRF middleware runs before admin-context gate |
| TC-014 | Idempotency | POST /activate without Idempotency-Key — auth gate fires first (401), unauth blocks evaluation | API smoke | ✅ PASS | `responses/tc-009a-activate-no-key.json` — 401 comes first (expected ordering: `requireAdminContext` runs before `parseIdempotencyKey`). Contract test `activate-deactivate.test.ts` covers the 400 `missing_idempotency_key` path with mocked auth. |
| TC-015 | Bundle integrity | `/admin/plans` body contains 0 argon2 refs + 0 Module-not-found errors | API smoke | ✅ PASS | `responses/tc-013-bundle-plans.txt` = 0 + `tc-013b-module-errors.txt` = 0. US1 fix `5bfa5e2` pattern still holds for the Phase 6 extended PlansTable. |
| TC-016 | i18n | 266 keys in all 3 locales — no new keys added by US4 (all US4 keys pre-staged in Phase 2f/Phase 4) | Script | ✅ PASS | `responses/tc-014-i18n.txt` — `[check:i18n] OK — 266 keys present in all 3 locales` |
| TC-017 | Quality | Lint + typecheck clean under strict + `exactOptionalPropertyTypes: true` | Script | ✅ PASS | `pnpm lint` 0 errors/0 warnings · `pnpm typecheck` 0 errors |
| TC-018 | Full suite | Unit+Contract 484/484 green + 149/150 integration (1 intentional skip for DEBUG_RLS_STATE) | Suite | ✅ PASS | Session verified green twice: once after writing Phase 6 tests, once after fixing browser walk. Phase 6 additions: +18 contract, +6 integration. |
| TC-019 | Console | Browser console clean during the full walk | Browser | ✅ PASS | `mcp browser_console_messages`: 46 messages total, **0 errors, 0 warnings** — only React DevTools nag + Fast Refresh HMR logs |

**Legend**: ✅ Pass | ❌ Fail | ⚠️ Partial | 🔵 Skipped/Deferred

---

## Acceptance Criteria Coverage

| Criterion | Test ID(s) | Status | Notes |
|-----------|-----------|--------|-------|
| US4 AS1 — Deactivate active plan → confirmation dialog → hidden from future signups + "Inactive" badge | TC-001, TC-007 | ✅ Met | Full browser walk + contract tests cover the round-trip. Audit row confirmed. |
| US4 AS2 — Soft-delete inactive plan (zero members) → removed from default list + audit entry | TC-002, TC-006, TC-008 | ✅ Met | Browser walk + audit query + F2 stub-checker path verified on live Neon |
| US4 AS3 — Refuse delete when ≥1 active member with affected-member list | TC-011 (integration), TC-008 (contract) | ✅ Met | Covered via stub `MemberAttachmentChecker` swap (F2 has no members table). F3 will replace the stub without touching Application code. API returns 409 `plan_has_active_members` with `details.affected_member_count` |
| US4 AS4 — Show deleted toggle + Undelete returns plan to Inactive (never Active) | TC-003, TC-004, TC-009 | ✅ Met | Repo-level force of `is_active=false` visible in browser walk (row returns with Inactive badge, not the Active state it had before deactivate) |
| SC-007 — Every state mutation produces reconstructable audit row | TC-006, TC-010 | ✅ Met | All 4 events round-trip `auditPayloadSchema.safeParse` with correct diff shape; integration test T126a provides automated coverage |
| FR-009 — Toggle is_active with confirmation dialog | TC-001, TC-005 | ✅ Met | Deactivate → dialog (destructive path). Activate → no dialog (non-destructive fast path per UX § 4.1 rationale) |
| FR-010 — Soft-delete blocked when members > 0 | TC-011, TC-008 | ✅ Met | F2-stub refusal path covered + contract test covers error envelope |
| Idempotency on state-mutating POSTs | TC-007, TC-008, TC-009 | ✅ Met | All 4 endpoints use `Idempotency-Key` + `hashRequestBody` — covered by contract tests with mocked classification |
| Bundle integrity regression | TC-015 | ✅ Met | Phase 6 extensions don't pull argon2 into the client bundle |
| i18n coverage | TC-016 | ✅ Met | All US4 copy keys (`admin.plans.actions/confirm/toast/errors`) were pre-staged from Phase 2/4 — Phase 6 reuses existing keys |

---

## Test Suite Results

### Unit + Contract (Vitest)

```
Test Files  50 passed (50)
Tests       484 passed (484)
Duration    ~38s
```

Breakdown relative to Phase 5: **+18 tests** from Phase 6 contract files:
- `activate-deactivate.test.ts` — 9/9
- `delete-plan.test.ts` — 5/5
- `undelete-plan.test.ts` — 4/4

### Integration (Vitest against live Neon Singapore)

```
Test Files  37 passed | 1 skipped (38)
Tests       149 passed | 1 skipped (150)
Duration    ~190s
```

Breakdown relative to Phase 5: **+6 tests** from Phase 6 integration files:
- `audit-diff-state-mutations.test.ts` — 4/4
- `soft-delete-with-members.test.ts` — 2/2

The 1 intentional skip is `rls-debug-state.test.ts` — runs only when `DEBUG_RLS_STATE=true` (dev-mode guard test, traded off for performance in CI default).

### Script checks

- `pnpm lint` → 0 errors, 0 warnings
- `pnpm typecheck` → 0 errors (strict + `exactOptionalPropertyTypes: true`)
- `pnpm check:i18n` → 266 keys present in all 3 locales

---

## Environment Info

- **OS**: Windows 11 Pro 10.0.22621
- **Runtime**: Node 20.18.1 · pnpm 10.0.0 · TypeScript 5.7 strict
- **Framework**: Next.js 16.2.3 + React 19 + Turbopack
- **Database**: Neon Postgres Singapore (`ap-southeast-1`) via Drizzle ORM (RLS with `SET LOCAL ROLE chamber_app`)
- **Redis**: Upstash Singapore (idempotency key store)
- **Browser**: Chromium via Playwright MCP (real browser, real dev server, real live Neon)
- **Application URL**: `http://localhost:3100/admin/plans`
- **Signed in as**: admin (`jirawat.p@eqho.com`), role `admin`, tenant `swecham`

---

## Metrics

- **Total scenarios**: 19 (TC-001 → TC-019)
- **Passed**: 19 ✅
- **Failed**: 0 ❌
- **Partial**: 0 ⚠️
- **Skipped/Deferred**: 0 🔵
- **Browser walk console errors**: 0 / 0 warnings
- **Automated coverage added for Phase 6**: +18 contract + 6 integration = **24 new green tests**

---

## Verdict

✅ **ALL PASSED** — F2 Phase 6 (US4 deactivate / soft-delete / undelete) ships cleanly. Browser walk, automated suites, API smoke, audit-trail verification, and bundle integrity all green. No critical findings. No deferred items added to `deferred-to-f3.md`.

### Next steps

- **Run `/speckit-ship`** to prepare an F2 MVP+US4 checkpoint release (US1 + US2 + US3 + US4 = all P1 MVP + first P2)
- OR **continue to `/speckit-implement Phase 7`** for US5 fee-config (remaining P2)
- OR **`git push`** the 12 commits to origin
