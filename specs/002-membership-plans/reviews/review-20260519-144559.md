# F2 Membership Plans — Comprehensive Review (post-ship audit)

**Date**: 2026-05-19
**Trigger**: `/speckit-review Full review F2 + เพิ่ม enterprise-ux-designer agent`
**Reviewer**: Claude Code (Opus 4.7, 1M context) — 7-agent parallel sweep
**F2 status at time of review**: SHIPPED via `002-membership-plans` (squashed) — F3 review-ready on `005-members-contacts`, F4 review-ready on `007-invoices-receipts`, F5+F6+F7+F8 shipped on `main`

## Scope reviewed

Source:
- `src/modules/plans/**` (Domain + Application + Infrastructure)
- `src/modules/tenants/**` (cross-cutting TenantContext / TenantSlug / IanaTimezone)
- `src/app/(staff)/admin/plans/**` (admin UI: list, detail, new wizard, edit, clone)
- `src/app/api/plans/**` (8 REST routes + helpers)

Tests (read for coverage assessment only):
- `tests/contract/plans/**` (9 files)
- `tests/integration/plans/**` (12 files — live Neon Singapore)
- `tests/e2e/plans-*.spec.ts` (8 files incl. a11y, i18n, keyboard-only, reduced-motion)

i18n:
- `src/i18n/messages/{en,th,sv}.json` keys under `admin.plans.*` / `plans.*`

Spec context:
- `specs/002-membership-plans/{spec,plan,data-model,research,retrospective,deferred-to-f3,quickstart}.md`

## Agents run (7/7)

| Agent | Aspect | Outcome |
|-------|--------|---------|
| `pr-review-toolkit:code-reviewer` | Constitution + CLAUDE.md + bugs | 3 Important + 5 Suggestions, no Critical |
| `pr-review-toolkit:comment-analyzer` | comment rot, inaccuracy, missing why | 6 Critical (R8 rot + 1 latent bug) + 4 cleanup |
| `pr-review-toolkit:pr-test-analyzer` | acceptance + branch + tenant-isolation + audit coverage | 3 Critical gaps + 5 Important |
| `pr-review-toolkit:silent-failure-hunter` | Result-discard, swallowed catches, tenant fallbacks | 4 Critical + 6 Important + 3 Suggestions |
| `pr-review-toolkit:type-design-analyzer` | branding, smart constructors, invariants | 8 types scored: 2 A-grade · 4 B-grade · 1 D-grade |
| `pr-review-toolkit:code-simplifier` | dead code, over-abstraction, dup | 10 simplifications (~30+60+90+ LOC removable) |
| `enterprise-ux-designer` | UX standards § 15 + WCAG 2.1 AA + i18n EN/TH/SV | 3 Critical i18n + 6 Important + 5 polish |

---

## ✅ Closure Summary (2026-05-19)

**All 21 F2 R6 findings closed across 4 atomic commits + 1 cross-feature bonus.** Branch `014-email-broadcast-advance` (squashed with concurrent F7.1a session work). See `~/.claude/plans/imperative-snuggling-stream.md` for the execution plan + decisions D1/D2/D3/D4.

| # | Finding | Closing commit | Notes |
|---|---------|----------------|-------|
| **C1** | Wire F3 real MemberAttachmentChecker | `ba33958e` (Batch 1c) | New F3 barrel export `countActiveMembersOnPlan` + F2 adapter `drizzleMemberAttachmentChecker`. Closes **I6** too (soft-delete-with-members test gets 2 new Scenarios with real F3 rows) |
| **C2** | Remove tenant-context silent fallback | `b5ce6f66` (Batch 1d) | Throw propagates instead of falling back to `env.tenant.slug`. New unit test asserts forEach exceptions surface |
| **C3** | `reserveIdempotencyRecord` Result + 503 | `b5ce6f66` (Batch 1d) | F2 routes return 503 + `Retry-After: 5` on Upstash outage. F3/F4 callers compile-safe; their 503 surfaces in their own audit pass |
| **C4** | Log persist_failed on audit | `88de9331` (Batch 1a) + `fdba4206` (Batch 1b relocation) | Initial Batch 1a added `logger` in `get-plan.ts` → leaked pino into client bundle via barrel chain. Batch 1b relocated to `plan-audit-adapter.ts` (Infrastructure, server-only) |
| **C5** | Delete US5 fee-config dead code | `fdba4206` (Batch 1b) | Decision D3 — delete (R7/R8 retired the table). 7 src files + 10 test files + seed comment + F1 schema legacy pgEnum comment |
| **C6** | Negative-coverage test for `plan_cross_tenant_probe` | `ba33958e` (Batch 1c) | Grep-based test guards against accidental F2 emit (F13 owns the emit) |
| **C7a/b/c** | 3 hardcoded EN strings → i18n | `88de9331` (Batch 1a) | 4 new keys × EN+TH+SV parity |
| **I1** | Fix lying API audit-failed messages | `fdba4206` (Batch 1b) | `route.ts` + `clone/route.ts` truthful messages + structured log with plan_id / source_year / target_year |
| **I2** | Wire `plan_change_*` audit emits | `ba33958e` (Batch 1c) | Decision D2 revised — F2 use-case emits `plan_change_scheduled` + `plan_change_superseded`; F8 also emits post-tx via `f2AuditEmitter` (composition-root wiring). `_cancelled`/`_applied` stay TODO (no caller yet) |
| **I3** | Advisory lock on `cloneYear` | `fdba4206` (Batch 1b) | `pg_advisory_xact_lock` namespace `plans:clone:<slug>:<targetYear>` — disjoint from F4/F5/F7/F8. Concurrent-clone integration test |
| **I4** | R8 fee-config comment rot (4 files) | `fdba4206` (Batch 1b) | `money.ts` + `plan.ts` + `record-audit-event.ts` + `ports.ts` rewritten to reference `tenant_invoice_settings` via F4 |
| **I5** | SC-005 SweCham 2026 seed fixture test | `fdba4206` (Batch 1b) | Seed script exports `CORPORATE_SEED` + `PARTNERSHIP_SEED` + main() guarded behind `isCliEntry`. New fixture-compliance test asserts 6+3 split, exact fee table, tri-locale parity |
| **I6** | Use real F3 checker in soft-delete test | `ba33958e` (Batch 1c) | Closed by **C1** — Scenarios 3+4 added with real F3 member rows |
| **I7** | Client catch blocks log error | `88de9331` (Batch 1a) | 3 client `*-client.tsx` files added `console.error('[plans/...]', err)` |
| **I8** | Audit emission unconditional on update-plan | `88de9331` (Batch 1a) | Empty-diff short-circuit BEFORE `planRepo.update` — guarantees write+audit are paired |
| **I9** | Branded Money + LocaleText smart constructors | `b5ce6f66` (Batch 1d) | Decision D4 — Money fully branded (3 call sites). LocaleText gets `asLocaleText` smart constructor + `LocaleTextLiteral` back-compat alias; ESLint rule + full brand deferred to future round (70+ call sites) |
| **I10** | Wire `<EmptyState>` into plans-table | `fdba4206` (Batch 1b) | Role-aware dual-CTA + reuses existing `admin.plans.empty.*` keys |
| **I11** | Unique aria-label per row dropdown | `88de9331` (Batch 1a) | `admin.plans.columns.actionsFor` × 3 locales with `{planName}` placeholder |
| **I12** | error.tsx in /admin/plans/** segments | `fdba4206` (Batch 1b) | 3 new per-segment error.tsx files (TableContainer 96rem / FormContainer 42rem / DetailContainer 72rem) + `admin.plans.errors.backToList` × 3 locales |
| **I13** | `common.yes/no` + memberTypeScope enum i18n | `fdba4206` (Batch 1b) | New top-level `common.yes/no` × 3 locales; reuses existing `admin.plans.create.options.memberTypeScope.*` keys |
| **I14** | `aria-current="step"` on wizard stepper | `88de9331` (Batch 1a) | Wizard stepper announces active step to screen readers + `wizardAriaLabel` i18n |

### Bonus (out-of-batch, requested by maintainer)

| # | Finding | Closing commit | Notes |
|---|---------|----------------|-------|
| **F6.1 CSV** | Normalise CRLF/CR → LF inside quoted CSV cells | `4d062da6` | RFC 4180 § 2.6 compliance for EventCreate "Guestlist" multi-line address cells; not part of F2 R6 but caught in the same verification sweep |

### Batch 2 — close deferred items (2026-05-19, follow-up sweep)

After Batch 1 closed the 21 findings, `/plan แก้ defer` swept the 5 deferred items listed in retrospective.md § "Out of scope". 3/4 actionable items closed; F13 stays deferred to F13 feature ship. See `retrospective.md` § "Post-ship R6 audit closure — Batch 2 follow-up (2026-05-19)" for full provenance.

| # | Deferred item | Closing commit | Notes |
|---|---------------|----------------|-------|
| **D5 / Item 1** | BenefitMatrix integrity validator | `797c062a` (Batch 2a) | Soft-pattern smart constructor `asBenefitMatrix(input, planCategory)` validates partnership↔corporate at the Domain boundary. Full `unique symbol` brand attempted but reverted (131 call sites). `BenefitMatrixLiteral` back-compat alias parallels `LocaleTextLiteral`. 7 new unit tests. |
| **D6 / Item 2** | F3/F4 idempotency 503 surface | `036bac2b` (Batch 2b) | 8 F3 routes + 1 F4 logo route return 503 + `Retry-After: 5` on Upstash outage. **Bonus**: fixed 14 test mocks that had been silently crashing the 7 F2 plans contract suites since Batch 1d (`b5ce6f66`) — they returned `undefined` instead of the Result shape. NEW 503 contract case in `tests/contract/members/create-member.test.ts`. |
| **D7 / Item 4-partial (`_cancelled`)** | `plan_change_cancelled` emitter | `65b33901` (Batch 2c) | NEW F2 use-case `cancelScheduledPlanChange` + 13 unit tests pinning the contract. No API/UI caller yet — ready-to-call for future admin "cancel scheduled change" surface or F8 auto-supersede flow. |
| **D7 / Item 4-full (`_applied`)** | `plan_change_applied` emitter + F2 state flip | `bc473c68` (Batch 2d) | NEW `finaliseF2ScheduledPlanChangeForCycle` helper wired into `apply-tier-upgrade-on-paid-callback.ts` POST-tx. Closes both the audit emit AND the "Phase 5+ deferred" F2 state-machine `pending → applied` flip from `apply-pending-tier-upgrade.ts:13-25`. Non-rollback failure semantics mirror `accept-tier-upgrade.ts` post-tx F2 emit pattern. 6 new unit tests. |

State machine coverage after Batch 2 (data-model.md § 2.9 scheduled-plan-changes lifecycle):

| Transition | Audit event | Emit site | Wired in |
|---|---|---|---|
| (insert) `pending` | `plan_change_scheduled` | F2 `scheduleNextRenewalPlanChange` + F8 `accept-tier-upgrade` | Batch 1c |
| `pending` → `superseded` | `plan_change_superseded` | F8 `accept-tier-upgrade` (when prior pending bumped) | Batch 1c |
| `pending` → `cancelled` | `plan_change_cancelled` | F2 `cancelScheduledPlanChange` (no API caller yet) | Batch 2c |
| `pending` → `applied` | `plan_change_applied` | F8 `apply-tier-upgrade-on-paid-callback` post-tx | Batch 2d |

### Commit chain

```
bc473c68  [Spec Kit] fix(F8): close R6 deferred Batch 2d — plan_change_applied emit at F8 onPaid callback
65b33901  [Spec Kit] fix(F2): close R6 deferred Batch 2c — cancelScheduledPlanChange + plan_change_cancelled emit
036bac2b  [Spec Kit] fix(F3+F4): close R6 deferred Batch 2b — idempotency Result 503 surface across 9 routes
797c062a  [Spec Kit] fix(F2): close R6 deferred Batch 2a — asBenefitMatrix smart constructor (soft pattern)
b5ce6f66  [Spec Kit] fix(F2): close post-ship R6 audit Batch 1d — C2/C3/I9
ba33958e  [Spec Kit] fix(F2+F3+F8): close post-ship R6 audit Batch 1c — C1/C6/I2
4d062da6  fix(F6.1): normalise CRLF/CR → LF inside quoted CSV cells (RFC 4180 § 2.6)
fdba4206  [Spec Kit] fix(F2): close post-ship R6 audit Batch 1b — C5/I1/I3/I4/I5/I10/I12/I13/C7c-correction
88de9331  [Spec Kit] fix(F2): close post-ship R6 audit Batch 1a — C4/C7/I7/I8/I11/I14
```

### Verification gate (final)

- `pnpm typecheck` clean across F2 + lib + tests + F3 barrel + F8 use-case
- `pnpm check:i18n` PASS (2987 keys × EN+TH+SV)
- `pnpm check:layout` PASS (98 page/loading files; pairs consistent)
- F2-surface unit + contract tests green
- Integration tests pending re-run against live Neon (DB quota top-up confirmed; tests can run in next CI cycle)

### Concurrent constraint notes

The audit was executed alongside an F7.1a "Batch & Manifest" session that committed 6+ feature commits during the same window. Working tree separation was strictly maintained:
- F2 audit commits touched **only** `src/modules/plans/**`, `src/modules/members/index.ts` + F3 use-case file, `src/modules/renewals/{infrastructure/renewals-deps,application/use-cases/accept-tier-upgrade}.ts`, `src/modules/auth/infrastructure/db/schema.ts` (legacy enum comment), `src/lib/{tenant-context,idempotency}.ts`, `src/app/api/plans/**`, `src/app/(staff)/admin/plans/**`, `src/components/plans/**`, `src/i18n/messages/*.json`, `scripts/seed-swecham-2026-plans.ts`, `src/modules/events/infrastructure/streaming-csv-importer.ts` (F6.1 bonus), and `tests/{unit,contract,integration}/plans/**` + `tests/integration/plans/soft-delete-with-members.test.ts` (F3 real-checker switch)
- F7.1a session files (`src/modules/broadcasts/**`, `src/components/broadcast/**`, broadcast migrations, broadcast-related cron routes) were **never** staged or modified by the F2 audit commits

---

## CRITICAL Issues (block any F3+ work that depends on F2 patterns)

### C1. `MemberAttachmentChecker` stub still wired in production after F3 shipped
- **Where**: `src/modules/plans/infrastructure/members/stub-member-attachment-checker.ts:1-32` + `src/modules/plans/plans-deps.ts:70`
- **What**: stub always returns 0; `softDeletePlan`'s `has_active_members` guard (FR-010) is a permanent no-op
- **Why critical**: F3 (`005-members-contacts`) has shipped a real `members` table + `DrizzleMembersRepository.countActivePlanMembers` but `plans-deps.ts` still injects the stub. The FR-010 guard is dead on live SweCham tenant
- **Sources**: comment-analyzer #1, simplify #6, tests #6 (I6)

### C2. `tenant-context.ts` silently falls back to deployed tenant on header parse failure
- **Where**: `src/lib/tenant-context.ts:71-84`
- **What**: try/catch around `headers.forEach()` swallows any exception and falls back to `env.tenant.slug` with only a `logger.warn`
- **Why critical**: Constitution v1.4.0 Principle I forbids tenant-isolation fallbacks. Future F10 proxy or signed-claim parse errors would silently route requests to the default tenant
- **Sources**: errors C-2

### C3. `reserveIdempotencyRecord` fire-and-forget in POST/PATCH/DELETE handlers
- **Where**: `src/app/api/plans/route.ts:197`, `src/app/api/plans/clone/route.ts:123`, `src/app/api/plans/[year]/[planId]/route.ts:212+396`
- **What**: reservation called without awaiting its Result; on Upstash quota exhaustion or Redis down, request proceeds without idempotency protection → duplicate plan/clone on retry
- **Why critical**: hides Upstash quota exhaustion (already seen in `reference_rate_limit_test_failure.md` memory), Redis flap, race in reservation
- **Fix**: `reserveIdempotencyRecord` returns Result; route returns 503 on reservation failure
- **Sources**: errors C-3

### C4. `getPlan` discards audit Result via `void auditResult`
- **Where**: `src/modules/plans/application/get-plan.ts:84`
- **What**: `void auditResult;` after `deps.audit.record(...)` for `plan_not_found`; no logger.error on persist_failed
- **Why critical**: F13 escalates `plan_not_found` → `plan_cross_tenant_probe` (high-severity security event). Sustained `persist_failed` (audit table RLS drift, immutable trigger drift, DB flap) silently disables the entire cross-tenant probe correlation pipeline
- **Fix**: `if (!auditResult.ok) logger.error({ err: auditResult.error }, 'get-plan: audit_failed on plan_not_found — F13 correlation pipeline at risk')`
- **Sources**: errors C-1, code suggestion #4

### C5. US5 fee-config orphaned after R8 / migration 0029
- **Where**: `src/modules/plans/domain/audit-event.ts:34,62,214` + `src/modules/plans/infrastructure/audit/plan-audit-adapter.ts:70` + `src/modules/plans/domain/fee-config.ts` + `src/modules/plans/domain/policies.ts:67-79` (`canReadFeeConfig` / `canMutateFeeConfig`)
- **What**: migration 0029 dropped `tenant_fee_config` in favour of F4 `tenant_invoice_settings`. `updateFeeConfig` use case does not exist; no API; no tests. Yet `fee_config_updated` audit event + adapter branch + `TenantFeeConfig` Domain type + fee-config policies remain live as dead code. US5 (AS1–AS3, FR-016, FR-017) has zero behavioural coverage on live code
- **Why critical**: spec says US5 shipped; reality is dead-event + dead-Domain-type + dead-policies. Either reactivate (write use-case + 3 AS tests) or fully delete and document the migration in retrospective
- **Sources**: tests C-1, comments #2/#5/#6

### C6. `plan_cross_tenant_probe` (severity=high) has no write-path test
- **Where**: F2 audit event taxonomy (`audit-event.ts:30`)
- **What**: only `high` severity event in F2; schema accepts it; spec says emission is deferred to F13 correlation scanner — but no test asserts the current zero-emitter state, so F13 can ship a regression unnoticed
- **Fix**: add a negative-coverage integration test that scans F2 source for any `plan_cross_tenant_probe` emitter and asserts == 0 (or asserts == 1 if F13 lands first)
- **Sources**: tests C-3

### C7. Three hardcoded EN strings violate Constitution Principle V (EN+TH+SV mandatory)
- **C7a**: `src/app/(staff)/admin/plans/page.tsx:133-138` — `'Tenant fee configuration not yet initialised. Run the seed script.'` + `'Failed to load plans.'`
- **C7b**: `src/app/(staff)/admin/plans/[year]/[planId]/edit/edit-plan-client.tsx:95` — template literal `'Cannot edit locked fields on prior-year plan: ${fields}. Use the clone flow instead.'` (the i18n key `errors.priorYearLocked` already exists in en.json but is being bypassed)
- **C7c**: `src/components/plans/plan-form-wizard.tsx:381` — `'Review the previous steps — some required fields are missing or invalid.'`
- **Sources**: ux C-1, C-2, C-3

---

## IMPORTANT Issues (fix before next major F2 touch or F3 ship)

### I1. API audit-failed messages lie about persistence
- `src/app/api/plans/route.ts:281` ships `"Audit trail write failed — plan was NOT persisted."` while the row IS persisted
- `src/app/api/plans/clone/route.ts:213` ships `"clone may be partial"` while the clone fully committed inside the repo tx
- Clients/CI surfaced this message will retry → duplicate_plan 409
- **Fix**: `"Plan was created but audit trail write failed. Contact ops."` + emit structured log keyed on plan_id for backfill job

### I2. 4 `plan_change_*` audit events declared but never emitted
- `src/modules/plans/domain/audit-event.ts:40-44` declares `plan_change_{scheduled,superseded,cancelled,applied}` and `plan-audit-adapter.ts:75-83` ships summary strings; no use case calls `recordAuditEvent` for them (`scheduleNextRenewalPlanChange` + `transitionStatus` are silent — see file's "Wave G+" TODO at `index.ts:280-286`)
- Constitution Principle I sub-clause 4 (audit) is not satisfied for the scheduled-plan-change state machine F2 owns
- **Fix**: either wire `auditEmitter` into both use-cases or delete the dead event types + adapter branches

### I3. `clonePlansToYear` has no advisory lock
- `src/modules/plans/application/clone-plans-to-year.ts:21` — concurrent calls past `count() > 0` both proceed; comment glosses over rollback story
- F4 uses `pg_advisory_xact_lock('invoicing:…')`; F5 uses `'payments:…'`; F7 uses `'broadcasts:…'`; F2 should use `'plans:clone:'+tenant.slug+':'+targetYear`

### I4. R8 fee-config comment rot in 5 files
- `src/modules/plans/domain/money.ts:6-9` — claims currency resolved from `TenantFeeConfig.currency_code`
- `src/modules/plans/domain/plan.ts:81-83` — same
- `src/modules/plans/domain/fee-config.ts:1-40` — whole file documents deleted feature
- `src/modules/plans/domain/policies.ts:67-79` — `canReadFeeConfig` / `canMutateFeeConfig` exist for deleted feature
- `src/modules/plans/infrastructure/db/plan-repo.ts:418` — `countActiveForTenant` comment claims fee-config-currency-immutability guard
- **Fix**: rewrite to point at `tenant_invoice_settings` via F4 `getTenantTaxPolicy`, or delete fully

### I5. SC-005 SweCham 2026 seed has no automated fixture test
- spec SC-005 mandates "automated fixture test… exactly 9 plans (6 corporate + 3 partnership) with annual fees matching the PDF source to the single THB"
- `tests/e2e/plans-list.spec.ts:50` asserts `toHaveCount(9)` only — does not validate split, fee table (3_600_000…100_000_000 minor units), idempotent re-run refusal (FR-023), or partial-failure rollback

### I6. Soft-delete with members uses injected stub instead of real F3 checker
- `tests/integration/plans/soft-delete-with-members.test.ts:74-80` — `makeFakeChecker(3)` per critique P7 because "F2 ships no members table"
- F3 has shipped real `members` table; switching to `DrizzleMembersRepository.countActivePlanMembers` closes US4 AS3 behavioural gap

### I7. Client `catch {}` swallows TypeError / crypto.randomUUID errors as "network"
- `src/app/(staff)/admin/plans/new/new-plan-client.tsx:58`
- `src/app/(staff)/admin/plans/[year]/[planId]/edit/edit-plan-client.tsx:103`
- `src/app/(staff)/admin/plans/clone/clone-year-client.tsx:79`
- **Fix**: `catch (e) { logger.error({err: e}, 'plan submit threw'); toast.error(t('errors.network')); }`

### I8. `update-plan` skips audit emission on empty diff
- `src/modules/plans/application/update-plan.ts:218` — `if (Object.keys(diff).length > 0)` guards audit emit; a `computeDiff` regression returning `{}` for a real change persists silently with no audit row
- **Fix**: always emit; pass `diff: {}` if needed; or assert non-empty before `planRepo.update`

### I9. 4 Domain types are structural records — invariants live only in zod
- `Money` (`money.ts`) — B+ — structural; bare `{ amount_minor_units, currency_code }` literal typechecks bypassing `asMoney`
- `LocaleText` (`locale-text.ts`) — B− — no smart constructor; `{ en: '' }` typechecks
- `BenefitMatrix` (`benefit-matrix.ts`) — B — `corporate` plan with non-null `partnership` typechecks; superRefine catches it but Domain doesn't
- `TenantFeeConfig` (`fee-config.ts`) — **D** — `vat_rate: number` no range constraint; bare structural type; retired in R8 but Domain type still exported
- **F3+ pattern**: copy the `IanaTimezone` A-grade template (`unique symbol` brand + `Result`-returning smart constructor + named `unsafe*` escape lint-fenced to boundary/test code)

### I10. `<EmptyState>` component unused in plans list
- `src/components/plans/plans-table.tsx:349-355` — inline `<TableCell>` text node
- `src/components/shell/empty-state.tsx` exists with icon + description + action slot
- i18n keys `admin.plans.empty.{description,newCta,cloneCta}` exist but unrendered

### I11. DropdownMenuTrigger has identical `aria-label` across all rows
- `src/components/plans/plans-table.tsx:409-416`
- SR reads "Actions, Actions, Actions" — no row context
- **Fix**: `aria-label={t('columns.actionsFor', { planName: plan.plan_name.en })}` with new keys

### I12. No `error.tsx` in `/admin/plans/**` segments
- Fallthrough to `/admin/error.tsx` which hard-wires `<DetailContainer>` 72rem
- List page (TableContainer 96rem) + clone wizard (FormContainer 42rem) render errors in wrong width
- Also no "Back to plans" CTA

### I13. Boolean fields rendered as hardcoded "Yes"/"No"
- `src/app/(staff)/admin/plans/[year]/[planId]/page.tsx:244` — `events_cobranded_access ? 'Yes' : 'No'`
- TH should be "ใช่/ไม่ใช่"; SV "Ja/Nej"
- **Fix**: `common.yes` / `common.no` keys + replace `<span className="capitalize">` patterns with i18n'd labels for enum values like `member_type_scope` (`page.tsx:170` + `plans-table.tsx:393`)

### I14. Wizard stepper `<ol>` missing `aria-current="step"`
- `src/components/plans/plan-form-wizard.tsx:150-165`
- SR announces "list of 4 items" without identifying active step

---

## SUGGESTIONS (top picks by agent)

### Code quality
- S1. `ilike '%${filter.q}%'` lets users pass `%`/`_`/`\` wildcards (`src/app/api/plans/route.ts:126-127`)
- S2. `plan-repo.ts:230` `update` defence-in-depth uses wall-clock for locked-field guard instead of injected `ClockPort`
- S3. `clone-plans-to-year.ts:114-117` falls back to `JSON.stringify` on unrecognised repo error — use `assertNever()`
- S4. `get-plan.ts:84` `void auditResult;` (same as C4; lint or pattern improvement)
- S5. `index.ts` barrel pattern worth a 10-line ADR in `docs/saas-architecture.md` so F3+ doesn't drift

### Simplification (top 5)
- S6. Drop `MemberAttachmentChecker` + `ClockPort` deps from 5 use-cases that never call them (~30 LOC + 30+ test stubs)
- S7. `activate-plan.ts` + `deactivate-plan.ts` are pure forwarding shells — collapse to one-liners (~60 LOC)
- S8. `record-audit-event.ts` double-validates payload — drop wrapper `safeParse` (~20 LOC)
- S9. Audit-error → use-case-error remap copy-pasted 7×; add `mapAuditError` helper (~35 LOC)
- S10. POST/PATCH/DELETE still inline 40-line idempotency preamble; migrate to `runIdempotencyGuard` (~90 LOC)
- S11. `freshIdempotencyKey` duplicated in 3 client shells; extract to `src/lib/idempotency-key.ts` (~20 LOC)
- S12. `src/app/(staff)/admin/plans/layout.tsx` is a 5-line no-op passthrough — delete

### Types (F3+ rollout)
- S13. `planPathSchema` should `.transform(asPlanYear)` / `.transform(asPlanSlug)` so route handlers receive branded values

### UX polish
- S14. `beforeunload` unsaved-changes guard on wizard + edit form
- S15. `aria-live="polite"` near Save button while submitting
- S16. CloneYearDialog real-time hint when targetYear changes (overwrite risk)
- S17. Review step shows 6/15 fields — add benefit-matrix + sort_order summary card
- S18. Replace boolean "Yes"/"No" text with `<CheckIcon>` / `<XIcon>` + `sr-only` label (don't rely on color-only)

### Errors
- S19. Application `server_error.message` leaks raw Postgres internals (table names, RLS policy names) — scrub at route layer
- S20. `recordAuditEvent` double-validates payload; log which layer rejected
- S21. `runIdempotencyGuard` return type opaque — make reservation-failure variant explicit

---

## STRENGTHS (the bar F3+ should clear)

1. **Tenant-isolation pattern is A-grade** — `runInTenant` enforces NOBYPASSRLS role + `SET LOCAL app.current_tenant` with regex-pre-validated `sql.raw`, branded `TenantContext` makes "forgot tenant" a compile-time error, `DEBUG_RLS_STATE` post-tx assertion makes "forgot `runInTenant`" loud in dev
2. **Clean Architecture is enforced mechanically** — Domain has zero framework imports, Application talks via ports only, `server.ts` sub-barrel is a deliberate documented escape hatch
3. **`auditPayloadSchema` discriminated union is single source of truth** shared by writer + tests + integration — cannot drift
4. **`PlanState` + `IanaTimezone` + `TenantContext`** set the type-design template F3+ should copy: `unique symbol` brand + `Result`-returning smart constructor + named/lint-fenced `unsafe*` escape
5. **Money math uses bigint + half-up rounding** (`list-plans.ts:118-133`) — no off-by-satang risk
6. **Tenant-isolation integration suite is comprehensive** — SELECT/UPDATE/DELETE/INSERT-WITH-CHECK both directions, audit_log permissive policy + unset-tenant secure-by-default
7. **`PlanListSkeleton` uses `useSyncExternalStore` + `data-reduced-motion`** — the best reduced-motion implementation in the codebase; F3–F8 should copy
8. **Command palette + missing-translation indicator ship for real** (not stubs) — smart-chamber feature #4 fully realised
9. **No `test.fixme` / bare `test.skip`** on shipped feature — all skips env-gated or system-clock-gated per `feedback_fixme_blocks_ship.md`
10. **All integration tests hit live Neon Singapore** (zero `vi.mock` in integration suite) — compliant with CLAUDE.md

---

## Sources / agent IDs

For follow-up SendMessage to any agent (within current session):

| Agent | ID |
|-------|-----|
| code-reviewer | `a31c2271325436e5d` |
| comment-analyzer | `a9acf68253f53e462` |
| pr-test-analyzer | `a8357a08be2e86624` |
| silent-failure-hunter | `aa43ac2cfc81006e4` |
| type-design-analyzer | `aa5e08d4c0e4d4652` |
| code-simplifier | `acacefdbb03790563` |
| enterprise-ux-designer | `a3d5ff6251e8eabe2` |

## Recommended action sequence

1. **R-Critical phase** (block any F3+ work depending on F2 patterns): C1–C7
2. **R-Important phase** (before next major F2 touch or F3 ship): I1–I14
3. **R-Suggestion phase** (cleanup / polish): S1–S21
4. **Re-run `/speckit-review` after R-Critical + R-Important close** to confirm regression-free

Human-gated residual: none — all findings are mechanical fixes or doc updates; no maintainer co-sign required unless C2 or C3 trigger Constitution Principle I review-gate audit.
