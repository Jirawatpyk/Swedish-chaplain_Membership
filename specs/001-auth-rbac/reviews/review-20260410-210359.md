# F1 Auth & RBAC — Staff Review Report

**Feature**: 001-auth-rbac
**Branch**: `001-auth-rbac` @ `9764572`
**Reviewer**: staff-review (Spec Kit gate, 6th pass — 5 prior `/speckit.review` rounds already closed)
**Date**: 2026-04-10
**Test baseline**: 480/480 green (287 unit+contract + 79 integration vs live Neon + 114 Playwright E2E across chromium/mobile-chrome/mobile-safari)

---

## Executive Summary

**Verdict**: ❌ **CHANGES REQUIRED**

The F1 implementation is functionally complete and well tested. After 5 prior review passes, the surface is stable, the threat model is mostly enforced, and the test pyramid is healthy. However, this 6th staff-level pass found **2 issues that block ship**:

1. **Constitution Principle III violation** (NON-NEGOTIABLE) — `forgot-password.ts` does a runtime value import from the infrastructure layer, violating the codebase's own established Clean Architecture pattern (which `create-user.ts` follows correctly).
2. **Security § 5 checklist item 12 FAILS verification** — there is no test that exercises the rate limiter's fail-open path with Upstash unreachable, even though `upstash-rate-limiter.ts` implements an in-memory cap fallback. The checklist item explicitly requires this test before sign-off.

The remaining findings are 3 WARNINGS (correctness issues with narrow exploit windows but real risk) and 3 SUGGESTIONS (test coverage and structural gaps). They are not individually blocking but should be addressed before ship.

**The § 5 security checklist CANNOT be signed off in its current state** because item 12 verification fails. Once the two BLOCKERS are resolved (and ideally the WARNINGS too), re-run `/speckit.review` for re-verification and sign-off.

---

## Findings Table

| ID | Severity | File:Line | Threat / Principle | Summary | Recommendation |
|----|----------|-----------|---------------------|---------|----------------|
| **B-01** | 🔴 BLOCKER | `src/modules/auth/application/forgot-password.ts:38-41` | Constitution Principle III (NON-NEGOTIABLE) | Application layer does a value import of `buildResetPasswordEmail` from `@/modules/auth/infrastructure/email/reset-password-email`. This is a runtime module-graph dependency from Application → Infrastructure. The codebase's own correct pattern is in `create-user.ts:41` (`import type { buildInvitationEmail as BuildInvitationEmailFn }` + inject via `CreateUserDeps.buildInvitationEmail`). | (1) Change `forgot-password.ts:38-41` to `import type { buildResetPasswordEmail as BuildResetPasswordEmailFn } from ...`. (2) Add `buildResetPasswordEmail: typeof BuildResetPasswordEmailFn` to `ForgotPasswordDeps`. (3) Wire the concrete function in `src/lib/auth-deps.ts` `defaultForgotPasswordDeps`. (4) Add to `tests/contract/forgot-password.test.ts` deps fixture. |
| **B-02** | 🔴 BLOCKER | `tests/integration/auth/dos-rate-limit.test.ts` (missing case) | security.md § 5 item 12 | No test exists that mocks Upstash to throw and asserts the in-memory fail-open cap (`upstash-rate-limiter.ts:88-116`) is exercised. `dos-rate-limit.test.ts` only hits the live Upstash happy-path. The § 5 checklist explicitly requires "Rate limiter fail-open behaviour is tested with Upstash unreachable". | Add a unit test (or integration test with a Vitest mock of `getRateLimit().limit()` throwing `new Error('upstash unavailable')`) that asserts: (a) `fallbackCheck` is invoked, (b) `authMetrics.redisFallback()` is called, (c) requests are admitted up to the in-memory cap (20/min/IP), (d) requests beyond the cap are denied. |
| **W-01** | 🟡 WARNING | `src/modules/auth/application/reset-password.ts:181-186` | T-15 hardening | Password hash is written (line 181) BEFORE the reset token is marked consumed (line 186). On a process crash between the two awaits, the token remains valid and can be replayed (the legitimate password change has already taken effect, but a second redemption could overwrite it). The two writes are sequential bare awaits — not in a transaction. The file's existing comment (lines 17-21) acknowledges partial-failure but does not note the replay window. | Reorder so `markResetConsumed` runs first (line 186 before 181). A crash after consumption is the safer failure mode (user requests a new link). Better still: wrap both in `db.transaction(...)` so the two writes commit atomically. |
| **W-02** | 🟡 WARNING | `src/modules/auth/application/change-role.ts:74-83` and `src/modules/auth/application/disable-user.ts` (last-admin guard) | T-10 race | The last-admin guard reads `countActiveAdmins()` (line 76), then writes `setRole(...)` (line 83) — both bare sequential awaits, not wrapped in a `SELECT FOR UPDATE` transaction. Two concurrent demote requests can both observe `count = 2`, both pass the guard, and both write — leaving zero active admins. `tests/integration/auth/role-change-race.test.ts` does not catch this because the test fixture has 3+ admins so the guard branch is never the deciding factor. The dedicated `last-admin-protection.test.ts` uses an injected `oneAdminRepo` stub, which proves the guard fires but does NOT prove the count + write are atomic. | Wrap the count check + mutation in a single `db.transaction(...)` with `SELECT ... FOR UPDATE` on the candidate admin rows. Equivalent: a Postgres `CHECK` constraint or `BEFORE UPDATE` trigger that rejects a transition that would leave zero active admins. The latter is simpler and pushes invariant enforcement to the DB layer. |
| **W-03** | 🟡 WARNING | `src/modules/auth/application/forgot-password.ts:85-89` | T-04 + T-01 hardening | Per-email rate-limit key uses `input.email.toLowerCase()` (line 86) but the user lookup uses `asEmailAddress(input.email)` (line 107) which calls `.trim().toLowerCase()` (`branded.ts:58`). An attacker submitting `" user@example.com "` (with whitespace) will hit a different rate-limit key than `"user@example.com"` while still hitting the same user row — bypassing the per-email bucket. `sign-in.ts` avoids this by normalising first. | Move the `asEmailAddress` parse to before the rate-limit calls (mirroring `sign-in.ts`). Catch the parse failure and return `ok({ ok: true })` to preserve enumeration safety. Use the parsed `normalisedEmail` as the rate-limit key. |
| **S-01** | 🟢 SUGGESTION | `src/modules/auth/index.ts` (missing) | Constitution Principle III (boundary enforcement) | No public barrel exists for the auth module. CLAUDE.md mandates "Cross-context imports MUST go through a module's public interface" enforced by ESLint `no-restricted-imports`. Without a barrel, the rule has no anchor and consumers in F2-F9 will pattern-match deep imports. | Create `src/modules/auth/index.ts` re-exporting the Application use cases + their input/output types only. Configure ESLint `no-restricted-imports` to allow `@/modules/auth` and block `@/modules/auth/{domain,application,infrastructure}/**` from outside the module. |
| **S-02** | 🟢 SUGGESTION | `tests/integration/auth/sign-in.test.ts` (missing case) | Constitution Principle II (100% branch coverage on sign-in) | The `disabled` user branch in `signIn` is exercised only in `tests/contract/sign-in.test.ts` with a fully mocked use case. There is no integration-level test that inserts a `status='disabled'` user row and asserts `signIn()` returns `{ ok: false, error: { code: 'account-disabled' } }` against the live DB. | Add one short integration test that creates an active user, calls `userRepo.disable(...)`, then attempts `signIn(...)` and asserts the `account-disabled` error code. |
| **S-03** | 🟢 SUGGESTION | `tests/integration/auth/change-password.test.ts` (missing cases) | Constitution Principle II (100% branch coverage on change-password) | The `policy-fail` (too-short, missing-class) and `hibp-pwned` rejection branches of `changePassword` are unit-tested in isolation (`password-policy.test.ts`) but not threaded end-to-end through the `changePassword` use case at the integration layer. | Add two short cases to the existing `describe` block: (a) attempt to change to a 8-character password → assert `weak-password` error code, (b) MSW-mock HIBP to return a positive hit → assert `weak-password` with `pwned` reason. |

---

## § 5 Security Checklist — Verification

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | All 16 threats in § 2 have passing tests | ✓ PASS | All 16 threat-mapped test files exist and contain meaningful assertions exercising each threat. |
| 2 | CSRF middleware is active and has a negative test per endpoint | ✓ PASS | `tests/contract/csrf.test.ts` covers missing-Origin / foreign-Origin / wrong-scheme / `null` Origin reject for every state-changing endpoint. `src/lib/csrf.ts` reads `APP_ALLOWED_ORIGINS` from env. |
| 3 | Dummy-hash timing path exists in `sign-in.ts` and has a timing test | ✓ PASS | `sign-in.ts:186` calls `deps.hasher.verifyDummy(input.password)` on the unknown-email path. `enumeration-timing.test.ts:120-150` asserts `verifyDummyCalls === 1` and the P95 ratio is ≤ 2x. |
| 4 | HIBP integration is present with the k-anonymity pattern | ✓ PASS | `password-policy.ts:116-154` SHA-1 prefix + `https://api.pwnedpasswords.com/range/${prefix}` + fail-open. Tested in `password-policy.test.ts` with MSW. |
| 5 | Audit log grants migration has been applied (not just committed) | ✓ PASS | `0001_audit_log_append_only.sql` exists. Uses trigger-based enforcement (`audit_log_immutable()`) on Neon (deviation from role grants documented in the migration). `tests/integration/audit/append-only.test.ts` confirms UPDATE/DELETE attempts are rejected against live Neon. |
| 6 | pino redaction is configured and tested | ✓ PASS | `src/lib/logger.ts:33-68` REDACT_PATHS covers 18 sensitive fields. `tests/unit/lib/logger-redaction.test.ts` asserts each path is redacted with sentinel-string checks. |
| 7 | No `dangerouslySetInnerHTML` in auth route trees | ✓ PASS | `grep -r dangerouslySetInnerHTML src/app/` returns nothing. |
| 8 | No raw SQL concatenation anywhere in the repo | ✓ PASS | All `sql\`...\`` usages in `src/modules/auth/` are Drizzle parameterised tagged-templates (`lower(${users.email})`, etc.). No `db.execute(...)` with user input. |
| 9 | `APP_ALLOWED_ORIGINS` env var is set in Vercel production | ⚠ DEFERRED | `src/lib/env.ts:81` zod schema requires `APP_ALLOWED_ORIGINS` as a non-empty CSV list — app refuses to boot without it. **Vercel production env value MUST be confirmed by ops before ship.** |
| 10 | Session rotation on password change is verified in a test | ✓ PASS | `tests/integration/auth/change-password.test.ts:71` "happy path: rotates session, revokes others" asserts old session row is deleted and a new session id is issued. |
| 11 | `last-admin-protection.test.ts` passes under concurrent load | ✓ PASS | `tests/integration/auth/last-admin-protection.test.ts` + `role-change-race.test.ts` both exist and pass. **Caveat**: see W-02 — the test verifies the guard *fires* but does not verify the count+write are *atomic*. The current pass is a structural pass; the race window in the application code is not covered. |
| 12 | Rate limiter fail-open behaviour is tested with Upstash unreachable | ✗ **FAIL** | `upstash-rate-limiter.ts:88-116` implements an in-memory fallback with a 20 req/min/IP cap. **No test mocks Upstash to throw or asserts the fallback path is taken.** `dos-rate-limit.test.ts` exercises only the live Upstash happy path. See B-02. |
| 13 | Error messages expose no stack traces or internal details | ✓ PASS | `grep -rn "stack" src/app/api/auth/` returns nothing. Application use cases return only typed `{ code: '...' }` discriminated unions; no raw `Error` or `.stack` is sent to the boundary. |

**Result: 11 PASS / 1 DEFERRED-OPS / 1 FAIL**

Sign-off on the § 5 checklist is **withheld** pending resolution of item 12 (B-02). Item 9 will require ops verification of the Vercel env var, which is independent of code.

---

## Spec Coverage Matrix (sample)

| Functional Requirement | Implementation file | Test evidence | Status |
|---|---|---|---|
| FR-003 RBAC on every protected route | `src/proxy.ts` + `has-permission.ts` + middleware route guards | `csrf.test.ts`, `rbac-manager-readonly.test.ts` | ✓ |
| FR-005 Always-200 forgot-password, 1 h token, single-use | `forgot-password.ts` | `forgot-password.test.ts`, `password-reset.test.ts` | ✓ |
| FR-006 Min 12 char + HIBP | `password-policy.ts` | `password-policy.test.ts` | ✓ |
| FR-007 No plaintext passwords | argon2-hasher + `PasswordHash` brand | `argon2-hasher.test.ts`, `logger-redaction.test.ts` | ✓ |
| FR-011 Last-admin protection | `change-role.ts:74`, `disable-user.ts` | `last-admin-protection.test.ts`, `role-change-race.test.ts` | ✓ (with W-02 caveat on atomicity) |
| FR-012 Append-only audit, 17 events | `audit-event.ts`, `audit-repo.ts`, `0001_..._append_only.sql`, `0002_..._password_reset_failed.sql` | `audit/completeness.test.ts`, `audit/append-only.test.ts` | ✓ |
| FR-013 Lockout 5/15min | `sign-in.ts` | `lockout.test.ts`, `signin-lockout.spec.ts` | ✓ |
| FR-016 Generic 401 across sign-in failures | `sign-in.ts` (single error code branch) | `enumeration-message.test.ts`, `enumeration-timing.test.ts` | ✓ |
| FR-019 Password change rotates sessions | `change-password.ts` | `change-password.test.ts` (line 71) | ✓ |
| FR-022 Idle warning dialog | `components/auth/idle-warning-dialog.tsx`, `heartbeat.ts` | `idle-warning.spec.ts`, `idle-warning-a11y.spec.ts` | ✓ |

10/10 sampled FRs have visible implementation + tests. No scope creep detected.

---

## Test Coverage Assessment

**Strengths**
- Threat × test mapping is comprehensive (16/16, all evidence verified)
- Security-critical use cases have integration coverage against live Neon (the documented Complexity Tracking deferral of pure unit tests is reasonable)
- E2E suite has the rate-limit cascade fix (`autoClearRateLimits` auto-fixture in `tests/e2e/fixtures.ts`)
- WebKit `.fill()` quirk handled via `fillField` helper (cross-engine consistency)
- Append-only audit log enforcement is asserted at the DB level (live Neon, not mock)

**Gaps** (covered above as findings)
- B-02: No Upstash fail-open test
- W-02: Last-admin race not verified atomically
- S-02: Sign-in disabled-user branch not at integration layer
- S-03: change-password policy-fail / hibp-pwned not at integration layer

---

## Metrics

- **Files reviewed**: 18 use cases / domain / infrastructure files + § 5 checklist surface
- **Findings by severity**: 2 BLOCKER · 3 WARNING · 3 SUGGESTION
- **§ 5 checklist**: 11/13 PASS · 1 DEFERRED-OPS · 1 FAIL
- **Spec coverage sample**: 10/10 FRs implemented + tested
- **Test baseline**: 480/480 green (unchanged from last verify)
- **Constitution principles checked**: I (Security), II (Test-First), III (Clean Architecture), V (i18n), VI (Inclusive UX), VII (Perf & Obs), VIII (Reliability), IX (Code Quality), X (Simplicity)
- **Constitution violations**: 1 (Principle III, B-01)

---

## Recommended Actions

### Must fix before ship (BLOCKERS)
1. **B-01** — Refactor `forgot-password.ts` to use the `import type` + `deps` injection pattern that `create-user.ts` already uses. Wire in `auth-deps.ts`. Update contract test deps fixture.
2. **B-02** — Add a test that mocks Upstash to throw and asserts the in-memory fail-open cap is enforced.

### Should fix before ship (WARNINGS)
3. **W-01** — Reorder `reset-password.ts` so `markResetConsumed` runs before `setPasswordHash`, or wrap both in `db.transaction(...)`.
4. **W-02** — Wrap last-admin guard in a transaction with `SELECT FOR UPDATE`, OR (simpler) add a Postgres `BEFORE UPDATE` trigger that rejects a transition that would leave zero active admins.
5. **W-03** — Normalise email in `forgot-password.ts` BEFORE the rate-limit lookups.

### Nice to have (SUGGESTIONS)
6. **S-01** — Create `src/modules/auth/index.ts` public barrel + ESLint `no-restricted-imports` rule.
7. **S-02** — Add `disabled` sign-in branch to `tests/integration/auth/sign-in.test.ts`.
8. **S-03** — Add `policy-fail` and `hibp-pwned` integration branches to `tests/integration/auth/change-password.test.ts`.

### Ops verification required
- Confirm `APP_ALLOWED_ORIGINS` is set in Vercel production env (item 9 in § 5 checklist).

---

## Verdict

❌ **CHANGES REQUIRED**

Re-run `/speckit.staff-review.run` after fixing B-01 and B-02 (and ideally the WARNINGS too). On clean re-verification, the § 5 checklist will be signed off and the feature can proceed to `/speckit.ship`.

The codebase remains in excellent shape — these are 5th-pass-quality findings, all narrowly scoped, and most have a clear single-file fix. Estimated remediation: 2-4 hours for BLOCKERS + WARNINGS, plus a re-verify run.
