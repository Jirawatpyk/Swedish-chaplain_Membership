## Summary

**F9 — Admin Dashboard + Audit Viewer + Member Timeline + Benefit Usage + Directory/E-Book + GDPR Export.** F9 is the **oversight & insight layer** on top of F1–F8: it lets chamber staff (and members, where appropriate) *see, search, understand, and prove* what happened — without a developer running a query. Ships **dark behind `FEATURE_F9_DASHBOARD`**.

Four staff-facing pillars + their member-facing counterparts:
1. **Admin Dashboard** (US1) — live operations home at `/admin`: health counts, YTD revenue, needs-attention, activity feed, smart insights (replaces the F1 placeholder).
2. **Audit Viewer** (US2) — queryable + exportable append-only audit log (filter by type/actor/target/date, CSV export, role redaction).
3. **Timeline** (US3) — F3 audit-only timeline enriched into a multi-source relationship history (invoices, payments, events, broadcasts, renewals, profile).
4. **Directory + E-Book** (US5) — searchable internal directory + deterministic PDF E-Book + structured opt-in data export.
5. **Benefit Usage** (US4) — member benefit used/entitlement + under-use warnings.
6. **GDPR Self-Service Export** (US6) — member data-portability archive (profile/contacts/invoices+PDFs/events/broadcasts/audit) + README + integrity manifest; admin on-behalf.

## Specification & artifacts (traceability)

- Spec: `specs/015-admin-dashboard/spec.md` (6 user stories, 13 success criteria, 37 FRs)
- Plan / data-model / contracts: `plan.md`, `data-model.md`, `contracts/`
- Tasks: `tasks.md` — **109/110 complete** (T101 = cron-job.org operator config, a **post-merge ship-day gate**, not a code task)
- Review: `reviews/` — latest **✅ APPROVED** (conditions cleared; no blockers; tenant isolation, RBAC, single-use download proxy, audit redaction, SC-008 manifest all verified)
- QA: `qa/qa-20260530-150958.md` — browser (Playwright ×3) + CLI; **all 3 findings since FIXED** (F9-QA-01/02/03, see below)

## Implementation

- New bounded context `src/modules/insights/**` (Clean Architecture: domain / application / infrastructure) + presentation in `src/app/(staff)/admin/**` + `src/app/(member)/portal/**`.
- **Multi-tenant**: two-layer isolation (Postgres RLS + explicit `tenant_id` predicate) per Constitution Principle I; cross-tenant REVIEW-GATE BLOCKER test **12/12 GREEN**.
- 4 new tables (`dashboard_metrics_cache`, `smart_insight_dismissals`, `directory_listings`, `export_jobs`) + member timeline view + audit indexes; migrations 0185–0194.
- Private Blob download proxy (single-use HMAC token, TTL) for export artefacts; PDPA/GDPR audit-subset reader.
- Cross-tenant cron coordinators (snapshot-refresh, process-export-jobs) with Bearer `CRON_SECRET`.
- **Note**: this branch also bundles incidental **F3** (member contact CRUD + address fields, migration 0195) and one **F5** fix that landed during F9 development.

## Testing

- **E2E (browser, Playwright ×3)**: 73 passed · 0 failed (the 2 prior flaky benefit-view a11y scans fixed via settle).
- **Integration (live Neon)**: 77 passed · 4 skipped (RUN_PERF staging gates) incl. cross-tenant blocker 12/12.
- **Unit + contract**: insights GREEN; portal/admin contract re-verified clean.
- **Static gates**: typecheck 0 · lint 0 · check:i18n 3501×3 · check:strict-aria 0 · check:f9-schema · check:audit-events.

## Review & remediation notes

Two formal review rounds + a security review + a QA run on this branch. Remediation in this PR:
- **Round-1/2 review**: tenant-isolation predicates (directory + export-job repos), member-lookup DB-fault discrimination (5 routes + shared `member-context` helper, with `.cause` errKind + hashed user id), cron observability, read-boundary locale narrow, comment-rot, +tests.
- **QA findings (all fixed)**: F9-QA-01 timeline `MISSING_MESSAGE` console spam (`.has` guard + `invoice_pdf_downloaded` label) · F9-QA-02 base-ui `data-slot` hydration mismatch (`Button` slot pinned after spread) · F9-QA-03 flaky benefit-view axe scan (settle on loaded card).
- Cross-cutting a11y sweep (table `scope="col"`, `motion-safe:` spinners, form submit spinners) across F1/F2/F4/F6/F7 surfaces.

## Checklist

- [x] Tasks completed (109/110 — T101 is a post-merge operator gate)
- [x] Code review passed (✅ APPROVED, 2 rounds + security review)
- [x] QA testing passed (findings fixed post-report)
- [ ] CI pipeline green (runs on first push)
- [x] Changelog updated
- [ ] Ship-day operator gates (T101 cron-job.org · Vercel env · flag-flip) — **post-merge**

## Known residuals (post-merge / operator)

- **T101** cron-job.org coordinators (snapshot-refresh */5 + process-export-jobs */5, Bearer `CRON_SECRET`) — documented in `docs/runbooks/cron-jobs.md` §F9.
- **T104** full cross-module CI reproduction — ship-day.
- SC-002 full-render p95 + FR-016 timeline wall-clock — confirmed on staging in-region (RUN_PERF gates committed).
- Deferred (tracked): I3 file-level coverage pins · I5-full repo-transition refactor · ~140 uncatalogued `audit.eventType` labels (humanize-fallback-OK, guarded by `.has` + unit test).

---
*Generated by `/speckit.ship` from spec-driven development artifacts. Commit range `ded9f976..796e6619` (84 commits).*
