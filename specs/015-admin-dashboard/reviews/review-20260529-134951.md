# Staff Code Review — F9 US6 (GDPR Self-Service + Admin-on-Behalf Data Export)

**Date**: 2026-05-29 · **Reviewer**: multi-agent staff review (`/speckit.staff-review.run` via Workflow — 5 dimensions × adversarial verify, 57 agents) · **Scope**: US6 changeset (commits `5b42fa76` + `6a4e6f43`), 51 files / +3108 LOC · **Base**: `HEAD~2`

## Executive Summary

**Verdict: ✅ APPROVED** (conditions cleared — see Remediation Addendum below; original assessment ⚠️ APPROVED WITH CONDITIONS)

> **Remediation Addendum (2026-05-29)** — all 3 conditions + 2 suggestions closed and verified:
> - **W1** → `tests/unit/insights/gdpr-archive-source-adapter.test.ts` (3 tests): PDF-fetch failure records the invoice without bytes (fail-soft, job still ready) + success path + member-not-found → null. GREEN.
> - **W2** → `export-job-repo.test.ts` `listRecentForSubject` test: subject+kind scoped, newest-first, empty for unknown subject. Integration 7/7 GREEN.
> - **W3** → `tests/contract/portal/data-export-route.contract.test.ts` (10 tests): member + admin request-route error mappings (503/401/403/404/202 + admin-attribution). GREEN.
> - **S1** → clarifying comment on the `subject_member_id: ?? ''` sentinel in `download-export.ts`.
> - **S2** → worker `subjectMemberId === null` branch test in `process-export-job.test.ts` (fails loud, no build/upload). GREEN.
> - Gates after remediation: typecheck 0 · 26 unit/contract GREEN · W2 integration 7/7 GREEN.

No 🔴 Blockers and **no correctness or security defects**. Tenant isolation (two-layer), RBAC (member own-only / admin on-behalf / manager-forbidden), the single-use private-blob download proxy, audit-subset redaction, and the SC-008 manifest are all **correctly implemented and verified**. The workflow's 3 "blocker"-tagged items were *positive confirmations* of correct security controls (not defects) — re-classified to ✅ below. Two flagged correctness items (invoice-pagination skip; null-subject silent failure) were **verified false positives** — the loop advances by `rows.length` and exits cleanly, and the worker guards `subjectMemberId === null` → `member_not_found` + `data_export_failed`.

The genuine signal is a set of **test-coverage gaps** (Principle II / solo-maintainer high-assurance substitute). Conditions to clear before ship: close W1–W3.

## Findings

### 🔴 Blockers — 0

*(The 3 workflow "blocker" entries were positive confirmations of correct controls, not defects:)*
- ✅ Admin route `members/write` RBAC (admin-only; manager forbidden) — correct per FR-031.
- ✅ `requestDataExport` member-own-only / admin-any / manager-forbidden + per-minute idempotency — correct.
- ✅ Private export proxy three-layer defence (session + RBAC `authorize()` + single-use HMAC token, atomic consume + audit) — correct per FR-030/SC-008.

### 🟡 Warnings (conditions to clear before ship) — 3 actionable

| ID | Severity | File:Lines | Finding | Recommendation |
|----|----------|------------|---------|----------------|
| **W1** | 🟡 | `tests/integration/insights/gdpr-export.test.ts` | **Untested resilience path**: the gather records an invoice reference *without bytes* + logs when `vercelBlobAdapter.downloadBytes` throws (`gdpr-archive-source-adapter.ts:120-129`). No test exercises this fail-soft branch — a regression could silently drop PDFs or abort the archive. | Add an integration/unit test: a PDF-fetch failure yields the archive with the invoice in `invoices.json` but no `invoices/<n>.pdf`, and the job still reaches `ready`. |
| **W2** | 🟡 | `tests/integration/insights/export-job-repo.test.ts` | **Untested repo method**: `listRecentForSubject` (new in this commit, backs the portal + admin lists) has no direct test — only indirect page usage. Tenant/subject scoping + ordering unverified by a test. | Add an integration test: returns only the subject's `gdpr_member_archive` jobs, newest-first, tenant-scoped. |
| **W3** | 🟡 | `tests/contract/insights/export-download.contract.test.ts` (+ new routes) | **Untested route error paths**: the 4 new routes' error branches (`feature_disabled` 503, `no_member_profile` 404, `forbidden` 403, `server_error` 500) are not contract-tested. | Add contract coverage for the request + download route error mappings (member + admin). |

### 🟡 Warnings — accepted with rationale (no change required)

| ID | File:Lines | Finding | Disposition |
|----|------------|---------|-------------|
| A1 | `gdpr-archive-source-adapter.ts:104-187` | Invoice PDFs + events + broadcasts loaded into memory for the zip | **Accept** — bounded by `MAX_INVOICES/EVENTS/BROADCASTS=1000` caps on a low-volume async cron path (research R5). Streaming-zip is post-MVP if a tenant approaches the cap. |
| A2 | `auth/.../gdpr-audit-subset-repo.ts:33-61` | `payload->>'member_id' / subject_member_id` union arms are not index-backed (seq-scan) | **Accept** — bounded by `(tenant_id, timestamp)` index + hard `LIMIT 5000`; documented in the repo header. Identical risk profile to the shipped `auditQueryReadAdapter`. |
| A3 | `gdpr-readme.ts:41-65` | README falls back to EN for missing locale | **Accept** — `check:i18n` enforces TH/SV key parity at build (CI-blocker on release), so the runtime fallback only fires for an *unknown* locale code. Correct. |

### 🟢 Suggestions (non-blocking) — selected

| ID | File:Lines | Finding |
|----|------------|---------|
| S1 | `download-export.ts:177-182` | `subject_member_id: job.subjectMemberId ?? ''` empty-string sentinel for directory artefacts — add a one-line comment, or make the field optional in the `data_export_downloaded` payload type. |
| S2 | `process-export-job.test.ts` | Add an explicit unit test for the worker's `subjectMemberId === null` → `member_not_found` branch (guard exists, untested). |
| S3 | `request-data-export.test.ts` | Add an idempotency stress test (two same-minute submits → one job, second `created=false`). |
| S4 | `gdpr-archive-zip.test.ts` | Manifest test validates checksums but not a tamper/corruption negative case (low value). |
| S5 | `vercel-blob-adapter.ts:92-112` (F4, out of US6 scope) | `downloadBytes` has no fetch timeout/retry — note for an F4 follow-up, not US6. |

*(Remaining ~14 workflow entries were positive confirmations — e.g. "gather correctly limits per FR-037", "HMAC includes jobId per SC-008", "requester_locale correctly isolated from idempotency key", "no-silent-failure correctly implemented" — recorded as evidence, not findings.)*

## Spec Coverage Matrix

| Requirement | Status | Evidence |
|-------------|--------|----------|
| **FR-029** archive (profile/contacts/invoices+PDFs/events/broadcasts/audit-subset) + README + manifest + audit redaction + requester locale | ✅ | `gdpr-archive-{source-adapter,zip,readme}.ts`; T086 verifies all 8 entries + redaction |
| **FR-030** delivery (signed link / in-portal) + request & delivery audited | ✅ | in-portal download model (documented decision); `data_export_requested/downloaded` audited |
| **FR-031** admin on-behalf, attributed to admin | ✅ | admin routes + card; T087 asserts `on_behalf=true` + admin actor |
| **FR-032** member cannot export others | ✅ | `requestDataExport` RBAC; T087 forbidden |
| **FR-032a** archived exportable / erased never resurrected | ✅ | gather reads live (archived resolves; absent → `member_not_found`); redaction only removes |
| **FR-033** tenant-scoped both layers | ✅ | RLS tx + explicit predicate; cross-tenant test (F1 remediation) 12/12 |
| **FR-034** i18n EN/TH/SV + BE display | ✅ | `dataExport.*`/`gdprExport.*` 3450 keys; `formatLocalisedDate` BE |
| **FR-035** a11y WCAG 2.1 AA | ✅ | f9-a11y scans (data-export page + admin card) 6/6; pre-existing F3/F4 debt also fixed |
| **SC-008** manifest checksums validate | ✅ | per-file SHA-256; T086 recomputes + matches |

## Test Coverage Assessment

- **Strong**: scoping predicate 100% branch (gdpr-audit-subset 12), zip+manifest (7), request RBAC+idempotency (6), worker gdpr branch (12), live-Neon archive+redaction (T086), authz+attribution (T087), cross-tenant reader (F1, 12/12), a11y (6/6), e2e member+admin (3).
- **Gaps (→ W1/W2/W3 + S2/S3)**: PDF-fetch fail-soft path, `listRecentForSubject`, route error mappings, null-subject worker branch, idempotency stress.

## Metrics

- Findings (post-triage): **0 blocker · 3 actionable warning · 3 accepted-warning · ~5 suggestion** (35 raw → de-duped/de-confirmed)
- Files reviewed: 51 (US6 changeset) · Spec coverage: **9/9 (100%)** FR/SC evidenced
- Constitution: Principle I ✅ · II ⚠ (test gaps W1–W3) · III ✅ · IV n/a · V ✅ · X ✅ (fflate justified)

## Recommended Actions (priority order)

1. **W1–W3** (before ship): add the 3 test coverages (PDF-fetch resilience, `listRecentForSubject`, route error paths). ~3 small tests.
2. S2/S3 (nice-to-have): worker null-subject branch + idempotency stress tests.
3. A1/A2 accepted — no action; S5 → F4 follow-up.

**Conditions to reach ✅ APPROVED**: close W1–W3, then proceed to `/speckit.ship` (after operator gates T101a/T101/T104/T105 + the Principle IX security co-sign T105).
