# Code Review Report: F9 — Admin Dashboard / Insights (Slice A) — Staff Review **Round 2**

**Reviewer**: AI Agent (Staff Engineer Perspective) — multi-agent: security-threat-modeler · pdpa-gdpr-compliance-officer · feature-dev:code-reviewer
**Date**: 2026-05-28
**Feature**: [specs/015-admin-dashboard/spec.md](../spec.md)
**Branch**: `015-admin-dashboard` @ `d8a5d960` + uncommitted R1 remediation
**Scope**: Round 2 = verify the Round-1 staff-review remediation delta (`git diff d8a5d960`) + surface NEW issues only
**Verdict**: ✅ **APPROVED**

---

## Executive Summary

Round 2 verified the Round-1 remediation against all three conditions. All three reviewers independently confirmed **B-1 / H-1 / R004 are closed** and introduced no regressions: the summary-email redaction is correct + applied at every manager surface (audit viewer, CSV export via shared `toRow`, dashboard feed) with admin unaffected; the `member_timeline_viewed` audit event is in three-way lockstep (tuple ↔ payload ↔ retention) with migration 0193 + Drizzle enum sync + parity gate; the member projection blanks the staff `actorUserId`/`actorDisplayName` with no path re-adding it; the redaction regex is ReDoS-safe (mutually-exclusive character classes); the best-effort emit cannot break the page render. Two minor NEW findings surfaced — both resolved in-session: a filter-group miscategorisation of the new event, and a documented scope decision for per-page-view audit granularity. No blockers.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Resolution |
|----|----------|------|---------|----------|---------|------------|
| R2-001 | 🟡 Warning | src/lib/audit-event-label.ts | 48 | Correctness | `member_timeline_viewed` fell through `startsWith('member_')` → grouped under "Members" instead of "Dashboard/Oversight" in the audit-viewer filter dropdown (the test header explicitly warns this is a trap for new `member_`-prefixed F9 events). Audit row written correctly; UI-grouping only. | **Fixed** — added to the `'dashboard'` branch + `audit-event-category.test.ts` table (23/23 GREEN). |
| R2-002 | 🟢 Suggestion | src/app/(staff)/admin/members/[memberId]/timeline/page.tsx | — | Spec Compliance (FR-036) | The `member_timeline_viewed` emit fires once on the full-timeline-page render; the load-more API + the 3-row preview snippet on the member-detail page do not re-emit. Privacy agent rated MEDIUM but deemed documentation proportionate. | **Documented** — the one-emit-per-page-view model (consistent with `member_benefit_viewed`) is now a code comment; the page-view event already establishes who accessed whose timeline; avoids audit-log inflation. |

---

## Round-1 Condition Verification

| R1 Condition | Round-2 Verdict | Evidence |
|--------------|-----------------|----------|
| R001 (summary email → manager) | ✅ Closed | `redactSummaryForRole` applied in `audit-query.ts` `toRow` (viewer + `auditExport` CSV via the same fn) + `activity-feed-query.ts` manager branch; admin full; regex ReDoS-safe; unit-tested (3 cases). |
| R002 (staff timeline read unaudited, FR-036) | ✅ Closed | `member_timeline_viewed` enum/payload/retention lockstep + migration 0193 + Drizzle sync + parity gate 15; emit for admin+manager; member self-view not audited; PII-free payload. |
| R003 (log hygiene) | ✅ Closed | timeline route logs `errKind(cause)`. |
| R004 (member sees staff UUID) | ✅ Closed | member `redactEvents` blanks `actorUserId`/`actorDisplayName` on audit rows; SSR + load-more both render the i18n label (no UUID); integration-asserted. |
| R007/R009 | ✅ Verified acceptable | CSV bounded by 10k cap; `occurred_at_utc` unambiguous. |
| R010/R011 | ✅ Added | live-Neon `audit_log_exported` write + manager-revenue assertions GREEN. |

---

## Spec Coverage Matrix (unchanged from R1)

Slice A (US1–US4): ~22/23 in-scope FRs implemented; FR-036 now **fully** met (timeline read audited). US5/US6 out of scope (Slice B).

---

## Test Coverage Assessment

| Area | Tests | Notes |
|------|-------|-------|
| Summary redaction (R001) | ✅ | `audit-redaction.test.ts` (3) + `activity-feed-query.test.ts` (manager/admin) |
| `member_timeline_viewed` parity | ✅ | `check:audit-events` 15; `audit-event-category.test.ts` (R2-001) |
| Member actor-id strip (R004) | ✅ | `timeline.test.ts` integration assertion |
| Audit export write-path (R010) | ✅ | `audit-query.test.ts` live-Neon |
| Manager dashboard revenue (R011) | ✅ | `dashboard-snapshot.test.ts` live-Neon |

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Remediation files reviewed | 19 (delta) |
| 🔴 Blockers | 0 |
| 🟡 Warnings | 1 (R2-001, fixed) |
| 🟢 Suggestions | 1 (R2-002, documented) |
| R1 conditions verified closed | 8/8 |
| Post-R2 gates | typecheck 0 · lint 0 · i18n 3330 · F9 audit parity 15 · insights unit GREEN · integration GREEN |

---

## Recommended Actions

### Must Fix (Blockers)
*None.*

### Should Fix
- R2-001 — **done** (category fix + test).

### Nice to Fix
- R2-002 — **done** (documented). Optional future: emit per load-more cursor call if per-page audit granularity is later required.

---

## Verdict

✅ **APPROVED** — Round-1 remediation fully verified (8/8 conditions closed, no regressions); both new Round-2 findings resolved in-session. Slice A is clean for `/speckit.ship`.

---

*Generated by `/speckit.staff-review.run` — staff-level multi-agent code review (Round 2).*
