# F4 Release Record — 2026-04-22 16:41:13

## PR

- **URL**: https://github.com/Jirawatpyk/Swedish-chaplain_Membership/pull/12
- **Number**: 12
- **Title**: `feat(invoicing): F4 — Membership invoicing & Thai-tax receipts`
- **State**: OPEN (Draft — awaiting CI + human gates)
- **Base**: `main`
- **Head**: `007-invoices-receipts` @ `4243f34`

## Stats

- **Files changed**: 353
- **Additions**: +44,018 lines
- **Deletions**: −954 lines
- **Commits on branch**: 80+ (squash-merge recommended at merge time)

## Ship-gate status at PR creation

### ✅ Closed

| Gate | Evidence |
|---|---|
| Spec + plan + tasks authored | `specs/007-invoices-receipts/` |
| All 7 user stories implemented | US1–US7 all green in integration suite |
| Tenant isolation Review-Gate (Principle I) | `tenant-isolation.test.ts` 17/17 + 4 probe tests |
| Coverage thresholds (Principle II) | Domain 100 % / App ≥ 80 % / Security-critical 100 % branch |
| Clean Architecture (Principle III) | Zero `sharp` imports in Application layer (R19 port extraction) |
| PCI DSS SAQ-A (Principle IV) | Zero card data / tokens / PAN / CVV in F4 |
| i18n EN + TH + SV | 1,123 keys × 3 locales |
| Perf budgets | PDF 88 ms / list 324 ms @ 5k×2 / seq 10 s — all with headroom |
| Audit behavioural coverage | 17/18 event types (T113a) |
| Local CI gates | typecheck + lint + check:i18n + check:layout green |
| Live Neon integration | 447 passed / 7 skipped / 1 todo / **0 failed** |
| Unit tests | 285/285 green |
| 5 staff reviews logged | R15 + R16 + R17 + R18 + R20 (floor ≥ 2) |
| QA parallel-agent audit | 5 specialist agents returned sign-off lines |

### ⏳ Held

| Gate | Owner | Blocks? |
|---|---|---|
| Remote CI / GitHub Actions | Platform | Will run on push; convert to Ready-for-review after green |
| Vercel Preview smoke test | Maintainer | Not ship blocker; verify before Ready-for-review |
| PG-2 Resend DPA legal signature | Legal / DPO | **Only** gates `FEATURE_F4_VOID_ATTACHMENT=true` in prod; link-only ship path works today |
| Real-device NVDA / VoiceOver / cross-browser / reduced-motion | Maintainer | Not ship blocker; recommended before GA announcement |

## Changelog (v1.0.0 entry)

### Added

- 5 new DB tables (`invoices`, `invoice_lines`, `credit_notes`, `tenant_invoice_settings`, `tenant_document_sequences`) + 13 F4 migrations (0019–0031).
- 18 new audit event types across the F4 event catalogue.
- 20 use-cases + 11 Application ports in `src/modules/invoicing/`.
- 4 bilingual (TH + EN) Thai-RD-compliant PDF templates (invoice, receipt, combined invoice-receipt, credit-note, void-stamped invoice).
- Sequential §87 "no-gaps" tax-document allocator with advisory-lock concurrency.
- Admin-side invoice lifecycle: draft → preview → issue → pay → void / credit-note.
- Member portal: invoice list + PDF download with ownership guard.
- F3 member detail page: invoices section + timeline integration (6 F4 event types).
- Tenant invoice settings form with logo upload (FR-034: MIME whitelist + dimension + EXIF-strip + `sharp` re-encode).
- Auto-email pipeline (outbox pattern) + manual PDF resend (admin + member-portal variants).
- Overdue derivation (FR-028) wired into 4 invoice UIs with at-most-once daily audit.
- Feature flag `FEATURE_F4_INVOICING` (proxy-level kill-switch) + `FEATURE_F4_VOID_ATTACHMENT` (DPA-gated attachment path).

### Changed

- `src/lib/logger.ts` `REDACT_PATHS` extended with `tax_id`, `member_*_snapshot`, `signed_url_token`, `pdf_binary`, `recipient_email` (depth-2), `payment_reference` (R19 + R20 defence-in-depth).
- `src/proxy.ts` kill-switch coverage extended to `/api/invoices/**`, `/api/credit-notes/**`, `/api/tenant-invoice-settings/**`, `/api/portal/invoices/**`, `/api/members/<uuid>/invoices` regex.
- F3 member timeline use-case extended with 6 F4 event types (US7 / FR-033).

### Fixed

- R17-02 void two-phase commit attachment-integrity hazard (dispatcher now sha256-verifies prefetched bytes before attach).
- R17-03 `record-payment.ts` settings read hoisted above `withTx` to avoid nested pool-deadlock.
- TC-07 `pdf-routes-cross-tenant-probe.test.ts` FK-ordering bug (seed `members` + `membership_plans` before `invoices`).

### Technical Notes

- Architecture per `specs/007-invoices-receipts/plan.md`: Domain / Application / Infrastructure / Presentation layers strictly enforced; `sharp` behind `ImageReEncodePort`, `@react-pdf/renderer` behind `PdfRenderPort`, Vercel Blob behind `BlobStoragePort`.
- Constitution v1.4.0 compliance: all 4 NON-NEGOTIABLE principles (Data Privacy + Tenant Isolation / Test-First / Clean Architecture / PCI DSS) verified with file:line evidence across 5 staff reviews.
- 10-year tax-document retention under "legal obligation" lawful basis (Thai Revenue Code §87/3 + GDPR Art. 6(1)(c)); snapshot immunity to member archive / GDPR erasure proven by integration test.
- Accepted-debt catalogue in `security.md § 5a` (AD-01 … AD-08) with pre-MTA / pre-F7 / pre-separate-mode resolution conditions.

## Recent commits on branch (R16+R17+R18+R19+R20 batch)

```
4243f34 docs(invoicing): F4 PR description for ship
38ca255 chore(workspace): agent-assign speckit extension + F4 agent memory + ops scripts
f814685 fix(invoicing): F4 R20 polish — port sum type + error sanitise + lang cleanup
f56ce2e fix(invoicing): F4 QA + R19 — close 2 blockers + 5 accepted-debt
af7c0bc fix(invoicing): F4 Phase 10 R16+R17+R18 review remediation
8522e05 chore: members directory desktop screenshots (before/after)
```

## Next actions (post-push)

1. Wait for GitHub Actions / Vercel CI to run — check at [PR #12 Checks tab](https://github.com/Jirawatpyk/Swedish-chaplain_Membership/pull/12/checks).
2. Spot-check Vercel Preview deployment when it lands.
3. Flip Draft → "Ready for review" once CI is green + Preview smoke looks good.
4. (Optional) Close PG-2 Resend DPA before merge if `FEATURE_F4_VOID_ATTACHMENT=true` day-1 is desired.
5. Squash-merge on GitHub → auto-close this spec's 80-commit branch into a single `main` commit.
6. Post-merge: consider `/speckit.retro` for F4 retrospective.

---

*Generated by `/speckit-ship-run` — 2026-04-22 16:41:13 local.*
