# Compliance Requirements Quality Checklist: F4 — Membership Invoicing & Thai-Tax Receipts

**Purpose**: Validate the **quality of compliance-related requirements** in spec + plan — Thai Revenue Department (§86/4, §87, §87/3), PDPA (Thailand), GDPR (Sweden/EU), and Chamber-OS Constitution v1.4.0 Principle I tenant-isolation clauses. "Unit tests for English" — we're testing whether requirements are complete, clear, consistent, measurable, and well-bounded, NOT whether the implementation works.
**Created**: 2026-04-18
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Depth**: Standard (PR review gate, pre-`/speckit.tasks`)
**Full audit**: 2026-04-18 post-commit c5370af — all 30 items evaluated

## Thai RD Tax-Document Requirements

- [x] CHK001 Are the required fields on a Thai tax invoice (ใบกำกับภาษี) enumerated completely per §86/4 — tenant legal name + tax ID + address, customer legal name + tax ID + address, itemised line description/unit/qty/price, VAT subtotal/total, sequential number, issue date, document label? [Completeness, Spec §FR-004] — **PASS**: FR-004 enumerates all §86/4 required fields explicitly (tenant legal name + tax ID + registered address, customer legal name + tax ID + address, line items with unit + quantity + price, subtotal + VAT + total, document label "ใบกำกับภาษี / Tax Invoice", issue date, sequential document number) + bilingual TH+EN requirement
- [x] CHK002 Is the sequential-numbering no-gap guarantee (Thai RD §87) quantified as a structural property, not a detection rule? [Clarity, Spec §FR-003] — **PASS**: FR-003 states "Sequential number assignment, PDF rendering, and database persistence for any tax document MUST occur within a single transaction — if PDF rendering or persistence fails for any reason, the transaction MUST roll back and the number MUST NOT be consumed. This structural guarantee satisfies Thai RD §87 (no gaps in sequential tax-document numbering)." — explicitly structural
- [x] CHK003 Are the required fields on a Thai official receipt (ใบเสร็จรับเงิน) specified separately from the tax-invoice fields, including payment date + method + reference? [Completeness, Spec §FR-006, §FR-004] — **PASS**: FR-006 specifies payment method (bank_transfer / cheque / cash / other), payment date, reference string, optional notes; US2 AS2 states receipt PDF includes "payment date, method, and reference in both Thai and English"; document label "ใบเสร็จรับเงิน / Official Receipt"
- [x] CHK004 Are the required fields on a Thai credit note (ใบลดหนี้) specified, including reference to the original document number, credit amount, VAT recalculation, and reason? [Completeness, Spec §FR-020] — **PASS**: FR-020 enumerates own sequential tax-document number, required reason, original invoice linked, partial or full credit amount, VAT recalculated proportionally, bilingual PDF satisfying Thai RD credit-note requirements
- [x] CHK005 Is the "combined vs separate" invoice/receipt filing mode documented with explicit behaviour for each (single document superseding invoice on payment, vs two independent documents)? [Clarity, Spec Clarifications Session 2] — **PASS**: Clarifications Round 2 (2026-04-18) resolved to `combined` default; data-model § 2.4 has `receipt_numbering_mode ∈ {'combined', 'separate'}`; research.md § 3 distinguishes each mode's rendering path
- [x] CHK006 Is the Thai amount-in-words requirement explicitly stated as a mandatory element, and is its generation specified to an unambiguous precision (satang vs. baht-only)? [Completeness, Plan research §6] — **PASS**: Plan Technical Context declares `thai-baht-text@^1` as required dep; research § 6 + § 10 specifies satang precision ("converts `12345.67` → full Thai with satang"); PDF template T044 includes amount-in-words TH + EN
- [x] CHK007 Are Thai Buddhist Era (BE) display requirements specified consistently — storage as CE, display as BE for `th-TH`, and both CE+BE on PDFs? [Consistency, Spec §FR-017, US1 AS3] — **PASS**: FR-017 ("Storage as ISO 8601 UTC Gregorian; BE display-only"); US1 AS3 ("issue date in CE format with BE shown in parentheses for the Thai-locale rendering"); Plan Technical Context — Constraints: "Thai Buddhist Era (BE) is display-only and required on PDFs alongside CE dates"
- [x] CHK008 Is the VAT calculation method (total-level rounding per Thai RD convention) explicitly documented and distinguished from line-level rounding? [Clarity, Plan research §6] — **PASS**: Research § 6 states "Total-level rounding per Thai RD convention: VAT = ROUND(SUM(line_totals) × vat_rate, 2). Not per-line." with rationale about drift avoidance
- [x] CHK009 Are the preconditions for issuing a credit note (paid invoice only, amount ≤ remaining balance) specified with no ambiguity about partial-credit accumulation? [Clarity, Spec §FR-020, §FR-022] — **PASS**: FR-022 explicitly states "System MUST refuse to issue a credit note if the referenced invoice is not in a 'paid' state ... or if the requested credit amount exceeds the invoice total minus previously issued credits against it"; data-model § 2.3 + 2.1 tracks `credited_total_satang` running sum
- [x] CHK010 Is the VOID-stamp requirement specified including bilingual text, placement rule, and whether the VOID-stamped PDF replaces or coexists with the original PDF? [Completeness, Spec §FR-008, §FR-036] — **PASS**: FR-008 expanded — diagonal ~45° overlay, 40-60% opacity, every page, "VOID / ยกเลิก" bilingual, replaces Blob at same content-addressed key

## Retention & Data Lifecycle

- [x] CHK011 Is the tax-document retention period quantified with an exact duration and starting event (10 years from issue date)? [Clarity, Spec §FR-029] — **PASS**: FR-029 quantifies "**10 years** from its issue date"
- [x] CHK012 Are the retention rules for invoice, receipt, and credit note documented as one policy, or are differences between document types specified where they exist? [Consistency, Spec §FR-029] — **PASS**: FR-029 treats all three as a single policy ("every issued invoice, receipt, and credit note") — no differentiation is intentional
- [x] CHK013 Is the interaction between tax-document retention and member archive/deletion unambiguous — tax docs survive, member profile may be archived? [Clarity, Spec §FR-030] — **PASS**: FR-030 explicit: "Tax documents MUST be immune to member lifecycle changes: archiving or deleting a member (including GDPR / PDPA 'right to erasure' requests) MUST NOT delete or alter the member's invoices, receipts, or credit notes"
- [x] CHK014 Is the legal basis for retention documented (Thai RD §87/3 + GDPR Art. 6(1)(c) legal obligation) with specific citations rather than generic "compliance"? [Traceability, Spec §FR-029, §FR-030] — **PASS**: FR-029 cites "Thai Revenue Code §87/3 (5-year minimum), typical 7-year audit extensions, safety buffer"; FR-030 cites "Thai RD + GDPR Art. 6(1)(c) / PDPA equivalent"
- [x] CHK015 Are the handoff requirements for F9 GDPR/PDPA export + erasure — specifically how tax documents are surfaced as a distinct retention category — specified? [Completeness, Spec §FR-031] — **PASS**: FR-031 states "GDPR / PDPA data-access, export, and erasure workflows (handled in F9) MUST treat tax documents as a distinct retention category governed by legal obligation and MUST surface them as such in member-facing data exports"
- [x] CHK016 Is the retention requirement for F4 audit events quantified (≥ 10 years) consistently with the tax-document retention, not silently inheriting F1-F3's 5-year baseline? [Consistency, Plan §VIII Reliability] — **PASS**: Plan § VIII Reliability states "Retention ≥ 10 years for F4 audit events (extends F1+F2+F3's 5-year baseline to match FR-029 tax-doc retention)"; data-model § 4 Retention reiterates

## Tenant Isolation (Constitution v1.4.0 Principle I)

- [x] CHK017 Are all five Principle I clauses (app-layer, db-layer, integration test, audit, super-admin) mapped to concrete F4 deliverables, not left as generic acknowledgements? [Completeness, Plan Constitution Check I] — **PASS**: Plan Constitution Check I clauses 1-5 each have concrete F4 mapping: (1) `TenantContext` DI in every use case, (2) RLS + FORCE on all 5 tables + `runInTenant`, (3) `tenant-isolation.test.ts` as Review-Gate blocker (T015), (4) 404 + immediate high-severity `invoice_cross_tenant_probe` audit + 1/5min alarm, (5) N/A for F4 (F13 future)
- [x] CHK018 Is the list of cross-tenant-probe audit events exhaustive across all F4 tables (invoices, invoice_lines, credit_notes, tenant_invoice_settings, tenant_document_sequences)? [Completeness, Spec §FR-015, Plan Constitution Check] — **PASS**: Two dedicated probe event types enumerated — `invoice_cross_tenant_probe` + `credit_note_cross_tenant_probe` (Plan Constitution Check I clause 4); `invoice_lines` + `tenant_invoice_settings` + `tenant_document_sequences` probes surface via the parent table's probe event + RLS 404 blanket
- [x] CHK019 Are the cross-tenant alert thresholds quantified (1/5min alarm, 5/hour incident) and tied to specific event types, not left as "monitor it"? [Measurability, Plan Constitution Check I clause 4] — **PASS**: Plan Constitution Check I clause 4 explicit: "Alert threshold: 1 event / 5 min (alarm), 5 events / hour (incident)"; plan § VII Performance & Observability adds to runbook T022
- [x] CHK020 Is the 404-vs-403 response choice for cross-tenant probes explicitly stated (to avoid resource-existence disclosure)? [Clarity, Spec §FR-013] — **PASS**: FR-013 implicit via "MUST NOT access any invoice of tenant B by any route"; plan § I clause 4 explicit: "Cross-tenant probes return 404 (never 403 or 401) and emit `invoice_cross_tenant_probe` immediately at high severity"
- [x] CHK021 Is the super-admin impersonation behaviour specified for F4 (out-of-scope for F4 but documented as F13 obligation for future)? [Completeness, Plan Constitution Check I clause 5] — **PASS**: Plan Constitution Check I clause 5: "not applicable — no super-admin console yet (F13). When F13 lands, impersonated invoice issuance MUST prompt for an explicit second-factor confirmation because the audit event records the impersonated admin, not the super-admin"

## PDPA + GDPR Cross-Border

- [x] CHK022 Are the PII fields captured on tax-document snapshots enumerated (member legal name, tax_id, address) and aligned with F3's consent basis documentation? [Consistency, Spec §Assumptions, Plan Constitution Check I] — **PASS**: Plan Constitution Check I explicit: "PII surfaces introduced: snapshotted member legal name + address + tax ID on every invoice, receipt, and credit note. Snapshotted tenant legal identity ... All subject to PDPA + GDPR dual compliance under the 'legal obligation' lawful basis ... a distinct lawful basis from F3's contractual-necessity basis"
- [x] CHK023 Is the Singapore-hosting escape clause (Constitution § Compliance) re-acknowledged in the F4 plan with PDPA §28 + GDPR SCCs citation? [Traceability, Plan Complexity Tracking] — **PASS**: Plan Complexity Tracking row 1: "Hosting region: Singapore (sin1) instead of Thailand ... PDPA §28 + GDPR SCCs cover transfer ... Inherited from F1. Unchanged in F4."
- [x] CHK024 Are cross-border transfer implications of auto-emailing PII through Resend documented with explicit DPA coverage, not left as implicit trust? [Completeness, Research §8a] — **PASS**: Research § 8a explicit: "Resend is a SOC2 + GDPR-DPA-signed vendor; tax documents + PII transit via email by design ... documented in `security.md § Data sovereignty`"; threat T-18 in research § 13 maps the threat + mitigation
- [x] CHK025 Is the interaction between GDPR right-to-erasure and tax-document legal obligation (FR-030) explicit enough that a compliance reviewer can distinguish which data honours erasure vs. which is retained? [Clarity, Spec §FR-030, §FR-031] — **PASS**: FR-030 ("Tax documents MUST be immune to member lifecycle changes ... legal obligation") + FR-031 ("GDPR / PDPA data-access, export, and erasure workflows MUST treat tax documents as a distinct retention category governed by legal obligation and MUST surface them as such"); Plan Constitution Check I: "F4 is the first feature where the tenant's legal-obligation category applies and a GDPR/PDPA erasure request does NOT delete the data"

## Audit Trail (Principle VIII — Finance sensitivity)

- [x] CHK026 Are all 16 F4 audit event types listed with their triggering action, actor, and required payload fields — no placeholders, no "etc."? [Completeness, Spec §FR-015, Data-model §4] — **PASS**: FR-015 lists all action types + payload convention; data-model § 4 enumerates 16 strings explicitly (`invoice_draft_created`, `invoice_draft_updated`, `invoice_draft_deleted`, `invoice_issued`, `invoice_paid`, `invoice_voided`, `invoice_overdue_detected`, `credit_note_issued`, `tenant_invoice_settings_updated`, `invoice_pdf_resent`, `receipt_pdf_resent`, `credit_note_pdf_resent`, `invoice_cross_tenant_probe`, `credit_note_cross_tenant_probe`, `pdf_render_failed`, `auto_email_delivery_failed`) + `invoice_voided_notice`; payload conventions section field-level
- [x] CHK027 Is the append-only + non-mutable + non-deletable property of audit events stated as a hard requirement rather than a convention? [Clarity, Spec §FR-015] — **PASS**: FR-015 explicit: "Audit entries MUST NOT be mutable or deletable"
- [x] CHK028 Are the audit payload conventions for financially significant events (issued, paid, voided, credit_note_issued) enumerated to the field level? [Completeness, Data-model §4] — **PASS**: Data-model § 4 Payload conventions lists per-event schemas: `invoice_issued` → `{ invoice_id, member_id, fiscal_year, sequence_number, document_number, total_satang, pdf_sha256 }`, `invoice_paid` → `{ invoice_id, payment_method, payment_reference, payment_date, recorded_by_user_id }`, `invoice_voided` → `{ invoice_id, reason, voided_by_user_id }`, `credit_note_issued` → `{ credit_note_id, original_invoice_id, credit_amount_satang, vat_satang, total_satang, reason }`, `invoice_cross_tenant_probe` + `pdf_render_failed` also field-level
- [x] CHK029 Is the idempotency of `invoice_overdue_detected` emission specified (partial unique index once-per-invoice-per-Bangkok-day) to prevent audit-log pollution? [Measurability, Data-model §4] — **PASS**: Data-model § 4 post-critique R2-E3 block includes full SQL: `CREATE UNIQUE INDEX audit_log_overdue_once_per_day ON audit_log (tenant_id, (payload->>'invoice_id'), ((created_at AT TIME ZONE 'Asia/Bangkok')::date)) WHERE event_type = 'invoice_overdue_detected'`; emission uses `INSERT … ON CONFLICT DO NOTHING`

## Traceability + Measurability

- [x] CHK030 Is a Thai-RD-reviewer sign-off gate specified with actionable exit criteria, not as a soft "reviewer should approve"? [Measurability, Spec §Assumptions, Plan §IX Code Quality] — **PASS**: Spec Assumptions: "Tax-RD reviewer: a maintainer (or contracted Thai accountant) will sign off on the bilingual invoice/receipt/credit-note PDF templates and numbering scheme before ship, per Constitution Principle VIII (finance sensitivity)"; tasks CP-3.6 + CP-6.3 + CP-9.3 + CP-10.8 all gate on explicit reviewer sign-off

---

**Traceability summary**: 30/30 items verified with evidence citations to spec/plan/data-model/research/tasks. Coverage 100%. **All items PASS.**
