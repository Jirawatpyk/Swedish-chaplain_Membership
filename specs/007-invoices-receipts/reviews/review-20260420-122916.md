# F4 Invoicing & Thai-Tax Receipts — Staff Review (Phase 6 US6 + R1)

**Branch**: `007-invoices-receipts` @ `5a6abef`
**Date**: 2026-04-20 (UTC 12:29:16)
**Scope**: Phase 6 US6 Credit Notes (ใบลดหนี้) + R1 review-fix cycle. Commits `db999a6..HEAD` (5 commits, 42 files, +3862 / -35 LOC).
**Method**: Direct code read on HEAD across all Phase 6 sources + test files, cross-checked against `/speckit-review-run` output (16 findings) and the just-landed fix commit `5a6abef` (closed 11 of 16).
**Prior reviews**: R10 staff review approved MVP ship at `0eb49de` (pre-Phase 6). This review continues from that baseline.
**Constitution**: v1.4.0.

---

## 1. Executive Summary — Verdict

### ⚠️ APPROVED WITH CONDITIONS

All 1 Critical + 7 Important findings from R1 are closed in commit `5a6abef`. Code paths verified on HEAD. 5 Suggestions are deferred by design with written rationale in the commit body. No Blockers. No open Warnings.

**Ship conditions**:
1. Before merge: run full CI (`pnpm lint && pnpm typecheck && pnpm test:coverage && pnpm check:i18n && pnpm check:layout && pnpm test:integration && pnpm test:e2e --workers=1`) on HEAD.
2. File T115 throwaway-tenant fixture in Phase 10 backlog so the 2 E2E `test.fixme` mutating paths (CN AS1 happy + CN AS2 happy) can be un-fixmed.
3. File S-7 `renderAndUploadPdf` helper extraction as Phase 10 cross-cutting polish (also applies to `issueInvoice` + `recordPayment`).

| Severity | Count | Status |
|----------|-------|--------|
| 🔴 Blocker | **0** | — |
| 🟡 Warning | **0** | — |
| 🟢 Suggestion | 5 (deferred) | Tracked in § 6 |

Observed state on HEAD:
- `pnpm typecheck` — green
- `pnpm lint` — green
- unit + contract: **211 / 211 passing** across 19 files (includes new N=3, N=5 drift-bound + zero-body contract cases)
- invoicing integration on live Neon: green (`credit-note-partial-accumulation` 8/8, `pdf-deterministic` 1/1, peers unchanged)

---

## 2. Scope & Change Inventory

### Phase 6 commits (chronological)

| Commit | Subject | Scope |
|--------|---------|-------|
| `db999a6` | Phase 6 (US6) — credit notes main | T075–T082: domain, policies, use-case, repo, route, form, tests |
| `fabcd85` | AS4 Tier 2 — credited-invoice PDF annotation | inline J2 re-render block in issue-credit-note; invoice-template creditedStamp + cnRefFooterBlock |
| `e9950e3` | R0 review fixes — CR-1 + 7 Important + 7 Suggestions | first review cycle closure |
| `67b604a` | Co-run flake — deterministic-render pino guard + cold-load timeout | stabilises contract test co-run |
| `5a6abef` | R1 review fixes — 11 findings | Critical C-1 synthetic line, 7 Important, 3 Suggestions |

### Surfaces touched (R1 only — 10 files)

| Area | File |
|------|------|
| Application | `src/modules/invoicing/application/use-cases/issue-credit-note.ts` |
| Route | `src/app/api/credit-notes/route.ts` |
| Route | `src/app/api/credit-notes/[creditNoteId]/route.ts` |
| Route | `src/app/api/credit-notes/[creditNoteId]/pdf/route.ts` |
| UI | `src/app/(staff)/admin/invoices/[invoiceId]/credit-notes/new/_components/credit-note-form.tsx` |
| Infrastructure | `src/modules/invoicing/infrastructure/adapters/vercel-blob-adapter.ts` |
| Unit test | `tests/unit/invoicing/calculate-credit-note-vat.test.ts` |
| Contract test | `tests/contract/credit-notes-route.test.ts` |
| Integration test | `tests/integration/invoicing/credit-note-partial-accumulation.test.ts` |
| Script | `scripts/clear-test-data.ts` |

---

## 3. Closure Verification (R1 Findings)

### 🔴 Critical — CLOSED

| ID | File:Line on HEAD | Fix confirmed |
|----|-------------------|---------------|
| **C-1** | `src/modules/invoicing/application/use-cases/issue-credit-note.ts:262–284` | `lines: loaded.lines` replaced with a single synthetic `registration_fee`-kind line (desc bilingual "Credit against {origDocNum}", `unitPrice = total = creditAmount`, `qty 1.0000`, `proRateFactor null`). Line-sum === totals-block on full + partial + multi-partial CNs. `asInvoiceLineId` imported from `domain/invoice-line.ts`. ✅ |

### 🟡 Important — CLOSED (7 of 7)

| ID | File:Line on HEAD | Fix confirmed |
|----|-------------------|---------------|
| **I-1** | `src/app/api/credit-notes/route.ts:36` | `settings_missing: 422` (aligned with issue-invoice semantics — Unprocessable Entity for server-side config gap, not 409 Conflict). Contract test updated (line 219). ✅ |
| **I-2** | `credit-note-form.tsx:108–113` | `router.refresh()` removed; inline comment explains the destination page (RSC) fetches fresh on mount, so refresh on the abandoned form route was a no-op at best. ✅ |
| **I-3** | `issue-credit-note.ts:451–466` | `applyInvoicePdfRegeneration` wrapped in `try/catch`; on throw, logs structured `{ err, invoiceId, creditNoteId }` at `error` level then throws `IssueCreditNoteInternalError({ code: 'concurrent_state_change' })`. Parity with J-step rollup pattern. ✅ |
| **I-4** | `src/app/api/credit-notes/[creditNoteId]/route.ts:26–54` | Full use-case + `serialiseCreditNote` wrapped in `try/catch`; unexpected throw (corrupt `document_number` / `pdf_sha256` / VAT-balance repo throws) → `logger.error` + `{ error: { code: 'internal_error' } }` @ 500 instead of unlogged runtime. ✅ |
| **I-5** | `src/app/api/credit-notes/[creditNoteId]/pdf/route.ts:32–55` | `getCreditNotePdfSignedUrl` wrapped. Type-narrowing via `let result: Awaited<ReturnType<...>>` keeps the downstream `result.ok` branch typed identically. ✅ |
| **I-6** | `tests/integration/invoicing/credit-note-partial-accumulation.test.ts:454, 471` | Pin added: `expect(afterRow.sha).toBe('b'.repeat(64))` AND `expect(payload.new_sha256).toBe('b'.repeat(64))`. Catches regression where `applyInvoicePdfRegeneration` writes a wrong sha (e.g. the CN's sha instead of the re-render's). ✅ |
| **I-7** | `tests/unit/invoicing/calculate-credit-note-vat.test.ts:101–152` | New `describe` block adds deterministic 3-part (⌈3/2⌉=2 drift bound) + 5-part (⌈5/2⌉=3 drift bound) cases. Property test stays bounded to N≤2 with tight ±1-satang guarantee. Both new tests pass. ✅ |

### 🟢 Suggestions — CLOSED (3 of 7)

| ID | File:Line on HEAD | Fix confirmed |
|----|-------------------|---------------|
| **S-3** | `tests/contract/credit-notes-route.test.ts:169–180` | New contract case: `creditTotalSatang: '0'` → 400 `invalid_body`. Also asserts `issueCreditNoteMock` never invoked (boundary rejection confirmed). Relies on `z.bigint().positive()` in `issueCreditNoteSchema:75`. ✅ |
| **S-6** | `src/modules/invoicing/infrastructure/adapters/vercel-blob-adapter.ts:79–91` | Stale "signed-URL on Vercel's roadmap" comment replaced with accurate current-state: bytes proxied through our own API route (`/api/.../pdf`), Blob URL never client-exposed, FR-005 T-05 mitigation satisfied by proxy. ✅ |
| **bonus** | `scripts/clear-test-data.ts:97–112` | Added DELETE on `credit_notes` → `invoice_lines` → `invoices` → `tenant_document_sequences` (scoped by `tenant_id LIKE 'test-%'`) BEFORE the users DELETE, so `invoices.draft_by_user_id` / `paid_by_user_id` / `voided_by_user_id` FK (ON DELETE restrict) no longer blocks idempotent cleanup. Discovered during fix-cycle verification — integration suite's `clear-test-data.test.ts` was failing pre-fix. ✅ |

### 🟢 Deferred Suggestions — ACCEPTED (with written rationale)

| ID | Rationale for deferral |
|----|------------------------|
| **I-8** `CreditNote.tenantId` brand | Reviewer's premise wrong: `Invoice.tenantId` is also plain `string` (invoice.ts:82). No drift between the two. Fixing one without the other would CREATE inconsistency. Handle as cross-module refactor across the invoicing Domain in Phase 10 if desired. |
| **S-1** `allowOverwrite` required-explicit | Would break every existing non-F4 caller (invoice uploads, logo uploads). Breadth outside Phase 6 scope. Current optional-with-`?? false`-default is safe because the adapter explicitly checks `allowOverwrite ?? false` at `vercel-blob-adapter.ts:35`. |
| **S-2** `code` vs `kind` discriminant convention | Documentation-only, no code change. Can land in project conventions doc when next refresh hits. |
| **S-4** AS3 `draft` → `invalid_status` integration test | DB constraint `invoices_non_draft_has_snapshots` blocks a draft-shaped invoice from reaching the use-case guard. Existing integration test comment at line 480–483 already documents this architectural reality. Marginal coverage gain vs seeding complexity. |
| **S-5** Strip review-ticket IDs (`IM-*`, `SG-*`, `CR-*`) from comments | Active-feature breadcrumbs — removing while feature is still in-flight loses navigation value. Clean sweep is appropriate post-ship when backlog tickets are archived. |
| **S-7** Extract `renderAndUploadPdf` helper | Cross-cutting: same pattern duplicated in `issueInvoice` + `recordPayment` use-cases. Belongs in Phase 10 polish pass so the refactor lands once across all three use-cases, not piecemeal in the CN-only path. |

---

## 4. Correctness & Logic — Fresh Pass

### 4.1 Synthetic line arithmetic (C-1 fix)

Verified at `issue-credit-note.ts:262–284`:
- `syntheticLine.unitPrice = creditAmount` and `syntheticLine.total = creditAmount` — identical because quantity=1, proRateFactor=null. Line-sum (1 × creditAmount) = subtotal (creditAmount) passed to template. ✅
- Totals block: `subtotal = creditAmount`, `vat = calculateCreditNoteVat(...)`, `total = creditAmount + vat`. `subtotal + vat = total` invariant holds. ✅
- Template `invoice-template.tsx:265–274` reads `l.descriptionTh`, `l.descriptionEn`, `l.unitPrice`, `l.total` — all present on synthetic line. No undefined access. ✅
- Bilingual: TH `"ลดหนี้ตาม {origDocNum}"` + EN `"Credit against {origDocNum}"` — meets Thai RD §86/4 bilingual requirement inherited from template. ✅

### 4.2 Regen error handling (I-3 fix)

Verified at `issue-credit-note.ts:451–466`:
- `try/catch` wraps the repo call. `throw` inside `withTx` triggers rollback — invoice row CREDITED flip is reverted if the regen write fails (consistent with step J rollup pattern). ✅
- `logger.error` at `error` level (upgraded from the siblings' `warn` for a mutation failure) with stringified `err` + contextual ids. No forbidden fields. ✅

### 4.3 Route try/catch wraps (I-4 + I-5)

Verified at both routes:
- `getCreditNote(...)` and `getCreditNotePdfSignedUrl(...)` now inside `try/catch`. Caught throws → structured `logger.error` + 500 response with `{ error: { code: 'internal_error' } }`. ✅
- The `result.ok` type narrowing is preserved — `let result: Awaited<ReturnType<...>>` declaration + assignment inside try. ✅
- No double-log: `logger.warn` in the `!result.ok` branch fires on typed errors only. ✅

### 4.4 Router-flow (I-2 fix)

Verified at `credit-note-form.tsx:108–113`:
- `router.push(invoice-detail)` is now the last navigation call. Destination RSC re-fetches on mount. ✅
- No stray `router.refresh()` / `startTransition` / `formAction` interactions. ✅

### 4.5 Clear-test-data DELETE order (bonus fix)

Verified at `scripts/clear-test-data.ts:97–112`:
- Order: `credit_notes` → `invoice_lines` → `invoices` → `tenant_document_sequences` → `email_change_tokens` → `notifications_outbox` → `contacts` → `members` → `plans` → `invoice_settings` → `invitations` → `users`. ✅
- All F4 deletions scoped to `tenant_id LIKE 'test-%'` — no production-tenant risk. ✅
- `audit_log` intentionally skipped (Principle VIII append-only). ✅

---

## 5. Constitution Alignment

| Principle | Status | Evidence |
|-----------|--------|----------|
| **I** Tenant isolation + RLS | ✅ | All CN reads/writes via `runInTenant(ctx, fn)`; `findByOriginalInvoiceInTx` reuses caller tx; RLS `credit_notes_tenant_isolation` active. |
| **II** Test-First / TDD | ✅ | Integration test precedes implementation; contract test covers HTTP boundary; property + deterministic unit cases; coverage thresholds unchanged. |
| **III** Clean Architecture | ✅ | Domain has no `next`/`drizzle`/`react`/`resend` imports (verified in `credit-note.ts`, policies). Application imports ports only. ESLint `no-restricted-imports` unchanged. |
| **IV** PCI DSS | N/A | No card data paths in CN flow. |
| **V** i18n (EN/TH/SV) | ✅ | CN PDF bilingual TH+EN (synthetic line desc in both); form labels via next-intl; 723 i18n keys × 3 locales unchanged. |
| **VI** Inclusive UX | ✅ | Typed-phrase gate FR-040 preserved; keyboard-submit path unchanged; form uses FormContainer 42rem. |
| **VII** Perf & Observability | ✅ | Added structured logs on new error paths (I-3, I-4, I-5). No new metric required (reuses existing invoice audit path). |
| **VIII** Append-only audit | ✅ | `credit_note_issued` + `invoice_pdf_regenerated` events; `clear-test-data` skips `audit_log`. |
| **IX** Code review ≥2 (solo-maintainer substitute) | ✅ | R0 + R1 agent review cycles closed; staff review (this doc) as ship gate. |
| **X** Simplicity | ⚠️ | `issue-credit-note.ts` at ~560 LOC with A–M + J2 letter flow is complex but the structure mirrors `issueInvoice` + `recordPayment`. `renderAndUploadPdf` extraction (S-7) in Phase 10 will compress all three. |

No violations. No new Complexity Tracking entries required.

---

## 6. Test Coverage Assessment

### 6.1 Acceptance Scenario coverage (US6)

| AS | Integration | Contract | Unit | E2E | Note |
|----|-------------|----------|------|-----|------|
| **AS1** full credit | ✅ `credit-note-partial-accumulation.test.ts:302–328` | ✅ 201 happy | ✅ policies | ⚪ `test.fixme` (T115) | Mutating E2E deferred to throwaway-tenant fixture; integration exercises the transition + audit. |
| **AS2** partial + accumulation | ✅ `:239–300` + `:330–354` | ✅ `credit_exceeds_remainder` with bigint serialise | ✅ `enforce-credit-cannot-exceed-remainder` 4 branches | ⚪ `test.fixme` (T115) | Same disposition as AS1. |
| **AS3** rejection (unpaid / void / terminal / over-remainder) | ✅ `:478–520` + rejection cases | ✅ 7 error→HTTP mappings | ✅ policy branches | ✅ FR-040 typed-phrase gate on UI | `draft` branch is DB-blocked; documented in-line. |
| **AS4** credited-invoice rollup re-render | ✅ `:364–476` full chain | — | — | ⚪ | sha + audit payload pinned (I-6); `allowOverwrite=true` asserted (CR-1). |

### 6.2 Numeric invariants

- §87 no-gaps sequential numbering — covered by `seq-number-atomicity.test.ts` on live Neon + explicit concurrent-race test at `:559–613` (Promise.all 60%+60% → exactly 1 success + 1 `credit_exceeds_remainder`).
- Proportional VAT invariant — fast-check property (500 runs, N≤2, ±1 satang) + new deterministic N=3 (⌈3/2⌉) + N=5 (⌈5/2⌉) drift bounds.
- Credit-note VAT balance — `assertCreditNoteVatBalance` Runtime guard in repo row→domain mapping; unit tested at domain level.

### 6.3 Observability & audit

- `credit_note_issued` + `invoice_pdf_regenerated` — both emit in integration test (verified at `:458–476`).
- Structured logs on error paths — `logger.error` with request/tenant/id context; `logger.warn` for rate-limit + typed-error surface.
- Forbidden fields check — no `password`, session id, reset token, invitation token, or `Authorization` in any new log call. ✅

### 6.4 Coverage gaps (all non-blocking)

| Gap | Severity | Disposition |
|-----|----------|-------------|
| 2 × E2E `test.fixme` mutating happy paths | 🟢 | T115 throwaway-tenant fixture in Phase 10. Integration layer gives full correctness coverage; E2E fixme leaves only UX regression net partial. |
| AS3 `draft` status not integration-tested | 🟢 | DB constraint blocks; in-line comment documents. |
| Property test N≥3 via fast-check | 🟢 | Replaced with deterministic pinning (I-7); real bookkeeping rarely issues >2 partials. |
| `signDownloadUrl` ttl behaviour | 🟢 | Current proxy approach documented (S-6); no per-request signing. |

---

## 7. Performance & Scalability

- Hot-path DB operations: all within a single `withTx` — invoice FOR UPDATE → advisory-lock → seq → insert CN → rollup → regen → audit. No N+1. Indexes on `credit_notes(tenant_id, original_invoice_id)` support the `findByOriginalInvoice` lookup. ✅
- `findByOriginalInvoiceInTx` + `.slice().sort()` for annotation refs — N is small (typically 1-3 CNs per invoice); no paging needed. Defensive sort kept (reviewer's simplify suggestion deferred). ✅
- PDF render serialized via module-level mutex (`renderChain`) in `deterministic-render.ts`. F4 SC-003 byte-identical. Throughput bounded by `@react-pdf` itself (~1-2 s/doc) — acceptable at SweCham scale. ✅
- `allowOverwrite=true` on AS4 re-render — single Blob PUT; no retry loop. ✅

No performance regressions observed. No SLO impact.

---

## 8. Security

- RBAC: route guards at `requireAdminContext` → reject manager + member. Contract test verifies 403 on manager role. ✅
- Rate limit: `f4:credit-note:{tenant}:{user}` 20 per 5 min per `auth-deps.rateLimiter`. Contract test verifies 429 + `Retry-After` header. ✅
- Tenant isolation: `resolveTenantFromRequest` + RLS. Cross-tenant probe code `member_cross_tenant_probe` audit event unchanged. ✅
- Input validation: `issueCreditNoteSchema` zod with `z.bigint().positive()`, UUID v7 invoice id, reason length bounds. Contract test covers zero, negative, non-numeric, missing. ✅
- No secrets in logs; forbidden-field check passes. ✅
- SAQ-A: N/A for CN flow.

No Blockers. No Warnings.

---

## 9. Spec Compliance Matrix

| FR | Status | Evidence |
|----|--------|----------|
| **FR-016** byte-identical PDF + explicit allowOverwrite for AS4 | ✅ | `vercel-blob-adapter.ts:35–50` honours `allowOverwrite`; AS4 path sets `true`; integration pins sha changes (I-6). |
| **FR-020** CN with own tax number + original link + VAT proportional + bilingual PDF | ✅ | `issue-credit-note.ts` full chain; `invoice-template.tsx` CN kind renders ใบลดหนี้ + origDocNum ref block + synthetic line. |
| **FR-021** transition invoice → credited / partially_credited, preserve original fields | ✅ | `applyCreditNoteRollup` updates `status` + `credited_total_satang` only; re-render via `applyInvoicePdfRegeneration` whitelisted for `pdf_sha256` by `invoices_immutable` trigger. |
| **FR-022** refuse unpaid / over-remainder | ✅ | `enforce-credit-cannot-exceed-remainder.ts` + use-case status guard; integration tests AS3 + AS2 cover each branch. |
| **FR-023** audit `credit_note_issued` | ✅ | Emitted at step K with full payload (credit_amount_satang, vat_satang, total_satang, reason, document_number, pdf_sha256). |
| **FR-040** typed-phrase confirmation | ✅ | `credit-note-form.tsx` requires typed "CREDIT" to enable submit; 3 non-mutating E2E cases exercise the gate. |

No scope creep. No missing coverage relative to US6 spec.

---

## 10. Metrics

- Files reviewed: **42** (Phase 6 scope) + **10** (R1 fix scope; subset)
- Code changed in R1: `+204 / -58` across 10 files
- Findings from `/speckit-review-run`: **16** (1 Critical + 8 Important + 7 Suggestions)
- Findings closed in `5a6abef`: **11** (1 + 7 + 3)
- Findings deferred with rationale: **5** (0 + 1 + 4)
- Current open findings: **0** Blocker, **0** Warning, **5** Suggestion
- Spec coverage (US6): **100%** of FR-020…023 + FR-040 + FR-016 integration verified
- Test counts on HEAD:
  - Unit + contract: **211 / 211** green (19 files)
  - Invoicing integration: green on live Neon
  - E2E CN non-mutating: green across 3-browser matrix
  - E2E CN mutating: **2 × `test.fixme`** (T115 pending)

---

## 11. Recommended Actions

### Before ship (merge blockers — none)

None. All Blockers + Warnings are closed.

### Before ship (merge conditions — 3 tracking items)

1. Run full CI chain locally on HEAD: `pnpm lint && pnpm typecheck && pnpm test:coverage && pnpm check:i18n && pnpm check:layout && pnpm test:integration && pnpm test:e2e --workers=1`. Attach green run-log to PR description.
2. Add **T115** "E2E throwaway-tenant fixture for mutating credit-note paths" to Phase 10 tasks.md. Un-fixme the 2 mutating AS1/AS2 happy-path E2E cases once T115 lands.
3. Add **Phase 10 polish item** "Extract `renderAndUploadPdf` Application helper" covering `issueInvoice`, `recordPayment`, `issueCreditNote` (G + J2 sites).

### After ship (Phase 10 polish)

4. Consider cross-module rename for `tenantId: string` → `tenantId: TenantId` across `Invoice` + `CreditNote` aggregates (I-8 deferred).
5. Post-feature-freeze clean sweep of review-ticket ID breadcrumbs (`IM-*`, `SG-*`, `CR-*`) in comments (S-5 deferred).
6. Document discriminant-field convention (`code` for Application errors, `kind` for Domain errors) in project conventions doc (S-2 deferred).

---

## 12. Verdict

**⚠️ APPROVED WITH CONDITIONS** — proceed to `/speckit.ship` after full CI confirmation on HEAD + filing T115 and the `renderAndUploadPdf` Phase 10 item.

Phase 6 US6 Credit Notes is feature-complete, spec-compliant, constitution-clean, and test-covered at the integration + contract layers. The deferred items are either deliberately-scoped polish (S-5, S-7), architectural decisions that need separate cross-module treatment (I-8, S-1), or already-documented reality (S-2, S-4). None block ship.
