# F4 Invoicing & Thai-Tax Receipts — Staff Review R14 (Phase 9 US5 close)

**Reviewer**: AI Agent (Staff Engineer Perspective)
**Date**: 2026-04-21 (UTC 11:21:31)
**Feature**: `specs/007-invoices-receipts/spec.md` (US5 — Admin voids an invoice, P3)
**Branch**: `007-invoices-receipts` @ `796ba80` (+ 1 uncommitted test-seed fix, see Observations)
**Scope**: Full Phase 9 delta — 4 commits (`7caae2a`, `9f8d087`, `2e156ad`, `796ba80`) comprising the US5 void-invoice ship + 3 review cycles + FR-036 attachment compliance + PG-2 DPA gate
**Constitution**: v1.4.0 (10 principles; 4 NON-NEGOTIABLE)
**Method**: Direct source read on HEAD; live Neon Singapore integration tests; unit test run; spec ↔ code cross-reference per FR
**Prior reviews**: R13 closed Phase 8 (2026-04-21 01:30:50). This is R14 opening the Phase 9 gate.

---

## Executive Summary

### ⚠️ APPROVED WITH CONDITIONS

Phase 9 ships US5 (admin void) + FR-036 cancellation email + FR-037 archive-race guard, passing 108/108 F4 integration tests on live Neon Singapore and 231/231 invoicing unit tests. The architecture is sound: two-phase commit (Phase 1 atomic DB+audit+outbox, Phase 2 post-commit blob+sha with discriminated failure audit) correctly resolves the original R-1 storage↔DB desync; a PG-2 DPA feature flag (`FEATURE_F4_VOID_ATTACHMENT=false` default) preserves PDPA §28 cross-border boundary until the Resend DPA amendment is signed; B-1 threaded `voidReason` end-to-end so FR-036's MUST-state-void-reason clause is satisfied; the destructive admin UI uses `<Alert variant="destructive">`, typed-phrase confirm (document number), correct locale-aware copy, and accessibility hooks.

Three prior review cycles identified 16 findings; all code-level issues are closed. The remaining conditions are:

1. **T099 integration test seed fix uncommitted** — discovered during this staff review when running `pnpm test:integration tests/integration/invoicing/issue-vs-archive-race.test.ts`: the test's member seed was missing `status: 'archived'` (DB CHECK `members_archived_at_iff_archived` requires `archived_at IS NOT NULL` and `status='archived'` together). Fix applied locally (1 line); re-run confirms green. **Must be committed before `/speckit.ship`.**
2. **PG-2 DPA legal amendment** — non-code DPO action item; `FEATURE_F4_VOID_ATTACHMENT` must stay `false` in production Vercel env until Resend DPA extension is signed, then flipped via env var (no code redeploy required).
3. **R-1c optimistic-version guard on `applyInvoicePdfRegeneration`** — deferred as theoretical; resend-pdf use case (T107) does not exist yet; re-evaluate when T107 lands.

No 🔴 Blockers. Post-commit of the T099 seed fix, Phase 9 is ready for `/speckit.ship`.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| R14-01 | 🟡 Warning | `tests/integration/invoicing/issue-vs-archive-race.test.ts` | 152–163 | Test Quality | Member seed set `archivedAt` without `status: 'archived'`; DB CHECK `members_archived_at_iff_archived` rejected the INSERT. Test failed on first staff-review run. | **FIXED LOCALLY** — seed now also sets `status: 'archived'`. Commit before ship. |
| R14-02 | 🟢 Suggestion | `src/modules/invoicing/application/use-cases/void-invoice.ts` | 117–120 | Architecture | `sanitiseErrorReason` exported from the use-case file. For a utility now shared with a standalone unit test, consider moving to `src/modules/invoicing/application/lib/sanitise-error-reason.ts` alongside `tx-abort.ts` for cleaner separation. | Defer to Phase 10 polish. Low priority. |
| R14-03 | 🟢 Suggestion | `src/modules/invoicing/infrastructure/email/invoice-auto-email.ts` | 268–274 | Security | `escapeHtml` is an inline minimal implementation (5 entities). The OWASP HTML context allows DOM-based injection via additional contexts (attributes, URL, JS, CSS). The current use site is body text only, so minimal entity escape is sufficient, but a comment citing this scope boundary would help future reviewers. | Add comment. Or swap for `@linter/html-escape` when another surface needs HTML escaping. |
| R14-04 | 🟢 Suggestion | `src/app/api/cron/outbox-purge/route.ts` | all | Operability | New cron route not registered in `vercel.json` (no such file in repo — Vercel cron configured via dashboard). Add a runbook entry or `docs/observability.md` pointer. | Document schedule + CRON_SECRET header requirement. Non-blocking. |
| R14-05 | 🟢 Suggestion | `src/modules/invoicing/application/use-cases/void-invoice.ts` | 352–367 | Observability | Phase 2 failure audit uses `eventType: 'pdf_render_failed'` with `context: 'invoice_void_phase2_sync'`. Dashboard queries are functional via `payload->>'context'`. A follow-up Phase 10 migration could add a dedicated `invoice_pdf_sync_failed` enum value for clean event-type filtering. | Track as Phase 10 task. Not a blocker. |
| R14-06 | 🟢 Suggestion | `src/modules/invoicing/infrastructure/repos/drizzle-invoice-repo.ts` | 629–649 | Reliability | `applyInvoicePdfRegeneration` UPDATE has no optimistic-version WHERE guard. Concurrent writers on the same row could silently overwrite. Real-world impact today is zero (only the void Phase 2 path calls it; resend-pdf T107 is unbuilt). | Add `WHERE status IN ('issued','void','credited','partially_credited')` + optimistic `expected_sha256` when T107 lands. |
| R14-07 | 🟢 Suggestion | `src/modules/invoicing/application/use-cases/void-invoice.ts` | 120–127 | Security | `sanitiseErrorReason` redacts `\d{13}` (Thai tax ID) but does not redact names, emails, or space-separated 13-digit sequences. Documented in T-SAN test. | Tighten regex to `\d[\d\s]{12,}` if member-name leakage is ever observed in `pdf_render_failed` reasons. Low probability today. |

**Categories**: Correctness, Security, Performance, Spec Compliance, Error Handling, Test Quality, Architecture, Observability, Reliability, Operability

---

## Spec Coverage Matrix (US5 + Related FRs)

| Requirement | Status | Evidence |
|-------------|--------|----------|
| **US5 AS1** — issued→void, PDF VOID-stamped, sequential number retired | ✅ Implemented | `void-invoice.ts` Phase 1 guards `status='issued'`; Phase 2 re-renders `kind='void_stamped_invoice'` + overwrites blob same key; `applyVoid` does NOT touch `sequence_number`; T-1 integration assertion confirms `sequenceNumber=1` retained post-void |
| **US5 AS2** — paid→rejected, directed to credit-note | ✅ Implemented | `lockedStatus !== 'issued'` returns typed `invalid_status`; route maps 409; `/admin/invoices/[id]/void` page 404s on non-issued; integration test "refuses to void a paid invoice" green |
| **US5 AS3** — voided→all further actions rejected (terminal) | ✅ Implemented | Same status guard returns `invalid_status`; integration test "refuses to re-void a voided invoice" green |
| **US5 AS4** — bilingual TH+EN cancellation email within 1 min, references doc number + void reason + "no longer payable" | ✅ Implemented | `buildInvoiceAutoEmail` interpolates `{docNumber}` + `{voidReason}` + `{attachmentClause}` across EN/TH/SV; outbox dispatch via cron; 7 unit tests verify copy + locale parity |
| **FR-008** — void overlay bilingual, 40–60% opacity, every page, replaces blob at same key | ✅ Implemented | `invoice-template.tsx` `voidStamp` at rgba(200,0,0,0.5)=50%, rotate(-45deg), `<Text fixed>` for every-page repeat; FR-008 geometry PASS per thai-tax-compliance-auditor R13 review |
| **FR-033** — `invoice_voided` in F3 member timeline | ✅ Implemented | Audit payload carries `member_id`; `invoice_voided` in `F4MemberTimelineAuditEventType` union + runtime array |
| **FR-036** — cancellation email bilingual + doc number + void reason + "no longer payable" declaration | ✅ Implemented (PG-2 gated for attachment) | All copy elements PASS; **attachment** ships only when `FEATURE_F4_VOID_ATTACHMENT=true`; copy adapts via `hasAttachment` to say "link below" when flag off. **Spec strict-MUST for attachment requires DPA sign-off before flag flip.** |
| **FR-037** — issue transaction locks member + verifies status='active' | ✅ Implemented | `issue-invoice.ts` B-step member FOR UPDATE + `member.isArchived` check (pre-existing); T099 integration test verifies no seq consumed on archived member — **1-line test seed fix applied locally during this review, needs commit** |
| **FR-040** — typed-phrase confirmation for Issue/Void/Credit Note | ✅ Implemented | `VoidConfirmDialog` requires exact document-number match (locale-aware toLocaleUpperCase); autoCapitalize="off" after UX-2 fix |

**Coverage**: 9/9 US5-scope requirements implemented (100%) — with 1 condition (FR-036 attachment PG-2 gated pending DPA).

---

## Test Coverage Assessment

| Area | Tests Exist? | Coverage | Notes |
|------|-------------|----------|-------|
| Void use-case — happy path | ✅ | Full | DB columns, render-input shape, blob overwrite + allowOverwrite flag, audit with `void_reason_sha256` + `member_id`, outbox with `voidReason` + `documentNumber`, in-memory R-1b sha patch |
| Void — refusals | ✅ | Full | paid→CN, voided→terminal, cross-tenant probe |
| Void — auto-email matrix | ✅ | Full | true, false, null (T-DISP fallback to settings) |
| Void — Phase 2 failure | ✅ | Partial (blob_upload branch) | sha_sync branch deferred (low risk; structurally similar code path) |
| Archive race FR-037 | ✅ | Full | T099 green after seed fix |
| Email template `buildInvoiceAutoEmail` | ✅ | Full | EN/TH/SV × hasAttachment × voidReason including HTML escape, whitespace-only, omitted |
| `sanitiseErrorReason` pure function | ✅ | Full | 7 cases: passthrough, truncate, single/multi redact, >13-digit limit, non-string coercion, truncate-after-redact |
| E2E | 🟡 | Fixme | AS1-AS3 + FR-036 deferred pending T115 seeder (Phase 10); 1 negative smoke runs live |

**Unit test count**: 231 passing (↑ from 210 at R13 baseline — 6 new sanitise + 14 new email + 1 applyInvoicePdfRegeneration mock)
**Integration test count**: 108 passing / 1 skipped / 1 todo on live Neon Singapore (14 test files).
**Coverage per Constitution Principle II**: Domain 100% line, Application 80% line+branch threshold met for void-invoice (exported `sanitiseErrorReason` now covered, `{autoEmailOnIssue}` fallback branch covered by T-DISP).

---

## Security & Privacy Review (Principle IV NON-NEGOTIABLE)

| Concern | Status | Evidence |
|---------|--------|----------|
| **PII in append-only audit** | ✅ Redacted | `void_reason_sha256` in audit payload; raw plaintext only on invoices row (legal-obligation retention track per FR-029) + outbox `context_data` (90-day purge via B-2 cron) |
| **Blob key in logs** | ✅ Stripped | `vercel-blob-adapter:downloadBytes` error message omits key (PG-1); Phase 2 `logger.error` also excludes blobKey (N-2) |
| **Render error PII** | ✅ Sanitised | `sanitiseErrorReason` truncates 200 chars + redacts `\d{13}` before entering typed error boundary |
| **PDPA §28 cross-border** | ⚠ Gated | `FEATURE_F4_VOID_ATTACHMENT=false` default keeps PDF bytes within the existing F1 Resend DPA scope (body/metadata only); attachment path honoured only after DPA amendment |
| **Outbox retention** | ✅ Bounded | `outbox-purge` DELETEs `sent`/`permanently_failed` > 90 days; `pending` rows preserved |
| **Forbidden log fields** (CLAUDE.md) | ✅ Clean | No `password`/`session`/`token`/`Authorization`/`raw email body` in any of 19 Phase 9 files |
| **Tenant isolation** (Principle I) | ✅ Enforced | All repo methods use `TenantTx`/`runInTenant`; `applyVoid` WHERE clause pins `tenant_id`; cross-tenant probe audit emitted on RLS-hidden rows |
| **Audit atomicity** (Principle VIII) | ✅ Phase 1 atomic | `audit.emit(tx, ...)` in Phase 1; Phase 2 audit emit uses `null` tx fire-and-forget with try/catch + logger fallback (M-1) |

---

## Reliability Review (Principle VIII)

| Concern | Status | Evidence |
|---------|--------|----------|
| **Blob↔DB atomicity** | ✅ Two-phase commit | Phase 1 commits DB+audit+outbox atomically; Phase 2 post-commit blob+sha with discriminated failure audit. Failure states: `blob_upload` → (old sha + old bytes) consistent-stale; `sha_sync` → (old sha + new bytes) split-detectable |
| **Dispatcher lock window** | ✅ Minimised | Peek SELECT + `downloadBytes` happen BEFORE `db.transaction()` in `dispatchOne` (R-2 fix); lock window = SELECT FOR UPDATE + UPDATE + Resend send only |
| **Lock ordering** | ✅ Deadlock-free | `voidInvoice` holds only invoice row lock in Phase 1; cannot cycle with issue-invoice (member→invoice→advisory→seq) or record-payment (invoice-only) or issue-credit-note (invoice-only) |
| **Retry semantics** | ✅ Bounded | EmailSender 3 retries + outbox 5 retries with exponential backoff per FR-012c; attachment bytes re-sent on retry (acceptable per F4 throughput projection <50 emails/day) |
| **Idempotency** | ✅ Status-guarded | `applyVoid` WHERE `status='issued'` serialises concurrent void+pay; outbox dispatch uses FOR UPDATE SKIP LOCKED |
| **Partial state visibility** | ✅ Observable | R-1a Phase 2 audit emit discriminates phase + `blob_bytes_uploaded` — sweeper/ops dashboard query: `event_type='pdf_render_failed' AND payload->>'context'='invoice_void_phase2_sync'` |
| **Typed errors across boundary** | ✅ Result<T,E> | 7 typed error codes in `VoidInvoiceError`; route maps cleanly (404/409/422/500); `TxAbort<T>` pattern prevents partial-tx commits |

---

## Architecture & Clean Code Review (Principle III)

| Concern | Status | Evidence |
|---------|--------|----------|
| **Domain purity** | ✅ | No new Domain changes in Phase 9 (relies on F1+F2 Invoice aggregate) |
| **Application purity** | ✅ | `void-invoice.ts` imports only `@/lib/result`, `@/lib/logger`, `@/lib/crypto`, and module-local ports; no drizzle/next/react leakage |
| **Port symmetry** | ✅ | `BlobStoragePort.downloadBytes`, `EmailOutboxPort.enqueue({voidReason, documentNumber})`, `InvoiceRepo.applyVoid` all added as proper port methods with typed inputs |
| **Barrel discipline** | ✅ | `src/modules/invoicing/index.ts` re-exports `voidInvoice` + `voidInvoiceSchema` + `VoidInvoiceError`; no deep imports from outside the module |
| **Two-phase commit documentation** | ✅ | `void-invoice.ts` header comment explains Phase 1/Phase 2 contract, failure modes, and recovery path (40+ lines of architectural comment) |

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Phase 9 files touched | 19 |
| Phase 9 commits | 4 (7caae2a, 9f8d087, 2e156ad, 796ba80) |
| Phase 9 LOC (net) | ~3,400 insertions / 86 deletions (incl. tests) |
| 🔴 Blockers | **0** |
| 🟡 Warnings | **1** (R14-01 — fix applied locally, uncommitted) |
| 🟢 Suggestions | **6** (R14-02..07) |
| Unit tests | 231 passing (**+21 vs R13 baseline**) |
| Integration tests | 108 passing on live Neon Singapore |
| Spec coverage (US5+FRs) | 9/9 (100%) |
| Constitution principle violations | 0 |
| PDPA/GDPR findings open | 1 non-code (PG-2 DPA amendment) |

---

## Recommended Actions

### Before `/speckit.ship` (required)

1. **R14-01**: Commit the `status: 'archived'` seed fix in `tests/integration/invoicing/issue-vs-archive-race.test.ts` (already applied, re-ran green). Suggested message:

   ```
   fix(test): seed status='archived' in issue-vs-archive-race test
   
   DB CHECK members_archived_at_iff_archived requires status + archived_at
   to be consistent. Found during staff review R14 (/speckit.staff.review.run).
   ```

2. **PG-2 DPA sign-off** (non-code): DPO confirms Resend DPA covers binary attachments containing Thai tax-document PII; update `specs/001-auth-rbac/plan.md § Complexity Tracking` entry; only then flip `FEATURE_F4_VOID_ATTACHMENT=true` in Vercel prod env. Reversible in 30 s without redeploy.

### Defer to Phase 10 (non-blocking)

3. **R14-02..07**: Polish — `sanitiseErrorReason` relocation, HTML-escape scope comment, `outbox-purge` runbook, dedicated `invoice_pdf_sync_failed` enum migration, optimistic-version guard for `applyInvoicePdfRegeneration` (when T107 resend-pdf lands), `sanitiseErrorReason` regex tightening.

4. **T115 e2e seeder**: unblocks the 4 `test.fixme` cases in `tests/e2e/invoice-void.spec.ts`.

### Ops readiness

5. Configure Vercel Cron for `/api/cron/outbox-purge` daily at 03:15 Bangkok (20:15 UTC) with `Authorization: Bearer $CRON_SECRET` header. No code required.

---

## Verdict

⚠️ **APPROVED WITH CONDITIONS** — Phase 9 implementation is complete and correct; 3 rounds of review have converged. The single blocker (R14-01 uncommitted test-seed fix) is already resolved locally and needs only a commit. Once committed, Phase 9 is ready for `/speckit.ship`.

PDPA §28 compliance is **code-gated** via `FEATURE_F4_VOID_ATTACHMENT=false` default — production can ship safely with link-only emails; the attachment path remains parked behind the flag until the DPA amendment is signed, which is an out-of-band legal process not blocking code ship.

---

**Signed**: AI Staff Reviewer · 2026-04-21T11:21:31Z
**Prior reviews**: R01–R13 (see `specs/007-invoices-receipts/reviews/`)
**Next gate**: `/speckit.ship` after R14-01 commit + PG-2 DPA confirmation
