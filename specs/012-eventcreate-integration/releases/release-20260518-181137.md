# F6 — EventCreate Integration · Release Artifact

| Field | Value |
|---|---|
| Feature | F6 (`012-eventcreate-integration`) |
| Release timestamp | 2026-05-19 01:11:37 UTC+7 (2026-05-18 18:11:37 UTC) |
| PR | (filled in after `gh pr create`) |
| PR title | `feat: F6 EventCreate Integration (ships dark behind FEATURE_F6_EVENTCREATE=false)` |
| Base | `main` |
| Head | `012-eventcreate-integration` |
| Commits | 237 (squash-merge recommended) |
| Files changed | 975 |
| Additions | +135,641 |
| Deletions | -4,142 |
| Git range | `origin/main..cb00ed1a` |
| Feature flag at merge | `FEATURE_F6_EVENTCREATE=false` (ships dark) |

## Readiness check (snapshot at ship)

| Check | Verdict | Note |
|---|---|---|
| Tasks | ✅ 156/162 | 6 incomplete = documented operator gates (T150 / T151 / T152 / T153 / T154 / T154a) |
| Latest review | ✅ APPROVED | R4 fix-all `cb00ed1a` — 0 BLOCKER · 0 CRITICAL outstanding after R1 → R2 → R3 → R4 progression |
| Latest QA | ✅ ALL PASSED | `qa/qa-20260518-173713.md` — 4852 unit + 204 F6 integration + 271 F8/audit integration GREEN on live Neon |
| Working tree | ⚠️ pre-ship-commit | QA artifacts + release files staged in this commit; `.claude/settings.json` excluded; pre-existing untracked retrospectives outside ship scope |
| Branch sync | ✅ 0 behind / 237 ahead of `main` | No rebase needed |
| Pre-push hook | ✅ `check:layout` GREEN | 98 page/loading pairs consistent |
| Local CI | ✅ lint + typecheck + i18n + layout + check:fixme GREEN | Full guard chain run pre-PR |
| gh CLI | ✅ v2.89.0 authed as Jirawatpyk | Ready for PR creation |

## Linked artifacts

- Spec: [`spec.md`](../spec.md)
- Plan: [`plan.md`](../plan.md)
- Tasks: [`tasks.md`](../tasks.md) — 162 tasks, 96.3% closure (156 closed in-session + 6 deferred to ship-day operator gates)
- Ship-day checklist: [`ship-day-checklist.md`](../ship-day-checklist.md)
- Latest QA: [`qa/qa-20260518-173713.md`](../qa/qa-20260518-173713.md)
- Review history: [`reviews/`](../reviews) (12 review reports across R1-R4 cycle)
- PR description (this release): [`pr-description-20260518-181137.md`](pr-description-20260518-181137.md)

## Changelog entry (also prepended to root CHANGELOG.md)

### F6 — EventCreate Integration — 2026-05-19

**Added**
- Two-mode attendee ingest: Zapier-mediated webhook (15-min polling, EventCreate tenants) AND CSV upload (primary path for non-EventCreate tenants + backfill / outage recovery for EventCreate tenants)
- `POST /api/webhooks/eventcreate/v1/{tenantSlug}` — HMAC-SHA256 signed POST + 5-min replay window + 7-day idempotency receipts + 10 req/min rate limit + HTTP 503 kill switch
- 4-rule attendee → member matching (exact contact email → email domain → fuzzy company name → non_member fallback) with personal-email deny list + Levenshtein ≤ 3 normalised company-name matching
- Strict ACID-tx atomicity (FR-037) — single tx covers event upsert + registration insert + idempotency receipt + quota decrement; failure rolls back entire tx + emits `webhook_rolled_back` audit in separate post-rollback tx
- Partnership tier per-event quota accounting (Diamond 6 / Platinum 4 / Gold 2 tickets/event) + Corporate tier annual cultural quota (Premium 2/yr / Large 1/yr) with refund credit-back
- Admin-archive lifecycle — `archived_at` soft-delete + reversed `counted_against_*` flags + audit-logged credit-back per registration
- Differentiated PII retention per PDPA / GDPR — member-linked 5y, non-member 2y + pseudonymise via deterministic salted hash; quota / aggregate stats preserved
- Admin-only PDPA §30 / GDPR Art. 17 erasure tool at `/admin/events/{eventId}/registrations/{registrationId}/erase`
- 24-hour secret rotation grace window with deprecated-grace audit tagging
- 5 admin surfaces: events list (search + filter chips + payment-status filter) / event detail (attendee table + match-rate + Clear filters CTA) / erasure tool / CSV import wizard (EventCreate-format detection + Option B+ status mapping) / integration config (secret reveal + rotation + Zapier walkthrough + recent deliveries)
- New bounded context `src/modules/events/` (Domain + Application + Infrastructure)
- ~11 OTel metrics + ~6 alerts + 3 runbooks (signature-failure investigation / match-rate degradation triage / secret-rotation operational procedure)
- 3 cron-job.org coordinators (pseudonymise-eventcreate daily / sweep-eventcreate-idempotency daily / recompute-match-rate hourly)
- 4 cumulative review rounds (R1 → R2 atomicity → R3 audit-emit boundary → R4 27-caller safeAuditEmit migration) with progressive defect-class hardening
- 2924 i18n keys × EN + TH + SV (+22 F6 keys); WCAG 2.1 AA via R3-U2 focus-guard + R3-F5 focus return + R4-U2 SR-announce + R4-S5 sr-only h3

**Changed**
- F2 + F3 + F4 + F5 + F7 + F8: no behavioral changes; F6 reads members + plans + tenant_invoice_settings via cross-context ports

**Technical Notes**
- R2 atomicity invariant (commit `f59782f8`) — `maybeApplyStateChange` outer catch re-throws ALL `TxStageError` stages; debit-branch advisory lock mirrors credit-branch
- R3 emitOrThrow boundary (commit `7671fb94`) — wraps `audit.emit()` raw throws into `TxStageError('audit_emit')`; R3-T1 fault-injection seam exercises on live Neon
- R4 27-caller safeAuditEmit migration (commit `cb00ed1a`) — every direct `audit.emit()` call site across 12 files migrated to `safeAuditEmit` helper; raw throws correctly classify as `audit_emit` instead of leaking as `unknown`
- Per-(tenant, member, event) `eventcreate-quota:` advisory-lock namespace (disjoint from F4 `invoicing:` and F5 `payments:`)
- Asia/Bangkok timezone for fiscal-year boundary derivation via `js-joda` (shared with F4 sequential numbering)
- Multi-tenant isolation: 4/4 cross-tenant search probes GREEN; Constitution Principle I sub-clauses 1–5 satisfied

**Pre-flag-flip operator gates** (6 outstanding)
- T150 maintainer co-signs security checklist (38 items)
- T151 maintainer signs off reliability + UX + observability + integration checklists (3 × 35-40 items)
- T152 staging `/speckit.qa.run` with `FEATURE_F6_EVENTCREATE=true`
- T153 SC-005 baseline measurement (1 pre-flag-flip event for manual time-baseline)
- T154 3 cron-job.org coordinator entries (pseudonymise / sweep / recompute-match-rate)
- T154a F8 port adapter live-wired verification at flag-flip time

## Post-merge action plan

1. **Vercel preview deploy** verifies F6 surfaces compile + boot under `FEATURE_F6_EVENTCREATE=false`
2. **6 operator gates** clear sequentially before flag-flip (recommended order: T150 → T151 → T154 → staging deploy → T152 → T153 → T154a)
3. **Flag-flip**: set `FEATURE_F6_EVENTCREATE=true` on Vercel production env + redeploy
4. **First 7 days post-flag**: monitor F6 metrics in Grafana per `docs/observability.md` § F6 alert rules

## Test baseline at ship

| Suite | Count | Duration |
|---|---|---|
| Full unit | 4852 GREEN | 415s |
| F6 unit | 505 GREEN | 55s |
| F6 integration on live Neon | 204 GREEN | 575s |
| F8 + audit integration on live Neon (R4-I1 regression net) | 271 GREEN + 5 env-gated skip | 318s |
| Guard chain (lint + typecheck + i18n + layout + fixme) | 5/5 GREEN | ~30s |
