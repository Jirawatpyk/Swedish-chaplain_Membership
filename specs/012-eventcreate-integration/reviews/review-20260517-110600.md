# Staff-Engineer Review Report: F6 EventCreate Integration + F6.1 CSV Import (EventCreate format)

**Reviewer**: AI Agent (Staff Engineer Perspective) — 5 specialised agents (code-reviewer, security-threat-modeler, performance-slo-guardian, chamber-os-architect, senior-tester) synthesised into one staff verdict
**Date**: 2026-05-17 11:06 UTC
**Feature**: [F6 spec](../spec.md) + [F6.1 spec](../../013-csv-import-eventcreate-format/spec.md)
**Branch**: `012-eventcreate-integration`
**Reviewed at HEAD**: `22310a6f` (after R1–R5 remediation closure: `1fd26cb5` → `7c70a224` → `18ac3757`)
**Verdict**: ❌ **CHANGES REQUIRED**

---

## Executive Summary

F6/F6.1 has reached a mature state after **5 rounds of multi-agent /speckit-review remediation** (≈90 findings closed across R1–R5 covering forensic-trail integrity, type-system invariants, cause threading, RBAC ordering, observability metrics, test gaps, and Constitution compliance). The 4 NON-NEGOTIABLE Constitution principles (I tenant-isolation, II TDD, III Clean Architecture, IV PCI DSS) all PASS. Cross-tenant probe forensic-trail integrity is now triple-guarded (allowlist + dedicated error classes + `wrapRepoError` re-throw). 22/22 Phase 10 integration tests green on live Neon Singapore.

**The lone ship-blocker is a SQL typo in the SLO-F6-005 match-rate gauge**: the `recompute-match-rate` cron queries event_type `match_resolution_completed` which **does not exist in the F6 audit taxonomy** — the gauge will permanently emit 0.0, silently masking the 30-day post-flag-flip observability window that drives the rollback decision (spec.md Rollback Plan). Two passes (Pass 1 code-review + Pass 3 perf-SLO) converged on this finding independently; the contract test for the cron only verifies bearer-auth + feature-flag gating, never the SQL semantics against seeded rows.

Three additional 🔴 Blockers concern test coverage gaps that allow false-positive ship-readiness signals: US2 AS4 unmatched-filter has zero coverage at any layer; the E2E webhook ingest happy-path assertion accepts `non_member` as valid (defeating the core "matches via contact-email" claim of FR-002); the error-CSV blob roundtrip integration test is `describe.skipIf(!BLOB_GATE)` and may be silently skipped in CI.

Beyond the blockers, **the branch ships dark behind `FEATURE_F6_EVENTCREATE=false`** so production impact is zero until ship-day operator runs T150–T154a. The 15 Warning findings are residual hardening items (Vercel Blob URL public-capability semantics, formula-injection in error-CSV, rate limit on PII bulk-download, `runInTenantTx` raw-string drift, SLO numbers UNVERIFIED in prod). All Warnings can be folded into a single Round 6 patch wave alongside the blocker fixes.

**Recommendation**: Fix the 4 🔴 Blockers (~2 hours estimated), re-run `/speckit-review`, then proceed to `/speckit.ship` for dark deployment. The remaining 15 Warnings should be addressed BEFORE flag-flip (T150) but do not block dark ship.

---

## Review Findings

### 🔴 Blockers (4)

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| **R001** | 🔴 Blocker | `src/app/api/internal/observability/recompute-match-rate/route.ts` | 67–91 | Correctness + Performance + Spec Compliance | SQL queries `event_type = 'match_resolution_completed'` which does NOT exist in F6 audit taxonomy (actual emitted types: `attendee_matched_member_contact/_domain/_fuzzy/_non_member/_unmatched` per `audit-port.ts:87-91`). Also references non-existent `payload.matchType` field — the match type is encoded in `event_type` column, not payload. Result: `eventcreate_match_rate_gauge` permanently reads 0.0 for every tenant. SLO-F6-005 (≥0.95) cannot be measured. SC-002 30-day post-flag-flip rollback signal is dead. | Rewrite SQL to filter on the 5 actual event types: `COUNT(*) FILTER (WHERE event_type IN ('attendee_matched_member_contact','attendee_matched_member_domain','attendee_matched_member_fuzzy'))::numeric AS matched, COUNT(*) FILTER (WHERE event_type IN ('attendee_matched_member_contact','attendee_matched_member_domain','attendee_matched_member_fuzzy','attendee_non_member','attendee_unmatched'))::numeric AS total`. Add a live-Neon integration test in `tests/integration/events/` that seeds N attendee audits and asserts gauge value reflects the ratio. |
| **R002** | 🔴 Blocker | `tests/e2e/eventcreate-webhook-ingest.spec.ts` | 108–111 | Test Quality | US1 AS1 happy-path assertion accepts `non_member` and `unmatched` as valid match outcomes: `expect(['member_contact','member_domain','member_fuzzy','non_member','unmatched']).toContain(body.matched)`. This is a FALSE POSITIVE — if the contact-email match rule (FR-002) silently breaks, the test still passes because every shape lands in the array. AS1 spec explicitly says "matches the attendee to a member via the contact-email rule". | Tighten assertion to `expect(body.matched).toBe('member_contact')` for the seeded fixture, OR seed `e2e-test@example.com` as a contact email in `seedF6Events()` helper and assert specifically on the matched member's identity. |
| **R003** | 🔴 Blocker | `tests/integration/events/error-csv-blob-roundtrip.test.ts` | 29 | Test Quality | `describe.skipIf(!BLOB_GATE)` — the F6.1 US5 AS2 ("Download error CSV") Vercel Blob put+generateSignedUrl+delete lifecycle is tested ONLY by this file. If `BLOB_READ_WRITE_TOKEN` is absent from CI environment, the entire AS2 acceptance path goes untested. | Verify `BLOB_READ_WRITE_TOKEN` is present in CI env (Vercel project settings). Add a CI assertion that fails the workflow if the gate variable is unset. Alternatively, add a contract-level test in `tests/contract/events/csv-import-api.test.ts` that mocks the Blob adapter and asserts the route returns `?download=1` with the correct expiry header. |
| **R004** | 🔴 Blocker | (test coverage) | — | Test Quality | F6 US2 AS4 ("Show unmatched only" filter — match_type IN ('unmatched','non_member')) has **zero acceptance test coverage** at any layer (unit, contract, integration, E2E). Found via senior-tester traceability pass. A regression that changes the filter from `IN ('unmatched','non_member')` to `= 'unmatched'` would ship without detection — `non_member` attendees disappear from the admin's unmatched-review workflow. | Add an integration test in `tests/integration/events/admin-list-detail.test.ts` that seeds 1 `unmatched` + 1 `non_member` + 1 `member_contact` row and asserts the `?unmatchedOnly=true` query parameter returns exactly 2 rows. Optionally add a contract test in `tests/contract/events/admin-events-api.test.ts` asserting the use-case receives the correct `matchTypeFilter` enum. |

### 🟡 Warnings (15)

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| **R005** | 🟡 Warning | `src/modules/events/application/use-cases/erase-attendee-pii.ts` | 151, 303–307 | Correctness | `completedWithinSecondsOfRequest` audit field is always 0 — both `requestStartedAt` and `completedAt` derive from the same `input.occurredAt.getTime()`. PDPA §30 / GDPR Art. 17 require demonstrating timely erasure latency; SC-012 (≤30d) is unmeasurable from this audit. | Capture `Date.now()` at completion: `const completedSeconds = Math.max(0, Math.round((Date.now() - requestStartedAt) / 1000));` |
| **R006** | 🟡 Warning | `src/lib/events-csv-import-deps.ts` | 412–448 | Security (defence-in-depth) | `lookupEventByIdTimingSafe` bypasses RLS via root `db` handle (intentional, for cross-tenant probe detection per FR-007). Relies implicitly on `neondb_owner.rolbypassrls = true`. A future infra migration to a non-bypassing role would silently disable `csv_import_cross_tenant_probe` audit emission without compile-time signal. | Add boot-time assertion in `src/lib/env.ts` or `src/lib/db.ts`: `SHOW row_security` + verify connected role's `rolbypassrls`. Alternative: SECURITY DEFINER stored procedure with explicit grant. |
| **R007** | 🟡 Warning | `src/app/api/admin/integrations/eventcreate/rotate-secret/route.ts` | 40 | Error Handling | `resolveTenantFromRequest` call is OUTSIDE the route's try/catch. If it throws (malformed host header), the route falls through to Next.js default 500 with NO structured log, NO requestId, NO metric. Sibling relink route has correct try/catch (see `relink/route.ts:128-146`). | Wrap line 40 in a try/catch matching the relink route pattern. |
| **R008** | 🟡 Warning | `src/app/api/webhooks/eventcreate/v1/[tenantSlug]/route.ts` | 634–765 | Security (Elevation) | `__test_webhook__` sentinel short-circuit relies on payload-content string matching. A malicious tenant insider with EventCreate config access can intentionally set their event ID to `__test_webhook__` to bypass the entire ACID pipeline (quota accounting, match resolution, registration insert) while still emitting a valid `webhook_test_invoked` info-severity audit. HMAC is verified — actor IS authenticated — but they're bypassing FR-013/015/016/009 invariants. | Either (a) require an additional HMAC-signed sentinel header (e.g. `X-Chamber-Test: true` signed alongside body) instead of payload content, OR (b) reject the short-circuit when `chamberTestMetadata` is null/malformed (currently lines 692-712 accept null). |
| **R009** | 🟡 Warning | `src/modules/events/application/use-cases/import-csv.ts` | 1367–1399 | Reliability + UX | FR-019b safety-net query failure is "fail-open": on Result.err or throw, the use-case logs an error + emits a fallback metric, then **continues with the import as if no prior matches existed** (`priorImports = []`). The admin gets ZERO UX signal that the safety check didn't run, defeating the FR-019b explicit defense against wrong-event-selection. | Either (a) surface a "safety-net unavailable" warning chip in the success response body so admin can manually verify, OR (b) fail-CLOSED — return `unexpected_error` and require admin retry. Option (a) preserves operator-friendliness; (b) preserves FR-019b strict contract. |
| **R010** | 🟡 Warning | `src/modules/events/infrastructure/vercel-blob-error-csv-store.ts` | 91–93 | Security (Information Disclosure / Capability Token) | Vercel Blob `access:'public'` URLs returned in 307 redirects from the error-CSV route are **capability tokens valid for up to 30 days** (until the TTL-sweep cron deletes the blob) regardless of the `?expires=` query parameter. `?expires=` is client-informational ONLY; Vercel Blob CDN does not enforce server-side expiry. The route-layer DB check against `errorCsvExpiresAt` is the only enforcement gate. | Document this explicitly in `docs/runbooks/f6-csv-error-csv-access.md`. Ensure the TTL-sweep cron monitoring is robust (P2 alert if cron skips >24h). Future: evaluate Vercel Blob private buckets (Pro/Enterprise tier) for true server-side token expiry — track as F6.2 backlog item. |
| **R011** | 🟡 Warning | `src/app/api/admin/events/import/[recordId]/error-csv/route.ts` | (route-level) | Security (DoS / Information Disclosure) | `GET /api/admin/events/import/{recordId}/error-csv` has NO rate limit. An admin actor (legitimate or compromised credential) can exfiltrate any number of error-CSVs in rapid succession. The `csv_import_error_csv_downloaded` audit emit provides a forensic trail but no application-layer throttle would trigger an alert or block. | Add a rate limit: e.g. 20 requests/hour per (tenantId, actorUserId) via existing Upstash limiter. Wire `eventcreate_csv_error_csv_download_rate_limit_exceeded` metric + P3 alert in `docs/observability.md`. |
| **R012** | 🟡 Warning | `src/modules/events/application/use-cases/import-csv.ts` | (error-row generation) | Security (CSV Formula Injection) | Error-CSV `reason` strings are server-generated from controlled row-rejection messages, but a malicious admin could trigger row rejection on values that include `=CMD()`, `+SUM()`, `-EVIL`, `@INDIRECT()` prefixes — when an operator downloads + opens the error CSV in Excel/Sheets, the formula auto-evaluates. | Add prefix sanitisation when writing error-CSV rows: prepend `'` (literal apostrophe) to any cell value that starts with `=`, `+`, `-`, or `@`. This is the OWASP-recommended formula-injection mitigation. |
| **R013** | 🟡 Warning | `src/modules/events/application/use-cases/_helpers/process-attendee-in-tx.ts` | 740–794 | Reliability (Race Condition) | `markRefunded` flip runs UNCONDITIONALLY on `refunded` re-uploads, while advisory-lock acquisition is gated on `hasMatchedQuotaScope`. For `unmatched`/`non_member` rows that were never quota-counted, no lock is acquired but the row UPDATE still races with concurrent `relinkRegistration`. End-state can be: relink succeeded → refund-flip committed simultaneously → row points to Member B but `payment_status=refunded` with no quota effect, while the relink's `quota_cultural_decremented` audit says quota WAS consumed. | Either (a) acquire per-(tenant, event, registration) advisory lock at refund-flip time even when no scope counted (cheap), OR (b) document as accepted race for SweCham scale + add regression test for audit-vs-state divergence. |
| **R014** | 🟡 Warning | `src/modules/events/application/use-cases/ingest-webhook-attendee.ts` | 98, 174; `create-event.ts:100`; `import-csv.ts:282,303` | Architecture (Principle I sub-clause 1) | `runInTenantTx` port parameter accepts `tenantId: string` (unbranded) instead of `TenantId` branded type. JSDoc explains "use-case brands internally via asTenantId" + composition root validates via `asTenantContext`. Inconsistent with F4/F5 which use branded `TenantId` everywhere; hides branding into Infrastructure so a test-seam can inject wrong tenant without type-level catch. | Tighten port signature to `TenantId` (branded). Migrate 4 use-case sites. Confirmed safe at runtime (composition root brands first) — type-level only refactor. |
| **R015** | 🟡 Warning | `eslint.config.mjs` | 224 (ignores `src/lib/**`) | Architecture (Principle III barrel discipline) | `src/lib/**` is in ESLint `ignores`, so `src/lib/events-{admin,webhook,create,csv-import,admin-integration}-deps.ts` deep-import 12+ paths into `@/modules/events/application/use-cases/*` and `infrastructure/*` (incl. Drizzle schema objects). Documented as F5 composition-adapter precedent, but the `tests/unit/architecture/events-barrel.test.ts` referenced in `index.ts:441` JSDoc — verify it exists and passes. | Confirm `pnpm test -- events-barrel` passes. If the test file does not exist, create it: enumerate the public barrel surface + assert no Routes/UI directly import Infrastructure adapters bypassing `src/lib/events-*-deps.ts`. |
| **R016** | 🟡 Warning | (operational) | — | Performance (SLO verification) | SLO-F6-001 (webhook ingest p95 <300ms), SLO-F6-002 (admin list <500ms), SLO-F6-003 (admin detail <800ms) are DESIGN-TIME targets. T136–T139 perf benches are claimed in tasks.md but `pnpm test:integration --reporter=verbose` output was NOT verified during this review session. | Pre-flag-flip: run T136–T139 with `--reporter=verbose`, capture p95 numbers in `specs/012-eventcreate-integration/perf-bench-T136.log`. If any SLO is violated, document deviation in `plan.md § Complexity Tracking`. |
| **R017** | 🟡 Warning | `src/modules/events/application/use-cases/import-csv.ts` | 920–938 | Performance (Concurrency) | Per-batch `pg_advisory_xact_lock('csv-import:{tenant}:{eventId}')` serialises the 3 concurrent batch-workers within a single import. `batchConcurrency=3` default in the input docs implies parallelism that does not materialise for same-event imports. At SC-006 1k-row envelope (10 × 100 rows), effective concurrency=1. If single-batch latency exceeds 15s under Neon pool contention, 200-row import could violate the 30s SC-006 SLO. | Either (a) explicitly document the serialised-within-import + parallel-across-imports tradeoff in the input type doc, OR (b) split the advisory lock to per-(tenant, event, batch) so siblings within one import can parallelise (more complex, requires careful lock-ordering to avoid deadlock). |
| **R018** | 🟡 Warning | `src/app/api/internal/retention/pseudonymise-eventcreate/route.ts` | 108–121 | Maintainability | Metric emission loop uses pre-emit-once + N-1-loop pattern: `emit once outside; if N > 1, emit N-1 more times`. Logically correct (handles N≥1 → N emits) but fragile and non-obvious. The `if (rowsPseudonymised > 1)` guard is redundant (a for-loop with i=1;i<1 doesn't execute). | Simplify to single loop: `for (let i = 0; i < Math.max(1, rowsPseudonymised); i++) { emit(...); }`. Or extend metric API to accept count: `.add(rowsPseudonymised, labels)`. |
| **R019** | 🟡 Warning | `src/modules/events/application/use-cases/_helpers/process-attendee-in-tx.ts` (`refund-credit-back path`) | (allotmentAfter arithmetic) | Correctness (defence-in-depth) | `allotmentAfter = oldAllotments.partnershipPerEvent - (oldConsumed.partnershipConsumedForEvent - 1)` assumes `consumed >= 1` when row's `counted_against_partnership=true`. Invariant is held by row state but NOT by Domain assertion or DB CHECK. Future quota-accounting bug creating `counted=true, consumed=0` row would yield `allotment + 1` audit. | Add defensive `Math.max(0, oldConsumed.partnershipConsumedForEvent - 1)`. Also applies to cultural scope (line 491-493 of relink-registration.ts). |

### 🟢 Suggestions (10)

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| **R020** | 🟢 Suggestion | `src/modules/events/application/use-cases/import-csv.ts` | 1331–1335 | Performance | CSV use-case collects all parsed rows into `rows: ParsedRow[]` before batching, forfeiting the streaming parser's bounded-heap benefit at <5k rows. Acceptable for SC-006 1k-row scale but documents the materialisation cost. | Keep as-is for SC-006; document tradeoff inline more explicitly. If F6.2 raises the 5k-row cap, revert to true streaming. |
| **R021** | 🟢 Suggestion | `tests/contract/cron/f6-recompute-match-rate.test.ts` | (whole file) | Test Quality | Contract test verifies bearer auth + feature-flag short-circuit but does NOT seed audit_log rows and assert gauge takes a non-zero value. This is the SAME pattern that allowed R001 to ship. | After R001 fix, add a live-Neon integration test that exercises the SQL semantics end-to-end. Bake this into the perf-bench T139 if possible. |
| **R022** | 🟢 Suggestion | `src/modules/events/domain/event-registration.ts` | `MatchResolutionInvariantError.raw` | Type Design | The error carries the full `raw: MatchResolution`. Useful for callers without row in scope, but `drizzle-registrations-repository.ts:80-114` catch site reads identifiers from `row` directly (not `error.raw`). Field is currently redundant at in-tree call site. | Either drop `raw` and replace with lighter `readonly type: MatchType` field (used in the error message), OR document in JSDoc that `raw` is for future out-of-tree consumers. |
| **R023** | 🟢 Suggestion | `src/modules/events/infrastructure/pino-audit-port.ts` | 233–296 | Type Design | `REDACT_ALLOWED_KEYS = new Set<string>(...)` is a runtime allowlist. A compile-time `Extract<AllPayloadKeys, ...literal-union...>` derived from `F6AuditPayloadByType` would prevent adding an allowlisted key that isn't a real payload field. Trade-off: cross-module type plumbing. | Keep current runtime allowlist + test snapshot (lower friction). Consider for F6.2 if audit-event additions become recurring. |
| **R024** | 🟢 Suggestion | `tests/unit/events/ingest-webhook-attendee.test.ts` | 444–453 | Test Quality | Refund test verifies `quota_credit_back_refund` is emitted once but does NOT assert that `quota_partnership_decremented` is NOT emitted in the same refund. A regression emitting BOTH (double-counting bug) would pass this test. | Add `expect(emitCalls.filter(c => c.eventType === 'quota_partnership_decremented').length).toBe(0)` to the refund path tests. |
| **R025** | 🟢 Suggestion | `tests/unit/events/ingest-webhook-attendee.test.ts` | 440–441 | Test Quality | Refund test asserts `acquire` was called once but does NOT verify the exact lock key shape `eventcreate-quota:{tenant}:{member}:{event}`. A regression swapping member/event order would acquire a different key and break TOCTOU protection silently. | Add `expect(acquire).toHaveBeenCalledWith(expect.stringMatching(/^eventcreate-quota:tenantId:memberId:eventId$/))`. |
| **R026** | 🟢 Suggestion | `tests/unit/events/infrastructure/redact-payload-for-fatal-log.test.ts` | (whole file) | Test Quality | Tests prove PII fields dropped + forensic fields preserved, but no negative test asserts arbitrary new field NOT in allowlist is dropped by default (allowlist-deny-by-default behaviour). A future refactor flipping to denylist pattern would leak PII undetected. | Add: `expect(redactPayloadForFatalLog({ unknownField: 'should not survive' })).not.toHaveProperty('unknownField')`. |
| **R027** | 🟢 Suggestion | `tests/e2e/eventcreate-webhook-ingest.spec.ts` | 153–173 | Test Quality | US1 AS3 idempotency test asserts HTTP 409 but does NOT verify DB has exactly 1 registration row (not 2). Spec: "no side effects (existing registration row unchanged, no quota double-decremented)". | Add a post-response Drizzle query: `expect(rows.length).toBe(1)`. Optionally verify `payment_status` + `quotaEffect` shape unchanged. |
| **R028** | 🟢 Suggestion | `src/lib/events-csv-import-deps.ts` | 333–342 | Architecture (Maintainability) | `hybrid: F6AuditPort = { ...audit, emit: standalone.emitStandalone }` spread+override pattern is non-obvious. If future refactor reorders the spread, `findPriorErasureCompletion` delegation could silently rewire. | Extract to a named factory function with explicit field assignment, OR add inline comment + test pinning the delegation order. |
| **R029** | 🟢 Suggestion | (various) | — | Test Quality | Property-based tests are limited to `branded-types-validators.test.ts` + `attendee-fingerprint.test.ts`. Additional invariants worth property-testing: GraceState pair ↔ migration 0129 CHECK; EventCreate CSV adapter roundtrip; markRefunded → quota-effect → audit triple. | Track as Round 6 / F6.2 backlog. Not blocking; fixed-case coverage is comprehensive. |

---

## Spec Coverage Matrix

### F6 (`specs/012-eventcreate-integration/spec.md`)

| Requirement | Status | Implementation Notes |
|-------------|--------|---------------------|
| FR-001…FR-035 | ✅ Implemented | All core requirements traceable. |
| FR-036 (match-rate gauge) | ❌ **REGRESSED** | SQL queries wrong event type (R001) — gauge permanently 0.0. SLO-F6-005 unmeasurable. |
| FR-037 (dual-write fallback) | ✅ Implemented | `emitRolledBack` + `pino.fatal` stderr; tested via `pino-audit-port-standalone-throw.test.ts`. |
| FR-032a (admin PII erasure) | ⚠️ Partial | Erasure works; `completedWithinSecondsOfRequest` always 0 (R005) — PDPA latency metric meaningless. |

**Coverage**: 36/37 implemented; 1 regression (R001). User stories US1–US7 implemented except US2 AS4 (R004 — zero test coverage).

### F6.1 (`specs/013-csv-import-eventcreate-format/spec.md`)

| Requirement | Status | Implementation Notes |
|-------------|--------|---------------------|
| FR-001…FR-024 | ✅ Implemented | All 24 FRs traceable; no scope creep. |
| FR-019b (event-mismatch safety net) | ⚠️ Partial | Fail-open on query failure (R009) — admin UX has no signal. |
| FR-022 (streaming CSV parser) | ✅ Implemented | Bounded-heap claim documented; `import-csv.ts:1331-1335` materialises into array post-parse (R020). |
| FR-024 (3 new audit events) | ✅ Implemented | `csv_import_error_csv_downloaded`, `csv_import_cross_tenant_probe`, `csv_import_event_mismatch_overridden` all wired. |

**Coverage**: 24/24 implemented; 1 partial (R009).

### Constitution v1.4.0 NON-NEGOTIABLE Compliance

| Principle | Status | Notes |
|-----------|--------|-------|
| **I. Data Privacy & Security** | ✅ PASS | All 5 sub-clauses pass. Cross-tenant probe forensic-trail triple-guarded (allowlist + dedicated error classes + `wrapRepoError` re-throw). 22/22 Phase 10 integration tests green. |
| **II. Test-First** | ✅ PASS | TDD red-first verified in commit history; domain 100% line; ≥1 integration test per use-case except `recompute-match-rate` (R001 root cause). |
| **III. Clean Architecture** | ✅ PASS | Domain pure (no framework imports); Application uses ports; F6→F8 bridge via structural typing (no nominal import). `src/lib/**` ESLint exemption documented (R015). |
| **IV. PCI DSS** | ✅ N/A | F6 has no card data. |

---

## Test Coverage Assessment

| Area | Tests Exist? | Assessment | Gaps |
|------|-------------|------------|------|
| Domain | ✅ | 100% line coverage on `branded-types`, `tenant-webhook-config`, `event-registration`, `levenshtein`, `normalise-company-name`, `secret-last-four` | None (R022/R023 are type-design polish only) |
| Application use-cases | ✅ | 80%+ line+branch on hot paths; 100% branch on security-critical (sign-in equiv: webhook signature verify, admin RBAC, PII erasure) | R024/R025 — refund-path double-emit + lock-key assertions |
| Infrastructure | ✅ | Repository adapters tested via integration on live Neon; invariant-defense tested via `_toAggregateForTesting` seam | R026 — allowlist-deny-by-default negative test |
| API contract | ✅ | 13 contract test files cover HTTP outcome matrix (200/400/401/403/404/409/413/415/429/500/503) | R021 — recompute-match-rate contract test doesn't seed audit rows |
| Integration (live Neon) | ✅ | 39 integration tests; 22 NEW Phase 10 green; tenant-isolation cross-tenant probes covered 4 ways | R003 — `error-csv-blob-roundtrip` conditionally skipped |
| E2E (Playwright) | ⚠️ Partial | `eventcreate-webhook-ingest.spec.ts`, `events-list-and-detail.spec.ts`, `integration-config-wizard.spec.ts`, `eventcreate-a11y.spec.ts` | **R002, R004, R027** — AS1 weak assertion, AS4 zero coverage, AS3 missing DB count check |

**Test count growth across R1–R5 remediation**: 416 → 432 events unit tests; +138 new test cases across 5 new test files.

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Files reviewed | ~120 (F6 source + tests + migrations + specs) |
| 🔴 Blockers | **4** |
| 🟡 Warnings | **15** |
| 🟢 Suggestions | **10** |
| Spec coverage (F6 + F6.1) | **60/61 FRs implemented (98.4%)** |
| Test coverage (events unit) | 432/432 GREEN (100% pass rate) |
| Integration coverage (live Neon) | 22/22 NEW Phase 10 + 39 total tests GREEN |
| i18n parity | 2893 keys × EN+TH+SV (verified post-R5) |
| Constitution NON-NEG | 4/4 PASS |
| Lint | 0 errors (2 pre-existing F1 warnings outside scope) |

---

## Recommended Actions

### Must Fix (Blockers) — ship-blocker

1. **R001** — Fix `recompute-match-rate` SQL to query the 5 actual event types (`attendee_matched_member_*` + `attendee_non_member` + `attendee_unmatched`). Add live-Neon integration test asserting non-zero gauge after seeded audits. **Est: 45 min.**
2. **R002** — Tighten `eventcreate-webhook-ingest.spec.ts:110` AS1 assertion to assert specific match type, or seed contact-email fixture so member match is deterministic. **Est: 20 min.**
3. **R003** — Verify `BLOB_READ_WRITE_TOKEN` is present in CI; OR add contract-level Blob roundtrip test that doesn't need live token. **Est: 30 min.**
4. **R004** — Add integration test for US2 AS4 `unmatchedOnly=true` filter (assert filter returns `match_type IN ('unmatched','non_member')` rows). **Est: 30 min.**

**Total blocker remediation: ~2 hours.**

### Should Fix (Warnings) — pre-flag-flip (T150)

1. **R005** — `eraseAttendeePii` real Date.now() at completion (5 min).
2. **R007** — `rotate-secret` route wrap `resolveTenantFromRequest` in try/catch (5 min).
3. **R008** — `__test_webhook__` sentinel — require HMAC-signed header OR reject null chamberTestMetadata (30 min).
4. **R009** — FR-019b safety-net surface UX warning when query fails (30 min).
5. **R011** — Add rate limit on `GET /api/admin/events/import/{recordId}/error-csv` (20 requests/hr per actor) (20 min).
6. **R012** — Prefix-sanitise error-CSV `reason` cells for formula injection (15 min).
7. **R013** — Either acquire lock at refund-flip for all rows OR document race + add regression test (30 min).
8. **R015** — Verify `events-barrel.test.ts` exists and passes; create if missing (15 min).
9. **R016** — Run T136–T139 perf benches with `--reporter=verbose`, log to `perf-bench-T136.log` (30 min).
10. **R018** — Simplify pseudonymise metric loop (5 min).
11. **R019** — Defensive `Math.max(0, ...)` in `allotmentAfter` arithmetic (5 min).

R006 (RLS bypass assertion), R010 (Vercel Blob capability docs), R014 (TenantId branded port) — track as Round 7 / F6.2 backlog items; not pre-flag-flip blockers but should land before second-tenant onboarding.

### Nice to Fix (Suggestions) — Round 6 polish

1. R020 — CSV streaming materialisation docs.
2. R021 — Add seeded audit assertion to f6-recompute-match-rate contract test.
3. R022 — Trim `MatchResolutionInvariantError.raw` field.
4. R023 — `REDACT_ALLOWED_KEYS` compile-time derivation.
5. R024 — Refund-path "no double-emit" assertion.
6. R025 — Refund-path advisory-lock-key shape assertion.
7. R026 — Redact-payload allowlist-deny-by-default negative test.
8. R027 — AS3 idempotency DB row-count assertion.
9. R028 — Refactor `hybrid: F6AuditPort` spread to named factory.
10. R029 — Property-based tests for GraceState ↔ CHECK + CSV adapter roundtrip.

---

## Round-over-Round Trajectory

| Round | Critical | Important | Suggestions | Resolution |
|---|---|---|---|---|
| R1 (Phase A–G) | 0 (32 fixes shipped) | — | — | Closed via commit `8481f08b` |
| R2 (Phase H) | 1 | 6 | 9 | Closed via commit `1fd26cb5` |
| R3 (R3.1–R3.7) | 1 | 7 | 10 | Closed via commit `7c70a224` |
| R4 (Round 4 review) | 1 | 8 | 16 | Closed via commit `18ac3757` |
| **R5 staff** (this) | **4** | **15** | **10** | **PENDING (CHANGES REQUIRED)** |

Round 5 (staff-level) found 1 NEW Critical class that the prior 4 multi-agent rounds did not catch: **a metric query that no test ever exercised end-to-end**. The other 3 Blockers are test coverage gaps that allowed false-positive ship-readiness signals. R005-R019 Warnings are residual hardening + design-debt items.

---

## Verdict

❌ **CHANGES REQUIRED**

**Reason**: R001 (match-rate gauge permanently 0.0 due to SQL typo querying non-existent audit event type) breaks the SC-002 30-day post-flag-flip rollback signal — the primary observability mechanism for the operator T150 gate. R002+R003+R004 are test coverage gaps that allowed R001 to ship and would allow analogous regressions to escape future rounds.

**Remediation plan**:
1. Fix R001 + R002 + R003 + R004 (~2 hours).
2. Re-run `/speckit-review` to verify blockers closed.
3. Either bundle R005-R019 Warnings into the same commit OR track as Round 6 follow-ups before T150 flag-flip.

**Path to ship**:
- **After R001-R004 closure**: branch is safe to commit + push. Run `/speckit.ship` for dark deployment behind `FEATURE_F6_EVENTCREATE=false`.
- **Before T150 flag-flip**: close all 🟡 Warnings (R005-R019), close ≥3 of the 🟢 Suggestions, run T136-T139 perf benches with raw output captured.
- **Post-flag-flip**: monitor SLO-F6-001/002/003 for 7 days (R016 unverified pre-prod); confirm SLO-F6-005 gauge emits non-zero after first attendee audit (R001 verification).

This branch represents 5 rounds of disciplined remediation work. The discovered Blockers are not a sign of poor implementation — they are evidence that staff-level review catches a different defect class (silent observability regressions + test coverage gaps that mask the very metrics that would catch them) than multi-agent review (architectural compliance + cause threading + type invariants). Both layers of defence are necessary; both are working as intended.

---

*Generated by `/speckit.staff-review-run` — Staff-level code review for spec-driven development. Synthesised from 5 specialised agents (code-reviewer, security-threat-modeler, performance-slo-guardian, chamber-os-architect, senior-tester) running in parallel.*
