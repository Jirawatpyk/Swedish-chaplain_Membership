# Code Review Report: F6 Phase 4 — Admin Events List + Event Detail

**Reviewer**: AI Agent (Staff Engineer Perspective) · 5 review passes (1 manual + 2 specialist agents) + 4 prior review rounds
**Date**: 2026-05-12
**Feature**: [specs/012-eventcreate-integration/spec.md](../spec.md) — User Story 2 (P1)
**Branch**: `012-eventcreate-integration` · 8 commits (`cf44b978`..`6e0d2434`)
**Scope**: `git diff cf44b978~1..HEAD` — 47 files, +6237/−156 LOC
**Verdict**: ⚠️ **APPROVED WITH CONDITIONS**

---

## Executive Summary

Phase 4 has gone through 4 prior review rounds closing ~108 findings. This 5th-round staff review (hybrid: 1 perspective-fresh code/spec/test pass + 2 specialist agents for security and performance) identified **1 CRITICAL contract-spec divergence + 1 HIGH audit-DoS vector** that the prior rounds missed, plus 5 MEDIUM hardening items and a handful of LOW polish suggestions.

The implementation is functionally correct against US2 AS1–AS5, Constitution Principles I/II/III/V/VI all hold, and the test surface is genuinely strong (479 unit/contract/arch tests + 13 live-Neon integration + Playwright E2E suites). Two findings are real bugs worth fixing before merge: (a) the email-search path uses `ilike(attendeeEmail, …)` instead of the `attendee_email_lower` STORED column the contract specifies AND the migration-0131 index targets — both a **spec divergence** AND a **perf regression vector**; (b) a `member`-role attacker supplying an oversized eventId bypasses the length-guard and lands a multi-MB row in `audit_log` per request.

Everything else is polish.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| R001 | 🔴 Blocker | `src/modules/events/infrastructure/drizzle-registrations-repository.ts` | 239 | Spec Compliance + Performance | `ilike(eventRegistrations.attendeeEmail, pattern)` searches the **non-lowercased** column. Contract (`admin-events-api.md:75`) explicitly specifies `attendee_email_lower`, and migration 0131 created `event_regs_tenant_email_lower_idx` on `(tenant_id, attendee_email_lower)` for this exact path. Current implementation triggers seq-scan on every q search + diverges from the wire contract specification. | Switch to `like(eventRegistrations.attendeeEmailLower, pattern.toLowerCase())` — index-eligible AND contract-aligned. Keep `ilike(attendeeName, pattern)` as-is for the name path (no name-lower column). |
| R002 | 🟡 Warning | `src/app/api/admin/events/[eventId]/route.ts` | 48-67 | Security | `eventId` length-check (line 66, `> 200 → 404`) runs AFTER the member-role audit emit on line 56-62. A `member`-role attacker supplying a 10MB eventId reaches `emitEventsRoleViolation`; the unbounded eventId is interpolated into both `payload.attemptedRoute` AND `summary` (`_lib/role-violation-audit.ts:65,80`), producing a ~10MB `audit_log` row per request. No rate-limit on this path. Few-thousand-request attack → multi-GB audit-table bloat + 5y retention budget eaten. | Move the `if (!eventId \|\| eventId.length > 200) return 404` guard ABOVE the role check, OR hard-cap eventId inside `emitEventsRoleViolation` itself. |
| R003 | 🟡 Warning | `src/app/api/admin/events/route.ts` · `[eventId]/route.ts` | 56 · 50 | Error Handling | `requireSession('staff').catch(() => null)` flattens BOTH "no session" AND "session-store unreachable / DB connection dropped" into a silent 404. Postgres/Upstash outage during session lookup → indistinguishable from unauthenticated request. Ops loses the infrastructure signal AND real staff users get a confusing 404 instead of 500. | Narrow the catch to known session-not-present error kinds (e.g., specific `SessionExpiredError`, `SessionNotFoundError`). Re-throw on infrastructure errors so the route hits the existing 500 path. |
| R004 | 🟡 Warning | `src/modules/events/infrastructure/drizzle-registrations-repository.ts` | 250-310 | Performance | `findByEventId` runs 3 queries sequentially: `matchCountRows` GROUP BY → `countRow` COUNT(*) → `rows` SELECT. `matchCounts` uses only `baseFilters` (independent of `itemFilters`), and count/items both use `itemFilters` but `items.offset` is pre-computed (no dependency on count value). All three can run in `Promise.all`. Saves ~2 RTTs (~20-30ms at Neon Singapore latency). MEDIUM at SweCham scale; load-bearing as event volume grows. | Replace 3 sequential `await` with `Promise.all([matchCountsQuery, countQuery, itemsQuery])`. |
| R005 | 🟡 Warning | `src/modules/events/application/use-cases/list-events.ts` · `drizzle-events-repository.ts` | 91-113 · 220-232 | Performance | Use-case calls `list()` then `getEmptyContext()` sequentially. Both depend only on `tenantId` — fully independent. Inside `list()`, count + items also run sequentially (lines 220-232). Both layers can parallelise. Total saving: 2 RTTs from the happy path (~16-24ms). | Parallelise count+items inside `list()` AND parallelise `list()` + `getEmptyContext()` at the use-case layer (skip the conditional `getMatchCountsByEventIds` reorganisation since it depends on `list()` items). |
| R006 | 🟡 Warning | `src/app/api/admin/events/_lib/role-violation-audit.ts` | 63-66 | Correctness (dead code) | Ternary `input.eventId !== null ? \`...\` : \`...\`` produces IDENTICAL strings in both branches. The `eventId` is implicitly carried via `attemptedRoute` already. Dead branching. | Either collapse to a single string template, OR genuinely differentiate (e.g., include `eventId` as a separate field in the detail summary). |
| R007 | 🟢 Suggestion | `src/app/(staff)/admin/events/[eventId]/page.tsx` | 137 | Security | `redirect(\`/admin/events/\${eventId}\${qs ? \`?\${qs}\` : ''}\`)` interpolates raw `eventId` into a Location URL. Next.js's same-origin guard prevents cross-origin redirects, but a crafted eventId with `../` or `..%2f` could redirect inside the origin to an unexpected URL. Path-segment encoding handles literal CRLF already; the residual risk is poor-pattern. | Validate `eventId` against the same `UUID_V4` regex the use-case uses BEFORE the redirect; `notFound()` on mismatch. |
| R008 | 🟢 Suggestion | `src/modules/events/infrastructure/sanitize-db-error.ts` | 91-93 | Security | `redactStack` regex covers `/var/`, `/usr/`, `/home/`, `/opt/`, `/tmp/`, `/root/`, `/users/`. Misses Vercel ESM `/private/var/task/...` paths, Next.js source-maps (`webpack-internal:///./src/...`), and `node_modules/` paths. Stack-disclosure reduced but not eliminated. | Add `private`, `webpack-internal`, `node_modules` to the prefix list (or invert to allow-list). |
| R009 | 🟢 Suggestion | `src/modules/events/infrastructure/pino-audit-port.ts` | 124-137 | Architecture | Audit sentinel `'system'` / `'zapier_webhook'` / `'csv_import'` / `'cron:f6'` written to `audit_log.actor_user_id` (TEXT). Forensic query `WHERE actor_user_id = 'system'` cannot distinguish F6 system-actor from any future system actor. Pattern inconsistent with F5 precedent (UUID-shape sentinel). | Namespace sentinels (`f6:system`, `f6:zapier_webhook`) OR add an `actor_kind` column in a Phase 10 audit-log refactor. Non-urgent; naming hygiene. |
| R010 | 🟢 Suggestion | `src/modules/events/application/use-cases/load-event-detail.ts` | 150 | Correctness (minor) | `tryEventId(input.eventId)` (returns null on empty) is followed by `UUID_V4.test(input.eventId)` (matches non-empty pattern). The non-empty check by `tryEventId` is redundant with the regex match. | Drop the `tryEventId` call; `UUID_V4.test(input.eventId)` is necessary AND sufficient. Saves one branch. |
| R011 | 🟢 Suggestion | `tests/integration/events/admin-list-detail.test.ts` | "emailSearch matches substring" test | Test Quality | Integration test asserts BEHAVIORAL correctness (case-insensitive substring match) but NOT the query plan. Did NOT catch R001 (index bypass). | Add an `EXPLAIN (ANALYZE, BUFFERS)` assertion in a perf-tagged integration test (Phase 10 T-bench-list scope) verifying `event_regs_tenant_email_lower_idx` is used for the `q` search path. |
| R012 | 🟢 Suggestion | `contracts/admin-events-api.md` | 29-55 | Spec drift | The list-response example does not include `lastUpdatedAt` field, but Phase 4 added it as a U5 trust signal in round-1 (`load-event-detail.ts:91`, `event-detail-header.tsx:43`). Non-breaking (clients ignore unknown), but contract example should match. | Update contract example to include `lastUpdatedAt: "2026-06-04T10:23:15Z"` in the items shape AND the detail event shape. |

**Categories**: Correctness, Security, Performance, Spec Compliance, Error Handling, Test Quality, Architecture

---

## Spec Coverage Matrix

| Requirement | Status | Implementation Notes |
|-------------|--------|---------------------|
| **FR-020** — events list at `/admin/events` with date+name+category+registrations+partner-benefit+match-rate, sortable by date desc, paginated, 3-variant empty state | ✅ Implemented | `list-events.ts` use-case + `events-list-table.tsx` + `page.tsx` empty-state branches. AS1+AS5 covered. |
| **FR-021** — event detail at `/admin/events/{eventId}` with metadata, paginated/searchable attendees, match-rate aggregate, "View on EventCreate" deep link | ⚠️ Partial | All elements present BUT `q` search hits `attendeeEmail` not `attendee_email_lower` (R001) — diverges from contract `admin-events-api.md:75` |
| **FR-030** — localise all admin UI EN+TH+SV | ✅ Implemented | 2324 i18n keys × 3 locales (verified `check:i18n`). |
| **FR-031** — WCAG 2.1 AA on all admin surfaces | ✅ Implemented | a11y E2E (`tests/e2e/eventcreate-a11y.spec.ts`) + manual aria-current/aria-busy/aria-live wired in round-1+4. Badge contrast bumped to `-600`/`-500` for both themes. |
| **FR-034** — `FEATURE_F6_EVENTCREATE` feature flag default false | ✅ Implemented | Kill-switch gated at both routes + both pages. Test T3 covers. |
| **FR-035** — RBAC matrix: admin+manager read; manager write→403+audit; member→404+audit | ⚠️ Partial | Read paths correct. **Member-role 404 audit emitted CORRECTLY but vulnerable to audit-bloat DoS via oversized eventId (R002)**. Manager-write 403 paths defer to Phase 6+ (no write actions in Phase 4 scope). |
| **FR-036** — observability (11 metrics + 6 alerts + 3 runbooks) | ⏭ Deferred | Phase 10 T124+ scope; not Phase 4 |
| **Constitution I** (tenant isolation) | ✅ | All queries via `runInTenant`; belt-and-braces `eq(tenantId, …)` in repos |
| **Constitution II** (TDD) | ✅ | RED commit `cf44b978` predates GREEN; 479 tests green |
| **Constitution III** (Clean Architecture) | ✅ | Use-cases import zero Drizzle/Next/React; barrel architecture test enforces |
| **Constitution V** (i18n EN+TH+SV) | ✅ | Parity verified |
| **Constitution VI** (Inclusive UX WCAG 2.1 AA) | ✅ | a11y suites green; contrast fixed in round 4 |

**Coverage**: 5/6 in-scope FRs fully implemented + 1 partial (FR-021 spec divergence on q-search column). **83% fully + 17% partial.**

---

## Test Coverage Assessment

| Area | Tests Exist? | Coverage | Gaps |
|------|-------------|----------|------|
| Route handlers (list + detail) | ✅ | 31 contract tests covering all status codes (200/400/404/500) + RBAC + audit + clamping + emptyStateContext variants | Audit-bloat DoS path (R002) untested; `requireSession` infra-error path (R003) untested |
| `listEvents` use-case | ✅ | Exercised via contract test mocks | No direct unit test (use-case is thin orchestrator; coverage via contract+integration acceptable) |
| `loadEventDetail` use-case | ✅ | 15 unit tests covering UUID v4 guard + isOverQuota 9-row truth table (incl. round-4 member_domain + member_fuzzy boundary) | None |
| `computeMatchRatePct` | ✅ | 15 boundary cases (round-3 imports REAL Domain helper) | None |
| `resolveTenantFromHeaders` | ✅ | 4 unit tests covering env-default, empty headers, valid X-Tenant override, malformed slug throw | `flatten` try/catch path (LOW-4 round-4) untested directly |
| Drizzle Phase 4 paths | ✅ | 13 integration tests on live Neon covering `list`, `getMatchCountsByEventIds`, `getEmptyContext`, `findByEventId` across 4 tenants | Query-plan / index-use assertions absent (R011) — behavioral tests passed despite R001 index-bypass |
| Architecture barrel | ✅ | 39 tests (round-4 expanded FORBIDDEN list) | None |
| E2E surface | ✅ (MANUAL-GATE) | 7 list-detail + 4 a11y + 9 i18n scenarios; gated on E2E_ADMIN_* env vars | Execution deferred to Phase 10 manual SR / cross-browser gate (paralleled F8 T270) |

**Behavioral coverage**: All US2 AS1–AS5 are end-to-end wired (contract + use-case + E2E). **Gap: query-plan assertions don't exist, so index-bypass-class perf regressions are not caught by tests**.

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Files reviewed | 47 changed (8 commits) |
| Test files | 7 (1 contract + 4 unit + 1 integration + 1 E2E) |
| Tests passing | 479 unit/contract/arch + 13 integration GREEN |
| 🔴 Blockers | **1** (R001 — q-search spec+perf divergence) |
| 🟡 Warnings | **5** (R002 audit-DoS, R003 broad-catch, R004 parallelise findByEventId, R005 parallelise list, R006 dead ternary) |
| 🟢 Suggestions | **6** (R007 redirect URL hardening, R008 redactStack prefixes, R009 sentinel namespacing, R010 redundant guard, R011 query-plan test, R012 contract update) |
| Spec coverage (in-scope FRs) | 5/6 full + 1/6 partial (83% + 17%) |
| Constitution principles aligned | 10/10 ✅ |
| Prior review rounds | 4 (closed ~108 findings) |
| Net Phase 4 LOC | +6237/−156 |

---

## Recommended Actions

### Must Fix (Blockers) — block merge

1. **R001** — In `drizzle-registrations-repository.ts:239`, replace `ilike(eventRegistrations.attendeeEmail, pattern)` with `like(eventRegistrations.attendeeEmailLower, pattern.toLowerCase())`. Keep `ilike(attendeeName, pattern)` for the name path. Closes contract divergence + restores `event_regs_tenant_email_lower_idx` use. Add an EXPLAIN-based perf test (R011) to lock the index plan.

### Should Fix (Warnings) — fix before flag-flip

2. **R002** — Move `eventId.length > 200 → 404` check ABOVE the role gate in `[eventId]/route.ts:48-67`, OR hard-cap eventId in `emitEventsRoleViolation` (`_lib/role-violation-audit.ts`). Closes audit-bloat DoS.
3. **R003** — Narrow `requireSession('staff').catch(() => null)` to known session-not-present error kinds; re-throw infrastructure errors to the existing 500 path. Both routes.
4. **R004** — Parallelise the 3 queries in `findByEventId` via `Promise.all` (`drizzle-registrations-repository.ts:250-310`).
5. **R005** — Parallelise count+items inside `list()` (`drizzle-events-repository.ts:220-232`) + `list()` + `getEmptyContext()` at the use-case layer (`list-events.ts:91-113`).
6. **R006** — Collapse the dead ternary at `_lib/role-violation-audit.ts:63-66` OR genuinely differentiate the summary by including a separate `eventId` field.

### Nice to Fix (Suggestions)

7. **R007** — Validate `eventId` against `UUID_V4` BEFORE the redirect in `[eventId]/page.tsx:137`.
8. **R008** — Extend `redactStack` prefix list to include `private`, `webpack-internal`, `node_modules`.
9. **R009** — Namespace audit sentinels (`f6:system`, etc.) OR introduce an `actor_kind` column in a future Phase 10 audit refactor.
10. **R010** — Drop redundant `tryEventId` call when `UUID_V4.test` follows in `load-event-detail.ts:150`.
11. **R011** — Add a perf-tagged integration test asserting `event_regs_tenant_email_lower_idx` use via `EXPLAIN (ANALYZE, BUFFERS)` (Phase 10 T-bench-list scope).
12. **R012** — Update `contracts/admin-events-api.md` example to include the `lastUpdatedAt` field on both list-item and detail-event shapes.

---

## Verdict — ⚠️ APPROVED WITH CONDITIONS

Phase 4 is functionally correct, spec-aligned on 5/6 FRs, and the test surface is genuinely strong. The 1 BLOCKER is a real spec-vs-implementation divergence (R001 — `attendee_email_lower` vs `attendeeEmail`) that ALSO causes a perf regression by bypassing the migration-0131 index. The 5 WARNINGS are concrete hardening items, of which R002 (audit-DoS) and R003 (broad-catch) are security-relevant.

**Conditions for ship**:
- **MUST**: fix R001 (single repo line + add EXPLAIN assertion via R011 — together close the contract divergence + the perf bypass)
- **SHOULD**: address R002 + R003 + R006 before flag-flip per FR-035 + FR-034 + spec correctness
- **MAY**: defer R004 + R005 perf parallelisation to Phase 10 T-bench-list if SLO benchmarks confirm acceptable headroom (SweCham scale: ~150ms p95 vs 500ms budget)

Suggestions (R007–R012) are non-blocking polish.

After R001 closed + R011 perf assertion landed: re-run `/speckit.staff-review.run` for verdict revision OR proceed to `/speckit.ship`.

---

*Generated by `/speckit.staff-review.run` — Staff-level code review for spec-driven development. Hybrid mode: 1 manual review pass (correctness/spec/tests) + 2 specialist agents (security/performance). 4 prior `/speckit.review` rounds closed ~108 findings; this round caught 12 additional issues the multi-agent rounds had missed.*
