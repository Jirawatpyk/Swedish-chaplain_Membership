# Critique Report (Round 3): F6 — EventCreate Integration

**Date**: 2026-05-12 (round 3, post-round-2-remediation verification)
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Previous critique rounds**:
- [critique-20260512-121004.md](./critique-20260512-121004.md) (round 1 — 6 must, 18 recs, 5 questions)
- [critique-20260512-123940-round2.md](./critique-20260512-123940-round2.md) (round 2 — 2 must, 6 recs)

**Verdict**: ⚠️ **PROCEED WITH UPDATES**

---

## Executive Summary

Round 2 successfully resolved M1 (audit payload column) and applied 5 of 6 recommendations cleanly. **However, the M2 fix (F6-owned idempotency table) was incomplete** — `plan.md`, `data-model.md`, and `contracts/*.md` were all updated to the new F6-owned table approach, but **`research.md` R3 ("Idempotency store") and R6 (strict-transactional ingest pseudocode) were not updated**. These two sections still describe the rejected "reuse F5's `idempotency_receipts` table" approach as the canonical decision. Since `research.md` is the **source-of-truth research-decision artefact** that `/speckit.tasks` consumes to generate work items, leaving R3 + R6 unupdated would lead to wrong tasks (e.g., "extend F5 idempotency_receipts source CHECK" tasks would appear instead of "create F6-owned eventcreate_idempotency_receipts table" tasks).

Additionally, 3 minor follow-up issues surface from M2's table addition:

- The cross-tenant `tenant-isolation.test.ts` still claims to probe "3 F6 tables" — now 4 with the new idempotency table.
- The TTL sweep cron for the new idempotency table isn't listed in `plan.md`'s cron-handler enumeration (it's mentioned in passing in `data-model.md` § 1.4 but missed from the canonical list).
- One stale comment in `admin-events-api.md:197` references bare `idempotency_receipts` in a JSON-example comment.

All round-1 must-address (6/6) + round-1 recommendations (18/18) + round-1 questions (5/5) remain closed. All round-2 round-1 carry-forwards (M1, R1–R5, R6 subsumed) hold. Only the round-2 M2 fix's research.md-update step was missed.

Net assessment: **5 findings (2 critical, 3 minor)** — all closable in ~10 minutes. Expected round 4 verdict: ✅ PROCEED.

---

## Product Lens Findings 🎯

(No new product-lens findings in round 3 — all round-1 + round-2 product findings remain closed.)

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| (none new) | — | — | — |

---

## Engineering Lens Findings 🔬

### Architecture Soundness

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| Z1 | 🎯 | **`research.md` R3 still describes the rejected approach as the canonical decision.** Section R3 (lines 74–92) is titled "**R3 — Idempotency store: reuse F5 `idempotency_receipts` table with `source` discriminator**" with a Decision block saying "F6 writes `X-Request-ID` deduplication rows into the existing F5-introduced `idempotency_receipts` table" — but the round-2 M2 fix established the opposite: F6 owns its own `eventcreate_idempotency_receipts` table. Since `research.md` is what `/speckit.tasks` reads to generate canonical work items, this contradicts the plan.md + data-model.md + contracts updates and would generate wrong tasks at the next gate. | Rewrite R3 with the new decision: title becomes "R3 — Idempotency store: F6-owned `eventcreate_idempotency_receipts` table"; Decision rewrites to describe the F6-owned approach (matches data-model § 1.4); the previous "reuse F5" content moves to the "Alternatives considered" block as a rejected option with the rationale "F5's `processor_events` is Stripe-specific (PK = stripe_event_id); generalising it requires cross-feature schema work and breaks F5's bounded-context discipline (Constitution Principle III). F6-owned table is contained and matches the F4/F5/F7/F8 per-feature idempotency pattern." |
| Z2 | 🎯 | **`research.md` R6 pseudocode (line 165) writes to bare `idempotency_receipts` name.** The strict-transactional ingest implementation pseudo-code step 2 says `INSERT INTO idempotency_receipts (tenant_id, source, request_id, processed_at, ttl_expires_at) ON CONFLICT DO NOTHING RETURNING request_id`. This is the wrong table name post-M2 fix — should be `eventcreate_idempotency_receipts`. Reading the pseudocode alone, an implementer would write code that INSERTs into a non-existent table. | Update line 165 step 2 from `INSERT INTO idempotency_receipts` to `INSERT INTO eventcreate_idempotency_receipts` (matches data-model § 1.4 + contracts/csv-import-api.md). |

### Operational Readiness

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| Z3 | 💡 | **`admin-events-api.md:197` JSON-example comment uses bare `idempotency_receipts` name** in the inline `// rows that hit idempotency_receipts ON CONFLICT (...)` comment for the `rowsAlreadyImported` field added in R3. Stale wording from before the M2 fix; doesn't change behaviour but creates documentation drift. | Update the comment to: `// rows that hit eventcreate_idempotency_receipts ON CONFLICT (duplicate of an earlier import within 7d) — distinguishes the "happy-path repeat upload" from "0 actually delivered"`. |
| Z4 | 💡 | **`tenant-isolation.test.ts` scope claims "3 F6 tables"** — now 4 with the F6-owned idempotency table added by M2. The cross-tenant test (Constitution v1.4.0 Principle I clause 3 Review-Gate blocker) should cover all tenant-scoped F6 tables: `events`, `event_registrations`, `tenant_webhook_configs`, AND `eventcreate_idempotency_receipts`. | Update `plan.md` Testing § from "asserts zero cross-tenant visibility on SELECT/INSERT/UPDATE/DELETE across all 3 F6 tables" to "all 4 F6 tables (events, event_registrations, tenant_webhook_configs, eventcreate_idempotency_receipts)". Add a sub-bullet asserting the idempotency table's RLS+FORCE policy is exercised. |
| Z5 | 💡 | **TTL sweep cron for the F6-owned idempotency table is not listed in plan.md's cron enumeration.** Data-model § 1.4 mentions "a daily TTL-sweep cron deletes expired rows opportunistically" but the canonical cron-handler list in plan.md (Technical approach section + Project Structure § 7) only enumerates `/api/internal/retention/pseudonymise-eventcreate` for PII pseudonymisation. The new idempotency-table TTL sweep needs its own entry. | Add to plan.md Technical approach: "**Two cron handlers** (both Node runtime, both Bearer-auth via `CRON_SECRET`): (1) `/api/internal/retention/pseudonymise-eventcreate` daily for FR-032 non-member PII pseudonymisation; (2) `/api/internal/retention/sweep-eventcreate-idempotency` daily for `eventcreate_idempotency_receipts.ttl_expires_at < NOW()` cleanup". Also add the second cron route to the Project Structure tree under `src/app/api/internal/retention/`. |

---

## Cross-Lens Insights 🔗

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|---------------|-------------------|------------|
| Y2 | **Round-2 M2 fix was incomplete because the spec-kit artefact graph has 5 sibling files** (spec.md, plan.md, research.md, data-model.md, contracts/*.md) and the fix touched only 4 of the 5. `research.md` was the original source-of-truth article that introduced the wrong decision (R3 "reuse F5") and was missed during the downstream fix. This is a pattern: when a decision changes, all 5 artefacts need a cross-walk. | Product: indirect — the rejected approach lingers in the canonical research article, would mislead any future maintainer trying to understand "why F6-owned"? | Engineering: direct — `/speckit.tasks` reads `research.md`. Wrong research → wrong tasks → wrong implementation → discovered at integration test. | Add a "round-2 M2 cross-walk checklist" note in the round-3 fix-application: every must-address fix to a multi-artefact decision MUST update spec/plan/research/data-model/contracts together. Apply to Z1 + Z2. |

---

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | 2 (Z1, Z2 — both research.md residual references to the round-2-rejected approach) |
| 💡 Recommendations | 3 (Z3, Z4, Z5) |
| 🤔 Questions | 0 |
| Round-1 findings still closed | 29 / 29 ✓ |
| Round-2 findings still closed | 8 / 8 ✓ |

---

## Consolidated Findings Table

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| Z1 | Engineering | 🎯 | Architecture | research.md R3 still describes "reuse F5 idempotency_receipts" — contradicts M2 fix in plan/data-model/contracts | Rewrite R3 title + Decision + Rationale + Alternatives to match F6-owned approach |
| Z2 | Engineering | 🎯 | Architecture | research.md R6 pseudocode line 165 writes to bare `idempotency_receipts` | Rename to `eventcreate_idempotency_receipts` in the SQL example |
| Z3 | Engineering | 💡 | Operations | admin-events-api.md:197 JSON comment uses bare `idempotency_receipts` | Update comment to use the F6-owned name |
| Z4 | Engineering | 💡 | Operations | tenant-isolation.test.ts scope claims 3 F6 tables — now 4 | Update plan.md Testing § to enumerate 4 tables |
| Z5 | Engineering | 💡 | Operations | TTL sweep cron for F6-owned idempotency table not in plan.md cron list | Add second cron handler to plan.md Technical approach + Project Structure |
| Y2 | Both | 💡 | Process | M2 fix was 4-of-5 sibling artefacts; research.md was missed (the original source-of-truth) | Establish cross-walk discipline for multi-artefact decisions; apply to Z1+Z2 fixes |

---

## Recommended Actions

### 🎯 Must-Address (Before `/speckit.tasks`)

1. **Z1 — Rewrite research.md R3** ("Idempotency store") to match the F6-owned approach:
   - Title: "R3 — Idempotency store: F6-owned `eventcreate_idempotency_receipts` table"
   - Decision: "F6 introduces its own idempotency table (`eventcreate_idempotency_receipts`) with `source IN ('eventcreate_webhook','eventcreate_csv')` discriminator. F5's `processor_events` cannot be reused because its PK = `stripe_event_id` is Stripe-specific. F6-owned table is contained and matches the per-feature idempotency pattern of F4 + F5 + F7 + F8."
   - Rationale: "(1) Schema impedance — F5's processor_events has Stripe-specific columns; (2) Bounded context discipline — Constitution Principle III favours per-feature data ownership; (3) Atomic with strict-transactional ACID unit — receipt INSERT in same tx as side effects (FR-037); (4) 7-day TTL sweep + partial index keep the table bounded at ~200 rows in flight."
   - Alternatives considered: move "reuse F5 idempotency_receipts" here as the rejected option with the same rationale.

2. **Z2 — Update research.md R6 step 2 pseudocode**:
   - Change line 165 `INSERT INTO idempotency_receipts (...)` → `INSERT INTO eventcreate_idempotency_receipts (...)`.

### 💡 Recommendations (Strongly Suggested — Address before `/speckit.implement`)

1. **Z3** — Update admin-events-api.md:197 inline JSON comment from `idempotency_receipts` → `eventcreate_idempotency_receipts`.
2. **Z4** — Update plan.md Testing § "tenant-isolation.test.ts" line from "3 F6 tables" → "4 F6 tables" with the new table enumerated.
3. **Z5** — Add second cron-handler `/api/internal/retention/sweep-eventcreate-idempotency` to plan.md Technical approach + Project Structure tree.

### 🤔 Questions (Need Stakeholder Input)

None.

---

**Severity Legend**:
- 🎯 **Must-Address**: Blocks proceeding to implementation
- 💡 **Recommendation**: Strongly suggested improvement
- 🤔 **Question**: Needs stakeholder input to resolve

---

*Generated by `/speckit.critique.run` (round 3, post-round-2-remediation verification) — Dual-lens strategic and technical review for spec-driven development.*
