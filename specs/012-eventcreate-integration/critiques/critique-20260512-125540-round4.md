# Critique Report (Round 4): F6 — EventCreate Integration

**Date**: 2026-05-12 (round 4, post-round-3 verification)
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Previous critique rounds**:
- [critique-20260512-121004.md](./critique-20260512-121004.md) (round 1 — 6 must, 18 recs, 5 questions)
- [critique-20260512-123940-round2.md](./critique-20260512-123940-round2.md) (round 2 — 2 must, 6 recs)
- [critique-20260512-125023-round3.md](./critique-20260512-125023-round3.md) (round 3 — 2 must, 3 recs)

**Verdict**: ✅ **PROCEED** (no must-address items; 2 light follow-up recommendations from the Z5 TTL-sweep cron addition)

---

## Executive Summary

Round 3 closed Z1 + Z2 + Z3 + Z4 + Z5 cleanly. The phantom-artefact references that plagued round 2 are now consistently fixed across all 5 spec-kit sibling artefacts. The R3 ↔ R6 ↔ data-model.md § 1.4 ↔ plan.md ↔ contracts ↔ quickstart.md cross-walk is complete and internally coherent. No new must-address items.

Two light recommendations surface specifically from the Z5 cron addition: the new `/api/internal/retention/sweep-eventcreate-idempotency` cron handler was added to the project structure + Technical approach + quickstart cron-job.org table, but the corresponding **observability metric** (FR-036 currently has 10 metrics; the new sweep needs an 11th) and **integration test** (Testing § enumerates 10 named test files; the new sweep needs an 11th) are not yet enumerated. Neither blocks `/speckit.tasks` — both would naturally surface as tasks during `/speckit.tasks` decomposition — but they're low-cost to add now while the cross-cutting refinement context is fresh.

Beyond AA1 + AA2, no other gaps found. F6 is **gate-ready for `/speckit.checklist` → `/speckit.tasks` → `/speckit.implement`**. Cumulative critique-driven refinements across rounds 1+2+3 = 41 findings closed.

---

## Product Lens Findings 🎯

(No new product-lens findings in round 4 — all prior rounds' product findings remain closed.)

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| (none) | — | — | — |

---

## Engineering Lens Findings 🔬

### Operational Readiness

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| AA1 | 💡 | **Z5 TTL-sweep cron lacks corresponding OTel metric in FR-036.** Round 3 added `/api/internal/retention/sweep-eventcreate-idempotency` to plan.md project structure + Technical approach + quickstart cron-job.org table, but FR-036's metric enumeration (~10 metrics) does not include a sweep-rows-deleted counter or sweep-duration histogram for the new cron. Without the metric, a silently-failing TTL sweep would not trigger an alert; the `eventcreate_idempotency_receipts` table could grow unbounded if the sweep stops running. | Add an 11th metric to FR-036: `eventcreate_idempotency_sweep_rows_total` (counter, labels: `tenant_id`) — increment by `rowsDeleted` per cron pass. Optionally also add `eventcreate_idempotency_sweep_duration_seconds` (histogram, labels: `tenant_id`) — symmetric to `eventcreate_pseudonymisation_sweep_rows_total` already in FR-036. The two new metrics are 1 line each in research.md R10's enumeration + 1 line in spec.md FR-036. |
| AA2 | 💡 | **Z5 TTL-sweep cron lacks an integration test in plan.md's Testing § enumeration.** Round 3 enumerated the cron handler but plan.md's Testing § lists `tests/integration/events/retention-sweep.test.ts` only for the PII pseudonymisation sweep (FR-032). The new idempotency-receipt TTL sweep has no test entry. | Add to plan.md Testing § list: `tests/integration/events/idempotency-ttl-sweep.test.ts` — seed `eventcreate_idempotency_receipts` rows with `ttl_expires_at` past and future; run the daily sweep cron; assert (a) all rows with `ttl_expires_at < NOW()` are deleted, (b) all future-expiry rows are preserved, (c) `eventcreate_idempotency_sweep_rows_total` metric increments by the correct count, (d) cross-tenant isolation holds (tenant A's expired rows do not affect tenant B's table). Mirrors the F4 + F8 cron-idempotency test pattern. |

### Architecture / Security / Performance / Testing / Dependencies

| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| (none new) | — | — | — |

All round-1 + round-2 + round-3 findings in these categories remain closed.

---

## Cross-Lens Insights 🔗

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|---------------|-------------------|------------|
| (none) | — | — | — | — |

(All round-2 + round-3 cross-lens insights are now closed.)

---

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | **0** |
| 💡 Recommendations | 2 (AA1, AA2) |
| 🤔 Questions | 0 |
| Round-1 findings still closed | 29 / 29 ✓ |
| Round-2 findings still closed | 8 / 8 ✓ |
| Round-3 findings still closed | 5 / 5 ✓ |
| **Cumulative findings closed (rounds 1+2+3)** | **42** |

---

## Consolidated Findings Table

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| AA1 | Engineering | 💡 | Operations | Z5 TTL-sweep cron missing OTel metric in FR-036 | Add `eventcreate_idempotency_sweep_rows_total` counter (+ optional duration histogram) to FR-036 / research.md R10 |
| AA2 | Engineering | 💡 | Testing | Z5 TTL-sweep cron missing integration test | Add `tests/integration/events/idempotency-ttl-sweep.test.ts` to plan.md Testing § list |

---

## Recommended Actions

### 🎯 Must-Address (Before `/speckit.tasks`)

None. Spec, plan, research, data-model, contracts, and quickstart are internally consistent and constitutional-compliant.

### 💡 Recommendations (Strongly Suggested — Address before `/speckit.implement`)

1. **AA1 — Add 1 metric (and optionally 1 histogram) to FR-036 + research.md R10**:
   - Spec FR-036 metric #11: `eventcreate_idempotency_sweep_rows_total` — counter, labels: `tenant_id`, increment per sweep pass with `rowsDeleted`
   - Research.md R10 metric list: same addition
   - Optionally an 11th alert in research.md R10: "Idempotency sweep failure spike" — `eventcreate_idempotency_sweep_rows_total{outcome='failed'} > 0` for 2 days in a row (signals a broken cron)
2. **AA2 — Add 1 integration test entry to plan.md Testing § list**:
   - `tests/integration/events/idempotency-ttl-sweep.test.ts` — seed mixed-expiry rows, run sweep, assert correct deletion behaviour + metric emission + cross-tenant isolation

### 🤔 Questions (Need Stakeholder Input)

None.

---

## Verdict Rationale

After three rounds of remediation:

1. **Round 1**: 6 must-address (audit schema, idempotency reuse claim, quota concurrency, F8 port name, RBAC drift, rollback audit fallback) + 18 recommendations + 5 deferred questions — all resolved.
2. **Round 2**: 2 must-address (M1 phantom payload column, M2 phantom idempotency table) + 6 recommendations — all resolved. Process learning Y2: multi-artefact decisions need cross-walk discipline.
3. **Round 3**: 2 must-address (Z1+Z2 research.md residual references to round-2-rejected approach) + 3 recommendations — all resolved.
4. **Round 4** (this report): 0 must-address; 2 light follow-up recommendations from the Z5 cron addition (observability metric + test).

The spec is **gate-ready** to advance to `/speckit.checklist` and subsequently `/speckit.tasks`. AA1 + AA2 can be applied now (recommended, ~3 min total) or deferred to `/speckit.tasks` decomposition where they naturally surface as work items.

---

**Severity Legend**:
- 🎯 **Must-Address**: Blocks proceeding to implementation
- 💡 **Recommendation**: Strongly suggested improvement
- 🤔 **Question**: Needs stakeholder input to resolve

---

*Generated by `/speckit.critique.run` (round 4, post-round-3 verification) — Dual-lens strategic and technical review for spec-driven development.*
