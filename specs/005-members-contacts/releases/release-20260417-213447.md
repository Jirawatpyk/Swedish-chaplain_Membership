# Release Artifact — F3 Members & Contacts

## Shipped

- **PR**: [#6 — feat(f3): Members & Contacts — US1-US7 + Phase 10 polish](https://github.com/Jirawatpyk/Swedish-chaplain_Membership/pull/6)
- **Branch**: `005-members-contacts` → `main`
- **Ship date**: 2026-04-17 21:34 +07
- **Commit range**: `58526ad..1dfc73e` (56 commits)
- **Files changed vs `58526ad`**: ~320
- **Lines added**: ~44,000

## Readiness Check (final)

| Check | Result |
|-------|--------|
| Tasks complete | 163 / 164 (T158 human-gated staging perf) |
| Staff review (final round 4) | ✅ APPROVED |
| QA (final) | ✅ ALL PASSED — 473/473 tests |
| Principle I tenant isolation | ✅ 14/14 Review-Gate |
| Principle III Clean Architecture | ✅ 0 Application → Infrastructure leaks |
| Principle VIII audit atomicity | ✅ throw-to-rollback across all 14 tenant-scoped use cases |
| `pnpm typecheck` | ✅ clean |
| `pnpm lint` | ✅ clean |
| i18n parity | ✅ 725 keys × EN/TH/SV |
| Unit tests | ✅ 953/953 project-wide (244 members + others) |
| Integration tests (live Neon) | ✅ 123/123 members |
| Contract tests | ✅ 106/106 members |
| Working tree on push | ✅ clean |

## Review + QA Trail

- Staff reviews (11 per-US + 4 holistic): `specs/005-members-contacts/reviews/`
- QA reports (6): `specs/005-members-contacts/qa/`
- Final review: `reviews/staff-review-20260417-190106-full-round4.md` → ✅ APPROVED
- Final QA: `qa/qa-20260417-191145.md` → ✅ ALL PASSED
- PR description: `releases/pr-description-20260417-202215.md`

## Shipped highlights (cross-ref CHANGELOG)

- US1–US3: Member + contact CRUD with primary-contact invariant, 20+ field forms, TanStack Table v8 directory with pg_trgm search (p95 = 258ms @ 5k rows)
- US3.b: 6-step atomic email-change transaction with dual-channel revert notification
- US4: Invite portal flow (F1 invitation + `contactRepo.linkUserInTx`)
- US5: Member self-service portal with whitelist-enforced patch
- US6: Plan change with bundle-change confirmation + override reason
- US7: Archive with session + invitation cascade (R001 column-grant preserved, R002 dedupe)
- Phase 10: WCAG 2.2 opportunistic adoption + L1-L3 outbox health observability
- T049: F1 invitation email migrated to outbox (eliminates class of silent-success bug)

## Post-ship (human-gated) checklist

- [ ] **T151 / T152**: Maintainer runs full local CI pipeline
  ```bash
  pnpm lint && pnpm typecheck && pnpm test:coverage && pnpm check:i18n \
    && pnpm test:integration && pnpm test:e2e
  ```
- [ ] **T158**: Deploy staging, capture p95/p99 via `@vercel/otel` for
  `GET/POST/PATCH /api/members`. Target: p95 < 400ms, p99 < 800ms (Principle VII).
- [ ] **Dashboard wiring** (backlog `docs/observability.md § 15`):
  `auth_invitation_enqueue_failed_total` + `outbox_permanent_failures_total` +
  `outbox_stuck_rows_total` — deferred until Grafana Cloud provisioned (F2+ roadmap).
- [ ] **Uptime monitor** for `/api/cron/outbox-dispatch` (external check — e.g.,
  UptimeRobot every 5 min) — catches "cron entirely down" bootstrap scenario
  that in-cron stuck-rows check cannot detect.
- [ ] **Human co-sign** for Constitution ≥2-reviewer clause on
  security-sensitive surfaces (auth + PII + GDPR export paths).

## Follow-up work (post-merge backlog)

1. **ESLint no-restricted-imports extension** — expand scope from `domain/**` to
   `application/**` to catch future Principle III regressions at lint time.
2. **Shared runInTenantWithAbort helper** — package UseCaseAbort + try/catch
   boilerplate in `src/lib/db.ts` to prevent W1-class bugs recurring.
3. **F10 "resend invitation" admin action** — closes the last silent-success
   recovery gap (currently requires manual DB intervention).
4. **Grafana Cloud migration (F2+ roadmap)** — unlocks dashboard + alert wiring
   for all metrics currently emitted but unused.

---

*Generated by `/speckit.ship` after 4 staff review rounds + final QA pass + human-gated push/PR confirmation.*
