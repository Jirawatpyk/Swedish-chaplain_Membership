# Critique Report (Round 2): F3 — Member & Contact Management + Smart Features

**Date**: 2026-04-15
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Previous critique**: [critique-2026-04-15T052948Z.md](./critique-2026-04-15T052948Z.md)
**Verdict**: ✅ **PROCEED** (with 4 low-severity cleanups offered optionally)

---

## Executive Summary

Both 🎯 Must-Address items from round 1 are **closed** with concrete, testable mitigations:

- **E6 / X2 — Admin email-change ATO**: dual-channel notification to OLD address + 48h revert token + 5-minute verification delay + high-severity audit + matching US3 AS7, FR-012a/b, new endpoint #16, new integration test `email-change-dual-channel.test.ts`, and § Security considerations section in spec.
- **E3 — Outbox permanent-failure lockout**: FR-012c recovery path + retry budget + `email_dispatch_failed` audit + admin "Re-send verification" endpoint #15 + chaos integration test `outbox-permanent-failure.test.ts`.

All 15 💡 recommendations from round 1 applied: MVP slice declared, US5 scoped to 3 real surfaces, `member_risk_flag` deferred to F8, bulk rate-limit FR-019b, compile-time whitelist tuple FR-014a, last_activity_at denormalization, search perf test, RLS coverage test extension, Migration Rollback Plan section, feature flag kill-switch, security.md drafted. All 3 🤔 questions answered and reflected in spec/plan.

Round 2 finds **zero new Must-Address items**. Four minor consistency/clarity issues remain, all 💡 non-blocking — they would tighten the spec before `/speckit.tasks` but don't change any architecture or behaviour decision. Verdict is PROCEED.

---

## Round-2 Findings

### Carried-over 🎯 Must-Address status

| Round 1 ID | Round 1 Severity | Round 2 Status | Evidence |
|---|---|---|---|
| E6 / X2 — Admin email-change ATO | 🎯 | ✅ Closed | spec § Security considerations, US3 AS6 6-step + AS7 revert + AS8 resend, FR-012a items (v)+(vi), FR-012b, endpoint #16, plan OWASP A07 note, new integration test listed |
| E3 — Outbox permanent-failure lockout | 🎯 | ✅ Closed | FR-012c, endpoint #15, retry-budget spec (60s/5m/30m/3h/12h + auto-refresh TTL), `email_dispatch_failed` high-severity audit, new `outbox-permanent-failure.test.ts`, runbook 4.2 in security.md |

### New round-2 findings

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| R2-E1 | Engineering | 💡 | Consistency | **Clarifications Q2 answer in spec drifted**: round-1 clarify answer says "Kill all active sessions … verified via an emailed token (24h TTL); old email is disabled at the same instant." The critique-apply added (a) 5-minute verification delay and (b) dual-channel revert notification. The original Q2 answer is now incomplete relative to FR-012a/b. | Append a follow-on bullet under the same Clarifications session dated 2026-04-15: `Q2 refinement (post-critique): added 5-minute verification delay on the new-email token + dual-channel notification-to-old-address carrying a 48-hour revert token (FR-012a item vi, FR-012b). This hardens Q2 against the admin-impersonation ATO vector identified by the round-1 critique.` |
| R2-E2 | Engineering | 💡 | Architecture | US3 misplacement of AS7 (revert) + AS8 (resend): both are new AS under "User Story 3 — Admin edits member details". AS7 is triggered by the **OLD-email recipient** (the victim member, not an admin), and AS8 is an admin recovery action. Neither is an "admin edits member details" flow. | Either (a) introduce a new `User Story 3.5 — Email-change revert + admin recovery` with priority P2 holding both AS, or (b) move AS7 into spec's new § Security considerations as a narrative scenario and keep AS8 under US3 (admin action). (a) keeps independent-test integrity; (b) is cheaper. Recommend (b) for F3 — the revert flow is sufficiently covered by § Security considerations + FR-012b + integration test. |
| R2-E3 | Engineering | 💡 | Data model | `members.last_activity_at` is declared as a denormalized timestamp but **how it gets updated is unspecified**. Options: (i) Postgres trigger `AFTER INSERT ON audit_log WHERE payload ? 'member_id'`, (ii) every use case updates it explicitly, (iii) Drizzle middleware hook. Each has different race-condition + audit-cascade semantics. | Add a one-line note to data-model.md § 1.1: `last_activity_at is updated by a Postgres AFTER-INSERT trigger on audit_log that reads payload->>'member_id' and sets members.last_activity_at = audit_log.timestamp. This keeps the write path in the same DB transaction as the audit event it mirrors, so there is no window where an event is logged but the timestamp is stale.` Avoids option (ii)'s cascade-of-forgettable-updates risk. |
| R2-E4 | Engineering | 💡 | Migration ops | Migration `0009_audit_log_f3_extension.sql` now adds **20+ `ALTER TYPE ADD VALUE` statements** (up from the round-1 plan's "17"). Each must run outside a transaction block per Postgres rules — a single typo + mid-run failure leaves the enum partially extended and the migration non-idempotent on re-run without manual fixup. | Two options: (a) Split into smaller, functionally-cohesive migrations (e.g., `0009_audit_members_core.sql`, `0010_audit_email_change.sql`, `0011_audit_bulk_rate_limit.sql`), each with < 8 values; drizzle-kit supports multiple migrations in one release. (b) Wrap each `ALTER TYPE ADD VALUE` in `DO $$ BEGIN … EXCEPTION WHEN duplicate_object THEN NULL; END $$;` blocks so the migration is idempotent on re-run even if a partial failure happens. Recommend (b) for single-file simplicity + resilience. |

### Cross-check: Principle I, II, III, IV, X

| Principle | Status |
|---|---|
| I — Tenant isolation (v1.4.0 clauses 1-5) | ✅ All 5 clauses mapped in plan + security.md |
| II — Test-First | ✅ TDD ordering stated; new integration tests listed; coverage thresholds explicit |
| III — Clean Architecture | ✅ Branded `UserId` rule added (E2); ESLint boundary rule extended |
| IV — PCI | ✅ N/A — explicitly stated |
| X — Simplicity | ✅ `member_risk_flag` deferred, US5 narrowed to 3 real surfaces, no CSV import, no real-time, no separate contacts module |

No Constitution violations.

### Scope boundary verification

| Claimed out of scope | Status |
|---|---|
| Bulk CSV import | ✅ called out twice |
| At-risk scoring (F8) | ✅ column deferred, placeholder rendering |
| Benefit-usage dashboard (F9) | ✅ |
| Invoice history per member (F4) | ✅ |
| Online renewal (F5) | ✅ |
| Event attendance (F6) | ✅ |

---

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendations | 4 |
| 🤔 Questions | 0 |
| Round-1 Must-Address closed | 2 of 2 |
| Round-1 Recommendations applied | 15 of 15 |
| Round-1 Questions answered | 3 of 3 |

---

## Recommended Actions

### 💡 Recommendations (non-blocking — apply before `/speckit.tasks` for a clean run)

1. **R2-E1** — spec.md § Clarifications: add a bullet noting the Q2 post-critique refinement (5-min delay + dual-channel revert).
2. **R2-E2** — spec.md US3: move AS7 (revert flow) into § Security considerations as narrative; keep AS8 (admin resend) under US3 renumbered as AS7. Cleaner US3 semantics.
3. **R2-E3** — data-model.md § 1.1: add one-line note explaining `last_activity_at` is updated by an `AFTER INSERT ON audit_log` trigger.
4. **R2-E4** — plan.md § Migration Rollback Plan: note that each `ALTER TYPE ADD VALUE` in `0009` is wrapped in idempotency-safe DO blocks for resilient re-run.

---

## Verdict

**✅ PROCEED** — round 1's blockers are closed; round 2 surfaces only consistency/clarity tightening. Safe to enter `/speckit.tasks` now, or apply the 4 × 💡 R2 recommendations first for a cleaner run.

---

**Severity Legend**:
- 🎯 **Must-Address**: Blocks proceeding to implementation
- 💡 **Recommendation**: Strongly suggested improvement
- 🤔 **Question**: Needs stakeholder input to resolve

---

*Generated by `/speckit.critique` — Dual-lens strategic and technical review for spec-driven development.*
