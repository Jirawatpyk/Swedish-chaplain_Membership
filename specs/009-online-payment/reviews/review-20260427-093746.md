# Code Review Report: F5 Phase 7 — User Story 5 Payment Failure Surface

**Reviewer**: AI Agent (Staff Engineer Perspective)
**Date**: 2026-04-27 09:37:46 +07:00
**Feature**: [`specs/009-online-payment/spec.md`](../spec.md) — US5, FR-028(d/g/j), FR-011b, FR-025c
**Branch**: `009-online-payment`
**Scope**: commits `3214f0c` (Phase 7 implementation) + `b4e84e1` (post-review fixes H1+H2+M1–M6+L3)
**Verdict**: ✅ **APPROVED**

---

## Executive Summary

Phase 7 (T119–T127) closes US5 — the payment-failure surface. The implementation reuses existing infrastructure (decline-code mapping in `card-form.tsx`, `useThreeDSecurePoll`, `StatusPanel`, `PaymentFailurePanel`, `processor_unavailable` route mapping) and adds two focused new surfaces: (a) consolidated FR-028(j) aria-live announcer extracted as a pure helper for testability, and (b) persistent `sonner.error` toast as a defense-in-depth signal alongside the inline failure panel. Test coverage expanded by 3 new files (1 unit, 1 integration on live Neon, 2 new E2E specs + 1 fixme'd shell). Two prior PR-review rounds were applied in this branch — the fix-batch (`b4e84e1`) addressed all HIGH+MEDIUM findings (CI hard-fail gates, typed-messages namespace pinning, comment hygiene, void-branch coverage, idempotency-key assertions). PCI SAQ-A posture preserved (no card data ever surfaces in toast or aria-live content). Constitution v1.4.0 — no violations. **Approved for merge.**

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| R001 | 🟢 Suggestion | `pay-sheet-internal.tsx` | 750-754, 783-789 | Test Quality / a11y | Polite (`pay-sheet-aria-announcer`) and assertive (`PaymentFailurePanel`) live regions both fire on `payState='failure'`. Code analysis shows content is complementary (different strings, defense-in-depth) but priority interleave behavior on NVDA/VoiceOver/TalkBack is implementation-defined. | Verify in T114 manual SR pass; if AT drops the polite announcement when assertive cuts in, downgrade `PaymentFailurePanel` to `aria-live="polite"` since the consolidated announcer already covers SC 4.1.3 obligations. |
| R002 | 🟢 Suggestion | `tests/e2e/payment-stale-invoice-auto-refund.spec.ts` | full file | Test Quality | T121 is `test.fixme(true, …)` pending admin-void surface + Stripe webhook driver harness + member refund-notification UI. All 3 dependencies are out-of-scope for Phase 7. Equivalent coverage at unit (`confirm-payment.test.ts` × 5 cases) + integration (`stale-invoice-auto-refund.test.ts` × 2 cases) is green. | Track as a follow-up gate when admin-void UI lands (Phase 8/9); the shell + comment block document the expected assertions clearly. |
| R003 | 🟢 Suggestion | `tests/integration/payments/stale-invoice-auto-refund.test.ts` | seed `beforeAll` | Performance / Test Quality | beforeAll seeds two parallel invoice+payment chains synchronously inside a single tx. Cost is small (~1s observed) but a future expansion to cover `credited` + `not_payable` branches would benefit from a per-test seed factory. | Defer until 4+ branches needed; current 2-branch shape is well-isolated. |

**Categories**: Correctness, Security, Performance, Spec Compliance, Error Handling, Test Quality, Architecture

---

## Spec Coverage Matrix

| Requirement | Status | Implementation Notes |
|-------------|--------|---------------------|
| **US5 AS-1** Card declined → bilingual message + invoice stays `issued` + retry without leaving page | ✅ Implemented | `card-form.tsx:148-179` decline-code switch maps `decline_code` / `code` to localized `t('retry.reason*')` keys; `<PaymentFailurePanel>` renders with retry CTA; T119 E2E `payment-card-decline.spec.ts` exercises insufficient_funds path; existing `payment-card-declined.spec.ts` covers generic_decline. |
| **US5 AS-2** PromptPay QR timeout → "QR expired — regenerate" CTA, fresh PI on regenerate | ✅ Implemented (Phase 4 + Phase 7) | `promptpay-panel.tsx` countdown + `handlePromptPayRefresh` in `pay-sheet-internal.tsx:573-579` bumps `promptpayRetryCount` to force fresh PI. |
| **US5 AS-3** Webhook delayed 2 min → "Payment processing" placeholder, eventual webhook still completes | ✅ Implemented | `<StatusPanel kind="processing">` + `OptimisticPaidOverlay` + retry chain in `index.tsx:232-286`. |
| **US5 AS-4** Processor unreachable → "Service temporarily unavailable" + NO PI created + single connectivity-failure log | ✅ Implemented (T124) | `initiate-payment.ts:471-475` + `route.ts:96-97,282-312` returns 502 `processor_unavailable` from gateway-error branches BEFORE any DB insert or audit emit. Route logs `payments.initiate.use_case_error` (warn) but never appends to audit_log for this code. |
| **FR-028(d)** 3DS challenge in-flight state with shimmer + cancel | ✅ Implemented (T125) | `<StatusPanel kind="three-d-secure">` + `useThreeDSecurePoll` poll for PI status; tertiary Cancel button → `handleCancel` → `firePaymentCancel`. |
| **FR-028(g)** Reduced-motion comprehensive coverage | ✅ Implemented (T126) | Sheet `motion-reduce:duration-0!` (index.tsx:420), confirmation `motion-safe:zoom-in-50` (confirmation-panel.tsx:151), card-form fade `motion-reduce:duration-0` (card-form.tsx:225), QR pulse `motion-reduce:animate-none` (promptpay-panel.tsx:311), `<Skeleton>` shimmer reduces inside primitive CSS, sonner respects OS reduced-motion natively. |
| **FR-028(j)** Bilingual aria-live region announcing payment-status transitions | ✅ Implemented (T127) | Consolidated `<div data-testid="pay-sheet-aria-announcer" aria-live="polite" role="status" className="sr-only">` derives text from pure helper `derivePayStateAnnouncement(payState, t)` covering processing/threeDSecure/success/failure (idle/initiating/card-form return ''); 8 unit tests green. |
| **FR-011b** Stale-invoice auto-refund + credit-note rollup | ✅ Implemented (Phase 6 + T121/T122 verified) | `confirm-payment.ts:243-291` calls `processorGateway.createRefund` with idempotency key `auto-refund-${paymentId}` + emits `payment_auto_refunded_stale_invoice` audit; T122 integration test (live Neon) verifies both `paid` and `void` branches with idempotency-key assertion. |
| **FR-025c** Deep-link `?pay=1` + drawer cancel cleanup on navigate-away | ✅ Implemented | `index.tsx:139-141, 330-335` — page unmount cleanup fires `firePaymentCancel('user_navigated_away')`; T120 E2E asserts cancel POST body pinned to `user_navigated_away`. |

**Coverage**: 9/9 in-scope requirements implemented (100%)

---

## Test Coverage Assessment

| Area | Tests Exist? | Coverage | Gaps |
|------|-------------|----------|------|
| `derivePayStateAnnouncement` pure helper | ✅ | 8 cases — all 5 branches (idle/initiating/card-form coalesced, processing, requires-action, success, failure) + empty-reason edge case | None at unit layer |
| Stale-invoice auto-refund (integration) | ✅ | 2 cases — `paid` + `void` branches, both with idempotency-key assertion | `credited` branch + `not_payable` error variant covered at unit layer (`confirm-payment.test.ts` lines 201-225) — sufficient |
| Card decline E2E | ✅ | 2 cases (existing `generic_decline` + new T119 `insufficient_funds`) | 3DS-rejection path covered by 3DS poll unit tests; `expired_card` + `incorrect_cvc` rely on the same switch — covered structurally |
| Resume + cancel E2E | ✅ | 2 cases (T120a reuse PI / T120b navigate-away cancel) | Explicit Cancel-button cancel path on ProcessingPanel/3DS panels not exercised; same code path as navigate-away (`firePaymentCancel`) — acceptable |
| Stale-invoice E2E (T121) | 🟡 fixme'd shell | 0 cases | Covered at unit + integration layers (T122); fixme is legitimate per US5 dependency stack |
| ARIA-live announcement (component) | 🟢 indirect | Asserted via T119 spec body | A dedicated component test mounting `<PaySheetInternal>` would tighten coverage but requires Stripe SDK + initiate-fetch mocks — pure-helper coverage is the proportional choice |

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Files reviewed | 8 (1 production, 1 doc/spec, 1 test infra, 5 test files — 4 new + 1 modified) |
| 🔴 Blockers | 0 |
| 🟡 Warnings | 0 |
| 🟢 Suggestions | 3 |
| Spec coverage | 9/9 in-scope FRs + AS = 100% |
| Test green | unit 8/8 + integration 2/2 (live Neon) = **all green** |
| Typecheck | ✅ 0 errors |
| Lint | ✅ 0 errors (10 pre-existing unrelated warnings) |
| Constitution v1.4.0 violations | 0 |
| PCI SAQ-A regressions | 0 (verified `toast.error(message)` cannot leak card data — message comes only from `t('retry.reason*')` localized keys) |
| LOC delta | +824 / −21 across Phase 7 |

---

## Constitution Alignment

| Principle | Status | Evidence |
|-----------|--------|----------|
| I. Data Privacy & Tenant Isolation (NON-NEG) | ✅ | T122 integration test runs under `runInTenant(tenant.ctx, …)` + `f5AuditAdapter` writes through tenant-scoped repo; cleanup verifies `tenant.cleanup()` on afterAll. |
| II. Test-First (NON-NEG) | ✅ | TDD shells for T121 (E2E) + T122 (initially `it.todo`, fleshed out post-review); 8 unit + 2 integration tests precede or accompany every code path. |
| III. Clean Architecture (NON-NEG) | ✅ | `derivePayStateAnnouncement` extracted as pure presentation-layer helper; no domain/application boundary crossings; `confirmPayment` deps composed via factory at integration test boundary. |
| IV. PCI DSS SAQ-A (NON-NEG) | ✅ | `clientSecret` remains ephemeral; new `toast.error(message)` content sourced from localized translation keys only — never from raw Stripe English text or card data. Verified via grep + code trace. |
| V. i18n (EN canonical + TH/SV) | ✅ | All new announcement strings pull from existing `portal.payment.{processing,threeDSecure,success,retry}.title` keys present in en/th/sv messages. |
| VI. Inclusive UX | ✅ | Consolidated aria-live + reduced-motion full coverage + bilingual decline reasons. |
| VII. Perf & Observability | ✅ | T122 integration test runs in 1002ms (paid) + 349ms (void); no new hot-path operations. |
| VIII. Reliability | ✅ | Idempotency-key contract `auto-refund-${paymentId}` now actively asserted; cancel reason pinned to `user_navigated_away` in E2E. |
| IX. Code Quality (Solo-maintainer substitute) | ✅ | Self-review applied; lint+typecheck green; 2 PR-review rounds + 1 staff-review pass complete. |
| X. Simplicity | ✅ | Helper extraction is minimal, single-purpose; toast addition is one line; no new abstractions introduced. |

---

## Recommended Actions

### Must Fix (Blockers)
*None.*

### Should Fix (Warnings)
*None.*

### Nice to Fix (Suggestions)
1. **R001** — Verify polite/assertive aria-live interleave on NVDA/VoiceOver/TalkBack as part of T114 manual SR pass; downgrade `PaymentFailurePanel` to `aria-live="polite"` if AT drops the polite announcement.
2. **R002** — Wire T121 E2E body when admin-void surface + Stripe webhook driver + member refund-notification UI land in subsequent phases.
3. **R003** — If 4+ stale-invoice branches need integration coverage in future, refactor `beforeAll` seed into a per-test factory.

---

## Verdict

✅ **APPROVED** — Phase 7 is safe to ship. No blockers, no warnings; only 3 nice-to-have suggestions tracked for follow-up phases. All in-scope spec requirements implemented, Constitution v1.4.0 fully aligned, PCI SAQ-A posture preserved, test suite green at unit + integration + (where harness allows) E2E.

**Next steps**: `/speckit.ship` to prepare the F5 release once Phase 8 (US6 receipt-email path verification) + Phase 9 (polish + cross-cutting) + Phase 10 (review-gate blockers including T114 SR QA) complete.

---

*Generated by `/speckit.staff-review.run` — Staff-level code review for spec-driven development.*
