# Code Review Report: F5 Online Payment — Staff Review #3 (R1+R2 closure verification)

**Reviewer**: AI Agent (Staff Engineer Perspective) — focused closure pass
**Date**: 2026-04-28 15:24:37 UTC
**Feature**: [`specs/009-online-payment/spec.md`](../spec.md) — F5 Online Payment (Stripe + PromptPay)
**Branch**: `009-online-payment` (HEAD = `5708434`; +110 commits ahead of `main`; 523 files / +69 158 / −1 076)
**Scope**: Verify closure of the 1 Warning (R1) + 2 Suggestions (R2, R3) raised in [`review-20260428-135636.md`](./review-20260428-135636.md); audit the 9 stale-test fixes in commit `5708434` for hidden production risk; spot-check working-tree state.
**Prior reviews**: 12 rounds (`review-20260423-211654.md` … `review-20260428-135636.md`).
**Verdict**: ✅ **APPROVED** — every shipping-condition from staff-review #2 is closed on disk; the test-only patch in `5708434` introduces zero production-code risk.

---

## Executive Summary

Staff-review #2 (`review-20260428-135636.md`) produced a verdict of ⚠️ **APPROVED WITH CONDITIONS** with two pre-ship asks (R1: refresh CT-7 wording so W4 reads as closed, not closure-pending; R2: commit the 3 working-tree script deletions). Both have been resolved in commit `5708434` (“F5 staff-review #2 + 9 stale-test fixes — full suite green”).

**R1 closure verified** — `complexity-tracking-addendum.md § CT-7` heading now reads “CT-7 (W4 closure) — perf benchmark sample-size” and the body documents the n=100 + 5-warmup re-run with async p95 = 939 ms (61 ms headroom vs the 1000 ms SLO-F5-002b dev budget). No future-tense language remains.

**R2 closure verified** — `scripts/check-drizzle-journal.ts`, `scripts/sync-drizzle-bookkeeping.ts`, `scripts/sync-drizzle-journal.ts` (300 LOC total) and the matching `db:sync-bookkeeping{,:prod}` entries in `package.json` are deleted in `5708434`. `drizzle-kit migrate` is now the sole migration path on `main` history — no fallback machinery to nudge teams off the canonical workflow.

**R3 (optional doc breadcrumb)** — the addendum still does not carry the explicit “Future migration discipline” pointer suggested in #2 R3, but #2 itself classed this as a 🟢 nice-to-have. Not blocking.

The 9 stale-test fixes in `5708434` are scoped strictly to test files (`tests/unit/**` × 2, `tests/integration/**` × 4) plus doc + tasks housekeeping. **No `src/**` changes**: confirmed via `git diff aa6d898..5708434 -- 'src/**'` returning empty. Each stale-test fix has a documented post-T166 / migration-0050+0056 root cause, none of them silences a real regression:

- `index-barrel.test.ts` — adds `logger` key to `ConfirmPaymentDeps` (matches H2 closure from R2).
- `confirmation-panel.test.tsx` — drops `{last4}` from i18n template (matches W15 closure); SR thresholds now `[5,3,1]` (S10 closure); 4 obsolete last4-mask tests removed (production code stopped emitting last4 to UI per W15, so the tests were asserting dead behaviour).
- `audit-coverage.test.ts` — bumps declared F4 inventory 18→20 (adds `receipt_rendered` + `pdf_render_permanently_failed`, both produced by the T166 async receipt-PDF path; the count drift was a stale fixture, not a missing event-type emission).
- `void-invoice.test.ts` — paid-invoice seed now sets `receipt_pdf_status='rendered'` so the migration-0056 CHECK `invoices_paid_has_receipt_status` is satisfied. Production code already produces this state through the dispatcher; the test fixture was the lagging artefact.
- `f4-markpaid-integration.test.ts` — 4 tests rewritten for T166 async-default: `renderSpy` no longer fires inline; assertions now target the `receipt_pdf_render` outbox row + `invoices.receipt_pdf_status='pending'` + `invoice_paid` email carrying `dependsOnReceiptPdf=true`. SC-003 reuse re-asserted via outbox payload shape. This is the correct shape for an async-by-default receipt-PDF render; the prior assertions were pinning behaviour that T166 explicitly changed.
- `out-of-band-refund.test.ts` — seed audit row uses `randomUUID()` coercion (`audit_log.id` and `request_id` are `uuid`, the prior string seed was a latent bug), adds NOT-NULL `summary`, and best-effort-swallows the `DELETE` rejection from the append-only trigger on cleanup. The swallowed error is the **expected** behaviour of T-13 (security.md append-only invariant) — leaked seed rows accumulate harmlessly because the invariant body re-asserts `retention_years=10` + `runbook_url` on every OOB row on every rerun (no false negative possible). The fix tightens the test, it does not paper over a defect.

Every test-fixture correction has a clearly identified production-side cause that already shipped (T166 async receipt-PDF; migration 0050 audit retention; migration 0056 receipt-status CHECK). The patch is the textbook “tests caught up to production” shape, not the dangerous “tests muted to silence a regression” shape.

The only remaining working-tree drift is the new untracked QA artefact `specs/009-online-payment/qa/qa-2026-04-28.md` (`?? specs/009-online-payment/qa/qa-2026-04-28.md`). It is a CLI-QA report (TC-001…TC-007 all ✅ except the explicitly deferred E2E browser walkthrough) and should be tracked alongside the prior QA notes — not shipping-critical, but worth committing before `/speckit.ship` so the final QA evidence is on `main`.

---

## Findings — Severity-Ranked

### 🔴 Blockers
*None.*

### 🟠 High-Priority Warnings
*None.*

### 🟡 Warnings (recommended pre-ship)
*None.*

### 🟢 Suggestions (nice-to-have)

| ID | File | Finding | Source |
|---|---|---|---|
| **R-S1** | working tree | Untracked CLI-QA artefact `specs/009-online-payment/qa/qa-2026-04-28.md` (TC-001…TC-007 evidence). Commit before `/speckit.ship` so the final QA trail lives on `main` history alongside `sr-qa-2026-04-28.md`. | this review |
| **R-S2** | `specs/009-online-payment/complexity-tracking-addendum.md` | (Carry from #2 R3) Add the “Future migration discipline” breadcrumb that mirrors CT-6 — e.g. “Bookkeeping table is `public.drizzle_migrations` per `drizzle.config.ts`. Never write manual `apply-NNNN.ts` scripts; always use `pnpm db:migrate` (wraps `drizzle-kit migrate`). The 3 sync-recovery scripts (deleted in `5708434`) are gone by design.” Closes the doc-pointer ask from #2 R3. | this review (#2 R3 carry-forward) |

---

## Verification of #2 Findings

| Prior ID | Severity | Status | Evidence |
|---|---|---|---|
| **R1** (#2) | 🟡 Warning | ✅ Closed | `complexity-tracking-addendum.md § CT-7` heading and body rewritten — “closure” not “closure-pending”; n=100 numbers documented; future-tense Decision removed |
| **R2** (#2) | 🟢 Suggestion | ✅ Closed | `scripts/check-drizzle-journal.ts` + `sync-drizzle-bookkeeping.ts` + `sync-drizzle-journal.ts` deleted in `5708434`; `package.json` no longer has `db:sync-bookkeeping{,:prod}` |
| **R3** (#2) | 🟢 Suggestion | ⚪ Open | Doc breadcrumb still missing — restated here as **R-S2** (still non-blocking) |

---

## Spec Coverage Matrix

Carry-forward from #2 — no surface changed:

| Requirement / User Story | Status | Notes |
|---|---|---|
| **US1** Card payment happy path | ✅ |
| **US2** PromptPay QR happy path | ✅ |
| **US3** Admin reconciliation view | ✅ |
| **US4** Admin refund (full + partial) | ✅ |
| **US5** Payment failure surface | ✅ |
| **US6** Receipt email on payment success | ✅ | T166 async + B3 stuck-pending recovery |
| **FR-005** SAQ-A scope preservation | ✅ | T166 worker scope explicit in `saq-a-attestation.md § 2.5` |
| **FR-026** Stripe API version pinning | ✅ |
| **FR-029/030** Audit retention | ✅ | 4-tier split documented in CT-5 |
| **SLO-F5-001** initiate p95 < 1.2 s | ✅ | 38 ms headroom |
| **SLO-F5-002a** webhook canceled/failed p95 < 500 ms (prod) | ✅ |
| **SLO-F5-002b** webhook succeeded p95 < 1000 ms (dev) | ✅ | 939 ms n=100 + 5-warmup, 61 ms headroom |
| Constitution Principle I (tenant isolation) | ✅ | RLS+FORCE; cross-tenant integration test green |
| Constitution Principle IV (PCI DSS) | ✅ | SAQ-A re-attested |
| PDPA §28 + §24 / GDPR Art. 6(1)(b) + Art. 44 SCC | ✅ | `data-transfers.md` lawful-basis doc |

---

## Test Coverage Assessment

Carry-forward from #2 plus the test-fixture corrections in `5708434`:

- Unit + contract: **2361/2361** pass (227 files, 161 s).
- Integration (live Neon Singapore): **623/623** pass + 10 skipped + 1 todo (128 files) — full suite green after the in-flight fixture fixes.
- E2E (Playwright + axe): not in scope this round; deferred to the human-gated T146 manual SR pass.
- Coverage thresholds (Constitution Principle II) preserved: Domain 100 % line; Application 80 % line + branch; security-critical paths (sign-in, payment-state-machine, refund-state-machine, webhook idempotency) 100 % branch.

**T118 staff-review gate**: this is round #3 — gate is now over-satisfied (≥6 `/speckit.review` rounds + 3 `/speckit.staff-review` rounds vs. the ≥6 + ≥2 minimum).

Outstanding human-gated items unchanged from #2 (none are blockers):
- T114 manual SR + cross-browser + reduced-motion (deferred)
- T117 maintainer co-sign on security checklist (deferred)
- T146 manual SR pass — template at `sr-qa-2026-04-28.md`
- T155 SAQ-A signature (T151/T155/T156)
- T115t throwaway-tenant E2E infra (deferred to Phase 10+)

---

## Metrics

| Metric | Value |
|---|---|
| Files changed since `main` | 523 |
| LOC delta | +69 158 / −1 076 |
| Commits ahead of `main` | 110 |
| Migrations | 0033 → 0062 (30 in F5 scope) |
| Audit event types added (F5) | 17 (+ R2-E4 backfill of 6 F4 types to 10y) |
| OTel metrics (F5) | 18 |
| Alert rules (F5) | 10 |
| Reviews completed | 13 (12 prior + this) |
| Findings — Blockers | 0 |
| Findings — High | 0 |
| Findings — Warnings | 0 |
| Findings — Suggestions | 2 (R-S1 untracked QA file; R-S2 doc breadcrumb carry-forward) |
| Spec coverage | 100 % of FR-### implemented |

---

## Recommended Actions

1. **Before `/speckit.ship`** (≤2 minutes, both optional but tidy):
   - Commit the untracked QA artefact `specs/009-online-payment/qa/qa-2026-04-28.md` (R-S1).
   - Optionally land R-S2 doc breadcrumb in the addendum.

2. **Then proceed to `/speckit.ship`**:
   - SAQ-A re-attestation already complete (`saq-a-attestation.md § 5`).
   - Vercel Rolling Releases plan (10 % → 50 % → 100 % with 30-min observation windows) is documented in `plan.md`.
   - Performance budget validated at p95 = 939 ms with 61 ms headroom on the most expensive path (T166 async receipt-PDF).

3. **First 30 days post-ship** (carry-forward from CT-4):
   - Watch `slo_f5_002b_breach` PromQL alert (W3 closure).
   - Watch `receipt_pdf_pending_count > 20` per tenant for thundering-herd risk; if it fires, apply the post-T166 follow-up `LIMIT 50` to the reconcile-cron sweep (S4).

4. **F9 audit-purge job** (deferred per CT-3):
   - Add maintenance-role DELETE bypass for `processor_events`. Documented retention = 5 years.
   - Same maintenance-role bypass also drains accumulated R020 staff-review test-seed rows in `audit_log` (intentionally append-only on the app role — see `out-of-band-refund.test.ts` cleanup-block comment).

---

## Verdict

✅ **APPROVED** — F5 is shipping-ready.

No code-level blockers, no high-priority warnings, no warnings, no constitution violations, and zero net-new production-code risk in `5708434` (the patch is test-fixture + docs only). Both pre-ship conditions from staff-review #2 (R1: CT-7 wording; R2: commit working-tree deletions) are closed on disk. The 13-round review trail demonstrates exhaustive due diligence; T166 architecture is robustly validated; PCI DSS SAQ-A scope is preserved with formal attestation; PDPA §28 + GDPR Art. 44 lawful basis is documented; Thai RC §87 no-gap invariant is preserved through both the F4 sequential allocator and the T166 receipt-number pre-allocation pattern.

**Next gate**: `/speckit.ship` (R-S1 + R-S2 are nice-to-have polish, not pre-conditions).

---

*Staff-review round #3. Combined with the 6 prior `/speckit.review` rounds and 2 prior `/speckit.staff-review` rounds, T118’s “≥6 review + ≥2 staff-review rounds” gate is satisfied with margin.*
