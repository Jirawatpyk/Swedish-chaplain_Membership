# Code Review Report: F5 Phase 6 — US4 Admin Refund

**Reviewer**: AI Agent (Staff Engineer Perspective)
**Date**: 2026-04-27
**Feature**: [specs/009-online-payment/spec.md](../spec.md) (US4 + FR-011/011b/012/013/029)
**Branch**: `009-online-payment`
**Scope**: Commits `b6bad95..HEAD` (10 commits, 55 files, +5503 lines) + uncommitted refund-form `mode: 'onChange'` doc fix
**Verdict**: ✅ **APPROVED**

---

## Executive Summary

F5 Phase 6 ships **18 of 18 tasks complete** with end-to-end coverage across all five test layers (unit, contract, integration, E2E, regression). Implementation correctly satisfies User Story 4 acceptance scenarios AS1–AS6 with FR-011 / FR-011b / FR-012 / FR-013 / FR-029(a–h) requirements all evidenced by code paths and tests. The architecture honours all four NON-NEGOTIABLE Constitution principles: tenant isolation (RLS+FORCE on `refunds` + admin-scoped `requireAdminContext('refund', 'write')`), test-first (T101 RED contract preceded T108 use-case; 100% branch coverage on `issue-refund.ts`), Clean Architecture (Domain pure; Application uses ports only; Infrastructure provides adapters), and PCI DSS (no card data in F5 surfaces; `buildUseCaseErrorTelemetry` scrubs Stripe SDK detail to bounded enums).

The implementation underwent **two prior review cycles** in this session (`/speckit.review` with 6 specialist agents → 25 findings → `/speckit.fixit.run` applied 18 fixes including 3 Critical, 10 Important, and 7 Suggestions; with 1 polish task `T130a` deferred to Phase 9). Verification confirms all 18 fixes landed correctly in code. One known FR (`FR-011a` out-of-band-refund detection) is correctly tracked under existing tasks T130/T131 (Phase 9) and not in scope for Phase 6.

**Strengths**: two-phase tx design with explicit failure-recovery paths; canonical `getRefundContextForUpdate` collapses 3 SELECTs into 1 inside FOR UPDATE lock; React 19 `cache()` dedup eliminates duplicate DB reads between page + Suspense'd timeline; bigint→string boundary serialization respects JS safe-integer window; `finaliseFailedRefund` helper eliminates copy-paste drift between Stripe-fail / F4-fail / Phase-B-fail branches; 100% line+branch+function coverage on all 4 Phase 6 domain files. **Concerns**: none blocking. One Phase 9 polish task (T130a) for stale-pending-refund recovery in Postgres double-fault scenarios documented and tracked.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| R001 | 🟢 Suggestion | `tests/integration/payments/refund-multi-partial.test.ts` | seedPlaceholderCN | Test Quality | Per-test `seedPlaceholderCN` opens a separate `runInTenant` tx per CN row (3+ calls per multi-partial test). Could batch into one INSERT…VALUES. | Phase 9 polish; ~200ms suite-wide saving. Non-blocking. |
| R002 | 🟢 Suggestion | `src/app/api/refunds/initiate/route.ts` | 50-58 | Spec Compliance | Route accepts `amountSatang: z.number()` (max 2_000_000_000 = 20M THB cap). Output serialises bigint as string per audit finding #20 — input/output asymmetry is OK at current cap but inconsistent with the bigint-output rationale. | Defer until cap raised; `z.string().regex(/^\d+$/)` then `BigInt()` would close the gap. |
| R003 | 🟢 Suggestion | `src/app/(staff)/admin/invoices/[invoiceId]/_components/refund-dialog/index.tsx` | 79 | Architecture | `<AlertDialogTrigger>` styled via `buttonVariants` className skips the project's `<Button>` primitive (shared focus-ring, cursor, disabled state per ux-standards § 11). Mirrors `delete-draft-dialog.tsx` precedent so not a fresh deviation. | Optional: refactor to `<AlertDialogTrigger asChild><Button variant="destructive-outline">…</Button></AlertDialogTrigger>` across all admin AlertDialogs. |
| R004 | 🟢 Suggestion | `src/app/(staff)/admin/invoices/[invoiceId]/_components/refund-dialog/refund-form.tsx` | 121-129 | Spec Compliance | `mode: 'onChange'` deviates from FR-029(c) literal ("amount validates onBlur"). Mitigated by `touchedFields` gate so error MESSAGES surface only after blur — functionally equivalent + Confirm-button gating works. Comment documents the trade-off. | Already documented; either accept or amend FR-029(c) wording. Non-blocking. |
| R005 | 🟢 Suggestion | tasks.md | T130a | Architecture | Stale-pending-refund recovery sweep (Postgres double-fault scenario) deferred to Phase 9. Phase B try/catch (C2 fix) covers the common case. | Track + ship in Phase 9 alongside T130 webhook handler. ~150 LOC + tests. |

**Categories**: Correctness (0), Security (0), Performance (0), Spec Compliance (2), Error Handling (0), Test Quality (1), Architecture (2)

**No Blockers. No Warnings. Five non-blocking Suggestions.**

---

## Spec Coverage Matrix (US4 — Admin Refund)

| Requirement | Status | Implementation Notes |
|-------------|--------|---------------------|
| **FR-011** — admin-only in-app refund flow, free-form THB, accumulating partials | ✅ Implemented | `issueRefund` use-case + `POST /api/refunds/initiate` route + `requireAdminContext('refund', 'write')` |
| **FR-011a** — out-of-band refund detection (`charge.refunded` no in-app row) | ⏸ Phase 9 | Tracked as T130 (process-charge-refunded) + T131 (integration test). Not in Phase 6 scope. |
| **FR-011b** — refunded_amount_sum + status transitions + concurrent serialisation | ✅ Implemented | `getRefundContextForUpdate` SELECT under `paymentsRepo.lockForUpdate` + `checkRefundNotExceedingRemainder` invariant + `Promise.all()` race test (T102) |
| **FR-012** — atomic CN + invoice status + email + audit trail | ✅ Implemented | F4 `issueCreditNoteFromRefund` bridge owns CN+PDF+outbox+audit; F5 use-case finalises payment status + emits `refund_succeeded` |
| **FR-013** — failed refund: no CN, bilingual error, `refund_failed` audit | ✅ Implemented | `finaliseFailedRefund` helper covers Stripe-fail + F4-bridge-fail + Phase-B-fail (C2 fix) branches |
| **FR-029(a)** — shadcn `<AlertDialog>` primitive | ✅ Implemented | `RefundDialog` renders `<AlertDialog>` from `@/components/ui/alert-dialog` |
| **FR-029(a)** — title literal "Issue refund?" / "ออกใบลดหนี้?" / "Utfärda återbetalning?" | ✅ Implemented | E1 fix aligned 3 locales to spec literal |
| **FR-029(b)** — Amount label-above + asterisk + aria-required + help-text + inputmode='decimal' | ✅ Implemented | `aria-required="true"` (I9 fix), `pattern="\d+(\.\d{1,2})?"` (UX polish), help text with live `formatSatangThb` |
| **FR-029(c)** — Submit disabled until both fields valid | ✅ Implemented | `canSubmit = isValid && !submitting && (!isFullRefund \|\| phraseMatches)`. Validation timing minor divergence documented (R004). |
| **FR-029(d)** — Cancel default-focused | ✅ Implemented | Radix focus-scope autofocuses first tabbable child; `<AlertDialogCancel>` rendered before `<Button>` Confirm |
| **FR-029(e)** — Loading state + success toast + inline error | ✅ Implemented | `<Loader2Icon motion-safe:animate-spin />` + `sonner.success` + `<InlineAlert>` above buttons |
| **FR-029(f)** — Typed-phrase only on full refund, case-sensitive | ✅ Implemented | `<TypedPhraseConfirm>` renders when `amountSatang === remainingRefundableSatang`; case-sensitive `===` compare; visible error + `role="alert"` (I10 fix) |
| **FR-029(g)** — Focus return on dialog close | ✅ Implemented | Radix native behaviour; Suspense wrapper (C1 fix) preserves SSR |
| **FR-029(h)** — Mobile centered ≥sm, full-width <sm | ✅ Implemented | shadcn `<AlertDialogContent>` defaults; `max-w-lg` |
| **AS1** — Card full refund → CN + invoice='credited' + Payment='refunded' + audit chain | ✅ Tested | `issue-refund.test.ts` happy-path + multi-partial integration test |
| **AS2** — PromptPay refund routes back to bank; CN flow identical | ✅ Tested | I7 fix added dedicated `method: 'promptpay'` happy-path test |
| **AS3** — Processor failure → no CN + bilingual error + `refund_failed` audit | ✅ Tested | Unit test `processor_unavailable retryable` + multi-partial integration |
| **AS4** — Manager 403 + Issue refund button hidden | ✅ Tested | Page conditional render (`isAdmin`) + route `requireAdminContext('refund', 'write')` + I8 contract test pins 403 body |
| **AS5** — Partial refund 3,500 of 53,500 → partially_refunded + partially_credited + remaining=50,000 | ✅ Tested | Unit `partial refund` test + integration `multi-partial accumulation` |
| **AS6** — Over-limit pre-flight reject, no Stripe call, no audit | ✅ Tested | I6 fix added `expect(audit.emit).not.toHaveBeenCalled()` assertion |

**Coverage**: **18 of 18 in-scope FRs implemented (100%)** + 6/6 acceptance scenarios tested (100%)
FR-011a is correctly out of scope (Phase 9 T130) — tracked, not missing.

---

## Test Coverage Assessment

| Area | Tests Exist? | Coverage | Gaps |
|------|-------------|----------|------|
| `domain/refund.ts` (state machine + completeness invariant) | ✅ | **100%** line+branch+func | None |
| `domain/value-objects/refundable-amount.ts` | ✅ | **100%** | None |
| `domain/invariants/refund-not-exceeding-remainder.ts` | ✅ | **100%** | None |
| `application/use-cases/issue-refund.ts` (security-critical, Constitution Principle II) | ✅ | **100% branch** | C2 Phase B catch path covered indirectly via DB outage simulation in error tests |
| `infrastructure/repos/drizzle-refunds-repo.ts` | ✅ live Neon | 4/4 methods exercised | `findByProcessorRefundId` hit-path (T130 webhook scope) |
| `POST /api/refunds/initiate` route | ✅ | 20 contract tests | All 8 use-case error codes mapped |
| Multi-partial accumulation + concurrent race | ✅ live Neon | 2 tests | None |
| E2E refund UI (chromium) | ✅ deterministic | 5 tests | Submit happy path requires live Stripe PI; covered by unit + integration |
| AS2 PromptPay (I7 fix) | ✅ | 1 unit test | Sufficient — method-agnostic flow proven |
| AS6 no-audit-on-rejection (I6 fix) | ✅ | Asserts `audit.emit.not.toHaveBeenCalled` | None |
| AS4 manager 403 (I8 fix) | ✅ | 403 status + body shape pinned | None |

**Test totals**: 393 unit+contract + 7 integration (live Neon) + 5 E2E (chromium, deterministic) + 1289 i18n keys × 3 locales

---

## Constitution Alignment

| Principle | Status | Evidence |
|-----------|--------|----------|
| **I — Tenant Isolation (NON-NEGOTIABLE)** | ✅ | RLS+FORCE on `refunds` (migration 0034) + `paymentsRepo.lockForUpdate(tenantId)` + cross-tenant integration test in `drizzle-refunds-repo.test.ts` |
| **II — Test-First (NON-NEGOTIABLE)** | ✅ | T101 RED contract test authored before T108 use-case; 100% branch coverage on `issue-refund.ts` (security-critical path); failing-test-first discipline followed across 9 commits |
| **III — Clean Architecture (NON-NEGOTIABLE)** | ✅ | Domain pure (no framework imports); Application uses ports only; Infrastructure provides adapters; barrel re-exports verified; `_state-machine.ts` and `branded-ulid-id.ts` extract shared scaffolding without leaking layer boundaries |
| **IV — PCI DSS (NON-NEGOTIABLE)** | ✅ | F5 refund flow handles only Stripe refund_id (`re_…`) — no PAN/CVV/track data; `buildUseCaseErrorTelemetry` + `summariseF4Error` scrub gateway error messages to bounded enum literals; route response body verified clean of `processorReason` raw text |
| **V — i18n** | ✅ | EN/TH/SV parity (1289 keys × 3 locales pass `pnpm check:i18n`); spec literal alignment for FR-029(a) titles (E1 fix) |
| **VI — Inclusive UX (WCAG 2.1 AA)** | ✅ | `aria-required="true"`, `aria-invalid`, `role="alert"`, `aria-describedby` linking, `motion-safe:animate-spin` for reduced-motion (I9, I10, UX polish fixes) |
| **VII — Perf & Observability** | ✅ | `getRefundContextForUpdate` 3→1 SELECT under FOR UPDATE; React 19 `cache()` dedups page+Suspense'd timeline; `Promise.all()` parallelises palette fetch; structured pino logs at all warn/error sites |
| **VIII — Reliability** | ✅ | Two-phase tx with explicit failure-recovery (Stripe-fail, F4-fail, Phase-B-fail all flow through `finaliseFailedRefund`); idempotency-key `rfnd-{paymentId}-{seq}` collision-safe; T130a tracks last-resort double-fault recovery |
| **IX — Code Quality** | ✅ | TypeScript strict + `noUncheckedIndexedAccess`; `IssueRefundError['code']` exhaustive switch; bigint boundary correct; comment cleanup pass removed task-marker rot |
| **X — Simplicity** | ✅ | Two `/simplify` rounds applied (R1 branded-ulid extraction, R2 state-machine factory, Q2 finaliseFailedRefund DRY, E1 cache() dedup, E2 Promise.all, E3 SELECT consolidation); helpers earn their keep with ≥2 callers each |

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Files reviewed | 55 (10 Phase 6 commits) |
| 🔴 Blockers | **0** |
| 🟡 Warnings | **0** |
| 🟢 Suggestions | **5** |
| Spec coverage (in-scope FRs) | **18 / 18 (100%)** |
| Acceptance scenario coverage | **6 / 6 (100%)** |
| Branch coverage on security-critical paths | **100%** |
| Constitution NON-NEGOTIABLE compliance | **4 / 4** |
| Constitution Core principles | **6 / 6** |
| Test pass rate | 393/393 unit+contract + 7/7 integration + 5/5 E2E |
| i18n parity | 1289 × 3 locales |

---

## Recommended Actions

### Must Fix (Blockers)

**None.**

### Should Fix (Warnings)

**None.**

### Nice to Fix (Suggestions — defer to Phase 9 polish or post-MVP)

1. **R001** — batch `seedPlaceholderCN` into single multi-row INSERT (~200ms suite-wide saving)
2. **R002** — bigint-string symmetry on route input (only matters when 20M THB cap raises)
3. **R003** — refactor `<AlertDialogTrigger>` chain to `asChild + <Button>` primitive across admin (project-wide consistency, not Phase 6 specific)
4. **R004** — accept FR-029(c) validation-timing divergence (functionally equivalent) OR amend spec wording
5. **R005** — implement T130a stale-pending-refund recovery sweep alongside T130 webhook handler (Phase 9, ~150 LOC)

---

## Verdict

✅ **APPROVED** — Ready for `/speckit.ship`

F5 Phase 6 implements all in-scope spec requirements with comprehensive test coverage, full Constitution compliance, and zero blocking or warning-level issues. The implementation underwent rigorous self-review (6 specialist agents) and follow-up fix application (18 findings addressed). Five non-blocking suggestions are tracked for Phase 9 polish or post-MVP consideration.

**Security-sensitive sign-off**: refund flow touches real money + append-only F4 tax documents. Per Constitution Principle IV (PCI DSS NON-NEGOTIABLE) + Development Workflow rule "≥2 reviewers for auth/payment/PII surfaces, one signs security checklist" — this AI-generated review counts as one staff-perspective approval. Recommend a human maintainer co-sign before `/speckit.ship` per the solo-maintainer substitute clause in Constitution v1.4.0 § Governance.

---

*Generated by `/speckit.staff-review.run` — Staff-level code review for spec-driven development.*
