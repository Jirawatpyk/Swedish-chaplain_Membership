# Code Review Report: F5 Online Payment — Phase 2 (Foundational)

**Reviewer**: AI Agent (Staff Engineer Perspective)
**Date**: 2026-04-23
**Feature**: [spec.md](../spec.md) — F5 Online Payment (Stripe + PromptPay)
**Branch**: `009-online-payment` (12 commits ahead of `main`)
**Commits reviewed**: `4d9fe5c…95e9f44` (Phase 1 + Phase 2 + 2 review-fix rounds)
**Verdict**: ⚠️ **APPROVED WITH CONDITIONS**

---

## Executive Summary

Phase 2 (Foundational) ships the multi-tenant database schema, F4→F5 bridge use-cases, Stripe SDK infrastructure, PCI-aware logger, module barrel + ESLint guards, i18n decline-reason catalogue, and per-request CSP allowlist. All 32 Phase 2 tasks are complete and have been through **two rounds of specialist review** (9 specialist agents total) + a verify pass. All 6 Critical + 13 Important findings have been remediated in commits `bda54b2` + `95e9f44`.

**Strengths**: Clean Architecture boundaries hold under ESLint enforcement (stripe/@stripe blocked at Application layer); tenant isolation has 2-layer defense (app-layer `runInTenant` + DB-layer RLS+FORCE) with 25/25 coverage tests green; PAN redaction has 9-level deep recursion + pretty-print normalization covering space/hyphen-formatted variants; Stripe client is lazily instantiated with bounded resilience (maxNetworkRetries=3, timeout=10s); kill-switch 503 body schema is normalized across F3/F4/F5/READ_ONLY with machine-readable `{error, message, retryAfterSeconds, supportUrl}`; migration 0041 anchors the `uuid REFERENCES users(id)` FK via a seeded system actor with full audit trail.

**Primary concerns for Phase 3**: (1) The `not_payable` branch unit coverage, while now present, still delegates to a mocked `getInvoice` dep — a property-test of the Money VO's zero/negative invariants would add confidence. (2) Phase 3 UI work needs the Stripe Elements shimmer contract + PromptPay QR countdown aria-live wiring locked in before the first route handler lands. (3) Doc-sync landed late in Phase 2 (15→16 audit events); establishing an automated check ("audit event count in prose vs enum count") would prevent recurrence.

**Ship condition for Phase 2**: No blockers. The 2 suggestions below are non-blocking Phase-3 groundwork notes.

---

## Review Findings

| ID | Severity | File | Line(s) | Category | Finding | Recommendation |
|----|----------|------|---------|----------|---------|----------------|
| R001 | 🟢 Suggestion | `src/modules/invoicing/application/use-cases/get-invoice-for-payment.ts` | 91 | Test Quality | `not_payable` branch relies on mocked `getInvoice` dep; no fast-check property test covers the Money-zero invariant end-to-end | Add a property test in Phase 3 asserting `totalSatang === 0n` never produces `ok` — fast-check is already in devDeps |
| R002 | 🟢 Suggestion | `specs/009-online-payment/*.md` + `scripts/check-i18n-coverage.ts` | n/a | Spec Compliance | Audit-event count (16 vs stale "15") drifted across 4 files before doc-sync caught it | Add `scripts/check-audit-event-count.ts` that parses `CREATE TYPE audit_event_type` + enumerates occurrences of "N F5 audit events" in specs/ and fails if they disagree |
| R003 | 🟢 Suggestion | Phase 3 (not yet written) | n/a | UX / Architecture | Stripe Elements shimmer contract + PromptPay QR expiry aria-live are locked-in-during-Phase-3 items flagged by UX reviewer | Add an explicit task T041a in Phase 3 to encode the `Skeleton` wrapper + `aria-live="polite"` countdown before any payment route-handler code lands |

**Categories covered**: Correctness, Security, Performance, Spec Compliance, Error Handling, Test Quality, Architecture

---

## Spec Coverage Matrix (Phase 2 scope)

Phase 2 is foundational — FR-001/002/004/011/012/018 (payment initiation + refund issuance) are DEFERRED to Phases 3–8. The matrix below lists only FRs that Phase 2 is expected to land implementation evidence for.

| Requirement | Status | Implementation Notes |
|-------------|--------|---------------------|
| FR-003 (payment row shape) | ✅ Implemented | `drizzle/migrations/0033_create_payments.sql` + `src/modules/payments/infrastructure/schema.ts` — 15 cols + 13 CHECK + partial UNIQUE per invoice |
| FR-005 (no PAN/CVV persistence) | ✅ Implemented | `src/lib/logger.ts` — REDACT_PATHS (24 new paths) + PAN_REGEX (Luhn schemes) + `redactPanValues` recursive depth-9 + pino `formatters.log` hook |
| FR-007 (webhook HTTPS + signature) | 🟡 Phase 3 | Infra in place (`STRIPE_WEBHOOK_SECRET` env + `getStripeClient().webhooks`), route handler deferred to T062+ |
| FR-008 (processor_event idempotency) | ✅ Implemented | `drizzle/migrations/0036_create_processor_events.sql` — append-only + 4-policy RLS + DELETE forbidden |
| FR-010 (env segregation) | ✅ Implemented | `src/lib/env.ts` cross-field assertion — `STRIPE_LIVE_MODE` ↔ `sk_test_`/`sk_live_` prefix + throws in non-prod + 5 unit tests |
| FR-015 (tenant_payment_settings) | ✅ Implemented | `drizzle/migrations/0035` + SweCham seed `0037` + RLS+FORCE |
| FR-017 (2-layer tenant isolation) | ✅ Implemented | `runInTenant` reused unchanged + RLS+FORCE on all 4 F5 tables + 25/25 rls-coverage tests |
| FR-019 (TLS + HSTS) | ✅ Implemented | `src/proxy.ts` HSTS_VALUE header + platform-level TLS |
| FR-020 (16 audit events) | ✅ Implemented | `drizzle/migrations/0040_audit_log_f5_extension.sql` — 16 idempotent `DO $$ ALTER TYPE ADD VALUE $$` blocks |
| FR-026 (API version pinning) | ✅ Implemented | `STRIPE_API_VERSION` env + `getStripeApiVersion()` lazy getter + `stripe-client.ts` cast |
| FR-001, 002, 004, 006, 009, 011–014, 016, 018, 021–025 | ⏸ Phase 3+ | User stories US1–US6 not in Phase 2 scope |

**Phase 2 Coverage**: 10/10 in-scope FRs implemented (100%). 15 FRs correctly deferred to Phase 3+ per tasks.md phase structure.

---

## Test Coverage Assessment

| Area | Tests Exist? | Coverage | Gaps |
|------|-------------|----------|------|
| F4→F5 bridge use-cases (markPaid, issueCreditNote, getInvoiceForPayment) | ✅ | 6/6 integration (live Neon) + 4 unit for not_payable branch | none material |
| Payments RBAC matrix | ✅ | 12/12 unit (4 resources × 3 roles + denial assertions + fail-closed) | none |
| Stripe client singleton + contract | ✅ | 3/3 unit (singleton, reset, surface) | none |
| Stripe API-version getter | ✅ | 2/2 unit (mockability + getter-not-const) | none |
| Logger PAN redaction | ✅ | 59/59 unit (card schemes + CVV variants + secrets + Stripe-Sig casings + nested + BigInt + pretty-print normalization) | none |
| Proxy CSP Stripe allowlist | ✅ | 16/16 unit (4 F5 routes × 3 directives + 9 non-F5 excludes + dev/prod + directive-specific hooks.stripe.com) | none |
| F3/F4/F5 kill-switches | ✅ | 30/30 integration (7 F3 + 10 readonly + 13 F5) | none |
| RLS+FORCE coverage | ✅ | 25/25 integration (F1–F5 tables, both qual + with_check, 4-policy processor_events) | none |
| env.ts cross-field assertions | ✅ | 5/5 unit (happy + 4 throw branches, using `vi.stubEnv`) | none |
| Barrel exports (F4 + F5 bridge) | ✅ | 2/2 unit (RED-first TDD) | none |
| F5 audit event emit paths | ⏸ | Deferred | Phase 3 — `tests/integration/payments/audit-coverage.test.ts` scheduled |
| Payment route handlers (initiate/confirm/refund/webhook) | ⏸ | Deferred | Phase 3–6 |
| E2E: payment-email-deep-link (signed-in/signed-out returnUrl) | ⚠️ Partial | Render-layer covered; click-through `test.fixme` pending F4 e2e seeder | Follow same `test.fixme` pattern as F4 `invoice-pay.spec.ts` |

**Overall**: Phase 2 test coverage is comprehensive for in-scope surfaces. No untested security-critical paths. Constitution Principle II (TDD) satisfied via RED-first barrel test + 100% branch on redaction/RBAC/CSP.

---

## Architecture & Spec Compliance Cross-Check

**Constitution v1.4.0 NON-NEGOTIABLE principles (4/4 satisfied)**:
- **I Tenant Isolation**: 2-layer (app-layer `runInTenant` + DB-layer RLS+FORCE) with cross-tenant probe audit events + 25/25 coverage tests
- **II TDD**: RED-first barrel test (T011); 8 new security-critical tests added during review fix rounds
- **III Clean Architecture**: `src/modules/payments/{domain,application,infrastructure}` split + public barrel + ESLint `no-restricted-imports` blocking stripe + @stripe subpaths at Application + inter-module barrel-guard + bidirectional F4↔members port block
- **IV PCI DSS SAQ-A**: No PAN/CVV in logs (verified by 59 redaction tests incl. pretty-print variants); card capture delegated to Stripe Elements (CSP scoped to `/portal/invoices/*` + `/admin/invoices/*`); secret key env-var-only; `sk_test_↔STRIPE_LIVE_MODE=false` cross-field boot assertion; env segregation audit event registered

**Core Principles (Phase 2 relevance)**:
- **V i18n (EN+TH+SV)**: 1142 keys × 3 locales + 21 decline-reasons sub-catalogue with parity check. TH `incorrect_cvc` adds SR-friendly parenthetical; SV `currency_not_supported` expands THB.
- **VII Perf & Observability**: Stripe SDK bounded (10s timeout × 3 retries) + pino structured logs for proxy tenant-resolver failures
- **VIII Reliability**: Result<T,E> discipline on all bridge wrappers; fail-closed RBAC; idempotent migrations (ON CONFLICT DO NOTHING in 0037, 0041; DO$$EXCEPTION in 0040)
- **IX Code Quality**: TypeScript strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes; lint + typecheck clean
- **X Simplicity**: Phase 2 does NOT ship speculative Phase 3+ code; `src/modules/payments/{domain,application}/` contains only skeleton + minimum viable primitives (RBAC policy, system-actor constant)

**Architecture deviations** (all documented in `plan.md` § Complexity Tracking, 8 entries including the newly-backfilled migration renumber):
- Webhook Node.js runtime (not Edge) — Stripe signature verification requires raw body
- Stripe API version pinning via env — prevents silent Stripe version drift
- Payment-initiation p95 < 1.2s (not < 400ms) — Stripe RTT Singapore→Ireland
- Webhook pre-tenant RLS bypass — narrow window for processor_events insert
- Stripe external dependency — Constitution Principle IV mandates certified processor
- F5 consumes F4 via barrel extensions landed on F5 branch — unidirectional
- Solo-maintainer review-gate substitute — 5-stack automated review
- **Migration numbering +1 shift** (newly documented) — 0032 taken by F3 hotfix

**No scope creep detected**. All implementation maps to a task ID. Zero functionality implemented outside spec.

---

## Metrics Summary

| Metric | Value |
|--------|-------|
| Commits reviewed | 12 (main…HEAD) |
| Files changed (cumulative) | 86 files, +8932/-110 |
| Phase 2 tasks | 32/32 ✅ |
| 🔴 Blockers | **0** |
| 🟡 Warnings | **0** |
| 🟢 Suggestions | 3 |
| Critical issues resolved (from 2 review rounds) | 6/6 ✅ |
| Important issues resolved (from 2 review rounds) | 13/13 ✅ |
| Spec coverage (Phase 2 in-scope FRs) | 10/10 (100%) |
| Unit tests | 382/382 passing (35 files) |
| Integration tests (affected) | 61/61 passing (5 files) — middleware + processor-bridge + RLS |
| Auth unit tests (regression) | 243/243 passing |
| i18n keys | 1142 × 3 locales + 21 decline-reasons × 3 |
| Migrations applied to live Neon | 0033–0041 (9 migrations) |

---

## Recommended Actions

### Must Fix (Blockers)

*None.*

### Should Fix (Warnings)

*None.*

### Nice to Fix (Suggestions) — defer to Phase 3 or next doc-sync

1. **R001** — Add a fast-check property test for `getInvoiceForPayment` asserting `totalSatang ≤ 0n ⟹ err('not_payable')`. File: new `tests/unit/invoicing/get-invoice-for-payment.prop.test.ts`. Estimated effort: ~30 lines.

2. **R002** — Add `scripts/check-audit-event-count.ts` to CI chain to prevent prose-vs-enum drift on audit event counts. Parse enum values from `drizzle/migrations/*.sql`; grep specs/*.md for pattern `\b(\d+) (?:F5 |)audit event`; fail if counts disagree. Estimated effort: ~60 lines + CI wiring.

3. **R003** — Before any Phase 3 payment route handler lands, add a task locking in: (a) `<Skeleton>` wrapper + Stripe Elements `ready` event for shimmer; (b) `aria-live="polite"` countdown + T-2min "Refresh QR" toast for PromptPay. File the task as T041a in `specs/009-online-payment/tasks.md`.

---

## Verdict

⚠️ **APPROVED WITH CONDITIONS**

No blockers or warnings. Phase 2 is ship-ready for the Review gate. The 3 suggestions above are Phase 3 groundwork notes and are non-blocking.

### Conditions
- 3 suggestions tracked for Phase 3 scope (not required for Phase 2 ship)
- Solo-maintainer review-gate substitute applied per plan.md § Complexity Tracking: 5-stack automated review (`/speckit.review` ×2, `/speckit.staff-review` ×1, `pci-saqa-guardian` ×1, `drizzle-migration-reviewer` ×1, `/speckit.verify` ×1) has been executed

### Next Steps
- **Commit** (already done: `95e9f44`)
- **Proceed to Phase 3** (`/speckit.implement Phase 3`) — US1 Member pays via card (P1 MVP)
- **OR tag Phase 2 as ready for merge** — if the solo-maintainer substitute is acceptable per Principle IX v1.3.0

---

*Generated by `/speckit.staff-review` — Staff-level code review for spec-driven development. Synthesized from 2 rounds of specialist review (9 agents) + `/speckit.verify` pass on commit `95e9f44`.*
