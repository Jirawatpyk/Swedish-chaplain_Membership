# Importer R4 (focused on R3 diff) — findings

verifiedCount=17 sweep=3 CONFIRMED=13 PLAUSIBLE=4

#1 [CONFIRMED] MEDIUM import-members.ts:179 (idempotency-correctness)
  SUMMARY: A genuinely-new, distinct company whose contact email(s) all collide with ACTIVE contacts of OTHER existing members is silently mislabeled as an idempotent re-run (skippedExistingMembers) and dropped with NO row flag, so the operator gets zero signal that a real member was skipped.
  SCENARIO: DB already has member Beta with an active contact email shared@x.com (e.g. a shared secretary/consultant address). The import batch contains a genuinely-new company 'Acme New' whose only contact is shared@x.com. For Acme New: the tenant-wide email query returns shared@x.com as active, so activeEmails={shared@x.com}, softEmails={}, hasNewEmail=false. The branch at activeEmails.size>0 && !hasNewEmail fires => skippedExistingMembers+=1; continue; with NO push to partialOverlapRows/primaryCollisionRows. Acme New is never created and is counted as an 'already-imported member'. Unlike the partial-ov

#2 [CONFIRMED] MEDIUM import-members.ts:179 (idempotency-correctness)
  SUMMARY: When an existing active member's import group also lists an email that is SOFT-DELETED in the DB (and nothing genuinely new), the soft-deleted contact the operator explicitly listed is silently ignored with NO skippedSoftDeletedContacts increment and no flag — inconsistent with the new-member path which counts it.
  SCENARIO: Import group for an existing company has contact A (active in DB) and contact B (soft-deleted in DB), no genuinely-new email. activeEmails={A}, softEmails={B}, hasNewEmail=false => skippedExistingMembers+=1; continue. Contact B, which the operator explicitly included in the workbook expecting it to be added, is dropped and NOT reactivated (correct per policy) but is NOT counted in skippedSoftDeletedContacts and not flagged — whereas on the NEW-member path the identical soft-deleted secondary increments skippedSoftDeletedContacts. The operator therefore has no record that a requested contact wa

#3 [CONFIRMED] MEDIUM validate.ts:210 (warn-logic)
  SUMMARY: The merge-mismatch warn loop runs even when the head row's tier/country failed to resolve, emitting misleading 'member_field_mismatch' warnings on a member that is already being dropped for a head-row error — and treating every resolvable sibling as a mismatch because headPlanId/headCountry is forced to null.
  SCENARIO: A company group where head row has tier='Narnia' (unmapped) and a sibling row has tier='Premium' (resolves). Line 165 errors head tier/unmapped, so headPlanId=null (line 207). The loop at 210 sees norm('Premium')!=norm('Narnia') -> differ true; rt.ok=true but headPlanId===null so equivalent=false -> warn(sibling,'tier','member_field_mismatch') at line 220. The whole member is then dropped (memberHasError, line 295). The report now tells the operator the sibling is a 'wrongly-merged distinct company' to split, when the real and only actionable problem is the head's garbage tier. Same for head c

#4 [CONFIRMED] MEDIUM import-members-validate.test.ts:179 (test-adequacy)
  SUMMARY: The R3 under-warn fix is only tested for the COUNTRY field with an unresolvable sibling ('Narnia'); the symmetric TIER under-warn case (sibling tier is unresolvable-but-different, e.g. 'Bronze') is never asserted, so a regression that re-dropped the unresolvable-tier warning would pass.
  SCENARIO: Head tier 'Premium' (resolves), sibling tier 'Bronze' (unresolvable, non-empty, differs). validate.ts line 218-220: rt = resolve('Bronze') is !ok, so equivalent=false, so it SHOULD warn member_field_mismatch on tier. The positive tier test (line 170) uses 'Thai Alumni' which RESOLVES to a different plan — it proves the resolved-diff branch but NOT the unresolvable-tier branch. Only the country unresolvable branch is tested; the tier unresolvable branch is identical code but separately untested.

#5 [CONFIRMED] MEDIUM import-members.test.ts:127 (test-adequacy)
  SUMMARY: The partial-overlap test only covers an overlap with a SINGLE pre-existing active email; a member overlapping an active email AND also carrying a soft-deleted email plus a new one (mixed active+soft+new) is untested, leaving the hasNewEmail computation under the combined sets unverified.
  SCENARIO: Member contacts = [emailA (active), emailSoft (soft-deleted), emailNew (new)]. activeEmails={A}, softEmails={soft}, hasNewEmail=true (emailNew is neither). Expected: skippedPartialOverlapMembers:1. If hasNewEmail wrongly treated a soft-deleted email as 'new' or vice-versa in a future edit, the partition could misroute this mixed case to skippedExistingMembers (under-flagging a genuine overlap). The test at line 127 has no soft email in the mix, so it cannot catch a soft-vs-new partition bug.

#6 [CONFIRMED] LOW report.ts:17 (idempotency-correctness)
  SUMMARY: The skippedExistingMembers docstring still says 'EVERY contact email already exists ACTIVE (idempotent re-run)', but after the R3 change the counter also fires when some emails are merely SOFT-deleted (not active), so the operator-facing definition is now inaccurate.
  SCENARIO: A member with one active contact A and one soft-deleted contact B (hasNewEmail=false) increments skippedExistingMembers, yet NOT every contact email exists active (B is soft-deleted). The report/docstring tells the operator this bucket means every email is active, masking that soft-deleted contacts were involved and silently skipped — misleading when reconciling the import outcome.

#7 [CONFIRMED] LOW import-members.ts:175 (boundary-and-query)
  SUMMARY: A workbook member whose contact emails are all ACTIVE but split across TWO different existing members is silently counted as `skippedExistingMembers` ("already imported"), with no row recorded, even though no single existing member actually matches it.
  SCENARIO: DB has member M1 with active contact a@x.com and member M2 with active contact b@x.com. The workbook has a company "Acme" with contacts a@x.com + b@x.com (and no new email). commitMembers builds activeEmails={a,b}, hasNewEmail=false, so it takes the `!hasNewEmail` branch -> skippedExistingMembers++ and `continue`. Acme is reported as an idempotent already-imported member, but it was never actually imported as a single member — the operator gets no partialOverlapRows/no flag and cannot tell the data is fragmented across M1/M2. Pre-existing email-dedup limitation that the R3 branch rewrite re-ce

#8 [CONFIRMED] LOW validate.ts:222 (boundary-and-query)
  SUMMARY: When the GROUP HEAD row's country (or tier) is blank/unresolvable, a sibling row that holds the CORRECT resolvable value is flagged with `member_field_mismatch` on the SIBLING row, pointing the operator at the good row instead of the bad head row.
  SCENARIO: Workbook group "Mix Co": head row (row 2) country='' (blank -> head errored, headCountry=null); sibling row 3 country='Sweden'. The R3 condition `r.country.trim().length>0 && norm('sweden')!==norm('')` is true; resolve('Sweden')='SE'; equivalent = rc.ok && (headCountry!==null) && ... = false (headCountry is null) -> warns member_field_mismatch on row 3. The member is already errored via the head's blank country, so the warning is cosmetic, but it accuses the row that actually has the right data. Low impact because the member is dropped from commit anyway.

#9 [CONFIRMED] LOW validate.ts:219 (warn-logic)
  SUMMARY: Even when the head tier/country DOES resolve but the head row separately errored on a different field (e.g. tax_id), or when both head and sibling carry unresolvable-different garbage, the loop still over-warns about a 'wrongly-merged company' for a member that is being dropped, conflating two unrelated problems in the report.
  SCENARIO: Head row tier='Premium'/country='TH' resolve fine but head tax_id fails the Thai checksum (company scope) -> head errors at line 180, member dropped. Sibling row has tier='Bronze' (unmapped). Loop fires: norm differs, rt.resolve('Bronze') ok=false -> equivalent=false -> warn member_field_mismatch on the sibling. Operator sees a tax_id error AND a merge-mismatch warning for the same single dropped member group, the latter implying they should split the company, which is not the fix. The merge-warn should be suppressed (or downgraded) when the group will not be committed.

#10 [CONFIRMED] LOW import-members.test.ts:171 (test-adequacy)
  SUMMARY: The soft-secondary test never asserts the membersCreated/contactsCreated COUNT for a member with MULTIPLE secondaries where only some are soft-deleted — only a 2-contact member (1 new primary + 1 soft secondary → contactsCreated:1) is covered.
  SCENARIO: Import a member with primary P (new), secondary S1 (new), secondary S2 (soft-deleted). Expected membersCreated:1, contactsCreated:2, skippedSoftDeletedContacts:1. The contact loop (lines 234-237) inserting the surviving secondary while skipping the soft one is only ever exercised with a single new contact; an off-by-one in the per-contact skip/insert partition (e.g. skipping the wrong contact, or double-counting) would not be caught because contactsCreated is only ever asserted as 1 or 2 on trivial shapes.

#11 [CONFIRMED] LOW import-members-validate.test.ts:148 (test-adequacy)
  SUMMARY: No test exercises the member_field_mismatch warn loop on a sibling row of a member that is itself EXCLUDED by a head-row error — the warn loop (validate.ts 210-227) still runs and pushes warnings for a member that never reaches the members[] array.
  SCENARIO: Group head row has an unmapped tier ('Bronze') → member errored + excluded, but a sibling row's country differs and is unresolvable ('Narnia'). validateRows still emits a member_field_mismatch warning on the sibling even though the whole company is dropped, producing operator-confusing warnings on a non-imported member. No test asserts whether warnings on excluded members are intended or suppressed; the behavior is unspecified and unverified.

#12 [CONFIRMED] LOW import-members-report.test.ts:88 (test-adequacy)
  SUMMARY: The committed-report render test asserts the members/contacts/partial-overlap/primary-collision lines but never asserts the 'Skipped:' summary line carrying skippedExistingMembers / skippedSoftDeletedContacts / skippedPartialOverlapMembers counts (report.ts 89-95).
  SCENARIO: Set skippedSoftDeletedContacts:1 and skippedExistingMembers:2 in the committed outcome; renderReportText emits 'Skipped: 2 already-imported members, 1 partial-overlap members, 1 soft-deleted contacts, 1 primary-collision members'. The test never checks that line, so a regression dropping or mislabeling any of those four counts (e.g. printing skippedExistingMembers where soft-deleted should go) would pass silently.

#13 [CONFIRMED] LOW import-members.test.ts:171 (test-adequacy)
  SUMMARY: No test asserts partialOverlapRows / primaryCollisionRows stay EMPTY on the happy soft-secondary insert path; the soft-secondary test asserts skip counts but not that the two row-list arrays are empty after a successful insert.
  SCENARIO: On the first soft-secondary import (line 189) the member is inserted (membersCreated:1) and neither the partial-overlap nor primary-collision branch fires, so both partialOverlapRows and primaryCollisionRows must be []. The test asserts skippedSoftDeletedContacts:1 but not that the row arrays are empty; a regression that erroneously pushed the head row into primaryCollisionRows while still inserting would not be caught here (only the re-run at line 201 checks partialOverlapRows length, on a different code path).

#14 [PLAUSIBLE] MEDIUM import-members.test.ts:171 (test-adequacy)
  SUMMARY: No test covers a NEW member whose EVERY email is soft-deleted (primary + all secondaries soft) — only the single-soft-primary (line 148) and single-soft-secondary+new-primary (line 171) cases are tested.
  SCENARIO: Import a member with primary email pSoft (soft-deleted) and secondary email sSoft (soft-deleted), zero new emails. commitMembers: activeEmails.size===0 (skips the active block), then primaryEmail is in softEmails so it hits skippedPrimaryCollisionMembers and never reaches the contact loop. The skippedSoftDeletedContacts counting + the 'member always keeps its primary' invariant are never exercised on this all-soft path; a regression that mis-routed it (e.g. inserting a member with no active contacts) would not be caught.

#15 [PLAUSIBLE] MEDIUM import-members.test.ts:148 (test-adequacy)
  SUMMARY: The primary-collision path has no re-run/idempotency test, unlike the soft-secondary path (line 199) — a member skipped via skippedPrimaryCollisionMembers is never re-run to prove the second pass behaves identically (still collision, not partial-overlap or insert).
  SCENARIO: After the line-148 import skips the member (skippedPrimaryCollisionMembers:1, nothing written), re-running the same member should again yield skippedPrimaryCollisionMembers:1 because the primary email is still soft-deleted and no active contact was created. No test asserts the second pass; if the branch ordering regressed so that a still-soft primary fell through to insert (creating a member whose primary can't be inserted, or a duplicate), the re-run case would expose it but is untested.

#16 [PLAUSIBLE] LOW import-members.ts:169 (boundary-and-query)
  SUMMARY: The collapsed `existing` query is fetched and partitioned per-member inside the loop with no LIMIT and no early-exit, so for a member with >1 email it returns every active+soft contact row tenant-wide for those emails; correct but the per-member round-trip + unbounded result set was the thing R3 set out to reduce and only halved.
  SCENARIO: Not a correctness bug for ~131 members, but if an email has many historical soft-deleted rows (legal — the partial unique index only constrains active rows), `existing` returns all of them each loop iteration; the Set dedups so semantics stay correct, but the query is O(rows-per-email) per member rather than a single batched lookup outside the loop. No wrong outcome, only avoidable per-member DB work.

#17 [PLAUSIBLE] LOW import-members.ts:195 (boundary-and-query)
  SUMMARY: The new-member primary-collision guard relies on validate always normalizing exactly one primary; if a ValidatedMember ever reached commit with zero `isPrimary` contacts, `primary?.email` is undefined, the soft-collision guard is skipped, and the member is inserted with all contacts is_primary=false (a member with no primary).
  SCENARIO: validateRows currently guarantees a primary (defaults the first contact + warns), so this is defensive-only today. But commitMembers is exported and integration-tested directly with hand-built ValidatedMember objects; a future caller (or test) passing contacts all with isPrimary:false would create a member that violates the intended one-active-primary invariant without any DB error (contacts_one_primary_per_member allows 0 primaries). No guard/assert in commitMembers itself.

SWEEP#1 LOW import-members.ts:109
  SUMMARY: The commitMembers function docstring claims soft-deleted-contact skip+count is unconditional, but after R3 it only happens on the new-member path — the existing-active-member path silently drops an operator-listed soft contact with no count, so this header comment now misdescribes the code (a second 'comment lies' site distinct from the already-found report.ts:17).
  SCENARIO: Operator re-imports an existing active member whose group also lists an email that is soft-deleted in the DB and nothing genuinely new; reader of commitMembers trusts the docstring ('skipped+counted') and assumes skippedSoftDeletedContacts incremented, but the activeEmails.size>0 / !hasNewEmail branch returns skippedExistingMembers with zero soft-contact accounting.

SWEEP#2 LOW import-members.ts:196
  SUMMARY: The new-member primary-collision guard uses `m.contacts.find(c => c.isPrimary)` and lower-cases its email, but the soft-deleted-secondary skip loop at 234-235 re-derives the primary implicitly from `c.isPrimary`; if validate ever emitted a member whose isPrimary contact is ALSO listed as a (different-cased duplicate) secondary, the guard and loop could disagree — but more concretely, when primary is undefined the guard at 197 short-circuits (primaryEmail falsy) and the member is inserted, with the loop assigning is_primary from each contact's own flag (all false), yielding a member with zero primary.
  SCENARIO: A ValidatedMember reaches commit with zero isPrimary contacts (validate's none_marked_defaulting_first warn path failed to set one, or a future refactor): primaryEmail is undefined, the softEmails guard is skipped, and the insert loop calls insertContact(..., c.isPrimary=false) for every contact, creating a member with no primary contact (violates the intended one-primary invariant, though contacts_one_primary_per_member allows 0).

SWEEP#3 LOW validate.ts:217
  SUMMARY: The sibling merge-mismatch loop compares raw cells with `norm` = trim+lowercase, which (unlike the tier resolver's normalizeTier that strips punctuation/whitespace) treats e.g. 'Premium Corporate' and 'premium-corporate' as raw-different, forcing an extra tierResolver.resolve() round per sibling on values that the resolver itself considers identical, and relying on the resolve step to suppress the false warn — fragile coupling the comment does not call out.
  SCENARIO: Head tier 'Premium', sibling tier 'Premium  Corporate' (double space) or 'premium-corporate': norm differs so the code resolves the sibling and only suppresses the warn because both map to planId 'premium'; if the resolver alias map ever loses the punctuation-stripping (normalizeTier) behavior, these equivalent spellings would start false-warning member_field_mismatch, because the gate is raw-string norm, not the canonical resolver key.