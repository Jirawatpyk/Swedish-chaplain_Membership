counts: {"raw": 15, "deduped": 9, "survived": 8, "sweep_new": 7, "final": 15}

[1] CONFIRMED/bug scripts/import-members.ts:187
    Idempotency regression: a member that was already CORRECTLY and FULLY imported is misflagged as a partial-overlap (skip + 'resolve manually') on every re-run when one of its secondary contacts' emails matches a pre-existing SOFT-DELETED row. First import: emails=[primary P, secondary S] where S pre-exists soft-deleted → activeEmails={}, member created, S skipped as skippedSoftDeletedContacts (correct). Re-run: P is now active, S is still soft-deleted → activeEmails={P}, softEmails={S}; emails.every(active) is false (S not active) so it falls through to the new `if (activeEmails.size > 0)` branch (line 187), incrementing skippedPartialOverlapMembers and pushing headRow into partialOverlapRows. The simplification removed the old existingMemberId branch that re-handled this case silently; now the operator is told to manually resolve a member for which there is nothing to resolve. The integration partial-overlap test only covers a fresh new email (emailB), not the soft-deleted-secondary-on-reimport case, so it misses this.

[2] CONFIRMED/bug scripts/import-members/validate.ts:216
    member_field_mismatch now only warns when a sibling row's country/tier RESOLVES (rc.ok / rt.ok). A sibling whose value is genuinely DIFFERENT but UNRESOLVABLE (e.g. head country 'SE', sibling country 'Narnia'; or sibling tier 'Bronze') now produces NO warning. The pre-fix raw-string compare (a !== b on trim+lowercase) DID warn in this case. Crucially, sibling rows are never given their own country/tier resolution-ERROR either (lines 161-165 only resolution-check the group HEAD), so the discrepancy becomes completely silent: the member is committed with the head's value and the divergent sibling cell is dropped with zero diagnostics. The R2 fix correctly stopped over-warning on EQUIVALENT spellings ('TH' vs 'Thailand'), but the swing to resolved-only comparison introduced an under-warn for the unresolvable-and-different case — exactly the 'two distinct companies merged under a shared display name' signal the warning exists to surface. Same gap applies to the tier branch at line 212 (rt.ok guard).

[3] CONFIRMED/bug scripts/import-members.ts:187
    Idempotency regression: a fully + correctly imported member is misflagged as a partial-overlap ('resolve manually') on every re-run when one of its secondary contact emails matches a pre-existing SOFT-DELETED row. The simplification removed the old existingMemberId branch that handled this silently.

[4] CONFIRMED/bug scripts/import-members/validate.ts:216
    member_field_mismatch under-warns: a sibling row whose country/tier is genuinely DIFFERENT but UNRESOLVABLE produces NO warning (rc.ok/rt.ok guard skips it), AND sibling rows are never independently resolution-error-checked (only the head is, lines 161-165), so the divergence is completely silent. The pre-fix raw-string compare DID warn.

[5] CONFIRMED/bug scripts/import-members/validate.ts:209
    The simplified mismatch loop only validates a sibling's tier/country when it resolves AND differs from the head. A sibling whose tier is unmappable or whose country is unresolvable now produces no signal at all (siblings are never independently error-checked — only the head row is, lines 162-168), narrowing warning coverage versus the pre-fix raw-compare.

[6] CONFIRMED/cleanup scripts/import-members/validate.ts:209
    The simplified member_field_mismatch loop only warns when BOTH the head and sibling tier/country resolve successfully and differ (rt.ok / rc.ok must be true). A sibling row whose tier is unmappable or whose country is unresolvable now produces NO signal at all: sibling rows are never independently validated for resolvability (only the head row's tier/country/regDate are error-checked at lines 162-168; the contacts loop only checks contact-level fields), and the mismatch loop skips them because rt.ok/rc.ok is false. The pre-fix raw trim+lowercase compare DID warn for any differing string including garbage values. So a real merge signal — e.g. head tier 'Premium' (resolves) vs sibling tier 'Premiumm' or a genuinely different unmappable label, or head country 'SE' vs sibling 'Narnia' — is now silently dropped. This is a narrowing of warning coverage introduced by the simplification (the resolved-compare correctly kills the equivalent-spelling false positives the fix targeted, but also kills the unmappable-sibling true positive).

[7] CONFIRMED/cleanup scripts/import-members/report.ts:96
    The two new renderReportText branches that print the partial-overlap and primary-collision row lists (lines 96-101: `partial-overlap rows (resolve manually): …` / `primary-collision rows (resolve manually): …`) have no test coverage. The report unit test (tests/unit/scripts/import-members-report.test.ts) only ever calls renderReportText with committed:null, and the integration test asserts the partialOverlapRows array on the CommitOutcome but never renders it. These branches are the operator-facing payload of R2 finding [13] (give the operator the exact Excel rows to fix); a future regression to the join/format string (e.g. printing PII, or an off-by-one, or dropping the list) would not be caught by any test.

[8] CONFIRMED/cleanup scripts/import-members/validate.ts:214
    The resolved-value mismatch compare silently swallows the warning when a SIBLING value does not resolve (rt.ok false / rc.ok false). Because sibling rows are only ever used as contacts — their country/tier/taxId cells are never independently validated (only head.country/head.tier are, lines 161-165) — an unresolvable or typo'd sibling country/tier produces neither a mismatch warning nor any other error. The pre-fix raw-string compare WOULD have flagged it. So a 'second company merged under the same name' whose country/tier is garbled is now completely invisible to the operator.

[9] CONFIRMED/cleanup scripts/import-members.ts:164
    Two SELECTs per member (one `isNull(removedAt)` for active, one `isNotNull(removedAt)` for soft) hit the DB twice for the same `lower(email) IN (emails)` predicate. They can be collapsed into a single `select({ email, removedAt }) ... WHERE lower(email) IN emails`, then partitioned in JS by `r.removedAt === null`. Halves the per-member round-trips on the import hot path (matters at ~131 members × 2 = 262 queries vs 131). This is the exact two-query→one-query cleanup flagged in the task.

[10] CONFIRMED/cleanup tests/integration/scripts/import-members.test.ts:127
    The NEW-member-with-a-SOFT-DELETED-SECONDARY-contact path is untested. commitMembers line 236 (`if (softEmails.has(c.email.toLowerCase())) { skippedSoftDeletedContacts += 1; continue; }`) and the `skippedSoftDeletedContacts` counter only execute when a NEW member (no active overlap) has a non-primary contact whose email matches a soft-deleted row — the member should be created, the primary inserted, the soft secondary skipped+counted. The integration suite covers soft-deleted PRIMARY (skips the whole member) and partial-overlap, but never the soft secondary branch, so the loop's skip logic and the counter are unexercised. The simplification's claim 'the member always keeps its primary while a soft secondary is skipped' is asserted only in comments.

[11] CONFIRMED/cleanup tests/unit/scripts/import-members-validate.test.ts:148
    The new member_field_mismatch test exercises the country branch (equivalent spelling → no warn; SE vs TH → warn) but never the tier branch's positive-warning case. validate.ts line 210-213 resolves a sibling row's tier and warns when `rt.value.planId !== headPlanId`; both test members use the same tier ('Premium' / 'Thai Alumni'), so the resolved-tier-mismatch warning path is never asserted. Add a sibling row resolving to a different planId (e.g. head 'Premium' vs sibling 'Thai Alumni') to cover it.

[12] CONFIRMED/cleanup scripts/import-members.ts:164
    Two SELECTs per member (one isNull(removedAt) for active, one isNotNull(removedAt) for soft) hit the DB twice for the same lower(email) IN (emails) predicate. Collapse into a single select({email, removedAt}) then partition in JS by removedAt===null — halves per-member round-trips on the import hot path (~262→131 queries for ~131 members).

[13] CONFIRMED/cleanup scripts/import-members/report.ts:96
    The two new renderReportText branches printing partial-overlap and primary-collision row lists (lines 96-101) have zero test coverage. The report unit test only ever calls renderReportText with committed:null, and the integration test asserts the partialOverlapRows array on the outcome but never renders it. These are the operator-facing payload of R2 finding [13] (the exact Excel rows to fix).

[14] CONFIRMED/cleanup tests/integration/scripts/import-members.test.ts:127
    The NEW-member-with-a-SOFT-DELETED-SECONDARY-contact path is untested. commitMembers line 236 (skippedSoftDeletedContacts counter inside the new-member contact loop) only fires when a NEW member with no active overlap has a non-primary contact whose email matches a soft-deleted row. The suite covers soft-deleted PRIMARY (skips whole member) and partial-overlap, but never the soft-secondary-on-new-member branch.

[15] CONFIRMED/cleanup tests/unit/scripts/import-members-validate.test.ts:148
    The new member_field_mismatch test exercises the country branch (equivalent spelling → no warn; SE vs TH → warn) but never the tier branch's positive-warning case at validate.ts line 212. Both test members share the same tier, so the resolved-tier-mismatch warning path is never asserted.

