# P2 Backlog — Triaged (post-launch)

> **Generated** 2026-06-02 by the `p2-backlog-triage` workflow (14 module agents) from the 152 P2 findings in `docs/Bug/go-live-findings-raw.json`, **re-verified against `main`** after F1–F9 + go-live P0/P1 + the invite-orphan fix (#33/#34) + /code-review remediation all merged.
> P2 = NOT launch-blocking. Work the waves **post-launch**; Wave 0 is correctness/security/data-integrity worth pulling forward.

## Counts

**152 P2** → **140 valid** · 3 already-fixed · 5 stale/invalid · 3 duplicate.

| Wave | n | meaning |
|---|---|---|
| **0** | 21 | correctness / security / data-integrity — pull forward |
| 1 | 45 | cheap (S) user-visible wins — a11y / i18n / UX polish |
| 2 | 26 | module correctness / robustness (M) |
| 3 | 50 | nice-to-have / cosmetic / cleanup / perf — defer |

By category: a11y 26 · perf 22 · ux-polish 21 · cleanup 14 · correctness 13 · security 12 · i18n 10 · data-integrity 9 · observability 9 · test 6

## Per-module

| module | total | valid | fixed | stale | dup |
|---|---|---|---|---|---|
| auth | 13 | 13 | 0 | 0 | 0 |
| plans | 11 | 10 | 0 | 1 | 0 |
| members | 13 | 11 | 0 | 2 | 0 |
| invoicing | 15 | 15 | 0 | 0 | 0 |
| payments | 10 | 8 | 0 | 0 | 2 |
| events | 18 | 18 | 0 | 0 | 0 |
| broadcasts | 15 | 11 | 1 | 2 | 0 |
| renewals | 11 | 11 | 0 | 0 | 0 |
| insights | 12 | 12 | 0 | 0 | 0 |
| presentation | 12 | 11 | 0 | 0 | 1 |
| compliance:pdpa-pii | 7 | 7 | 0 | 0 | 0 |
| tenants + compliance:clean-arch (P2) | 4 | 4 | 0 | 0 | 0 |
| compliance:pci-payments + compliance:thai-tax-invoicing (P2) | 5 | 4 | 1 | 0 | 0 |
| journey:member+admin+manager | 6 | 5 | 1 | 0 | 0 |

---

## Wave 0 — correctness / security / data-integrity (do first) (21)

- **[auth · data-integrity · M]** forgot-password: token invalidation not atomic with token creation — user can lose reset link on crash
  - `src/modules/auth/application/forgot-password.ts:161-165`
  - Confirmed still present: L161 `await deps.tokens.invalidateAllUnconsumedForUser(user.id, now)` then L162 `await deps.tokens.createReset({...})` are two separate top-level awaits with no transaction. A crash between them invalidates all prior tokens and creates none — the user has no live reset link and no email. token-repo.ts already exposes `invalidateAllUnconsumedForUserInTx` (L153/275) and `createInvitationInTx` (L171/312) to mirror, but there is NO `createResetInTx` (only non-tx `createReset` at L129/203). Action: add `createResetInTx` to TokenRepo + impl, wrap both calls in `db.transaction`. Email send + audit emit stay post-commit. Wave 0 because it can silently strand a user mid-recovery.
- **[plans · data-integrity · M]** softDeletePlan: TOCTOU window between countActivePlanMembers and planRepo.softDelete
  - `src/modules/plans/application/soft-delete-plan.ts:87-123`
  - Confirmed. Step 3 (lines 88-100) calls deps.members.countActivePlanMembers in its own runInTenant round-trip; step 4 (lines 107-115) calls deps.planRepo.softDelete in a separate round-trip (plan-repo.ts findOne/softDelete each open their own runInTenant). No advisory lock, SELECT FOR UPDATE, or FK blocks a concurrent member-assignment racing between the count and the delete, leaving active members on a soft-deleted plan. Latent at-scale data-integrity defect -> wave 0 per rubric. Action: wrap the count + softDelete in one runInTenant tx with a per-(tenant,planId,year) pg_advisory_xact_lock (namespace 'plans:soft-delete:' — disjoint from F4 invoicing:/F5 payments:/F7 broadcasts:) and re-check the count inside that tx. Low real-world probability (admin-initiated, low concurrency) so M effort, but the fix is cheap and correctness-critical.
- **[members · data-integrity · M]** member-self-update: pre-tx ownership reads are not re-validated inside the transaction (lost-update vs concurrent archive)
  - `src/modules/members/application/use-cases/member-self-update.ts:194`
  - Confirmed race. memberRepo.findById (:194) and contactRepo.findById (:201) run OUTSIDE runInTenant; the tx (:287) writes via updateFieldsInTx (:292) / updateInTx (:302) from the stale patch with no FOR UPDATE re-read and no `status !== 'archived'` guard. inline-edit.ts does it correctly: findByIdInTx FOR UPDATE inside the tx (:124) then `if (current.status === 'archived') throw` (:190). A concurrent archive between the pre-tx read and the tx lets a portal member write website/description onto a now-archived member, bypassing the archived-immutability invariant inline-edit enforces. Fix: inside runInTenant, re-read with findByIdInTx and assert status !== 'archived' before writing.
- **[insights · data-integrity · S]** prepareExportDownload expiry check is not atomic with the token mint write
  - `src/modules/insights/application/use-cases/download-export.ts:105-116`
  - Confirmed: prepareExportDownload reads job via findById (own tx, line 105), checks isExpired at line 108, then mints in a SEPARATE runInTenant tx at lines 113-115. The setDownloadTokenInTx guard (drizzle-export-job-repo.ts:229-235) only filters `inArray(status, ['ready','delivered'])` — there is NO `expiresAt > now()` predicate, so a TTL boundary crossing between the read and the write mints a token on an expired row. Action (per recommendation): add `gt(exportJobs.expiresAt, new Date())` to the setDownloadTokenInTx WHERE clause so the mint is atomic with the expiry boundary; setOk=false then returns err('expired')/'not_ready' cleanly. GDPR PII surface — promote to wave 0 despite P2 label.
- **[presentation · data-integrity · S]** prepareExportDownload expiry TOCTOU: token minted after expiresAt passes
  - `src/modules/insights/infrastructure/repos/drizzle-export-job-repo.ts:225-238`
  - Confirmed. download-export.ts prepareExportDownload checks expiry in the app layer at line 108 (findById tx), then mints+sets the token in a SEPARATE runInTenant at lines 113-114. setDownloadTokenInTx (repo lines 225-238) guards only `status IN (ready, delivered)` — no `expiresAt` predicate — so a job that crosses expiry between the two steps still gets a token minted. Severity is bounded (the actual downloadExport verify re-checks `isExpired` at line 151 and consumeForDownloadInTx requires a non-null hash), so a stale GDPR-PII link cannot actually be streamed — but the data-integrity invariant ('no token on an expired job') is violated. Wave 0 (PII export surface). Fix: add `gt(exportJobs.expiresAt, now)` to the setDownloadTokenInTx WHERE clause.
- **[compliance:pdpa-pii · data-integrity · M]** GDPR portability archive (Art. 20) omits material personal-data fields from profile projection
  - `src/modules/insights/infrastructure/sources/gdpr-archive-source-adapter.ts:270-305`
  - PARTIALLY fixed, residual still valid. Address fields ARE now added (lines 276-282, 'S1-P1-12' comment: addressLine1/addressLine2/city/province/postalCode). But the profile projection (lines 270-293) STILL omits: notes (line 73 of drizzle-member-repo maps it onto the Member entity — trivially addable), turnoverThb (entity line 67 — trivially addable), and the F8/risk fields riskScore/riskScoreBand/riskScoreFactors/riskSnoozedUntil + renewalRemindersOptedOut/renewalRemindersOptedOutAt + blockedFromAutoReactivation/blockedFromAutoReactivationReason (these are NOT on the Member domain entity per drizzle-member-repo rowToMember lines 56-83, so they need entity+repo plumbing too). The contacts projection (lines 294-305) omits dateOfBirth (contact.ts:93 `readonly dateOfBirth: Date | null`). Verified absent: grep of the adapter returns false for notes/turnoverThb/riskScore/renewalRemindersOptedOut/blockedFromAutoReactivation/dateOfBirth. ACTION: add notes+turnoverThb to profile and dateOfBirth to contacts (cheap, on-entity); plumb risk/renewal/block fields through the Member entity then add to projection (M effort). Data-subject right-of-access/portability gap → wave 0.
- **[compliance:pdpa-pii · data-integrity · M]** export_jobs rows with member PII (subjectMemberId/requestedBy) are never hard-deleted after expiry/failure
  - `src/app/api/cron/insights/process-export-jobs/route.ts:74-107`
  - Confirmed. TTL sweep (route lines 74-107) deletes the private Blob artefact and calls repo.markExpiredInTx — it only transitions status to 'expired', it does NOT delete the export_jobs row. schema-insights.ts:130-131 shows the row carries subjectMemberId (uuid) + requestedBy (uuid) indefinitely. grep for hardDelete/deleteExpired/DELETE FROM export_jobs in the insights module finds only listSweepable (drizzle-export-job-repo.ts:265) which feeds the blob-delete sweep, never a row purge. No scheduled row-purge exists. GDPR Art. 5(1)(e) storage-limitation gap on identifying UUIDs that grows unbounded → wave 0. ACTION: add a sweep that hard-deletes rows WHERE status IN ('expired','failed') AND updated_at < NOW() - INTERVAL '90 days' (as recommended); document in RoPA.
- **[invoicing · observability · S]** export-paid-invoices-csv: list failure cause silently dropped — Neon transients unobservable
  - `src/modules/invoicing/application/use-cases/export-paid-invoices-csv.ts:143-149`
  - Confirmed: the catch block returns err({code:'list_failed'}) with no logger call (lines 143-149). The comment's claim that 'the route side logs e.constructor.name separately' is FALSE — the route (src/app/api/admin/invoices/export.csv/route.ts:93-96) logs `err: result.error`, which is just {code:'list_failed'}; the original exception (SQLSTATE/pool exhaustion) is gone at the use-case boundary. Action: add `logger.error({tenantId: input.tenantId, err: e instanceof Error ? e.message : String(e)}, 'exportPaidInvoicesCsv: listPaged scan failed')` before the return. Do not widen the Result shape.
- **[payments · observability · S]** e.message logged in 7 pino catch sites across payments initiate/cancel/refund routes (H-4 rule not applied)
  - `src/app/api/payments/initiate/route.ts:131`
  - VALID. All 7 catch sites still log raw err: e instanceof Error ? e.message : String(e): payments/initiate/route.ts:131,194,332; payments/[id]/cancel/route.ts:82,136,216; refunds/initiate/route.ts:255 — all logger.error (pino, hits prod logs; confirmed at initiate L122-134). The sibling webhook route (webhooks/stripe/route.ts:174,207,310,357,458,734,845) consistently applies the 'H-4 rule' err: e instanceof Error ? e.constructor.name : 'unknown' precisely because Postgres/framework errors can carry SQL params + literal values, which violates the project log-redact contract (forbidden fields in logs). This is a PII/secrets-in-logs hardening gap on payment routes. Fix: swap e.message→e.constructor.name at all 7 sites (OTel traces own the call-chain diagnostic). Wave 0 — security/observability data-leak class, trivial, do ASAP.
- **[renewals · observability · L]** 9 of 13 §23.1 catalogued metrics have no instrument in metrics.ts
  - `src/lib/metrics.ts:1673`
  - Confirmed: docs/observability.md §23.1 (lines 1116-1180) catalogues these instruments; grep of src/lib/metrics.ts shows they are absent as real instruments — renewals.pipeline.row_count, renewals.pipeline.lapsed_tab_visit_total, renewals.at_risk.recompute_members_succeeded_total, .recompute_members_failed_total, .snooze_total, .outreach_recorded_total, renewals.coordinator.tenants_{enqueued,succeeded,failed}_total, renewals.coordinator.duration_ms, renewals.cron_bearer_auth_rejected_total. The two strings 'recompute_duration_ms' and 'cron_bearer_auth_rejected' that grep matched in metrics.ts are BOTH comment-only (lines 2408 doc-comment 'pairs with recompute_duration_ms already in the operational block above' — but no such histogram instrument actually exists; and 3493 is an unrelated F6 comment). Confirmed snooze-at-risk-member.ts and record-at-risk-outreach.ts emit NO metrics. These feed documented alerts F8-A1 (coordinator.tenants_failed_total) and F8-A3 (cron_bearer_auth_rejected_total) which therefore cannot fire — a silent observability gap on cron-fanout failures and cron auth-bypass attempts. Wave 0 (observability gap on a security/reliability alert path); L because it spans ~9 instruments across coordinator routes + 2 use-cases + page handlers. Prioritise coordinator.tenants_failed_total and cron_bearer_auth_rejected_total first.
- **[presentation · observability · S]** search-plans.ts error type leaks raw exception message to server logs
  - `src/app/api/plans/search/route.ts:275`
  - Confirmed on current main. search-plans.ts:65-66 still defines `SearchPlansError = { type: 'server_error'; message: string }`; line 364-367 sets `message: e instanceof Error ? e.message : String(e)` from the raw DB exception. route.ts:274-276 logs `{ requestId, err: result.error }` via pino — the raw message (potentially SQL fragments / table/column names) reaches the log sink. Wave 0 (PII/observability leak, latent). Fix: drop `message` from SearchPlansError (code-only union), log `errKind(e)` at the catch site in search-plans.ts, and narrow the route log to `errKind` instead of `err: result.error`.
- **[auth · security · M]** disableUser / changeRole accept any cross-tenant userId with no tenant-ownership verification
  - `src/modules/auth/application/disable-user.ts:74`
  - Confirmed: disable-user.ts:74 and change-role.ts:63 call `deps.users.findById(input.targetUserId)` against the global cross-tenant `users` table with no tenant predicate. Route handlers (api/auth/users/[id]/disable/route.ts:21, .../role/route.ts:41) pass raw `asUserId(id)` from the URL with no membership check. NOT exploitable today (single-tenant MTA+STD: `users` holds one tenant). Mandatory F10 pre-condition. Action: add `UserRepo.findByIdInTenant(id, tenantSlug)` (JOIN users→contacts→members), 404 on null to avoid existence leak; add a cross-tenant integration test (POST disable of tenant-B user from tenant-A admin → 404). Wave 0 as a security-correctness gap to land before any 2nd tenant onboards.
- **[invoicing · security · S]** Plaintext recipient email in audit_log.summary persists for 10 years
  - `src/modules/invoicing/application/use-cases/resend-pdf.ts:249,265,342`
  - Confirmed PII/LINDDUN disclosure. Lines 249/265/342 embed plaintext `${recipientEmail}` in the summary string. audit-port.ts:120-122 sets invoice_pdf_resent / receipt_pdf_resent / credit_note_pdf_resent to 10y retention; audit-adapter.ts:56-65 INSERTs `${event.summary}` into audit_log.summary — so the raw email persists 10 years. Code comment lines 245-248 falsely calls the summary 'transient — not retained'. The payload already carries recipient_email_sha256, so the summary email is redundant PII. Action: replace plaintext with a placeholder e.g. `Invoice ${documentNumber} PDF resent to [member email]` in all 3 summaries; remove the 'transient' comment; add a unit assertion that summary contains no '@' for these 3 event types.
- **[events · security · S]** drizzleAttendeeMatcher singleton bound to root db exported without RLS guard
  - `src/modules/events/infrastructure/drizzle-attendee-matcher.ts:51,227`
  - CONFIRMED — factory signature `makeDrizzleAttendeeMatcher(executor: TenantTx | typeof db = db)` at lines 50-52 defaults to root `db` (BYPASSRLS pool), and line 227 exports a pre-built singleton `export const drizzleAttendeeMatcher = makeDrizzleAttendeeMatcher()` bound to that root db. Any SELECT it issues against members/contacts bypasses RLS. Grep confirms no current consumer of the singleton (only the export site), so it is a latent footgun, not an active leak — but it is a tenant-isolation hazard (Constitution Principle I) that any future dev importing from infra can trip. Action: remove the singleton export (line 227) and the `= db` default param so callers must supply a tx-bound executor (di.ts factories already pass tx). Wave 0 — security/tenant-isolation, cheap.
- **[events · security · S]** attendeeEmailLower absent from pino REDACT_PATHS
  - `src/lib/logger.ts:537-542`
  - CONFIRMED — REDACT_PATHS (logger.ts:537-542) lists `attendee_email`/`*.attendee_email`/`*.*.attendee_email` + `attendeeEmail`/`*.attendeeEmail`/`*.*.attendeeEmail`, but NOT `attendeeEmailLower`/`attendee_email_lower`. The field is a real PII surface: schema.ts:139 `attendee_email_lower`, read via readAttendeeEmailLower (repo:75-78), and selected in row queries (repo:236). EVIDENCE NUANCE: the finding's claim it is 'logged via the aggregate in toAggregate()' is inaccurate — toAggregate (repo:145-169) only emits attendee.email, NOT attendeeEmailLower; the exposure is via raw EventRegistrationRow logging, not the aggregate. Still a genuine defense-in-depth gap: the lowercase twin of an already-redacted PII field is unredacted. Action: add 'attendeeEmailLower'/'*.attendeeEmailLower' (+ snake-case) to REDACT_PATHS in the same commit. Wave 0 — PII redaction.
- **[broadcasts · security · S]** customRecipientEmails PII array returned in GET /api/broadcasts/[id] response
  - `src/app/api/broadcasts/[id]/route.ts:87`
  - Confirmed unchanged: line 87 'customRecipientEmails: broadcast.customRecipientEmails' is unconditionally in the JSON body. Persists the full per-recipient email list (cap 100). Endpoint already enforces an owner check (line 64) so it is not an open IDOR today, but it needlessly returns a PII batch -- any future IDOR regression or session compromise leaks it. Wave 0 because it is a cheap PII-minimisation win on a member-facing surface. Action: replace with customRecipientCount: broadcast.customRecipientEmails?.length ?? null; add a contract test asserting the raw array is absent.
- **[broadcasts · security · M]** bodySource stored unsanitised and returned in GET /api/broadcasts/[id] response
  - `src/app/api/broadcasts/[id]/route.ts:84`
  - Confirmed latent stored-XSS write path. submit-broadcast.ts persists bodyHtml as sanitised.value.sanitisedHtml (line 587/617) but bodySource: input.bodySource raw (line 588/618); sanitize-html.ts only processes rawHtml->bodyHtml, never bodySource. draft/route.ts:42 and submit/route.ts:56 cap bodySource at 200KB with z.string() but no DOMPurify. schema.ts:175 stores body_source TEXT. The GET response (route.ts:84) returns it raw. Today compose-form.tsx sets bodySource=client-sanitised bodyHtml, but a crafted API call stores arbitrary HTML that is re-served raw to the draft-reopen UI. Wave 0 (latent security). Action: run bodySource through the same sanitizer at save-draft + submit write boundaries (or validate-as-JSON if Tiptap JSON), and stop returning it from the member GET; add an integration test POSTing a <script> bodySource asserting reject/sanitise.
- **[renewals · security · M]** confirm-renewal portal endpoint has no rate-limit (explicitly deferred in code)
  - `src/app/api/portal/renewal/[memberId]/confirm/route.ts:15-17`
  - Confirmed on current main: the deferral comment at lines 15-17 ('Rate-limit: 10/1h per member per FR-027 (deferred — wired alongside existing rate-limit infra in a follow-on)') is still present and no rateLimiter.check() call exists anywhere in the POST handler (reviewed full file). This endpoint triggers F4 invoice draft+issuance on the public member portal, so an unbounded confirm loop is a real abuse/DoS + invoice-spam vector against a state-changing money path. Wave 0 (security/data-integrity despite P2 label). Action: add rateLimiter.check('f8:confirm-renewal:<tenantId>:<memberId>', 10, 3600) after requireMemberContext, return 429 with Retry-After, plus an integration test asserting the 11th POST is rejected. Note Upstash rate-limit infra already exists in this codebase (renewalsMetrics.redisFallback references the cron rate-limit path).
- **[insights · security · M]** No rate limit on member GDPR export request — unbounded job queue growth
  - `src/app/api/portal/account/data-export/route.ts`
  - Confirmed: the entire POST handler (lines 24-107) has no rateLimiter import or call — only the minute-bucket idempotency key bounds duplicates within a single UTC minute. The admin on-behalf path src/app/api/admin/members/[id]/data-export/route.ts also has zero rateLimiter references (verified). The reference pattern in src/app/api/admin/audit/export.csv/route.ts:67-78 (rateLimiter.check + 429 + Retry-After) is live and reusable. Action: add `rateLimiter.check('gdpr-export-request:'+memberId, 3, 3600)` after the role gate (~line 39) on the portal route + a per-tenant limit on the admin path; return 429 with Retry-After; add a contract test for the 4th-request-429. DoS/resource-exhaustion on a job queue that triggers ZIP+blob builds — wave 0.
- **[presentation · security · M]** No rate limit on member GDPR export request (POST /api/portal/account/data-export)
  - `src/app/api/portal/account/data-export/route.ts:24-107`
  - Confirmed: the POST handler (lines 24-107) has no rateLimiter import/call. requestDataExport keys idempotency on a per-MINUTE UTC bucket (request-data-export.ts:68-71, 89), so a member can queue one fresh archive job per minute indefinitely (60/hr). Each job drives an fflate ZIP + react-pdf invoice PDFs + GDPR archive source reads against live Neon — an unauthenticated-rate (well, member-authenticated) resource-exhaustion vector. Pattern already exists in sibling portal routes (renewal confirm, invoices search/resend). Wave 0 (DoS). Fix: Upstash `rateLimiter.check('gdpr-export-request:'+memberId, 3, 3600)`, 429 + Retry-After, best-effort `gdpr_export_rate_limited` audit, contract test for the 4th request.
- **[compliance:pci-payments + compliance:thai-tax-invoicing (P2) · security · S]** .env.example missing all eight F5/Stripe environment variables
  - `.env.example:273`
  - Confirmed. .env.example is 273 lines and contains zero Stripe/F5 entries (grep for STRIPE_*, FEATURE_F5_* returns nothing), yet it documents F2/F3/F4/F6/F7/F7.1a/F8/F9 sections. src/lib/env.ts:222-280 defines 8 F5 vars: STRIPE_SECRET_KEY, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_API_VERSION, STRIPE_ACCOUNT_ID_SWECHAM, STRIPE_LIVE_MODE, FEATURE_F5_ONLINE_PAYMENT, FEATURE_F5_ASYNC_RECEIPT_PDF. Wave 0 (not wave 1) because this is a payment-config misconfig risk: env.ts:633-650 hard-couples STRIPE_LIVE_MODE to the sk_live_/sk_test_ secret-key prefix and to NODE_ENV — an operator with no template can deploy a live key under STRIPE_LIVE_MODE=false (boot refuses, but the failure mode is opaque without doc). Action: add an F5 section after the F4 block with placeholders for all 8 vars, marking STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET as SECRET, noting NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY is the only client-visible key, and defaulting STRIPE_LIVE_MODE=false with the prefix/NODE_ENV coupling note.

## Wave 2 — module correctness / robustness (26)

- **[auth · correctness · S]** enable-user: users.enable() and audit.append() not atomic; stale pre-update row returned on null read
  - `src/modules/auth/application/enable-user.ts:60-72`
  - Confirmed: L60 `enable(target.id)` commits status='active', L71 reads back, L72 `return ok({ user: updated ?? target })`. On a null read-after-write the response returns `target` (the pre-update DISABLED row) while the DB row is active — caller sees `status==='disabled'`. Note change-role.ts:125-127 was already hardened to `if (!updated) return err({code:'not-found'})`; disable-user.ts:125 and enable-user.ts:72 still use the stale `updated ?? target` fallback. Action (matches existing change-role pattern): `if (!updated) throw new Error('enable-user: read-after-write returned null'); return ok({ user: updated })`. Response-level inconsistency only (DB is correct), hence wave 2.
- **[auth · cleanup · M]** Proxy path-matching for F9 admin dashboard routes is absent — no kill-switch guard in proxy.ts
  - `src/proxy.ts:372-397`
  - Partially overstated premise: proxy.ts has isF7Path (L329-337) + isF8Path (L372-389) blocks but NO isF9Path. HOWEVER the finding's claim that F9 routes are 'always reachable even if an operator disables F9' is FALSE — every F9 surface already self-guards on `env.features.f9Dashboard`: all 14 API routes return 503 feature_disabled (e.g. api/internal/exports/[jobId]/download:49, api/portal/directory/route.ts:42, api/portal/account/data-export/route.ts:26, api/cron/insights/process-export-jobs/route.ts:40) and admin pages call notFound() (admin/audit/page.tsx:75). The kill-switch IS effective. The real, valid gap is defence-in-depth parity: F8 explicitly added proxy guards as 'first gate' (see proxy.ts:381-386 comment) so a disabled deploy short-circuits before rendering layouts/server-component fetches. env.features.f9Dashboard already exists (env.ts:808). Action: add an isF9Path block (build503) covering /api/cron/insights/**, /api/internal/exports/**, /api/admin/directory/exports/**, /api/admin/members/*/data-export/**, /api/portal/account/data-export/**, /api/portal/directory/**, /admin/audit/**, /admin/directory/**. Wave 2, defence-in-depth not a live exposure.
- **[auth · correctness · L]** FR-025 bounce inline warning: user-facing piece missing from forgot-password-form
  - `src/components/auth/forgot-password-form.tsx`
  - Confirmed: forgot-password-form.tsx has only submit/countdown/resend logic — no polling, SSE, or delivery-status fetch. No delivery-status endpoint exists under src/app/api/auth/ (grep for delivery-status returns nothing; api/auth route list has no such file). The Resend webhook DOES satisfy the operational-alert half (webhooks/resend/route.ts:267-277 emits logger.warn on bounced/complained). But FR-025 (spec.md:517-522) explicitly mandates 'the user MUST see an inline message ... check their spam folder or contact support', and SC-017 (spec.md:625-626) requires a test that 'a Resend webhook reporting a bounce triggers a visible inline warning on the waiting page' — both currently unsatisfiable. Action: add GET /api/auth/email-delivery-status?requestId= (or Redis message_id keyed by rate-limit bucket) that the form polls post-submit to surface an inline warn banner on bounce. L effort (new endpoint + form polling + test). Wave 2: basic reset flow works without it, but it is an outstanding spec/SC gate.
- **[broadcasts · correctness · M]** reconcileStuckSending: BroadcastConcurrentMutationError from markSent surfaces as reconcile.server_error
  - `src/modules/broadcasts/application/use-cases/reconcile-stuck-sending.ts:202-315`
  - Confirmed on current main. markSent (line 202) calls deps.broadcastsRepo.withTx(...) (line 219) which runs applyTransition(..., 'sent', ..., 'sending') with the conditional WHERE status='sending'. No try/catch around withTx in markSent. A concurrent webhook transitioning sending->sent between findById (line 131, no tx) and applyTransition makes the conditional UPDATE affect 0 rows -> BroadcastConcurrentMutationError propagates to the outer catch at line 194 -> returned as reconcile.server_error, polluting the cron error counter. Action: in markSent, wrap the withTx call and on BroadcastConcurrentMutationError return ok({kind:'not_stuck_yet', observedStatus:'already_transitioned'}); re-throw other errors. Add a throw-path test (mock-only suites hide this).
- **[broadcasts · correctness · M]** dispatch-scheduled-broadcast.ts: getMemberPrimaryContact called outside try/catch at Step 2
  - `src/modules/broadcasts/application/use-cases/dispatch-scheduled-broadcast.ts:459-462`
  - Confirmed. await deps.membersBridge.getMemberPrimaryContact(deps.tenant, requestingMember) at lines 459-462 runs with no try/catch, after the Step 1 withTx block (whose own try/catch ends at line 447). If the bridge throws (Neon outage/RLS/timeout) it escapes dispatchScheduledBroadcast entirely, bypassing failDispatchAndAudit. The cron caller's per-broadcast loop DOES have a try/catch (route.ts:190) bucketed as uncaught_error, so other broadcasts in the tick are NOT blocked -- finding's 'short-circuit the loop' claim is partly overstated. But the real defect stands: this throw path skips the typed dispatch.server_error Result and the failDispatchAndAudit cleanup/audit trail. Action: wrap in try/catch, route failures through failDispatchAndAudit + return err({kind:'dispatch.server_error'}).
- **[broadcasts · test · M]** BroadcastApprovalCounter cross-module port has no contract test -- F9 dashboard count silently breakable
  - `src/modules/broadcasts/application/ports/broadcast-approval-counter.ts:10`
  - Confirmed gap. Port (single method countAwaitingApproval, line 10) is exported from the barrel (index.ts:247-248) and implemented by drizzle-broadcast-approval-counter.ts (runInTenant + status='submitted' count). The only test touching it is the insights boundary contract (source-adapters-boundary.contract.test.ts) which the file itself states is STRUCTURAL-only -- behavioural conformance (real query returning correct aggregates) is left as an it.todo at the integration layer. So no test asserts the count is correct given a seeded mix of submitted/approved/rejected rows. Wave 2 (test robustness, not user-blocking). Action: add an integration/contract test seeding mixed-status rows and asserting countAwaitingApproval returns only the 'submitted' count.
- **[compliance:pdpa-pii · correctness · M]** acknowledgeBroadcastsTerms audit emit failure leaves consent recorded but unaudited — no backfill cron exists
  - `src/modules/broadcasts/application/use-cases/acknowledge-broadcasts-terms.ts:142-171`
  - Confirmed unchanged. Lines 142-171: the member_acknowledged_broadcasts_terms emit is wrapped in try/catch that only logger.error's 'broadcasts.acknowledge.audit_emit_failed' then falls through to ok(); the comment at lines 138-141 still references a hypothetical 'future backfill cron' that does not exist (no cron in the codebase reconstructs missing audit rows from the F3 broadcastsAcknowledgedAt column). GDPR Art. 7(1) demonstrability / PDPA §24 consent-record gap: the consent is durably in the F3 column but the demonstrability audit row can silently go missing. ACTION: implement the compensating cron (members WHERE broadcasts_acknowledged_at IS NOT NULL AND no matching audit_log row) OR make the emit transactional with the column write. Module robustness, not user-blocking → wave 2.
- **[compliance:pdpa-pii · data-integrity · L]** No documented retention/pseudonymisation enforcement for member & contact rows after archival
  - `src/modules/members/domain/member.ts:174`
  - Confirmed. member.ts:174 ARCHIVE_UNDELETE_WINDOW_DAYS=90 is ONLY the undelete window — after 90 days the row cannot be undeleted but is never hard-deleted or pseudonymised. The F6 contrast pattern exists (src/modules/events/application/use-cases/pseudonymise-stale-non-member-pii.ts sweeps non-member event PII after 2y) but there is NO equivalent for members/contacts (grep for pseudonymis/hardDelete/retention in modules/members returns only the schema-members.ts comment). GDPR Art. 5(1)(e) + Thai RC §87/3: archived-member PII (companyName/email/phone/taxId/address) retained indefinitely with no schedule. ACTION: define + document a retention schedule (e.g. pseudonymise archived members >5y) and implement a low-volume annual cron mirroring the F6 pattern. L effort, not user-blocking → wave 2.
- **[events · data-integrity · M]** isRefundTransition reads pre-lock snapshot of existingReg.ticket.paymentStatus
  - `src/modules/events/application/use-cases/_helpers/process-attendee-in-tx.ts:829-833`
  - CONFIRMED present. Line 829 `const existingReg = regInsert.value.registration;` then lines 830-833 compute isRefundTransition from `existingReg.ticket.paymentStatus !== 'refunded'` — the snapshot comes from the INSERT...CONFLICT RETURNING value, and the advisory lock at lines 842-846 (`hasMatchedQuotaScope`) is acquired AFTER. The code itself documents this exact race in a long comment at lines 810-828 (R6.W/Round5 R013) and notes MVCC+UPDATE...RETURNING prevents lost writes, so divergence is audit-vs-state only and 'unobservable at SweCham single-admin scale'. Real but latent; bites only at multi-admin MTA concurrency. Action (F6.2 backlog): acquire per-registration advisory lock unconditionally before reading the snapshot, or re-read the row inside lock scope (CAS). Not user-blocking now.
- **[events · perf · S]** Fuzzy matcher loads all tenant members into V8 heap with no upper bound — O(M) per attendee, computes Levenshtein in JS
  - `src/modules/events/infrastructure/drizzle-attendee-matcher.ts:131-137`
  - CONFIRMED — lines 131-137 `executor.select({memberId, companyName}).from(members).where(eq(members.tenantId, input.tenantId))` has NO .limit(); all M members are pulled into JS heap and normaliseCompanyName + levenshtein run per pair (lines 139-150) for every attendee. Comment at line 16 documents the '~<2k design envelope' but no defensive cap exists. Bites a large/MTA tenant (heap + O(M·|name|) latency on the import hot path). Action: add MAX_MEMBERS_FOR_FUZZY_MATCH (~3000) constant + .limit() on the SELECT; when the cap fires, logger.warn and skip the fuzzy pass (fall through to non_member — conservative, avoids false positives). Single-line infra change. Wave 2 robustness (latent at scale, not user-blocking at SweCham).
- **[insights · perf · S]** export_jobs TTL sweep and stuck-processing scan have no index on time predicates
  - `drizzle/migrations/0188_f9_export_jobs.sql:66-67`
  - Confirmed: migration only creates export_jobs_tenant_status_idx (tenant_id, status) at lines 66-67; grep across all migrations finds no index covering expires_at or updated_at. listSweepable (repo:265-278) filters (tenant_id, status IN ready/delivered, expires_at < NOW()) and listStuckProcessing (repo:296-309) filters (tenant_id, status='processing', updated_at < cutoff) — both do an index scan on (tenant_id,status) + recheck on the unindexed time column. Both run in the export cron every ~5 min. Distinct from #9/#10. Action: follow-up migration adding two partial indexes per the recommendation. M-scale data is small now but a missing-index follow-up migration is a concrete module-robustness item — wave 2 (S effort, but DB schema change → keep visible).
- **[insights · correctness · M]** FR-007a engagement score absent from member profile page
  - `src/app/(staff)/admin/members/[memberId]/page.tsx`
  - Confirmed spec-compliance gap: spec.md:385-389 (FR-007a) requires the engagement score 'displayed on the member profile and the dashboard'. Grep of the 890-line member profile page returns ZERO matches for engagement/riskScore/risk_score/projectEngagementScore/EngagementBand/riskScoreBand. The score IS surfaced on the list page (admin/members/page.tsx:29 import + :227 projectEngagementScore call) but neither the profile page nor the dashboard (admin/page.tsx shows only the raw atRisk count) renders it. Action: on the profile page call projectEngagementScore({riskScore, riskScoreBand}) and render score+band label (text+icon, non-colour-alone per FR-035) in the Company-details dl; optionally surface a dashboard engagement chip, or scope the dashboard clause as P3. Unmet stated requirement → correctness, wave 2.
- **[invoicing · a11y · M]** Five admin invoicing routes missing error.tsx boundaries
  - `src/app/(staff)/admin/invoices/error.tsx (+4 sibling paths)`
  - Confirmed: all 5 paths still MISSING error.tsx (invoices/, [invoiceId]/, [invoiceId]/void/, [invoiceId]/credit-notes/new/, settings/invoicing/). The only catcher is the root src/app/(staff)/admin/error.tsx which hardcodes <DetailContainer> (line 44) for every route — so a throw in the LIST route renders inside detail-width, a real container/CLS mismatch. Hotspots in page.tsx (creditNoteRepo.findByOriginalInvoice, userRepo.findById, drizzleTenantSettingsRepo.getForIssue) have no local try/catch and bubble to that root boundary. Action: add 5 route-level error.tsx using the matching container (TableContainer for list, DetailContainer for detail/void/credit-note, FormContainer for settings) following the AdminRouteError pattern with back-link + reset() CTA.
- **[invoicing · correctness · L]** FR-026 — no proactive auto_email_delivery_failed surface on invoice detail page
  - `src/app/(staff)/admin/invoices/[invoiceId]/page.tsx`
  - Confirmed gap. auto_email_delivery_failed is emitted by the outbox cron (api/cron/outbox-dispatch/route.ts:837,902,1025) with i18n labels present, but grep over the [invoiceId]/ tree finds NO query for it and NO failure banner. The detail page (820 lines) only exposes a manual 'Resend' more-menu action (lines 417-448) and an Issue AlertDialog (320) — there is no proactive alert telling the admin an auto-email failed. FR-026 requires surfacing the failure on the detail page with a retry/edit-recipient action. Not user-blocking at launch (manual resend exists), so wave 2. Action: query the invoice's outbox/audit rows for auto_email_delivery_failed and render an inline alert with a Resend CTA pairing the existing resend-pdf use-case. Scope=escalate (FR-level work, not a trivial fix).
- **[journey:member+admin+manager · ux-polish · M]** Profile Plan card omits the plan/tier name — 'view own plan' shows only a year
  - `src/app/(member)/portal/profile/page.tsx:184-219`
  - Confirmed on main. The Plan card (lines 184-219) renders only planYear (195), registrationDate (202), and status badge (210-214) — no plan name/tier. profile/page.tsx imports only getMember from @/modules/members (line 20); no plans port / listPlans / getPlan is wired, and member.planId is never resolved to a human-readable name. Tier identity is only on /portal/benefits and the unreachable renewal page. Action: resolve planId -> plan name+tier via an F2 plans port in profile/page.tsx, render in the Plan card, and add a 'View benefits' link to /portal/benefits. Member-journey robustness, not user-blocking → wave 2.
- **[journey:member+admin+manager · correctness · S]** New-invoice member picker excludes 'inactive' members (loads status:['active'] only), blocking a billing path the domain allows
  - `src/app/(staff)/admin/invoices/new/page.tsx:85-88`
  - Confirmed: new/page.tsx:87 calls directorySearch with status:['active'] only. invoice-form.tsx:75-80 silently resets the ?memberId= deep-link to '' when the member is not in that list. The domain PERMITS inactive members — both create-invoice-draft.ts:128 and issue-invoice.ts:209 reject ONLY member.isArchived ({code:'member_archived'}); inactive passes. So an admin cannot raise an invoice for an inactive (e.g. lapsed/unpaid) member via the UI even though the backend would accept it. Action: load picker with status:['active','inactive'] (keep archived excluded to match the guard); optionally, when ?memberId= resolves to a member outside the list, show it as a pre-selected option rather than silently clearing. Module correctness, not user-blocking → wave 2.
- **[journey:member+admin+manager · ux-polish · M]** Post-onboarding /portal landing still shows the stale F4/F5/F6 'coming soon' roadmap card
  - `src/app/(member)/portal/page.tsx:48-90`
  - Partly addressed but still valid. redeemInvite still lands on /portal (redeem-invite.ts) and portal/page.tsx:48-90 STILL renders the roadmap card listing F4 Invoices (57-64), F6 Events (66-75), F5 Renewal (77-87) as future work — all shipped — plus the static info@swecham.se contact card (92-105). Mitigation since the finding: an InvoicesSummaryCard now renders at line 46 (US7 AS4), so it is no longer the bare F1 placeholder. But the core defect — a 'coming soon' roadmap describing already-shipped features, and no 'complete your profile / view plan & benefits / renew' entry points — remains. Action (minimum): delete the stale roadmap card; better: replace with live entry points. Member-journey content correctness, not blocking → wave 2.
- **[members · ux-polish · M]** Missing route-level error.tsx for /admin/members/ and /admin/members/new/
  - `src/app/(staff)/admin/members/page.tsx:1`
  - Confirmed: only `[memberId]/error.tsx` exists (glob of members/**/error.tsx). The directory listing (members/page.tsx) and create page (members/new/page.tsx) have no local boundary, so a throw in requireSession/resolveTenant/listPlans bubbles to the present /admin/error.tsx fallback, losing the TableContainer/FormContainer + breadcrumb framing. Fix: add members/error.tsx (TableContainer mirror of [memberId]/error.tsx) and members/new/error.tsx (FormContainer), reusing errors.generic + buttons.retry keys.
- **[members · ux-polish · M]** Bulk 'Send portal invite' ConfirmationDialog shows no in-flight loading indicator
  - `src/app/(staff)/admin/members/_components/bulk-action-bar.tsx:222`
  - Confirmed at :222-231 — the invite ConfirmationDialog passes `confirmDisabled={executing}` but no spinner; the sibling ArchiveConfirmDialog (:219) gets `pending={executing}` and shows Loader2 during the request. Verified src/components/shell/confirmation-dialog.tsx has confirmDisabled but NO `pending` prop and NO Loader2. During a bulk invite the confirm button is merely disabled with no progress cue. Fix: add an optional `pending` prop to the shared ConfirmationDialog (Loader2 next to confirm label) and pass `pending={executing}`. M because it touches a shared component.
- **[payments · correctness · S]** processChargeRefunded: getRefundContextForUpdate called before lockForUpdate — stale-snapshot window
  - `src/modules/payments/application/use-cases/process-charge-refunded.ts:286`
  - VALID. At L286-295 the webhook-recovery path reads the aggregate via refundsRepo.getRefundContextForUpdate (L286) BEFORE locking the parent payment row via paymentsRepo.lockForUpdate (L291); ctx.succeededSumSatang (used at L303 to derive isFullyRefunded) is read before the FOR UPDATE lock. The correct ordering exists in issue-refund.ts (lockForUpdate L278 precedes getRefundContextForUpdate L292). Under READ COMMITTED a concurrent issueRefund Phase B committing between L286 and L291 makes the locked parent row newer than ctx. Real but narrow window, and the L306 expectedCurrentStatus race-guard + L314 null-check already prevent a wrong status WRITE (it would just no-op). Fix is trivial and matches the established pattern: swap so lockForUpdate runs first, then read the aggregate under the lock. Wave 2 (module robustness, not user-blocking; the existing guard contains the blast radius).
- **[presentation · correctness · M]** FR-007a: Engagement Score absent from member profile page
  - `src/app/(staff)/admin/members/[memberId]/page.tsx`
  - Confirmed spec-compliance gap. spec.md FR-007a (line 385-389): the Engagement Score MUST be 'displayed on the member profile and the dashboard.' grep of the member detail page returns zero 'engagement'/'projectEngagement'/'riskScore' references. The member LIST page (members/page.tsx:29,226-246) and the table column (members-table.tsx) correctly project + render it via `projectEngagementScore` from the insights barrel — only the [memberId] profile is missing it. Wave 2 (module/spec correctness, not user-blocking since the score is visible on the list). Effort M: add a projectEngagementScore call to the detail page, render score + non-colour band label (FR-035 text-not-colour-alone) in the Company Details card, plus i18n band labels.
- **[renewals · correctness · M]** decideUpgrade uses minTurnoverThb ordering instead of tierBucket enum — allows same-bucket upgrade suggestion + non-deterministic thresholdMetAt
  - `src/modules/renewals/application/use-cases/evaluate-tier-upgrade.ts:108-118,134`
  - Confirmed on current main (file untouched since F8 ship #24). The `candidatesAbove` filter at lines 108-118 only requires `p.minTurnoverThb > currentPlan.minTurnoverThb` (threshold ordering); there is NO tierBucket rank comparison, so two plans in the same tier-bucket that differ by minTurnoverThb can produce a same-bucket tier_upgrade_suggested audit, contradicting the docstring at lines 80-91 ('tier-bucket is strictly above'). Separately, `const thresholdMetAt = new Date().toISOString()` at line 134 is wall-clock-dependent inside the pure decideUpgrade function — and the caller already computes `nowIso = new Date().toISOString()` at line 477 but does NOT thread it into decideUpgrade. NOTE: there is no TIER_BUCKET_ORDER constant in the module today (grep found none), so the fix must also introduce that ordering map (tier-bucket enum is 'thai_alumni'|'start_up'|'regular'|'premium'|'partnership' per plan-lookup-for-renewal.ts:17). Action: add a TIER_BUCKET_ORDER rank filter to candidatesAbove, and thread nowIso from line 477 into decideUpgrade replacing new Date() at 134.
- **[renewals · correctness · S]** computeAtRiskScore uses wall-clock new Date() for computedAt inside a ClockPort-injected module
  - `src/modules/renewals/application/use-cases/compute-at-risk-score.ts:196`
  - Confirmed: line 196 reads `computedAt: new Date().toISOString()` passed to memberRenewalFlagsRepo.setRiskScore, while the module declares ClockPort via RenewalsDeps and sibling use-cases use deps.clock.now(). One-line fix: replace with `deps.clock.now().toISOString()`. Low blast radius but it makes the persisted computedAt non-deterministic under test and inconsistent with the rest of the clock-injected module. M-class as a clean fix would also add/adjust a determinism test; the edit itself is S.
- **[renewals · security · M]** reassignEscalationTask does not verify to_user_id belongs to the same tenant
  - `src/app/api/admin/renewals/tasks/[taskId]/reassign/route.ts:31-33`
  - Confirmed: route BodySchema validates `to_user_id: z.string().uuid()` only (line 32) and never checks tenant membership of the assignee before calling reassignEscalationTask. The use-case docstring (reassign-escalation-task.ts:9-13) explicitly states it does NOT independently verify the assignee exists in the tenant — defence is delegated entirely to the combobox (client-only). The listForAdminQueue users JOIN (drizzle-renewal-escalation-task-repo.ts:448-497) carries an explicit comment that the JOIN is non-defensive and would leak cross-tenant once per-tenant user scoping lands. MITIGATION present today: the task row itself is RLS+FORCE protected (only same-tenant tasks reassignable) and the F1 users table is currently globally unique (single-tenant deploy), so the practical exploit is bounded until MTA user-scoping lands — hence wave 2 not wave 0. Action: add a server-side staff-user/tenant-membership check before reassign, return 400 invalid_assignee on miss, + cross-tenant-UUID integration test.
- **[tenants + compliance:clean-arch (P2) · test · M]** Missing unit tests for tenant-slug.ts — asTenantSlug and unsafeBrandTenantSlug have no dedicated test coverage
  - `tests/unit/tenants/`
  - Confirmed: tests/unit/tenants/ contains only domain/tenant-context.test.ts + iana-timezone.test.ts — no tenant-slug.test.ts. Grep shows InvalidTenantSlugStringError (thrown by asTenantSlug at src/modules/tenants/domain/tenant-slug.ts:45-50) is referenced in ZERO test files; asTenantSlug/unsafeBrandTenantSlug get only happy-path indirect coverage via fixtures in other suites. The reject paths (empty/uppercase/underscore/>63-char/non-string) and the escape-hatch unsafeBrandTenantSlug (security-sensitive, enumerated call sites tenant-slug.ts:58-82) are unexercised. Violates Constitution Principle II (100% Domain line coverage) on a security-sensitive file. Action: add tests/unit/tenants/tenant-slug.test.ts mirroring tenant-context.test.ts — asTenantSlug happy + reject paths, InvalidTenantSlugStringError.attempted carries the bad slug, unsafeBrandTenantSlug returns branded value without validation.
- **[tenants + compliance:clean-arch (P2) · cleanup · M]** Invoicing composition root misplaced in application/ instead of infrastructure/
  - `src/modules/invoicing/application/invoicing-deps.ts:13-32`
  - Confirmed on main: file still at src/modules/invoicing/application/invoicing-deps.ts and imports concrete Infrastructure adapters (makeDrizzleInvoiceRepo, vercelBlobAdapter, reactPdfRenderAdapter, f4AuditAdapter, CURRENT_TEMPLATE_VERSION, etc.) from ../infrastructure/** at lines 13-32 — a composition root. Comparison modules put their -deps.ts in infrastructure/: src/modules/renewals/infrastructure/renewals-deps.ts + src/modules/insights/infrastructure/insights-deps.ts. Companion src/modules/invoicing/upload-tenant-logo-deps.ts also sits at module root. Principle III (NON-NEGOTIABLE) structural placement defect, but non-user-blocking (was scoped post-launch-backlog). Action: move both -deps.ts files to invoicing/infrastructure/, update the barrel src/modules/invoicing/index.ts re-export + any src/app or src/lib importers, then pnpm typecheck + test:integration. Note eslint.config.mjs has *-deps.ts ignores (lines 288/533) so the application-layer ban currently tolerates this file in place.

## Wave 1 — cheap user-visible wins (45)

### a11y (25)
- [auth·S] InviteUserDialog locked-member display uses role=textbox on a non-focusable div — `src/components/auth/invite-user-dialog.tsx:281-292`
- [auth·S] PasswordStrength bar text is text-muted-foreground at 12px — marginal contrast in light mode — `src/components/auth/password-strength.tsx:120`
- [auth·S] invite-redeem-form and reset-password-form linkInvalid error cards have no managed focus on mount — `src/components/auth/invite-redeem-form.tsx:149-165`
- [plans·S] Plans <Table> missing <TableCaption> — WCAG SC 1.3.1 — `src/components/plans/plans-table.tsx:323`
- [plans·S] Wizard Save button missing Loader2 spinner and aria-busy during submission — `src/components/plans/plan-form-wizard.tsx:538-544`
- [members·S] InlineCountryCell and InlineNotesCell enter edit mode via onDoubleClick only — keyboard cannot activate (WCAG 2.1 SC 2.1.1) — `src/components/members/members-table.tsx:386`
- [members·S] Select-all checkbox shows no indeterminate state when rows are partially selected — `src/components/members/members-table.tsx:575`
- [members·S] Expired-undelete tooltip in ArchivedBanner is keyboard-inaccessible — inner span has no tabIndex — `src/components/members/archived-banner.tsx:143`
- [invoicing·S] CSV export dialog validation error not associated to date inputs (WCAG 1.3.1/3.3.1) — `src/app/(staff)/admin/invoices/_components/csv-export-dialog.tsx:112-140`
- [invoicing·S] Credit note form reason field missing aria-invalid — `src/app/(staff)/admin/invoices/[invoiceId]/credit-notes/new/_components/credit-note-form.tsx:171-182`
- [payments·S] RefundForm amount field: inline error not referenced by aria-describedby — AT cannot announce validation error — `src/app/(staff)/admin/invoices/[invoiceId]/_components/refund-dialog/refund-form.tsx:267`
- [payments·S] ConfirmationPanel auto-close pause button is 24px min — below the codebase 44px touch-target convention — `src/app/(member)/portal/invoices/[invoiceId]/_components/pay-sheet/confirmation-panel.tsx:235`
- [events·S] 'Not counted' quota badge in attendee table uses HTML title= attribute instead of Radix Tooltip — not keyboard-accessible — `src/components/events/attendee-table.tsx:714`
- [events·S] Checkmark glyph '✓' in CSV preview table header is hardcoded, not i18n-keyed, and announced verbatim by screen readers — `src/components/events/csv-mapping-form.tsx:952`
- [events·S] CSV preview table header canonical-column accent border (border-b-emerald-500 on emerald-50/70 bg) fails WCAG SC 1.4.11 non-text contrast in light mode — `src/components/events/csv-mapping-form.tsx:935`
- [events·S] Events list filtered-empty state has no heading element — AT heading navigation skips it — `src/app/(staff)/admin/events/page.tsx:429-440`
- [events·S] Events list page inline error card has no role=alert — screen reader users not immediately notified — `src/app/(staff)/admin/events/page.tsx:211`
- [broadcasts·S] ApproveDialog has no pending-state Loader2 spinner on the Confirm button — `src/components/broadcast/admin/approve-dialog.tsx:254-262`
- [broadcasts·S] SubmitButton (member compose) uses text-only pending state -- no Loader2 spinner — `src/components/broadcast/submit-button.tsx:52-61`
- [renewals·S] TierUpgradeQueue table missing accessible caption (SC 1.3.1) + raw plan-id slugs — `src/app/(staff)/admin/renewals/tier-upgrades/_components/tier-upgrade-queue.tsx:140-176`
- [renewals·S] ScheduleEditor Save button missing aria-busy during pending state — `src/app/(staff)/admin/settings/renewals/schedules/_components/schedule-editor.tsx:750-756`
- [insights·S] ActivityFeedRefresh Refresh button missing aria-busy during in-flight refresh — `src/components/dashboard/activity-feed-refresh.tsx:36-41`
- [insights·S] InsightDismissButton missing aria-busy while dismissal is in flight — `src/components/dashboard/insight-dismiss-button.tsx:55-66`
- [insights·S] ActivityFeedRefresh live-region setTimeout 50ms may under-fire on NVDA — `src/components/dashboard/activity-feed-refresh.tsx:30-31`
- [presentation·S] ActivityFeedRefresh announces 'refreshed' ~50ms after click, before the server round-trip completes — `src/components/dashboard/activity-feed-refresh.tsx:30-31`

### correctness (1)
- [journey:member+admin+manager·S] Dashboard 'Overdue invoices' link points to ?status=issued (superset), not the now-fixed overdue subset — `src/app/(staff)/admin/page.tsx:192-197`

### i18n (8)
- [plans·S] Plan detail page renders enum values via hardcoded humanize() — breaks TH/SV locale — `src/app/(staff)/admin/plans/[year]/[planId]/page.tsx:227,231,235,245,290`
- [members·S] Zod validation messages in MemberForm are hardcoded English strings, not i18n keys — `src/components/members/member-form.tsx:51`
- [invoicing·S] Admin detail and portal formatDate bypass next-intl — risk Buddhist-Era year under full ICU — `src/app/(staff)/admin/invoices/[invoiceId]/page.tsx:105-112 + src/app/(member)/portal/invoices/_utils/format.ts:28-35`
- [broadcasts·S] compose error codes broadcast_custom_recipient_empty and broadcast_custom_recipient_too_many missing from all three locale files — `src/components/broadcast/compose-form.tsx:78-79`
- [renewals·M] Cycle-detail activity section renders reminder status, channel, and escalation task status/role as raw machine-readable lowercase values — `src/app/(staff)/admin/renewals/[cycleId]/page.tsx:693,701,726,740`
- [insights·S] Thai Buddhist Era calendar not applied on 5 admin date-format instances — `src/app/(staff)/admin/page.tsx:147`
- [compliance:pdpa-pii·S] GDPR archive notes field carries admin internal annotations — needs README disclosure + DPO sign-off — `src/modules/insights/infrastructure/sources/gdpr-readme.ts:45-78`
- [tenants + compliance:clean-arch (P2)·S] feeConfigMissing error surfaced to staff UI contains developer-only instruction 'Run the seed script' — `src/i18n/messages/en.json:808`

### ux-polish (11)
- [auth·S] EmailChangeRevertForm submit button has no Loader2 spinner during submission — `src/components/auth/email-change-revert-form.tsx:103-111`
- [auth·S] Password-strength bar uses bg-emerald-500 / bg-amber-500 instead of --success / --warning tokens — `src/components/auth/password-strength.tsx:90`
- [plans·S] read_only_mode 503 response falls through to generic toast in all mutation clients — `src/app/(staff)/admin/plans/new/new-plan-client.tsx:53-57`
- [invoicing·S] Success-state icons hardcode emerald instead of the canonical --success token — `src/app/(staff)/admin/invoices/[invoiceId]/_components/payment-timeline.tsx:135,141,146 + copy-charge-id-button.tsx:66`
- [events·S] CSV-import degraded warning banners hand-roll amber instead of the InlineAlert warning primitive — `src/components/events/csv-import-result.tsx:193,212,231,332`
- [events·S] Webhook-config and Zapier info cards hand-roll blue instead of InlineAlert/info token — `src/components/events/webhook-config-wizard.tsx:223`
- [events·S] generateMetadata for erase-PII page uses eventId.slice(0,8) as attendeeName placeholder — `src/app/(staff)/admin/events/[eventId]/registrations/[registrationId]/erase/page.tsx:48`
- [renewals·S] At-risk widget empty-state success icon hardcodes text-emerald-500 instead of text-success token — `src/app/(staff)/admin/renewals/_components/at-risk-widget.tsx:291`
- [presentation·S] Member directory logo preview hardcodes bg-white plate (dark-mode parity break) — `src/components/directory/directory-logo-control.tsx:92`
- [presentation·S] DirectoryTable company-name cell has no link to member profile — `src/components/directory/directory-table.tsx:76`
- [journey:member+admin+manager·S] 'Record payment' link from member detail uses a #payment anchor that does not exist on the invoice detail page — `src/app/(staff)/admin/members/[memberId]/_components/member-invoices-section.tsx:388`

## Wave 3 — defer (50)

Nice-to-have / cosmetic / cleanup / micro-perf. Not enumerated individually — counts by category:

- perf: 20
- cleanup: 12
- ux-polish: 6
- observability: 5
- test: 4
- i18n: 2
- security: 1

_(full list in `docs/Bug/go-live-findings-raw.json` + per-finding notes in the triage workflow result.)_

---

## Triaged out

### already-fixed (3)
- [broadcasts] Admin broadcast detail page BatchBreakdown mount unverified for US1 (FR-006) — `src/app/(staff)/admin/broadcasts/[id]/page.tsx:102-105,229-233`
  - Verification resolved -- the concern no longer holds. The admin detail page src/app/(staff)/admin/broadcasts/[id]/page.tsx exists and: imports BatchBreakdown (line 13) and isF71aUs1Enabled (line 18); gates the manifest load on const f71aUs1On = isF71aUs1Enabled() (line 102, skipping the DB query when OFF, lines 103-105); and conditionally mounts {f71aUs1On && !batchLoadFailed ? <BatchBreakdown .../> : ...} (lines 229-233). It also handles load failure via a null sentinel (loadBatchBreakdownRows, line 283+). The ship-gate the finding asked to escalate is satisfied by current code; no action needed.
- [compliance:pci-payments + compliance:thai-tax-invoicing (P2)] MemberIdentitySnapshot.tax_id nullable but PDF shows no warning when a corporate member has no tax_id — `src/modules/invoicing/infrastructure/pdf/templates/invoice-template.tsx:265`
  - Already fixed by the S1-P1-16 P1 remediation, which is strictly stronger than this finding's PDF-warning fallback. issue-invoice.ts:223-228 blocks issuance entirely — `if (member.memberTypeScope === 'company' && (member.snapshot.tax_id ?? '').trim() === '') return err({ code: 'tax_id_required' })` — and the comment at lines 211-215 places it BEFORE allocateNext so a missing buyer tax_id never burns a §87 sequence number (FR-009a / Revenue Code §86). Provenance: commit 1391b806 'require buyer tax_id to issue a company tax invoice (S1-P1-16)', with unit (issue-invoice.test.ts:301,321 incl. whitespace-only branch) and live-Neon integration (issue-vs-archive-race.test.ts:295,312) coverage. Consequently a corporate member can never reach the PDF render at invoice-template.tsx:265-267 with a null tax_id, so the silent-blank concern is moot; for individual/person members (memberTypeScope 'individual'/'both'/null) a null tax_id is correct and intentional, so no PDF warning is desired. Note the finding's suggested field member.entityType==='corporate' does not exist; the implemented gate uses member.memberTypeScope==='company'. Residual (NOT this finding): code comment at lines 217-222 flags a future-tenant 'both'-scope plan with company members as an unscoped edge — no live defect for SweCham 2026 (corporate tiers are 'company', partnership 'individual').
- [journey:member+admin+manager] InvitePortalButton / Archive / Contact actions surface a 403 to managers as a misleading generic 'server error' toast — `src/components/members/invite-portal-button.tsx:46-64`
  - Already-fixed re: the manager journey via the finding's own PREFERRED remediation. The three write affordances are no longer reachable by managers: page.tsx:404 `const canWrite = session.user.role === 'admin'` gates InvitePortalButton+ContactActions behind `{canWrite && (` at line 328 (comment 'S1-P1-10: write affordances hidden for the read-only manager') and ArchiveMemberButton behind `{canWrite && member.status !== 'archived' && (` at line 575. Invoice actions in member-invoices-section.tsx use canMutate + ManagerDisabledAction. So managers cannot trigger the POST/DELETE that returns 403, and the 403→serverError fall-through is now dead for them. Residual (out of scope for this manager-journey finding): the client switch (invite-portal-button.tsx:46-68, contact-actions.tsx:56-64, archive-member-button.tsx:74-79) still has no explicit 403 branch — and note the route's 403 body is the STRING {error:'forbidden'} (admin-context.ts:99) while clients read body.error?.code, so even if re-exposed it would still hit default→serverError. A defensive 403 branch is a minor hardening nicety, not the reported manager-facing defect.

### stale-invalid (5)
- [plans] cross-tenant probe test not verifiable in-module (Principle I sub-clause 3) — `tests/integration/plans/us3-tenant-isolation.test.ts`
  - Stale. The referenced file tests/integration/plans/us3-tenant-isolation.test.ts does NOT exist (ls of tests/integration/plans confirms). The authoritative membership_plans cross-tenant probe is tests/integration/plans/tenant-isolation.test.ts (T027, the Principle I sub-clause 3 Review-Gate blocker). It is fully self-contained: it seeds 3 plans per tenant via seedPlansFor() (no member_id/audit payload involved) and asserts zero cross-tenant SELECT/INSERT/UPDATE/DELETE visibility from both directions plus secure-by-default unset-tenant -> 0 rows (lines 124-251). The described 'camelCase memberId vs snake_case member_id seed bug making the probe non-evaluable' does not exist in this file — the only memberId usages in the suite are Drizzle ORM column keys (soft-delete-with-members.test.ts:193) and zod input fields (cancel-scheduled-plan-change-tenant-isolation.test.ts), not the plans cross-tenant probe. Referenced production line change-plan.ts:244 also does not exist (no src/modules/plans/**/change-plan.ts). No code or test action needed for plans.
- [members] Portal profile GET exposes company tax_id and turnover_thb to portal member sessions — `src/app/api/portal/profile/route.ts:21`
  - Stale/incorrect. The local serialiseMember() (:21-54) is a field WHITELIST, not a spread — its parameter type does not even declare taxId/turnoverThb, and the returned object (:37-53) explicitly lists only company_name/country/website/description/plan_id/etc. The GET response at :120-125 spreads serialiseMember(...) (the picked object), NOT the raw member. Verified by string scan: zero occurrences of `tax_id`/`taxId`/`turnover`/`...member` in the file; `git log -S turnover_thb` on this file returns nothing (the column was never present). The finding's premise ('field present on member object is forwarded') is wrong for a pick-based serializer.
- [members] findLastPlanChangedAt uses raw sql template that bypasses runInTenant RLS guard on audit_log — `src/modules/members/infrastructure/db/drizzle-member-repo.ts:944`
  - Stale/self-contradicting. The title's premise is false: the query IS inside `runInTenant(ctx, async (tx) => tx.execute(sql...))` (:949) which sets app.current_tenant, AND adds explicit defence-in-depth `tenant_id = ${ctx.slug}` (:953). All three predicates (tenant_id :953, event_type :954, payload->>'member_id' :955) are present, and the audit payload is written as snake_case `member_id` (change-plan.ts:244, bulk-action.ts:293) — so the query is correct. Usable indexes exist: 0009 audit_log_member_id_idx on (payload->>'member_id') and 0115 partial (tenant_id, timestamp) WHERE member_plan_changed. The finding's own body concludes 'No code change required here'; the only residual is an optional doc comment, not a defect. (Aside, out of slice scope: migration 0078's partial index keys on camelCase `(payload->>'memberId')`, which never matches this snake_case query and is effectively dead for it — a separate observation, not what this finding asserts.)
- [broadcasts] No N+1 in segment recipient resolution -- batch suppression lookup confirmed — `src/modules/broadcasts/application/use-cases/resolve-segment-recipients.ts:114-128`
  - Not a defect -- this is a verification note whose own recommendation is 'No action required.' Confirmed current code: suppression filter calls marketingUnsubscribes.lookupBatch once (line 117, single inArray query per drizzle-marketing-unsubscribes-repo.ts), no per-recipient loop. Code is correct; nothing to fix. Marked stale-invalid because it describes a non-issue, not an unaddressed defect.
- [broadcasts] Tick-memoized members bridge correctly amortizes getMembersBySegment across cron tick — `src/modules/broadcasts/infrastructure/tick-memoized-members-bridge.ts:33-64`
  - Not a defect -- verification note, recommendation is 'No action required.' Confirmed: makeTickMemoizedMembersBridge wraps getMembersBySegment with a Map keyed by tenant.slug + segmentType + sorted params (lines 36-62), and the cron route wires it (dispatch-scheduled/route.ts:184-187) so multiple all_members/tier broadcasts in a tick share one Neon RTT. Working as intended; nothing to fix.

### duplicate (2)
- [payments] FR-021 funnel-dropoff metric inviteToPaymentFunnelStep defined but never emitted — `src/lib/metrics.ts:998`
  - DUPLICATE of 'member_invite_to_payment_funnel_dropoff counter defined but never called' (kept). Identical root cause, identical evidence (src/lib/metrics.ts:998-1012, zero emit sites), identical six funnel steps (invite_sent…payment_succeeded), and identical recommendation (wire the currently-instrumentable checkpoints, defer the email-open step). Only the framing differs — that finding tags it 'observability', this one tags it 'scope'/'FR-021'. Triaged once under the kept finding (wave 3, post-MVP F5.1).
- [presentation] ActivityFeedRefresh live region announced at 50ms — may miss NVDA's refresh cycle — `src/components/dashboard/activity-feed-refresh.tsx:31`
  - Duplicate of finding [10] (same file:line, activity-feed-refresh.tsx:31, the `setTimeout(..., 50)`). This finding's narrow ask (bump 50ms→100ms for NVDA reliability) is subsumed by [10]'s correct fix: announce on the `isPending` true→false transition via useEffect. Keying the announcement to the actual server round-trip completion eliminates the fixed-delay timing concern entirely, so the 50ms threshold becomes moot. Keeping [10] as canonical.

